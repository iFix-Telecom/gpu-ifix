# Binaries and build artifacts
bin/
*.exe
*.out
*.test

# Go cache / coverage
coverage.out
coverage.html

# Python cache
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# Weights and large binary fixtures — never committed
pod/weights/*.gguf
pod/weights/*.bin
pod/weights/*.pt
pod/weights/*.safetensors
pod/smoke/fixtures/*.wav
pod/smoke/fixtures/*.mp3

# Phase 8 INT-02 deliberately commits ONE short real WhatsApp speech clip —
# the transcription-quality gate needs real speech, not synthetic noise.
# Allow-rule so the committed fixture + baseline are always tracked.
!scripts/integration-smoke/fixtures/whatsapp-sample.ogg
!scripts/integration-smoke/fixtures/whatsapp-sample.baseline.json

# Smoke reports (archived to .planning/, not repo root)
smoke-report.json
smoke-report-*.json

# Local env
.env
.env.local
.env.portainer
.env.portainer.*
!.env.portainer.example

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Docker
*.Zone.Identifier

# Phase 11: load-test fixtures (sanitized audit_log JSONL — local-only, gitignored)
.planning/load-test-fixtures/*.jsonl

# Phase 11: LGPD signed letters (binary PDFs — gitignored; templates in same dir ARE committed)
.planning/legal/*.pdf

# Local agent + scratch (added 2026-05-26 for Phase 10 cut-release porcelain gate)
.claude/
ConverseAI_GPU_Stack_Guide.*
gatewayctl
pod/health-bridge/health-bridge
