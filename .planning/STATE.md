---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: executing
last_updated: "2026-05-27T09:42:41.455Z"
progress:
  total_phases: 4
  completed_phases: 3
  total_plans: 18
  completed_plans: 18
  percent: 75
---

# STATE: ifix-ai-gateway

> Project memory. Single source of truth for "where am I now?"
> Updated on phase/plan transitions.

## Project Reference

- **Project:** ifix-ai-gateway
- **Core Value:** Nenhuma aplicação da Ifix sente quando a GPU cai. Failover invisível.
- **Current Milestone:** v1 — Ship the first working gateway with pod + auth + failover + auto-provisioning + 6 app integrations
- **Granularity:** fine (10 phases)
- **Mode:** yolo

## Current Position

Phase: 10 (prod-deploy-ai-gateway) — EXECUTING
Plan: 1 of 6
Next autonomous-eligible work: Phase 06.7 — discuss-phase first (4 open questions in ROADMAP: embed CPU host, TTS server shim vs community wrapper, voice-cloning workflow, tts tier-0 role). Engine decided: Kani-TTS-2-pt (Apache 2.0, PT-BR, 3 GB VRAM, zero-shot voice clone).

- **Phases 1–5:** COMPLETE on disk (all autonomous plans + VERIFICATION). Each carries a `human_needed` / `passed_partial` live-UAT deferral — the standard pattern when the dev stack is not yet deployed:
  - Phase 1: smoke.yml Vast.ai HUMAN-UAT pending
  - Phase 2: live deploy UAT pending (`02-VERIFICATION.md` human_needed); 02-09 cold-storage export is OPTIONAL — deferred to Phase 7/10 per Codex scope-creep ruling (GW-10 closed by 02-02)
  - Phase 3: SC-1 live failover UAT pending (`03-VERIFICATION.md` human_needed)
  - Phase 4: SC-1/SC-2/SC-4 live UAT deferred pending ai-gateway-dev stack deploy (`04-VERIFICATION.md` human_needed)
  - Phase 5: SC-4 + SC-5 deferred (`05-VERIFICATION.md` passed_partial)
- **Phase 6 (COMPLETE 2026-05-17):** Emergency-Pod Template Refactor (SEED-001). All 5 waves shipped on develop. PR1 (Strategy B Locked) + PR2 (cleanup): 06-01..06-07 all GREEN. Live UAT lifecycle 39 (Vast 4090 Spain ES, $0.43/h, 25min) validated end-to-end — `/v1/chat/completions` HTTP 200, Qwen3 thinking + reasoning_content, system_fingerprint=b9128-856c3adac, prompt 278 tok/s + predict 48 tok/s. Hot-fixes carried during UAT: c75bf6b (entrypoint→onstart in payload — Vast API has no entrypoint field, vast-cli coerces at api/instances.py:85), 4896004 (drop apt-get install curl debconf hang; add optional POD_DEBUG_SSH_PUBLIC_KEY inline sshd for operator realtime debug + -p 22:22 docker port mapping), 19a66a3 (rename env generic). Cleanup PR2 (064702e) deleted pod/Dockerfile + emerg-bootstrap.sh + build-pod.yml (-404 lines). **Known gap:** SC-2 cold-start P90 ≤6min NOT met (actual 20m 58s — 16.74 GB WAN download Hetzner DE → Spain ES @ 14 MB/s dominant); arch follow-ups (Vast volumes + host pin, image pre-bake, geo-filter offers) tracked in 06-06-SUMMARY.md, not Phase 6 blocker. Phase 6.5 HUMAN-UAT now unblocked.
- **Phase 6.6 (2026-05-18, PARTIAL):** Primary-Pod Refactor — Strategy B Full-Stack Upstream Images. 11/13 plans GREEN on develop (02 vastutil, 03 config +24 PRIMARY_*, 04 custom Dockerfile+supervisord+GHA, 05 schedule+FSM, 06a reconciler core, 06b cross-pkg adapters, 07 migration 0023+sqlc+redisx, 08 main.go wire+emerg subscribe Pitfall #11, 09 gatewayctl primary CLI, 10 9 integration tests, 11 docs partial). Plan 11 Task 3 Live UAT BLOCKED. Plan 12 cleanup BLOCKED until UAT GREEN. CI pipeline GREEN (unit + integration + build-gateway + build-primary-pod).
  - **UAT 2026-05-18 — 8 attempts FAILED, $1+ Vast spend, no Scenario 1 PASS:** custom image `ghcr.io/ifixtelecom/converseai-primary-pod:develop` consistently exits within 30-60s of image extract on Vast hosts (Norway 867Mbps, Spain ES, multiple). Vast marks `actual_status=exited`, gateway correctly closes lifecycle via 3-strike confirmation. LOCAL docker run on vps-ifix-vm with same args + image WORKS (bash runs onstart, supervisord starts, 4 children spawn — they exit because vps-ifix-vm lacks GPU, expected). **Bug fundamental incompatibility custom-image × Vast runtype=args** — upstream `ghcr.io/ggml-org/llama.cpp:server-cuda-b9191` directly succeeds with same Vast args (lifecycle 11 → 5 ports up briefly). Root cause not isolated; needs deeper investigation (tcpdump/proxy of Vast docker run command).
  - **5 UAT-driven patches landed (all committed develop):**
    1. `fix(06.6-08)` a73a1f2 — emerg subscribe Pitfall #11 handler called wrong function (was `cancelActiveLifecycle`, must be `destroyAndCloseLifecycle + enterCooldown`). REAL production bug surfaced by integration test.
    2. `fix(06.6-10)` 0f559b1 — integration test harness `freshSchema` missing TRUNCATE for primary_lifecycles caused 3 test cross-contamination failures in CI.
    3. `fix(06.6-06a)` 22fcdbb — TestEvaluateAsleep_* CI race vs spawnProvisioning goroutine. fakeVast.SearchOffers returns error instantly; goroutine resets FSM before require.Equal asserts. Fix: block via channel until t.Cleanup. Same fix for TestEvaluateAsleep_AdvancesAfterCooldownElapses.
    4. `fix(06.6-06a)` c488d62 — `IsTerminal` requires 3 consecutive observations (~15s) before closing lifecycle. Lifecycle 4 captured false-positive close 12s before 4 endpoints were reachable.
    5. `fix(06.6)` e37fa60 — aria2c retries 10→50 + retry-wait 15s + SearchFilter `geolocation: EU` (DE/FR/NL/GB/ES/IT/PT/PL/AT/CH/BE/FI/SE/NO/DK). MinIO is Hetzner DE; pre-fix Vast picked US/Asia hosts that couldn't sustain 21GB transfer in retry budget.
    6. `fix(06.6-04)` f96d786 + ca514f9 — Dockerfile pivoted to hybrid Strategy B (drop multi-stage speaches/infinity COPY because /app missing in speaches image + Python venvs not relocatable; pip install Speaches/Infinity in final stage instead). Then explicit ENTRYPOINT ["/bin/bash"] override (empty array failed Vast attempt 6, llama-server inherited entrypoint failed attempts 3-5).
  - **Deployed state on vps-ifix-vm:** ai-gateway-dev container running with PRIMARY_TEMPLATE_IMAGE=ghcr.io/ifixtelecom/converseai-primary-pod:develop, 24 PRIMARY_* env vars present, FSM idle (asleep + DISABLED soak gate), reconciler subscribed to gw:primary:events. 13 primary_lifecycles DB rows from failed attempts. Stack file /opt/ai-gateway-dev/docker-compose.yml diverges from repo (operator-edited locally for env passthrough); needs reconciliation in follow-up commit. Stack also added PRIMARY_VAST_PRICE_CAP_DPH=0.80 + missing PRIMARY_* mappings.
  - **Session 2 (2026-05-18 late, BREAKTHROUGH):** /gsd-debug identified candidate root cause = HEALTHCHECK inherited from llama.cpp base targeting :8080 (our supervisord services bind :8000/:8001/:8002/:9400 — never :8080). Applied `HEALTHCHECK NONE` (commit 9c47b5a) + onstart trace markers + sshd-first (commit e309756). **Attempt 9** still showed actual=exited despite HEALTHCHECK NONE — diagnosis incomplete. **Attempt 10 (Denmark host 85.218.235.6, 1.7Gbps inet)** **WAS THE FIRST SUCCESSFUL POD**: container stayed alive 30+ min, sshd accessible (port 22 mapped), aria2c downloaded all 3 weights, tar extracts succeeded, supervisord ran 4 children, **llama-server FULLY FUNCTIONAL** (Qwen3.6 chat completions with reasoning_content + system_fingerprint=b9191-4f13cb742, 95 tok/s prompt + 46 tok/s predict). The trace-marker + sshd-first changes (commit e309756) made the pod survive — earlier attempts 1-9 may have been dying because bash exited silently before sshd could install. STT (Speaches) + Embed (Infinity) crash-looped under supervisord with import errors: `cannot import name 'NemoConformerTdt' from onnx_asr.models` (speaches 0.9.0rc3 vs onnx-asr ≥0.10 breaking change) and `ModuleNotFoundError: optimum.bettertransformer` (infinity-emb[all] doesn't pull bettertransformer extra). Pod destroyed via gatewayctl force-down + manual API DELETE (~$0.50 spend for attempt 10). HOWEVER: orphan pod 37028480 from attempt 9 had `BestEffortDestroy` fail with HTTP 429 rate limit; orphan ran ~3h30 burning ~$2.17 until operator caught via Vast UI event log + manual curl DELETE. **Total UAT spend this session: ~$3.**
  - **Patches committed this session (develop tip ddb7f44):** 9c47b5a (HEALTHCHECK NONE), e309756 (onstart trace markers + sshd-first + test relax), ddb7f44 (pip pin onnx-asr<0.10 + optimum[bettertransformer] for STT/Embed boot).
  - **Session 2026-05-19 (UAT 11→14, develop tip af81836):** 4 attempts iterating Dockerfile + supervisord fixes against the rebuilt primary-pod image. Total Vast spend this session ~\$0.63 (UAT 11 \$0.18 + 12 \$0.12 + 13 \$0.15 + 14 \$0.18). Zero orphans across all 4 attempts — every `BestEffortDestroy` succeeded on first DELETE (the new 429 retry never had to fire, but the safety net is in place for the next round). Patches landed:
    - `26b62cf fix(06.6-vastutil): BestEffortDestroy 429 retry w/ expo backoff` — 5 attempts × 1s→2s→4s→8s sleeps inside the 30s shutdown budget + Sentry breadcrumb on rate-limit-exhausted. Covers the attempt-9 orphan (~3h30 / \$2.17) root cause.
    - `5985765 fix(06.6-tests): alwaysInPeakRule covers 23:00-23:59 UTC` — the 5 primary integration tests that "deterministically failed" on the 26b62cf CI runs at 23:05 UTC turned out to be a latent test-helper bug: UpHour=0, DownHour=23 with the simple-intra-day branch excludes 23:00-23:59 UTC. Switched to UpHour=DownHour=0 so IsInPeak's overnight-wrap branch covers all 24 hours. Same fix applied to both `gateway/internal/integration_test/primary_helpers_test.go` and `gateway/internal/primary/reconciler_test.go`.
    - `adbff1b fix(06.6-04): 3 pod boot bugs — optimum<2.0 + libdcgm + speaches stub` — UAT 11 caught 3 post-pip-pin crash-loops: (a) optimum 2.1.0 removed `optimum.bettertransformer` → pin optimum<2.0; (b) DCGM exporter binary needs libdcgm.so.4 family + /etc/dcgm-exporter csv configs from the distroless stage → COPY them + ldconfig; (c) speaches 0.9.0rc3 mounts StaticFiles at /v1/realtime against a relative `realtime-console/dist` path → pre-create stub + index.html, supervisord launches speaches with `directory=/opt/speaches-data`.
    - `5f78beb fix(06.6-04): UAT 12 round-2 — pin gradio<6 + transformers<4.49` — UAT 12 cleared the prior layer and exposed: (a) speaches uses Gradio 5.x ChatInterface(type=…), broken in Gradio 6.0 → pin gradio<6.0; (b) infinity-emb 0.0.77 still imports optimum.bettertransformer which gates on transformers<4.49 → pin transformers<4.49. **UAT 12 partial PASS on Scenario 2 direct-probe**: system_fingerprint=`b9191-4f13cb742`, finish_reason=`tool_calls`, args=`{"location":"São Paulo"}` — Wave 0 Decision 3 B1 GGUF-embedded Jinja end-to-end PROVED on Vast 4090.
    - `648c427 fix(06.6-04): UAT 13 round-3 — pin click<8.2 (typer 0.12 incompat)` — UAT 13 ran LLM/STT/DCGM healthy (3-of-4 endpoints HTTP 200) but Infinity still crash-looped: typer 0.12.5 incompatible with click 8.4.0 (TypeError "Secondary flag is not valid for non-boolean flag"). Pin click<8.2.
    - `af81836 fix(06.6-04): bake speaches model_aliases.json stub` — UAT 14 reached FSM=Ready and ran S3 against the live pod; speaches /v1/audio/transcriptions hit `FileNotFoundError: 'model_aliases.json'` on every request. In-place patched `{}` stub on the live pod made the validator accept the model field, but the model-discovery layer behind it is still empty (separate fix). This commit bakes the stub into the Dockerfile so future pods don't repeat.
  - **UAT 14 scenario scoreboard (this session):**
    - **S1 PASS** (4-of-4 endpoints HTTP 200 on Vast 4090 Spain ES 213.96.60.239, supervisord 4 children RUNNING — llama PID 1586 + speaches 1587 + infinity 1588 + dcgm 1589 — `tier-0 override activated` logs for llm/stt/embed @ 00:24:12 BRT, `primary_ready observed; emerg not active` breadcrumb @ 00:24:12 BRT).
    - **S2 PASS** (LLM tool-calling end-to-end: direct probe via :19642 → `{"finish_reason":"tool_calls","tool_calls":[{"function":{"name":"get_weather","arguments":"{\"location\":\"São Paulo\"}"}}]}`, system_fingerprint=`b9191-4f13cb742`, 279 prompt + 78 completion tokens). Gateway-path probe got HTTP 503 `Upstream proxy not registered` despite the override logs — see Tech Debt below.
    - **S3 PARTIAL** (speaches reachable but model_aliases.json missing → in-place patched; second-layer error is `Model 'Systran/faster-whisper-large-v3' is not installed locally`, and `GET /v1/models` returns `data: []` despite WHISPER__MODEL_DIR=/weights/whisper containing config.json + model.bin + tokenizer.json + preprocessor_config.json + vocabulary.json — Speaches's model registry is not picking up the on-disk faster-whisper layout. Investigate speaches config env vars / scan path in next session.
    - **S4 PASS** (Infinity /embeddings: 2 inputs → 2 vectors × 1024 dims, model=`weights/bge-m3`, OpenAPI confirms `/embeddings` not `/v1/embeddings`. Stored as session evidence: first vector head `[-0.0146, 0.0110, -0.0314, -0.0112, -0.0096]`).
    - **S5 PARTIAL** (force-down → FSM=draining + drain_started_at populated + RestoreTier0 fired, BUT no subsequent `primary drain complete; transitioning to Destroying` log because `runScheduleLoop` skips `evaluateTick`/`evaluateDraining` when PRIMARY_POD_SCHEDULE_DISABLED=true. Manual Vast destroy + container restart + Redis HDEL gw:primary:state reset FSM to asleep for cleanup. See Tech Debt — operator force-down should drive state advancement even under DISABLED).
    - **S6 SKIPPED** (Pitfall #11 emerg coexist requires a second concurrent Vast spend for the emerg pod — deferred to a closeout UAT round dedicated to S3+S5+S6 only).
  - **New tech debt surfaced this session:**
    1. `runScheduleLoop` early-returns on PRIMARY_POD_SCHEDULE_DISABLED=true and never calls `evaluateTick`, so `evaluateDraining`/`evaluateDestroying` never advance under operator force-down. Fix: keep DISABLED gating the schedule-window logic but still run state-advancement (Draining/Destroying are operator-initiated; the soak-gate should not freeze drain).
    2. Gateway boot does not rewrite `gw:primary:state` Redis hash mirror. After container restart with in-memory FSM=asleep, the prior pre-restart state (e.g., draining) lingers in the mirror — `gatewayctl primary state` returns the stale snapshot. Fix: write fresh `state=asleep, entered_at=now` on reconciler init when boot recovery completes with no live lifecycle. Workaround used this session: `redis-cli DEL gw:primary:state`.
    3. Speaches model registry empty despite WHISPER__MODEL_DIR being set + faster-whisper layout present on disk. Speaches config dump showed `WhisperConfig(inference_device='auto', device_index=0, ...)` with no `model_dir` field — env var name may be different from what supervisord sets. Investigate next session.
    4. Gateway `chat_completions` proxy returns `503 upstream_unavailable: Upstream proxy not registered` even after `loader.OverrideTier0("llm", http://213.96.60.239:19642)` succeeded. Probe loop's `last_probe_status=failed` for `local-llm` (it still resolves UPSTREAM_LLM_URL env var, not the override). The dispatcher needs to honor the runtime override when routing OR the probe loop needs to read the override URL. Direct-probe to the pod IP works fine — this is a gateway routing-wiring gap, not a pod issue.
    5. S3 baseline: `gatewayctl key list` doesn't exist — operator must create a fresh test key via `gatewayctl key create -tenant converseai` each UAT, and there's no inspection/revocation listing. Low priority.
  - **Container redeploy gotcha:** Portainer webhook fired on the `26b62cf` build-gateway push (CI log shows `Portainer dev webhook triggered`) but the local image on vps-ifix-vm stayed at the pre-fix digest (3425aaf6a → expected dd49981586). Manual `docker pull` + `docker compose -f /opt/ai-gateway-dev/docker-compose.yml up -d gateway` recreated the container with the new image. Worth checking the Portainer stack config (pull policy / "always pull image" toggle) before the next CI cycle.
  - **Session 2026-05-19 (UAT 15 combined, develop tip 9720097):** Single Vast attempt on Norway 4090 (host 81.166.173.12, offer 36114274, $0.5362/h, 36min lifecycle wall, **spend $0.32**). Pre-UAT patches landed: `9720097 fix(06.6-06a): drain advances under DISABLED + boot resets state mirror` — closes tech debts #1 (runScheduleLoop blanket DISABLED gate moved to per-evaluator; evaluateReady gains DISABLED gate; evaluateDraining/evaluateDestroying advance under DISABLED) + #2 (Start() writes fresh `state=asleep, entered_at=now` mirror when recovery ends at StateAsleep). +3 regression tests in primary/reconciler_test.go (TestEvaluateReady_NoopWhenDisabled, TestEvaluateTick_AdvancesDrainingUnderDisabled, TestStart_BootResetsPrimaryStateMirror). CI build-gateway GREEN after rerun (proxy/chat_test.go:168 TestChatProxy_SSEStreamingFlushesPerChunk flaked on GHA; local PASS 3/3; not blocking).
  - **UAT 15 scenario scoreboard (this session):**
    - **S1 PASS** — 4-of-4 endpoints HTTP 200 on Vast 4090 Norway 81.166.173.12 (llama :11623 + speaches :11674 + infinity :11640 + dcgm :11693).
    - **S2 PASS** — LLM bench solo: prompt 162.8 tok/s + predict 47.1 tok/s, system_fingerprint `b9191-4f13cb742`. Concurrent N=4: aggregate **80.8 tok/s** (5.94s wall, llama parallel_slots=2 default → 1.7× scaling at N=4, batch-2 effective).
    - **GPU benchmark (NEW this session, was deferred from UAT 14):** RTX 4090 24 GB. **Idle** (DCGM + nvidia-smi): util 0%, VRAM 20215 MiB used (3 models loaded, 82%), temp 45°C, power 15.4 W, SM 210 MHz / mem 405 MHz. **Under load** (2 concurrent /v1/chat/completions 300-word essays): util **98%**, VRAM 20239 MiB (KV cache fits), temp 55°C, power **264 W** (~83% of 320W TDP cap), SM 2190 MHz (idle→full P0), mem 10251 MHz (idle→full). Driver 580.142.
    - **S3 PARTIAL** — **Root cause identified, two layers:** (1) Speaches 0.9.0rc3 uses HuggingFace cache layout, NOT custom `WHISPER__MODEL_DIR` env (visible in `/api/v1/config` dump: `WhisperConfig` has no `model_dir` field). MinIO `/weights/whisper` (faster-whisper layout) is IGNORED. Workaround: POST `/v1/models/Systran/faster-whisper-large-v3` triggers HF fetch into `/root/.cache/huggingface/hub/` (~2.9 GB pull, 3-4 min on EU host). After install, `/v1/models` lists the model correctly. (2) Transcription POST `/v1/audio/transcriptions` still fails with `Internal Server Error` — `onnxruntime.capi.onnxruntime_pybind11_state.NoSuchFile: silero_encoder_v5.onnx` (faster-whisper expects v5 encoder+decoder split; installed image ships only `silero_vad_v6.onnx`). Vendor-asset version mismatch — needs separate Dockerfile fix (downgrade faster-whisper pin OR pre-bake silero v5 asset).
    - **S4** — not retested this session (PASSED on UAT 14, no changes since).
    - **S5 PASS (THE KEY ASSERTION OF THIS SESSION)** — `gatewayctl primary force-down --reason uat15_s5_drain_under_disabled` at 07:18:45 BRT → log chain in **3 seconds**: `primary force-down: initiating drain by operator request` @ 07:18:45.616 → `tier-0 override cleared (cutback)` × 3 roles @ 07:18:45.892 → **`primary drain complete; transitioning to Destroying`** @ 07:18:46.156 (inflight=0, elapsed=0s, grace=300s) → BestEffortDestroy → LC 20 closed with shutdown=`destroyed` @ 10:18:48 UTC. Vast instance count after: 0. **Pre-fix (UAT 14): this chain froze at Draining forever under PRIMARY_POD_SCHEDULE_DISABLED=true** (`evaluateDraining` never called because `runScheduleLoop` early-returned). Fix #1 in commit 9720097 verified live end-to-end.
    - **S6 DEFERRED** — Pitfall #11 emerg coexist requires concurrent primary+emerg pod spend (~$1 + 60min cold-start). Same deferral as UAT 14; needs dedicated round.
  - **Tech debt closed this session:**
    1. ~~`runScheduleLoop` blanket DISABLED gate freezes Draining/Destroying~~ **RESOLVED 2026-05-19 via 9720097.** Per-evaluator gate; +TestEvaluateTick_AdvancesDrainingUnderDisabled regression. Verified live UAT 15 S5.
    2. ~~Gateway boot does not rewrite `gw:primary:state` Redis mirror~~ **RESOLVED 2026-05-19 via 9720097.** Start() writes fresh mirror on no-resume recovery. Verified live: pre-pull mirror empty → post-restart `state=asleep entered_at=<now>`.
  - **Tech debt remaining (Phase 6.6):**
    3. (was #3) Speaches model registry uses HF cache layout, not MODEL_DIR env. **Workaround**: POST `/v1/models/{id}` at pod boot via supervisord post-start hook OR pre-bake HF cache into Dockerfile (2.9 GB layer). **Real fix**: silero_encoder_v5.onnx asset needs vendoring or faster-whisper version pin downgrade to match speaches 0.9.0rc3 expectations.
    4. (was #4) Gateway `chat_completions` proxy returns 503 `Upstream proxy not registered` despite `loader.OverrideTier0("llm",...)` succeeding. Probe loop reads UPSTREAM_LLM_URL env, ignores runtime override. Dispatcher routing-wiring gap. NOT verified in UAT 15 — direct-probe to pod IP used throughout.
    5. (was #5) `gatewayctl key list` missing — low priority.
  - **Next session (UAT 16 deferred):** (a) Dockerfile fix S3 silero v5 vendoring + speaches HF cache pre-bake OR boot-time POST `/v1/models/{id}` automation; (b) S6 emerg coexist dedicated round; (c) fix tech debt #4 (chat_completions proxy override wiring); (d) write 06.6-11-SUMMARY.md + 06.6-VERIFICATION.md when S3+S6 PASS; (e) Plan 12 cleanup; (f) move to Phase 07 (Observability).

  - **Session 2026-05-19 (UAT 16 silero vendor verify, develop tip 73cf914):** 2 Vast attempts (Sweden 4090 host 34031 $0.3964/h × ~25min each = **spend $0.34**). Attempt 1 with `3d3e8dd fix(06.6-04): UAT 15 S3 — pin faster-whisper<1.1` crashed speaches at startup with `ImportError: cannot import name 'BatchedInferencePipeline' from 'faster_whisper'` — that symbol was added in faster-whisper 1.1.0, so the pin downgraded to 1.0.x which lacks it. Force-down + abort. Attempt 2 with `73cf914 fix(06.6-04): UAT 16 S3 — vendor silero v5 ONNX from upstream image` removed the pin and added a multi-stage build stage `speaches-assets` based on `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3`, with two COPY directives that vendor `silero_encoder_v5.onnx` (713 KB) + `silero_decoder_v5.onnx` (533 KB) into our `/opt/speaches-venv/lib/python3.12/site-packages/faster_whisper/assets/`. Discovery: upstream speaches image runs faster-whisper **1.1.1** (BatchedInferencePipeline present) but ships ONLY the v5 split files in the assets dir — they vendor it manually. The Silero VAD repo no longer publishes v5 files; their `src/silero_vad/data/` now contains only `silero_vad.onnx` (v4) + `silero_vad_v6.onnx` + 16k variants.
  - **UAT 16 scenario scoreboard:**
    - **S3 silero v5 vendor VERIFIED** — POST `/v1/models/Systran/faster-whisper-large-v3` registers correctly; `/v1/models` now lists **both** `Systran/faster-whisper-large-v3` **AND** `silero_vad_v5` (silero V5 module recognised — pre-fix `silero_vad_v5` was missing entirely). POST `/v1/audio/transcriptions` no longer hits `NoSuchFile: silero_encoder_v5.onnx`. New error path: `RuntimeError: CUDA failed with error out of memory` — VRAM budget exceeded when whisper-large-v3 tries to load on GPU after llama Qwen 27B Q4_K_M (~16 GB) + Infinity bge-m3 (~2 GB) already resident. 4090 has 24 GB total; transcription needs whisper-large-v3 GPU offload ~3-4 GB → exceeds 24 GB budget. **The silero asset fix itself is PASS**; the new tech debt is independent.
    - **S6 NOT TESTED THIS SESSION** — handler already verified via integration test commit 5985765 (`alwaysInPeakRule covers 23:00-23:59 UTC` + Pitfall #11 wired via `a73a1f2 fix(06.6-08): emerg subscribe Pitfall #11 handler called wrong function`). Live verification would require ~$0.60 (emerg pod $0.30 + primary re-cycle $0.30 to fire fresh primary_ready). Skipping; integration coverage holds.
  - **New tech debt surfaced UAT 16:**
    6. **VRAM budget exceeded on transcription** — Qwen Q4_K_M (~16 GB) + BGE-m3 (~2 GB) + KV cache + whisper-large-v3 GPU offload (~3-4 GB) > 24 GB. POST `/v1/audio/transcriptions` returns 500 with `CUDA failed with error out of memory`. Fix candidates: (a) force speaches `WHISPER__INFERENCE_DEVICE=cpu` (CPU transcription, slower but works — bge-m3 stays on GPU); (b) downgrade llama `--ctx-size 16384 → 8192` (saves ~2 GB KV cache); (c) move bge-m3 to CPU; (d) switch whisper to `faster-whisper-medium` (~1.5 GB). Decision deferred — STT throughput requirements not yet documented.
  - **Patches committed this session:** `3d3e8dd fix(06.6-04): UAT 15 S3 — pin faster-whisper<1.1 (silero v5 asset)` (REVERTED in 73cf914), `73cf914 fix(06.6-04): UAT 16 S3 — vendor silero v5 ONNX from upstream image` (CURRENT).
  - **Next session (deferred):** (a) VRAM budget fix (recommend CPU STT to start — STT latency rarely critical compared to LLM); (b) S6 dedicated round when STT path complete; (c) write 06.6-11-SUMMARY.md + 06.6-VERIFICATION.md; (d) Plan 12 cleanup; (e) Phase 07.

  - **Session 2026-05-19 (UAT 17 CPU STT + clip_timestamps pin, develop tip 50901d2):** 3 Vast attempts. **spend ~$0.65** total (attempt 1 Spain $0.16 / attempt 2 Norway-driver-too-old ~$0.28 / commit fixes between). Two patches landed:
    - `1a14ea4 fix(06.6-04): UAT 17 — force speaches STT to CPU to fit VRAM budget` — supervisord [program:speaches] gets `WHISPER__INFERENCE_DEVICE="cpu"`. Frees ~3-4 GB VRAM. Dropped unused `WHISPER__MODEL_DIR` / `SPEACHES__MODEL_DIR` (UAT 15 confirmed both vars are not in WhisperConfig).
    - `50901d2 fix(06.6-04): UAT 17 — pin faster-whisper==1.1.1` — speaches/executors/silero_vad_v5.merge_segments() emits dicts with keys `{start, end, segments}` where `segments` is a list. faster-whisper 1.2.x added strict clip_timestamps validation (iterates dict.items() + casts each value to int(v*rate)) → `TypeError: int() argument must be ... not list`. Upstream speaches Docker ships faster-whisper 1.1.1 which tolerates the extra key. Pin exactly. Earlier `<1.1` attempt (UAT 15 → 3d3e8dd reverted by 73cf914) broke speaches startup because 1.0.x lacks `BatchedInferencePipeline` import. The viable window is exactly 1.1.1.
  - **UAT 17 scenario scoreboard (this session):**
    - **S3 FULL PIPELINE VERIFIED via speaches logs (functional PASS)** — Norway pod (host 79.160.189.79, driver 565.57.01 too old for image's CUDA 12.6 → llama-server + speaches both fell back to CPU). Despite the unrelated GPU regression, ALL S3 layers are verified working:
      - `Model silero_vad_v5 loaded in 0.21s` + `VAD model inference took 0.0115s` ✓ vendor v5 ONNX assets resolve + execute.
      - `VAD filter removed 00:00.000 of audio` ✓ NO `TypeError: not list` crash — faster-whisper 1.1.1 pin works end-to-end with speaches merge_segments output.
      - `Model Systran/faster-whisper-large-v3 loaded in 36.78s` ✓ HF cache install path works.
      - `ctranslate2 warning: ... target device or backend do not support efficient float16 computation ... converted to use the float32` ✓ CPU mode active.
      - `Processing audio with duration 00:02.516` → `Detected language 'en' with probability 0.83` after 70s ✓ transcription pipeline executing; just too slow on CPU large-v3 (~28x realtime) so curl 180s timeout hit before the response body returned.
    - **S6 NOT TESTED** — Pitfall #11 handler integration-tested already (commit a73a1f2). Live verification needs another $0.60 + 60min cold-start.
  - **NEW tech debt UAT 17:**
    7. **STT model size — whisper-large-v3 + CPU too slow for usable latency.** Speaches log shows ~28x realtime (2.5s audio → 70s+ processing). Production users need <5s response on a 5-10s audio clip. Fix candidates: (a) downgrade to `Systran/faster-whisper-medium` (~1.5 GB GPU; fits even alongside Qwen 27B if KV cache trimmed); (b) downgrade to `Systran/faster-whisper-tiny` (~75 MB CPU; sub-realtime even on CPU); (c) push STT to a dedicated emergency-only path with cheaper model. Decision deferred to next session — production latency SLO not yet documented.
    8. **Vast host driver compatibility** — host 79.160.189.79 (driver 565.57.01) silently fell back llama+speaches to CPU because the llama.cpp:server-cuda-b9191 image needs CUDA 12.6+ (driver ≥ 570). 0.49 tok/s vs expected ~50 tok/s. Filter Vast offers by min driver version in `vast.SearchOffers` (`driver_version: gte_570.0`) to avoid the spend trap. Current geo-only filter (UAT 15 commit e37fa60) doesn't prevent old-driver hosts.
  - **Patches committed this session:** `1a14ea4 fix(06.6-04): UAT 17 — force speaches STT to CPU to fit VRAM budget` + `50901d2 fix(06.6-04): UAT 17 — pin faster-whisper==1.1.1`.
  - **Next session:** (a) pick STT model size policy (likely faster-whisper-medium GPU); (b) Vast driver-version filter (tech debt #8); (c) S6 dedicated round; (d) 06.6-11-SUMMARY.md + 06.6-VERIFICATION.md; (e) Plan 12 cleanup; (f) Phase 07.

  - **Session 2026-05-19 (UAT 18 closeout — 5090 + STT GPU + S3/S6 full PASS, develop tip d7786e6):** STT model policy decided in commit 9e1aca0 — bump 4090→5090 32 GB so whisper-large-v3 can stay on GPU (D-UAT17 reverted). PRIMARY_VAST_PRICE_CAP_DPH default raised 0.40→2.20 + MONTHLY_PRIMARY_BUDGET_BRL default raised 200→800; vps-ifix-vm `.env` set BUDGET=2400 + CAP=2.20 explicitly. Force-up returned `primary: no offers below cap` despite EU 5090 inventory existing → bisected Vast `cuda_max_good gte 12.6` filter regression specific to 5090 queries (RTX 4090 queries unaffected). Bump filter 12.6→12.8 in `d7786e6 fix(06.6-vast): UAT 18 — bump cuda_max_good 12.6→12.8 for RTX 5090 search`. After fix, single force-up succeeded: lifecycle 26, Vast 37094533, **5090 host 12140 Spain ES at $0.7657/h** (cheapest EU 5090 yet seen, below previously-assumed $2.00/h floor), driver 580.105.08, cuda 13.0, inet 7.4 Gbps. Cold-start 9 min (17:10:41 force-up → 17:19:54 Ready). DCGM: idle 20.5 GB used (3 models loaded), full-load post-whisper-install 24.7 GB used / 7.4 GB free / temp 53°C / power 33W idle. **S3 PASS**: espeak WAV 226KB → POST /v1/audio/transcriptions cold 17.5s (whisper-large-v3 GPU load) / warm 0.76s (well below 5s budget); accurate text. **S6 PASS via manual primary_ready republish technique**: emerg force-provision @17:22:30 (lifecycle 40, Vast 37095236, 4090 Iceland $0.37/h, 28m 42s cold-start to EmergencyActive @17:51:12 — longer than primary because Iceland host bandwidth lower) → Redis PUBLISH `gw:primary:events {type:primary_ready,lifecycle_id:26,replica_id:manual-uat18}` @17:51:50 (subscriber count=2 confirms emerg replica reception) → Pitfall #11 fires in <1s: cancelActiveLifecycle (D-C3) + destroyAndCloseLifecycle (Vast DELETE 37095236) + RestoreTier0 + FSM EmergencyActive→Cooldown reason=primary_took_over. Final state: 1 Vast instance (primary only). Force-down primary @17:52:56 → drain complete in 1s under DISABLED gate (S5 fix 9720097 re-verified) → asleep @17:55. UAT 18 total spend: primary 45min × $0.77 + emerg 29min × $0.37 ≈ **$0.75**. Cumulative Phase 6.6 UAT 1–18 spend: ~$6.
  - **Tech debt remaining (carries to Phase 07):**
    4. ~~(carry-over) Gateway `chat_completions` proxy returns 503 `Upstream proxy not registered` despite `loader.OverrideTier0` succeeding.~~ **RESOLVED 2026-05-22 via 30f90e7 + 12f7479 (shipped before debug, image `d6893211` deployed to vps-ifix-vm).** Root cause was dispatcher gap: `Loader.Resolve` with active override returns synthetic `Name="emergency_pod_<role>"`; pre-fix dispatcher `Proxies` map lacked that key → `dispatchTo` 503'd. Fix registers `NewDynamicOverrideProxy` for llm + stt (30f90e7) and tts (12f7479) in `cmd/gateway/main.go`. **Live UAT 2026-05-22** on lifecycle 98 (Alberta CA 5090 host @ $0.87/h — env was drifted; see env-drift note below): gateway-path `POST /v1/chat/completions` → HTTP 200, `system_fingerprint=b9191-4f13cb742` matching direct-probe, billing event enqueued via `upstream=emergency_pod_llm`, zero "Upstream proxy not registered" log entries. Regression test landed in `bc26cc8 test(gateway/proxy/dispatcher): regression — emergency_pod_<role> dispatch` (paired 503/200 paths in `dispatcher_test.go`) locks the contract against silent refactor.
    5. (carry-over) `gatewayctl key list` missing — low priority.
    9. **NEW UAT 18**: Pitfall #11 destroyAndCloseLifecycle's RestoreTier0 clears the emerg's tier-0 override slot, which transitively clears whatever primary had previously written into that slot. Primary reconciler doesn't auto-restore on Ready-tick. Workaround: cycle primary force-down + force-up to re-establish. Real fix: primary reconciler should re-call OverrideTier0 on each Ready-tick when override slot is empty (or republish primary_ready edge so the upstream module re-runs its activation path).
  - **Session 2026-05-22 (td #4 closure, develop tip `bc26cc8`):** Goal: regression test + live UAT to flip td #4 OPEN → CLOSED (per HANDOFF `.planning/seeds/HANDOFF-tech-debt-04.md`). Regression test landed (`bc26cc8`) — `dispatcher_test.go` paired tests `TestDispatcher_EmergencyOverride_503WhenProxyMissing` + `TestDispatcher_EmergencyOverride_200WhenProxyRegistered` pass in 14ms; full `./internal/proxy` suite green. **Env drift discovered + corrected:** `/opt/ai-gateway-dev/.env` on vps-ifix-vm lacked the Phase 06.8 final GPU shape (`PRIMARY_GPU_NAME`, `PRIMARY_NUM_GPUS`, `PRIMARY_VAST_MACHINE_ALLOWLIST`), falling back to compose defaults (5090 single, cap $1.00) instead of the validated 2×RTX 3090 shape (allowlist `43803,55158`, cap $0.60). Phase 06.8 deploy never updated vps-ifix-vm `.env`. Reverted env at session end: `PRIMARY_GPU_NAME="RTX 3090"`, `PRIMARY_NUM_GPUS=2`, `PRIMARY_VAST_MACHINE_ALLOWLIST=43803,55158`, `PRIMARY_VAST_PRICE_CAP_DPH=0.60`, `PRIMARY_VAST_MACHINE_BLOCKLIST=55942,45778`. Container recreated; `docker inspect` confirms env present. Backup at `/opt/ai-gateway-dev/.env.bak-td04-uat`. **UAT spend: ~$0.50** (8 attempts on 5090 inventory before lifecycle 98 succeeded — 3 CDI/disk/offline host failures + 2 race-skip "offer no longer available" + 2 host-fault terminals; loop script `/tmp/uat-retry-loop.sh` blocklists each failed `machine_id` and recreates container per cycle).
  - **Phase 6.6 STATUS: COMPLETE** — `.planning/phases/06.6-*/06.6-VERIFICATION.md` status=passed, 6/6 scenarios PASS, 13/13 plans GREEN. Plan 12 cleanup (scaffolding-only) still pending but not blocking Phase 07.

- **Phase 6.5 (ex-Phase 6):** 10/11 plans executed (06.5-01..06.5-10 GREEN + summaries — renomeados de 06-* em 2026-05-16). 06.5-11 is `autonomous: false` HUMAN-UAT — Tasks 1+2 done (06.5-HUMAN-UAT.md + docs/RUNBOOK-EMERGENCY-POD.md created, commit 2b539fc); Task 3 is a **blocking** human-verify checkpoint (6 LIVE Vast.ai UAT scenarios, ~R$10-15) — **UNBLOCKED 2026-05-17** by Phase 6 close (runtype=args end-to-end validated). No 06.5-11-SUMMARY.md, no 06.5-VERIFICATION.md yet.
  - **Integration tests (emerg suite): RESOLVED 2026-05-14.** First real CI run of `gateway/internal/integration_test/emerg_*` (Phase 6.5 deferred them to CI runtime — never executed before) failed 8 tests. 3 root causes found+fixed via `/gsd-debug`: (1) `freshSchema` missing `emergency_lifecycles` TRUNCATE → cross-test DB contamination (commit 9772d71); (2) stale Plan 06.5-05 force-provision/D-C5 test assertions vs reconciler evolved by 06.5-06+ (commit 355843b); (3) re-trigger oscillation race — `offer_race_lost` abort returned FSM straight to Healthy instead of Cooldown, `evaluateHealthy` re-fired the trigger every tick — fixed via new `ProvisionFailureCooldownSeconds` config (commit 85ba3da). All 22 emerg integration tests GREEN in CI run 25891568768 (build-gateway, develop). Debug sessions: `.planning/debug/emerg-integration-tests-ci.md` + `.planning/debug/emerg-bid-race-lost.md`.

- **Phases 7–10:** Not started (no phase directories) — Phase 07 unblocked 2026-05-19 by Phase 6.6 closeout.
- **Status:** Executing Phase 10

## Performance Metrics

- **Phases completed:** 6 / 11 (1–6 on disk; Phase 6.5 plans done, human UAT now UNBLOCKED by Phase 6 close)
- **Plans completed:** 59 / 61 (Phase 1: 9/9 · Phase 2: 8/9, 02-09 deferred · Phase 3: 8/8 · Phase 4: 9/9 · Phase 5: 8/8 · Phase 6: 7/7, all GREEN with SC-2 perf gap noted · Phase 6.5: 10/11, 06.5-11 human UAT now unblocked)
- **v1 requirements covered by executed plans:** POD-01..07, GW-01..10, TEN-01..09, RES-01..08, LSH-01..05, PRV-01..10 — 49/70 (remaining: OBS-01..08, INT-01..06, PRD-01..07 in Phases 7-10)

## Accumulated Context

### Key Decisions (from research + PROJECT)

- Gateway language: Go (chi v5 + stdlib `httputil.ReverseProxy` + slog)
- LLM server: `llama.cpp` native (not `llama-cpp-python`)
- STT server: `speaches-ai/speaches` (not custom FastAPI)
- Embedding server: `michaelf34/infinity` (not `sentence-transformers`)
- Saturation signal: composite (inflight + P95 + VRAM + hysteresis), not GPU util alone
- Primary GPU: Vast.ai RTX 4090 (cost) with emergency Vast.ai pod failover (not RunPod Secure)
- LLM model: Qwen 3.5 27B Q4_K_M GGUF, fixed both primary and OpenRouter fallback
- Deploy: Docker Compose + Portainer + webhook GitHub (standard Ifix)
- Postgres: shared DO cluster with dedicated `ai_gateway` schema
- Pre-baked pod Docker image (`ghcr.io/ifixtelecom/ifix-ai-pod`, slim ~2 GB) with weights downloaded from Ifix MinIO at boot via `onstart.sh` (revised by Phase 1 per D-01/D-02/D-04 — image stays small, weights versioned by key path with SHA-256 integrity D-05)
- Plan 02-08: ship `/gateway` + `/gatewayctl` in the same distroless image (27.7 MB total) — ops model is `docker exec ifix-ai-gateway /gatewayctl <cmd>` rather than a separate sidecar image
- Plan 02-08: boot-time migrations via `AI_GATEWAY_MIGRATE_ON_BOOT` env flag instead of a dedicated CI migration job; goose idempotency makes this safe across restarts
- Plan 02-08: GitHub Actions `paths:` filter on pull_request only (not push) — mirrors build-pod.yml to avoid silently skipping stable-release tag pushes when the tag commit itself doesn't touch gateway/**

### Open Todos (for upcoming phases)

- [ ] Phase 2 close: live deploy UAT — set GitHub Secrets `PORTAINER_WEBHOOK_URL_DEV_GATEWAY` + `PORTAINER_WEBHOOK_URL_PROD_GATEWAY`; create Portainer stack `ai-gateway-dev` (Repository + webhook → `gateway/docker-compose.yml` on `develop`); run the 10-step post-push checklist in `02-08-SUMMARY.md`
- [ ] Phase 6.5 close (BLOQUEADO por Phase 6 refactor): executar 06.5-HUMAN-UAT.md (6 LIVE Vast.ai scenarios, ~R$10-15) → fill sign-off → write 06.5-11-SUMMARY.md + 06.5-VERIFICATION.md. Pre-req: Phase 6 (template refactor SEED-001) precisa fix runtype=ssh bug antes — atual UAT impossível end-to-end.
- [ ] Phase 7: 02-09 cold-storage audit export (Parquet + MinIO + retention DROP) — re-evaluate when audit_log grows past ~60 days of production traffic
- [ ] Phase 7: Confirm Ifix WhatsApp provider (Evolution API / Z-API / Chatwoot / proprietary)
- [ ] Phase 7: Choose dashboard auth (Better Auth instance vs shared with ConverseAI vs SSO)
- [ ] Phase 9: Obtain LGPD review sign-off from Ifix legal before activating sensitive tenants
- [ ] Tech debt (deferred from Phase 6.5): `gateway/internal/auth` argon2id tests hang under `-race`; `gateway/internal/db/migrate_test.go:53` migration count hard-coded 18, now 19 — fix via `/gsd-quick`
- [ ] Tech debt (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 29): `audit flush failed: invalid input value for enum data_class: "" (SQLSTATE 22P02)` a cada FSM state-change. Root cause: `gateway/internal/emerg/fsm.go:331` chama `auditWriter.WriteStateChange("fsm_transition", audit.Event{...})` sem setar `DataClass`; zero-value "" viola enum `ai_gateway.data_class` (`normal|sensitive`) que é NOT NULL desde migration 0019. State-change audit rows perdidos em batches de 2; FSM e Sentry breadcrumb não afetados. Fix candidato A (simples): `WriteStateChange` em `gateway/internal/audit/writer.go:167` seta default `ev.DataClass = "normal"`. Fix candidato B (semanticamente correto): nova migration adiciona valor `system` ao enum + `WriteStateChange` usa `system`. Fix via `/gsd-quick` pós-UAT.
- [ ] Tech debt (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 29): reconciler `provisionLifecycle` não detecta `status_msg=Error: GPU error, unable to start instance.` retornado pelo Vast.ai quando host tem hardware fault na 4090. Sintoma observado: instance 36904592 (host 1647/machine 15489) ficou ~6min em `actual=created` + `cur=running`/`stopped`/`running` oscilando, com `status_msg="Error: GPU error..."` desde o início, mas reconciler só fazia health-poll e iria dar `health_timeout` em 30min (coldstart_budget=1800s). Fix: em `gateway/internal/emerg/lifecycle.go` (loop de health-poll) adicionar checagem paralela de `vast.GetInstance(id).status_msg` — se contém `"Error:"`, `"GPU error"`, ou `"unable to start"` → `DestroyInstance` + `closeLifecycle(reason="vast_gpu_error")` + novo `ErrVastGPUError` em `errors.go` + breadcrumb Sentry. Reduz failed-host detection de 30min pra ~10s. Workaround atual: operator vê erro na UI Vast.ai + `gatewayctl emerg force-destroy` manual. Fix via `/gsd-quick` pós-UAT.
- [x] ~~Tech debt CRÍTICO (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 31): `handleForceProvision` não trata FSM em estado `cooldown`.~~ **RESOLVIDO 2026-05-16 via quick 260516-rym.** Fix: precheck `FSM.State()` rejeita força-provision se estado in `{EmergencyProvisioning, EmergencyActive, Recovering}`; substitui 2x `Transition(from, to)` por `SetState(StateEmergencyProvisioning)` que CAS-loops até commit regardless of current state. 2 regression tests em `emerg_force_command_cooldown_test.go`. Ver `.planning/quick/260516-rym-fix-force-provision-cooldown-transition/SUMMARY.md`.
- [ ] Tech debt (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 32): leader-recovery não reseta FSM ao detectar lost lifecycle. Sintoma observado: container ai-gateway-dev recreated com FSM=emergency_provisioning (LC32 in-flight). Recovery em `gateway/internal/emerg/recovery.go` escaneou live lifecycles, detectou Vast.ai instance 36906461 "not found at Vast (lost)", fechou LC32 com `shutdown_reason=leader_recovery_lost`, MAS deixou FSM em emergency_provisioning sem activeLifecycle. `force-destroy` ficou no-op (handleForceDestroy retorna Warn quando activeLifecycle nil). Fix: em `resumeFSMFromEvents` (ou wrapper), quando recovery resulta em "instance not found at Vast (lost)" → fechar lifecycle + `FSM.SetState(StateHealthy, ..., "leader_recovery_lost_no_resume")`. Workaround atual: restart container ou apagar redis mirror manualmente. Fix via `/gsd-quick` pós-UAT.
- [ ] Tech debt (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 32): gateway boot não rewrite Redis mirror `gw:emerg:state`. Sintoma observado: após restart container com FSM in-memory iniciando em healthy, mirror Redis manteve snapshot anterior (`state=emergency_provisioning entered_at=1778974096`). `gatewayctl emerg state` lê mirror direto → mostra stale state mesmo com gateway healthy. UI/ops decisions baseadas em mirror stale podem dar resultado errado. Fix: em `cmd/gateway/main.go` boot path, após NewFSM + recovery completar, escrever mirror inicial via `EmergStateKey()` HSET com state atual + entered_at = time.Now(). Workaround atual: `redis-cli -n 0 DEL gw:emerg:state` manual. Fix via `/gsd-quick` pós-UAT.
- [ ] **Tech debt CRÍTICO (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 33): `Runtype="ssh"` em `gateway/internal/emerg/lifecycle.go:687` IGNORA `ENTRYPOINT/CMD` da Docker image.** Vast.ai com `runtype=ssh` roda apenas `sshd` como PID 1 do container — o `CMD ["/usr/local/bin/emerg-bootstrap.sh"]` do `pod/Dockerfile` nunca arranca. Por isso TODOS os lifecycles 29-33 ficaram travados: image baixou OK, Vast marca `actual_status=running`, MAS `emerg-bootstrap.sh` nunca rodou → Qwen nunca baixou → llama-server nunca subiu → porta 8000 sem listener → health-poll do gateway falha até timeout (1800s). Métricas do pod confirmaram pod idle (CPU 0%, mem 5MB, GPU 20°C). Comentário do código (linhas 682-685) afirma "Emergency pods run the image's baked-in CMD" — **errado pra runtype=ssh**. **Este bug explica por que Phase 6.5 UAT live nunca funcionou end-to-end ainda — todos os "health_timeout" anteriores na tabela emerg lifecycles compartilham essa root cause.** Decisão Pedro 2026-05-16: NÃO fix runtype, em vez disso refatorar pra usar templates Vast.ai Ubuntu+CUDA (ver SEED-001) — abordagem que simultaneamente resolve este bug + ganho 2-4min cold-start + iteração dev rápida. Workaround imediato impossível: nenhum lifecycle vai chegar emergency_active enquanto custom image + runtype=ssh combination existe. Próxima sessão: `/gsd-discuss-phase` baseado em SEED-001 → implementar via quick task ou nova phase.

### Blockers

- **Phase 6.5 cannot reach COMPLETE without operator action:** 06.5-11 Task 3 is a blocking human-verify checkpoint requiring real Vast.ai spend. Autonomous mode cannot satisfy it. Phases 7-10 do not hard-depend on Phase 6.5 *verification* (they depend on Phase 6.5 FSM states/code, which exist) — but Phase 7's goal explicitly visualizes Phase 6.5 FSM states, so plan Phase 7 with Phase 6.5 code as-built. **Phase 6.5 UAT now also blocked by Phase 6 (template refactor) — runtype=ssh bug must be fixed first.**

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260515-ayc | Fix STATE.md corruption (heading `## Current Position` duplicado injetado no meio do Phase 6.5 bullet) | 2026-05-15 | f44cf11 | [260515-ayc-fix-state-md-corruption-linha-40-42-tem-](./quick/260515-ayc-fix-state-md-corruption-linha-40-42-tem-/) |
| 260516-rym | Fix handleForceProvision não trata FSM em cooldown — pod Vast.ai órfão queimava $$ quando operator force-provision após falha. Precheck FSM.State() + SetState(EmergencyProvisioning) em vez de 2x Transition. +2 regression tests. | 2026-05-16 | 5aec0eb | [260516-rym-fix-force-provision-cooldown-transition](./quick/260516-rym-fix-force-provision-cooldown-transition/) |
| Phase 06.7 P02 | 10m | 2 tasks | 7 files |
| Phase 06.7 P03 | 25m | 2 tasks | 9 files |
| Phase 06.7 P04 | ~20m | 2 tasks | 8 files |
| Phase 06.7 P05 | 4 | 2 tasks | 4 files |

## Session Continuity

- **Last session:** 2026-05-27T09:42:41.444Z
- **Next session should:** Run the Phase 06.7 live HUMAN-UAT. Plan 06.7-09 Task 1 is DONE (commit `41039b4`: `docs/RUNBOOK-PRIMARY-POD-TTS.md` + `06.7-HUMAN-UAT.md` 6-scenario sheet + CLEANUP). **Task 2 is a BLOCKING human-verify checkpoint** — operator must run S1–S6 + cleanup on a live Vast 5090 (real GPU spend; autonomous mode cannot satisfy it), sign each PASS/FAIL, record spend. After all 6 + cleanup are signed: write `06.7-09-SUMMARY.md` + `06.7-VERIFICATION.md`, then `state advance-plan`. Any FAIL → `/gsd:plan-phase 06.7 --gaps`. Phase 06.7 Plan stays at 9 (06.7-09) — NOT advanced (plan incomplete until UAT signed).

---

*State created: 2026-04-17*
*State repaired against disk: 2026-05-14*

## Decisions

- [Phase 06.7]: Wave 0 test stubs are engine-agnostic on the Go side (assert tts ROLE plumbing — probe path, tier-0 map, route class, re-assert); engine-specific only in the pod pytest (Chatterbox 24kHz, zero-shot audio_prompt_path, no .pt)
- [Phase 06.7]: Every Wave 0 test stub carries an OWNER annotation + skip reason naming the downstream plan (03/05/07/08) that must unskip + implement it (Codex consensus action #3 — a skipped stub satisfies compile-time scaffolding but does not fail if implementation never happens)
- [Phase 06.7]: 06.7-03: tier-0 dynamic-override roster {llm,stt,embed}->{llm,stt,tts} — tts dynamic (D-11), embed static (D-03); added Loader.Tier0OverrideURL getter for Plan 08 re-assert (D-13)
- [Phase ?]: [Phase 06.7]: 06.7-04 voices.tenant_id = UUID FK->tenants(id) ON DELETE CASCADE, matching the verified ai_gateway schema convention (api_keys/usage_counters/billing_events) over the PATTERNS TEXT placeholder; no .pt column (Chatterbox zero-shot D-08)
