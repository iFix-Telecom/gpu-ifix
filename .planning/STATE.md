---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: executing
last_updated: "2026-07-31T14:40:26.792Z"
progress:
  total_phases: 22
  completed_phases: 15
  total_plans: 99
  completed_plans: 89
  percent: 68
---

# STATE: ifix-ai-gateway

> Project memory. Single source of truth for "where am I now?"
> Updated on phase/plan transitions.

## Project Reference

- **Project:** ifix-ai-gateway
- **Core Value:** Nenhuma aplicação da Ifix sente quando a GPU cai. Failover invisível.
- **Current Milestone:** v1 — Ship the first working gateway with pod + auth + failover + auto-provisioning + 6 app integrations
- **Granularity:** fine (10 phases)
- **Mode:** yolo

## Current Position

Phase: 22 (funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod) — EXECUTING
Plan: 1 of 7
Next work (phase 18): 18-04 (dashboard UI /tenants/gerenciar: listar + criar tenant + gerar key raw-1× + revogar-confirm + seletor data-class + nav; owner-edita/operator read-only; checkpoint human-verify E2E — TEN-UI-10/11). 18-03 DONE 2026-07-05: 3 owner-gated server actions createTenant/createTenantKey/revokeKey em admin-actions-core.ts (server-only) espelhando updatePodConfigCore 1:1 — requireOwner PRIMEIRA await (identidade só da sessão via wrappers, CR-01) → validação server-side (slug /^[a-z0-9][a-z0-9-]*$/ + name não-vazio + data_class∈{normal,sensitive}) ANTES do gateway → gatewayAdminPost (server-only X-Admin-Key, só CONSOME o body do 18-02, NÃO edita gateway-admin.ts) → EXATAMENTE 1 admin_audit_log/ação (tenant.create{slug,name} / key.create{tenant,data_class,key_prefix} / key.revoke{key_id}) → safeRevalidateTenants("/tenants/gerenciar"). createTenantKey RETORNA o raw {id,key_prefix,data_class,key} ao caller p/ exibição efêmera 1× mas NUNCA audita o raw. 3 wrappers thin "use server" em admin-actions.ts (requireOwner() sem args); requireOwner/writeAuditLog NÃO exportados (CR-02). 6 testes TEN-UI provam operator→0 gateway+0 audit, owner→1 POST+1 audit, raw key ausente do metadata, slug/data_class inválidos→0 gateway. vitest 19/19, tsc clean, zero deps novas. Commits 5f01fa5 + d45c9ba. TEN-UI-08/09 complete. --- 18-02 DONE 2026-07-05: dashboard server-side tenant layer. GATE PASSED — stack 40 (dashboard prod, Portainer endpoint 6/worker-vm) GATEWAY_BASE_URL=http://gateway:8080 = swarm alias 'gateway' of service ai-gateway-prod_gateway (consolidated worker-vm gateway, image ifix-ai-gateway@sha256:2b3e561c; dashboard+gateway share both overlay nets), NOT n8n-ia-vm/vps-ifix-vm. Generalized gateway-admin.ts: common gatewayAdminMutate<T>(method,path,body); gatewayAdminPost<T> returns gateway JSON verbatim (create-key {id,key_prefix,data_class,key}, never key_hash/key_lookup_hash), gatewayAdminPatch kept Promise<void>. Added TenantRow/TenantKeyRow (mirror 18-01 Go handlers field-for-field, last_used_at nullable) + fetchTenants/fetchTenantKeys via GET-only proxy + fetchTenantsServer() RSC (extracted shared proxyGetServer<T> helper — DRY dedup of fetchPodConfigServer). Leak-guard green (GATEWAY_ADMIN_KEY only in {route.ts, gateway-admin.ts}); vitest 16/16; tsc clean; zero new deps. Commits 618016a + 363b913. TEN-UI-06/07 complete.
Original next work: 19-06 (soak validation → unmask+repoint ops timers → decommission n8n-ia-vm + vps-ifix-vm gateway stacks). 19-05 DONE 2026-07-04 (continuation, checkpoint APPROVED "flip both"): INGRESS CUTOVER LIVE. Recorded cutover_ts=2026-07-04T19:22:52Z, then flipped BOTH edge Traefik file-provider service blocks on vps-ifix-vm (ai-gateway-prod.yml) → http://10.10.10.50:80 (ai-gateway-prod-upstream was :8080, ai-dashboard-prod-upstream was :3001, both on 10.10.10.20). grep "10.10.10.50:80"=2, residual 10.10.10.20 in URLs=0. Traefik v3.6 file-watcher hot-reloaded (no restart, no cert re-issue — DNS unchanged). Public ai-gateway.converse-ai.app/health→200 version=main-5553bd4 (worker-vm; was n8n-ia-vm), latency 0.074s < 0.098s baseline; ai-dashboard→307 reachable. Public smoke real prod keys: chat-ifix normal→200 openrouter/deepseek-v4-flash, embeddings captain-embed→200, telefonia sensitive→503 upstream_unavailable_for_sensitive_tenant (RES-08 auth-OK), bogus→401, prod admin ****613f /admin/metrics→200. 15-sample×60s monitoring window (19:25:39→19:41:45Z) HELD — every sample canary=200, edge5xx=0, gw401/403=0, gwDBerr=0 (dormant-pod 172.18.0.1:18000 tokenize WARN excluded), billing 2-8/2m; armed auto-rollback never tripped. Since cutover 42 live billing_events in bd_ai_gateway (chat=29, stt=12, embed=1 — real ~18-tenant prod incl voip STT metering correctly). Rollback target n8n-ia-vm ifix-ai-gateway Up 46h healthy (:8080/health=200), left UP for 19-06; DNS/Cloudflare untouched; ops timers still MASKED. Backup .bak-pre19-05 (diff-identical pre-flip snapshot) = one-line rollback: cp .bak-pre19-05 → ai-gateway-prod.yml. --- 19-04 DONE 2026-07-04 (continuation, checkpoint APPROVED w/ admin-purge adjustment): atomic migrate.sql COMMITted bd_ai_gateway_prod → bd_ai_gateway in ONE tx — pre-deleted 928 dev billing_events (NO-ACTION FK: partitioned parent + 3 partitions all confdeltype='a', single parent DELETE propagates), DELETEd 4 dev tenants (CASCADE dropped 22 api_keys + 10 usage_counters + voices), PURGED 5 dev admin_keys (approved adjustment — incl. 19-02-validation), then INSERT 18 prod tenants + 19 api_keys (in-tx byte-for-byte hash JOIN=19 on key_hash+key_lookup_hash) + 2 prod admin_keys + 31 current-period usage_counters. In-tx RAISE guards passed (tenants=18, api_keys=19, api_hash=19, admin_hash=2, admin_total=2); staging ai_gateway_migration.* DROPPED. Final: tenants=18, api_keys=19, admin_keys=2 (prod-ops-2026-05-26 ****613f active present), usage=31, billing=0 (prod 33k history stays archived in _prod). data_class copied VERBATIM. Live auth proofs vs worker-vm gateway (internal Traefik Host ai-gateway.converse-ai.app→10.10.10.50:80, pre-cutover): chat-ifix normal key→200 openrouter/deepseek + fresh billing row; prod admin ****613f /admin/metrics→200; telefonia+cobrancas sensitive keys→503 upstream_unavailable_for_sensitive_tenant (RES-08 auth-OK — sensitivity keys off api_keys.data_class NOT tenants.data_class); bogus key→401 (negative control proves the 503s are auth-PRESERVED). Root-600 backups retained ~/gw-migration-19/ (main .backup_main_path dump + source dump + affected-tables dump + migrate.sql); CSVs shredded post-COMMIT; ops timers gateway-price-sync.timer + prod-primary-report.timer left MASKED for 19-06. bd_ai_gateway_prod SOURCE read-only untouched. Cutover (19-05) now a pure ingress flip. --- 19-03 DONE 2026-07-04: dashboard + rerank consolidated onto worker-vm as UI-editable digest-pinned Portainer swarm stacks — stack 40 (ai-gateway-dashboard, ghcr digest @sha256:07b7d18, worker_intra+ai-gateway, Host(ai-dashboard.converse-ai.app):3001; two-DSN — DASHBOARD_DATABASE_URL kept at bd_ai_dashboard_prod login store [no-verify+search_path=public], AI_GATEWAY_PG_DSN→bd_ai_gateway; login-DB proven via clean 401 sign-in + owner row present) + stack 41 (ai-gateway-rerank, infinity 0.0.77 @sha256:11e8b3, bge-reranker-base, host port 7998 — gateway is NOT rerank-fronted, verified DIRECT /v1/rerank 200 ranked; consumer=agents RAG, only healthchecks in 48h → consolidate-regardless, drop deferred). model_aliases reconciled ADDITIVELY into bd_ai_gateway 10→19: 9 prod-only client-facing aliases all classified PORT (identical upstream sets both DBs) — captain-embed, gpt-5.4-mini(+2026-03-17 copilot), ia-kanban(×2), whisper-large-v3(×4 voip). Resolver picks up via 60s POLL (models/resolver.go, NO pg_notify for aliases). Real-call verified 200: chat gpt-5.4-mini-2026-03-17/gpt-5.4-mini/ia-kanban, embed captain-embed(1024 local bge-m3), STT whisper-large-v3 → "Hello, this is a transcription." Pitfall 6 (merged-traffic 404 drift) CLOSED. bd_ai_gateway_prod UNCHANGED (read-only, still 17 aliases); LIVE PROD gateway (n8n-ia-vm) untouched — Up 28h healthy, public /health 200. Verify key ifix_sk_****7rsc (superseded by 19-04). --- 19-02 DONE 2026-07-04: consolidated gateway + embed live on worker-vm as Portainer swarm stacks 38 (ai-gateway-prod: gateway digest-pinned ghcr.io/ifixtelecom/ifix-ai-gateway@sha256:382a0fc + fresh redis alias infra-redis-1) + 39 (ai-gateway-embed: bge-m3 CPU digest-pinned @sha256:11e8b3), endpoint 6, UI-editable, reading bd_ai_gateway (goose v31) + R2 + infra-redis-1 + local embed(embed:7997). Pre-cutover green baseline with DEV tenant via internal Traefik (Host route 10.10.10.50:80): chat 200 tier-1 openrouter-chat/deepseek (172.18.0.1:18000 dormant tier-1-only, GPU-less), embed 200 bge-m3 from gateway netns (~0.5s CPU), admin 200; billing_events 918→923 (route=chat + route=embed upstream=local-embed). worker-vm .env root-600 derived from prod secrets (NOT committed). LIVE PROD gateway (n8n-ia-vm) untouched throughout — Up 27h healthy, public /health 200. SCHEME A routing (zero Traefik mutation). Validation creds: dev tenant ifix_sk_****fyei + admin ifix_admin_****eb79 (superseded by 19-04 migration). --- v1 milestone close (separate track). Phase 16 CLOSED the sole v1 code blocker AND live-proved it. Producer `applyAudioEmbedUsage` wired into 7 STT/embed proxies + RequestAudioSecondsMiddleware mounted. Live-UAT on ai-gateway-dev (rev 3374999) found+fixed a path-rewrite bug (gemini-stt rewrites URL.Path → route misclassified 'chat' → metering dropped; fix commit 3374999 stamps billing route from inbound path pre-rewrite into ctx). Verified live: billing_events route=stt audio_seconds=5 + route=embed embeds_count=2; usage_counters audio_seconds=5 embeds_count=5 (quota source populated, was 0). Remaining for v1 "complete": (1) reconcile remaining stale REQUIREMENTS checkboxes (40 done 2026-06-27); (2) sign off 08/09 client UAT (INT-01..06 — operator + LGPD, process gap); (3) flip Phase 13 VERIFICATION status→passed; (4) backfill 06.5 VERIFICATION.md. Then re-run /gsd:audit-milestone → /gsd:complete-milestone v1. All 25 phases executed.

PROD PROMOTED 2026-06-29: main FF'd develop→72ffcb5 (pushed). Gateway :main rebuilt on n8n-ia-vm (manual recipe, context=repo root, -f gateway/Dockerfile) + recreated (--pull never). Prod healthy, leadership reacquired, primary pod UP (Mon 9-17 BRT). TEN-04 metering LIVE-PROVEN in PROD against REAL traffic: bd_ai_gateway_prod billing_events shows stt/gemini-stt audio_seconds=0.6/0.8/1.4 (real prod STT calls now metered via the fixed path-rewrite path; were 0/dropped) + embed/local-embed embeds_count=2 (smoke). Rollback tag ghcr.io/...:rollback-pre-ten04. NOTE: manual VM build bakes no git-rev label (version="dev") — confirm prod deploys via behavior/billing rows, not rev label. PROD dashboard NOT rebuilt (Phase 16 has no dashboard change).

Previously: Phases 13/14/15 all COMPLETE/passed (13 user-mgmt deployed live a1c9adb; 14 vram-adaptive-stt passed; 15 economia/histórico passed 17/17). Earlier: Phase 11.2 — COMPLETE passed_partial. 11-06 + 11-07 live UATs DEFERRED 2026-05-28T20:55Z — pre-flight Stage 1 gate fail: `bd_ai_gateway_prod` ~57 replayable rows over 7 days vs `[reviews LOW #4]` gate ≥1000 + 5 route classes. Re-attempt once natural traffic accumulates ≥1000 rows in a 1-hour window with chat + embed + STT + tool-call + stream coverage. See `.planning/phases/11-prod-hardening/11-06-EVIDENCE.md` pre-flight re-attempt section.

- **Phases 1–5:** COMPLETE on disk (all autonomous plans + VERIFICATION). Each carries a `human_needed` / `passed_partial` live-UAT deferral — the standard pattern when the dev stack is not yet deployed:
  - Phase 1: smoke.yml Vast.ai HUMAN-UAT pending
  - Phase 2: live deploy UAT pending (`02-VERIFICATION.md` human_needed); 02-09 cold-storage export is OPTIONAL — deferred to Phase 7/10 per Codex scope-creep ruling (GW-10 closed by 02-02)
  - Phase 3: SC-1 live failover UAT pending (`03-VERIFICATION.md` human_needed)
  - Phase 4: SC-1/SC-2/SC-4 live UAT deferred pending ai-gateway-dev stack deploy (`04-VERIFICATION.md` human_needed)
  - Phase 5: SC-4 + SC-5 deferred (`05-VERIFICATION.md` passed_partial)
- **Phase 6 (COMPLETE 2026-05-17):** Emergency-Pod Template Refactor (SEED-001). All 5 waves shipped on develop. PR1 (Strategy B Locked) + PR2 (cleanup): 06-01..06-07 all GREEN. Live UAT lifecycle 39 (Vast 4090 Spain ES, $0.43/h, 25min) validated end-to-end — `/v1/chat/completions` HTTP 200, Qwen3 thinking + reasoning_content, system_fingerprint=b9128-856c3adac, prompt 278 tok/s + predict 48 tok/s. Hot-fixes carried during UAT: c75bf6b (entrypoint→onstart in payload — Vast API has no entrypoint field, vast-cli coerces at api/instances.py:85), 4896004 (drop apt-get install curl debconf hang; add optional POD_DEBUG_SSH_PUBLIC_KEY inline sshd for operator realtime debug + -p 22:22 docker port mapping), 19a66a3 (rename env generic). Cleanup PR2 (064702e) deleted pod/Dockerfile + emerg-bootstrap.sh + build-pod.yml (-404 lines). **Known gap:** SC-2 cold-start P90 ≤6min NOT met (actual 20m 58s — 16.74 GB WAN download Hetzner DE → Spain ES @ 14 MB/s dominant); arch follow-ups (Vast volumes + host pin, image pre-bake, geo-filter offers) tracked in 06-06-SUMMARY.md, not Phase 6 blocker. Phase 6.5 HUMAN-UAT now unblocked.
- **Phase 6.6 (2026-05-18, PARTIAL):** Primary-Pod Refactor — Strategy B Full-Stack Upstream Images. 11/13 plans GREEN on develop (02 vastutil, 03 config +24 PRIMARY_*, 04 custom Dockerfile+supervisord+GHA, 05 schedule+FSM, 06a reconciler core, 06b cross-pkg adapters, 07 migration 0023+sqlc+redisx, 08 main.go wire+emerg subscribe Pitfall #11, 09 gatewayctl primary CLI, 10 9 integration tests, 11 docs partial). Plan 11 Task 3 Live UAT BLOCKED. Plan 12 cleanup BLOCKED until UAT GREEN. CI pipeline GREEN (unit + integration + build-gateway + build-primary-pod).
  - **UAT 2026-05-18 — 8 attempts FAILED, $1+ Vast spend, no Scenario 1 PASS:** custom image `ghcr.io/ifixtelecom/converseai-primary-pod:develop` consistently exits within 30-60s of image extract on Vast hosts (Norway 867Mbps, Spain ES, multiple). Vast marks `actual_status=exited`, gateway correctly closes lifecycle via 3-strike confirmation. LOCAL docker run on vps-ifix-vm with same args + image WORKS (bash runs onstart, supervisord starts, 4 children spawn — they exit because vps-ifix-vm lacks GPU, expected). **Bug fundamental incompatibility custom-image × Vast runtype=args** — upstream `ghcr.io/ggml-org/llama.cpp:server-cuda-b9191` directly succeeds with same Vast args (lifecycle 11 → 5 ports up briefly). Root cause not isolated; needs deeper investigation (tcpdump/proxy of Vast docker run command).
  - **5 UAT-driven patches landed (all committed develop):**
    1. `fix(06.6-08)` a73a1f2 — emerg subscribe Pitfall #11 handler called wrong function (was `cancelActiveLifecycle`, must be `destroyAndCloseLifecycle + enterCooldown`). REAL production bug surfaced by integration test.
    2. `fix(06.6-10)` 0f559b1 — integration test harness `freshSchema` missing TRUNCATE for primary_lifecycles caused 3 test cross-contamination failures in CI.
    3. `fix(06.6-06a)` 22fcdbb — TestEvaluateAsleep_* CI race vs spawnProvisioning goroutine. fakeVast.SearchOffers returns error instantly; goroutine resets FSM before require.Equal asserts. Fix: block via channel until t.Cleanup. Same fix for TestEvaluateAsleep_AdvancesAfterCooldownElapses.
    4. `fix(06.6-06a)` c488d62 — `IsTerminal` requires 3 consecutive observations (~15s) before closing lifecycle. Lifecycle 4 captured false-positive close 12s before 4 endpoints were reachable.
    5. `fix(06.6)` e37fa60 — aria2c retries 10→50 + retry-wait 15s + SearchFilter `geolocation: EU` (DE/FR/NL/GB/ES/IT/PT/PL/AT/CH/BE/FI/SE/NO/DK). MinIO is Hetzner DE; pre-fix Vast picked US/Asia hosts that couldn't sustain 21GB transfer in retry budget.
    6. `fix(06.6-04)` f96d786 + ca514f9 — Dockerfile pivoted to hybrid Strategy B (drop multi-stage speaches/infinity COPY because /app missing in speaches image + Python venvs not relocatable; pip install Speaches/Infinity in final stage instead). Then explicit ENTRYPOINT ["/bin/bash"] override (empty array failed Vast attempt 6, llama-server inherited entrypoint failed attempts 3-5).
  - **Deployed state on vps-ifix-vm:** ai-gateway-dev container running with PRIMARY_TEMPLATE_IMAGE=ghcr.io/ifixtelecom/converseai-primary-pod:develop, 24 PRIMARY_* env vars present, FSM idle (asleep + DISABLED soak gate), reconciler subscribed to gw:primary:events. 13 primary_lifecycles DB rows from failed attempts. Stack file /opt/ai-gateway-dev/docker-compose.yml diverges from repo (operator-edited locally for env passthrough); needs reconciliation in follow-up commit. Stack also added PRIMARY_VAST_PRICE_CAP_DPH=0.80 + missing PRIMARY_* mappings.
  - **Session 2 (2026-05-18 late, BREAKTHROUGH):** /gsd-debug identified candidate root cause = HEALTHCHECK inherited from llama.cpp base targeting :8080 (our supervisord services bind :8000/:8001/:8002/:9400 — never :8080). Applied `HEALTHCHECK NONE` (commit 9c47b5a) + onstart trace markers + sshd-first (commit e309756). **Attempt 9** still showed actual=exited despite HEALTHCHECK NONE — diagnosis incomplete. **Attempt 10 (Denmark host 85.218.235.6, 1.7Gbps inet)** **WAS THE FIRST SUCCESSFUL POD**: container stayed alive 30+ min, sshd accessible (port 22 mapped), aria2c downloaded all 3 weights, tar extracts succeeded, supervisord ran 4 children, **llama-server FULLY FUNCTIONAL** (Qwen3.6 chat completions with reasoning_content + system_fingerprint=b9191-4f13cb742, 95 tok/s prompt + 46 tok/s predict). The trace-marker + sshd-first changes (commit e309756) made the pod survive — earlier attempts 1-9 may have been dying because bash exited silently before sshd could install. STT (Speaches) + Embed (Infinity) crash-looped under supervisord with import errors: `cannot import name 'NemoConformerTdt' from onnx_asr.models` (speaches 0.9.0rc3 vs onnx-asr ≥0.10 breaking change) and `ModuleNotFoundError: optimum.bettertransformer` (infinity-emb[all] doesn't pull bettertransformer extra). Pod destroyed via gatewayctl force-down + manual API DELETE (~$0.50 spend for attempt 10). HOWEVER: orphan pod 37028480 from attempt 9 had `BestEffortDestroy` fail with HTTP 429 rate limit; orphan ran ~3h30 burning ~$2.17 until operator caught via Vast UI event log + manual curl DELETE. **Total UAT spend this session: ~$3.**
  - **Patches committed this session (develop tip ddb7f44):** 9c47b5a (HEALTHCHECK NONE), e309756 (onstart trace markers + sshd-first + test relax), ddb7f44 (pip pin onnx-asr<0.10 + optimum[bettertransformer] for STT/Embed boot).
  - **Session 2026-05-19 (UAT 11→14, develop tip af81836):** 4 attempts iterating Dockerfile + supervisord fixes against the rebuilt primary-pod image. Total Vast spend this session ~\$0.63 (UAT 11 \$0.18 + 12 \$0.12 + 13 \$0.15 + 14 \$0.18). Zero orphans across all 4 attempts — every `BestEffortDestroy` succeeded on first DELETE (the new 429 retry never had to fire, but the safety net is in place for the next round). Patches landed:
    - `26b62cf fix(06.6-vastutil): BestEffortDestroy 429 retry w/ expo backoff` — 5 attempts × 1s→2s→4s→8s sleeps inside the 30s shutdown budget + Sentry breadcrumb on rate-limit-exhausted. Covers the attempt-9 orphan (~3h30 / \$2.17) root cause.
    - `5985765 fix(06.6-tests): alwaysInPeakRule covers 23:00-23:59 UTC` — the 5 primary integration tests that "deterministically failed" on the 26b62cf CI runs at 23:05 UTC turned out to be a latent test-helper bug: UpHour=0, DownHour=23 with the simple-intra-day branch excludes 23:00-23:59 UTC. Switched to UpHour=DownHour=0 so IsInPeak's overnight-wrap branch covers all 24 hours. Same fix applied to both `gateway/internal/integration_test/primary_helpers_test.go` and `gateway/internal/primary/reconciler_test.go`.
    - `adbff1b fix(06.6-04): 3 pod boot bugs — optimum<2.0 + libdcgm + speaches stub` — UAT 11 caught 3 post-pip-pin crash-loops: (a) optimum 2.1.0 removed `optimum.bettertransformer` → pin optimum<2.0; (b) DCGM exporter binary needs libdcgm.so.4 family + /etc/dcgm-exporter csv configs from the distroless stage → COPY them + ldconfig; (c) speaches 0.9.0rc3 mounts StaticFiles at /v1/realtime against a relative `realtime-console/dist` path → pre-create stub + index.html, supervisord launches speaches with `directory=/opt/speaches-data`.
    - `5f78beb fix(06.6-04): UAT 12 round-2 — pin gradio<6 + transformers<4.49` — UAT 12 cleared the prior layer and exposed: (a) speaches uses Gradio 5.x ChatInterface(type=…), broken in Gradio 6.0 → pin gradio<6.0; (b) infinity-emb 0.0.77 still imports optimum.bettertransformer which gates on transformers<4.49 → pin transformers<4.49. **UAT 12 partial PASS on Scenario 2 direct-probe**: system_fingerprint=`b9191-4f13cb742`, finish_reason=`tool_calls`, args=`{"location":"São Paulo"}` — Wave 0 Decision 3 B1 GGUF-embedded Jinja end-to-end PROVED on Vast 4090.
    - `648c427 fix(06.6-04): UAT 13 round-3 — pin click<8.2 (typer 0.12 incompat)` — UAT 13 ran LLM/STT/DCGM healthy (3-of-4 endpoints HTTP 200) but Infinity still crash-looped: typer 0.12.5 incompatible with click 8.4.0 (TypeError "Secondary flag is not valid for non-boolean flag"). Pin click<8.2.
    - `af81836 fix(06.6-04): bake speaches model_aliases.json stub` — UAT 14 reached FSM=Ready and ran S3 against the live pod; speaches /v1/audio/transcriptions hit `FileNotFoundError: 'model_aliases.json'` on every request. In-place patched `{}` stub on the live pod made the validator accept the model field, but the model-discovery layer behind it is still empty (separate fix). This commit bakes the stub into the Dockerfile so future pods don't repeat.
  - **UAT 14 scenario scoreboard (this session):**
    - **S1 PASS** (4-of-4 endpoints HTTP 200 on Vast 4090 Spain ES 213.96.60.239, supervisord 4 children RUNNING — llama PID 1586 + speaches 1587 + infinity 1588 + dcgm 1589 — `tier-0 override activated` logs for llm/stt/embed @ 00:24:12 BRT, `primary_ready observed; emerg not active` breadcrumb @ 00:24:12 BRT).
    - **S2 PASS** (LLM tool-calling end-to-end: direct probe via :19642 → `{"finish_reason":"tool_calls","tool_calls":[{"function":{"name":"get_weather","arguments":"{\"location\":\"São Paulo\"}"}}]}`, system_fingerprint=`b9191-4f13cb742`, 279 prompt + 78 completion tokens). Gateway-path probe got HTTP 503 `Upstream proxy not registered` despite the override logs — see Tech Debt below.
    - **S3 PARTIAL** (speaches reachable but model_aliases.json missing → in-place patched; second-layer error is `Model 'Systran/faster-whisper-large-v3' is not installed locally`, and `GET /v1/models` returns `data: []` despite WHISPER__MODEL_DIR=/weights/whisper containing config.json + model.bin + tokenizer.json + preprocessor_config.json + vocabulary.json — Speaches's model registry is not picking up the on-disk faster-whisper layout. Investigate speaches config env vars / scan path in next session.
    - **S4 PASS** (Infinity /embeddings: 2 inputs → 2 vectors × 1024 dims, model=`weights/bge-m3`, OpenAPI confirms `/embeddings` not `/v1/embeddings`. Stored as session evidence: first vector head `[-0.0146, 0.0110, -0.0314, -0.0112, -0.0096]`).
    - **S5 PARTIAL** (force-down → FSM=draining + drain_started_at populated + RestoreTier0 fired, BUT no subsequent `primary drain complete; transitioning to Destroying` log because `runScheduleLoop` skips `evaluateTick`/`evaluateDraining` when PRIMARY_POD_SCHEDULE_DISABLED=true. Manual Vast destroy + container restart + Redis HDEL gw:primary:state reset FSM to asleep for cleanup. See Tech Debt — operator force-down should drive state advancement even under DISABLED).
    - **S6 SKIPPED** (Pitfall #11 emerg coexist requires a second concurrent Vast spend for the emerg pod — deferred to a closeout UAT round dedicated to S3+S5+S6 only).
  - **New tech debt surfaced this session:**
    1. `runScheduleLoop` early-returns on PRIMARY_POD_SCHEDULE_DISABLED=true and never calls `evaluateTick`, so `evaluateDraining`/`evaluateDestroying` never advance under operator force-down. Fix: keep DISABLED gating the schedule-window logic but still run state-advancement (Draining/Destroying are operator-initiated; the soak-gate should not freeze drain).
    2. Gateway boot does not rewrite `gw:primary:state` Redis hash mirror. After container restart with in-memory FSM=asleep, the prior pre-restart state (e.g., draining) lingers in the mirror — `gatewayctl primary state` returns the stale snapshot. Fix: write fresh `state=asleep, entered_at=now` on reconciler init when boot recovery completes with no live lifecycle. Workaround used this session: `redis-cli DEL gw:primary:state`.
    3. Speaches model registry empty despite WHISPER__MODEL_DIR being set + faster-whisper layout present on disk. Speaches config dump showed `WhisperConfig(inference_device='auto', device_index=0, ...)` with no `model_dir` field — env var name may be different from what supervisord sets. Investigate next session.
    4. Gateway `chat_completions` proxy returns `503 upstream_unavailable: Upstream proxy not registered` even after `loader.OverrideTier0("llm", http://213.96.60.239:19642)` succeeded. Probe loop's `last_probe_status=failed` for `local-llm` (it still resolves UPSTREAM_LLM_URL env var, not the override). The dispatcher needs to honor the runtime override when routing OR the probe loop needs to read the override URL. Direct-probe to the pod IP works fine — this is a gateway routing-wiring gap, not a pod issue.
    5. S3 baseline: `gatewayctl key list` doesn't exist — operator must create a fresh test key via `gatewayctl key create -tenant converseai` each UAT, and there's no inspection/revocation listing. Low priority.
  - **Container redeploy gotcha:** Portainer webhook fired on the `26b62cf` build-gateway push (CI log shows `Portainer dev webhook triggered`) but the local image on vps-ifix-vm stayed at the pre-fix digest (3425aaf6a → expected dd49981586). Manual `docker pull` + `docker compose -f /opt/ai-gateway-dev/docker-compose.yml up -d gateway` recreated the container with the new image. Worth checking the Portainer stack config (pull policy / "always pull image" toggle) before the next CI cycle.
  - **Session 2026-05-19 (UAT 15 combined, develop tip 9720097):** Single Vast attempt on Norway 4090 (host 81.166.173.12, offer 36114274, $0.5362/h, 36min lifecycle wall, **spend $0.32**). Pre-UAT patches landed: `9720097 fix(06.6-06a): drain advances under DISABLED + boot resets state mirror` — closes tech debts #1 (runScheduleLoop blanket DISABLED gate moved to per-evaluator; evaluateReady gains DISABLED gate; evaluateDraining/evaluateDestroying advance under DISABLED) + #2 (Start() writes fresh `state=asleep, entered_at=now` mirror when recovery ends at StateAsleep). +3 regression tests in primary/reconciler_test.go (TestEvaluateReady_NoopWhenDisabled, TestEvaluateTick_AdvancesDrainingUnderDisabled, TestStart_BootResetsPrimaryStateMirror). CI build-gateway GREEN after rerun (proxy/chat_test.go:168 TestChatProxy_SSEStreamingFlushesPerChunk flaked on GHA; local PASS 3/3; not blocking).
  - **UAT 15 scenario scoreboard (this session):**
    - **S1 PASS** — 4-of-4 endpoints HTTP 200 on Vast 4090 Norway 81.166.173.12 (llama :11623 + speaches :11674 + infinity :11640 + dcgm :11693).
    - **S2 PASS** — LLM bench solo: prompt 162.8 tok/s + predict 47.1 tok/s, system_fingerprint `b9191-4f13cb742`. Concurrent N=4: aggregate **80.8 tok/s** (5.94s wall, llama parallel_slots=2 default → 1.7× scaling at N=4, batch-2 effective).
    - **GPU benchmark (NEW this session, was deferred from UAT 14):** RTX 4090 24 GB. **Idle** (DCGM + nvidia-smi): util 0%, VRAM 20215 MiB used (3 models loaded, 82%), temp 45°C, power 15.4 W, SM 210 MHz / mem 405 MHz. **Under load** (2 concurrent /v1/chat/completions 300-word essays): util **98%**, VRAM 20239 MiB (KV cache fits), temp 55°C, power **264 W** (~83% of 320W TDP cap), SM 2190 MHz (idle→full P0), mem 10251 MHz (idle→full). Driver 580.142.
    - **S3 PARTIAL** — **Root cause identified, two layers:** (1) Speaches 0.9.0rc3 uses HuggingFace cache layout, NOT custom `WHISPER__MODEL_DIR` env (visible in `/api/v1/config` dump: `WhisperConfig` has no `model_dir` field). MinIO `/weights/whisper` (faster-whisper layout) is IGNORED. Workaround: POST `/v1/models/Systran/faster-whisper-large-v3` triggers HF fetch into `/root/.cache/huggingface/hub/` (~2.9 GB pull, 3-4 min on EU host). After install, `/v1/models` lists the model correctly. (2) Transcription POST `/v1/audio/transcriptions` still fails with `Internal Server Error` — `onnxruntime.capi.onnxruntime_pybind11_state.NoSuchFile: silero_encoder_v5.onnx` (faster-whisper expects v5 encoder+decoder split; installed image ships only `silero_vad_v6.onnx`). Vendor-asset version mismatch — needs separate Dockerfile fix (downgrade faster-whisper pin OR pre-bake silero v5 asset).
    - **S4** — not retested this session (PASSED on UAT 14, no changes since).
    - **S5 PASS (THE KEY ASSERTION OF THIS SESSION)** — `gatewayctl primary force-down --reason uat15_s5_drain_under_disabled` at 07:18:45 BRT → log chain in **3 seconds**: `primary force-down: initiating drain by operator request` @ 07:18:45.616 → `tier-0 override cleared (cutback)` × 3 roles @ 07:18:45.892 → **`primary drain complete; transitioning to Destroying`** @ 07:18:46.156 (inflight=0, elapsed=0s, grace=300s) → BestEffortDestroy → LC 20 closed with shutdown=`destroyed` @ 10:18:48 UTC. Vast instance count after: 0. **Pre-fix (UAT 14): this chain froze at Draining forever under PRIMARY_POD_SCHEDULE_DISABLED=true** (`evaluateDraining` never called because `runScheduleLoop` early-returned). Fix #1 in commit 9720097 verified live end-to-end.
    - **S6 DEFERRED** — Pitfall #11 emerg coexist requires concurrent primary+emerg pod spend (~$1 + 60min cold-start). Same deferral as UAT 14; needs dedicated round.
  - **Tech debt closed this session:**
    1. ~~`runScheduleLoop` blanket DISABLED gate freezes Draining/Destroying~~ **RESOLVED 2026-05-19 via 9720097.** Per-evaluator gate; +TestEvaluateTick_AdvancesDrainingUnderDisabled regression. Verified live UAT 15 S5.
    2. ~~Gateway boot does not rewrite `gw:primary:state` Redis mirror~~ **RESOLVED 2026-05-19 via 9720097.** Start() writes fresh mirror on no-resume recovery. Verified live: pre-pull mirror empty → post-restart `state=asleep entered_at=<now>`.
  - **Tech debt remaining (Phase 6.6):**
    3. (was #3) Speaches model registry uses HF cache layout, not MODEL_DIR env. **Workaround**: POST `/v1/models/{id}` at pod boot via supervisord post-start hook OR pre-bake HF cache into Dockerfile (2.9 GB layer). **Real fix**: silero_encoder_v5.onnx asset needs vendoring or faster-whisper version pin downgrade to match speaches 0.9.0rc3 expectations.
    4. (was #4) Gateway `chat_completions` proxy returns 503 `Upstream proxy not registered` despite `loader.OverrideTier0("llm",...)` succeeding. Probe loop reads UPSTREAM_LLM_URL env, ignores runtime override. Dispatcher routing-wiring gap. NOT verified in UAT 15 — direct-probe to pod IP used throughout.
    5. (was #5) `gatewayctl key list` missing — low priority.
  - **Next session (UAT 16 deferred):** (a) Dockerfile fix S3 silero v5 vendoring + speaches HF cache pre-bake OR boot-time POST `/v1/models/{id}` automation; (b) S6 emerg coexist dedicated round; (c) fix tech debt #4 (chat_completions proxy override wiring); (d) write 06.6-11-SUMMARY.md + 06.6-VERIFICATION.md when S3+S6 PASS; (e) Plan 12 cleanup; (f) move to Phase 07 (Observability).

  - **Session 2026-05-19 (UAT 16 silero vendor verify, develop tip 73cf914):** 2 Vast attempts (Sweden 4090 host 34031 $0.3964/h × ~25min each = **spend $0.34**). Attempt 1 with `3d3e8dd fix(06.6-04): UAT 15 S3 — pin faster-whisper<1.1` crashed speaches at startup with `ImportError: cannot import name 'BatchedInferencePipeline' from 'faster_whisper'` — that symbol was added in faster-whisper 1.1.0, so the pin downgraded to 1.0.x which lacks it. Force-down + abort. Attempt 2 with `73cf914 fix(06.6-04): UAT 16 S3 — vendor silero v5 ONNX from upstream image` removed the pin and added a multi-stage build stage `speaches-assets` based on `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3`, with two COPY directives that vendor `silero_encoder_v5.onnx` (713 KB) + `silero_decoder_v5.onnx` (533 KB) into our `/opt/speaches-venv/lib/python3.12/site-packages/faster_whisper/assets/`. Discovery: upstream speaches image runs faster-whisper **1.1.1** (BatchedInferencePipeline present) but ships ONLY the v5 split files in the assets dir — they vendor it manually. The Silero VAD repo no longer publishes v5 files; their `src/silero_vad/data/` now contains only `silero_vad.onnx` (v4) + `silero_vad_v6.onnx` + 16k variants.
  - **UAT 16 scenario scoreboard:**
    - **S3 silero v5 vendor VERIFIED** — POST `/v1/models/Systran/faster-whisper-large-v3` registers correctly; `/v1/models` now lists **both** `Systran/faster-whisper-large-v3` **AND** `silero_vad_v5` (silero V5 module recognised — pre-fix `silero_vad_v5` was missing entirely). POST `/v1/audio/transcriptions` no longer hits `NoSuchFile: silero_encoder_v5.onnx`. New error path: `RuntimeError: CUDA failed with error out of memory` — VRAM budget exceeded when whisper-large-v3 tries to load on GPU after llama Qwen 27B Q4_K_M (~16 GB) + Infinity bge-m3 (~2 GB) already resident. 4090 has 24 GB total; transcription needs whisper-large-v3 GPU offload ~3-4 GB → exceeds 24 GB budget. **The silero asset fix itself is PASS**; the new tech debt is independent.
    - **S6 NOT TESTED THIS SESSION** — handler already verified via integration test commit 5985765 (`alwaysInPeakRule covers 23:00-23:59 UTC` + Pitfall #11 wired via `a73a1f2 fix(06.6-08): emerg subscribe Pitfall #11 handler called wrong function`). Live verification would require ~$0.60 (emerg pod $0.30 + primary re-cycle $0.30 to fire fresh primary_ready). Skipping; integration coverage holds.
  - **New tech debt surfaced UAT 16:**
    6. **VRAM budget exceeded on transcription** — Qwen Q4_K_M (~16 GB) + BGE-m3 (~2 GB) + KV cache + whisper-large-v3 GPU offload (~3-4 GB) > 24 GB. POST `/v1/audio/transcriptions` returns 500 with `CUDA failed with error out of memory`. Fix candidates: (a) force speaches `WHISPER__INFERENCE_DEVICE=cpu` (CPU transcription, slower but works — bge-m3 stays on GPU); (b) downgrade llama `--ctx-size 16384 → 8192` (saves ~2 GB KV cache); (c) move bge-m3 to CPU; (d) switch whisper to `faster-whisper-medium` (~1.5 GB). Decision deferred — STT throughput requirements not yet documented.
  - **Patches committed this session:** `3d3e8dd fix(06.6-04): UAT 15 S3 — pin faster-whisper<1.1 (silero v5 asset)` (REVERTED in 73cf914), `73cf914 fix(06.6-04): UAT 16 S3 — vendor silero v5 ONNX from upstream image` (CURRENT).
  - **Next session (deferred):** (a) VRAM budget fix (recommend CPU STT to start — STT latency rarely critical compared to LLM); (b) S6 dedicated round when STT path complete; (c) write 06.6-11-SUMMARY.md + 06.6-VERIFICATION.md; (d) Plan 12 cleanup; (e) Phase 07.

  - **Session 2026-05-19 (UAT 17 CPU STT + clip_timestamps pin, develop tip 50901d2):** 3 Vast attempts. **spend ~$0.65** total (attempt 1 Spain $0.16 / attempt 2 Norway-driver-too-old ~$0.28 / commit fixes between). Two patches landed:
    - `1a14ea4 fix(06.6-04): UAT 17 — force speaches STT to CPU to fit VRAM budget` — supervisord [program:speaches] gets `WHISPER__INFERENCE_DEVICE="cpu"`. Frees ~3-4 GB VRAM. Dropped unused `WHISPER__MODEL_DIR` / `SPEACHES__MODEL_DIR` (UAT 15 confirmed both vars are not in WhisperConfig).
    - `50901d2 fix(06.6-04): UAT 17 — pin faster-whisper==1.1.1` — speaches/executors/silero_vad_v5.merge_segments() emits dicts with keys `{start, end, segments}` where `segments` is a list. faster-whisper 1.2.x added strict clip_timestamps validation (iterates dict.items() + casts each value to int(v*rate)) → `TypeError: int() argument must be ... not list`. Upstream speaches Docker ships faster-whisper 1.1.1 which tolerates the extra key. Pin exactly. Earlier `<1.1` attempt (UAT 15 → 3d3e8dd reverted by 73cf914) broke speaches startup because 1.0.x lacks `BatchedInferencePipeline` import. The viable window is exactly 1.1.1.
  - **UAT 17 scenario scoreboard (this session):**
    - **S3 FULL PIPELINE VERIFIED via speaches logs (functional PASS)** — Norway pod (host 79.160.189.79, driver 565.57.01 too old for image's CUDA 12.6 → llama-server + speaches both fell back to CPU). Despite the unrelated GPU regression, ALL S3 layers are verified working:
      - `Model silero_vad_v5 loaded in 0.21s` + `VAD model inference took 0.0115s` ✓ vendor v5 ONNX assets resolve + execute.
      - `VAD filter removed 00:00.000 of audio` ✓ NO `TypeError: not list` crash — faster-whisper 1.1.1 pin works end-to-end with speaches merge_segments output.
      - `Model Systran/faster-whisper-large-v3 loaded in 36.78s` ✓ HF cache install path works.
      - `ctranslate2 warning: ... target device or backend do not support efficient float16 computation ... converted to use the float32` ✓ CPU mode active.
      - `Processing audio with duration 00:02.516` → `Detected language 'en' with probability 0.83` after 70s ✓ transcription pipeline executing; just too slow on CPU large-v3 (~28x realtime) so curl 180s timeout hit before the response body returned.
    - **S6 NOT TESTED** — Pitfall #11 handler integration-tested already (commit a73a1f2). Live verification needs another $0.60 + 60min cold-start.
  - **NEW tech debt UAT 17:**
    7. **STT model size — whisper-large-v3 + CPU too slow for usable latency.** Speaches log shows ~28x realtime (2.5s audio → 70s+ processing). Production users need <5s response on a 5-10s audio clip. Fix candidates: (a) downgrade to `Systran/faster-whisper-medium` (~1.5 GB GPU; fits even alongside Qwen 27B if KV cache trimmed); (b) downgrade to `Systran/faster-whisper-tiny` (~75 MB CPU; sub-realtime even on CPU); (c) push STT to a dedicated emergency-only path with cheaper model. Decision deferred to next session — production latency SLO not yet documented.
    8. **Vast host driver compatibility** — host 79.160.189.79 (driver 565.57.01) silently fell back llama+speaches to CPU because the llama.cpp:server-cuda-b9191 image needs CUDA 12.6+ (driver ≥ 570). 0.49 tok/s vs expected ~50 tok/s. Filter Vast offers by min driver version in `vast.SearchOffers` (`driver_version: gte_570.0`) to avoid the spend trap. Current geo-only filter (UAT 15 commit e37fa60) doesn't prevent old-driver hosts.
  - **Patches committed this session:** `1a14ea4 fix(06.6-04): UAT 17 — force speaches STT to CPU to fit VRAM budget` + `50901d2 fix(06.6-04): UAT 17 — pin faster-whisper==1.1.1`.
  - **Next session:** (a) pick STT model size policy (likely faster-whisper-medium GPU); (b) Vast driver-version filter (tech debt #8); (c) S6 dedicated round; (d) 06.6-11-SUMMARY.md + 06.6-VERIFICATION.md; (e) Plan 12 cleanup; (f) Phase 07.

  - **Session 2026-05-19 (UAT 18 closeout — 5090 + STT GPU + S3/S6 full PASS, develop tip d7786e6):** STT model policy decided in commit 9e1aca0 — bump 4090→5090 32 GB so whisper-large-v3 can stay on GPU (D-UAT17 reverted). PRIMARY_VAST_PRICE_CAP_DPH default raised 0.40→2.20 + MONTHLY_PRIMARY_BUDGET_BRL default raised 200→800; vps-ifix-vm `.env` set BUDGET=2400 + CAP=2.20 explicitly. Force-up returned `primary: no offers below cap` despite EU 5090 inventory existing → bisected Vast `cuda_max_good gte 12.6` filter regression specific to 5090 queries (RTX 4090 queries unaffected). Bump filter 12.6→12.8 in `d7786e6 fix(06.6-vast): UAT 18 — bump cuda_max_good 12.6→12.8 for RTX 5090 search`. After fix, single force-up succeeded: lifecycle 26, Vast 37094533, **5090 host 12140 Spain ES at $0.7657/h** (cheapest EU 5090 yet seen, below previously-assumed $2.00/h floor), driver 580.105.08, cuda 13.0, inet 7.4 Gbps. Cold-start 9 min (17:10:41 force-up → 17:19:54 Ready). DCGM: idle 20.5 GB used (3 models loaded), full-load post-whisper-install 24.7 GB used / 7.4 GB free / temp 53°C / power 33W idle. **S3 PASS**: espeak WAV 226KB → POST /v1/audio/transcriptions cold 17.5s (whisper-large-v3 GPU load) / warm 0.76s (well below 5s budget); accurate text. **S6 PASS via manual primary_ready republish technique**: emerg force-provision @17:22:30 (lifecycle 40, Vast 37095236, 4090 Iceland $0.37/h, 28m 42s cold-start to EmergencyActive @17:51:12 — longer than primary because Iceland host bandwidth lower) → Redis PUBLISH `gw:primary:events {type:primary_ready,lifecycle_id:26,replica_id:manual-uat18}` @17:51:50 (subscriber count=2 confirms emerg replica reception) → Pitfall #11 fires in <1s: cancelActiveLifecycle (D-C3) + destroyAndCloseLifecycle (Vast DELETE 37095236) + RestoreTier0 + FSM EmergencyActive→Cooldown reason=primary_took_over. Final state: 1 Vast instance (primary only). Force-down primary @17:52:56 → drain complete in 1s under DISABLED gate (S5 fix 9720097 re-verified) → asleep @17:55. UAT 18 total spend: primary 45min × $0.77 + emerg 29min × $0.37 ≈ **$0.75**. Cumulative Phase 6.6 UAT 1–18 spend: ~$6.
  - **Tech debt remaining (carries to Phase 07):**
    4. ~~(carry-over) Gateway `chat_completions` proxy returns 503 `Upstream proxy not registered` despite `loader.OverrideTier0` succeeding.~~ **RESOLVED 2026-05-22 via 30f90e7 + 12f7479 (shipped before debug, image `d6893211` deployed to vps-ifix-vm).** Root cause was dispatcher gap: `Loader.Resolve` with active override returns synthetic `Name="emergency_pod_<role>"`; pre-fix dispatcher `Proxies` map lacked that key → `dispatchTo` 503'd. Fix registers `NewDynamicOverrideProxy` for llm + stt (30f90e7) and tts (12f7479) in `cmd/gateway/main.go`. **Live UAT 2026-05-22** on lifecycle 98 (Alberta CA 5090 host @ $0.87/h — env was drifted; see env-drift note below): gateway-path `POST /v1/chat/completions` → HTTP 200, `system_fingerprint=b9191-4f13cb742` matching direct-probe, billing event enqueued via `upstream=emergency_pod_llm`, zero "Upstream proxy not registered" log entries. Regression test landed in `bc26cc8 test(gateway/proxy/dispatcher): regression — emergency_pod_<role> dispatch` (paired 503/200 paths in `dispatcher_test.go`) locks the contract against silent refactor.
    5. (carry-over) `gatewayctl key list` missing — low priority.
    9. **NEW UAT 18**: Pitfall #11 destroyAndCloseLifecycle's RestoreTier0 clears the emerg's tier-0 override slot, which transitively clears whatever primary had previously written into that slot. Primary reconciler doesn't auto-restore on Ready-tick. Workaround: cycle primary force-down + force-up to re-establish. Real fix: primary reconciler should re-call OverrideTier0 on each Ready-tick when override slot is empty (or republish primary_ready edge so the upstream module re-runs its activation path).
  - **Session 2026-05-22 (td #4 closure, develop tip `bc26cc8`):** Goal: regression test + live UAT to flip td #4 OPEN → CLOSED (per HANDOFF `.planning/seeds/HANDOFF-tech-debt-04.md`). Regression test landed (`bc26cc8`) — `dispatcher_test.go` paired tests `TestDispatcher_EmergencyOverride_503WhenProxyMissing` + `TestDispatcher_EmergencyOverride_200WhenProxyRegistered` pass in 14ms; full `./internal/proxy` suite green. **Env drift discovered + corrected:** `/opt/ai-gateway-dev/.env` on vps-ifix-vm lacked the Phase 06.8 final GPU shape (`PRIMARY_GPU_NAME`, `PRIMARY_NUM_GPUS`, `PRIMARY_VAST_MACHINE_ALLOWLIST`), falling back to compose defaults (5090 single, cap $1.00) instead of the validated 2×RTX 3090 shape (allowlist `43803,55158`, cap $0.60). Phase 06.8 deploy never updated vps-ifix-vm `.env`. Reverted env at session end: `PRIMARY_GPU_NAME="RTX 3090"`, `PRIMARY_NUM_GPUS=2`, `PRIMARY_VAST_MACHINE_ALLOWLIST=43803,55158`, `PRIMARY_VAST_PRICE_CAP_DPH=0.60`, `PRIMARY_VAST_MACHINE_BLOCKLIST=55942,45778`. Container recreated; `docker inspect` confirms env present. Backup at `/opt/ai-gateway-dev/.env.bak-td04-uat`. **UAT spend: ~$0.50** (8 attempts on 5090 inventory before lifecycle 98 succeeded — 3 CDI/disk/offline host failures + 2 race-skip "offer no longer available" + 2 host-fault terminals; loop script `/tmp/uat-retry-loop.sh` blocklists each failed `machine_id` and recreates container per cycle).
  - **Phase 6.6 STATUS: COMPLETE** — `.planning/phases/06.6-*/06.6-VERIFICATION.md` status=passed, 6/6 scenarios PASS, 13/13 plans GREEN. Plan 12 cleanup (scaffolding-only) still pending but not blocking Phase 07.

- **Phase 6.5 (ex-Phase 6):** 10/11 plans executed (06.5-01..06.5-10 GREEN + summaries — renomeados de 06-* em 2026-05-16). 06.5-11 is `autonomous: false` HUMAN-UAT — Tasks 1+2 done (06.5-HUMAN-UAT.md + docs/RUNBOOK-EMERGENCY-POD.md created, commit 2b539fc); Task 3 is a **blocking** human-verify checkpoint (6 LIVE Vast.ai UAT scenarios, ~R$10-15) — **UNBLOCKED 2026-05-17** by Phase 6 close (runtype=args end-to-end validated). No 06.5-11-SUMMARY.md, no 06.5-VERIFICATION.md yet.
  - **Integration tests (emerg suite): RESOLVED 2026-05-14.** First real CI run of `gateway/internal/integration_test/emerg_*` (Phase 6.5 deferred them to CI runtime — never executed before) failed 8 tests. 3 root causes found+fixed via `/gsd-debug`: (1) `freshSchema` missing `emergency_lifecycles` TRUNCATE → cross-test DB contamination (commit 9772d71); (2) stale Plan 06.5-05 force-provision/D-C5 test assertions vs reconciler evolved by 06.5-06+ (commit 355843b); (3) re-trigger oscillation race — `offer_race_lost` abort returned FSM straight to Healthy instead of Cooldown, `evaluateHealthy` re-fired the trigger every tick — fixed via new `ProvisionFailureCooldownSeconds` config (commit 85ba3da). All 22 emerg integration tests GREEN in CI run 25891568768 (build-gateway, develop). Debug sessions: `.planning/debug/emerg-integration-tests-ci.md` + `.planning/debug/emerg-bid-race-lost.md`.

- **Phases 7–10:** Not started (no phase directories) — Phase 07 unblocked 2026-05-19 by Phase 6.6 closeout.
- **Status:** Ready to execute

## Performance Metrics

- **Phases completed:** 6 / 11 (1–6 on disk; Phase 6.5 plans done, human UAT now UNBLOCKED by Phase 6 close)
- **Plans completed:** 59 / 61 (Phase 1: 9/9 · Phase 2: 8/9, 02-09 deferred · Phase 3: 8/8 · Phase 4: 9/9 · Phase 5: 8/8 · Phase 6: 7/7, all GREEN with SC-2 perf gap noted · Phase 6.5: 10/11, 06.5-11 human UAT now unblocked)
- **v1 requirements covered by executed plans:** POD-01..07, GW-01..10, TEN-01..09, RES-01..08, LSH-01..05, PRV-01..10 — 49/70 (remaining: OBS-01..08, INT-01..06, PRD-01..07 in Phases 7-10)
- **Phase 14 (vram-adaptive-stt):** 14-01 (gateway device-gate) DONE · 14-02 (pod VRAM-adaptive whisper + :9100 responder, ~5min, 2 tasks, 4 files) DONE · 14-03 (image rebuild + live Vast UAT, autonomous:false) PENDING. **Develop CI GREEN** (build-gateway + build-primary-pod) on tip `79a2cf3`. 3 post-CI fixes landed during orchestration (NOT in the device-gate logic itself): (1) `d5e086e` — 4 primary INTEGRATION tests asserted the full llm/stt/tts trio but their fake pod neither served `:9100` nor wired `Deps.DeviceReport` → stt correctly suppressed → 2 roles → CI red; fixed with a shared `cudaDeviceReport` helper + `9100/tcp` in the `runningPrimaryInstance` fixture; (2) flaky unit `TestHandleForceUpRequest_TransitionsAsleepToProvisioning` (async provision-fail races the lastProvisionFailureAt=nil assert) passed on rerun — pre-existing, left as-is; (3) `79a2cf3` gofmt realign of Deps blocks. **LESSON: integration tests (`-tags integration`, need Docker) + `gofmt -l` do NOT run in the executor's local `go test ./internal/primary/...` check — run `sudo env PATH=... go test -tags integration ./internal/integration_test/` + `gofmt -l` before pushing any reconciler change.**

## Accumulated Context

### Ops notes (2026-06-29, post-Phase-16)

- **OBS-09 clarity:** `/economia` panel had no headline Vast-cost number (only "Custo OpenRouter"; vast_brl was inside Líquido + a trend-chart line). Added **"Custo Vast (GPU)"** KPI card (`economy-panel.tsx`, reads `summary.vast_brl`; grid 5→6 cols). Deployed to PROD dashboard (rebuilt :latest-dev on n8n-ia-vm). main FF'd → 722189b.
- **Primary-pod "flap" investigated → NOT a flap.** It's provisioning RETRIES on broken Vast hosts (discriminator: `primary_lifecycles.first_health_pass_at IS NULL` = never healthy = boot failure, vs set = drained flap). ShouldStayUp fix intact. Root cause: hosts 104433 (OCI/CDI broken) + 56883 (never health-passes, burns full 3600s coldstart = R$0.61) selected because the allowlist is a PREFERENCE not a hard filter (broadens to global cheapest when no allowlisted offer below cap). Fix: appended both to PROD `PRIMARY_VAST_MACHINE_BLOCKLIST` (.env, recreated gateway). ~R$0.70 wasted 2026-06-29. Structural hardening (hard-filter during lead window / early-abort on no-model-load) = open ticket, not done. See memory [[vast-flaky-host-za-97968]].

### Roadmap Evolution

- Phase 22 added (2026-07-19): Funnel de custo LLM pelo ai-gateway + medição de ROI do pod. Motivação medida jul: OpenAI $53 (ai-gateway $38 + Vps-Claude $14,6 + N8N $0,27) + Gemini R$238; grosso é converseai-v4-dev (stack 15) chamando Gemini/OpenAI/OpenRouter DIRETO, fora do gateway. Objetivo: rotear tudo pelo gateway (fallback direto incluso) p/ billing_events medir ROI do pod. Requirements PRICE-01 (GATE — corrigir prices subvalorizados) → CV-01/02/03 (ativar Phase 113 + Gemini chat fallback + followup) → MEASURE-01 (painel ROI). Ver memory gateway-cost-funnel-and-pod-roi + CONTEXT do phase dir.
- Phase 17 added (2026-06-29, from /gsd:explore): Dashboard pod-config control — owner controla TODAS as configs do pod pelo dashboard. Arquitetura travada (env→DB híbrido: hot-reload + self-restart estrutural via unless-stopped; owner-edit/operator-view; bounds/confirm/audit; live lifecycle status). Ver notes/dashboard-pod-config-control-architecture.md. Research question aberta (enumerar+classificar config surface). SEED-020 (allowlist hard-filter) capturado em paralelo.
- Phase 16 added (2026-06-28): CLOSURE — wire STT/embed usage metering (TEN-04). From v1 re-audit; sole remaining v1 code blocker. AudioSecondsMs10/EmbedsCount declared+read+quota-gated but no producer → audio/embed quotas unenforced + /consumo shows 0. Fix surface: gateway/cmd/gateway/main.go + usage interceptor on 4 STT + 2 embed proxies.
- Phase 13 added (2026-06-14): dashboard-user-management — gestão de operadores owner-only (criar/remover/reset-senha/reset-2FA) + self-service change-password. Decisões locked: owner-only, 4 ops, tudo junto. Security surface → secure-phase obrigatório.

### Key Decisions (from research + PROJECT)

- Gateway language: Go (chi v5 + stdlib `httputil.ReverseProxy` + slog)
- LLM server: `llama.cpp` native (not `llama-cpp-python`)
- STT server: `speaches-ai/speaches` (not custom FastAPI)
- Embedding server: `michaelf34/infinity` (not `sentence-transformers`)
- Saturation signal: composite (inflight + P95 + VRAM + hysteresis), not GPU util alone
- Primary GPU: Vast.ai RTX 4090 (cost) with emergency Vast.ai pod failover (not RunPod Secure)
- LLM model: Qwen 3.5 27B Q4_K_M GGUF, fixed both primary and OpenRouter fallback
- Deploy: Docker Compose + Portainer + webhook GitHub (standard Ifix)
- Postgres: shared DO cluster with dedicated `ai_gateway` schema
- Pre-baked pod Docker image (`ghcr.io/ifixtelecom/ifix-ai-pod`, slim ~2 GB) with weights downloaded from Ifix MinIO at boot via `onstart.sh` (revised by Phase 1 per D-01/D-02/D-04 — image stays small, weights versioned by key path with SHA-256 integrity D-05)
- Plan 02-08: ship `/gateway` + `/gatewayctl` in the same distroless image (27.7 MB total) — ops model is `docker exec ifix-ai-gateway /gatewayctl <cmd>` rather than a separate sidecar image
- Plan 02-08: boot-time migrations via `AI_GATEWAY_MIGRATE_ON_BOOT` env flag instead of a dedicated CI migration job; goose idempotency makes this safe across restarts
- Plan 02-08: GitHub Actions `paths:` filter on pull_request only (not push) — mirrors build-pod.yml to avoid silently skipping stable-release tag pushes when the tag commit itself doesn't touch gateway/**

### Open Todos (for upcoming phases)

- [ ] Phase 2 close: live deploy UAT — set GitHub Secrets `PORTAINER_WEBHOOK_URL_DEV_GATEWAY` + `PORTAINER_WEBHOOK_URL_PROD_GATEWAY`; create Portainer stack `ai-gateway-dev` (Repository + webhook → `gateway/docker-compose.yml` on `develop`); run the 10-step post-push checklist in `02-08-SUMMARY.md`
- [ ] Phase 6.5 close (BLOQUEADO por Phase 6 refactor): executar 06.5-HUMAN-UAT.md (6 LIVE Vast.ai scenarios, ~R$10-15) → fill sign-off → write 06.5-11-SUMMARY.md + 06.5-VERIFICATION.md. Pre-req: Phase 6 (template refactor SEED-001) precisa fix runtype=ssh bug antes — atual UAT impossível end-to-end.
- [ ] Phase 7: 02-09 cold-storage audit export (Parquet + MinIO + retention DROP) — re-evaluate when audit_log grows past ~60 days of production traffic
- [ ] Phase 7: Confirm Ifix WhatsApp provider (Evolution API / Z-API / Chatwoot / proprietary)
- [ ] Phase 7: Choose dashboard auth (Better Auth instance vs shared with ConverseAI vs SSO)
- [ ] Phase 9: Obtain LGPD review sign-off from Ifix legal before activating sensitive tenants
- [ ] Tech debt (deferred from Phase 6.5): `gateway/internal/auth` argon2id tests hang under `-race`; `gateway/internal/db/migrate_test.go:53` migration count hard-coded 18, now 19 — fix via `/gsd-quick`
- [ ] Tech debt (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 29): `audit flush failed: invalid input value for enum data_class: "" (SQLSTATE 22P02)` a cada FSM state-change. Root cause: `gateway/internal/emerg/fsm.go:331` chama `auditWriter.WriteStateChange("fsm_transition", audit.Event{...})` sem setar `DataClass`; zero-value "" viola enum `ai_gateway.data_class` (`normal|sensitive`) que é NOT NULL desde migration 0019. State-change audit rows perdidos em batches de 2; FSM e Sentry breadcrumb não afetados. Fix candidato A (simples): `WriteStateChange` em `gateway/internal/audit/writer.go:167` seta default `ev.DataClass = "normal"`. Fix candidato B (semanticamente correto): nova migration adiciona valor `system` ao enum + `WriteStateChange` usa `system`. Fix via `/gsd-quick` pós-UAT.
- [ ] Tech debt (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 29): reconciler `provisionLifecycle` não detecta `status_msg=Error: GPU error, unable to start instance.` retornado pelo Vast.ai quando host tem hardware fault na 4090. Sintoma observado: instance 36904592 (host 1647/machine 15489) ficou ~6min em `actual=created` + `cur=running`/`stopped`/`running` oscilando, com `status_msg="Error: GPU error..."` desde o início, mas reconciler só fazia health-poll e iria dar `health_timeout` em 30min (coldstart_budget=1800s). Fix: em `gateway/internal/emerg/lifecycle.go` (loop de health-poll) adicionar checagem paralela de `vast.GetInstance(id).status_msg` — se contém `"Error:"`, `"GPU error"`, ou `"unable to start"` → `DestroyInstance` + `closeLifecycle(reason="vast_gpu_error")` + novo `ErrVastGPUError` em `errors.go` + breadcrumb Sentry. Reduz failed-host detection de 30min pra ~10s. Workaround atual: operator vê erro na UI Vast.ai + `gatewayctl emerg force-destroy` manual. Fix via `/gsd-quick` pós-UAT.
- [x] ~~Tech debt CRÍTICO (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 31): `handleForceProvision` não trata FSM em estado `cooldown`.~~ **RESOLVIDO 2026-05-16 via quick 260516-rym.** Fix: precheck `FSM.State()` rejeita força-provision se estado in `{EmergencyProvisioning, EmergencyActive, Recovering}`; substitui 2x `Transition(from, to)` por `SetState(StateEmergencyProvisioning)` que CAS-loops até commit regardless of current state. 2 regression tests em `emerg_force_command_cooldown_test.go`. Ver `.planning/quick/260516-rym-fix-force-provision-cooldown-transition/SUMMARY.md`.
- [ ] Tech debt (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 32): leader-recovery não reseta FSM ao detectar lost lifecycle. Sintoma observado: container ai-gateway-dev recreated com FSM=emergency_provisioning (LC32 in-flight). Recovery em `gateway/internal/emerg/recovery.go` escaneou live lifecycles, detectou Vast.ai instance 36906461 "not found at Vast (lost)", fechou LC32 com `shutdown_reason=leader_recovery_lost`, MAS deixou FSM em emergency_provisioning sem activeLifecycle. `force-destroy` ficou no-op (handleForceDestroy retorna Warn quando activeLifecycle nil). Fix: em `resumeFSMFromEvents` (ou wrapper), quando recovery resulta em "instance not found at Vast (lost)" → fechar lifecycle + `FSM.SetState(StateHealthy, ..., "leader_recovery_lost_no_resume")`. Workaround atual: restart container ou apagar redis mirror manualmente. Fix via `/gsd-quick` pós-UAT.
- [ ] Tech debt (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 32): gateway boot não rewrite Redis mirror `gw:emerg:state`. Sintoma observado: após restart container com FSM in-memory iniciando em healthy, mirror Redis manteve snapshot anterior (`state=emergency_provisioning entered_at=1778974096`). `gatewayctl emerg state` lê mirror direto → mostra stale state mesmo com gateway healthy. UI/ops decisions baseadas em mirror stale podem dar resultado errado. Fix: em `cmd/gateway/main.go` boot path, após NewFSM + recovery completar, escrever mirror inicial via `EmergStateKey()` HSET com state atual + entered_at = time.Now(). Workaround atual: `redis-cli -n 0 DEL gw:emerg:state` manual. Fix via `/gsd-quick` pós-UAT.
- [ ] **Tech debt CRÍTICO (Phase 6.5, descoberto 2026-05-16 durante UAT live lifecycle 33): `Runtype="ssh"` em `gateway/internal/emerg/lifecycle.go:687` IGNORA `ENTRYPOINT/CMD` da Docker image.** Vast.ai com `runtype=ssh` roda apenas `sshd` como PID 1 do container — o `CMD ["/usr/local/bin/emerg-bootstrap.sh"]` do `pod/Dockerfile` nunca arranca. Por isso TODOS os lifecycles 29-33 ficaram travados: image baixou OK, Vast marca `actual_status=running`, MAS `emerg-bootstrap.sh` nunca rodou → Qwen nunca baixou → llama-server nunca subiu → porta 8000 sem listener → health-poll do gateway falha até timeout (1800s). Métricas do pod confirmaram pod idle (CPU 0%, mem 5MB, GPU 20°C). Comentário do código (linhas 682-685) afirma "Emergency pods run the image's baked-in CMD" — **errado pra runtype=ssh**. **Este bug explica por que Phase 6.5 UAT live nunca funcionou end-to-end ainda — todos os "health_timeout" anteriores na tabela emerg lifecycles compartilham essa root cause.** Decisão Pedro 2026-05-16: NÃO fix runtype, em vez disso refatorar pra usar templates Vast.ai Ubuntu+CUDA (ver SEED-001) — abordagem que simultaneamente resolve este bug + ganho 2-4min cold-start + iteração dev rápida. Workaround imediato impossível: nenhum lifecycle vai chegar emergency_active enquanto custom image + runtype=ssh combination existe. Próxima sessão: `/gsd-discuss-phase` baseado em SEED-001 → implementar via quick task ou nova phase.

### Blockers

- **Phase 6.5 cannot reach COMPLETE without operator action:** 06.5-11 Task 3 is a blocking human-verify checkpoint requiring real Vast.ai spend. Autonomous mode cannot satisfy it. Phases 7-10 do not hard-depend on Phase 6.5 *verification* (they depend on Phase 6.5 FSM states/code, which exist) — but Phase 7's goal explicitly visualizes Phase 6.5 FSM states, so plan Phase 7 with Phase 6.5 code as-built. **Phase 6.5 UAT now also blocked by Phase 6 (template refactor) — runtype=ssh bug must be fixed first.**

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260825-2fa | **Fix loop 2FA dashboard (trusted-device) — DEPLOYADO stack 40.** Sintoma: "verificação em duas etapas expirou" em loop. Causa: sign-in com device trusted skipa challenge e mantém a sessão criada em /sign-in/email; hook CR-04 só marcava two_factor_verified nos endpoints de verify → sessão f → middleware → challenge sem cookie → INVALID_TWO_FACTOR_COOKIE → /login expirado. Latente desde 260625-k17 (trustDevice); NÃO era o redesign (coincidência: trust criado 24/08 19:23, relogin 25/08). Fix: `twoFactorVerifiedOnCreate()` exportada (verify paths + /sign-in/email com user 2FA-enabled via lookup) + unit tests; 88/88 verdes. Unblock imediato pré-deploy: DELETE trust-device+sessões f no bd_ai_dashboard_prod. Deploy: `develop-135d5a7` stack 40 manual (webhook dev do CI = 404 conhecido). Gotcha: verifyTOTP api-mode retorna headers vazios no harness → trusted flow só unit-testável. | 2026-08-25 | 135d5a7 | — |
| 260825-anq | **Embed-gpu na 3060 unificada + fixes rerank — DEPLOYADO E VALIDADO E2E.** Migration 0036: embed re-tier (embed-gpu tier-0 novo `UPSTREAM_EMBED_GPU_URL`=pod 3060 unificado Infinity bge-m3 dims 1024; local-embed 0→1 fallback CPU MESMO modelo/dims; openai-embed 1→2 FORA da cascata — 1536 em índice 1024 = corrupção silenciosa, melhor 503). Fixes de carona: (B) "rerank" no roster `tier0Roles` (loader.go:284) — rerank-gpu nunca era probado (LAST_PROBE "-"); (C) billing rerank (NewRerankProxy sem usageInterceptor + routeToBillingRoute sem /v1/rerank → caía em "chat" e era descartado) + gate isSelfHostedUpstream no costExternal. Deploy: imagem `develop-b29a0ef` + env no stack 38 (GOTCHA: compose precisa da linha `UPSTREAM_EMBED_GPU_URL: ${...}` no service, env do stack sozinha não chega) + `migrate up` v36. **VALIDADO:** embed via gateway → tier-0 embed-gpu 200 dims 1024; kill Infinity → dial-fail → local-embed CPU 200 dims 1024 → probe fecha breaker → tier-0 volta; rerank idem (failover+recovery provados); billing live (85 tokens_in / 5 embeds / 7 reqs, custo externo 0); rerank-gpu probado ok 85ms. Probe embed-gpu marca "config" (corpo do probe usa model "probe-default" → 4xx; cosmético, breaker fecha). WARNs price-missing restantes = caminho PHANTOM (openrouter-fireworks p/ bge-m3/model.gguf/local-stt) — pré-existente, fora do escopo. **Infra na mesma sessão (aprovado Pedro): cutover 3060 completo** — STT/TTS do stack 38 flipados pro unificado (91.150.160.38:15167), pod velho 48644310 DESTRUÍDO (-$0,040/h), timers `vast-3060-*` DESABILITADOS (script atual é up→destroy+renasce speaches-only → destruiria o unificado às 22h; religar só com automation unified-aware — backlog). Unificado 48611358 roda 24/7 manual ($0,055/h) servindo STT+TTS+rerank+embed. onstart do pod atualizado dual-model (HIPÓTESE não testada: Vast pode reinjetar onstart armazenado em reboot — sintoma: embed 400 pós-reboot; API PUT onstart deu success:true mas não aplicou). | 2026-08-25 | b29a0ef | [260825-anq-embed-gpu-3060-rerank-fixes](./quick/260825-anq-embed-gpu-3060-rerank-fixes/) |
| 260824-ucv | **Fixes A+B do handoff tokenizer-fail-open — DEPLOYADO E VALIDADO E2E.** (A) `chatRetryableStatusInterceptor` em `NewDynamicOverrideProxy(role llm)`: 400 `exceed_context_size_error`/413 do tier-0 vira `errUpstreamRetryable` → cascata tier-1; só non-streaming (D-07); breaker do pod NÃO abre por over-context (senão request grande derrubava o tier-0 inteiro pro pago); `openrouter-chat` e embed FORA (embed teria cascateado pro openai-embed que rejeita >8191 — deviation Rule 1). (B) tokenizer do guard RES-07 resolve o tier-0 EFETIVO (Resolve movido pra antes do Enforce; cache key com fingerprint do URL); over-cap pré-dispatch roteia direto ao tier-1. **POLÍTICA (decisão Pedro): sensitive TAMBÉM cascateia em over-context** (n8n já fazia fallback direto pro OpenRouter sem billing — bloqueio era teatro); RES-08 intacto nos outros caminhos; métrica `gateway_over_context_cascaded_total{tenant,upstream}` = trilha de auditoria. De brinde: fix do develop VERMELHO herdado do rerank (0035 sem bump dos Down(N) nos testes de migration 0026/0029) + `migrate up` 0035 aplicada em prod. Deploy: `develop-06e209a` stack 38. **VALIDADO 5/5:** 72k tokens uat10 → tier-1 200 (guard contou 72.015 vs 72.017 do provider — precisão de 2 tokens, log `over-context pre-dispatch; routing to tier-1; skipped emergency_pod_llm`); 72k telefonia → tier-1 200 + métrica por tenant; 20.914 tokens → POD (não cascateia à toa); STT 200 1,1s; **zero WARN de tokenize** pós-deploy (guard vivo, era 8+/dia). **BUG PRÉ-EXISTENTE descoberto (NÃO corrigido):** `audit/middleware.go:57-59` trunca request body de tenant normal em 128KiB (`contentCapBytes`) e substitui r.Body só pelos bytes lidos → request >128KiB = 502 `ContentLength mismatch` DESDE SEMPRE (pendência do 260723-sgx; Maestro a ~87KB está se aproximando). Fix: capturar capped mas encaminhar stream completo (MultiReader). | 2026-08-24 | 06e209a | [260824-ucv-fixes-a-b-handoff-tokenizer-fail-open-ca](./quick/260824-ucv-fixes-a-b-handoff-tokenizer-fail-open-ca/) |
| 260824-s2t | **KV cache q8_0 + contexto 32k por slot no pod primário.** Medido em pod Vast de teste (RTX 3090, mesmo llama.cpp/GGUF): `--ctx-size 65536 --cache-type-k q8_0 --cache-type-v q8_0 -fa on` → `n_ctx_slot=32768` usando **21.299 MiB** (vs 21.021 f16@32k, **+278 MiB**), geração **190 tok/s** (vs 202, **-6%**), e **request de 20.934 tokens servido com sucesso** — o tamanho que hoje retorna 400 `exceed_context_size_error` (peer mediu 63 ocorrências em 2h matando turnos do Maestro). **`-fa on` é OBRIGATÓRIO**: sem ele o llama-server recusa carregar (`quantized V cache requires flash_attn to be enabled`) → crash-loop no boot. Aplicado nos 4 pontos de args (supervisord.conf = runtime real, docker-compose.yml, emerg/lifecycle.go, primary/onstart.go) + `ChatContextCap` 16384→32768 + 3 docs stale. **DEPLOYADO PROD**: gateway `develop-2576e50@sha256:bdd4f8ce` + `PRIMARY_TEMPLATE_IMAGE` `converseai-primary-pod@sha256:b91a844c` no stack 38 (Env 81 vars preservado). Rollback: gateway `develop-b7daed5@sha256:227d8eef`, pod `@sha256:4eaa0fa4`. Validado: sem panic, leadership adquirida, /admin/metrics 200, chat 200. **Ganho é capacidade, NÃO velocidade** (-6% throughput). **VALIDADO no pod real na mesma noite** (3 force-ups de teste): `/props` → n_ctx/slot=32768, request de 20.929 tokens → 200 direto no pod, e lifecycle 371 chegou a READY (LLM+STT+DCGM 200) antes do drain correto por janela. Falhas 369/370 eram defeito de host Vast (auto-reportado), não da imagem. `ChatContextCap` segue INERTE em prod (bug do tokenizer, HANDOFF-tokenizer-fail-open) — ganho vem do pod, não do guard. | 2026-08-24 | dcb6b0e, 2576e50 | [260824-s2t-kv-q8-0-ctx-32k-por-slot-no-pod-primario](./quick/260824-s2t-kv-q8-0-ctx-32k-por-slot-no-pod-primario/) |
| 260821-uhr | **Fase 1 do redesign do ai-dashboard** (direção aprovada 2026-08-21 via mockup, ver memória dashboard-redesign-approved). Identidade nova: tokens dark navy + accent lima, paleta categórica daltonismo-safe (LLM #3d8fe0 / STT #c9791e / externo #9d5fe0 / embed #1f9e6d), fontes Space Grotesk/Inter/JetBrains Mono via next/font/google (self-hosted, zero deps novas). Painéis: seção "Por pod" na Visão geral (3090 primário + secundários, reusa /admin/operations), KPI custo com sparkline real (/admin/economy série diária), latência LineChart→BarChart agrupada P50/P95/P99 por rota, custo/dia ComposedChart, Consumo com top tenants em barras horizontais + donut local-vs-externo + lacuna de billing como `null` visível (connectNulls=false, nunca zero). **Zero mudança no gateway Go** — endpoints existentes cobriram tudo. Gates: tsc limpo, 87 testes vitest verdes, build ok. Deviation: FsmPanel mantido (failover FSM ≠ lifecycle do pod, não redundante). **Validação visual APROVADA pelo Pedro 2026-08-24** (dev server em ops-claude:3001 contra dados reais de prod). **DEPLOYADO PROD 2026-08-24**: develop pusheado (3f7a7fb), CI build-dashboard buildou `develop-3f7a7fb@sha256:f0e93b5a`, stack Portainer 40 atualizado via API (endpoint 6, Env 78 vars completo, PullImage=true). Rollback = `develop-b7daed5@sha256:3cd5d4cc`. Validado em `ai-dashboard.converse-ai.app`: /login 200, rotas protegidas 307, fontes (`--font-space-grotesk`/`--font-inter`/`--font-jetbrains-mono`) e paleta (#3d8fe0/#c9791e/#9d5fe0/#1f9e6d) no CSS de prod, task nova ready em 279ms sem erro. Achado da validação: a lacuna de billing 01–21/ago renderiza como linha em ZERO, não como buraco — os dias existem na série com `phantom_brl=0` (custo Vast existe, só o phantom foi dropado); só os dias sem linha nenhuma (fins de semana, pod down) viram null/buraco. Tratamento explícito (faixa hachurada) fica pra fase 2. Fase 2 (séries de latência/erro no tempo, persistir snapshots) fica pra depois. | 2026-08-22 | a3930f1 | [260821-uhr-dashboard-redesign-fase1](./quick/260821-uhr-dashboard-redesign-fase1/) |
| 260822-secpods | Dashboard passou a mostrar os pods secundários (o 3060 STT/TTS + qualquer pod futuro). Antes só via o primário (3090 LLM, FSM do gateway); o 3060 é gerenciado por script externo (vast3060.py) e era invisível. Design: `ListInstances()` no cliente Vast do gateway + seção `secondary_pods` no `/admin/operations` (lista todas as instâncias Vast da conta exceto o primário ativo, filtro por ActiveInstanceID, cache 60s, dph→BRL server-side, só campos não-secretos, nil-safe) + card "Outros pods" na tela Operação. Read-only. **DEPLOYADO PROD** gateway+dashboard develop-b7daed5 (stacks 38+40). Validado E2E: endpoint retorna 3060 (running, R$0,13/h, up 14h); UI renderiza. GOTCHA evitado: typed-nil `*vast.Client` em interface = panic (main.go passa nil não-tipado quando VAST_AI_API_KEY unset). | 2026-08-22 | b7daed5 | [20260822-dashboard-secondary-pods](./quick/20260822-dashboard-secondary-pods/) |
| 260821-2fa-b | RAIZ REAL do "abriu direto no 2fa, não logou; refazendo o login funciona": cookie de challenge (`two_factor`, 10min, setado pelo /sign-in/email) expirado/ausente ao abrir /2fa/challenge direto → verify retorna `INVALID_TWO_FACTOR_COOKIE` (401), que a UI mostrava como "Código incorreto" e deixava o user preso num código que nunca passa. Fix dashboard develop-4382d25: detecta o code → redireciona `/login?two_factor_expired=1` + banner "verificação expirou, refaça o login". Validado E2E (cookie morto→redirect; login completo→entra). NÃO era skew de relógio (as medições -30s/-210s eram latência copiar↔testar). | 2026-08-21 | 4382d25 | — |
| 260821-2fa | Diagnóstico "2FA do dashboard não funciona": (1) raiz do caso do Pedro = relógio do device ~30s atrasado, na borda do `window=1` do better-auth (hardcoded, não-configurável — fix é sincronizar o device; secret decriptável do banco confirmou skew); (2) BUG rate-limit GLOBAL — Traefik interno do worker-vm sem `forwardedHeaders.trustedIPs` colapsava todo client IP em 10.10.10.30 → bucket de rate-limit 2FA compartilhado (um user bloqueava todos com 429). Fix infra: `trustedIPs=10.10.10.30` em `/etc/worker/traefik-internal.yml` (deployado + validado: access log mostra IP real, 429 de um IP não afeta outro); (3) UI mascarava 429 como "Código incorreto" → fix dashboard develop-4422e72 (429→"aguarde 1min" + dica de relógio). Ver [[traefik-internal-forwardedheaders]]. | 2026-08-21 | 4422e72 (dashboard) + /etc/worker/traefik-internal.yml (infra) | — |
| 260821-dash | Fixes da avaliação do ai-dashboard: (A) `/admin/operations` scheduleSection lia `ParseScheduleEnv` (env morta 9–17/disabled) divergindo da `liveRule()` do reconciler (pod_config 8–19/ativa) → exportado `LiveRule()` + handler usa rec quando não-nil; (B) Histórico de incidentes sempre vazio — primary FSM tinha hook de audit com writer `nil` (main.go) e emerg gravava sem `DataClass` (enum NOT NULL → envenenava batch) → novo `PrimaryFSMAuditAdapter`, kinds `primary_state_change`/`emerg_state_change`. **DEPLOYADO PROD** develop-c20e377 (stack 38). Validado E2E: /admin/operations 8→19/ativa; /incidents com 5 eventos reais (inclusive `operator_force_down:gatewayctl-cli`). | 2026-08-21 | 6f02236, c20e377 | [20260821-dash-schedule-audit-fixes](./quick/20260821-dash-schedule-audit-fixes/) |
| 260821-h9v | llama `--ctx-size` 16384→32768 (16k/slot com `-np 2`) — OPERACOES-26306: análise de chamada 38min (11.2k tokens) estourava 8192/slot com 400 `exceed_context_size_error`. 4 pontos de código (compose, onstart.go, lifecycle.go emerg, **supervisord.conf** — runtime real, fora do handoff) + 3 docs; `ChatContextCap=16384` mantido (agora = limite por slot de fato). **DEPLOYADO PROD:** gateway develop-5f82be3 + pod digest 4eaa0fa4 no stack 38, pod ciclado (lifecycle-366). Guard n8n no PROCESSADOR-IA-GRAVACOES-V3: `Cabe no Pod?` (est_tokens=chars/3 ≤ 14000 → pod; senão AI Agent direto, sem 5 retries de 400); de tabela fallback ganhou `system_message` correto. **Incidente achado no deploy:** `EnsurePartitions` não cobria `billing_events` → partições seed acabaram em 202607 → TODO billing de 01–21/ago dropado (23514); fix manual (partições 202608–202611) + `5f82be3` adiciona à automação. Fecha pendências ctx-size + funil n8n do 260723-sgx. | 2026-08-21 | 18e2910, 5f82be3 | [20260821-llama-ctx-32768](./quick/20260821-llama-ctx-32768/) |
| 260724-ktg | Auto-migração do pod STT/TTS 3060 (Vast spot): `ops/vast-3060/vast3060.py` (Python stdlib, ensure/stop/migrate/status) + state `/var/lib/vast-3060/state.json` + secrets vast-3060.env + units systemd. 8 ensures falhos (~2h bloqueada) → migra: offer ≤$0,035 excl. machine_avoid → cria → modelos → valida direto → flipa stack 38 → valida edge → destrói velha → WhatsApp via Dinastia. **2 drills E2E reais:** falha (38103 CN timeout → bail limpo + avoid) e sucesso (143726 KR $0,0316/h, 6min57s fim-a-fim, velha destruída). Pod atual: 45729612, 58.79.62.163:35327. | 2026-07-24 | 6a07ca8 | [260724-ktg-3060-auto-migrate](./quick/260724-ktg-3060-auto-migrate/) |
| 260723-sgx | STT movido pro pod 3060 dedicado + cascata no override `emergency_pod_stt`. Fix 1: `NewDynamicOverrideSTTProxy` sem `sttRetryableStatusInterceptor` (Phase 22 pousou só no genérico) → 500 do pod (CUDA OOM áudio longo, 17min real reproduzia) vazava cru pro cliente sem cascatear; prepend espelhado + 2 testes. Fix 2: `WHISPER_GPU_THRESHOLD_MIB` 22000→30000 → 1×3090 reporta `whisper_device=cpu` → sem override STT → dispatcher roteia `local-stt` = pod 3060 dedicado (Vast 45646652, $0,0348/h, vetado RTF 33×; `UPSTREAM_STT_URL` flipado no stack 38). **DEPLOYADO PROD** develop-a585956 + bounce primary; validado: 17min 200 em 47s via local-stt, pod up reporta cpu, STT segue local-stt, qwen 200 do pod. Pendências anotadas no SUMMARY (truncamento 128KiB, ctx-size, funil n8n). | 2026-07-23 | a585956 | [260723-sgx-stt-3060-cascata-override](./quick/260723-sgx-stt-3060-cascata-override/) |
| 260704-ubt | Substituir o TTS tier-1 morto (`voice-api-piper`, sumiu do vps-ifix-vm) por `kokoro-tts` — Kokoro-FastAPI OpenAI-compat (`/v1/audio/speech`) rodando no worker-vm (stack Portainer **42** `ai-gateway-tts`, overlay `ai-gateway`, DNS `kokoro-tts:8880`, digest-pinado). Código: config `UPSTREAM_TTS_KOKORO_URL` + `ttsRoleProxies["kokoro-tts"]` via `NewTTSProxy` passthrough (Kokoro fala OpenAI 1:1, sem adapter como piper) + migration **0032** (UPDATE reversível piper→kokoro-tts, mesmo slot `('tts',1)` por UNIQUE(role,tier)). probe.go intocado (branch else OpenAI já cobre non-piper). **DEPLOYADO PROD 2026-07-04:** build `main-c9aba2b` (@sha256:2b3e561), stack 38 digest + env, `gatewayctl migrate up` (v32). **GOTCHA:** Portainer "Env" só injeta se o compose referenciar `${VAR}` no `environment:` — faltava a linha `UPSTREAM_TTS_KOKORO_URL=${...}` no compose do stack 38 → var não chegava no container → 503 "no fallback"; fix = adicionar a linha + redeploy. Validado E2E: `/v1/audio/speech` PT-BR pelo gateway → 200 WAV 24kHz (pod down → tier1 kokoro serve). Probe status "config" (voice=random do probe genérico dá 400 no Kokoro mas não abre breaker — cosmético). Simetria: pod GPU up→Chatterbox tier0, down→Kokoro tier1. Ver [[gateway-consolidated-worker-vm]]. develop unpushed. | 2026-07-04 | b821620 | [260704-ubt-kokoro-tts-upstream](./quick/260704-ubt-kokoro-tts-upstream/) |
| 260704-rkc | STT Gemini MIME sniffing: `extractAudioFromMultipart` forwarded a client-declared generic MIME (`application/octet-stream`, from uploads with no Content-Type — Maestro/WhatsApp) verbatim to Gemini → HTTP 502 `INVALID_ARGUMENT: Unsupported MIME type` → transcription broke. Added `sniffAudioMIME(audio []byte)` (magic bytes: OggS→audio/ogg, RIFF+WAVE→audio/wav, fLaC→flac, EBML→webm, ftyp→mp4, ID3/0xFFFB→mpeg, else audio/wav); `extractAudioFromMultipart` now trusts declared only if `audio/*`, else sniffs. Reproduced live (opus/octet-stream 502→200) + unit tests (11 sniff subtests + 3 extract cases). **DEPLOYED PROD 2026-07-04:** built on worker-vm, pushed `ghcr.io/ifixtelecom/ifix-ai-gateway:main-08b0481` (@sha256:51bcfa2…), updated Portainer stack 38 digest + redeploy (endpoint 6); validated live — wav+opus octet-stream now 200, audio/* passthrough 200. Ver [[gateway-consolidated-worker-vm]]. develop unpushed. | 2026-07-04 | 05b742a | [260704-rkc-stt-gemini-mime-sniff](./quick/260704-rkc-stt-gemini-mime-sniff/) |
| 260702-nse | Primary pod provision policy: cheapest open-market nas 2 primeiras tentativas, allowlist-first só da 3ª em diante. Novo `failStreak` = run contíguo de lifecycles falhos (`first_health_pass_at IS NULL AND ended_at IS NOT NULL`) desde o último sucesso, via nova query sqlc `CountConsecutiveFailedPrimaryProvisions` (window ROW_NUMBER, exclui a row aberta, COALESCE sentinel). `provisionLifecycle` gate: allowlist pass só roda com `failStreak >= 2 && len(allowlist) > 0`; `failStreak < 2` → market full (blocklist-only, dph asc, cheapest). `fail_streak`+`mode` (market_cheapest/allowlist_preferred) logados nos 3 offer-pick events. DB-error → failStreak 0 (nunca bloqueia provisão). Motivo: PROD fixava host 7970 $0.219 via allowlist quando mercado tinha 1×3090 ~$0.136 (self-vet dos baratos, fallback confiável na 3ª). TDD: 3 gate tests (RED provado) + 28 pkg green. Integration (`-tags integration`) verde no CI. **DEPLOYADO PROD 2026-07-02:** FF main→develop (5553bd4) → CI buildou :main + push GHCR; webhook Portainer não puxou (gotcha) → pull+recreate manual; migrations 0030+0031 estavam Pending no prod (Phase 17 nunca migrou prod) → `gatewayctl migrate up` aplicou → gateway healthy, version=main-5553bd4, pod_config seedado do env (allowlist={7970,12863}). Política vale no próximo provision (pod dorme seg-sex fora 9-17 BRT). Também corrigido: 3 testes migration Down-count off-by-one por 0031 HEAD (commit 5553bd4, pré-existente do Phase 17). Ver [[gateway-prod-build-deploy]]. | 2026-07-02 | 5553bd4 | [260702-nse-primary-pod-provision-cheapest-market-na](./quick/260702-nse-primary-pod-provision-cheapest-market-na/) |
| 260617-nrk | Fix 3 migration integration tests blocking build-gateway on main. Migration 0030 (probe_status_allow_config) added a new HEAD; the 0026/0029 round-trip tests hard-code `db.Down(n)` step counts relative to old HEAD (0029) → each Down walk off by one (reverted 0030 instead of target boundary). 0030 Down is row-neutral (CHECK constraint swap on upstreams), so bumped each count +1: 0026 UpDownUp Down(2)→Down(3); 0026 DownAborts Down(4)→Down(5); 0029 Down_Symmetric Down(1)→Down(2). 7/7 integration tests green locally. Unblocked `:main` build (30f4c81) → deployed prod gateway → PRIMARY_POD_SERVE_STT=false now live → STT routes to gemini (2.04s, HTTP 200) instead of pod CPU whisper (~17s timeout). | 2026-06-17 | c3655f7 | [260617-nrk-migration-down-counts](./quick/260617-nrk-migration-down-counts/) |
| 260617-jod | SEED-018: gateway STT model-rewrite — `/v1/audio/transcriptions` sent literal `model=whisper` to speaches (local-stt + primary-override paths) → 404 "Model 'whisper' not installed" whenever pod Ready. Reused existing `rewriteMultipartModelViaResolver`/`BuildOpenAIWhisperDirector`: `NewAudioProxy` now takes resolver + rewrites via `local-stt` target (no bearer); new `NewDynamicOverrideSTTProxy` resolves override against tier-0 `local-stt` (synthetic `emergency_pod_stt` not in model_aliases). +373 test lines (both paths). build+`go test ./internal/...` green; openai-whisper/groq/gemini/WhisperAbortGuard unchanged. | 2026-06-17 | c22b482 | [260617-jod-rewrite-stt-model-alias-to-upstream-targ](./quick/260617-jod-rewrite-stt-model-alias-to-upstream-targ/) |
| 260515-ayc | Fix STATE.md corruption (heading `## Current Position` duplicado injetado no meio do Phase 6.5 bullet) | 2026-05-15 | f44cf11 | [260515-ayc-fix-state-md-corruption-linha-40-42-tem-](./quick/260515-ayc-fix-state-md-corruption-linha-40-42-tem-/) |
| 260516-rym | Fix handleForceProvision não trata FSM em cooldown — pod Vast.ai órfão queimava $$ quando operator force-provision após falha. Precheck FSM.State() + SetState(EmergencyProvisioning) em vez de 2x Transition. +2 regression tests. | 2026-05-16 | 5aec0eb | [260516-rym-fix-force-provision-cooldown-transition](./quick/260516-rym-fix-force-provision-cooldown-transition/) |
| 260527-wgs | Fix primary reconciler silent-exit on transient ErrInstanceNotFound. waitForReadyOrDestroy now uses 3-strike confirm pattern + BestEffortDestroy on confirmed terminal (orphan Vast pod prevention). +2 regression subtests. Closes carry-forward critical bug from .planning/debug/primary-reconciler-silent-hang.md; unblocks Phase 11 11-06/07 live UATs. | 2026-05-27 | 01e7558 | [260527-wgs-fix-primary-reconciler-silent-terminal](./quick/260527-wgs-fix-primary-reconciler-silent-terminal/) |
| 260528-h12 | SEED-005: breaker.Set.EffectiveStateSnapshot() emits "forced-open" when Redis force-override is active; /v1/health/upstreams switched off raw Snapshot() so health surface matches dispatcher routing (EffectiveState). 4 RED/GREEN commits, 5 new sub-tests. Unblocks smoke-sensitive-failover.py ensure_tier0_open pre-condition on force-open-driven test beds. | 2026-05-28 | 7d0b345 | [260528-h12-breaker-snapshot-honor-force-override](./quick/260528-h12-breaker-snapshot-honor-force-override/) |
| 260528-h8z | SEED-006: scripts/integration-smoke/smoke-sensitive-failover.py query_audit refactored into bounded-deadline poll wrapper (5s/250ms) around new _query_audit_once helper. Stops racing audit-writer 1s flush timer; honest gates against correct gateway. Single-file blast, +74/-22 LOC, schema unchanged, call site source-compatible via defaults. T-09-07/T-09-09 contracts preserved verbatim. | 2026-05-28 | 2d30a60 | [260528-h8z-smoke-audit-query-retry-loop](./quick/260528-h8z-smoke-audit-query-retry-loop/) |
| 260614-l0t | Fix login redirect 2FA no dashboard: login/page.tsx ignorava data.twoFactorRedirect → push("/") ricocheteava pra /login?session_expired=1, nunca chegava em /2fa/challenge. handleSubmit agora captura o sinal via onSuccess(context).data.twoFactorRedirect (literal data.* não typecheck em better-auth 1.4.22) e roteia pra /2fa/challenge. +3-case regression test. tsc limpo, 34 testes pass. | 2026-06-14 | e054125 | [260614-l0t-fix-login-redirect-bug-dashboard-ignora-](./quick/260614-l0t-fix-login-redirect-bug-dashboard-ignora-/) |
| 260614-ny5 | middleware.ts redirecionava no-session pra /login?session_expired=1 (banner "Sessão encerrada" em toda primeira visita). Stage 1 agora redireciona pra /login limpo (param removido; Edge não distingue never-logged de expired). Banner em login/page.tsx mantido (só não é mais disparado pelo middleware). +test case (a) atualizado. tsc limpo, 7/7. | 2026-06-14 | 3508773 | [260614-ny5-middleware-redirect-login-limpo-sem-sess](./quick/260614-ny5-middleware-redirect-login-limpo-sem-sess/) |
| 260614-ov8 | Fix bounce 2FA: middleware lê twoFactorVerified do cookieCache (maxAge 60s) mas dashboard nunca chama get-session (useSession só em first-login, poll vai pra /api/gateway) → cache expira, fallback pessimista → /2fa/challenge loop após 60s. auth.ts cookieCache.maxAge 60→1800 (= session idle 30min); cache vive a sessão. twoFactorVerified monotônico (staleness inócuo); tradeoff: revogação propaga em até 30min. +comments/RUNBOOK. tsc limpo, 34 testes. | 2026-06-14 | f1b9acd | [260614-ov8-fix-2fa-bounce-raise-cookiecache-maxage-](./quick/260614-ov8-fix-2fa-bounce-raise-cookiecache-maxage-/) |
| 260625-v04 | Tier 2 dashboard "Operação": saúde operacional do pod. Backend: endpoint agregado GET /admin/operations (gateway/internal/admin/operations.go) expondo fsm (primary+emerg state, leader, active lifecycle) + schedule (up/down hour, next_transition) + lifecycles[] (ListPrimaryLifecycles, últimos N) + breakers[] (breakerSet.EffectiveStateSnapshot) + vast_cost (today/month_brl + budget 2400 + pct, agregação Go com accrual ao-vivo da lifecycle aberta). Reconciler.Snapshot() getter lockless. Rota 1-linha chi + X-Admin-Key herdado. Frontend: página /operacao + 4 painéis (FSM/schedule, lifecycle timeline, breaker badges semânticas, custo Vast+budget bar) + fetchOperations + sidebar. Economia vs OpenRouter DEFERIDA (phantom é tenant-scoped, precisa query nova). Gates: gofmt/go build/go test ./internal/admin + npm build (/operacao 5.61kB). Deploy: gateway rebuild (sha 31fb625) + dashboard rebuild, ambos compose up, rollback tags rollback-pre-tier2. Smoke: /admin/operations retorna fsm/schedule/50 lifecycles/9 breakers/vast_cost. PENDENTE follow-up: vast_cost=R$0 (lifecycle cost_brl histórico=0, deve popular em runs novos com fx setado) + schedule.next_transition=null (checar serialização NextTransition no handler). | 2026-06-25 | 7f8f3ed | [260625-v04-tier-2-dashboard-pagina-operacao-saude-o](./quick/260625-v04-tier-2-dashboard-pagina-operacao-saude-o/) |
| 260625-sna | Dashboard nova página /consumo (Tier 1 — melhorar dados): custo/uso AGREGADO de todos os tenants com dado real do /admin/usage. /admin/usage exige tenant (sem agregado) → agregação client-side: fetchMetrics().tenants dedup → Promise.allSettled(fetchUsage) → soma (tolera falha parcial). Entregue: lib/consumo.ts (aggregateSummary/aggregateDaily/perTenantRows), sidebar link, consumo-trend-chart.tsx (recharts dual-Y: tokens/dia + custo R$/dia), consumo-table.tsx (por tenant, ordenável por custo), app/(dashboard)/consumo/page.tsx (KPIs Custo R$/tokens/áudio/embeds + filtro período month-to-date reusando popover+calendar, isoDate America/Sao_Paulo). Honesto: audio/embeds 0 (metering não grava), custo só em tráfego novo — renderiza como vem. npm run build exit 0 (/consumo 4.74kB). Deploy prod (rebuild img latest-dev + compose up, rollback-260625-sna). Frontend only; dado vem do price-sync 260625-s3i. | 2026-06-25 | 277aa23 | [260625-sna-dashboard-nova-pagina-consumo-tier-1-cus](./quick/260625-sna-dashboard-nova-pagina-consumo-tier-1-cus/) |
| 260625-s3i | Job diário sync preços OpenRouter → ai_gateway.prices (custo dashboard era R$0). Causa: lookup é match exato {model,provider,unit}; pod grava usage.Model()=`model.gguf` verbatim, mas prices só tinha seed `qwen3.5-27b` → sem match → cost_local_phantom_brl=0. `scripts/price-sync/gateway-price-sync.sh` (ops-claude): fetch live OpenRouter pricing (/api/v1/models .pricing.prompt/.completion) + forex USD/BRL (open.er-api.com) → `gatewayctl prices set/-fx`, fail-safe (is_pos_number guard, API down não sobrescreve), idempotente (auto-expire), DRY_RUN, token via EnvironmentFile. MODEL_MAP keyed `model.gguf`+deepseek-v4-flash+qwen+llama. systemd user timer seg-sex 08:30 BRT (antes pod 09:00). Deploy+smoke live: prices set OK, request billed → cost_local_phantom_brl 0→8e-05 (math confere). Timer ativo, next sex 08:30. Escopo: só o dado; UI Consumo (Tier 1) depois. | 2026-06-25 | 74c67b9 | [260625-s3i-job-diario-sync-precos-openrouter-para-t](./quick/260625-s3i-job-diario-sync-precos-openrouter-para-t/) |
| 260625-k17 | Dashboard sessão 30min → 7 dias + trust-device 30d no 2FA. Sucessor do 260614-ov8: o 30min ainda forçava relogin+bounce /2fa/challenge porque o dashboard só bate /api/gateway (nunca /api/auth) → sessão não renova → expira em 30min. auth.ts: expiresIn 30*60→7*24*60*60 + cookieCache.maxAge 1800→7*24*60*60 (casados, evita bounce mid-session no Edge); challenge/page.tsx: verifyTotp({code, trustDevice:true}) pula TOTP ~30d/navegador. Tradeoff: revogação propaga ao Edge em até 7d (imediato via DELETE public.session). Build verde, deploy prod (rebuild img latest-dev + compose up; rollback tag rollback-260625). | 2026-06-25 | 7b884a5 | [260625-k17-dashboard-sessao-better-auth-30min-para-](./quick/260625-k17-dashboard-sessao-better-auth-30min-para-/) |
| 260616-gtj | Fix tier-1 probe false-negative: probe.go gravava last_probe_status="failed" pra 4xx mesmo o breaker tratando 4xx como sucesso (D-A4). probeOne agora extrai *breaker.HTTPError via errors.As → 4xx vira status="config" (não health-failure, não bumpa ProbeFailureTotal); 5xx→failed, timeout→timeout, 2xx→ok inalterados. Escolhido "config" (zero blast radius: /v1/health/upstreams deriva do breaker state, nenhum consumer faz branch na string). Resolve openrouter-chat reportando failed enquanto serve 200 (provado live: deepseek/Novita). +TestProbe TDD RED/GREEN. build+test verde. Contexto: 12-FIELD-FINDINGS-2026-06-16 finding 2; classe SEED-012 tier-1. | 2026-06-16 | 844bf2a | [260616-gtj-fix-tier-1-probe-false-negative-status-4](./quick/260616-gtj-fix-tier-1-probe-false-negative-status-4/) |
| 260620-o7d | Fix TTS health prober per-upstream: `dispatch` `case "tts":` hardcodava `POST /v1/audio/speech` (OpenAI body) pra todo tts upstream. Tier-1 `voice-api-piper` só serve `POST /tts` `{"text":...}` → probe 404 → status `config` (falso). Branch em `u.Name` (único discriminador in-scope; `UpstreamConfig` não expõe `UrlEnv`): `voice-api-piper`→`/tts` body Piper (espelha `piperTTSAdapter`), demais→`/v1/audio/speech` inalterado. 2xx Piper→`ok`. +`piper_posts_tts`/`piper_trailing_slash` subtests; `primary-tts` OpenAI-path intactos. build+vet+gofmt+test verde. Contexto: prod URL fix (172.18.0.1→10.10.10.30) já resolveu 503 live; este fix tira o status `config` enganoso. Deploy: precisa GHCR push + redeploy gateway n8n-ia-vm. | 2026-06-20 | 639e4c7 | [260620-o7d-fix-tts-health-prober-per-upstream-probe](./quick/260620-o7d-fix-tts-health-prober-per-upstream-probe/) |
| Phase 06.7 P02 | 10m | 2 tasks | 7 files |
| Phase 06.7 P03 | 25m | 2 tasks | 9 files |
| Phase 06.7 P04 | ~20m | 2 tasks | 8 files |
| Phase 06.7 P05 | 4 | 2 tasks | 4 files |
| Phase 11.2 P06 | 35min | 3 tasks | 8 files |
| Phase 11.2 P07 | 8m | 2 tasks | 3 files |
| Phase 11.2 P08 | 2h 20min | 3 tasks | 10 files |
| Phase 17 P01 | 20min | 2 tasks | 9 files |
| Phase 17 P02 | 25min | 2 tasks | 5 files |
| Phase 17 P03 | 45min | 3 tasks | 9 files |
| Phase 17 P04 | 35min | 3 tasks | 7 files |
| Phase 17 P05 | 12min | 2 tasks | 7 files |
| Phase 17 P06 | 30min | 3 tasks | 7 files |
| Phase 19 P01 | 5m 4s | 3 tasks | 1 files |
| Phase 19 P06 | 50min | 3 tasks | 3 files |
| Phase 18 P01 | 5 min | 3 tasks | 5 files |

## Session Continuity

- **Last session:** 2026-07-25T19:36:40.915Z
- **Next session should:** Execute Phase 12 Plan 04 Task 2 — the **BLOCKING human-verify dev chaos UAT**. Plan 12-04 Task 1 is DONE (commit `431f351`: `12-04-DEV-CHAOS-UAT.md` — 5-scenario dev chaos sheet S1-S5 with Vast-credit + tier-1/OpenRouter preflight + price-first pod selection D-17). **Task 2 is a BLOCKING human-verify checkpoint** — operator must run the live dev chaos kill against `ai-gateway-dev` (vps-ifix-vm) per `12-04-DEV-CHAOS-UAT.md`: record PF-1 (Vast credit) + PF-2 (tier-1/OpenRouter health) + PF-3 (new image digest) BEFORE the kill; provision cheapest qualified pod; drive ~20-concurrency load incl. one sensitive stream; `bash scripts/chaos/vast-delete.sh` (set `GATEWAYCTL_SSH=vps-ifix-vm` + `GATEWAY_BASE_URL=dev`); sign S1 (RES-11 death detection), S2 (RES-13 zero connection-class 502 via audit_log, cross-ref PF-2), S3 (RES-08/D-10 sensitive 503), S4 (RES-12 health truth + D-13 force-close), S5 (cleanup count=0 + spend). Real Vast spend + destructive kill → autonomous mode cannot satisfy it. After all S1-S5 signed PASS + preflight recorded: type "dev-chaos approved", then write `12-04-SUMMARY.md` + advance. Any FAIL → `/gsd:plan-phase 12 --gaps`. Phase 12 Plan stays at 04 (NOT advanced — plan incomplete until UAT signed).

---

*State created: 2026-04-17*
*State repaired against disk: 2026-05-14*

## Decisions

- [Phase 06.7]: Wave 0 test stubs are engine-agnostic on the Go side (assert tts ROLE plumbing — probe path, tier-0 map, route class, re-assert); engine-specific only in the pod pytest (Chatterbox 24kHz, zero-shot audio_prompt_path, no .pt)
- [Phase 06.7]: Every Wave 0 test stub carries an OWNER annotation + skip reason naming the downstream plan (03/05/07/08) that must unskip + implement it (Codex consensus action #3 — a skipped stub satisfies compile-time scaffolding but does not fail if implementation never happens)
- [Phase 06.7]: 06.7-03: tier-0 dynamic-override roster {llm,stt,embed}->{llm,stt,tts} — tts dynamic (D-11), embed static (D-03); added Loader.Tier0OverrideURL getter for Plan 08 re-assert (D-13)
- [Phase ?]: [Phase 06.7]: 06.7-04 voices.tenant_id = UUID FK->tenants(id) ON DELETE CASCADE, matching the verified ai_gateway schema convention (api_keys/usage_counters/billing_events) over the PATTERNS TEXT placeholder; no .pt column (Chatterbox zero-shot D-08)
- [Phase ?]: Phase 11.2 Plan 07 — created docs/RUNBOOK-OPS.md as canonical STT cascade operator playbook
- [Phase ?]: Phase 11.2 Plan 07 — gateway/.env.example created mirroring pod/.env.example parity
- [Phase 14]: 14-02 — pod self-decides whisper device at onstart via nvidia-smi total-VRAM sum (awk, no bc): >=30000 MiB → cuda pinned to the max-free GPU index (WHISPER__DEVICE_INDEX; llama --split-mode layer leaves no Qwen-free card so max-free, not non-Qwen, is the headroom pick); below threshold / nvidia-smi absent → cpu. Exports precede exec supervisord so [program:speaches] inherits the device (supervisord.conf:46 cpu pin removed, HF_HUB_CACHE kept — env inheritance Option A, fail-open). Minimal python3 :9100 /whisper_device responder (NOT health-bridge — that's compose-only) + -p 9100:9100 forwarded; fulfills the Plan 14-01 device-report contract. Image rebuild + live UAT gated in 14-03.
- [Phase ?]: pod_config columns all NOT NULL — env seed always supplies all 36 values; no optional knobs
- [Phase ?]: Generated model is gen.AiGatewayPodConfig (schema-qualified), not gen.PodConfig — downstream 17-02..06 must use it
- [Phase ?]: Phase 17-02: podconfig.ScheduleRule is a local data-only mirror (not a primary import) to keep podconfig import-cycle-free — package primary imports podconfig in 17-03
- [Phase ?]: 17-03: reconciler reads 16 HOT pod_config fields from live snapshot; edits apply in seconds without restart (POD-CFG-04)
- [Phase ?]: 17-04: three X-Admin-Key admin endpoints (lifecycle read, config read, config PATCH write); config-read reads pod_config not boot-env; write uses static field->query allowlist + live-bound validation; no restart/structural path
- [Phase ?]: 17-05: dashboard pod-config write path — updatePodConfig/updatePodConfigBound mirror inviteOperator (requireOwner FIRST → refetch live config → validate vs refetched bound → gateway PATCH → 1 audit row {field,old,new}); admin key isolated in server-only gateway-admin.ts (2nd legit reader, key-leak guard test)
- [Phase ?]: 17-05: vitest alias server-only → next compiled empty.js so import 'server-only' resolves in tests (no new package installed)
- [Phase ?]: 17-06: RSC reads pod config server-side via fetchPodConfigServer (absolute self-proxy) — no new admin-key reader; structural fields render read-only (not pod_config columns)
- [Phase 19]: Portainer swarm-string create route confirmed = POST /api/stacks/create/swarm/string?endpointId=6 (legacy ?type=1 is 405); endpoint-6 swarmID=wg4ns7gcgbf0lygbmah5k3vxv; env vars go in body env[]
- [Phase 19]: worker-vm internal Traefik = docker/swarmMode watching worker_intra, entrypoint web:80, needs deploy.labels; ai-gateway overlay created (attachable); infra-redis-1 alias proven (PONG)
- [Phase 19]: 19-06: gateway consolidation COMPLETE — worker-vm is the single ai-gateway; n8n-ia-vm prod stacks + vps-ifix-vm dev stack 34 decommissioned (volumes/dirs/bd_ai_gateway_prod retained); ops timers unmasked+repointed at worker-vm; rollback=rebuild via ~/gw-decomm-archive-19
