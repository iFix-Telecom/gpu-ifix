---
phase: 06-emergency-pod-template-refactor
plan: 05
type: execute
wave: 3
depends_on: [06-04]
files_modified:
  - gateway/internal/integration_test/emerg_leader_test.go
autonomous: true
requirements: [PRV-06, PRV-01, PRV-05, PRV-07, PRV-08, PRV-09, PRV-10]
threat_refs: [T-06-02]
validates_sc: [SC-1, SC-3, SC-4]
tags: [integration-test, refactor, phase-6-pr1]

must_haves:
  truths:
    - "Linha 47 de `emerg_leader_test.go` (atual `cfg.EmergencyPodImageTag = \"v1.0\"`) substituida por `cfg.EmergencyTemplateImage = \"ghcr.io/ggml-org/llama.cpp:server-cuda-b9128\"`"
    - "Build do pacote integration_test GREEN apos mudanca (sem erros de referencia a campo removido)"
    - "Suite emerg integration tests completa (22 tests per STATE.md:42) GREEN em CI / local run"
    - "Sem outras ocorrencias de `EmergencyPodImageTag`/`ifix-ai-pod`/`EMERGENCY_POD_IMAGE_TAG` em qualquer arquivo `.go` do gateway/ (grep gate)"
  artifacts:
    - path: "gateway/internal/integration_test/emerg_leader_test.go"
      provides: "Test fixture aligned with Strategy B Config (EmergencyTemplateImage)"
      contains: "EmergencyTemplateImage.*ghcr.io/ggml-org/llama.cpp:server-cuda-b9128"
  key_links:
    - from: "gateway/internal/integration_test/emerg_leader_test.go"
      to: "gateway/internal/config/config.go (Cfg.EmergencyTemplateImage)"
      via: "cfg.EmergencyTemplateImage assignment in test fixture"
      pattern: "cfg\\.EmergencyTemplateImage\\s*="
---

<objective>
Plan 06-05 fecha o ultimo arquivo Go que ainda referencia `Cfg.EmergencyPodImageTag` apos plans 06-02 (config) e 06-04 (lifecycle): `gateway/internal/integration_test/emerg_leader_test.go:47` (per PATTERNS.md linha 401 — grep survey VERIFIED em 06-01). Sem este fix, a suite integration_test/emerg_* NAO compila apos merge dos plans Wave 1+2.

Adicionalmente, este plano roda a suite inteira de emerg integration tests (22 testes — STATE.md:42) com payload Strategy B novo para detectar regressao silenciosa em qualquer assertion que dependa de Runtype=ssh ou image=ifix-ai-pod (Pitfall 7 RESEARCH.md:456 — Wave 0 grep ja confirmou nao ha mais ocorrencias, mas re-validation post-merge eh barato).

Per PATTERNS.md linha 428 ("Não há ocorrências em outros emerg_*_test.go integration tests (Wave 0 survey confirmed via grep -rln)"), este plano so toca 1 arquivo. Se o grep survey de plan 06-01 / Task 3 retornar contagem >6 arquivos (surpresa), este plano expande escopo (defensive — investiga e atualiza analogous fixtures).

Purpose: Wave 3 single-plan porque touche 1 arquivo de teste; rapido. Eh o final gate de PR1 antes do HUMAN-UAT live (06-06). Quando este plano passa, PR1 esta pronto pra merge tecnico — falta so UAT live.

Output: 1 arquivo modificado (~1 linha de diff). Zero novos arquivos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-emergency-pod-template-refactor/06-CONTEXT.md
@.planning/phases/06-emergency-pod-template-refactor/06-PATTERNS.md
@.planning/phases/06-emergency-pod-template-refactor/06-VALIDATION.md

<interfaces>
<!-- emerg_leader_test.go atual (linha ~33-50 defaultTestCfg) -->

```go
func defaultTestCfg(t *testing.T) config.Config {
    // ... setup helper que preenche Config com defaults pra integration tests
    cfg := config.Config{ /* ... */ }
    cfg.EmergencyPodImageTag = "v1.0"   // LINHA 47 atual — DELETAR
    // ... outros campos
    return cfg
}
```

<!-- Pos-refactor (1 linha trocada) -->

```go
cfg.EmergencyTemplateImage = "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128"
```

<!-- Plan 06-04 ja garantiu que TestBuildCreateRequest passa unit-level.
     Integration tests usam buildCreateRequest indiretamente (e.g.,
     emerg_provision_happy_test.go simulou Vast API responses) — mas
     fake Vast client devolve InstanceID hardcoded, sem inspecionar
     o payload. Portanto Wave 0 survey nao encontrou outras assertions
     contra image/runtype. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar emerg_leader_test.go + rodar suite integration_test/emerg_*</name>
  <files>gateway/internal/integration_test/emerg_leader_test.go</files>
  <read_first>
    gateway/internal/integration_test/emerg_leader_test.go (linhas 33-65 — defaultTestCfg ou helper equivalent)
    .planning/phases/06-emergency-pod-template-refactor/06-PATTERNS.md (lines 396-428 emerg_leader_test pattern + final survey)
    .planning/phases/06-emergency-pod-template-refactor/06-WAVE0-GATES.md (grep survey count confirma escopo)
  </read_first>
  <action>
    1. `grep -n "EmergencyPodImageTag" gateway/internal/integration_test/emerg_leader_test.go` — confirma 1 hit na linha 47 (per Wave 0 survey). Se >1 hit ou linha differente, investigar antes de aplicar fix.
    2. Substituir essa unica linha de `cfg.EmergencyPodImageTag = "v1.0"` por `cfg.EmergencyTemplateImage = "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128"`.
    3. Verificar imports do arquivo: se ja importa `config "github.com/.../config"`, OK. Se nao, adicionar.
    4. Rodar grep de seguranca em todo o diretorio de integration tests:
       ```
       grep -rln "EmergencyPodImageTag\|ifix-ai-pod" gateway/internal/integration_test/
       ```
       Esperado: ZERO hits. Se hits aparecem, PATTERNS.md analog scope foi underestimated — investigar e estender escopo.
    5. Rodar suite emerg integration tests local (rapido pra catch issues pre-CI):
       ```
       cd gateway && go test ./internal/integration_test/ -run TestEmerg -count=1 -timeout 120s
       ```
       Esperado: 22 testes GREEN (per STATE.md linha 42 — "All 22 emerg integration tests GREEN in CI run 25891568768"). Se algum falha por payload assertion, esse teste tem hardcoded `Runtype="ssh"` ou similar — investigar + estender escopo do plano.
    6. NAO modificar outros files. Se algum teste falha, abrir issue/quick-task; nao bypass.
    7. Commit message: `test(06-05): refactor — emerg_leader_test uses EmergencyTemplateImage (Strategy B)`
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/integration_test/ -run TestEmerg -count=1 -timeout 120s 2>&1 | tail -20 | grep -E '^(ok|FAIL|---)' | tee /tmp/06-05-emerg-int.log; ! grep -q FAIL /tmp/06-05-emerg-int.log</automated>
  </verify>
  <done>
    `go test ./internal/integration_test/ -run TestEmerg -count=1 -timeout 120s` mostra `ok` ao final (sem `FAIL`). `grep -rln 'EmergencyPodImageTag\|ifix-ai-pod' gateway/internal/integration_test/` retorna 0 arquivos. `grep -rln 'EmergencyPodImageTag\|ifix-ai-pod' gateway/ --include='*.go'` retorna 0 arquivos (Wave 0 promise cumprida).
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-suite gate + grep-final certifica PR1 ready</name>
  <files></files>
  <read_first>
    .planning/phases/06-emergency-pod-template-refactor/06-VALIDATION.md (lines 30-32 Sampling Rate full-suite)
  </read_first>
  <action>
    Esta task NAO modifica arquivos. Eh um gate de qualidade rodando:
    1. Build inteiro do gateway: `cd gateway && go build ./...`
    2. Suite inteira do gateway (curta, sem -race pra ser rapido): `cd gateway && go test ./... -count=1 -short -timeout 180s`
    3. Grep certificacao final em todo o repositorio (nao so gateway/):
       ```
       grep -rln 'EmergencyPodImageTag\|"EMERGENCY_POD_IMAGE_TAG"' gateway/ --include='*.go'
       grep -rln '^EMERGENCY_POD_IMAGE_TAG=' gateway/ --include='*.env*' --include='*.dev' --include='*.example'
       ```
       Ambos devem retornar 0 arquivos.
    4. Documentar resultado dos 3 comandos em arquivo `06-05-SUMMARY.md` (proximo task de execucao do SUMMARY).
    5. Se qualquer comando falha: nao prosseguir para Wave 4 (HUMAN-UAT). Abrir gap-closure plan via `/gsd:plan-phase --gaps`.
  </action>
  <verify>
    <automated>cd gateway && go build ./... && go test ./... -count=1 -short -timeout 180s 2>&1 | tail -10 | grep -q '^ok\b' && ! grep -rn 'EmergencyPodImageTag' gateway/ --include='*.go' && ! grep -rn '^EMERGENCY_POD_IMAGE_TAG=' gateway/ --include='*.env*' --include='*.dev' --include='*.example'</automated>
  </verify>
  <done>
    Full build GREEN, full short-test suite GREEN, 0 grep hits em todo gateway/ (Go + env files). PR1 esta tecnicamente pronto pra merge — falta apenas HUMAN-UAT live (plan 06-06).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test (local DB testcontainer) → gateway code | Fake Vast client devolve mock responses (T-06-02 mitigated by deterministic mocks) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | Integration test fake Vast responses | accept | Fake Vast client em integration tests (Phase 6.5 pattern) nao inspeciona Runtype/Image/Args field na assertion default — devolve `Instance{ID: 999}` hardcoded. Risco residual: se algum teste novo do plan 06-04 adicionou assertion via JSON unmarshal, esse teste poderia falhar. Mitigado por Task 2 full-suite gate |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Plan 06-05 nao instala packages |
</threat_model>

<verification>
- `cd gateway && go test ./internal/integration_test/ -run TestEmerg -count=1 -timeout 120s` GREEN
- `cd gateway && go build ./...` GREEN
- `cd gateway && go test ./... -count=1 -short -timeout 180s` GREEN
- `grep -rn 'EmergencyPodImageTag' gateway/ --include='*.go'` 0 hits
- `grep -rn '^EMERGENCY_POD_IMAGE_TAG=' gateway/ --include='*.env*' --include='*.dev' --include='*.example'` 0 hits
</verification>

<success_criteria>
- emerg_leader_test atualizado para `EmergencyTemplateImage` field
- Suite emerg integration tests (22 testes) GREEN
- Build inteiro gateway GREEN; short-test suite GREEN
- Zero referencias ao campo/env var antigo em todo o gateway/
- PR1 tecnicamente ready — apenas HUMAN-UAT live falta (plan 06-06)
</success_criteria>

<output>
Create `.planning/phases/06-emergency-pod-template-refactor/06-05-SUMMARY.md` when done. Resumir: 1 linha de diff, suite GREEN counts, grep certifications, gate status para Wave 4.
</output>