---
phase: 06-emergency-pod-template-refactor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/06-emergency-pod-template-refactor/06-SPIKE-runtype-args.md
  - .planning/phases/06-emergency-pod-template-refactor/06-WAVE0-GATES.md
autonomous: false
requirements: [PRV-06]
threat_refs: [T-06-04]
validates_sc: [SC-3]
tags: [spike, wave-0, vast-ai, runtype-args, jinja-template]
user_setup:
  - service: vast.ai
    why: "Provisionar 1 pod manual ~10min para validar empíricamente Strategy B (runtype=args + ghcr.io/ggml-org/llama.cpp:server-cuda-b9128) ANTES de implementação"
    env_vars:
      - name: VAST_AI_API_KEY
        source: "CLAUDE.md token store ja configurado em ops-claude"
    dashboard_config:
      - task: "Confirmar saldo Vast.ai >= $1 (custo spike ~$0.10)"
        location: "https://cloud.vast.ai/billing"
  - service: minio
    why: "Upload qwen3.5-27b-tool-calling.jinja para MinIO (D-04-B opcao B2) com sha256 fixo"
    env_vars:
      - name: MINIO_ACCESS_KEY
        source: "CLAUDE.md token store - HjEgnpAOTlKXBRW5Ggdo"
      - name: MINIO_SECRET_KEY
        source: "CLAUDE.md token store"

must_haves:
  truths:
    - "Spike confirma empiricamente que ghcr.io/ggml-org/llama.cpp:server-cuda-b9128 + Runtype=args + Args=[--host 0.0.0.0 --port 8000 --version] funciona em pod Vast.ai 4090"
    - "Spike confirma que llama-server eh PID 1 quando Runtype=args (crash detection clean - Pitfall 3 RESEARCH.md:414)"
    - "Spike confirma se onstart roda dentro do container quando Runtype=args (Open Question 4 RESEARCH.md:624)"
    - "Operator decidiu Jinja strategy B1 (image overlay) ou B2 (MinIO fetch); decisao em 06-WAVE0-GATES.md"
    - "Se B2: Jinja qwen3.5-27b-tool-calling.jinja uploaded em MinIO key `emerg-onstart/templates/qwen3.5-27b-tool-calling-1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67.jinja` com sha256 confirmado"
    - "Grep survey confirma escopo exato de arquivos que referenciam EmergencyPodImageTag / EMERGENCY_POD_IMAGE_TAG / ifix-ai-pod (Pitfall 7 RESEARCH.md:456)"
  artifacts:
    - path: ".planning/phases/06-emergency-pod-template-refactor/06-SPIKE-runtype-args.md"
      provides: "Empirical evidence Strategy B viability"
      contains: "Spike Result|PID 1|onstart location|cache-hit"
    - path: ".planning/phases/06-emergency-pod-template-refactor/06-WAVE0-GATES.md"
      provides: "Operator decisions: Jinja strategy B1 vs B2, Jinja sha256, Disk filter"
      contains: "Jinja Strategy|sha256|Disk"
  key_links:
    - from: "spike pod evidencia"
      to: "Strategy B Locked (CONTEXT.md D-01-B..D-08-B)"
      via: "manual UAT result documented"
      pattern: "Strategy B (.*confirmed|.*viable)"
---

<objective>
Wave 0 spike + Wave 0 gates fechamento ANTES de implementacao. Strategy B (CONTEXT.md D-01-B locked) tem 3 assumptions criticas (RESEARCH.md A1, A2, A5 + A8) que devem ser validadas empiricamente em 1 pod Vast.ai manual com custo ~$0.10. Sem spike, plan 06-04 (lifecycle.go refactor) corre risco de descobrir runtime que `args` runtype tem comportamento inesperado em hosts Vast 4090.

Adicionalmente, operator deve decidir Jinja strategy (B1 overlay image vs B2 MinIO fetch — CONTEXT.md D-04-B) e — se B2 — fazer upload manual do Jinja template do repo (`pod/templates/qwen3.5-27b-tool-calling.jinja`, sha256 `1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67`) para MinIO antes do plan 06-04 implementar fetch logic.

Purpose: Burnt-bridge mitigation. CONTEXT.md `<specifics>` linha 158 obriga spike paralelo. Sem decisao Jinja B1/B2 antes de 06-04, planner nao pode escrever Tasks deterministas para lifecycle.go onstart.

Output: 2 documentos de operator-checkpoint (`06-SPIKE-runtype-args.md` + `06-WAVE0-GATES.md`); 0 mudancas de codigo; 1 objeto MinIO opcional (se B2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-emergency-pod-template-refactor/06-CONTEXT.md
@.planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md
@.planning/phases/06-emergency-pod-template-refactor/06-PATTERNS.md
@.planning/phases/06-emergency-pod-template-refactor/06-VALIDATION.md

<interfaces>
<!-- Estruturas relevantes para o spike -->

Vast.ai API spike payload reference (RESEARCH.md lines 685-704):
```
vast set api-key $VAST_AI_API_KEY
OFFER_ID=$(vast search offers 'gpu_name=RTX_4090 reliability>0.99 dph_total<0.40 rentable=true verified=true cuda_max_good>=12.8' -o dph_total --raw | head -1 | jq -r .id)
vast create instance $OFFER_ID \
  --image ghcr.io/ggml-org/llama.cpp:server-cuda-b9128 \
  --disk 40 \
  --args --host 0.0.0.0 --port 8000 --version
```

Jinja template current location:
```
pod/templates/qwen3.5-27b-tool-calling.jinja          (8595 bytes, baked into custom GHCR image atualmente)
pod/templates/qwen3.5-27b-tool-calling.jinja.sha256   (contem: 1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67)
```

MinIO upload command pattern (from CLAUDE.md `## API Tokens` block):
```
mc alias set ifix https://s3.ifixtelecom.com.br HjEgnpAOTlKXBRW5Ggdo <secret>
mc cp pod/templates/qwen3.5-27b-tool-calling.jinja ifix/ai-gateway/emerg-onstart/templates/qwen3.5-27b-tool-calling-<sha>.jinja
```

Grep survey scope (RESEARCH.md Wave 0 line 675 + PATTERNS.md line 416):
```
grep -rln "EmergencyPodImageTag\|ifix-ai-pod\|EMERGENCY_POD_IMAGE_TAG" gateway/ --include='*.go'
# Expected hits per PATTERNS.md line 420-426:
#   gateway/internal/config/config.go (line 125, 243)
#   gateway/internal/config/config_test.go (lines 450, 488-489, 543, 573-574)
#   gateway/internal/emerg/lifecycle.go (line 650, 658, 687)
#   gateway/internal/emerg/vast/types.go (line 125 — comentario)
#   gateway/internal/emerg/errors.go (line 6 — comentario, baixa prioridade)
#   gateway/internal/integration_test/emerg_leader_test.go (line 47)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Spike manual Vast.ai - validar Strategy B empiricamente</name>
  <what-needs-doing>
    Provisionar 1 pod Vast.ai manual via `vast` CLI ou UI usando `ghcr.io/ggml-org/llama.cpp:server-cuda-b9128` + `runtype=args` + `args=["--host","0.0.0.0","--port","8000","--version"]`. Custo estimado: $0.10 (10min @ $0.30/h). Manter pod ate confirmar 4 outputs abaixo; depois destroy imediato.
  </what-needs-doing>
  <read_first>
    .planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md (lines 681-706 "Pre-Spike Manual Validation")
    .planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md (lines 414-424 Pitfall 3 SSH access expectation)
    .planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md (lines 624-628 Open Question 4 onstart in container or host)
    .planning/seeds/SEED-001-emergency-pod-template-vast-vs-custom-image.md (motivacao + trade-offs)
  </read_first>
  <how-to-verify>
    1. `vast set api-key $VAST_AI_API_KEY`
    2. Search 1 offer: `vast search offers 'gpu_name=RTX_4090 reliability>0.99 dph_total<0.40 rentable=true verified=true' -o dph_total --raw | head -1` → guarde offer_id
    3. Create instance: `vast create instance <offer_id> --image ghcr.io/ggml-org/llama.cpp:server-cuda-b9128 --disk 40 --onstart-cmd 'echo IN_CONTAINER > /tmp/marker; date >> /tmp/marker' --args --host 0.0.0.0 --port 8000 --version` (NOTE: `--args` na CLI sao REMAINDER — passa tudo depois)
    4. Aguardar `actual_status=running` (`vast show instances`); medir tempo total ate aqui (cold-pull image + start)
    5. `vast ssh <id>` (Pitfall 3 — pode falhar com runtype=args; se falhar, anotar)
    6. Se SSH OK: `cat /tmp/marker` (confirma onstart in-container); `ps -ef | grep llama-server` (confirma PID 1 == llama-server; se shell wrapper PID 1, anota)
    7. Destroy: `vast destroy instance <id>` IMEDIATAMENTE apos confirmar (limite ~10min de custo)
    8. Documentar 4 evidencias em `.planning/phases/06-emergency-pod-template-refactor/06-SPIKE-runtype-args.md`:
       (a) cold-start total (search → actual_status=running): ___s
       (b) onstart roda in-container (cat /tmp/marker funciona): YES / NO
       (c) llama-server eh PID 1 (ps -ef PID 1 mostra `/app/llama-server --host 0.0.0.0 ...`): YES / NO
       (d) `vast ssh <id>` funciona com runtype=args: YES / NO (afeta debug ergonomics, nao bloqueia Strategy B)
    9. (Opcional, se budget permitir) Repetir 2x em hosts 4090 diferentes para cache-hit empirical (CONTEXT.md `<specifics>` linha 159)
  </how-to-verify>
  <resume-signal>Pedro responde com path do `06-SPIKE-runtype-args.md` preenchido OU "spike skipped — Strategy B trusted from research" (em ultimo caso, planner-revision para anotar risco residual)</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decidir Jinja strategy (B1 image overlay vs B2 MinIO fetch) + Disk filter</name>
  <decision>
    Como o pod Vast deve obter o Jinja template `qwen3.5-27b-tool-calling.jinja` (Ifix-patched, sha256 `1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67`) que `ghcr.io/ggml-org/llama.cpp:server-cuda-b9128` upstream NAO contem? (Pitfall 8 RESEARCH.md:466). Tambem decidir o filtro `Disk` em DefaultSearchFilter — atualmente 80GB em lifecycle.go:693, RESEARCH.md Open Question 6 sugere reduzir para 40GB.
  </decision>
  <context>
    CONTEXT.md D-04-B oferece 2 opcoes, recomenda B2 default. RESEARCH.md Open Question 2 (linhas 614-617) concorda B2 preserva intento D-08 ("delete custom image"). Mas B1 garante reprodutibilidade tag-pinned.
    Disk filter: Strategy B template ~3GB + weights 16GB + tmp = ~22GB; 40GB margem confortavel; 80GB exclui mais hosts spot market.
  </context>
  <read_first>
    .planning/phases/06-emergency-pod-template-refactor/06-CONTEXT.md (lines 34-37 D-04-B opcoes)
    .planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md (lines 466-475 Pitfall 8 Jinja path mismatch)
    .planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md (lines 633-636 Open Question 6 Disk budget)
    .planning/phases/06-emergency-pod-template-refactor/06-PATTERNS.md (lines 446-451 B1 vs B2 analogs)
  </read_first>
  <options>
    <option id="b2-minio-40gb">
      <name>B2 MinIO fetch + Disk=40GB (recomendada — CONTEXT.md D-04-B default)</name>
      <pros>Sem novo image CI/GHCR. Mantem D-08-B "no custom image" intent. Operator-controlled via config env (EmergencyJinjaTemplateKey + EmergencyJinjaTemplateSHA256). Disk=40GB abre mais offers spot market (RESEARCH.md OQ6).</pros>
      <cons>Onstart inline cresce ~5 linhas (curl MinIO + sha256-c + write /app/templates/). MinIO disponibilidade = nova dep runtime (mitigado: pod ja depende MinIO pra weights).</cons>
    </option>
    <option id="b1-overlay-40gb">
      <name>B1 image overlay `ghcr.io/ifixtelecom/qwen-templates:v1` + Disk=40GB</name>
      <pros>Tag-pinned reproducibility absoluta. Onstart mais simples (sem fetch Jinja).</pros>
      <cons>Nova GHCR image custom (10KB layer sobre upstream). Novo CI workflow `.github/workflows/build-qwen-templates.yml`. Defeats parcialmente "no custom image" goal D-08-B. Cleanup PR2 nao remove esta imagem.</cons>
    </option>
    <option id="b2-minio-80gb">
      <name>B2 MinIO fetch + Disk=80GB (status quo Disk filter)</name>
      <pros>Match Phase 6.5 behavior pra A/B comparison facil durante UAT.</pros>
      <cons>Exclui hosts spot market sem necessidade.</cons>
    </option>
  </options>
  <resume-signal>Pedro responde "b2-minio-40gb" / "b1-overlay-40gb" / "b2-minio-80gb"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Se B2 — upload Jinja para MinIO + grep survey de escopo</name>
  <what-needs-doing>
    CONDICIONAL: executa SOMENTE se Task 2 escolheu uma opcao B2-*. Upload do Jinja template repo `pod/templates/qwen3.5-27b-tool-calling.jinja` para MinIO bucket `ai-gateway` em key contendo sha256 (immutable per RESEARCH.md OQ5 recommendation). Confirmar sha256 disk match repo .sha256 file. Adicionalmente, em TODOS os casos (B1 ou B2), executar grep survey final pra detectar surpresas antes de Wave 1.
  </what-needs-doing>
  <read_first>
    .planning/phases/06-emergency-pod-template-refactor/06-PATTERNS.md (lines 416-428 Wave 0 survey command)
    pod/templates/qwen3.5-27b-tool-calling.jinja.sha256 (1 linha, hex)
  </read_first>
  <how-to-verify>
    1. Confirmar Jinja sha256 sao identicos disco-vs-file:
       ```
       cd /home/pedro/projetos/pedro/gpu-ifix
       sha256sum pod/templates/qwen3.5-27b-tool-calling.jinja
       cat pod/templates/qwen3.5-27b-tool-calling.jinja.sha256
       # Ambos devem ser 1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67
       ```
    2. (B2 only) Upload para MinIO em key fixo:
       ```
       mc alias set ifix https://s3.ifixtelecom.com.br HjEgnpAOTlKXBRW5Ggdo $MINIO_SECRET_KEY
       mc cp pod/templates/qwen3.5-27b-tool-calling.jinja \
         ifix/ai-gateway/emerg-onstart/templates/qwen3.5-27b-tool-calling-1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67.jinja
       mc stat ifix/ai-gateway/emerg-onstart/templates/qwen3.5-27b-tool-calling-1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67.jinja
       ```
    3. Grep survey final (qualquer estrategia):
       ```
       cd /home/pedro/projetos/pedro/gpu-ifix
       grep -rln "EmergencyPodImageTag\|ifix-ai-pod\|EMERGENCY_POD_IMAGE_TAG\|Runtype.*\"ssh\"" gateway/ --include='*.go'
       ```
       Esperado (do PATTERNS.md): exatamente 6 arquivos
         - gateway/internal/config/config.go
         - gateway/internal/config/config_test.go
         - gateway/internal/emerg/lifecycle.go
         - gateway/internal/emerg/vast/types.go
         - gateway/internal/emerg/errors.go (comentario apenas)
         - gateway/internal/integration_test/emerg_leader_test.go
       Se grep retorna >6 arquivos OU arquivo nao esperado: Wave 0 incompleta — investigar antes de Wave 1.
    4. Preencher `.planning/phases/06-emergency-pod-template-refactor/06-WAVE0-GATES.md` com:
       - Jinja Strategy: B1 ou B2
       - Disk: 40GB ou 80GB
       - (B2 only) Jinja MinIO Key: `emerg-onstart/templates/qwen3.5-27b-tool-calling-1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67.jinja`
       - (B2 only) Jinja sha256 hex: `1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67`
       - Grep survey actual file count: ___ (esperado 6)
       - Spike result reference: 06-SPIKE-runtype-args.md
  </how-to-verify>
  <resume-signal>Pedro responde "approved" + path `.planning/phases/06-emergency-pod-template-refactor/06-WAVE0-GATES.md` preenchido</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation → Vast.ai API | Spike provision/destroy crosses here (Bearer auth) |
| Operator workstation → MinIO | Jinja upload (B2 only) crosses here (HMAC sigv4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | `ghcr.io/ggml-org/llama.cpp:server-cuda-b9128` tag mutability | mitigate | Tag is SHA-pinned by ggml-org build process (b9128 = immutable build number per CONTEXT.md D-01-B). Operator pinned in `EmergencyTemplateImage` config field — never `:server-cuda` floating. Future Wave 1 plan documents `docker manifest inspect` digest verification |
| T-06-01 | Information Disclosure | Vast.ai API key in operator terminal during spike | accept | Operator runs spike on `ops-claude` VM (already trusted with VAST_AI_API_KEY). Spike destroys instance immediately → no persisting attack surface |
| T-06-SC | Tampering | Spike does not install packages | accept | No npm/pip/cargo installs in this plan; supply-chain protection deferred to plan 06-04 (`docker pull` of pinned image is the only "install") |
</threat_model>

<verification>
- Spike documento `06-SPIKE-runtype-args.md` existe com 4 evidencias (cold-start, in-container onstart, PID 1, vast-ssh)
- `06-WAVE0-GATES.md` existe com 5 campos preenchidos (Jinja Strategy, Disk, MinIO Key se B2, sha256 se B2, grep count)
- Se B2: `mc stat ifix/ai-gateway/emerg-onstart/templates/...` retorna 200 + size>0
- Grep survey retorna exatamente os 6 arquivos previstos (PATTERNS.md line 420-426). Desvio bloqueia Wave 1.
</verification>

<success_criteria>
- Operator decidiu Jinja strategy (B1 ou B2) → planner downstream tem deterministic Tasks
- Strategy B empiricamente validada (PID 1 + cold-start ≤6min + onstart in-container)
- Grep survey scope confirmado — Wave 1 sabe exatamente os 6 arquivos a tocar
</success_criteria>

<output>
Create `.planning/phases/06-emergency-pod-template-refactor/06-01-SUMMARY.md` when done. Resumir: spike outcome, Jinja decision, grep survey count, links to 2 operator docs.
</output>