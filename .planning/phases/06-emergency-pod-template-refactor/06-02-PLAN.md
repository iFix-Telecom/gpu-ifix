---
phase: 06-emergency-pod-template-refactor
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - gateway/internal/config/config.go
  - gateway/internal/config/config_test.go
  - gateway/.env.portainer.dev
autonomous: true
requirements: [PRV-06]
threat_refs: [T-06-01]
validates_sc: [SC-1, SC-5]
tags: [config, env-vars, refactor, phase-6-pr1]

must_haves:
  truths:
    - "EmergencyPodImageTag field REMOVIDO de Config struct e Load() (config.go)"
    - "EMERGENCY_POD_IMAGE_TAG env var REMOVIDA de phase6OptionalEnv (config_test.go) e .env.portainer.dev"
    - "4 fields novos existem em Config struct + Load(): EmergencyTemplateImage (default `ghcr.io/ggml-org/llama.cpp:server-cuda-b9128`), EmergencyJinjaTemplateKey (default vazio), EmergencyJinjaTemplateSHA256 (default vazio), EmergencyLlamaArgs ([]string CSV default nil)"
    - "TestLoad_Phase6Defaults asserts os 4 novos defaults sem referencia a EmergencyPodImageTag"
    - "TestLoad_Phase6CustomValues exercita override dos 4 fields com t.Setenv"
    - "Go build sucesso (sem ocorrencias remanescentes de Cfg.EmergencyPodImageTag fora de lifecycle.go que sera tocado em 06-04)"
  artifacts:
    - path: "gateway/internal/config/config.go"
      provides: "Config struct extended with 4 Strategy B fields, EmergencyPodImageTag removed"
      contains: "EmergencyTemplateImage|EmergencyJinjaTemplateKey|EmergencyJinjaTemplateSHA256|EmergencyLlamaArgs"
    - path: "gateway/internal/config/config_test.go"
      provides: "Phase 6 tests cover 4 new fields, EmergencyPodImageTag assertions deleted"
      contains: "TestLoad_Phase6Defaults|TestLoad_Phase6CustomValues|EmergencyTemplateImage"
    - path: "gateway/.env.portainer.dev"
      provides: "Portainer env file aligned with 4 new vars, EMERGENCY_POD_IMAGE_TAG line gone"
      contains: "EMERGENCY_TEMPLATE_IMAGE"
  key_links:
    - from: "gateway/internal/config/config.go"
      to: "gateway/internal/emerg/lifecycle.go (downstream consumer plan 06-04)"
      via: "Cfg.EmergencyTemplateImage / Cfg.EmergencyJinjaTemplateKey / Cfg.EmergencyJinjaTemplateSHA256 / Cfg.EmergencyLlamaArgs"
      pattern: "cfg\\.Emergency(Template|Jinja|Llama)"
---

<objective>
Plan 06-02 substitui o campo de config `EmergencyPodImageTag` (CONTEXT.md D-08-B original) pelos 4 campos da Strategy B Locked (CONTEXT.md D-01-B..D-08-B):

- `EmergencyTemplateImage` (string) → imagem completa `ghcr.io/ggml-org/llama.cpp:server-cuda-b9128`
- `EmergencyJinjaTemplateKey` (string, opcional B2) → MinIO object key do Jinja
- `EmergencyJinjaTemplateSHA256` (string, opcional B2) → integrity check
- `EmergencyLlamaArgs` ([]string CSV, opcional) → override do default const em lifecycle.go

Atualiza tests `TestLoad_Phase6Defaults` / `TestLoad_Phase6CustomValues` que hoje afirmam contra `EmergencyPodImageTag`. Atualiza `.env.portainer.dev` para refletir os novos env vars.

Purpose: Wave 1 paralela. Plan 06-03 (types.go) compartilha 0 arquivos com este plan — podem rodar simultaneos. Wave 2 (06-04 lifecycle.go) consome os 4 novos fields.

Output: 2 arquivos Go modificados (config.go, config_test.go), 1 arquivo env modificado. Zero novos arquivos. Build deve passar parcialmente (lifecycle.go ainda referencia EmergencyPodImageTag — sera fixado em 06-04, mesma wave 2 separada por design).

Per CONTEXT.md `<decisions>` Strategy B Locked block (autoritativo): 4 fields, NAO 6 (sem EmergencyLlamaBinTag, EmergencyLlamaBinSHA256, EmergencyLlamaBinMirrorURL, EmergencyOnstartVersion, EmergencyOnstartSHA256 — todos eliminados porque Strategy B nao baixa binario nem script externo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-emergency-pod-template-refactor/06-CONTEXT.md
@.planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md
@.planning/phases/06-emergency-pod-template-refactor/06-PATTERNS.md

<interfaces>
<!-- Padrao envOr / csvOr helpers ja existentes (config.go:317-355) -->

```go
// gateway/internal/config/config.go (helpers ja existentes, REUTILIZAR)
func envOr(key, fallback string) string {
    if v := os.Getenv(key); v != "" { return v }
    return fallback
}
func csvOr(value string, fallback []string) []string {
    if value == "" { return fallback }
    parts := strings.Split(value, ",")
    // trim each + filter empty (ver implementacao real)
    return parts
}
```

<!-- Bloco Phase 6 atual em Config struct (config.go:119-149) e Load() (config.go:240-265) -->

```go
// Config struct line 125 (a DELETAR):
EmergencyPodImageTag              string  // EMERGENCY_POD_IMAGE_TAG (default "v1.0")

// Load() line 243 (a DELETAR):
EmergencyPodImageTag:              envOr("EMERGENCY_POD_IMAGE_TAG", "v1.0"),

// Novos 4 fields a ADICIONAR no bloco Phase 6 do struct (entre linha 124 e 126):
EmergencyTemplateImage        string   // EMERGENCY_TEMPLATE_IMAGE (default "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128"; CONTEXT.md D-01-B; tag SHA-pinned)
EmergencyJinjaTemplateKey     string   // EMERGENCY_JINJA_TEMPLATE_KEY (MinIO key; D-04-B opcao B2; vazio = B1 image overlay path, lifecycle.go omite fetch)
EmergencyJinjaTemplateSHA256  string   // EMERGENCY_JINJA_TEMPLATE_SHA256 (hex; D-04-B opcao B2)
EmergencyLlamaArgs            []string // EMERGENCY_LLAMA_ARGS (CSV; D-07-B; vazio = hardcoded const em lifecycle.go)

// Novos 4 loaders a ADICIONAR no bloco Phase 6 do Load() (entre linha 242 e 244):
EmergencyTemplateImage:        envOr("EMERGENCY_TEMPLATE_IMAGE", "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128"),
EmergencyJinjaTemplateKey:     os.Getenv("EMERGENCY_JINJA_TEMPLATE_KEY"),
EmergencyJinjaTemplateSHA256:  os.Getenv("EMERGENCY_JINJA_TEMPLATE_SHA256"),
EmergencyLlamaArgs:            csvOr(os.Getenv("EMERGENCY_LLAMA_ARGS"), nil),
```

<!-- phase6OptionalEnv list (config_test.go:449-462) UPDATE: -->

```go
var phase6OptionalEnv = []string{
    // REMOVE: "EMERGENCY_POD_IMAGE_TAG",
    "EMERGENCY_TEMPLATE_IMAGE",         // ADD
    "EMERGENCY_JINJA_TEMPLATE_KEY",     // ADD
    "EMERGENCY_JINJA_TEMPLATE_SHA256",  // ADD
    "EMERGENCY_LLAMA_ARGS",             // ADD
    "MONTHLY_EMERGENCY_BUDGET_BRL",
    "PRIMARY_HOST_ID",
    "PROVISION_COLDSTART_BUDGET_SECONDS",
    "PROVISION_FAILURE_COOLDOWN_SECONDS",
    "PROVISION_HEALTHY_DURATION_SECONDS",
    "PROVISION_IDLE_GRACE_SECONDS",
    "PROVISION_TRIGGER_FAILED_OVER_SECONDS",
    "USD_TO_BRL_RATE",
    "VAST_AI_API_KEY",
    "VAST_API_QPS_LIMIT",
    "VAST_PRICE_CAP_DPH",
}
```

<!-- gateway/.env.portainer.dev linha do Phase 6 atual (a substituir): -->

```
EMERGENCY_POD_IMAGE_TAG=latest-dev   # CONFIRM via `grep -n EMERGENCY_POD_IMAGE_TAG gateway/.env.portainer.dev`; pode estar em outro lugar do arquivo
```

Novo bloco:
```
# ============================================================
# Phase 6 — emergency-pod template (Strategy B Locked, CONTEXT.md D-01-B..D-08-B)
# ============================================================
EMERGENCY_TEMPLATE_IMAGE=ghcr.io/ggml-org/llama.cpp:server-cuda-b9128
EMERGENCY_JINJA_TEMPLATE_KEY=emerg-onstart/templates/qwen3.5-27b-tool-calling-1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67.jinja
EMERGENCY_JINJA_TEMPLATE_SHA256=1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67
# EMERGENCY_LLAMA_ARGS= (vazio -> usa default const em lifecycle.go)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar config.go + config_test.go — substituir EmergencyPodImageTag por 4 fields Strategy B</name>
  <files>gateway/internal/config/config.go, gateway/internal/config/config_test.go</files>
  <read_first>
    gateway/internal/config/config.go (linhas 119-149 struct Phase 6 + linhas 240-265 Load() block + linhas 317-355 helpers envOr/csvOr)
    gateway/internal/config/config_test.go (linhas 446-462 phase6OptionalEnv + linhas 478-524 TestLoad_Phase6Defaults + linhas 529-579 TestLoad_Phase6CustomValues)
    .planning/phases/06-emergency-pod-template-refactor/06-PATTERNS.md (lines 212-292 config.go pattern)
    .planning/phases/06-emergency-pod-template-refactor/06-CONTEXT.md (lines 47-54 Config Gateway Env Vars — 4 fields autoritativo)
  </read_first>
  <behavior>
    - Test 1 (TestLoad_Phase6Defaults): cfg.EmergencyTemplateImage default == "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128"
    - Test 2 (TestLoad_Phase6Defaults): cfg.EmergencyJinjaTemplateKey default == "" (vazio - B1 disables MinIO fetch)
    - Test 3 (TestLoad_Phase6Defaults): cfg.EmergencyJinjaTemplateSHA256 default == ""
    - Test 4 (TestLoad_Phase6Defaults): cfg.EmergencyLlamaArgs default == nil (vazio CSV → nil per csvOr behavior)
    - Test 5 (TestLoad_Phase6CustomValues): t.Setenv("EMERGENCY_TEMPLATE_IMAGE","ghcr.io/ggml-org/llama.cpp:server-cuda-b9200") → cfg.EmergencyTemplateImage == "ghcr.io/ggml-org/llama.cpp:server-cuda-b9200"
    - Test 6 (TestLoad_Phase6CustomValues): t.Setenv("EMERGENCY_JINJA_TEMPLATE_KEY","emerg-onstart/templates/qwen-abc.jinja") → cfg.EmergencyJinjaTemplateKey == "emerg-onstart/templates/qwen-abc.jinja"
    - Test 7 (TestLoad_Phase6CustomValues): t.Setenv("EMERGENCY_JINJA_TEMPLATE_SHA256","deadbeef") → cfg.EmergencyJinjaTemplateSHA256 == "deadbeef"
    - Test 8 (TestLoad_Phase6CustomValues): t.Setenv("EMERGENCY_LLAMA_ARGS","--host,0.0.0.0,--port,8000") → cfg.EmergencyLlamaArgs == []string{"--host","0.0.0.0","--port","8000"}
    - Negative: tests NAO devem referenciar `cfg.EmergencyPodImageTag` (campo removido) — compile error garante remocao completa
  </behavior>
  <action>
    Em `gateway/internal/config/config.go`:
    1. No bloco Phase 6 do struct Config (linha 119 comment header ate linha 149) — DELETAR linha 125 (`EmergencyPodImageTag string ...`) e INSERIR 4 novos fields per `<interfaces>` block acima imediatamente APOS o header comment Phase 6 (manter ordem: EmergencyTemplateImage, EmergencyJinjaTemplateKey, EmergencyJinjaTemplateSHA256, EmergencyLlamaArgs ANTES dos campos existentes MonthlyEmergencyBudgetBRL/etc).
    2. Atualizar comment header Phase 6 (linha 119-124): trocar "All twelve fields" por "All fifteen fields" (12 - 1 + 4 = 15). Adicionar nota "Strategy B Locked per CONTEXT.md D-01-B..D-08-B".
    3. No `Load()` (linha 240+ bloco Phase 6) — DELETAR linha 243 (`EmergencyPodImageTag: envOr(...)`) e INSERIR os 4 novos loaders per `<interfaces>` block. Manter ordem mirror do struct.
    4. NAO adicionar nenhum dos 4 novos env vars ao `requiredOrder` (config.go:286-310). Pattern Phase 6 graceful-degrade preservado (Strategy B com 4 envs vazios cai em defaults; reconciler ainda funciona porque EmergencyTemplateImage tem default seguro).

    Em `gateway/internal/config/config_test.go`:
    5. No slice `phase6OptionalEnv` (linha 449-462): DELETAR `"EMERGENCY_POD_IMAGE_TAG"`. INSERIR `"EMERGENCY_TEMPLATE_IMAGE"`, `"EMERGENCY_JINJA_TEMPLATE_KEY"`, `"EMERGENCY_JINJA_TEMPLATE_SHA256"`, `"EMERGENCY_LLAMA_ARGS"` na mesma ordem alfabetica/logica.
    6. Em `TestLoad_Phase6Defaults` (linha 478): DELETAR if-block linhas 488-490 que afirma `EmergencyPodImageTag != "v1.0"`. INSERIR 4 novos if-blocks afirmando defaults dos 4 fields novos (per Test 1-4 acima).
    7. Em `TestLoad_Phase6CustomValues` (linha 529): DELETAR linha 543 `t.Setenv("EMERGENCY_POD_IMAGE_TAG", "v1.1-rc2")` e linhas 573-575 if-block. INSERIR 4 t.Setenv + 4 if-blocks per Test 5-8 acima.
    8. Atualizar comment header dos testes Phase 6 (linhas 471-477 acima de TestLoad_Phase6Defaults): trocar "D-A1, D-A4, D-A5, D-C1, D-D1, D-D2, D-D4" por "D-01-B (template), D-A1, D-A4, D-A5, D-C1, D-D1, D-D2, D-D4". Atualizar Plan reference de "06-01 Plan Behavior Test 6" para "06-02 Plan Behavior Test 1-8 Strategy B Locked".

    NAO tocar lifecycle.go, integration_test/emerg_leader_test.go, vast/types.go ou .env.portainer.dev nesta task — files separados.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/config -run TestLoad_Phase6 -count=1 -v</automated>
  </verify>
  <done>
    `go test ./internal/config -run TestLoad_Phase6` passa com 3 sub-testes GREEN (Defaults, CustomValues, FloatOrBogusValue). `grep -c EmergencyPodImageTag gateway/internal/config/config.go gateway/internal/config/config_test.go` retorna 0 em ambos arquivos. Build do package config NAO depende de lifecycle.go — compila isoladamente.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar .env.portainer.dev — substituir EMERGENCY_POD_IMAGE_TAG por 3 vars Strategy B</name>
  <files>gateway/.env.portainer.dev</files>
  <read_first>
    gateway/.env.portainer.dev (arquivo inteiro — esperado <100 linhas; buscar EMERGENCY_POD_IMAGE_TAG)
    .planning/phases/06-emergency-pod-template-refactor/06-WAVE0-GATES.md (Jinja sha256 e key gravados pelo plan 06-01)
    .planning/phases/06-emergency-pod-template-refactor/06-PATTERNS.md (lines 374-390 .env block pattern)
  </read_first>
  <action>
    1. `grep -n "EMERGENCY_POD_IMAGE_TAG\|^# *Phase 6" gateway/.env.portainer.dev` para localizar bloco atual.
    2. Se linha `EMERGENCY_POD_IMAGE_TAG=...` existe (esperado): substituir essa linha + adicionar 2 linhas novas (3 env vars total). Se o bloco Phase 6 nao tem header comment: adicionar header conforme `<interfaces>` block.
    3. Linhas finais devem ser exatamente:
       ```
       EMERGENCY_TEMPLATE_IMAGE=ghcr.io/ggml-org/llama.cpp:server-cuda-b9128
       EMERGENCY_JINJA_TEMPLATE_KEY=emerg-onstart/templates/qwen3.5-27b-tool-calling-1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67.jinja
       EMERGENCY_JINJA_TEMPLATE_SHA256=1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67
       ```
       (NOTA: se WAVE0-GATES decidiu B1, linhas 2 e 3 ficam comentadas: `# EMERGENCY_JINJA_TEMPLATE_KEY=...` `# EMERGENCY_JINJA_TEMPLATE_SHA256=...` — Jinja vem do overlay image B1 em vez de MinIO fetch.)
    4. NAO adicionar `EMERGENCY_LLAMA_ARGS` — fica vazio (default = const em lifecycle.go per CONTEXT.md D-07-B).
    5. Operator-side: documentar no SUMMARY que Portainer UI stack `ai-gateway-dev` precisa atualizar env vars manualmente — `.env.portainer.dev` no repo nao auto-sync com Portainer (CONTEXT.md `<code_context>` linha 363).
  </action>
  <verify>
    <automated>! grep -v '^#' gateway/.env.portainer.dev | grep -q EMERGENCY_POD_IMAGE_TAG && grep -v '^#' gateway/.env.portainer.dev | grep -q '^EMERGENCY_TEMPLATE_IMAGE=ghcr.io/ggml-org/llama.cpp:server-cuda-b9128$'</automated>
  </verify>
  <done>
    Arquivo `.env.portainer.dev` nao tem mais linhas nao-comentadas `EMERGENCY_POD_IMAGE_TAG=...`. Tem linha exata `EMERGENCY_TEMPLATE_IMAGE=ghcr.io/ggml-org/llama.cpp:server-cuda-b9128`. Se B2: tem tambem `EMERGENCY_JINJA_TEMPLATE_KEY=...` + `EMERGENCY_JINJA_TEMPLATE_SHA256=...` ativas (sem `#` prefix).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.portainer.dev` (git) → Portainer stack | Operator copies env values manually (CONTEXT.md `<code_context>` line 363) — not auto-sync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `EMERGENCY_JINJA_TEMPLATE_SHA256` em logs | accept | sha256 hex eh public-grade — funciona como hash, nao secret |
| T-06-04 | Tampering | `EmergencyTemplateImage` default `ghcr.io/ggml-org/llama.cpp:server-cuda-b9128` | mitigate | Tag SHA-pinned por ggml-org build process (b9128 = immutable build per CONTEXT.md). Operator pode override via env se quiser pinning mais forte por digest (`@sha256:...`) — RESEARCH.md security domain recommendation |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Plan 06-02 nao instala packages — apenas modifica config files |
</threat_model>

<verification>
- `cd gateway && go test ./internal/config -run TestLoad_Phase6 -count=1 -v` 3 sub-tests GREEN
- `grep -c EmergencyPodImageTag gateway/internal/config/config.go` retorna 0
- `grep -c EmergencyPodImageTag gateway/internal/config/config_test.go` retorna 0
- `grep -c EmergencyTemplateImage gateway/internal/config/config.go` retorna >=2 (struct + Load)
- `grep -c '^EMERGENCY_TEMPLATE_IMAGE=' gateway/.env.portainer.dev` retorna 1
- `grep -c '^EMERGENCY_POD_IMAGE_TAG=' gateway/.env.portainer.dev` retorna 0
</verification>

<success_criteria>
- 3 testes Phase 6 do config GREEN, afirmando defaults novos + override custom values
- Zero ocorrencias remanescentes de `EmergencyPodImageTag` em config.go ou config_test.go
- Portainer env file reflete os 3 novos vars exatos (4o `EMERGENCY_LLAMA_ARGS` opcional/comentado)
- Plan 06-04 (lifecycle.go Wave 2) tem 4 novos fields disponiveis em `r.deps.Cfg`
</success_criteria>

<output>
Create `.planning/phases/06-emergency-pod-template-refactor/06-02-SUMMARY.md` when done. Resumir: 4 fields adicionados, 1 field removido, env file substituicoes, comando UAT-style para operator atualizar Portainer.
</output>