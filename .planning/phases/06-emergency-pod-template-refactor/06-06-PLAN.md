---
phase: 06-emergency-pod-template-refactor
plan: 06
type: execute
wave: 4
depends_on: [06-05]
files_modified:
  - .planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md
  - docs/RUNBOOK-EMERGENCY-POD.md
autonomous: false
requirements: [PRV-06, PRV-01, PRV-02, PRV-03, PRV-04, PRV-05, PRV-07, PRV-08, PRV-09, PRV-10]
threat_refs: [T-06-04, T-06-05]
validates_sc: [SC-1, SC-2, SC-3, SC-4, SC-5]
tags: [human-uat, live-vast-ai, blocking-checkpoint, phase-6-pr1-gate]
user_setup:
  - service: vast.ai
    why: "3 lifecycles consecutivos LIVE no Vast.ai 4090 — gate burnt-bridge mitigation (D-08-B-risk) antes do PR2 delete"
    env_vars:
      - name: VAST_AI_API_KEY
        source: "Portainer UI stack ai-gateway-dev (ja configurado em Phase 6.5)"
    dashboard_config:
      - task: "Confirmar saldo Vast.ai >= $5 (3 lifecycles ~$0.30 cada = $1; com retries/falhas ~$3 buffer)"
        location: "https://cloud.vast.ai/billing"
      - task: "Stack ai-gateway-dev no Portainer com env vars novos (EMERGENCY_TEMPLATE_IMAGE + EMERGENCY_JINJA_TEMPLATE_KEY + EMERGENCY_JINJA_TEMPLATE_SHA256) atualizados (operator manual)"
        location: "https://portainer3.ifixtelecom.com.br stack ai-gateway-dev"

must_haves:
  truths:
    - "3 lifecycles emergency consecutivos atingem `FSM=emergency_active` end-to-end (search → create → cold-start → /v1/models 200 → routing)"
    - "Cada lifecycle log inclui: instance_id Vast.ai, cold-start medido (search → /v1/models OK), `Runtype=args` no payload Vast, image=`ghcr.io/ggml-org/llama.cpp:server-cuda-b9128` (NAO ifix-ai-pod)"
    - "Nenhum lifecycle trava em health_timeout 1800s (bug STATE.md:85 — Phase 6 fix VALIDADO)"
    - "Cold-start P90 ≤ 6 min em 3/3 lifecycles (Success Criteria #2 ROADMAP.md:138)"
    - "RUNBOOK-EMERGENCY-POD.md atualizado refletindo Strategy B (image source + onstart pattern + troubleshooting Runtype=args expected behavior)"
    - "06-HUMAN-UAT.md sign-off section preenchido pelo operator (Pedro) com 3 lifecycle IDs + custos + observacoes"
  artifacts:
    - path: ".planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md"
      provides: "Live UAT scenario document with 3-lifecycle GREEN sign-off section"
      contains: "Lifecycle 1|Lifecycle 2|Lifecycle 3|Sign-off"
    - path: "docs/RUNBOOK-EMERGENCY-POD.md"
      provides: "Updated runbook reflecting Strategy B image source + onstart"
      contains: "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128|Runtype=args|EMERGENCY_TEMPLATE_IMAGE"
  key_links:
    - from: ".planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md"
      to: "Phase 6.5 06.5-HUMAN-UAT.md template"
      via: "structural reuse — 3 lifecycle scenarios + sign-off"
      pattern: "Lifecycle (1|2|3):"
    - from: "PR2 plan 06-07 (cleanup) gate"
      to: "06-HUMAN-UAT.md sign-off"
      via: "blocking gate — 3 GREEN before delete"
      pattern: "sign-off.*GREEN"
---

<objective>
Plan 06-06 eh o **gate critico burnt-bridge mitigation** (CONTEXT.md D-08-B-risk + `<specifics>` linha 163): antes de deletar a custom image GHCR + Dockerfile + workflow (PR2 plan 06-07), Strategy B precisa provar funcionar end-to-end em LIVE Vast.ai por 3 lifecycles consecutivos GREEN.

Adicionalmente, este plano DESBLOQUEIA Phase 6.5 HUMAN-UAT (STATE.md linha 41 — `06.5-11-PLAN.md` task 3 bloqueada por bug runtype=ssh). Apos 06-06 GREEN, Pedro pode executar Phase 6.5 UAT separadamente sem o CMD-ignore bug.

3 cenarios LIVE (custo estimado ~R$3-10):
1. **Cold-start happy path**: `gatewayctl emerg force-provision` → mede tempo total ate `emergency_active`; verifica `:8000/v1/models` 200; destroy manual ao final.
2. **Vast offer race / retry**: provisiona com price_cap baixo artificial → forca offer bid race → verifica retry exponential backoff + audit `shutdown_reason="offer_race_lost"` apos 3 falhas (Phase 6.5 D-A3, pattern Phase 6.5 plan 06.5-06).
3. **Cancel-in-flight**: provisiona, durante cold-start mata o trigger (simulate primary recovery), verifica destroy automatico + audit `shutdown_reason="cancelled_in_flight"`.

Tambem produz documentation update em `docs/RUNBOOK-EMERGENCY-POD.md` (criado em Phase 6.5 plan 06.5-11 Task 2 — verificar existe ou criar; refletir Strategy B differences vs Phase 6.5 Strategy A documentado anteriormente).

Purpose: Mitigation factor para D-08-B (delete custom image). Sem 3 lifecycles GREEN, PR2 nao deve mergir — operator nao tem como reverter sem code change.

Output: 2 docs (HUMAN-UAT script + RUNBOOK update); 0 codigo modificado; 3 lifecycles Vast.ai (destrudos ao final, custo total esperado ~R$3-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-emergency-pod-template-refactor/06-CONTEXT.md
@.planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md

<interfaces>
<!-- Phase 6.5 HUMAN-UAT pattern (06.5-HUMAN-UAT.md) — analog do qual reuse structure -->

```markdown
# Phase 6.5 — HUMAN-UAT: 6 cenarios LIVE Vast.ai

## Pre-requisitos
- Stack ai-gateway-dev deployed em Portainer com env vars atualizadas
- VAST_AI_API_KEY validado via vast.Ping() at boot
- Saldo Vast.ai >= $15

## Lifecycle 1 — Cold-start happy path
... (steps)
- [ ] FSM transitiona healthy → failed_over → emergency_provisioning → emergency_active
- [ ] Time-to-emergency_active ≤ 10min
- [ ] Sign-off: [data] [operator]

## Lifecycle 2 — Bid race retry
...
```

<!-- RUNBOOK-EMERGENCY-POD.md ja existe (criado em Phase 6.5 plan 06.5-11 Task 2) — atualizar -->

```bash
ls -la docs/RUNBOOK-EMERGENCY-POD.md
# Deve existir per Phase 6.5 STATE.md:41 ("06.5-HUMAN-UAT.md + docs/RUNBOOK-EMERGENCY-POD.md created, commit 2b539fc")
```

<!-- gatewayctl emerg force-provision pattern (Phase 6.5 plan 06.5-10) -->

```bash
docker exec ai-gateway-dev /gatewayctl emerg force-provision
# Returns lifecycle_id; FSM transitions healthy → emergency_provisioning sob force flag
docker exec ai-gateway-dev /gatewayctl emerg state
# Mostra current FSM + active_lifecycle
docker exec ai-gateway-dev /gatewayctl emerg force-destroy
# Forces tear-down + audit shutdown_reason="manual"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar 06-HUMAN-UAT.md scenario sheet com 3 lifecycles</name>
  <files>.planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/06.5-auto-provisioning-emergency-pod-vast-ai/06.5-HUMAN-UAT.md (template completo — 6 cenarios; reuse structure)
    .planning/phases/06-emergency-pod-template-refactor/06-CONTEXT.md (Strategy B Locked block)
    .planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md (lines 681-706 spike pattern — re-utiliza)
  </read_first>
  <action>
    1. Verificar se `.planning/phases/06.5-auto-provisioning-emergency-pod-vast-ai/06.5-HUMAN-UAT.md` existe (esperado SIM per STATE.md:41). Ler estrutura completa.
    2. Criar `.planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md` com 3 cenarios (NAO 6 — Phase 6 escopo eh menor, foco em validacao Strategy B):
       - **Pre-requisitos** section: Portainer stack ai-gateway-dev com env vars atualizadas (EMERGENCY_TEMPLATE_IMAGE=ghcr.io/ggml-org/llama.cpp:server-cuda-b9128 + EMERGENCY_JINJA_TEMPLATE_* + EMERGENCY_LLAMA_ARGS opcional vazio); saldo Vast.ai >= $5; vast CLI configurado no ops-claude.
       - **Lifecycle 1 — Cold-start happy path**:
         - Steps: `docker exec ai-gateway-dev /gatewayctl emerg force-provision`; aguardar FSM=emergency_active via `gatewayctl emerg state` polling cada 30s; medir cold-start (start_time da primeira force-provision ate FSM=emergency_active); confirmar `curl http://<pod>:8000/v1/models` 200; confirmar log `emerg-bootstrap`-equivalente em Vast.ai dashboard mostra Strategy B onstart progress (mc cp Qwen + sha256 OK); destroy via `gatewayctl emerg force-destroy`; documentar em audit `SELECT ... FROM ai_gateway.emergency_lifecycles WHERE id=<lc_id>` que `vast_instance_id`, `accepted_dph`, `started_at`, `ended_at`, `shutdown_reason="manual"` estao populados.
         - Acceptance: cold-start ≤ 6min (P90 success criteria); FSM transita ate emergency_active; lifecycle audit completo; SEM Sentry errors.
       - **Lifecycle 2 — Bid race retry (price-cap pressure test)**:
         - Steps: Atualizar Portainer env `VAST_PRICE_CAP_DPH=0.20` (artificialmente baixo, vai forcar offer rejection); restart gateway container; `gatewayctl emerg force-provision`; observar log "no offers below cap" OU "offer_race_lost" apos 3 retries; confirmar audit shutdown_reason; reverter env para 0.40; restart.
         - Acceptance: lifecycle fecha com shutdown_reason ∈ {offer_race_lost, no_offers_below_cap}; Sentry breadcrumb gravado; NAO hangup infinito.
       - **Lifecycle 3 — Cancel-in-flight**:
         - Steps: `gatewayctl emerg force-provision` → aguardar FSM=emergency_provisioning + vast_instance_id populated em audit; ENQUANTO ainda esta em cold-start (antes de emergency_active), trigger primary recovery simulation OU `gatewayctl emerg force-destroy`; verificar instance destruida em Vast.ai dashboard ANTES de health-poll OK; audit shutdown_reason="cancelled_in_flight" OU "manual".
         - Acceptance: triple-layer cancel works (context + pubsub + post-create destroy per Phase 6.5 D-D5); pod nao continua a custar dinheiro apos cancel.
       - **Sign-off** section: tabela com colunas `Lifecycle | Start | End | Duration | Cost USD | Audit shutdown_reason | Sentry events | Operator notes`.
       - **Failure handling** section: se qualquer lifecycle falha, NAO prosseguir para PR2 (plan 06-07). Open `/gsd:plan-phase --gaps` para gap closure.
    3. Salvar arquivo. NAO executar a UAT — o operator (Pedro) eh quem roda na Task 3 deste plano.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md && grep -c '^## Lifecycle' .planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md | grep -q '^3$' && grep -q 'EMERGENCY_TEMPLATE_IMAGE\|ghcr.io/ggml-org/llama.cpp' .planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md</automated>
  </verify>
  <done>
    `06-HUMAN-UAT.md` existe com 3 lifecycle sections (`## Lifecycle 1`, `## Lifecycle 2`, `## Lifecycle 3`) + Pre-requisitos + Sign-off + Failure handling sections. Referencia explicita a Strategy B image (`ghcr.io/ggml-org/llama.cpp:server-cuda-b9128`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar docs/RUNBOOK-EMERGENCY-POD.md refletindo Strategy B</name>
  <files>docs/RUNBOOK-EMERGENCY-POD.md</files>
  <read_first>
    docs/RUNBOOK-EMERGENCY-POD.md (arquivo inteiro — esperado existe per STATE.md:41; se nao existe, criar do zero)
    .planning/phases/06.5-auto-provisioning-emergency-pod-vast-ai/06.5-11-PLAN.md (referencia ao runbook original)
    .planning/phases/06-emergency-pod-template-refactor/06-CONTEXT.md (Strategy B Locked block)
  </read_first>
  <action>
    1. `test -f docs/RUNBOOK-EMERGENCY-POD.md` — se existe, ler e identificar secoes que referenciam Strategy A (custom image, Runtype=ssh, EMERGENCY_POD_IMAGE_TAG, ifix-ai-pod). Se nao existe, criar arquivo skeleton.
    2. Atualizar / criar as seguintes secoes:
       - **"Image source"** section: descrever Strategy B (`ghcr.io/ggml-org/llama.cpp:server-cuda-b9128` upstream HF-endorsed; tag pinned por build number); como atualizar via `EMERGENCY_TEMPLATE_IMAGE` env no Portainer UI; pattern para bumping (docker manifest inspect digest verification recommended).
       - **"Onstart behavior"** section: descrever que onstart eh inline na CreateRequest, faz `mc cp` Qwen weights de MinIO + sha256-c; se EMERGENCY_JINJA_TEMPLATE_KEY setado, tambem baixa Jinja. PID 1 do container eh `/app/llama-server` (Runtype=args preserves ENTRYPOINT).
       - **"Troubleshooting"** section: Adicionar 3 sintomas comuns:
         (a) `actual_status=running` mas `:8000/v1/models` connection-refused → onstart falhou no sha256-c; check Vast logs via UI ou `vast ssh <id>` se runtype permite.
         (b) Sentry alert "sha256 mismatch" → operator deve verificar `WEIGHTS_QWEN_SHA256` e `EMERGENCY_JINJA_TEMPLATE_SHA256` env vars contra arquivos repo (`pod/templates/qwen3.5-27b-tool-calling.jinja.sha256` + MinIO weights manifest).
         (c) Lifecycle stuck em `EMERGENCY_PROVISIONING` por >10min → cold-start budget excedido; check audit `events JSONB` column for last state + Vast `actual_status_msg` para hardware errors (STATE.md:81 GPU error detection tech debt — workaround manual `gatewayctl emerg force-destroy`).
       - **"Reverting to Strategy A (legacy custom image)"** section: GARANTIR esta secao SO EXISTE com aviso DEPRECATED — porque PR2 deletara as fontes. Apos PR2 merge esta secao deve ser removida tambem (note: "Strategy A no longer supported — image ghcr.io/ifixtelecom/ifix-ai-pod deleted; rollback requires reverting gateway commit"). Decision: deixar a secao com warning ate PR2 merge; depois remover.
       - **"Phase 6 refactor — date + commit reference"** appendix: "Refactored from Strategy A (custom GHCR image `ghcr.io/ifixtelecom/ifix-ai-pod` + Runtype=ssh — bug STATE.md:85 CMD-ignore) to Strategy B (`ghcr.io/ggml-org/llama.cpp:server-cuda-b9128` upstream + Runtype=args + inline onstart). See `.planning/phases/06-emergency-pod-template-refactor/` for full context."
    3. Salvar. Commit message: `docs(06-06): RUNBOOK reflects Strategy B emergency-pod template`
  </action>
  <verify>
    <automated>grep -q 'ghcr.io/ggml-org/llama.cpp:server-cuda-b9128' docs/RUNBOOK-EMERGENCY-POD.md && grep -q 'Runtype=args' docs/RUNBOOK-EMERGENCY-POD.md && grep -q 'EMERGENCY_TEMPLATE_IMAGE' docs/RUNBOOK-EMERGENCY-POD.md</automated>
  </verify>
  <done>
    Runbook atualizado com Strategy B references (image upstream + Runtype=args + EMERGENCY_TEMPLATE_IMAGE env). Pelo menos 3 secoes novas / atualizadas (Image source, Onstart, Troubleshooting). Secao "Reverting to Strategy A" mantida com warning.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator executa 3 lifecycles LIVE Vast.ai + assina UAT</name>
  <what-built>
    Plans 06-01..06-05 implementaram Strategy B em codigo (Phase 6 PR1). Tarefa 1 deste plano produziu o script de UAT; Tarefa 2 atualizou o runbook. Falta apenas executar os 3 cenarios live no Vast.ai e capturar evidencias.
  </what-built>
  <how-to-verify>
    1. Pre-flight check:
       - `ssh ops-claude` OR ficar no `ops-claude` host (este host)
       - Confirmar saldo Vast.ai >= $5: `vast show user`
       - Confirmar Portainer stack `ai-gateway-dev` tem 3 env vars novos: EMERGENCY_TEMPLATE_IMAGE=ghcr.io/ggml-org/llama.cpp:server-cuda-b9128, EMERGENCY_JINJA_TEMPLATE_KEY=emerg-onstart/templates/qwen3.5-27b-tool-calling-1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67.jinja (se B2 escolhido em 06-01), EMERGENCY_JINJA_TEMPLATE_SHA256=1067302cc6d927210a84775b9a060f724da15debc168c79710cbf763512e9f67. SE B1 escolhido: vars de Jinja vazios + image deve ser ghcr.io/ifixtelecom/qwen-templates:v1 ou similar — verificar 06-WAVE0-GATES.md.
       - Restart container ai-gateway-dev para pegar novos env vars: Portainer UI "Recreate" no stack.
       - Confirmar boot OK: `ssh vps-ifix-vm 'docker logs ai-gateway-dev --tail 30'` mostra `vast.Ping() OK` + reconciler iniciou.
    2. Executar Lifecycle 1 (happy path) per script em `.planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md`. Capturar:
       - Lifecycle ID (do output `gatewayctl emerg force-provision`)
       - Vast instance_id (do audit `SELECT vast_instance_id FROM ai_gateway.emergency_lifecycles WHERE id=<lc_id>`)
       - Cold-start duration (start_time → first audit event `state_change` para `emergency_active`)
       - `curl -m 10 http://<pod_url>:8000/v1/models` 200 (use port mapping from `vast show instance <id>`)
       - Custo USD do lifecycle (audit `total_cost_brl / usd_to_brl_rate`)
    3. Executar Lifecycle 2 (price-cap pressure). Capturar shutdown_reason + Sentry breadcrumb.
    4. Executar Lifecycle 3 (cancel-in-flight). Capturar shutdown_reason + verificar Vast.ai dashboard mostra instance destroyed.
    5. SE qualquer lifecycle FAIL: parar imediatamente, abrir bug report como quick-task ou novo plan 06-XX-PLAN.md.
    6. Preencher Sign-off table em `06-HUMAN-UAT.md` com 3 rows (1 por lifecycle), commit.
    7. Verificar STATE.md tech debt update — adicionar bullet "Phase 6 PR1 UAT GREEN 3/3 — STATE.md:85 bug RESOLVIDO" abaixo de Quick Tasks table.
  </how-to-verify>
  <resume-signal>Pedro responde "approved" + paste do sign-off table em 06-HUMAN-UAT.md commit hash + STATE.md updated. SE algum lifecycle FAIL: responde "failed — <description>" + abre gap closure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude → Vast.ai API | live UAT crosses this 3x (Bearer auth) |
| Vast.ai pod → MinIO | live weights + Jinja download (HMAC sigv4) |
| Vast.ai pod → ghcr.io/ggml-org | live image pull (anonymous HTTPS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Live ghcr.io pull during UAT | mitigate | Tag pinned `:server-cuda-b9128` immutable per ggml-org build; Vast.ai pulls over HTTPS; image is signed via GitHub Packages provenance (transparently verified). Test ASUMES upstream legit (RESEARCH.md security_domain "Approved upstream canonical") |
| T-06-05 | Availability | UAT may fail (burnt-bridge) | mitigate | Plan 06-07 (PR2 cleanup) eh BLOCKED ate 3/3 GREEN. Se UAT falha, PR1 stays merged mas custom image GHCR ainda existe (fallback path). Operator pode reverter via Portainer UI env edit (EMERGENCY_TEMPLATE_IMAGE=ghcr.io/ifixtelecom/ifix-ai-pod:latest-dev) sem code change |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Plan 06-06 nao instala packages em codigo gateway. Apt-get install dentro do pod Vast.ai (curl + ca-certificates) faz parte do onstart per plan 06-04 — risco aceito (Debian official repos) |
</threat_model>

<verification>
- `test -f .planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md` exit 0
- `grep -c '^## Lifecycle' .planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md` == 3
- `grep -q 'ghcr.io/ggml-org/llama.cpp:server-cuda-b9128' docs/RUNBOOK-EMERGENCY-POD.md`
- Apos Task 3: 3 lifecycles GREEN documentados no Sign-off section
- Apos Task 3: STATE.md updated com Phase 6 UAT result
</verification>

<success_criteria>
- Phase 6 PR1 codigo + 3 lifecycles LIVE Vast.ai GREEN end-to-end (search → create → cold-start ≤6min → /v1/models 200 → audit complete)
- Bug STATE.md:85 (runtype=ssh CMD-ignore) RESOLVIDO em produc - validado live
- Phase 6.5 HUMAN-UAT (06.5-11 task 3) DESBLOQUEADO — operator pode rodar separadamente
- PR2 plan 06-07 (delete) liberado para execucao
- Cold-start P90 ≤ 6min documented (SC #2 da Phase 6 ROADMAP.md:138)
- RUNBOOK atualizado refletindo Strategy B troubleshooting patterns
</success_criteria>

<output>
Create `.planning/phases/06-emergency-pod-template-refactor/06-06-SUMMARY.md` when done. Resumir: 3 lifecycle outcomes + cold-start medio + custo total + STATE.md tech debt update + decisao GO/NO-GO para PR2.
</output>