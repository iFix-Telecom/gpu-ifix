---
phase: 06-emergency-pod-template-refactor
plan: 03
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - gateway/internal/emerg/vast/types.go
autonomous: true
requirements: [PRV-06]
threat_refs: [T-06-02]
validates_sc: [SC-3]
tags: [vast-types, dto, refactor, phase-6-pr1]

must_haves:
  truths:
    - "`CreateRequest` struct ganha campo `Args []string` com JSON tag `args,omitempty` (Pitfall 5 RESEARCH.md:436 — campo eh `args`, NAO `image_args` nem `args_str`)"
    - "Comment do campo `Runtype` atualizado com 3 valores validos (`args` Strategy B, `ssh_proxy` legacy, `ssh` deprecated bug STATE.md:85)"
    - "Comment block topo do package vast (lines 1-28) atualizado com nota sobre Phase 6 Strategy B"
    - "go build da pacote vast continua passando isoladamente"
    - "json.Marshal de CreateRequest com Args preenchido emite `\"args\":[...]`; sem Args (vazio) omite o campo via omitempty"
  artifacts:
    - path: "gateway/internal/emerg/vast/types.go"
      provides: "CreateRequest struct estende com Args field"
      contains: "Args.*\\[\\]string.*json:\"args,omitempty\"|Runtype values:"
  key_links:
    - from: "gateway/internal/emerg/vast/types.go"
      to: "gateway/internal/emerg/vast/client.go (CreateInstance)"
      via: "json.Marshal(body CreateRequest)"
      pattern: "json\\.Marshal\\(body\\)"
    - from: "gateway/internal/emerg/vast/types.go"
      to: "gateway/internal/emerg/lifecycle.go (buildCreateRequest plan 06-04)"
      via: "vast.CreateRequest{Args: ...}"
      pattern: "CreateRequest\\{"
---

<objective>
Plan 06-03 adiciona o campo `Args []string` ao DTO `vast.CreateRequest` em `gateway/internal/emerg/vast/types.go`. Este eh o unico mudanca no package `vast` para suportar Strategy B Locked (CONTEXT.md D-06-B + D-07-B): `Runtype="args"` exige que o payload tenha o campo JSON `args` (array of strings) per `vast-cli/vast.py:2509` (VERIFIED em RESEARCH.md linhas 519-535).

Adicionalmente atualiza o comentario do campo `Runtype` (atualmente diz "ssh" como unico valor — agora reflete os 3 valores possiveis com warning sobre o bug Phase 6 STATE.md:85) e adiciona nota sobre Strategy B no package godoc topo do arquivo.

`vast/client.go:CreateInstance` (linha 199-238) usa `json.Marshal(body)` direto, que auto-serializa qualquer campo exportado novo via JSON tag — ZERO mudanca em client.go necessaria (PATTERNS.md linha 206 confirma).

Purpose: Wave 1 paralela com 06-02 (config). types.go nao tem overlap de arquivos com config.go, podem executar simultaneos. Plan 06-04 (Wave 2 lifecycle.go) depende deste plan para preencher `Args: []string{...}` na nova `buildCreateRequest`.

Output: 1 arquivo modificado (types.go) com diff de ~4-6 linhas (1 campo + 2 comments). Zero novos arquivos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-emergency-pod-template-refactor/06-CONTEXT.md
@.planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md
@.planning/phases/06-emergency-pod-template-refactor/06-PATTERNS.md

<interfaces>
<!-- Struct atual (types.go:120-129) -->

```go
// CreateRequest is the JSON body of PUT /asks/{offer_id}/.
//
// `Env` map keys are the literal strings Vast.ai uses to forward `docker
// run -p ...` flags — e.g. `"-p 9100:9100"` mapped to `"1"`. The peculiar
// shape is documented in `pod/scripts/vast-ai.sh` (Phase 1 reference).
//
// `Onstart` is a bash script (not a base64 wrapper) — the gateway sends
// the script body directly; Vast handles base64 encoding internally.
//
// `TargetState` defaults to "running" if omitted — kept explicit so test
// fixtures can stop the instance for the leader-recovery zombie test.
type CreateRequest struct {
    ClientID    string            `json:"client_id"` // always "me"
    Image       string            `json:"image"`
    Env         map[string]string `json:"env"`
    Onstart     string            `json:"onstart"`
    Runtype     string            `json:"runtype"` // "ssh"
    Disk        int               `json:"disk"`
    Label       string            `json:"label"`
    TargetState string            `json:"target_state,omitempty"` // "running" default
}
```

<!-- Struct novo per PATTERNS.md lines 163-181 -->

```go
type CreateRequest struct {
    ClientID    string            `json:"client_id"` // always "me"
    Image       string            `json:"image"`
    Env         map[string]string `json:"env"`
    Onstart     string            `json:"onstart"`
    // Runtype values: "args" (Phase 6 Strategy B Locked — preserves image
    // ENTRYPOINT, args field is REMAINDER list passed to entrypoint),
    // "ssh_proxy" (Vast injects sshd sidecar; REPLACES ENTRYPOINT with
    // vast-ai/base-image chain — see RESEARCH.md Pitfall 1),
    // "ssh" (deprecated alias for ssh_proxy; Phase 6 root cause STATE.md:85
    // bug — CMD silently ignored, lifecycles 29-33 timeouts).
    Runtype     string            `json:"runtype"`
    // Args is the JSON `args` field (NOT image_args, NOT args_str — VERIFIED
    // via vast-cli/vast.py:2509 `json_blob["args"] = args.args`, RESEARCH.md
    // Pitfall 5 line 436). Array of CLI tokens passed REMAINDER-style to the
    // image ENTRYPOINT when Runtype="args". Omitempty so ssh_proxy / ssh
    // runtypes do not send the field on the wire.
    Args        []string          `json:"args,omitempty"`
    Disk        int               `json:"disk"`
    Label       string            `json:"label"`
    TargetState string            `json:"target_state,omitempty"` // "running" default
}
```

<!-- Existing client.go:CreateInstance (linha 199-203) — JA suporta novo campo via json.Marshal -->

```go
func (c *Client) CreateInstance(ctx context.Context, offerID int64, body CreateRequest) (Instance, error) {
    payload, err := json.Marshal(body)  // serializa Args automaticamente via JSON tag
    ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar campo Args + atualizar comments Runtype + package godoc</name>
  <files>gateway/internal/emerg/vast/types.go</files>
  <read_first>
    gateway/internal/emerg/vast/types.go (arquivo inteiro — 186 linhas)
    .planning/phases/06-emergency-pod-template-refactor/06-PATTERNS.md (lines 142-189 types.go pattern)
    .planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md (lines 436-444 Pitfall 5 — args vs image_args vs args_str)
    .planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md (lines 519-535 Verified vast-cli source — JSON body construction)
  </read_first>
  <behavior>
    - Test 1: `json.Marshal(CreateRequest{Args: []string{"--host","0.0.0.0"}})` emite substring `"args":["--host","0.0.0.0"]`
    - Test 2: `json.Marshal(CreateRequest{})` (Args zero-value) NAO emite substring `"args":` (omitempty)
    - Test 3: `json.Marshal(CreateRequest{Args: []string{}})` (Args empty slice nao-nil) — Go json/v1 emite `[]` ou omite — testar comportamento real e documentar (`reflect.DeepEqual([]string{},nil) == false` mas json.Marshal trata diferente per spec)
    - Test 4: Field name na JSON tag eh exatamente `args` lowercase (NAO `image_args`, NAO `args_str`)
  </behavior>
  <action>
    Em `gateway/internal/emerg/vast/types.go`:
    1. No package godoc topo (linhas 1-28): ADICIONAR no final do bloco (antes do `package vast`) uma 5a bullet:
       ```
       //   - Phase 6 Strategy B Locked (CONTEXT.md D-06-B + D-07-B): `Runtype="args"`
       //     preserves the image ENTRYPOINT and passes `Args []string` as the JSON
       //     `args` REMAINDER field (NOT `image_args`, NOT `args_str` — VERIFIED via
       //     vast-cli/vast.py:2509). Earlier Phase 6.5 lifecycles used `Runtype="ssh"`
       //     which silently overrode the image CMD (STATE.md:85 — bug fixed here).
       ```
    2. No struct `CreateRequest` (linha 120-129): substituir comment inline da linha 125 (`// "ssh"`) pelo bloco multi-line de 5 linhas per `<interfaces>` (3 valores explicados: args, ssh_proxy, ssh).
    3. INSERIR novo campo `Args []string` IMEDIATAMENTE APOS o campo `Runtype` (entre linha 125 e 126), com bloco de comment de 4 linhas per `<interfaces>` (cita Pitfall 5 + vast-cli source).
    4. NAO mudar nenhum outro campo. NAO renomear nada. Ordem dos campos preservada (ClientID, Image, Env, Onstart, Runtype, Args, Disk, Label, TargetState).
    5. NAO criar um arquivo de teste `types_test.go` novo se nao existir — os 4 behaviors acima podem ser cobertos no plan 06-04 dentro de `lifecycle_test.go::TestBuildCreateRequest_JSONShape` (PATTERNS.md lines 344-354). Se ja existe `types_test.go` no diretorio, adicionar 1 funcao curta `TestCreateRequestArgsOmitempty` que cobre Tests 1-4 acima.
  </action>
  <verify>
    <automated>cd gateway && go build ./internal/emerg/vast/... && go vet ./internal/emerg/vast/...</automated>
  </verify>
  <done>
    `go build ./internal/emerg/vast/...` GREEN. `grep -c 'Args.*\[\]string.*json:"args,omitempty"' gateway/internal/emerg/vast/types.go` retorna 1. `grep -c 'Runtype values:' gateway/internal/emerg/vast/types.go` retorna 1. `gateway/internal/emerg/vast/client.go` NAO foi modificado (`git diff --name-only HEAD gateway/internal/emerg/vast/client.go` vazio).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway Go → Vast.ai API | CreateRequest JSON body cruza este boundary (Bearer auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | Vast.ai REST API payload (in-flight) | accept | Vast API enforces Bearer auth via `Authorization: Bearer ${VAST_AI_API_KEY}` over HTTPS. JSON serialization (json.Marshal) eh deterministica via struct tags — sem usuario-input free-form em qualquer campo do CreateRequest. `Args` campo eh hardcoded const em lifecycle.go (plan 06-04) — nao chega de input externo |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Plan 06-03 nao instala packages — apenas modifica DTO struct |
</threat_model>

<verification>
- `cd gateway && go build ./internal/emerg/vast/...` GREEN
- `cd gateway && go vet ./internal/emerg/vast/...` GREEN
- `grep -c 'json:"args,omitempty"' gateway/internal/emerg/vast/types.go` == 1
- `grep -c 'image_args\|args_str' gateway/internal/emerg/vast/types.go` == 0 (nao introduzir nomes errados, mesmo em comentario excepto para warning)
- client.go nao foi modificado: `git diff --quiet HEAD -- gateway/internal/emerg/vast/client.go` exit 0
</verification>

<success_criteria>
- Campo `Args []string` adicionado a `CreateRequest` struct com JSON tag `args,omitempty`
- Comentario Runtype documenta os 3 valores (args, ssh_proxy, ssh) e cita Phase 6 fix STATE.md:85
- json.Marshal omite `args` quando vazio (Go omitempty); inclui quando preenchido
- client.go intacto — `json.Marshal(body)` lida com novo field via JSON tag automaticamente
</success_criteria>

<output>
Create `.planning/phases/06-emergency-pod-template-refactor/06-03-SUMMARY.md` when done. Resumir: 1 campo adicionado, 2 comentarios atualizados, evidence de que client.go nao precisou mudar.
</output>