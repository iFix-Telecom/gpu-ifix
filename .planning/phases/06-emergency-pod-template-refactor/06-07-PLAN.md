---
phase: 06-emergency-pod-template-refactor
plan: 07
type: execute
wave: 5
depends_on: [06-06]
files_modified:
  - pod/Dockerfile
  - pod/scripts/emerg-bootstrap.sh
  - .github/workflows/build-pod.yml
  - docs/RUNBOOK-EMERGENCY-POD.md
autonomous: false
requirements: [PRV-06]
threat_refs: [T-06-05]
validates_sc: [SC-5]
tags: [cleanup, delete-pr2, phase-6-pr2, post-uat]
user_setup:
  - service: ghcr.io
    why: "Operator opcional cleanup do package ghcr.io/ifixtelecom/ifix-ai-pod (image GHCR Ifix) apos PR2 merge"
    dashboard_config:
      - task: "Verificar nenhuma stack Portainer (dev OU prod) ainda referencia ghcr.io/ifixtelecom/ifix-ai-pod"
        location: "Portainer UI -> Stacks -> ai-gateway-{dev,prod} -> Env"
      - task: "Apos 2-week observability window: gh api -X DELETE /orgs/ifixtelecom/packages/container/ifix-ai-pod (delete completo do GHCR, free namespace)"
        location: "github.com/orgs/ifixtelecom/packages"

must_haves:
  truths:
    - "`pod/Dockerfile` deletado (-77 linhas)"
    - "`pod/scripts/emerg-bootstrap.sh` deletado (-73 linhas)"
    - "`.github/workflows/build-pod.yml` deletado (-254 linhas)"
    - "RUNBOOK-EMERGENCY-POD.md secao 'Reverting to Strategy A' atualizada para refletir que rollback EXIGE git revert do PR1 (custom image GHCR ja deletada)"
    - "Plan 06-06 sign-off 3 lifecycles GREEN VERIFIED como pre-requisito (gate humano blocking)"
    - "Phase 1 `pod/onstart.sh` + `pod/scripts/download-weights.sh` + `pod/health-bridge/` PRESERVADOS — sao usados pelo path Phase 1 primary pod (custom HOST-mode docker-compose), nao pelo emergency pod refactored"
    - "Phase 1 `.github/workflows/smoke.yml` PRESERVADO — testa primary pod path via pod/Dockerfile-equivalent OU substituido por novo workflow (smoke.yml depende do Dockerfile? verificar)"
    - "Apos delete, full-suite gateway test continua GREEN (cleanup nao quebra emerg integration tests porque fake Vast client nao usa Dockerfile)"
  artifacts:
    - path: "pod/Dockerfile"
      provides: "DELETADO"
      contains: ""
    - path: "pod/scripts/emerg-bootstrap.sh"
      provides: "DELETADO"
      contains: ""
    - path: ".github/workflows/build-pod.yml"
      provides: "DELETADO"
      contains: ""
  key_links:
    - from: ".github/workflows/smoke.yml (Phase 1)"
      to: "pod/Dockerfile (potentially)"
      via: "build step references"
      pattern: "FROM|build|Dockerfile"
---

<objective>
Plan 06-07 eh o **PR2 cleanup** — deleta os artefatos da Strategy A custom image apos Strategy B ser validada live (gate plan 06-06 task 3 GREEN 3/3). Per CONTEXT.md D-08-B + `<specifics>` linha 163, PR1/PR2 split eh mitigation factor para burnt-bridge risk.

Arquivos deletados (4 total, ~400 linhas):
1. `pod/Dockerfile` (77 linhas) — custom image GHCR baseada em `ghcr.io/ggml-org/llama.cpp:server-cuda-b9128` que apenas adicionava Jinja + emerg-bootstrap.sh. Redundante apos Strategy B usar upstream direct.
2. `pod/scripts/emerg-bootstrap.sh` (73 linhas) — bootstrap script baked como CMD da Dockerfile. Logica MinIO + sha256-c migrou para `lifecycle.go::emergencyOnstartB2` em plan 06-04.
3. `.github/workflows/build-pod.yml` (254 linhas) — CI workflow que builda + push para GHCR. Sem image custom, sem necessidade do workflow.
4. `pod/sshd_config` (se existe) — config sshd baked. Strategy B Runtype=args nao tem sshd in-container.

PRESERVADOS (CRITICAL — nao deletar):
- `pod/onstart.sh` — usado pelo PRIMARY pod Vast.ai (Phase 1 D-01..D-08). Phase 6 nao toca PRIMARY path.
- `pod/scripts/download-weights.sh` — usado por onstart.sh primary path.
- `pod/scripts/upload-weights.sh` — operator utility para upload weights MinIO.
- `pod/scripts/vast-ai.sh` — operator CLI helper.
- `pod/health-bridge/` — Phase 1 health bridge sidecar (Phase 6.5 D-C2 omitiu pra emergency pod, mas primary pod USA).
- `pod/docker-compose.yml` — primary pod multi-service stack.
- `pod/templates/qwen3.5-27b-tool-calling.jinja` + `.sha256` — usado por primary pod + (se Strategy B option B2) uploaded MinIO via plan 06-01 Task 3. Se decisao foi B1: tambem usado pelo overlay image `ghcr.io/ifixtelecom/qwen-templates`.
- `pod/smoke/` — Phase 1 smoke test fixture.
- `pod/README.md`, `pod/.env.example`, `pod/weights/` — docs + fixtures.
- `.github/workflows/smoke.yml` — Phase 1 smoke pipeline. Verificar dependencia de Dockerfile antes de remover/manter (action item da Task 1).

Adicionalmente atualiza RUNBOOK section "Reverting to Strategy A" para refletir que rollback agora requer `git revert` do PR1.

Purpose: Burnt-bridge irrevocavel — apos PR2 merge, rollback exige code change. Operator confirmacao explicita necessaria.

Output: 4 arquivos deletados (~400 lines net), 1 doc updated. SEM mudanca em codigo Go (gateway/), SEM mudanca em config — todos esses ja vieram nos plans Wave 1+2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-emergency-pod-template-refactor/06-CONTEXT.md
@.planning/phases/06-emergency-pod-template-refactor/06-RESEARCH.md
@.planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md
@.planning/phases/06-emergency-pod-template-refactor/06-06-SUMMARY.md

<interfaces>
<!-- Pre-flight gate: confirmar UAT GREEN antes de comecar -->

```bash
# Espera-se ver "GREEN 3/3" no SUMMARY do plano 06-06
test -f .planning/phases/06-emergency-pod-template-refactor/06-06-SUMMARY.md
grep -q "GREEN 3/3\|3 lifecycles consecutivos\|GO for PR2" .planning/phases/06-emergency-pod-template-refactor/06-06-SUMMARY.md
```

<!-- Arquivos a deletar (verificacao previa) -->

```bash
ls -la pod/Dockerfile pod/scripts/emerg-bootstrap.sh .github/workflows/build-pod.yml
# pod/sshd_config — verificar existencia (PATTERNS.md:23 noted "NAO existe no repo")
test -f pod/sshd_config && echo EXISTS || echo NOT_EXISTS
```

<!-- Arquivos a PRESERVAR (verificacao explicita pra evitar delete acidental) -->

```bash
ls -la \
  pod/onstart.sh \
  pod/scripts/download-weights.sh \
  pod/scripts/upload-weights.sh \
  pod/scripts/vast-ai.sh \
  pod/health-bridge/ \
  pod/docker-compose.yml \
  pod/templates/qwen3.5-27b-tool-calling.jinja \
  pod/templates/qwen3.5-27b-tool-calling.jinja.sha256 \
  pod/smoke/ \
  pod/README.md \
  pod/.env.example \
  pod/weights/ \
  .github/workflows/smoke.yml
# TODOS devem existir apos plan 06-07
```

<!-- smoke.yml dependency on Dockerfile (Task 1 action item) -->

```bash
grep -n 'Dockerfile\|build-pod\|ifix-ai-pod' .github/workflows/smoke.yml
# Se referencia Dockerfile direto: precisa decidir se smoke.yml fica obsoleto OU se atualiza para usar upstream image
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight gate — confirmar UAT GREEN + Portainer env updated + smoke.yml impact</name>
  <what-built>
    Plan 06-06 produziu 3 lifecycles GREEN (espera-se). Antes de deletar artefatos Strategy A, operator confirma 3 condicoes:
    (a) UAT sign-off table em `06-HUMAN-UAT.md` tem 3 lifecycles com status `GREEN`/`PASS`.
    (b) Portainer stack `ai-gateway-{dev,prod}` env vars refletem Strategy B (EMERGENCY_TEMPLATE_IMAGE setado, EMERGENCY_POD_IMAGE_TAG REMOVIDO OU vazio).
    (c) smoke.yml (Phase 1 workflow) dependencia em pod/Dockerfile foi analizada — escolha: A) preservar Dockerfile + smoke.yml (Phase 1 primary path inalterado, mas custom image GHCR ainda buildada por commits ao `pod/`); B) tambem deletar smoke.yml + reescrever (escopo maior — adia para nova phase); C) atualizar smoke.yml para usar `ghcr.io/ggml-org/llama.cpp` upstream direto sem Dockerfile.
  </what-built>
  <how-to-verify>
    1. `cat .planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md | grep -A 20 'Sign-off'` — confirma table tem 3 rows preenchidos com cold-start ≤6min.
    2. `cat .planning/phases/06-emergency-pod-template-refactor/06-06-SUMMARY.md | grep -i 'green\|pass\|go for pr2'` — confirma decisao GO/NO-GO documented.
    3. Operator (Pedro) acessa Portainer UI:
       - https://portainer3.ifixtelecom.com.br -> Stacks -> ai-gateway-dev -> Environment
       - Confirma EMERGENCY_TEMPLATE_IMAGE setado para `ghcr.io/ggml-org/llama.cpp:server-cuda-b9128`
       - Confirma EMERGENCY_POD_IMAGE_TAG NAO existe OU vazio
       - Repete para ai-gateway-prod (se ja deployado)
    4. `grep -n 'Dockerfile\|build-pod\|ifix-ai-pod' .github/workflows/smoke.yml` — analisa output:
       - Se `smoke.yml` builda a image: precisa decisao operator (manter smoke.yml builda + Dockerfile preserved, OU rewrite smoke.yml para usar upstream).
       - Se `smoke.yml` apenas USA imagem GHCR ja published: smoke.yml continua funcionando ate operator deletar GHCR package (out-of-scope, deferred).
    5. Pedro escolhe decisao smoke.yml: A / B / C (per `<what-built>` acima).
  </how-to-verify>
  <resume-signal>
    Pedro responde com:
    (a) "UAT GREEN 3/3 confirmed in 06-HUMAN-UAT.md commit <sha>"
    (b) "Portainer ai-gateway-dev env updated (EMERGENCY_TEMPLATE_IMAGE set, EMERGENCY_POD_IMAGE_TAG removed)"
    (c) "smoke.yml decision: [A|B|C] + reasoning"

    SE qualquer pre-flight falha: nao avancar; abrir gap closure plan.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Deletar 3 (ou 4 se sshd_config existe) arquivos Strategy A + verificar preservados</name>
  <files>pod/Dockerfile, pod/scripts/emerg-bootstrap.sh, .github/workflows/build-pod.yml</files>
  <read_first>
    pod/Dockerfile (77 linhas — leitura ultima vez antes de delete; arquivar trecho em SUMMARY)
    pod/scripts/emerg-bootstrap.sh (73 linhas — idem; sha256 logica esta migrada em lifecycle.go::emergencyOnstartB2)
    .github/workflows/build-pod.yml (254 linhas — idem; CI workflow obsoleto)
  </read_first>
  <action>
    1. Pre-delete sanity check (artefatos PRESERVADOS):
       ```
       for f in pod/onstart.sh pod/scripts/download-weights.sh pod/scripts/upload-weights.sh pod/scripts/vast-ai.sh pod/docker-compose.yml pod/templates/qwen3.5-27b-tool-calling.jinja pod/templates/qwen3.5-27b-tool-calling.jinja.sha256 pod/README.md pod/.env.example .github/workflows/smoke.yml; do
         test -f "$f" || { echo "MISSING PRESERVED FILE: $f"; exit 1; }
       done
       test -d pod/health-bridge || { echo "MISSING pod/health-bridge/"; exit 1; }
       test -d pod/smoke || { echo "MISSING pod/smoke/"; exit 1; }
       test -d pod/weights || { echo "MISSING pod/weights/"; exit 1; }
       ```
       Se algum preserved file/dir nao existe: abort task. Investigar antes de prosseguir.
    2. Deletar arquivos Strategy A:
       ```
       git rm pod/Dockerfile pod/scripts/emerg-bootstrap.sh .github/workflows/build-pod.yml
       # Verificar pod/sshd_config — se existe, deletar tambem:
       test -f pod/sshd_config && git rm pod/sshd_config
       ```
    3. Smoke.yml handling baseado em decisao Task 1:
       - Decisao A (preserva): NAO tocar smoke.yml; documentar em SUMMARY que Phase 1 primary path inalterado.
       - Decisao B (delete tambem): `git rm .github/workflows/smoke.yml`. Documentar em SUMMARY que primary pod smoke CI removido — sera reescrito em nova phase.
       - Decisao C (rewrite): NAO incluida neste plano — abre quick-task ou novo plan 06-XX-PLAN.md.
    4. Verificar build status com arquivos deletados:
       ```
       cd gateway && go build ./...
       cd gateway && go test ./... -count=1 -short -timeout 120s
       ```
       Espera-se: GREEN. Como gateway code nao referencia Dockerfile/emerg-bootstrap.sh/build-pod.yml, nao deve haver impacto.
    5. NAO commit ainda — Task 3 atualiza runbook antes do commit unico.
  </action>
  <verify>
    <automated>! test -f pod/Dockerfile && ! test -f pod/scripts/emerg-bootstrap.sh && ! test -f .github/workflows/build-pod.yml && test -f pod/onstart.sh && test -f pod/scripts/download-weights.sh && test -d pod/health-bridge && cd gateway && go build ./... && go test ./... -count=1 -short -timeout 120s 2>&1 | tail -5 | grep -q '^ok\b'</automated>
  </verify>
  <done>
    3 (ou 4) arquivos Strategy A deletados via `git rm`. Todos arquivos PRESERVADOS continuam existindo. `go build ./...` GREEN. `go test ./... -short` GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atualizar RUNBOOK secao "Reverting to Strategy A" para refletir delete</name>
  <files>docs/RUNBOOK-EMERGENCY-POD.md</files>
  <read_first>
    docs/RUNBOOK-EMERGENCY-POD.md (esperado: editado por plan 06-06 Task 2; secao "Reverting to Strategy A" tinha warning DEPRECATED)
  </read_first>
  <action>
    1. Localizar secao `## Reverting to Strategy A` (criada em plan 06-06 Task 2).
    2. Substituir conteudo dessa secao por:
       ```markdown
       ## Reverting to Strategy A (legacy custom image)

       **NO LONGER POSSIBLE via config rollback.** PR2 (Phase 6 plan 06-07) deleted:
       - `pod/Dockerfile`
       - `pod/scripts/emerg-bootstrap.sh`
       - `.github/workflows/build-pod.yml`

       To revert to Strategy A:

       1. `git revert <PR2-merge-commit>` to restore Dockerfile + script + workflow.
       2. `git revert <PR1-merge-commit>` to restore Cfg.EmergencyPodImageTag + lifecycle.go Strategy A buildCreateRequest.
       3. CI re-builds `ghcr.io/ifixtelecom/ifix-ai-pod:latest-dev` (if GHCR package still exists — see GHCR cleanup section below).
       4. Portainer ai-gateway-dev env: change `EMERGENCY_TEMPLATE_IMAGE` back to `EMERGENCY_POD_IMAGE_TAG=latest-dev`.
       5. Restart gateway container.

       Total rollback time: ~30 min (vs ~2 min for config-only rollback that was available before PR2).

       **Pre-PR2 rollback path (config-only):** documented for historical reference but no longer applicable. Operator who needs custom image must follow git revert path above.

       ## GHCR Cleanup (optional, post-2-week-observability-window)

       `ghcr.io/ifixtelecom/ifix-ai-pod` package on GitHub Container Registry was NOT deleted by PR2 — it remains as a safety net during the 2-week observability window after PR2 merge. After confidence in Strategy B is established (2 weeks of stable emergency lifecycles), operator can free the namespace:

       ```bash
       gh api -X DELETE /orgs/ifixtelecom/packages/container/ifix-ai-pod
       ```

       After this command runs, `git revert` rollback (above) will fail at step 3 (CI re-build) unless operator also re-creates the GHCR package and re-pushes manually.
       ```
    3. Adicionar bullet em "Phase 6 refactor" appendix: `- PR2 (date YYYY-MM-DD): deleted pod/Dockerfile, pod/scripts/emerg-bootstrap.sh, .github/workflows/build-pod.yml. Strategy A no longer reversible without git revert.`
    4. Commit unico cobrindo Task 2 + Task 3: `chore(06-07): PR2 — delete Strategy A artifacts (Dockerfile + emerg-bootstrap.sh + build-pod.yml) + runbook update`
  </action>
  <verify>
    <automated>grep -q 'NO LONGER POSSIBLE via config rollback' docs/RUNBOOK-EMERGENCY-POD.md && grep -q 'git revert.*PR2' docs/RUNBOOK-EMERGENCY-POD.md && grep -q 'GHCR Cleanup' docs/RUNBOOK-EMERGENCY-POD.md</automated>
  </verify>
  <done>
    Runbook secao "Reverting to Strategy A" reflete realidade pos-PR2 (delete irrevocavel sem git revert). GHCR cleanup section adicionada com comando `gh api`. Phase 6 refactor appendix updated.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator final review — confirma PR2 escopo + STATE.md update</name>
  <what-built>
    Tasks 1-3 executaram delete + runbook update. Antes de marcar Phase 6 completa, operator confirma:
    (a) `git diff HEAD~1 --stat` mostra ~3-4 arquivos deletados + 1 doc atualizado (sem surpresas em outros arquivos).
    (b) Build + test gateway continua GREEN.
    (c) STATE.md updated com Phase 6 status COMPLETO + reflete que Phase 6.5 HUMAN-UAT esta desbloqueado.
    (d) ROADMAP.md Phase 6 marker `[x]` (complete).
  </what-built>
  <how-to-verify>
    1. `git log --oneline -5 && git diff --stat HEAD~2 HEAD` — review last 2 commits (plans 06-06 + 06-07).
    2. Confirmar deletes esperados:
       - `pod/Dockerfile` (-77 lines)
       - `pod/scripts/emerg-bootstrap.sh` (-73 lines)
       - `.github/workflows/build-pod.yml` (-254 lines)
       - (opcional) `pod/sshd_config` ou `.github/workflows/smoke.yml`
    3. `cd gateway && go test ./... -count=1 -timeout 300s` (full suite com -race opcional) — espera-se GREEN.
    4. Atualizar STATE.md:
       - Linha "Phase 6 (NOVO 2026-05-16): Emergency-Pod Template Refactor" — mudar para `[x]` complete + adicionar referencia 3 lifecycles GREEN + custos
       - Bullet "Tech debt CRITICO STATE.md:85" — marcar `[x] RESOLVIDO via Phase 6 plan 06-04 + UAT plan 06-06`
       - Adicionar bullet "Phase 6.5 HUMAN-UAT — DESBLOQUEADO (Phase 6 GREEN). Next: operator runs 06.5-11-PLAN.md task 3"
    5. Atualizar ROADMAP.md Phase 6 entry — `[ ]` → `[x]`.
    6. Commit `docs(state,roadmap): Phase 6 COMPLETE — Strategy B emergency-pod refactor green`.
  </how-to-verify>
  <resume-signal>
    Pedro responde "approved" + STATE/ROADMAP commit sha. SE diff inesperado: investiga antes de prosseguir.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git history → operator rollback path | PR2 makes rollback path heavier (git revert vs config flip) — documented threat T-06-05 |
| GHCR package lifecycle | Custom image preserved on GHCR for 2-week observability — eventual operator delete out-of-scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Availability | Burnt-bridge — rollback irrevocavel sem git revert | mitigate | 4 gates blocking: (1) plan 06-06 3 lifecycles GREEN; (2) plan 06-07 Task 1 operator pre-flight (Portainer env + UAT confirm + smoke.yml decision); (3) plan 06-07 Task 4 operator final review com diff stat; (4) GHCR package preserved 2 weeks — runbook documents how to reverse |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Plan 06-07 nao instala packages — apenas deleta arquivos |
</threat_model>

<verification>
- `test ! -f pod/Dockerfile` exit 0 (deletado)
- `test ! -f pod/scripts/emerg-bootstrap.sh` exit 0 (deletado)
- `test ! -f .github/workflows/build-pod.yml` exit 0 (deletado)
- `test -f pod/onstart.sh && test -d pod/health-bridge && test -f pod/templates/qwen3.5-27b-tool-calling.jinja` exit 0 (preservados)
- `cd gateway && go build ./... && go test ./... -count=1 -short -timeout 180s` GREEN
- RUNBOOK contem secoes "Reverting to Strategy A" + "GHCR Cleanup"
- STATE.md Phase 6 marker = complete
- ROADMAP.md Phase 6 marker = `[x]`
</verification>

<success_criteria>
- 3 (ou 4) arquivos Strategy A deletados via `git rm`
- Arquivos preservados (Phase 1 primary path) intactos
- Build + test gateway GREEN apos cleanup
- RUNBOOK refletido pos-delete (rollback requer git revert)
- STATE.md + ROADMAP.md atualizados para Phase 6 complete
- Phase 6.5 HUMAN-UAT desbloqueado (Phase 6 dependency fechada)
- GHCR package preserved (2-week observability window per runbook)
</success_criteria>

<output>
Create `.planning/phases/06-emergency-pod-template-refactor/06-07-SUMMARY.md` when done. Resumir: arquivos deletados (paths + line counts), preserved files verificados, smoke.yml decisao, STATE/ROADMAP updates, links para 06-VERIFICATION.md (proximo passo: `/gsd:verify-work`).
</output>