---
phase: 10-prod-deploy-ai-gateway
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - gateway/docs/RUNBOOK-DEPLOY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PRD-04]    # PRD-04 partial — RUNBOOK-DEPLOY.md only (D-18); full incident runbook → Phase 11
user_setup: []
tags: [phase-10, prod-deploy, runbook, requirements-remap, phase-11-placeholder]
must_haves:
  truths:
    - "Operator can read RUNBOOK-DEPLOY.md alone to perform first-time bring-up, roll-forward, rollback, or cut-release without touching CONTEXT or RESEARCH"
    - "REQUIREMENTS.md §Traceability shows PRD-01/02/03/05/06 mapped to Phase 11 (not Phase 10) — closing the D-16 split"
    - "PRD-04 keeps a Phase 10 row tagged 'partial — DEPLOY only' + a new Phase 11 row tagged 'full incident runbook'"
    - "ROADMAP.md has a Phase 11 placeholder section listing the deferred PRD IDs with Depends-on: Phase 10"
  artifacts:
    - path: "gateway/docs/RUNBOOK-DEPLOY.md"
      provides: "Operator playbook: Triggers / Preconditions / Steps (first-time + roll-forward + rollback + cut-release) / Verification / Rollback / Postmortem stub"
      contains: "## Triggers"
      min_lines: 200
    - path: ".planning/REQUIREMENTS.md"
      provides: "§Traceability table with PRD-01/02/03/05/06 mapped to Phase 11 + PRD-04 split row + PRD-07 + INT-06 kept on Phase 10"
      contains: "Phase 11: prod-hardening"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11 placeholder section after Phase 10"
      contains: "### Phase 11: prod-hardening"
  key_links:
    - from: "gateway/docs/RUNBOOK-DEPLOY.md"
      to: "scripts/deploy/preflight.sh + bootstrap-postgres.sh + migrate-dashboard.sh + cf-dns-create.sh"
      via: "Step-by-step references in the Steps section"
      pattern: "scripts/deploy/(preflight|bootstrap-postgres|migrate-dashboard|cf-dns-create)\\.sh"
    - from: "gateway/docs/RUNBOOK-DEPLOY.md"
      to: ".planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml"
      via: "rsync target documented in Step 1"
      pattern: "ai-gateway-prod\\.yml"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md (Phase 11 deferred IDs)"
      via: "shared PRD-01/02/03/05/06 IDs"
      pattern: "PRD-0[12356]"
---

<objective>
Ship the Phase 10 documentation deliverables that close PRD-04 (partial — DEPLOY runbook only, per D-18) and finalize the D-16 split that moves PRD-01/02/03/05/06 from Phase 10 to a new Phase 11. Three files: (a) `gateway/docs/RUNBOOK-DEPLOY.md` (mirrors RUNBOOK-FAILOVER.md / RUNBOOK-PRIMARY-POD.md header structure; references Plan 10-01/02/03 scripts + artifacts as the executable steps); (b) `.planning/REQUIREMENTS.md` §Traceability remap; (c) `.planning/ROADMAP.md` Phase 11 placeholder.

Purpose: PRD-04 partial coverage requires a real artifact, not a placeholder. Operator Plan 10-06 HUMAN-UAT reads this RUNBOOK during the live deploy. Independently, the requirements traceability table MUST reflect what Phase 10 actually delivers (otherwise verify-work flags missing coverage when those rows are inspected).

Output: 3 files modified/created; verified by grep-positive header sentinels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-prod-deploy-ai-gateway/10-CONTEXT.md
@.planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md
@.planning/phases/10-prod-deploy-ai-gateway/10-PATTERNS.md

# Analog runbooks
@gateway/docs/RUNBOOK-FAILOVER.md
@gateway/docs/RUNBOOK-PRIMARY-POD.md

# Phase 10 Wave 0 / Wave 1 scripts the runbook references
@gateway/docker-compose.prod.yml
@gateway/.env.prod.example
@scripts/deploy/preflight.sh
@scripts/deploy/bootstrap-postgres.sh
@scripts/deploy/migrate-dashboard.sh
@scripts/deploy/cf-dns-create.sh
@.planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml

<interfaces>
<!-- Header structure analog from RUNBOOK-FAILOVER.md (read the file's top 15 lines + section grep) -->
Section header convention (positive-assertion grep gate per RESEARCH How To #8):
- `## Triggers`
- `## Preconditions`
- `## Steps — First-Time Bring-Up`
- `## Steps — Roll-Forward to new :v1.0.X`
- `## Steps — Rollback to previous :v1.0.X-1`
- `## Steps — Cut-Release Procedure`
- `## Verification`
- `## Rollback`
- `## Postmortem stub`

Verification gate: `grep -cE "^## (Triggers|Preconditions|Steps|Verification|Rollback)$" gateway/docs/RUNBOOK-DEPLOY.md` >= 5

<!-- REQUIREMENTS.md current state — read with grep to find Phase 10 rows -->
Current rows in §Traceability (lines 220-228) all map PRD-01..PRD-07 to "Phase 10: Production Hardening & GA". D-16 splits 5 of them out:
- KEEP on Phase 10: INT-06 (line 220), PRD-04 (partial), PRD-07
- MOVE to Phase 11: PRD-01, PRD-02, PRD-03, PRD-05, PRD-06
- PRD-04 gets a Phase 10 row marked "partial — DEPLOY runbook only" + a new Phase 11 row marked "full incident runbook"

<!-- ROADMAP.md current state — Phase 10 placeholder at lines 16-26 (Goal: [To be planned]) -->
Existing Phase 11 reference: NONE. This plan creates the placeholder section.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author gateway/docs/RUNBOOK-DEPLOY.md</name>
  <files>gateway/docs/RUNBOOK-DEPLOY.md</files>
  <read_first>
    - gateway/docs/RUNBOOK-FAILOVER.md (section header convention — read top 50 lines + run `grep -n "^## " gateway/docs/RUNBOOK-FAILOVER.md`)
    - gateway/docs/RUNBOOK-PRIMARY-POD.md (Triggers/Preconditions/Steps/Verification structure — closest analog by content shape)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §How To #8 (RUNBOOK-DEPLOY.md scope + structure — full ~80-line draft)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §How To #6 (release cut procedure)
    - .planning/phases/10-prod-deploy-ai-gateway/10-PATTERNS.md §Pattern 4 (RUNBOOK family analog)
    - All Phase 10 Wave 0/Wave 1 scripts (so each Step references the correct script name + env var contract)
  </read_first>
  <action>
    Create `gateway/docs/RUNBOOK-DEPLOY.md`. Mirror RUNBOOK-FAILOVER.md / RUNBOOK-PRIMARY-POD.md header convention. Required top-level headers (positive-assertion grep gate counts ≥5 of these): `## Triggers`, `## Preconditions`, `## Steps`, `## Verification`, `## Rollback`. Body structure per RESEARCH §How To #8 (verbatim ~300-line target):
    - **# RUNBOOK: Production Deploy & Cut-Release** title block.
    - **## Triggers** (4 bullets: first-time bring-up; roll-forward `:v1.0.X`; rollback to previous tag; cut-release develop→main→tag procedure).
    - **## Preconditions** (SSH aliases n8n-ia-vm + vps-ifix-vm; `~/.claude/CLAUDE.md` open for CF + MinIO + OpenRouter; DO Postgres console; Sentry org admin; GH PAT in `~/.git-credentials`; all Phase 02-09 tests green on develop tip — verify `gh run list --limit 5 --workflow build-gateway.yml`).
    - **## Steps — First-Time Bring-Up (~45 min including DNS propagation)** — 7 sub-steps (`### Step 1` … `### Step 7`):
      1. Preflight + edge Traefik route (NO DNS YET) — invoke `scripts/deploy/preflight.sh`, scp `gateway/docker-compose.prod.yml` + populated `.env` to `n8n-ia-vm:/opt/ai-gateway-prod/`, scp `.planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml` to `vps-ifix-vm:/home/pedro/projetos/pedro/infra/traefik-dynamic/`, tail edge Traefik for `router added` line. Pitfall 3 reminder: DNS comes LATER in Step 6.
      2. Postgres bootstrap — `DO_ADMIN_DSN=... scripts/deploy/bootstrap-postgres.sh` creates both `bd_ai_gateway_prod` + `bd_ai_dashboard_prod`. Pitfall 2 reminder: `ai_gateway` schema name stays hardcoded; isolation by DB-name.
      3. First `docker compose up -d` (MIGRATE=true) — `ssh n8n-ia-vm "cd /opt/ai-gateway-prod && docker compose pull && docker compose up -d"`; tail boot log expecting `goose: applied 27 migrations, current version: 27` + `/gateway --self-check` exit 0; internal-Traefik discovery probe `docker service logs traefik-internal_traefik | grep -i ai_gateway`.
      4. Flip MIGRATE off — `sed -i s/^AI_GATEWAY_MIGRATE_ON_BOOT=true/AI_GATEWAY_MIGRATE_ON_BOOT=false/` + `docker compose up -d --force-recreate ifix-ai-gateway`. Pitfall 8 reference.
      5. Dashboard schema bootstrap — `DASHBOARD_DATABASE_URL=... scripts/deploy/migrate-dashboard.sh` (Better Auth CLI one-shot).
      6. DNS flip (FINAL — triggers ACME challenge) — `CF_API_TOKEN=cfut_... scripts/deploy/cf-dns-create.sh`; `dig +short ai-gateway.converse-ai.app @1.1.1.1` → `162.55.92.154`; first `curl -sS -I https://ai-gateway.converse-ai.app/health` triggers TLS-ALPN-01; verify `acme.json` post-issuance via `ssh vps-ifix-vm 'docker exec infra-traefik-1 cat /letsencrypt/acme.json | jq .letsencrypt.Certificates[].domain.main'`. Pitfall 3 + 4 reminders.
      7. Tenant provisioning + per-tenant golden-path — `provision-tenants.sh` + `provision-tenants.sh --mint-keys` against prod DSN (RESEARCH §How To #7 verbatim). 6 destination apps listed for key paste (ConverseAI, Chat Ifix, Telefonia, Cobrancas, Campanhas, voice-api) + 1 admin key for `GATEWAY_ADMIN_KEY` in `.env`.
    - **## Steps — Roll-Forward to new `:v1.0.X`** — sed image tag + `docker compose pull && docker compose up -d`; verify build_version in boot log.
    - **## Steps — Rollback to previous `:v1.0.X-1`** — sed reverse + `docker compose up -d --force-recreate`; INT-06 timed-rollback gate: `time` end-to-end < 5 min; schema-down note (if v1.0.X had a migration, rollback requires `goose down`).
    - **## Steps — Cut-Release Procedure** — RESEARCH §How To #6 verbatim: `git checkout develop && git pull && git checkout main && git pull && git merge --ff-only develop && git tag -a v1.0.0 -m "..." && git push origin main && git push origin v1.0.0 && gh run watch`; Pitfall 5 reminder (tag BEFORE deploy so `BuildVersion=v1.0.0` propagates to Sentry).
    - **## Verification (post-deploy gate)** — bulletted checklist: `/health` JSON, `/v1/health/upstreams` 6 upstreams, `gatewayctl upstreams list`, Sentry releases tab shows `v1.0.0` env `production`, dashboard `/` 200.
    - **## Rollback (escape hatch)** — 3 tiers: (1) DNS panic-revert via `curl -X DELETE` against CF DNS records (TTL 300s); (2) container-only revert via image tag swap; (3) full revert — `/opt/ai-gateway-dev/` on vps-ifix-vm still running per Phase 10 cutover policy (RESEARCH §Discretion Cutover sequence); flip client app `gateway_base_url` env back to `https://gateway-dev.ifixtelecom.com.br`.
    - **## Postmortem stub** — Date / Trigger / Detection / Mitigation / Recovery / Action items (template only).
    Cite Pitfalls inline by ID (Pitfall 1 = network rename, Pitfall 2 = schema name, Pitfall 3 = DNS-before-route, Pitfall 4 = certResolver literal, Pitfall 5 = Sentry release tag, Pitfall 6 = capacity, Pitfall 7 = shared upstream keys, Pitfall 8 = MIGRATE flag).
  </action>
  <verify>
    <automated>test -f gateway/docs/RUNBOOK-DEPLOY.md &amp;&amp; grep -cE "^## (Triggers|Preconditions|Steps|Verification|Rollback)$" gateway/docs/RUNBOOK-DEPLOY.md | awk '$1>=5{exit 0}{exit 1}' &amp;&amp; grep -q 'scripts/deploy/preflight.sh' gateway/docs/RUNBOOK-DEPLOY.md &amp;&amp; grep -q 'scripts/deploy/bootstrap-postgres.sh' gateway/docs/RUNBOOK-DEPLOY.md &amp;&amp; grep -q 'scripts/deploy/migrate-dashboard.sh' gateway/docs/RUNBOOK-DEPLOY.md &amp;&amp; grep -q 'scripts/deploy/cf-dns-create.sh' gateway/docs/RUNBOOK-DEPLOY.md &amp;&amp; grep -q 'ai-gateway-prod.yml' gateway/docs/RUNBOOK-DEPLOY.md &amp;&amp; grep -qE '\bv1\.0\.0\b' gateway/docs/RUNBOOK-DEPLOY.md &amp;&amp; grep -q 'bd_ai_gateway_prod' gateway/docs/RUNBOOK-DEPLOY.md &amp;&amp; awk 'END{exit !(NR>=200)}' gateway/docs/RUNBOOK-DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    File exists; ≥5 standard `##` headers; all 4 deploy scripts referenced by path; route YAML referenced; `v1.0.0` literal present; prod DB name present; ≥200 lines.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remap REQUIREMENTS.md §Traceability per D-16 (PRD-01/02/03/05/06 → Phase 11; PRD-04 split row)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (existing §Traceability table — lines 220-228; identify the exact text of Phase 10 rows)
    - .planning/phases/10-prod-deploy-ai-gateway/10-CONTEXT.md D-16 + D-18
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §File Manifest (REQUIREMENTS.md remap row + ROADMAP Phase 11 placeholder row owned by Plan 10-04)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` §Traceability table. KEEP unchanged: `INT-06 | Phase 10: Production Hardening & GA | Pending` (Phase 10 still owns this), `PRD-07 | Phase 10: Production Hardening & GA | Pending`. CHANGE the Phase 10 name across all in-scope rows from `Phase 10: Production Hardening & GA` to `Phase 10: prod-deploy-ai-gateway` to align with the actual phase slug (cosmetic correctness — D-16 split makes the old label inaccurate). REMAP: PRD-01, PRD-02, PRD-03, PRD-05, PRD-06 → row "Phase 11: prod-hardening | Pending" (per D-16). SPLIT PRD-04 into TWO rows: `PRD-04 (partial) | Phase 10: prod-deploy-ai-gateway | Pending — RUNBOOK-DEPLOY.md only` AND `PRD-04 (full) | Phase 11: prod-hardening | Pending — full incident-response runbook`. Update the Coverage summary block (lines 232-235) if total mapped count changes — recompute totals: 70 v1 reqs total; mapped to phases 70 (still 100%, just redistributed across phases 10 + 11). Add an audit footer comment block under §Traceability: `<!-- 2026-05-26: Phase 10 plan-phase per D-16 split PRD-01/02/03/05/06 from Phase 10 → Phase 11; PRD-04 split into partial (Phase 10 RUNBOOK-DEPLOY.md) + full (Phase 11 incident runbook). -->`
  </action>
  <verify>
    <automated>grep -cE '\| PRD-0[12356] \| Phase 11: prod-hardening' .planning/REQUIREMENTS.md | awk '$1==5{exit 0}{exit 1}' &amp;&amp; grep -E '\| PRD-04 \(partial\) \| Phase 10: prod-deploy-ai-gateway' .planning/REQUIREMENTS.md &amp;&amp; grep -E '\| PRD-04 \(full\) \| Phase 11: prod-hardening' .planning/REQUIREMENTS.md &amp;&amp; grep -E '\| INT-06 \| Phase 10' .planning/REQUIREMENTS.md &amp;&amp; grep -E '\| PRD-07 \| Phase 10' .planning/REQUIREMENTS.md &amp;&amp; grep -q '2026-05-26: Phase 10 plan-phase per D-16' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    Exactly 5 rows map PRD-0{1,2,3,5,6} → Phase 11; PRD-04 split row present in both phases; INT-06 + PRD-07 still on Phase 10; audit footer dated 2026-05-26 cites D-16.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add Phase 11 placeholder to ROADMAP.md + update Phase 10 goal + plan count</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (existing Phase 10 placeholder lines 16-26 + Phase 06.9 entry as the latest "completed" shape analog at lines 29-49)
    - .planning/phases/10-prod-deploy-ai-gateway/10-CONTEXT.md (Phase 10 goal narrative — extract one-paragraph summary from §Phase Boundary)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Summary (use the first paragraph as the canonical Phase 10 goal text)
  </read_first>
  <action>
    **Precheck (idempotency gate — run FIRST):**

    ```bash
    GOAL_PLANNED=$(grep -c '^**Goal:**\s*\[To be planned\]' .planning/ROADMAP.md || true)
    PHASE10_HAS_GOAL=$(awk '/^### Phase 10: prod-deploy-ai-gateway/,/^---|^### Phase /' .planning/ROADMAP.md | grep -c '^\*\*Goal:\*\* First production deploy' || true)
    PHASE11_PRESENT=$(grep -c '^### Phase 11: prod-hardening' .planning/ROADMAP.md || true)
    ```

    **If `PHASE10_HAS_GOAL >= 1` AND `PHASE11_PRESENT >= 1`** (current repo state at revision time — Phase 10 goal already populated and Phase 11 placeholder block already inserted at the canonical anchor): Task 3 is a **no-op for the populate + append**. Proceed directly to the verify step (the grep gate still asserts both anchors and MUST still pass). Do NOT re-populate, do NOT re-insert, do NOT duplicate the Phase 11 block. Log a single line: "Task 3: ROADMAP.md already at target state — skipping populate + append; verify gate is the authoritative check."

    **Otherwise** (one or both anchors missing — e.g. fresh tree or partial prior edit): apply the original populate + append below.

    Edit `.planning/ROADMAP.md`. (1) Populate Phase 10 placeholder (replace `**Goal:** [To be planned]`): set Goal to "First production deploy of the ifix-ai-gateway (gateway + dashboard) — operator-managed `docker compose` stack at /opt/ai-gateway-prod/ on n8n-ia-vm (VM 101), public hostnames ai-gateway.converse-ai.app + ai-dashboard.converse-ai.app served via edge Traefik on vps-ifix-vm, new Postgres prod databases bd_ai_gateway_prod + bd_ai_dashboard_prod, new Sentry project ifix-ai-gateway-prod, develop→main fast-forward, cut release v1.0.0, per-tenant golden-path smoke for the 6 client apps, cascade-close Phase 02/03/04/05 live-UAT deferrals." Set `**Requirements:**` to `INT-06, PRD-04 (partial), PRD-07` (matches REQUIREMENTS.md remap from Task 2). Set `**Plans:**` to `6 plans` and replace the placeholder list with 6 rows: `[ ] 10-01-PLAN.md — Wave 0 reconciliation + compose/env scaffolds + capacity gate`, `[ ] 10-02-PLAN.md — Postgres prod databases + dashboard better-auth migrations`, `[ ] 10-03-PLAN.md — Edge Traefik file-provider entry + Cloudflare DNS records`, `[ ] 10-04-PLAN.md — RUNBOOK-DEPLOY + REQUIREMENTS remap + ROADMAP Phase 11 placeholder`, `[ ] 10-05-PLAN.md — develop→main promotion + v1.0.0 tag + GHA build verify`, `[ ] 10-06-PLAN.md — HUMAN-UAT (deploy + 8 smoke scenarios + 4 cascade-close commits)`. (2) Append AFTER the Phase 10 block (insert immediately before the existing `---` separator that precedes Phase 06.9 inserted-section block) a new Phase 11 section: `### Phase 11: prod-hardening` with `**Goal:** TBD — production-hardening follow-on to Phase 10 covering load test (PRD-01), chaos tests (PRD-02/03), full incident runbook (PRD-04 full), LGPD legal sign-off coordination (PRD-05), dashboard SSO hardening (PRD-06).`, `**Requirements:** PRD-01, PRD-02, PRD-03, PRD-04 (full), PRD-05, PRD-06`, `**Depends on:** Phase 10`, `**Plans:** 0 plans` + `Plans: - [ ] TBD (run /gsd:plan-phase 11 after Phase 10 closes)`.
  </action>
  <verify>
    <automated>grep -q '^### Phase 11: prod-hardening' .planning/ROADMAP.md &amp;&amp; grep -q 'PRD-01, PRD-02, PRD-03, PRD-04 (full), PRD-05, PRD-06' .planning/ROADMAP.md &amp;&amp; grep -q 'Depends on:.*Phase 10' .planning/ROADMAP.md &amp;&amp; grep -q '10-01-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -q '10-06-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -q 'INT-06, PRD-04 (partial), PRD-07' .planning/ROADMAP.md &amp;&amp; ! grep -q 'Goal:.*\[To be planned\]' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    Phase 10 placeholder is no longer `[To be planned]`; new Phase 11 section present with the 6 deferred PRD IDs; both Phase 10 plan list bookends (10-01-PLAN.md + 10-06-PLAN.md) present; goal/requirements/plans-list consistent across REQUIREMENTS.md and ROADMAP.md.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo writers → planning docs | All edits to REQUIREMENTS.md + ROADMAP.md are within the same repo; no cross-system trust. |
| repo writers → runbook consumers | RUNBOOK-DEPLOY.md becomes the SOURCE OF TRUTH for the operator deploy procedure; incorrect step ordering = incident. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | RUNBOOK Step ordering bug (e.g. DNS before route) leading to broken cert issuance | mitigate | Task 1 mandates explicit Pitfall 3 citation inline; verify step asserts the Steps 1→6 ordering by line-number progression. |
| T-10-04-02 | Repudiation | REQUIREMENTS.md remap audit-trail loss | mitigate | Task 2 includes an explicit dated `<!-- 2026-05-26: ... per D-16 -->` footer; git blame preserves authorship. |
| T-10-04-03 | Information disclosure | RUNBOOK accidentally including secret values in code-fence examples | mitigate | All code fences use `<PLACEHOLDER>` / variable form (e.g. `DO_ADMIN_DSN=...`); verify step does NOT need to grep for secrets because Task 1 explicitly forbids real values. |
| T-10-04-04 | Tampering | ROADMAP and REQUIREMENTS drift (one says Phase 11, the other still Phase 10) | mitigate | Both tasks share the same 5 PRD-IDs constant set; verify steps grep the symmetric anchors in both files. |
| T-10-04-SC | Tampering | Supply-chain — npm/pip/cargo installs | accept | Documentation-only edits; zero package operations. |
</threat_model>

<verification>
End of Plan 10-04 gate:
- [ ] RUNBOOK-DEPLOY.md exists with ≥5 standard headers + ≥200 lines + all 4 deploy script references
- [ ] REQUIREMENTS.md §Traceability: PRD-01/02/03/05/06 on Phase 11; PRD-04 split row; INT-06 + PRD-07 on Phase 10
- [ ] ROADMAP.md: Phase 10 goal populated + 6 plans listed + new Phase 11 placeholder block
- [ ] No `[To be planned]` literal remaining for Phase 10
</verification>

<success_criteria>
3/3 tasks GREEN; PRD-04 (partial) satisfied; D-16 fully reflected in REQUIREMENTS.md + ROADMAP.md; downstream `/gsd:verify-work` will not flag PRD-01/02/03/05/06 as uncovered against Phase 10 because they have been remapped to Phase 11.
</success_criteria>

<output>
Create `.planning/phases/10-prod-deploy-ai-gateway/10-04-SUMMARY.md` when done.
</output>
