---
phase: 10-prod-deploy-ai-gateway
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - scripts/deploy/bootstrap-postgres.sh
  - scripts/deploy/migrate-dashboard.sh
autonomous: true
requirements: []          # data-layer bootstrap; the requirement IDs (INT-06, PRD-04, PRD-07) are covered by 10-04/10-06
user_setup:
  - service: "DigitalOcean Managed Postgres"
    why: "Phase 10 requires two NEW databases on the existing instance (bd_ai_gateway_prod, bd_ai_dashboard_prod) — see RESEARCH Pitfall 2 (schema rename out of scope; new database is the implementable form of D-05)"
    env_vars: []
    dashboard_config:
      - task: "Verify Trusted Sources includes 162.55.92.154 (already does per CLAUDE.md Hetzner topology)"
        location: "DO Managed Postgres → Settings → Trusted Sources"
      - task: "Operator runs `bootstrap-postgres.sh` (Plan 10-06 HUMAN-UAT step) with `DO_ADMIN_DSN` env var pointing at the doadmin user — script issues `CREATE DATABASE bd_ai_gateway_prod` + `CREATE DATABASE bd_ai_dashboard_prod` idempotently"
        location: "ops-claude shell with DO_ADMIN_DSN env var sourced from password manager (NOT committed)"
tags: [phase-10, prod-deploy, postgres, migrations, dashboard, better-auth]
must_haves:
  truths:
    - "Operator can run a single idempotent script to create both prod Postgres databases without manual SQL"
    - "Operator can run the better-auth dashboard schema migration as a single docker one-shot command"
    - "The bootstrap script refuses to drop / overwrite existing data (idempotent CREATE DATABASE IF NOT EXISTS-style guard)"
  artifacts:
    - path: "scripts/deploy/bootstrap-postgres.sh"
      provides: "Idempotent CREATE DATABASE wrapper for bd_ai_gateway_prod + bd_ai_dashboard_prod"
      exports: ["main()", "ensure_database()"]
    - path: "scripts/deploy/migrate-dashboard.sh"
      provides: "Docker-one-shot wrapper running `npx @better-auth/cli migrate` against bd_ai_dashboard_prod"
      min_lines: 25
  key_links:
    - from: "scripts/deploy/bootstrap-postgres.sh"
      to: "DO managed Postgres (db-grupoifix-do-user-7520351-0.j.db.ondigitalocean.com:25060)"
      via: "DO_ADMIN_DSN env var (operator-supplied at runtime)"
      pattern: "DO_ADMIN_DSN"
    - from: "scripts/deploy/migrate-dashboard.sh"
      to: "bd_ai_dashboard_prod database"
      via: "DASHBOARD_DATABASE_URL env from /opt/ai-gateway-prod/.env"
      pattern: "DASHBOARD_DATABASE_URL"
---

<objective>
Ship two operator-runnable scripts that bootstrap the prod data layer: (a) idempotent CREATE DATABASE for `bd_ai_gateway_prod` + `bd_ai_dashboard_prod` (the implementable form of D-05/D-06 per RESEARCH Pitfall 2 — schema name `ai_gateway` is hardcoded in 27 migrations + `pool.go:35` so a new schema is out of scope; isolation is achieved by a new database with the SAME schema name); (b) one-shot `npx @better-auth/cli migrate` runner that creates the `user`/`session`/`account`/`verification` tables inside `bd_ai_dashboard_prod`.

Purpose: Plan 10-06 HUMAN-UAT Step 2 (Postgres bootstrap) + Step 5 (dashboard schema bootstrap) need these scripts to exist and be idempotent so the operator does not handwrite SQL or remember Better-Auth CLI invocation syntax mid-UAT. Gateway migrations themselves (0001..0027) are handled by `AI_GATEWAY_MIGRATE_ON_BOOT=true` at first `docker compose up -d` — no script needed for that path (already wired in `gateway/internal/db/migrate.go`).

Output: `scripts/deploy/bootstrap-postgres.sh` + `scripts/deploy/migrate-dashboard.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-prod-deploy-ai-gateway/10-CONTEXT.md
@.planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md
@.planning/phases/10-prod-deploy-ai-gateway/10-PATTERNS.md

# Analogs
@scripts/integration-smoke/provision-tenants.sh
@gateway/internal/db/migrate.go
@gateway/internal/db/pool.go

<interfaces>
<!-- Hardcoded schema name + migration count anchors. -->
From gateway/internal/db/pool.go:35:
- Every connection runs `SET search_path = ai_gateway, public` — both new databases need their `doadmin` user to be the schema owner (default in DO managed Postgres).

From gateway/db/migrations/:
- 27 .sql files numbered 0001..0027 (last = `0027_openrouter_target_deepseek_v4_flash.sql`). On first deploy, goose runs ALL 27 in order. Expected boot log: `goose: applied 27 migrations, current version: 27`.

From RESEARCH §How To #3 + Pitfall 2:
- DO NOT attempt `CREATE SCHEMA ai_gateway_prod` — schema name is hardcoded.
- DO `CREATE DATABASE bd_ai_gateway_prod` + `CREATE DATABASE bd_ai_dashboard_prod` (psql against postgres admin DB).
- DSN format: `postgres://doadmin:PASS@db-grupoifix-do-user-7520351-0.j.db.ondigitalocean.com:25060/<DB>?sslmode=require`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author scripts/deploy/bootstrap-postgres.sh (idempotent CREATE DATABASE wrapper)</name>
  <files>scripts/deploy/bootstrap-postgres.sh</files>
  <read_first>
    - scripts/integration-smoke/provision-tenants.sh (existing bash idiom + idempotency pattern — "tenant create returns 'already exists' is OK")
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §How To #3 (Postgres prod database + migrations — exact CREATE DATABASE steps)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Pitfall 2 (schema-name confusion — implementable form is NEW DB)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Assumption A5 (DO admin doadmin has CREATE DATABASE rights)
  </read_first>
  <action>
    Create `scripts/deploy/bootstrap-postgres.sh`. Shebang `#!/usr/bin/env bash` + `set -euo pipefail`. Required env vars (validated upfront, fail-fast with helpful message): `DO_ADMIN_DSN` (form `postgres://doadmin:PASS@db-grupoifix-do-user-7520351-0.j.db.ondigitalocean.com:25060/defaultdb?sslmode=require` — operator supplies at runtime; NEVER committed). Constants: `GATEWAY_DB=bd_ai_gateway_prod`, `DASHBOARD_DB=bd_ai_dashboard_prod`. Implement function `ensure_database(name)` that runs `psql "$DO_ADMIN_DSN" -tAc "SELECT 1 FROM pg_database WHERE datname='${name}'"`; if output empty, run `psql "$DO_ADMIN_DSN" -c "CREATE DATABASE \"${name}\""` and log success; if non-empty, log "already exists — skipping" (Pitfall 2 implementable form of D-05/D-06). Then call `ensure_database "$GATEWAY_DB"` + `ensure_database "$DASHBOARD_DB"`. Post-creation sanity probes: for each new DB, run `psql "${DO_ADMIN_DSN/defaultdb/$name}?sslmode=require" -c "SELECT current_database()"` and assert it prints the DB name. Final summary block prints the two DSNs the operator must paste into `/opt/ai-gateway-prod/.env`: `AI_GATEWAY_PG_DSN=postgres://doadmin:PASS@db-grupoifix-do-user-7520351-0.j.db.ondigitalocean.com:25060/bd_ai_gateway_prod?sslmode=require` + `DASHBOARD_DATABASE_URL=postgres://doadmin:PASS@db-grupoifix-do-user-7520351-0.j.db.ondigitalocean.com:25060/bd_ai_dashboard_prod?sslmode=require&options=-c%20search_path%3Ddashboard_auth` (with `PASS` placeholder — script does NOT echo the actual password; that lives in the operator's DSN). NO destructive operations (no DROP, no TRUNCATE). Script returns exit 0 only after both DBs exist and both sanity probes succeed.
  </action>
  <verify>
    <automated>test -x scripts/deploy/bootstrap-postgres.sh &amp;&amp; bash -n scripts/deploy/bootstrap-postgres.sh &amp;&amp; grep -q 'set -euo pipefail' scripts/deploy/bootstrap-postgres.sh &amp;&amp; grep -q 'bd_ai_gateway_prod' scripts/deploy/bootstrap-postgres.sh &amp;&amp; grep -q 'bd_ai_dashboard_prod' scripts/deploy/bootstrap-postgres.sh &amp;&amp; grep -q 'DO_ADMIN_DSN' scripts/deploy/bootstrap-postgres.sh &amp;&amp; ! grep -qE '\bDROP\b|\bTRUNCATE\b' scripts/deploy/bootstrap-postgres.sh &amp;&amp; grep -qE "SELECT 1 FROM pg_database WHERE datname" scripts/deploy/bootstrap-postgres.sh</automated>
  </verify>
  <acceptance_criteria>
    Script is executable, passes `bash -n`, asserts both DB names, requires `DO_ADMIN_DSN`, contains no destructive SQL, uses idempotent `SELECT 1 FROM pg_database` probe before CREATE DATABASE.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author scripts/deploy/migrate-dashboard.sh (one-shot better-auth migrate inside dashboard image)</name>
  <files>scripts/deploy/migrate-dashboard.sh</files>
  <read_first>
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §How To #3 step 7 (`docker run --rm -e DASHBOARD_DATABASE_URL=… ghcr.io/ifixtelecom/ifix-ai-dashboard:v1.0.0 npx @better-auth/cli migrate`)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Pitfall (none specific — Assumption A3 noted as needing operator confirm during HUMAN-UAT first signup)
    - dashboard/.env.example (if present — to learn the env var contract used by Better Auth CLI)
    - scripts/deploy/bootstrap-postgres.sh (from Task 1 — for env style consistency)
  </read_first>
  <action>
    Create `scripts/deploy/migrate-dashboard.sh`. Shebang `#!/usr/bin/env bash` + `set -euo pipefail`. Required env vars: `DASHBOARD_DATABASE_URL` (the prod DSN pointing at `bd_ai_dashboard_prod`; can be sourced from `/opt/ai-gateway-prod/.env` via `set -a; source /opt/ai-gateway-prod/.env; set +a` documented in header comment); optional `DASHBOARD_IMAGE_TAG` (default `v1.0.0` per D-13). Body invokes: `ssh n8n-ia-vm "docker run --rm --network intra -e DASHBOARD_DATABASE_URL='${DASHBOARD_DATABASE_URL}' ghcr.io/ifixtelecom/ifix-ai-dashboard:${DASHBOARD_IMAGE_TAG} npx @better-auth/cli migrate --yes"` — note network `intra` so the container can reach the DO Postgres egress via the same NAT path as the prod gateway. Capture exit code; on non-zero, echo "Better Auth migration FAILED — check DASHBOARD_DATABASE_URL + image tag + DO Trusted Sources for 162.55.92.154" + exit 1. Post-migrate sanity probe: `ssh n8n-ia-vm "docker run --rm --network intra -e DASHBOARD_DATABASE_URL='${DASHBOARD_DATABASE_URL}' postgres:16-alpine psql \"\$DASHBOARD_DATABASE_URL\" -tAc \"SELECT count(*) FROM information_schema.tables WHERE table_schema='dashboard_auth' AND table_name IN ('user','session','account','verification')\""` — assert it prints `4`. Header WARNING: script MUST run AFTER `bootstrap-postgres.sh` has created `bd_ai_dashboard_prod`. NO destructive operations.
  </action>
  <verify>
    <automated>test -x scripts/deploy/migrate-dashboard.sh &amp;&amp; bash -n scripts/deploy/migrate-dashboard.sh &amp;&amp; grep -q 'set -euo pipefail' scripts/deploy/migrate-dashboard.sh &amp;&amp; grep -q 'DASHBOARD_DATABASE_URL' scripts/deploy/migrate-dashboard.sh &amp;&amp; grep -q 'better-auth/cli migrate' scripts/deploy/migrate-dashboard.sh &amp;&amp; grep -q 'dashboard_auth' scripts/deploy/migrate-dashboard.sh &amp;&amp; ! grep -qE '\bDROP\b|\bTRUNCATE\b' scripts/deploy/migrate-dashboard.sh</automated>
  </verify>
  <acceptance_criteria>
    Script is executable, passes `bash -n`, requires `DASHBOARD_DATABASE_URL`, invokes `@better-auth/cli migrate`, asserts the 4 expected dashboard_auth tables post-run, contains no destructive SQL.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → DO Postgres admin | DO_ADMIN_DSN crosses untrusted local terminal history; operator MUST NOT paste it into log files. |
| docker run --rm → DO Postgres | DASHBOARD_DATABASE_URL passed via `-e` exposes the password in `docker inspect` for the ephemeral container lifetime. Acceptable because the container is `--rm` and exits within seconds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Information disclosure | DO_ADMIN_DSN leaked via shell history / process listing | mitigate | Header comment instructs operator to `unset HISTFILE` or use `read -s` prompt; script never echoes the DSN, only DB name + sanity probe results. |
| T-10-02-02 | Tampering | Accidental DROP / TRUNCATE if script template copy-paste error | mitigate | Verify step explicitly greps for `DROP|TRUNCATE` and fails. Idempotent path uses SELECT-then-CREATE only. |
| T-10-02-03 | Information disclosure | Schema collision dev↔prod (catastrophic table overwrites) | mitigate | RESEARCH Pitfall 2 — Plan uses separate DATABASE not separate schema. `psql` connects via DSN whose database name differs; `pg_database` row check prevents creating prod inside dev DB. |
| T-10-02-04 | Denial of service | DO managed Postgres rejects new connections from VM 101 if Trusted Sources lapses | accept | RESEARCH §How To #3 confirms 162.55.92.154 already whitelisted; Plan 10-06 Gate B re-verifies egress IP. |
| T-10-02-05 | Tampering | Better Auth CLI version drift creating different table shapes between operator runs | mitigate | Script pins `DASHBOARD_IMAGE_TAG=v1.0.0` (D-13 immutable tag); same image always runs same `@better-auth/cli` version baked at build time. |
| T-10-02-SC | Tampering | Supply-chain — npm/pip/cargo installs | accept | No new packages introduced; `@better-auth/cli` is already in `dashboard/package.json` from Phase 07. |
</threat_model>

<verification>
End of Wave 1 (Plan 10-02) gate:
- [ ] Both scripts pass `bash -n`
- [ ] Both scripts are executable + present in `scripts/deploy/`
- [ ] No destructive SQL in either script
- [ ] Bootstrap script's idempotency path verified by grep on `SELECT 1 FROM pg_database`

The actual SSH execution of these scripts against the live DO Postgres + n8n-ia-vm happens during Plan 10-06 HUMAN-UAT Step 2 + Step 5 (operator-driven, autonomous executor lacks the DO_ADMIN_DSN).
</verification>

<success_criteria>
2/2 tasks GREEN; Plan 10-06 HUMAN-UAT operator can invoke `DO_ADMIN_DSN=... scripts/deploy/bootstrap-postgres.sh` and `DASHBOARD_DATABASE_URL=... scripts/deploy/migrate-dashboard.sh` without modifying either script.
</success_criteria>

<output>
Create `.planning/phases/10-prod-deploy-ai-gateway/10-02-SUMMARY.md` when done.
</output>
