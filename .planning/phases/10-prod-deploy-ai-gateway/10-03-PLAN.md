---
phase: 10-prod-deploy-ai-gateway
plan: 03
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - .planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml
  - scripts/deploy/cf-dns-create.sh
autonomous: true
requirements: [PRD-07]    # DNS + TLS reachable
user_setup:
  - service: "Cloudflare DNS"
    why: "Phase 10 needs 2 new A records on zone converse-ai.app pointing at 162.55.92.154 (proxied=OFF for LE TLS-ALPN-01)"
    env_vars:
      - name: CF_API_TOKEN
        source: "~/.claude/CLAUDE.md (Cloudflare DNS API Token block, zone IDs included)"
    dashboard_config:
      - task: "After Plan 10-06 operator runs cf-dns-create.sh, verify the two records show proxied=OFF + TTL 300 in CF dashboard"
        location: "Cloudflare → converse-ai.app zone → DNS"
tags: [phase-10, prod-deploy, dns, tls, traefik, file-provider, cloudflare]
must_haves:
  truths:
    - "Edge Traefik on vps-ifix-vm picks up 2 new routers within 1s of file-rsync (hot-reload)"
    - "After DNS flip + ACME challenge, both hostnames serve a valid LE certificate (not the Traefik default)"
    - "Operator can run a single curl-based script to create both CF DNS records idempotently"
  artifacts:
    - path: ".planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml"
      provides: "Edge Traefik file-provider entry routing ai-gateway.converse-ai.app + ai-dashboard.converse-ai.app to http://10.10.10.20:80 with certResolver letsencrypt + passHostHeader=true"
      contains: "Host(`ai-gateway.converse-ai.app`)"
    - path: "scripts/deploy/cf-dns-create.sh"
      provides: "Idempotent CF DNS POST wrapper for both prod hostnames"
      exports: ["main()", "ensure_record()"]
  key_links:
    - from: ".planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml"
      to: "vps-ifix-vm:/home/pedro/projetos/pedro/infra/traefik-dynamic/ai-gateway-prod.yml (operator-rsync'd during HUMAN-UAT Step 1)"
      via: "scp"
      pattern: "rsync|scp"
    - from: "scripts/deploy/cf-dns-create.sh"
      to: "Cloudflare zone 0e779b74b86957bdb628d646dbf33978 (converse-ai.app)"
      via: "POST /zones/{zone_id}/dns_records"
      pattern: "0e779b74b86957bdb628d646dbf33978"
---

<objective>
Author the two artifacts that make the prod hostnames publicly reachable: (a) `.planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml` — the edge Traefik file-provider entry that the operator rsyncs to `vps-ifix-vm:/home/pedro/projetos/pedro/infra/traefik-dynamic/` (RESEARCH §How To #2 / Pitfall 4); (b) `scripts/deploy/cf-dns-create.sh` — the idempotent Cloudflare DNS POST wrapper using the token from `~/.claude/CLAUDE.md` and zone id `0e779b74b86957bdb628d646dbf33978`.

Purpose: Plan 10-06 HUMAN-UAT Step 1 (edge route, NO DNS yet) + Step 6 (DNS flip, FINAL — triggers ACME) need both artifacts. Order matters per RESEARCH Pitfall 3: route file lands FIRST (edge logs `no certificate for X`), DNS records POST SECOND (first :443 request triggers TLS-ALPN-01 challenge → cert cached in `acme.json`). This plan parallels 10-02 (no file overlap; 10-02 touches `scripts/deploy/bootstrap-postgres.sh` + `migrate-dashboard.sh`; this plan touches a different YAML artifact + a different deploy script).

Output: `.planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml` + `scripts/deploy/cf-dns-create.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-prod-deploy-ai-gateway/10-CONTEXT.md
@.planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md
@.planning/phases/10-prod-deploy-ai-gateway/10-PATTERNS.md

# Local analog
@/home/pedro/projetos/pedro/infra/traefik-dynamic/voice-api.yml

<interfaces>
<!-- Edge Traefik static config anchor -->
From /home/pedro/projetos/pedro/infra/docker-compose.yml (vps-ifix-vm edge Traefik):
- certResolver literal in static config = `letsencrypt` (NOT `letsencryptresolver`) — Pitfall 4 source
- entryPoints = `web` (:80, http) + `websecure` (:443, https)
- providers.file.directory = `/etc/traefik/dynamic/` mounted from host `/home/pedro/projetos/pedro/infra/traefik-dynamic/`

From RESEARCH §How To #2:
- Mirror file shape: vps-ifix-vm:/home/pedro/projetos/pedro/infra/traefik-dynamic/n8n-ia.yml routes 4 hostnames to http://10.10.10.20:80 with passHostHeader=true
- Hot-reload watch picks up new file in ~1s; no Traefik restart needed

From ~/.claude/CLAUDE.md (Cloudflare API Token block):
- Token literal: `<REDACTED-CF-TOKEN>`
- Zone `converse-ai.app` → id `0e779b74b86957bdb628d646dbf33978`
- Permissions: Zone:Read + DNS:Edit on the 3 zones (converse-ai.app + converseai.app.br + ifixtelecom.com.br)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the edge Traefik file-provider route YAML</name>
  <files>.planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml</files>
  <read_first>
    - /home/pedro/projetos/pedro/infra/traefik-dynamic/voice-api.yml (local analog — 2-router skeleton + certResolver: letsencrypt literal)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §How To #2 (full edge file-provider draft, lines 935-956)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Pitfall 3 (DNS flip before route — order matters)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Pitfall 4 (certResolver literal — letsencrypt NOT letsencryptresolver)
    - .planning/phases/10-prod-deploy-ai-gateway/10-PATTERNS.md §Pattern 3
  </read_first>
  <action>
    Create the directory `.planning/phases/10-prod-deploy-ai-gateway/artifacts/` (if not present) and write `ai-gateway-prod.yml`. Mirror the RESEARCH §How To #2 draft verbatim. Required content: top-level `http:` block with `routers:` and `services:` sub-blocks. Two routers: `ai-gateway-prod` (rule `Host(\`ai-gateway.converse-ai.app\`)`, service `n8n-ia-prod-internal`, entryPoints `[websecure]`, tls.certResolver `letsencrypt`); `ai-dashboard-prod` (symmetric, rule `Host(\`ai-dashboard.converse-ai.app\`)`). Single service `n8n-ia-prod-internal` with `loadBalancer.servers[0].url: "http://10.10.10.20:80"` + `passHostHeader: true`. File MUST open with a header comment block that cites: (a) mirror pattern `n8n-ia.yml` on vps-ifix-vm; (b) certResolver literal `letsencrypt` (not `letsencryptresolver`) — Pitfall 4; (c) ACME challenge = TLS-ALPN-01 (`tlschallenge=true` in edge static config) NOT DNS-01 — first :443 request triggers cert issuance; (d) Pitfall 3 — operator MUST rsync this file to vps-ifix-vm BEFORE flipping DNS (route exists, cert not yet issued, edge logs "no certificate for X" cleanly); (e) `passHostHeader: true` reason — Better Auth cookie scope + audit log Host context; (f) the rsync target path: `vps-ifix-vm:/home/pedro/projetos/pedro/infra/traefik-dynamic/ai-gateway-prod.yml`. The file lives under `.planning/phases/10-prod-deploy-ai-gateway/artifacts/` in this repo because the rsync target is on a different host (vps-ifix-vm) that is NOT the git tree — this artifacts directory is the canonical source-of-truth that Plan 10-06 HUMAN-UAT pushes from.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml &amp;&amp; python3 -c "import yaml,sys; d=yaml.safe_load(open('.planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml')); routers=d['http']['routers']; assert set(routers.keys())=={'ai-gateway-prod','ai-dashboard-prod'}; assert all(r['tls']['certResolver']=='letsencrypt' for r in routers.values()); assert all(r['entryPoints']==['websecure'] for r in routers.values()); svc=d['http']['services']['n8n-ia-prod-internal']; assert svc['loadBalancer']['servers'][0]['url']=='http://10.10.10.20:80'; assert svc['loadBalancer']['passHostHeader'] is True; print('OK')" &amp;&amp; ! grep -q 'letsencryptresolver' .planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml</automated>
  </verify>
  <acceptance_criteria>
    YAML parses; 2 routers + 1 service with the exact contract above; `letsencryptresolver` does NOT appear anywhere (Pitfall 4 sentinel). Header comment cites Pitfalls 3 + 4 + the rsync target path.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author scripts/deploy/cf-dns-create.sh (idempotent CF DNS POST wrapper)</name>
  <files>scripts/deploy/cf-dns-create.sh</files>
  <read_first>
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §How To #2 / Pattern 3 (Cloudflare DNS A record creation — lines 410-438)
    - .planning/phases/10-prod-deploy-ai-gateway/10-PATTERNS.md §Pattern 3 (Cloudflare DNS A record creation — idempotent POST)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Anti-Patterns (DO NOT set proxied=true)
    - scripts/deploy/bootstrap-postgres.sh (Plan 10-02 — bash style consistency)
  </read_first>
  <action>
    Create `scripts/deploy/cf-dns-create.sh`. Shebang `#!/usr/bin/env bash` + `set -euo pipefail`. Required env var: `CF_API_TOKEN` (sourced by operator from `~/.claude/CLAUDE.md` Cloudflare block; script fails fast if unset with "export CF_API_TOKEN=cfut_... — see ~/.claude/CLAUDE.md"). Constants: `ZONE_ID=0e779b74b86957bdb628d646dbf33978` (converse-ai.app), `ORIGIN_IP=162.55.92.154`, `HOSTS=(ai-gateway ai-dashboard)`. Implement function `ensure_record(name)`: first GETs `https://api.cloudflare.com/client/v4/zones/${ZONE_ID}/dns_records?type=A&name=${name}.converse-ai.app` to check existence; if `result|length` == 0, POST with body `{"type":"A","name":"${name}.converse-ai.app","content":"${ORIGIN_IP}","proxied":false,"ttl":300,"comment":"Phase 10 prod ifix-ai-gateway (created $(date -Iseconds))"}` per RESEARCH §How To #2 Pattern 3; if 1+ result already exists, log "DNS record ${name}.converse-ai.app already present — skipping" (idempotent). MUST assert `proxied=false` (RESEARCH §Anti-Patterns — TLS-ALPN-01 requires direct origin reachability). After both records exist, run `dig +short ai-gateway.converse-ai.app @1.1.1.1` + `dig +short ai-dashboard.converse-ai.app @1.1.1.1` and assert each returns `162.55.92.154` (allow up to 30s for CF propagation; loop with 5s sleep, max 6 iterations). Final summary block prints: "DNS records live. Next: edge Traefik route file MUST already be deployed (Plan 10-06 Step 1) BEFORE first https probe — first request triggers ACME TLS-ALPN-01 challenge." Header WARNING cites Pitfall 3 (route-before-DNS order) and CF token rotation pointer to https://dash.cloudflare.com/profile/api-tokens.
  </action>
  <verify>
    <automated>test -x scripts/deploy/cf-dns-create.sh &amp;&amp; bash -n scripts/deploy/cf-dns-create.sh &amp;&amp; grep -q 'set -euo pipefail' scripts/deploy/cf-dns-create.sh &amp;&amp; grep -q '0e779b74b86957bdb628d646dbf33978' scripts/deploy/cf-dns-create.sh &amp;&amp; grep -q '162.55.92.154' scripts/deploy/cf-dns-create.sh &amp;&amp; grep -q '"proxied":false' scripts/deploy/cf-dns-create.sh &amp;&amp; grep -qE 'ai-gateway.*ai-dashboard|ai-dashboard.*ai-gateway|HOSTS=\(ai-gateway ai-dashboard\)' scripts/deploy/cf-dns-create.sh &amp;&amp; grep -q 'CF_API_TOKEN' scripts/deploy/cf-dns-create.sh &amp;&amp; ! grep -qE '\bDELETE\b\s+http|curl.*-X DELETE' scripts/deploy/cf-dns-create.sh</automated>
  </verify>
  <acceptance_criteria>
    Script is executable, passes `bash -n`, contains zone id + origin IP + both hostnames + `proxied=false` literal, requires `CF_API_TOKEN`, contains no DELETE operations (creation-only — destructive ops live in the RUNBOOK rollback section). Idempotent path is grep-visible.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → Cloudflare API | CF_API_TOKEN exposed in process environment; idempotent script minimizes blast radius (creates only, never deletes). |
| operator workstation → vps-ifix-vm | The route YAML is rsync'd over SSH (key-based auth, no token in transit). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | YAML parse failure on `ai-gateway-prod.yml` causing Traefik hot-reload silently to skip the file | mitigate | Task 1 verify step parses YAML + asserts router/service shape; RUNBOOK-DEPLOY.md Step 1 has the operator-side `python3 -c "import yaml; yaml.safe_load(...)"` validation immediately before scp (RESEARCH §How To #2 mitigation for T-05-03-04). |
| T-10-03-02 | Information disclosure | CF_API_TOKEN logged to process listing or shell history | mitigate | Script header WARNING; never echoes the token; uses curl `-H` (visible in argv only briefly). Operator follows rotation pointer if leak suspected. |
| T-10-03-03 | Spoofing | Operator misreading zone id / accidentally creating records in wrong CF zone | mitigate | Zone id is HARDCODED in script (not env-var); verify step greps for the literal value. |
| T-10-03-04 | Denial of service | `proxied=true` set accidentally breaks ACME TLS-ALPN-01 first-issue | mitigate | Verify step greps for `"proxied":false` literal. RESEARCH §Anti-Patterns documents the failure mode. |
| T-10-03-05 | Tampering | Stale cert / cert not issued before DNS flip → browser-reject UX | mitigate | Pitfall 3 ordering encoded in script footer message + RUNBOOK-DEPLOY.md Step 1A vs Step 6 explicit gate; route YAML rsync MUST happen first. |
| T-10-03-06 | Information disclosure | CF token scope creep — single token covers 3 zones | accept | Operator-managed; rotation cadence per `~/.claude/CLAUDE.md` security note. |
| T-10-03-SC | Tampering | Supply-chain — npm/pip/cargo installs | accept | No new packages; pure curl + jq + dig + python yaml (all already on ops-claude). |
</threat_model>

<verification>
End of Plan 10-03 gate:
- [ ] `ai-gateway-prod.yml` parses as valid YAML with the exact router + service shape
- [ ] `cf-dns-create.sh` is executable + passes `bash -n` + greps assert zone id + origin IP + `proxied=false`
- [ ] Neither artifact references `letsencryptresolver` (Pitfall 4 sentinel)

The actual rsync of the YAML to vps-ifix-vm + the live curl POST to Cloudflare happen during Plan 10-06 HUMAN-UAT Step 1 + Step 6.
</verification>

<success_criteria>
2/2 tasks GREEN; Plan 10-06 operator can `scp .planning/phases/10-prod-deploy-ai-gateway/artifacts/ai-gateway-prod.yml vps-ifix-vm:/home/pedro/projetos/pedro/infra/traefik-dynamic/` and `CF_API_TOKEN=cfut_... scripts/deploy/cf-dns-create.sh` without modifying either artifact.
</success_criteria>

<output>
Create `.planning/phases/10-prod-deploy-ai-gateway/10-03-SUMMARY.md` when done.
</output>
