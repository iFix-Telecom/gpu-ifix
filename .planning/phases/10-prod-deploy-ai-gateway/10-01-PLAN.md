---
phase: 10-prod-deploy-ai-gateway
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - gateway/docker-compose.prod.yml
  - gateway/.env.prod.example
  - scripts/deploy/preflight.sh
  - .planning/phases/10-prod-deploy-ai-gateway/10-01-CAPACITY-OBSERVED.md
autonomous: true
requirements: []          # Wave 0 scaffolding — no requirement IDs satisfied directly here
user_setup: []
tags: [phase-10, prod-deploy, compose, env-contract, capacity-gate, network-reconciliation]
must_haves:
  truths:
    - "Operator can `docker compose up -d` the prod stack on n8n-ia-vm without `network not found` errors"
    - "Operator can populate `.env` from `.env.prod.example` with documented sources for every secret"
    - "Operator has recorded live capacity values for n8n-ia-vm before committing prod traffic"
    - "Internal-Traefik discovery semantics for standalone-compose-on-Swarm are proven (or fallback option recorded)"
  artifacts:
    - path: "gateway/docker-compose.prod.yml"
      provides: "Operator-managed prod compose stack (gateway + dashboard + redis-gateway-prod, network: intra)"
      contains: "networks:\n  intra:\n    external: true"
    - path: "gateway/.env.prod.example"
      provides: "Documented prod env contract — sources column for every value"
      min_lines: 60
    - path: "scripts/deploy/preflight.sh"
      provides: "Idempotent capacity + intra-overlay attachability + Traefik-discovery probe runner"
      exports: ["main()"]
    - path: ".planning/phases/10-prod-deploy-ai-gateway/10-01-CAPACITY-OBSERVED.md"
      provides: "Recorded ssh probe output (free -h, df -h, docker network ls, docker info|grep swarm, curl ifconfig.io)"
  key_links:
    - from: "gateway/docker-compose.prod.yml"
      to: "intra (external overlay on n8n-ia-vm)"
      via: "networks.intra.external: true"
      pattern: "networks:\\s*\\n\\s*intra:\\s*\\n\\s*external:\\s*true"
    - from: "gateway/docker-compose.prod.yml"
      to: "/opt/ai-gateway-prod/.env"
      via: "env_file: .env"
      pattern: "env_file:\\s*\\.env"
    - from: "gateway/.env.prod.example"
      to: "gateway/docker-compose.prod.yml"
      via: "every var referenced by the compose lives in env_file"
      pattern: "AI_GATEWAY_PG_DSN|AI_GATEWAY_REDIS_PASSWORD|UPSTREAM_LLM_OPENROUTER_AUTH_BEARER|SENTRY_DSN|DASHBOARD_DATABASE_URL|GATEWAY_ADMIN_KEY"
---

<objective>
Reconcile the three CONTEXT.md assumptions RESEARCH proved wrong (network `worker_intra`→`intra`; schema `ai_gateway_prod`→new database `bd_ai_gateway_prod` with hardcoded `ai_gateway` schema; edge Traefik lives on vps-ifix-vm, not the Proxmox host) by shipping the foundational scaffolds every subsequent Phase 10 plan depends on: a Compose v2 operator-managed prod stack file, a documented prod env contract, and a preflight script the operator runs once to record live capacity + prove Traefik-Swarm discovery semantics.

Purpose: Wave 0 closes the contract between RESEARCH's discovered reality and what every downstream plan assumes. Without this, Plan 10-02 (Postgres bootstrap) does not know which database name to target, Plan 10-03 (Traefik route + DNS) does not have a working compose to deploy behind, and Plan 10-06 (HUMAN-UAT) operator burns Vast spend on a stack that throws `network worker_intra not found`.

Output: `gateway/docker-compose.prod.yml` + `gateway/.env.prod.example` + `scripts/deploy/preflight.sh` + a captured `10-01-CAPACITY-OBSERVED.md` snapshot recording the n8n-ia-vm live values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-prod-deploy-ai-gateway/10-CONTEXT.md
@.planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md
@.planning/phases/10-prod-deploy-ai-gateway/10-PATTERNS.md
@.planning/phases/10-prod-deploy-ai-gateway/10-VALIDATION.md

# Direct analogs the executor will read while writing
@gateway/docker-compose.yml
@gateway/.env.portainer.example
@gateway/internal/db/pool.go

<interfaces>
<!-- Hardcoded schema name in gateway/internal/db/pool.go — DO NOT change. -->
<!-- This is why Phase 10 uses a new DATABASE (bd_ai_gateway_prod) rather than a new schema. -->

From gateway/internal/db/pool.go:
- Line 35: `conn.Exec(ctx, "SET search_path = ai_gateway, public")` — every connection in the pool runs this
- Lines 73-74: hardcoded enum references `ai_gateway.api_key_status`, `ai_gateway.data_class`

From gateway/docker-compose.yml (Swarm template — DO NOT mutate; this plan creates a SIBLING file gateway/docker-compose.prod.yml):
- Network: `worker_intra` external — Pitfall 1 source. The prod compose MUST use `intra` instead.
- `deploy:` block (Swarm-only) — Pitfall 3 source. Replace with top-level `restart: unless-stopped` + top-level `resources` in prod compose.
- Traefik label `traefik.http.routers.ai_gateway.tls.certresolver=letsencryptresolver` + `entrypoints=websecure` — Pitfall 4 source. Internal Traefik on n8n-ia-vm listens on :80 only; TLS terminates at edge Traefik on vps-ifix-vm. Prod compose MUST drop the `tls.certresolver` label and change `entrypoints` to `web`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author gateway/.env.prod.example with documented sources</name>
  <files>gateway/.env.prod.example</files>
  <read_first>
    - gateway/.env.portainer.example (header banner + section structure analog)
    - gateway/docker-compose.yml (env-var references — every `${VAR}` in the canonical Swarm template must have a row in the prod env contract)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Code Examples lines 584-676 (verified prod env contract draft)
    - .planning/phases/10-prod-deploy-ai-gateway/10-PATTERNS.md §Pattern 2 (`gateway/.env.prod.example`)
    - .planning/phases/10-prod-deploy-ai-gateway/10-CONTEXT.md D-05 + D-06 + D-07 + D-08 + D-10 + D-14
  </read_first>
  <action>
    Create `gateway/.env.prod.example` as the documented env contract for `/opt/ai-gateway-prod/.env` on n8n-ia-vm. Mirror the verbatim text drafted in RESEARCH §Code Examples lines 584-676 (already verified). Header MUST cite source of each value: `~/.claude/CLAUDE.md` (CF + OpenRouter + MinIO + Vast.ai + OpenAI keys per D-08), `sentry.io` UI (new project DSN per D-14), `openssl rand -hex 24` (Redis password per D-07 + optional BETTER_AUTH_SECRET), `gatewayctl admin-key create` (dashboard admin key per D-09). Required section headers (banner-separated): `--- Runtime ---`, `--- Postgres (DO managed) ---` (DSN points at NEW DB `bd_ai_gateway_prod` per RESEARCH How To #3; `AI_GATEWAY_PG_DSN=postgres://...:25060/bd_ai_gateway_prod?sslmode=require`; `AI_GATEWAY_MIGRATE_ON_BOOT=true` for first deploy per D-05), `--- Redis (local container redis-gateway-prod) ---` (per D-07), `--- Upstreams ---` (local tier-0 paths `UPSTREAM_LLM_URL`/`UPSTREAM_STT_URL`/`UPSTREAM_EMBED_URL`/`UPSTREAM_HEALTH_BRIDGE_URL`), `--- Tier-1 fallback (REUSE dev keys per D-08) ---` (with explicit comment: `UPSTREAM_LLM_OPENROUTER_MODEL=` left empty per Phase 06.9 D-06 schema-row-wins), `--- Vast.ai primary + emerg (REUSE dev key + budgets per D-08) ---` (including Phase 06.8 final shape: `PRIMARY_GPU_NAME=RTX 3090`, `PRIMARY_NUM_GPUS=2`, `PRIMARY_VAST_MACHINE_ALLOWLIST=43803,55158`, `PRIMARY_VAST_PRICE_CAP_DPH=0.60`, `PRIMARY_VAST_MACHINE_BLOCKLIST=55942,45778`, `PRIMARY_POD_SCHEDULE_DISABLED=true`), `--- MinIO (REUSE dev creds) ---`, weight SHA-256 placeholders, `--- Sentry (NEW prod project) ---` (`SENTRY_DSN=<from sentry.io UI after creating ifix-ai-gateway-prod project>` per D-14), `--- Dashboard ---` (per D-06: `BETTER_AUTH_URL=https://ai-dashboard.converse-ai.app`, `DASHBOARD_DATABASE_URL=postgres://...:25060/bd_ai_dashboard_prod?sslmode=require&options=-c%20search_path%3Ddashboard_auth`, `GATEWAY_BASE_URL=http://ifix-ai-gateway:8080`, `GATEWAY_ADMIN_KEY=<from gatewayctl admin-key create>`), `--- Bootstrap ---` (`BOOTSTRAP_TENANT_SLUG=converseai`), `--- Schedule routing (Phase 04) ---` (`WRITE_TIMEOUT_CHAT_SECONDS=0`, `WRITE_TIMEOUT_EMBED_SECONDS=30`, `WRITE_TIMEOUT_AUDIO_SECONDS=120`). Every value uses placeholder syntax `<PLACEHOLDER>` or empty `=` (never a real secret). Mark `D-08 shared-key invariant` explicitly in a HEADER WARNING (rotation must touch BOTH `/opt/ai-gateway-dev/.env` + `/opt/ai-gateway-prod/.env`).
  </action>
  <verify>
    <automated>test -f gateway/.env.prod.example &amp;&amp; grep -c '^[A-Z_]\+=' gateway/.env.prod.example | awk '$1>=30{exit 0}{exit 1}' &amp;&amp; grep -q 'bd_ai_gateway_prod' gateway/.env.prod.example &amp;&amp; grep -q 'bd_ai_dashboard_prod' gateway/.env.prod.example &amp;&amp; grep -q 'ifix-ai-gateway-prod' gateway/.env.prod.example &amp;&amp; grep -q 'PRIMARY_VAST_MACHINE_ALLOWLIST=43803,55158' gateway/.env.prod.example &amp;&amp; grep -q '^UPSTREAM_LLM_OPENROUTER_MODEL=$' gateway/.env.prod.example</automated>
  </verify>
  <acceptance_criteria>
    File exists; ≥30 KEY= lines; the four anchor invariants pass grep (new DBs cited, new Sentry project cited, Phase 06.8 GPU shape pinned, OpenRouter MODEL env-empty per D-06). Header WARNING about shared-key rotation present.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author gateway/docker-compose.prod.yml (Compose v2, network intra, no deploy block, edge-TLS-friendly labels)</name>
  <files>gateway/docker-compose.prod.yml</files>
  <read_first>
    - gateway/docker-compose.yml (Swarm template analog — reuse service shape, healthcheck, container_name, labels structure; DO NOT copy `deploy:`, `worker_intra`, or `tls.certresolver=letsencryptresolver`)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Code Examples lines 678-759 (verified prod compose draft)
    - .planning/phases/10-prod-deploy-ai-gateway/10-PATTERNS.md §Pattern 1 (`gateway/docker-compose.prod.yml`) — 11-row diff table vs analog
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Pitfall 1 (intra not worker_intra), §Pitfall 4 (certresolver literal), §Anti-Patterns
  </read_first>
  <action>
    Create `gateway/docker-compose.prod.yml` mirroring the RESEARCH §Code Examples lines 680-759 verbatim. Services: `ifix-ai-gateway` (image `ghcr.io/ifixtelecom/ifix-ai-gateway:${TAG:-v1.0.0}` per D-12+D-13; `container_name: ifix-ai-gateway` for `docker exec ifix-ai-gateway /gatewayctl …`; `restart: unless-stopped` — top-level NOT `deploy.restart_policy` per Pitfall 3; `env_file: .env`; healthcheck `CMD /gateway --self-check` interval=30s timeout=5s retries=3 start_period=60s; labels `traefik.enable=true` + `traefik.docker.network=intra` + `traefik.http.routers.ai_gateway.rule=Host(\`ai-gateway.converse-ai.app\`)` + `traefik.http.routers.ai_gateway.entrypoints=web` per Pitfall 4 (NO `tls.certresolver=letsencryptresolver` label; NO `websecure` entrypoint) + `traefik.http.services.ai_gateway.loadbalancer.server.port=8080` + `traefik.http.services.ai_gateway.loadbalancer.passHostHeader=true`); `ifix-ai-dashboard` (image `ghcr.io/ifixtelecom/ifix-ai-dashboard:${DASHBOARD_TAG:-v1.0.0}`; `container_name: ifix-ai-dashboard`; `restart: unless-stopped`; `env_file: .env`; environment `TZ`/`NODE_ENV=production`/`PORT=3001`; `depends_on: [ifix-ai-gateway]`; symmetric Traefik labels with `routers.ai_dashboard.rule=Host(\`ai-dashboard.converse-ai.app\`)` and `loadbalancer.server.port=3001`); `redis-gateway-prod` (image `redis:7-alpine`; `container_name: redis-gateway-prod`; `restart: unless-stopped`; command `redis-server --requirepass ${AI_GATEWAY_REDIS_PASSWORD} --maxmemory 256mb --maxmemory-policy allkeys-lru --save "" --appendonly no` per D-07 no-persistence; healthcheck `redis-cli -a ${AI_GATEWAY_REDIS_PASSWORD} ping` interval=10s timeout=3s retries=3). Networks block at file end: `networks:\n  intra:\n    external: true` per Pitfall 1 (NOT `worker_intra`). NO host port publishes (Anti-Pattern); NO `deploy:` block (Pitfall 3); NO `tls.certresolver` label (Pitfall 4). Header comment block cites the 4 Pitfalls and the rsync target path `/opt/ai-gateway-prod/docker-compose.yml` on n8n-ia-vm per D-11.
  </action>
  <verify>
    <automated>test -f gateway/docker-compose.prod.yml &amp;&amp; python3 -c "import yaml,sys; d=yaml.safe_load(open('gateway/docker-compose.prod.yml')); assert 'services' in d and set(d['services'].keys())=={'ifix-ai-gateway','ifix-ai-dashboard','redis-gateway-prod'}; assert d['networks']['intra']['external'] is True; assert 'deploy' not in d['services']['ifix-ai-gateway']; assert all('certresolver' not in l for l in d['services']['ifix-ai-gateway']['labels']); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    YAML parses; 3 services present (ifix-ai-gateway + ifix-ai-dashboard + redis-gateway-prod); external network `intra` declared; no `deploy:` block on the gateway service; no `certresolver` substring anywhere in the gateway labels list. RESEARCH Pitfalls 1+3+4 all closed by file shape.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author scripts/deploy/preflight.sh (capacity + intra attachability + Traefik discovery probe)</name>
  <files>scripts/deploy/preflight.sh,.planning/phases/10-prod-deploy-ai-gateway/10-01-CAPACITY-OBSERVED.md</files>
  <read_first>
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §How To #1 (n8n-ia-vm capacity probe matrix)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Pitfall 6 (capacity ceiling — disk at 67%, mem at 9.8G available)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Open Question 2 (Traefik Swarm-provider standalone-discovery probe — Wave 0 mandatory)
    - .planning/phases/10-prod-deploy-ai-gateway/10-RESEARCH.md §Assumption A2 + A4 (intra attachable; Swarm-provider Traefik discovery — risk if wrong = fallback dual-provider)
    - .planning/phases/10-prod-deploy-ai-gateway/10-VALIDATION.md §Wave 0 Requirements (capacity gate + Traefik discovery probe)
    - scripts/integration-smoke/provision-tenants.sh (existing bash style — set -euo pipefail; log() helper; exit codes; idempotent invocation guard)
  </read_first>
  <action>
    Create `scripts/deploy/preflight.sh` as an idempotent operator-runnable bash script. Shebang `#!/usr/bin/env bash` + `set -euo pipefail`. Five sections each gated by a `log_section` banner: (1) **Connectivity** — `ssh n8n-ia-vm 'echo ok'` returns "ok" or exit 1; (2) **Capacity snapshot** — `ssh n8n-ia-vm 'free -h; df -h /; df -h /var/lib/docker; docker info | grep -iE "swarm|cpus"; curl -s --max-time 5 ifconfig.io'` — capture stdout into `.planning/phases/10-prod-deploy-ai-gateway/10-01-CAPACITY-OBSERVED.md` (overwrite-safe with timestamp banner); abort if `df -h /` > 80% (echo "DISK CRITICAL — run docker image prune first" + exit 2); abort if ifconfig.io output ≠ `162.55.92.154` (egress IP must match DO Trusted Sources per RESEARCH How To #3); (3) **Intra overlay attachability** — `ssh n8n-ia-vm 'docker network inspect intra -f "{{.Attachable}}"'` must print `true` (Assumption A4); abort with explicit `Pitfall 1 reminder` if network missing; (4) **Internal-Traefik discovery probe** (Assumption A2 / Open Question 2) — start ephemeral container `preflight-hello` on `intra` overlay with Traefik labels (`traefik.enable=true`, `traefik.http.routers.preflight_hello.rule=Host(\`preflight-hello.invalid\`)`, `traefik.http.services.preflight_hello.loadbalancer.server.port=80`); wait 3 s; grep `docker service logs traefik-internal_traefik` (last 60 s) for `preflight_hello` + `router added` patterns; capture PASS/FAIL into the CAPACITY-OBSERVED.md file; remove the ephemeral container regardless; if probe FAIL → echo `FALLBACK REQUIRED: switch traefik-internal to dual-provider (add --providers.docker=true alongside --providers.docker.swarmMode=true) — see RESEARCH Open Question 2 option B` and exit 3 (operator must remediate before Plan 10-02); (5) **GHA runners health** (Assumption A7) — `gh run list --limit 1 --workflow build-gateway.yml` returns ≤ 60 s old run OR an explicit "no recent runs" message that flags operator to confirm runner pool. Final exit 0 only if (1)+(2)+(3)+(4) PASS. ALSO create the initial `.planning/phases/10-prod-deploy-ai-gateway/10-01-CAPACITY-OBSERVED.md` template (the script populates it on first run) with frontmatter `phase: 10`, `plan: 01`, `captured: <ISO date placeholder>` and 5 section anchors matching the probes above.
  </action>
  <verify>
    <automated>test -x scripts/deploy/preflight.sh &amp;&amp; bash -n scripts/deploy/preflight.sh &amp;&amp; grep -q 'set -euo pipefail' scripts/deploy/preflight.sh &amp;&amp; grep -q 'docker network inspect intra' scripts/deploy/preflight.sh &amp;&amp; grep -q 'preflight-hello' scripts/deploy/preflight.sh &amp;&amp; grep -q '162.55.92.154' scripts/deploy/preflight.sh &amp;&amp; grep -q '10-01-CAPACITY-OBSERVED.md' scripts/deploy/preflight.sh &amp;&amp; test -f .planning/phases/10-prod-deploy-ai-gateway/10-01-CAPACITY-OBSERVED.md</automated>
  </verify>
  <acceptance_criteria>
    `scripts/deploy/preflight.sh` is executable, passes `bash -n` syntax, contains all 5 probes by recognisable command. Capacity observed file scaffold present. Operator can later run `./scripts/deploy/preflight.sh` and the script populates the captured file end-to-end (this autonomous task does NOT execute the SSH probes — they require live infra access and are covered in Plan 10-06 HUMAN-UAT Gate B).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → repo | This plan only authors files in the repo; no SSH or secret material handled at execute-time. |
| repo → operator (Plan 10-06 consumer) | `.env.prod.example` documents but does NOT contain secrets; operator populates `.env` from `~/.claude/CLAUDE.md` during HUMAN-UAT. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Information disclosure | `.env.prod.example` accidentally containing real secret values | mitigate | Task 1 mandates `<PLACEHOLDER>` syntax for every value; verify step greps for the placeholder pattern. File header WARNING reminds operator never to commit `.env`. |
| T-10-01-02 | Tampering | YAML parse failure on `docker-compose.prod.yml` causing `docker compose up -d` to silently misbehave | mitigate | Task 2 verify step runs `python3 -c "import yaml; yaml.safe_load(...)"` and asserts the expected service set + external network. |
| T-10-01-03 | Denial of service | n8n cluster CPU/mem starvation if prod stack added past capacity ceiling | mitigate | Task 3 preflight script gates on `df -h /` > 80% (RESEARCH Pitfall 6) before allowing downstream plans to deploy; capacity values recorded in `10-01-CAPACITY-OBSERVED.md` for audit. |
| T-10-01-04 | Information disclosure | `D-08 shared-key invariant` (dev + prod share OpenRouter/OpenAI/Vast keys) confused as separation | accept | Documented as known limitation in `.env.prod.example` header WARNING (RESEARCH Pitfall 7); Phase 11 may revisit. |
| T-10-01-05 | Tampering | Traefik internal Swarm provider silently NOT discovering standalone-compose containers on `intra` overlay → gateway unreachable post-deploy | mitigate | Task 3 preflight script emits explicit `FALLBACK REQUIRED: dual-provider` directive + exit 3 if probe fails; downstream plans cannot deploy until operator remediates (RESEARCH Open Question 2). |
| T-10-01-SC | Tampering | Supply-chain — npm/pip/cargo installs | accept | Phase 10 installs ZERO new packages (RESEARCH §Package Legitimacy Audit). No new dependency vectors introduced. |
</threat_model>

<verification>
End of Wave 0 quality gate (manual operator review before Plan 10-02 starts):
- [ ] `gateway/docker-compose.prod.yml` parses as valid YAML with 3 services + external `intra` network
- [ ] `gateway/.env.prod.example` contains ≥30 `KEY=` lines with placeholder syntax (no real secrets)
- [ ] `scripts/deploy/preflight.sh` is executable + passes `bash -n`
- [ ] `.planning/phases/10-prod-deploy-ai-gateway/10-01-CAPACITY-OBSERVED.md` scaffold exists (real capture happens during Plan 10-06 Gate B operator run)

The actual execution of `./scripts/deploy/preflight.sh` against the live n8n-ia-vm host is INTENTIONALLY deferred to Plan 10-06 HUMAN-UAT Gate B — autonomous executor does not have SSH credentials at runtime and the probe results are operator-recorded evidence.
</verification>

<success_criteria>
3/3 tasks GREEN; downstream plans (10-02 Postgres bootstrap, 10-03 ingress, 10-06 HUMAN-UAT) can reference the compose + env contract without modification.
</success_criteria>

<output>
Create `.planning/phases/10-prod-deploy-ai-gateway/10-01-SUMMARY.md` when done.
</output>
