---
phase: 14-vram-adaptive-stt
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - .planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-VERIFICATION.md
  - pod/smoke/uat-14.sh
autonomous: false
requirements: [STT-SHAPE-3090, STT-SHAPE-GPU, STT-MIGRATE, FLAG-REMOVE]
user_setup:
  - service: vast.ai
    why: "Live UAT provisions real GPU pods (1x3090 + a >=30GB shape) to validate end-to-end STT routing"
    env_vars:
      - name: VAST_AI_API_KEY
        source: "CLAUDE.md / MEMORY vast-ai-api-key (check Vast credit FIRST before any provision)"
    dashboard_config:
      - task: "Confirm Vast.ai account has credit before provisioning (MEMORY: check Vast credit FIRST)"
        location: "https://cloud.vast.ai/billing/"
      - task: "After UAT green, remove PRIMARY_POD_SERVE_STT from the ai-gateway-dev Portainer stack 34 env (Advanced + Update together) and redeploy via webhook"
        location: "Portainer stack 34 (/opt/ai-gateway-dev/) env UI"

must_haves:
  truths:
    - "The new primary-pod image is built, pushed to GHCR, and PRIMARY_TEMPLATE_IMAGE is promoted to its SHA"
    - "On a 1x3090 (24GB) pod the gateway routes STT to gemini-stt (200, ~2-3s), does NOT override stt, and the pod reports whisper_device=cpu"
    - "On a >=30GB shape (2x3090 or 5090) the gateway routes STT to the pod whisper-GPU (<5s, no CUDA OOM) and the pod reports whisper_device=cuda"
    - "PRIMARY_POD_SERVE_STT is removed from the prod stack env"
  artifacts:
    - path: pod/smoke/uat-14.sh
      provides: "the 2-shape live UAT runner (provision, assert device + STT route, destroy)"
    - path: .planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-VERIFICATION.md
      provides: "phase verdict rollup"
  key_links:
    - from: pod/smoke/uat-14.sh
      to: gateway/internal/primary/onstart.go
      via: "UAT asserts the pod-reported whisper_device matches the shape (cuda on >=30GB, cpu on 24GB)"
      pattern: "whisper_device"
---

<objective>
Roll out the SEED-019 parts 2+3 change and prove it on real hardware. Build + push + promote the rebuilt
primary-pod image (carries the supervisord.conf:46 edit, baked at build time), then run the two phase-gate
live Vast UATs that close CONTEXT's validation requirement, remove the dead prod env flag, and write the
phase verdict.

Purpose: a code change baked into the image is meaningless until the image ships and the live STT route is
proven bidirectional (24GB -> gemini, >=30GB -> pod GPU, no OOM).

Output: promoted image SHA, two green UATs, prod env cleaned, 14-VERIFICATION.md verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-CONTEXT.md
@.planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-RESEARCH.md
@.planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-VALIDATION.md
@.planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-01-SUMMARY.md
@.planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-02-SUMMARY.md
@.github/workflows/build-primary-pod.yml

<interfaces>
<!-- Reuse the established UAT operating recipe from prior live UATs. Do NOT invent a new provision path. -->
<!-- gatewayctl primary force-up / force-down drives the FSM; the gateway provisions on Vast via the env-pinned
     PRIMARY_VAST_* shape (1x3090 dev cap $0.30 per MEMORY primary-gpu-shape-11.1-final / 06.8-final). The
     >=30GB shape requires an allowlist/shape override per the ladder runbook (06.8-04). -->
<!-- MEMORY rules that bind this plan: -->
<!--   - check Vast credit FIRST (primary-gpu-shape-06.8-final) -->
<!--   - GHCR Actions install broken org-wide since 2026-06-09 -> PAT retag workaround (ghcr-actions-install-broken) -->
<!--   - ai-gateway-dev = Portainer GitOps stack 34; env via UI Advanced+Update together; redeploy via webhook -->
<!--   - MinIO weights store is in BR; provision Americas pods; cold-start budget raised (minio-sao-paulo-pod-geo) -->

Device-report contract under test (from Plan 14-01/14-02):
  GET :9100/whisper_device -> {"whisper_device":"cuda"} on >=30GB ; {"whisper_device":"cpu"} on 24GB
Routing assertion: POST a real audio clip to the gateway /v1/audio/transcriptions and confirm which upstream
served it (pod local-stt vs tier-1 gemini-stt) via gateway logs / latency (pod GPU <5s; gemini ~2-3s).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + push + promote the rebuilt primary-pod image; author uat-14.sh</name>
  <read_first>
    - .github/workflows/build-primary-pod.yml (the build + push pipeline + image-bump policy)
    - pod/smoke/ (existing uat-*.sh runners — e.g. uat-11.2.sh — mirror structure: provision, poll Ready, assert, destroy, cost-report)
    - the MEMORY notes referenced above for the force-up recipe and PAT-retag fallback
    - .planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-RESEARCH.md (Runtime State Inventory — image rebuild required; Environment Availability — Vast credit)
  </read_first>
  <action>
    Trigger the primary-pod image build via build-primary-pod.yml (GHCR push). If the GITHUB_TOKEN push fails
    org-wide (known broken since 2026-06-09), use the PAT-retag workaround from MEMORY ghcr-actions-install-broken:
    build locally, retag, push with the PAT. Capture the resulting image SHA and record it in the SUMMARY for the
    PRIMARY_TEMPLATE_IMAGE promotion (the Portainer env flip is the operator checkpoint in Task 2). Author
    pod/smoke/uat-14.sh: a runner that (per shape arg) force-ups the primary pod via gatewayctl, polls to Ready,
    curls GET :9100/whisper_device and asserts the value matches the shape (cpu for 1x3090, cuda for the >=30GB
    shape), POSTs a real audio clip to the gateway /v1/audio/transcriptions, asserts the serving upstream
    (gemini-stt for 24GB, pod local-stt for >=30GB) and the latency band, greps the pod speaches.log for
    "CUDA out of memory" (must be ABSENT on the >=30GB run), then force-downs and confirms BestEffortDestroy.
    Include a Vast credit pre-check that aborts before any provision (MEMORY rule).
  </action>
  <verify>
    <automated>bash -n pod/smoke/uat-14.sh && grep -c "whisper_device" pod/smoke/uat-14.sh</automated>
  </verify>
  <acceptance_criteria>
    - The build pipeline produced a pushed image SHA (recorded in 14-03-SUMMARY.md).
    - pod/smoke/uat-14.sh passes bash -n (syntax) and references whisper_device, /v1/audio/transcriptions, and a CUDA OOM grep.
    - grep -ci "credit" pod/smoke/uat-14.sh is >= 1 (Vast credit pre-check present).
  </acceptance_criteria>
  <done>Rebuilt image is pushed to GHCR with a captured SHA; uat-14.sh exists, syntactically valid, asserts device + route + no-OOM for both shapes with a credit pre-check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Gateway device-gated STT (Plan 14-01) + VRAM-adaptive pod whisper + :9100 device-report (Plan 14-02),
    rebuilt image pushed, uat-14.sh runner (Task 1). This checkpoint runs the 2 live Vast UATs end-to-end.
  </what-built>
  <how-to-verify>
    PRECONDITION: confirm Vast.ai credit at https://cloud.vast.ai/billing/ BEFORE provisioning (MEMORY rule).
    Promote PRIMARY_TEMPLATE_IMAGE to the Task-1 SHA in the ai-gateway-dev Portainer stack 34 env (Advanced +
    Update together) and redeploy via webhook.

    UAT A — 1x3090 (24GB), STT-SHAPE-3090 + STT-MIGRATE:
      1. Run bash pod/smoke/uat-14.sh 3090 (or force-up the 1x3090 shape via gatewayctl primary force-up).
      2. Poll to Ready. Assert GET :9100/whisper_device returns whisper_device cpu.
      3. POST a real ~10s audio clip to the gateway /v1/audio/transcriptions.
      4. Confirm it is served by tier-1 gemini-stt (NOT the pod), HTTP 200, ~2-3s; gateway did NOT set the stt override.
      5. force-down; confirm pod destroyed.

    UAT B — >=30GB shape (2x3090 or 5090), STT-SHAPE-GPU:
      1. Provision the >=30GB shape (allowlist/shape override per the 06.8 ladder runbook).
      2. Poll to Ready. Assert GET :9100/whisper_device returns whisper_device cuda.
      3. POST the same real audio clip.
      4. Confirm it is served by the pod whisper-GPU local-stt, HTTP 200, <5s.
      5. Assert NO "CUDA out of memory" in the pod speaches.log (device_index landed off the contended GPU).
      6. force-down; confirm pod destroyed + BestEffortDestroy succeeded (no orphan).

    AFTER both green: remove PRIMARY_POD_SERVE_STT from the Portainer stack 34 env (it is now dead — Plan 14-01
    deleted the gateway read) and redeploy via webhook.
  </how-to-verify>
  <resume-signal>Type "approved" with the two device strings + serving upstreams + latencies + OOM-grep result, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 14-VERIFICATION.md verdict + advance STATE/ROADMAP</name>
  <read_first>
    - .planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-01-SUMMARY.md + 14-02-SUMMARY.md + 14-03 checkpoint result
    - a prior phase VERIFICATION.md (e.g. 11.2-VERIFICATION.md) for the rollup format
    - .planning/STATE.md + .planning/ROADMAP.md (Phase 14 entry)
  </read_first>
  <action>
    Write 14-VERIFICATION.md: per-requirement verdict (STT-AUTO, STT-FAILSAFE, STT-PROBE, POD-VRAM, FLAG-REMOVE,
    STT-SHAPE-3090, STT-SHAPE-GPU, STT-MIGRATE) with the evidence from the UATs + unit suite + the captured
    device strings/latencies/OOM-grep from the checkpoint. Record the promoted image SHA and that
    PRIMARY_POD_SERVE_STT was removed from the prod stack. Set the verdict (passed / passed_partial) and Vast
    spend vs cap. Mark the Phase 14 plans complete in ROADMAP.md and advance STATE.md current position.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-VERIFICATION.md && grep -c "STT-SHAPE-GPU" .planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 14-VERIFICATION.md exists with a per-requirement verdict table covering all 8 requirement IDs.
    - It records the promoted image SHA + PRIMARY_POD_SERVE_STT removal + Vast spend vs cap.
    - ROADMAP.md Phase 14 plans marked complete; STATE.md current position advanced.
  </acceptance_criteria>
  <done>14-VERIFICATION.md captures the verdict + evidence for all 8 requirements; ROADMAP + STATE updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Vast.ai | live pod provisioning spends real credit; orphan pods burn money |
| operator -> Portainer stack 34 env | promoting the image + removing the dead flag changes prod routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Denial of Service / cost | Vast pod orphan after UAT | mitigate | uat-14.sh force-downs + asserts BestEffortDestroy; credit pre-check aborts before provision (MEMORY rule). |
| T-14-09 | Tampering | wrong image SHA promoted | mitigate | Task 1 records the exact pushed SHA; checkpoint promotes that SHA explicitly before UAT. |
| T-14-10 | Repudiation | flag removal before gateway stops reading it | mitigate | PRIMARY_POD_SERVE_STT removal happens AFTER UAT green and AFTER Plan 14-01 deleted the gateway read (ordered in the checkpoint). |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. No legitimacy gate required. |
</threat_model>

<verification>
- bash -n pod/smoke/uat-14.sh passes.
- Both live UATs green (device strings + serving upstreams + latencies + no-OOM recorded at checkpoint).
- 14-VERIFICATION.md exists with the 8-requirement verdict; ROADMAP + STATE advanced.
</verification>

<success_criteria>
- Rebuilt image pushed + promoted; 1x3090 routes STT to gemini, >=30GB routes STT to pod GPU <5s no OOM.
- PRIMARY_POD_SERVE_STT removed from prod; phase verdict written.
</success_criteria>

<output>
Create `.planning/phases/14-vram-adaptive-stt-gateway-auto-decides-pod-stt-via-pod-repor/14-03-SUMMARY.md` when done
</output>
