---
phase: 09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api
plan: 04
type: execute
wave: 2
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - .planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-HUMAN-UAT.md
autonomous: false
requirements: [INT-03, INT-04, INT-05]
must_haves:
  truths:
    - "An operator has a live-verification scenario sheet covering SC1 (sensitive-failover smoke), SC2 (cobrancas+campanhas quotas + cost), SC3 (voice-api LLM-via-gateway), SC4 (per-app rollback drill + LGPD legal sign-off)"
    - "The sheet drills the sensitive-failover smoke against a data_class=sensitive tenant key, asserting exit 0 + gates.all_passed"
    - "The sheet has a blocking checkpoint for the external LGPD legal sign-off — the operator attaches the signed-off LGPD-REVIEW-CHECKLIST.md"
    - "The autonomous Phase 9 artifacts (09-01..09-03) ship the gpu-ifix-side scripts + docs and are NOT blocked by this plan or the un-deployed gateway"
  artifacts:
    - path: ".planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-HUMAN-UAT.md"
      provides: "live-verification scenario sheet + sign-off table for SC1/SC2/SC3/SC4 + the blocking LGPD legal sign-off checkpoint"
      contains: "Sign-off"
  key_links:
    - from: ".planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-HUMAN-UAT.md"
      to: "scripts/integration-smoke/provision-tenants.sh + smoke-sensitive-failover.py"
      via: "the UAT scenarios run the seed script + the sensitive-failover smoke against the deployed gateway"
      pattern: "provision-tenants|smoke-sensitive-failover"
    - from: ".planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-HUMAN-UAT.md"
      to: "gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md + gateway/docs/LGPD-REVIEW-CHECKLIST.md"
      via: "the rollback drill executes the runbook ROLLBACK procedures; the LGPD checkpoint attaches the signed checklist"
      pattern: "RUNBOOK-CLIENT-INTEGRATION-SENSITIVE|LGPD-REVIEW-CHECKLIST"
---

<objective>
The phase-closing HUMAN-UAT plan — the live-credential, live-traffic, external-sign-off verification that cannot run autonomously, mirroring 03-08 / 04-09 / 06-11 / 07-09 / 08-04. The autonomous Phase 9 plans (09-01..09-03) ship only the gpu-ifix-side artifacts — the extended `provision-tenants.sh` (4 mixed-data-class tenants + quotas), `smoke-sensitive-failover.py` + its schema, the sensitive-tenant runbook, and the two LGPD docs — and stay green; they are NOT blocked. This plan writes the HUMAN-UAT scenario sheet, then gates the live integration behind a blocking human-verify checkpoint.

Note the **double gate**, same as Phase 8 plus one more: (1) the gateway itself is not deployed yet — build-gateway is currently blocked on Phase 6 emergency-pod integration tests (a separate debug session); (2) the LGPD legal sign-off is an EXTERNAL gate — the operator obtains it from Ifix legal before activating the sensitive tenants in production. So the live UAT is gated on the gateway being deployed AND the operator-run env-var switch in each of the 4 client repos AND the external legal sign-off.

Purpose: close Phase 9 with an operator-executed evidence sheet — the same deferred-UAT pattern every prior phase used — and make the LGPD legal sign-off a blocking, attributable checkpoint.
Output: `.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-CONTEXT.md
@.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-PATTERNS.md
@.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-01-SUMMARY.md
@.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-02-SUMMARY.md
@.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-03-SUMMARY.md

<interfaces>
HUMAN-UAT plan analog — `.planning/phases/08-client-integration-converseai-chat-ifix/08-04-PLAN.md` (the plan structure: objective explaining the autonomous-build-not-blocked framing, ONE `type="auto"` write task + ONE `type="checkpoint:human-verify" gate="blocking"` task).
Produced-artifact analog — `.planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md` — the structure to copy:
  YAML frontmatter: `status: pending`, `phase`, `source`, `started`/`updated`/`operator`/`date_executed` blanks, `final_status: pending  # pass | partial | fail`
  intro: live-integration framing + the "autonomous build NOT blocked" paragraph
  `## Prerequisites` — `- [ ]` checkbox list (gateway-deployed gate, provision-tenants run, per-app env switch, LGPD sign-off obtained, deps installed)
  `## Tests` — numbered `### N. UAT-N — <name> (SC-N)` blocks: Pre-conditions / Steps (numbered/fenced bash) / Expected / pass-fail
  `## Summary`
  `## Sign-off` — table (Result / Date / Operator / Notes per scenario + an overall phase-status line)
  `## Final Sign-off`
  `## passed_partial fallback` — the deploy/credential-blocked fallback note (copy verbatim shape)
  `## Gaps`

Phase 9 SC (from ROADMAP Phase 9):
  SC1: Telefonia/NextBilling transcribes call audios through the gateway with `data_class: sensitive`; during induced primary failure the requests queue or fail closed — never reaching OpenAI/OpenRouter — and the audit table records the decision
  SC2: Cobranças + Campanhas send LLM personalization + embedding lookups through the gateway with tenant-specific quotas, and metrics confirm cost-per-request is reported correctly
  SC3: voice-api continues to run TTS locally on CPU but retrieves LLM-generated scripts through the gateway
  SC4: each app has an individual production smoke + a <5-min rollback playbook; LGPD review is documented before sensitive tenants go live in prod

The 4 client sibling repos + deploy flows (CLAUDE.md `## Dev Environment` + 09-CONTEXT.md):
  telefonia/NextBilling → `fallback-register-ramais-nextbilling`
  cobrancas → `cobrancas-api` (Bun + Elysia; runner `cobrancas-api-vps`; Portainer deploy)
  campanhas → `campanhas-chatifix` (Bun monorepo backend; runner `campanhas-chatifix-vps`)
  voice-api → `voice-api` (runner `voice-api-vps`; TTS stays local CPU — only LLM script generation through the gateway)
  ALL FOUR are SIBLING repos, NOT part of gpu-ifix — the base_url switch is an operator action.

Phase 9 artifacts the UAT consumes (from 09-01/09-02/09-03):
  scripts/integration-smoke/provision-tenants.sh        — seed the 4 tenants + quotas; mint keys with `--mint-keys`
  scripts/integration-smoke/smoke-sensitive-failover.py — RES-08 fail-closed/never-external/audit smoke
  gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md  — the 4 per-app <5-min ROLLBACK procedures
  gateway/docs/LGPD-SUBPROCESSORS.md + LGPD-REVIEW-CHECKLIST.md — the LGPD disclosure + the checklist with the legal sign-off table

`passed_partial` fallback shape — `08-HUMAN-UAT.md` (the deploy/credential-blocked section): if the gateway-not-deployed gate fails, every UAT is `passed_partial`, blocker noted in the Sign-off table Notes column; the autonomous build is already green and NOT blocked.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write 09-HUMAN-UAT.md scenario sheet + sign-off table + LGPD checkpoint</name>
  <read_first>
    - .planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md (the closest produced-artifact analog IN FULL — frontmatter, the "autonomous build not blocked" intro, Prerequisites checkbox list, numbered UAT blocks, Summary, Sign-off table, Final Sign-off, the passed_partial fallback, Gaps)
    - .planning/phases/08-client-integration-converseai-chat-ifix/08-04-PLAN.md (the closing-plan structure — one write task + one blocking checkpoint task; the autonomous-build-not-blocked framing in the objective)
    - .planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-CONTEXT.md (the SC1-SC4 scope, the gpu-ifix-side-vs-operator-action boundary, the gateway-not-deployed gate, the LGPD-external-gate framing)
    - .planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-01-SUMMARY.md + 09-02-SUMMARY.md + 09-03-SUMMARY.md (the exact invocation of the seed script + the sensitive smoke + the names/locations of the runbook + LGPD docs the UAT steps reference)
    - gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md (the ROLLBACK procedures the rollback-drill UAT executes)
    - gateway/docs/LGPD-REVIEW-CHECKLIST.md (the checklist whose signed-off copy the LGPD checkpoint attaches)
  </read_first>
  <action>
    Create `.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-HUMAN-UAT.md` mirroring `08-HUMAN-UAT.md`.

    **YAML frontmatter:** `status: pending`, `phase: 09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api`, `source: [09-CONTEXT.md, 09-04-PLAN.md Task 1, ROADMAP Phase 9 SC1-SC4]`, `started: ___`, `updated: ___`, `operator: ___________`, `date_executed: ___________`, `final_status: pending  # pass | partial | fail`.

    **Intro** — the live-integration framing + the "autonomous build NOT blocked" paragraph: the autonomous Phase 9 plans (09-01..09-03) ship the gpu-ifix-side artifacts and are already green; this sheet is the live-credential / live-traffic / external-sign-off verification. State the double gate: the gateway is not deployed yet (blocked on Phase 6 emerg integration tests — a separate debug session) AND the LGPD legal sign-off is an external gate.

    **`## Prerequisites`** — `- [ ]` checkbox list: gateway deployed to the `ai-gateway-dev` Portainer stack (note this is currently blocked on Phase 6 emerg integration tests); the 4 tenants provisioned via `scripts/integration-smoke/provision-tenants.sh --mint-keys` and the 4 tenant keys + admin key captured; the per-app `base_url`/`api_key` env vars switched to the gateway in each of the 4 client repos' deploy configs (`fallback-register-ramais-nextbilling`, `cobrancas-api`, `campanhas-chatifix`, `voice-api`); `LGPD-REVIEW-CHECKLIST.md` worked through and submitted to Ifix legal; Python deps installed from `scripts/integration-smoke/requirements.txt` (incl. `psycopg`); `AI_GATEWAY_PG_DSN` available for the smoke's audit gates.

    **`## Tests`** — numbered `### N. UAT-N — <name> (SC-N)` blocks, each with **Pre-conditions / Steps (fenced bash) / Expected / pass-fail**:
      - **UAT-1 — Telefonia sensitive-failover smoke (SC1):** switch the `fallback-register-ramais-nextbilling` transcription `base_url` + key to the gateway, redeploy; run `python scripts/integration-smoke/smoke-sensitive-failover.py --gateway-url <dev gateway> --api-key <telefonia sensitive key> --pg-dsn <gateway DSN> --out /tmp/sensitive-failover-report.json` (the smoke induces the upstream failure per its `--induce-failure-via` arg — follow its printed operator pre-step). Expected: exit code 0, the report's `gates.all_passed` is `true` (fail_closed: 503 + `upstream_unavailable_for_sensitive_tenant` + `Retry-After: 30`; never_external: audit_log `upstream='blocked_sensitive'`; audit_decision: row found + zero `audit_log_content` rows). This is the load-bearing RES-08 proof — never reaches an external provider.
      - **UAT-2 — Cobranças + Campanhas quotas + cost (SC2):** switch the `cobrancas-api` + `campanhas-chatifix` backend `base_url` + keys to the gateway, redeploy; send LLM-personalization + embedding requests through the gateway for both tenants; verify the per-tenant quotas (set by the seed script) are enforced and that the Phase-7 dashboard / `/admin/usage` reports cost-per-request for `cobrancas` + `campanhas`. Note cobrancas is `sensitive` — also confirm a quick fail-closed check (or re-run UAT-1's smoke with the cobrancas key) so cobrancas' never-external guarantee is exercised too. Expected: quotas enforced, cost reported per tenant.
      - **UAT-3 — voice-api LLM-via-gateway (SC3):** switch the `voice-api` LLM `base_url` + key to the gateway, redeploy; trigger an LLM script-generation call; confirm it returns through the gateway and that TTS still runs on local CPU (NOT through the gateway). Expected: LLM script generation works via the gateway, TTS unaffected.
      - **UAT-4 — Per-app rollback drill, timed <5 min each (SC4):** for EACH of the 4 apps, start a stopwatch and execute the corresponding `### To roll back` procedure in `gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md` (revert env vars, redeploy, verify with the `psql` audit_log row-count going to 0); stop the stopwatch. Expected: each app fully rolled back to direct providers, verified, in under 5 minutes; record the measured time per app.

    **`## Summary`** — a short results paragraph the operator fills in.

    **`## Sign-off`** — a table: one row per UAT scenario with Result / Date / Operator / Notes columns, plus an overall phase-status line (`passed` / `passed_partial` / `human_needed`).

    **`## Final Sign-off — LGPD legal review (BLOCKING, external gate)`** — a dedicated section: the operator must attach the signed-off `gateway/docs/LGPD-REVIEW-CHECKLIST.md` (signed by Ifix legal per the checklist's `## Sign-off` table). State explicitly that sensitive tenants (telefonia, cobrancas) MUST NOT be activated in production until this signature exists — this is the ROADMAP SC4 / PRD-05 LGPD-review-documented-before-go-live gate. A table: Reviewer / Role / Date / approval reference.

    **`## passed_partial fallback`** — copy the `08-HUMAN-UAT.md` shape verbatim: if the gateway-not-deployed gate fails OR the LGPD sign-off is not yet obtained, mark the affected scenarios `passed_partial`, blocker noted in the Sign-off table Notes column; the autonomous Phase 9 build (09-01..09-03) is already green and NOT blocked. If a real defect is found (not a missing credential/deploy/signature), describe it precisely so a `/gsd-plan-phase --gaps` pass can close it.

    **`## Gaps`** — empty section for the operator to fill if a real defect is found.
  </action>
  <verify>
    <automated>UAT=.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-HUMAN-UAT.md; test -f "$UAT" && grep -qi 'Sign-off' "$UAT" && grep -qi 'Prerequisites' "$UAT" && grep -qi 'passed_partial' "$UAT" && [ "$(grep -c 'UAT-' "$UAT")" -ge 4 ] && grep -q 'smoke-sensitive-failover' "$UAT" && grep -q 'provision-tenants' "$UAT" && grep -qi 'LGPD' "$UAT" && grep -qi 'RUNBOOK-CLIENT-INTEGRATION-SENSITIVE' "$UAT" && grep -qi 'telefonia' "$UAT" && grep -qi 'voice-api' "$UAT" && grep -qi 'final_status' "$UAT"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/09-.../09-HUMAN-UAT.md` exists with YAML frontmatter (`status`, `operator`, `date_executed`, `final_status: pending`) and a `## Prerequisites` checkbox list including the gateway-not-deployed gate, the provision-tenants run, the per-app env switch for all 4 repos, and the LGPD-checklist-submitted item
    - It has at least 4 numbered UAT scenarios mapped to SC1/SC2/SC3/SC4, each with Pre-conditions / Steps (fenced bash) / Expected / pass-fail
    - UAT-1 invokes `smoke-sensitive-failover.py` with a sensitive tenant key + `--pg-dsn` and asserts exit 0 + `gates.all_passed`; UAT-4 drills the `RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md` ROLLBACK procedure for all 4 apps and times each <5 min
    - It has a `## Final Sign-off` section making the LGPD legal sign-off a BLOCKING external gate — the operator attaches the signed `LGPD-REVIEW-CHECKLIST.md`, and sensitive tenants must not go to production without it
    - It has a Sign-off table (Result / Date / Operator / Notes) + an overall phase-status line, and documents the `passed_partial` path for an un-deployed gateway / missing credentials / pending LGPD signature (autonomous build not blocked)
  </acceptance_criteria>
  <done>The HUMAN-UAT scenario sheet exists, mirrors 08-HUMAN-UAT.md, covers SC1-SC4 (sensitive-failover smoke / cobrancas+campanhas quotas+cost / voice-api LLM-via-gateway / 4-app rollback drill), lists the provisioning + per-app-env-switch + LGPD prerequisites, has a blocking LGPD legal sign-off section, and has a sign-off table with the passed_partial fallback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the live sensitive-tenant UAT scenarios + LGPD sign-off + record results</name>
  <what-built>
    The Phase 9 gpu-ifix-side artifacts are built and the autonomous plans (09-01..09-03) are green: the extended `provision-tenants.sh` (seeds the 4 mixed-data-class tenants — telefonia + cobrancas `sensitive`, campanhas + voice-api `normal` — and applies per-tenant quotas to cobrancas + campanhas), `smoke-sensitive-failover.py` + `sensitive-failover-report-schema.json` (the RES-08 black-box proof — fail-closed 503 envelope + never-external + audit-decision gates against the deployed gateway and its DB), `gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md` (the 4 per-app <5-min ROLLBACK procedures + the expected-sensitive-503 symptom + the no-peak-mode note), and `gateway/docs/LGPD-SUBPROCESSORS.md` + `LGPD-REVIEW-CHECKLIST.md` (the sub-processor disclosure listing OpenAI/OpenRouter/Vast.ai + the legal-review checklist). This task is the live-credential, live-traffic, external-sign-off verification that cannot run autonomously.
  </what-built>
  <how-to-verify>
    1. Complete the **Prerequisites** in `09-HUMAN-UAT.md`: confirm the gateway is deployed to the `ai-gateway-dev` Portainer stack (currently blocked on Phase 6 emerg integration tests — a separate debug session; if still blocked, this whole task is `passed_partial`); run `scripts/integration-smoke/provision-tenants.sh --mint-keys` once to seed the 4 tenants + quotas + mint the keys; switch the `base_url`/`api_key` env vars in each of the 4 client repos' deploy configs (`fallback-register-ramais-nextbilling`, `cobrancas-api`, `campanhas-chatifix`, `voice-api`); work through `gateway/docs/LGPD-REVIEW-CHECKLIST.md` and submit it to Ifix legal; install `scripts/integration-smoke/requirements.txt`; have `AI_GATEWAY_PG_DSN` available for the smoke's audit gates.
    2. Execute UAT-1..UAT-4 in `09-HUMAN-UAT.md` in order. For each, record Result / Date / Operator / Notes in the Sign-off table.
    3. Obtain the LGPD legal sign-off: Ifix legal signs the `## Sign-off` table in `LGPD-REVIEW-CHECKLIST.md`; record the signature in the `## Final Sign-off` section of `09-HUMAN-UAT.md`. Sensitive tenants (telefonia, cobrancas) MUST NOT be declared production-ready until this signature exists.
    4. If the gateway is not deployed, a credential is genuinely unavailable, or the LGPD signature is not yet obtained, mark the affected scenarios `passed_partial` and note the blocker — the autonomous build (09-01..09-03) is already green; live validation + legal sign-off are the deferred-gate pattern.
    5. Fill the overall phase-status line (`passed` if all live scenarios pass AND the LGPD sign-off is attached; `passed_partial` if some are deploy/credential/signature-blocked; `human_needed` if a real defect is found).
    6. If a real defect is found (not a missing credential/deploy/signature), describe it precisely in `## Gaps` so a `/gsd-plan-phase --gaps` pass can close it.
  </how-to-verify>
  <resume-signal>Type "approved" with the Sign-off table filled and the LGPD legal sign-off recorded, or describe the defects found for a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → deployed gateway + 4 client-app deploy configs | The live UAT switches real client-app env vars and exercises the production-dev gateway with real tenant credentials, two of which are `data_class: sensitive` |
| client apps (telefonia, cobrancas, campanhas, voice-api) → gateway `/v1/*` | Real LGPD-sensitive workloads (call audio, financial data) cross the gateway auth + RES-08 sensitive-block boundary for the first time |
| LGPD review → Ifix legal → sensitive-tenant production activation | The external legal sign-off gates whether telefonia + cobrancas may be activated in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Information Disclosure | the tenant/admin keys exposed in the HUMAN-UAT sheet | mitigate | `09-HUMAN-UAT.md` references env var NAMES and the seed-script invocation — never raw key VALUES. The keys are surfaced once by `provision-tenants.sh` stdout (T-09-01) and pasted directly into the client-app deploy configs by the operator; CLAUDE.md's deploy pattern keeps stack env vars out of git. Mirrors 08-04-PLAN.md threat T-08-14. |
| T-09-17 | Repudiation | sensitive tenants activated in production with no attributable LGPD review or operator record | mitigate | `09-HUMAN-UAT.md` has a `## Final Sign-off` section that is a BLOCKING external gate: the signed-off `LGPD-REVIEW-CHECKLIST.md` (signed by Ifix legal) must be attached before sensitive tenants are declared production-ready. The Sign-off table records Operator + Date per UAT scenario + the `date_executed`/`operator` frontmatter — the live verification AND the legal review are both attributable. Satisfies ROADMAP SC4 / PRD-05. |
| T-09-18 | Tampering | a rollback leaves a sensitive app half-switched (some endpoints on the gateway, some direct) — LGPD-sensitive data still flowing through an un-reviewed path | mitigate | UAT-4 drills the per-app `RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md` ROLLBACK procedure end-to-end for all 4 apps, each with the `psql` audit_log row-count verify step that must reach 0 — a half-switched state (rows still landing) is caught by the verify step before the stopwatch stops. The per-app procedures are independent so one app's partial rollback does not mask another's. |
| T-09-19 | Elevation of Privilege | the live UAT exercising a sensitive workload through a tenant key that was minted `normal` by mistake — sensitive call audio could fail over to OpenAI/OpenRouter | mitigate | UAT-1 (and the cobrancas check in UAT-2) run `smoke-sensitive-failover.py` against the telefonia/cobrancas keys — the smoke's `never_external` + `audit_decision` gates assert `audit_log upstream='blocked_sensitive'`. A key minted `normal` by mistake would FAIL those gates (the request would have served or failed over with a different `upstream` value), so the misconfiguration is caught by the UAT before any production activation. The seed script (09-01) mints with explicit per-tenant `data_class`; this UAT is the runtime confirmation. |
| T-09-20 | Spoofing | a client app pointed at a wrong/malicious gateway URL during the env switch | accept | The operator sets the gateway URL in each client-app deploy config; TLS validation is on by default in the OpenAI SDK + httpx. A wrong host is caught by the per-app UAT smokes (the sensitive smoke fails its gates; the quota/cost UAT shows no traffic under the expected tenant). Same posture as Phase 8 (T-08-17) — no new mitigation in a HUMAN-UAT plan. |
</threat_model>

<verification>
- `.planning/phases/09-.../09-HUMAN-UAT.md` exists and is committed.
- The Task 2 blocking checkpoint produces a filled Sign-off table with an overall phase-status line AND the LGPD legal sign-off recorded.
- This plan is `autonomous: false` — it cannot reach COMPLETE without operator action + external legal sign-off, consistent with 03-08 / 04-09 / 06-11 / 07-09 / 08-04.
</verification>

<success_criteria>
- The HUMAN-UAT scenario sheet covers SC1 (sensitive-failover smoke proving RES-08 fail-closed + never-external + audit), SC2 (cobrancas + campanhas quotas + cost-per-request), SC3 (voice-api LLM-via-gateway with TTS staying local), SC4 (4-app rollback drill timed <5 min each + the LGPD review documented).
- An operator executes the scenarios, records a Sign-off table with an overall phase-status line, and attaches the Ifix-legal-signed `LGPD-REVIEW-CHECKLIST.md` as the blocking gate for sensitive-tenant production activation; `passed_partial` is the documented path when the gateway is not yet deployed, credentials are unavailable, or the LGPD signature is pending.
</success_criteria>

<output>
After completion, create `.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-04-SUMMARY.md` and `.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-VERIFICATION.md`
</output>
