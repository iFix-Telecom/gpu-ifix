---
phase: 09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md
  - gateway/docs/LGPD-SUBPROCESSORS.md
  - gateway/docs/LGPD-REVIEW-CHECKLIST.md
autonomous: true
requirements: [INT-03, INT-04, INT-05]
must_haves:
  truths:
    - "An operator has a documented per-app rollback procedure for telefonia + cobrancas + campanhas + voice-api, each drilled to under 5 minutes"
    - "The runbook documents that sensitive tenants getting 503s during an upstream outage is EXPECTED RES-08 behavior, not a bug"
    - "The runbook documents that sensitive tenants CANNOT be set to peak mode (the chk_sensitive_no_peak triple-defense)"
    - "An operator has an LGPD sub-processor disclosure doc that explicitly lists OpenAI, OpenRouter, and Vast.ai with the data class each can receive"
    - "An operator has an LGPD review checklist gating sensitive-tenant production activation, with checkbox items the operator works through before legal sign-off"
  artifacts:
    - path: "gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md"
      provides: "operator runbook for the 4 sensitive/normal client integrations, with 4 per-app <5-min rollback procedures + the sensitive-503-is-expected symptom"
      contains: "ROLLBACK"
    - path: "gateway/docs/LGPD-SUBPROCESSORS.md"
      provides: "LGPD sub-processor disclosure listing OpenAI, OpenRouter, Vast.ai and which data class each can receive"
      contains: "OpenRouter"
    - path: "gateway/docs/LGPD-REVIEW-CHECKLIST.md"
      provides: "operator checklist gating sensitive-tenant production activation, signed off by Ifix legal"
      contains: "sign-off"
  key_links:
    - from: "gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md"
      to: "the 4 client sibling repos (fallback-register-ramais-nextbilling, cobrancas-api, campanhas-chatifix, voice-api)"
      via: "the per-app rollback procedure documents the env-var revert + redeploy + verify"
      pattern: "base_url|BASE_URL|Portainer"
    - from: "gateway/docs/LGPD-SUBPROCESSORS.md"
      to: "the RES-08 sensitive-block mechanism + RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md"
      via: "the disclosure doc cross-references that sensitive tenants are never proxied to OpenAI/OpenRouter"
      pattern: "RES-08|blocked_sensitive|sensitive"
---

<objective>
Deliver the three Phase-9 documentation artifacts: the sensitive-tenant integration runbook and the two LGPD compliance docs.

1. `gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md` — the operator runbook for the 4 Phase-9 integrations, mirroring the Phase-8 `RUNBOOK-CLIENT-INTEGRATION.md` skeleton, with **4** per-app `### To roll back` subsections (telefonia, cobrancas, campanhas, voice-api), each a <5-minute env-var-revert + redeploy + verify procedure, plus sensitive-specific content the Phase-8 runbook does not have.
2. `gateway/docs/LGPD-SUBPROCESSORS.md` — a sub-processor disclosure document that explicitly lists **OpenAI, OpenRouter, and Vast.ai** as sub-processadores with the data class each can receive.
3. `gateway/docs/LGPD-REVIEW-CHECKLIST.md` — an operator checklist gating sensitive-tenant production activation; the actual legal sign-off is an external gate captured in the Phase-9 HUMAN-UAT plan (09-04).

This covers the SC4 rollback-playbook + LGPD-review artifacts for INT-03, INT-04, INT-05.

Purpose: the operator needs a runbook to safely point the sensitive + normal apps at the gateway and roll them back fast, plus the LGPD paperwork that must exist before sensitive tenants go live in production.
Output: `gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md`, `gateway/docs/LGPD-SUBPROCESSORS.md`, `gateway/docs/LGPD-REVIEW-CHECKLIST.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-CONTEXT.md
@.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-PATTERNS.md

<interfaces>
Runbook analog — `gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md` (Phase 8 08-04) — copy its full section skeleton:
  Title + "Read this when:" trigger bullets (incl. "needs to be rolled back")
  ## Mental Model (30 seconds) — the env-var contract + a compact diagram
  ## Quick Diagnosis (~2 minutes) — numbered curl / psql / docker exec block
  ## Incident Response by Symptom — `### Symptom N` blocks (Likely cause / Diagnose / Mitigate / Recovery)
  ## ROLLBACK procedure — per-app `### To roll back <app>` subsections
  ## Required Env Vars — `| Var | App | Required | Gateway value | Direct value | Purpose |` table
  ## Escalation
  ## Related Docs
Secondary analog — `gateway/docs/RUNBOOK-FAILOVER.md` for the numbered rollback-procedure shape and `### Symptom 3 — Sensitive tenant reports 503s` (cross-reference it — that symptom IS the expected RES-08 behavior).

The 4 client sibling repos + their deploy flows (from CLAUDE.md `## Dev Environment` + 09-CONTEXT.md):
  telefonia/NextBilling → `fallback-register-ramais-nextbilling` repo
  cobrancas → `cobrancas-api` repo (Bun + Elysia, port 3400; runner `cobrancas-api-vps`; Portainer deploy)
  campanhas → `campanhas-chatifix` repo (Bun monorepo backend; runner `campanhas-chatifix-vps`)
  voice-api → `voice-api` repo (runner `voice-api-vps`; TTS stays on local CPU — only LLM script generation goes through the gateway)
  ALL FOUR are SIBLING repos, NOT part of gpu-ifix — the base_url switch is an operator action, NOT edited by Phase 9.

data_class split (09-CONTEXT.md `## Decisions`):
  telefonia = sensitive (call audio is PII), cobrancas = sensitive (financial/collections data)
  campanhas = normal (marketing personalization), voice-api = normal (LLM script generation)
  → sensitive tenants are NEVER proxied to OpenAI/OpenRouter during failover (RES-08); normal tenants CAN fail over external.

RES-08 mechanism (Phase 3) — `data_class: sensitive` requests during failover queue (bounded ~4s retry) or fail closed with a 503 (`upstream_unavailable_for_sensitive_tenant`, `Retry-After: 30`); an `audit_log` row records `upstream='blocked_sensitive'`. The `chk_sensitive_no_peak` DB constraint + the Phase-4 triple-defense mean a sensitive tenant CANNOT be set to `mode=peak`.

LGPD docs — NO analog exists in the repo (`docs/` has only `CONVENTIONS.md` + a RUNBOOK; `gateway/docs/` is all RUNBOOKs). Structurally reuse the RUNBOOK markdown shell: top-level `#` title, dated, `##`-sectioned. The checklist reuses the `08-HUMAN-UAT.md` `## Prerequisites` `- [ ]` checkbox idiom. Content is net-new from 09-CONTEXT.md `## Decisions` + `## Specific Ideas`. The three external sub-processors to disclose: OpenAI, OpenRouter, Vast.ai (Vast.ai hosts the GPU pod — i.e. the LOCAL upstream; OpenAI + OpenRouter are the failover providers `normal` tenants can reach).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md</name>
  <read_first>
    - gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md (the FULL Phase-8 runbook — the section skeleton, and especially the per-app `### To roll back <app>` subsections — the load-bearing pattern to replicate for 4 apps)
    - gateway/docs/RUNBOOK-FAILOVER.md (the numbered rollback-procedure shape + `### Symptom 3 — Sensitive tenant reports 503s` — the symptom to cross-reference as EXPECTED behavior; the Required Env Vars table shape)
    - .planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-CONTEXT.md (the 4-tenant model, the data_class split, the gpu-ifix-side-vs-operator-action boundary, the <5-min rollback bar)
    - .planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-PATTERNS.md (the "RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md — NEW" section: the section skeleton, the sensitive-specific content NOT in the Phase-8 runbook)
  </read_first>
  <action>
    Create `gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md` mirroring the `RUNBOOK-CLIENT-INTEGRATION.md` section skeleton:

    (1) **Title + "Read this when:"** — `# Client Integration (Sensitive Tenants) — Telefonia + Cobranças + Campanhas + voice-api`, dated. Trigger bullets: one of the 4 apps is being pointed at the gateway; a sensitive tenant is getting 503s during an outage; an integration is misbehaving; an integration needs to be rolled back.

    (2) **## Mental Model (30 seconds)** — the env-var contract (`base_url` → gateway, `api_key` → per-tenant key) AND the data-class split: telefonia + cobrancas are `sensitive` (NEVER fail over to OpenAI/OpenRouter — they queue or 503), campanhas + voice-api are `normal` (CAN fail over external). A compact ASCII diagram showing the two paths. State that voice-api only routes LLM script-generation through the gateway — TTS stays on local CPU.

    (3) **## Quick Diagnosis (~2 minutes)** — numbered curl / psql / docker exec block: curl each tenant's endpoint; `psql` the gateway `audit_log` per tenant slug; **add a sensitive-block check** — `psql` query for `audit_log WHERE upstream='blocked_sensitive'` to confirm the RES-08 path is firing for the sensitive tenants during an outage; check the Phase-7 dashboard shows all 4 tenants.

    (4) **## Incident Response by Symptom** — `### Symptom N` blocks (Likely cause / Diagnose / Mitigate / Recovery) for: "client app gets 401" (wrong/unset key); "client app gets 429" (tenant quota/rate-limit — note cobrancas + campanhas have per-tenant quotas from the seed script); **"sensitive tenant (telefonia/cobrancas) getting 503s during an upstream outage"** — this is EXPECTED RES-08 behavior, NOT a bug: the sensitive tenant fails closed rather than leaking PII to an external provider; cross-reference `RUNBOOK-FAILOVER.md` Symptom 3; the Mitigation is to restore the local upstream, not to "fix" the 503; "transcription/LLM latency or quality regressed" (point at the smoke scripts).

    (5) **## ROLLBACK procedure** — THE load-bearing section. **4** per-app `### To roll back <app>` subsections — telefonia, cobrancas, campanhas, voice-api — each a numbered procedure: 1. revert the `*_BASE_URL` / `*_API_KEY` env vars in that app's deploy config (name the deploy target per app: `cobrancas-api` Portainer stack, `campanhas-chatifix` deploy flow, etc. — where the exact env-var names cannot be confirmed from the sibling repo, use the OpenAI-SDK-standard names and add a `> CONFIRM:` note that the operator must verify them against the actual client repo); 2. redeploy (the exact webhook/Portainer trigger); 3. verify — an exact `psql` `audit_log` row-count query for that tenant slug that must go to 0 (no new rows after rollback). State the **<5-min budget per app** explicitly and that each procedure MUST be drilled in the HUMAN-UAT, not just written.

    (6) **## Required Env Vars** — a `| Var | App | Required | Gateway value | Direct value | Purpose |` table covering the per-app `*_BASE_URL` / `*_API_KEY` vars for all 4 apps.

    (7) **## Sensitive-tenant notes** — a short section: sensitive tenants CANNOT be set to `mode=peak` (the `chk_sensitive_no_peak` DB constraint + the Phase-4 triple-defense — attempting it is rejected); sensitive content is never persisted to `audit_log_content` (D-B2).

    (8) **## Escalation** — 1st responder / escalation contact / per-severity comms (mirror the Phase-8 runbook).

    (9) **## Related Docs** — cross-link `09-CONTEXT.md`, `scripts/integration-smoke/provision-tenants.sh`, `scripts/integration-smoke/smoke-sensitive-failover.py`, `gateway/docs/RUNBOOK-FAILOVER.md`, `gateway/docs/LGPD-SUBPROCESSORS.md`, `gateway/docs/LGPD-REVIEW-CHECKLIST.md`, and the Phase-9 `09-HUMAN-UAT.md`.
  </action>
  <verify>
    <automated>test -f gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md && grep -qi 'ROLLBACK' gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md && grep -qi '5 min\|5-min\|five min' gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md && [ "$(grep -ciE '### To roll back' gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md)" -ge 4 ] && grep -qi 'telefonia' gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md && grep -qi 'cobrancas\|cobranças' gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md && grep -qi 'campanhas' gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md && grep -qi 'voice-api' gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md && grep -qi 'blocked_sensitive' gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md && grep -qi 'chk_sensitive_no_peak\|peak' gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md && grep -qiE 'Required Env Vars|BASE_URL' gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md</automated>
  </verify>
  <acceptance_criteria>
    - `gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md` exists and follows the `RUNBOOK-CLIENT-INTEGRATION.md` section skeleton (Read-this-when → Mental Model → Quick Diagnosis → Symptom blocks → ROLLBACK → Required Env Vars → Escalation → Related Docs)
    - The ROLLBACK section has exactly 4 `### To roll back` subsections (telefonia, cobrancas, campanhas, voice-api), each with env-var revert + redeploy + a `psql` audit_log row-count verify step, and states the <5-min-per-app budget + must-be-drilled requirement
    - There is a Symptom block documenting that sensitive-tenant 503s during an outage are EXPECTED RES-08 behavior (cross-referencing RUNBOOK-FAILOVER.md Symptom 3), not a bug
    - A sensitive-tenant-notes section documents that sensitive tenants cannot be set to `peak` mode (`chk_sensitive_no_peak`) and that sensitive content is never persisted to `audit_log_content`
    - The Mental Model documents the env-var contract + the sensitive-vs-normal data-class failover split; there is a Required Env Vars table covering all 4 apps
    - Related Docs cross-links the seed script + the sensitive smoke + RUNBOOK-FAILOVER.md + both LGPD docs + the Phase-9 HUMAN-UAT plan
  </acceptance_criteria>
  <done>The sensitive-tenant client-integration runbook exists in gateway/docs/, mirrors the Phase-8 runbook structure, has 4 per-app <5-min ROLLBACK procedures, documents the expected sensitive-503 symptom + the no-peak-mode constraint, and cross-links the LGPD docs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write LGPD-SUBPROCESSORS.md + LGPD-REVIEW-CHECKLIST.md</name>
  <read_first>
    - gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md (the markdown shell to reuse — top-level `#` title, dated, `##`-sectioned; there is no compliance-doc analog so the RUNBOOK shell is the structural template)
    - .planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md (the `## Prerequisites` `- [ ]` checkbox idiom — the checklist reuses this exact idiom)
    - .planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-CONTEXT.md (the LGPD content requirements: the 3 sub-processors to disclose, the sensitive-vs-normal data-class split, the external-gate framing for the legal sign-off)
    - .planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-PATTERNS.md (the "LGPD-SUBPROCESSORS.md + LGPD-REVIEW-CHECKLIST.md — NEW" section: the structure-only guidance, the content requirements, the cross-references)
  </read_first>
  <action>
    Create two markdown docs in `gateway/docs/`, both using the RUNBOOK markdown shell (top-level `#` title, dated, `##`-sectioned).

    **`gateway/docs/LGPD-SUBPROCESSORS.md`** — the sub-processor disclosure document:
    - `# LGPD — Sub-processadores do ifix-ai-gateway`, dated.
    - `## Purpose` — this doc discloses the external sub-processadores that may receive data routed through the gateway, as required for the LGPD review before sensitive tenants go live.
    - `## Sub-processadores` — a table with one row per sub-processor: **OpenAI**, **OpenRouter**, **Vast.ai**. Columns: Sub-processor / Role / Data class it can receive / When data flows to it. Key facts to encode: Vast.ai hosts the GPU pod — i.e. it runs the LOCAL upstream, all tenants' data is processed there; OpenAI + OpenRouter are the failover providers that ONLY `normal`-class tenants (campanhas, voice-api) can reach during a local outage. `data_class: sensitive` tenants (telefonia, cobrancas) are **NEVER** proxied to OpenAI/OpenRouter — they queue or fail closed (RES-08). State this never-external guarantee explicitly.
    - `## Tenant data-class mapping` — a table: each of the 4 tenants → its data_class → which sub-processadores can receive its data.
    - `## Mechanism` — a short paragraph: how the never-external guarantee is enforced (the RES-08 sensitive-block path, the `audit_log upstream='blocked_sensitive'` record, the smoke that verifies it). Cross-reference `RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md` and `RUNBOOK-FAILOVER.md`.
    - `## Related Docs` — cross-link `LGPD-REVIEW-CHECKLIST.md`, `RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md`.

    **`gateway/docs/LGPD-REVIEW-CHECKLIST.md`** — the operator checklist gating sensitive-tenant production activation:
    - `# LGPD — Checklist de Revisão (ativação de tenants sensíveis)`, dated.
    - `## Purpose` — this checklist is worked through by the operator and presented to Ifix legal; the actual legal sign-off is an EXTERNAL gate captured as a blocking checkpoint in the Phase-9 HUMAN-UAT plan. The checklist is the artifact; the signature is obtained from Ifix legal.
    - `## Checklist` — `- [ ]` checkbox items (the `08-HUMAN-UAT.md` `## Prerequisites` idiom), covering at minimum: `LGPD-SUBPROCESSORS.md` reviewed and current (OpenAI, OpenRouter, Vast.ai disclosed); base legal for processing sensitive call-audio (telefonia) + financial data (cobrancas) documented; the never-external guarantee for sensitive tenants verified (the `smoke-sensitive-failover.py` run passed — exit 0, `gates.all_passed`); the privacy-policy disclosure of OpenAI/OpenRouter/Vast.ai as sub-processadores published for the affected apps; data-retention posture for `audit_log` confirmed (and that `audit_log_content` holds no sensitive content — D-B2); the rollback procedure for the 2 sensitive apps drilled (`RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md`).
    - `## Sign-off` — a table for the Ifix legal reviewer: Reviewer / Role / Date / Signature/approval reference / Notes. State that this signed-off checklist is the evidence attached in the Phase-9 HUMAN-UAT and (per ROADMAP) carried into Phase 10 / PRD-05.
    - `## Related Docs` — cross-link `LGPD-SUBPROCESSORS.md`, `RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md`, the Phase-9 `09-HUMAN-UAT.md`.
  </action>
  <verify>
    <automated>test -f gateway/docs/LGPD-SUBPROCESSORS.md && test -f gateway/docs/LGPD-REVIEW-CHECKLIST.md && grep -q 'OpenAI' gateway/docs/LGPD-SUBPROCESSORS.md && grep -q 'OpenRouter' gateway/docs/LGPD-SUBPROCESSORS.md && grep -qi 'vast.ai\|vast' gateway/docs/LGPD-SUBPROCESSORS.md && grep -qi 'sensitive' gateway/docs/LGPD-SUBPROCESSORS.md && grep -qiE 'never|nunca|RES-08|blocked_sensitive' gateway/docs/LGPD-SUBPROCESSORS.md && grep -qE '^- \[ \]' gateway/docs/LGPD-REVIEW-CHECKLIST.md && grep -qi 'sign-off\|sign off\|assinatura' gateway/docs/LGPD-REVIEW-CHECKLIST.md && grep -qi 'smoke-sensitive-failover' gateway/docs/LGPD-REVIEW-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `gateway/docs/LGPD-SUBPROCESSORS.md` exists with a `## Sub-processadores` table that explicitly lists OpenAI, OpenRouter, and Vast.ai with each one's role + the data class it can receive
    - It states explicitly that `data_class: sensitive` tenants (telefonia, cobrancas) are NEVER proxied to OpenAI/OpenRouter, and has a tenant→data-class→sub-processadores mapping table for all 4 tenants
    - `gateway/docs/LGPD-REVIEW-CHECKLIST.md` exists with a `## Checklist` of `- [ ]` checkbox items gating sensitive-tenant production activation (including the `smoke-sensitive-failover.py` pass + the sub-processor disclosure + the rollback drill) and a `## Sign-off` table for the Ifix legal reviewer
    - Both docs use the RUNBOOK markdown shell (dated `#` title, `##` sections) and cross-link each other + `RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md` + the Phase-9 HUMAN-UAT plan
  </acceptance_criteria>
  <done>The LGPD sub-processor disclosure doc lists OpenAI/OpenRouter/Vast.ai with their data-class scope and the sensitive-never-external guarantee, and the LGPD review checklist provides the checkbox gate + legal sign-off table for sensitive-tenant production activation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → client-app deploy configs | The runbook's rollback procedures direct the operator to change real `*_BASE_URL`/`*_API_KEY` env vars in the 4 client sibling repos' deploy configs |
| gateway → external sub-processadores (OpenAI, OpenRouter) | The LGPD docs describe the boundary where `normal`-class tenant data can cross to a third party; `sensitive`-class data must NOT cross it |
| LGPD review artifacts → Ifix legal → production activation | The checklist + disclosure doc gate whether sensitive tenants are allowed to go live in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Information Disclosure | the runbook or LGPD docs embedding raw tenant/admin API key VALUES | mitigate | All three docs reference env var NAMES and the seed-script invocation only — never raw key VALUES. The keys are surfaced once by `provision-tenants.sh` stdout (T-09-01) and pasted by the operator into the client-app deploy configs; CLAUDE.md's deploy pattern keeps stack env vars out of git. The `> CONFIRM:` notes in the rollback procedures name env vars to verify, not secrets to embed. |
| T-09-12 | Tampering | a documented rollback that leaves a sensitive app half-switched (some endpoints on the gateway, some direct) | mitigate | Each of the 4 `### To roll back` subsections is a complete env-var-revert + redeploy + **verify** procedure — the verify step is a `psql` `audit_log` row-count for the tenant slug that must reach 0, so a half-switched state (new rows still landing) is caught before the procedure is declared done. The HUMAN-UAT (plan 09-04) drills each procedure end-to-end and times it. |
| T-09-13 | Repudiation | sensitive tenants activated in production with no record of LGPD review | mitigate | `LGPD-REVIEW-CHECKLIST.md` carries a `## Sign-off` table (Reviewer / Role / Date / approval reference) — the signed-off checklist IS the attributable evidence. The Phase-9 HUMAN-UAT plan (09-04) makes the legal sign-off a BLOCKING checkpoint: sensitive tenants cannot be declared production-ready until the signed checklist is attached. This satisfies the ROADMAP PRD-05 requirement (LGPD review evidence before activating sensitive tenants). |
| T-09-14 | Information Disclosure | the LGPD disclosure doc UNDER-disclosing — omitting a sub-processor that actually receives data, so the published privacy policy is incomplete | mitigate | `LGPD-SUBPROCESSORS.md` is built directly from the gateway's actual upstream topology: Vast.ai (GPU pod host — the local upstream), OpenAI + OpenRouter (the failover tier-1 providers). The `## Tenant data-class mapping` table forces an explicit per-tenant statement of which sub-processadores can receive that tenant's data, so an omission is visible. The doc is cross-referenced from the runbook + the checklist so a topology change (a new upstream) surfaces a review of this doc. RES-08's `audit_log upstream='blocked_sensitive'` record is the runtime proof that the disclosed never-external guarantee for sensitive tenants actually holds. |
| T-09-15 | Spoofing | an operator following the runbook points a sensitive app at a wrong/malicious gateway URL | accept | The operator sets the gateway URL in the client-app deploy config; TLS validation is on by default in the OpenAI SDK + httpx. A wrong host is caught by the per-app smoke + the UAT-4 dashboard cross-check (no traffic under the expected tenant). Same posture as Phase 8 (T-08-17) — no new mitigation in a docs-only plan. |
</threat_model>

<verification>
- `gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md` exists with 4 per-app <5-min ROLLBACK procedures + the expected-sensitive-503 symptom + the no-peak-mode note.
- `gateway/docs/LGPD-SUBPROCESSORS.md` explicitly lists OpenAI, OpenRouter, Vast.ai with their data-class scope.
- `gateway/docs/LGPD-REVIEW-CHECKLIST.md` has the `- [ ]` checklist + the legal sign-off table.
- All three docs use the RUNBOOK markdown shell and cross-link each other.
</verification>

<success_criteria>
- An operator has a runbook covering the 4 sensitive/normal client integrations with a per-app <5-min rollback procedure, the knowledge that sensitive-tenant 503s during an outage are expected RES-08 behavior, and the constraint that sensitive tenants cannot run in peak mode.
- An operator has an LGPD sub-processor disclosure doc listing OpenAI/OpenRouter/Vast.ai and an LGPD review checklist with a legal sign-off table — the artifacts that must exist before sensitive tenants go live in production.
</success_criteria>

<output>
After completion, create `.planning/phases/09-client-integration-sensitive-tenants-telefonia-cobran-as-campanhas-voice-api/09-03-SUMMARY.md`
</output>
