---
plan: 06.8-02
title: STT fix — live-pod validation record
status: in-progress
started: 2026-05-22
---

# STT Fix — Live-Pod Validation Record (Plan 06.8-02)

The CLAUDE.md anti-blind-commit gate: image/script-level STT fix MUST pass on a
live pod before any promotion to canonical / commit to develop.

## Build artifacts (Task 1 — off-develop, no shared-state mutation)

| Artifact | Value |
|----------|-------|
| Validation branch | `validate/06.8-stt` @ `150c04016721a127ae1b5b30e8d27520fbbc7221` (forked from `gsd/06.8-stt-fix-candidate`, carries Plan-01 commits c6d7c3b + 454b3f9) |
| Candidate MinIO key | `whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz` (2.65 GiB) |
| Candidate WEIGHTS_WHISPER_SHA256 | `d8e8f6e489c9eae7836a2faebc06af6fa6c631248fc1c644ca695d9bef3c34f0` |
| Validation image (tag) | `ghcr.io/ifixtelecom/converseai-primary-pod:dev-validate-06.8-stt-150c040` |
| Validation image (also) | `ghcr.io/ifixtelecom/converseai-primary-pod:validate-06.8-stt` |
| Validation image digest | `sha256:b57179d43a89e3ad989add0abc71227c4979a371cff7ab91aa4f2ade4236ad8e` |
| Whisper repo commit hash baked | `edaa852ec7e145841d8ffdb056a99866b5f0a478` (REAL HF main hash — A1 answered: real hash, not arbitrary) |
| CI build run | `26267685164` (build-primary-pod, workflow_dispatch --ref validate/06.8-stt, success 26m17s) |

**Deviations from PLAN.md text (forced by repo state, both reversible):**
- Plan-01 source was already committed on `gsd/06.8-stt-fix-candidate` (not an
  uncommitted working tree as the plan text assumed) → validation branch forks
  from the candidate branch.
- `build-primary-pod.yml` only auto-fires on `develop`/`v*` push → built via
  `workflow_dispatch --ref validate/06.8-stt` (off-develop, develop untouched).

**Shared-state state after Task 1:**
- canonical key `whisper-large-v3/v1.0.0/model.tar.gz` — UNTOUCHED (still 2026-05-12).
- develop — UNTOUCHED.
- GH Secrets / dev-stack env — UNTOUCHED.

## Live checklist (Task 2 — PASS 2026-05-22)

Validation pod: instance `37309227` on offer `36299652` / **machine 43803** (the
2026-05-21 spike-proven CDI host), 2×RTX 3090 48GB, driver 580.95.05, Estonia,
$0.401/h. ~26min lifecycle, ≈$0.17. Destroyed immediately after pass.
(First attempt instance `37308602`/machine 57777 California was destroyed —
persistent `ghcr.io/v2/: EOF` image-pull fault; host network defect, not our bug.)

- [x] 1. `/weights/whisper/models--Systran--faster-whisper-large-v3/snapshots/edaa852…/` → model.bin (3.09GB), config.json, tokenizer.json, vocabulary.json, preprocessor_config.json, README.md — HF-hub-cache layout intact.
- [x] 2. `curl localhost:8001/v1/models` → `Systran/faster-whisper-large-v3` listed. **Local resolution worked — no CacheNotFound, no MODEL_CARD_DOESNT_EXISTS.**
- [x] 3. `POST /v1/audio/transcriptions` (model=Systran/faster-whisper-large-v3, 6s synthetic WAV) → **HTTP 200** + `{"text":"you","logprobs":null,"usage":null}`. The pre-fix 500 `CacheNotFound` is GONE.
- [x] 4. whisper on GPU: GPU0 VRAM 12247→**16302 MiB (+4.0GB)** during transcription, util 38% → GPU offload confirmed (not CPU).
- [x] 5. silero v5 VAD ONNX present: `faster_whisper/assets/silero_{encoder,decoder}_v5.onnx`.
- [x] 6. **A1 RESOLVED**: the REAL HF commit hash `edaa852ec7e145841d8ffdb056a99866b5f0a478` is baked into `refs/main` and speaches accepted the on-disk snapshot layout. (No arbitrary hash was used — Plan 01 resolves the real hash at upload time, so the A1 "arbitrary hash" question is moot/zero-risk.)
- [x] 7. `/health` → `{"message":"OK"}`; supervisord spawned all 4 children (llama, speaches, chatterbox, dcgm) RUNNING; speaches lazy-loaded whisper on first transcription (delayed-load confirmed).

**Verdict: PASS.** STT fix (HF-hub-cache tarball + HF_HUB_CACHE=/weights/whisper) proven end-to-end on live hardware. Cleared for promotion (Task 3).

## Promotion log (Task 3 — PASS path, 2026-05-22)

Ordered sequence per Convergent #1 (env SHA updated before any next provision; onstart `sha256sum -c` never drifts):

- **(a)** `mc cp` candidate → canonical `whisper-large-v3/v1.0.0/model.tar.gz` (+ `.sha256`). Canonical now serves the validated tarball; canonical sha = `d8e8f6e489c9eae7836a2faebc06af6fa6c631248fc1c644ca695d9bef3c34f0` (was `15b60754…`, 2026-05-12).
- **(b)** GH Secrets (`IfixTelecom/gpu-ifix`): `PRIMARY_WHISPER_WEIGHTS_SHA256` (newly created) + `WEIGHTS_WHISPER_SHA256` → `d8e8f6e…`.
- **(c)** dev-stack `ai-gateway-dev` (vps-ifix-vm, `/opt/ai-gateway-dev/.env`): both whisper SHA vars `15b60754…` → `d8e8f6e…`; gateway container recreated (`docker compose up -d gateway`), Up healthy, `docker inspect` confirms new SHA. `.env` backed up to `.env.bak-pre-stt-fix-*`.
- **(d)** `gatewayctl primary force-down` published (no primary pod was running — fsm asleep, schedule-disabled; clean).

**Convergent #1 satisfied — no SHA drift:** canonical key == GH Secrets == gateway env == `d8e8f6e…`.

**develop commit (H1):** Plan-01 source cherry-picked onto develop:
- `5635502 fix(06.8-01): regenerate whisper tarball in HF-hub-cache layout …` (pod/scripts/upload-weights.sh)
- `999e290 fix(06.8-01): point speaches at pre-shaped HF cache via HF_HUB_CACHE` (pod/primary/supervisord.conf)

The supervisord.conf change is under `pod/primary/**` → pushing develop fires `build-primary-pod.yml`, rebuilding `ghcr.io/ifixtelecom/converseai-primary-pod:develop` (the tag the primary reconciler provisions: `template_image=…:develop`) with the HF_HUB_CACHE fix baked in.

**Cleanup:** validation branch `validate/06.8-stt` deleted (local + remote). Validation image tags (`dev-validate-06.8-stt-150c040`, `validate-06.8-stt`) left on GHCR (harmless, not a consumer default; prune later). Candidate MinIO key left in place (not a consumer default).
