---
phase: 06.8-multi-pod-gpu-topology-sizing-stt-fix
plan: 02
subsystem: stt-live-validation
tags: [stt, speaches, whisper, hf-hub-cache, live-validation, vast, promotion]
requires:
  - 06.8-01 (whisper HF-cache tarball + HF_HUB_CACHE supervisord change, held on gsd/06.8-stt-fix-candidate)
provides:
  - live-pod proof the STT fix works (transcriptions 200, whisper on GPU)
  - promoted canonical whisper tarball (HF-cache layout) + propagated SHA
  - Plan-01 source committed to develop
affects:
  - MinIO canonical key whisper-large-v3/v1.0.0/model.tar.gz
  - GH Secrets PRIMARY_WHISPER_WEIGHTS_SHA256 + WEIGHTS_WHISPER_SHA256
  - dev-stack ai-gateway-dev .env (whisper SHA)
  - develop (pod/scripts/upload-weights.sh + pod/primary/supervisord.conf)
key-files:
  created:
    - .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md
  modified: []
decisions:
  - "Validation branch forked from gsd/06.8-stt-fix-candidate (Plan-01 source already committed there), not an uncommitted working tree as the plan text assumed"
  - "Validation image built via workflow_dispatch --ref validate/06.8-stt (build-primary-pod.yml only auto-fires on develop/v* — off-develop dispatch keeps develop clean)"
  - "Validation host: machine 43803 (spike-proven CDI 2x3090) after first host (machine 57777) failed with persistent ghcr.io pull EOF"
  - "A1 moot: upload-weights.sh bakes the REAL HF commit hash (edaa852…); speaches accepted the on-disk snapshot layout — no arbitrary hash risk"
metrics:
  vast_spend: ~$0.17 (machine 43803, 2x3090, ~26min) + ~$0 (machine 57777 destroyed on ghcr fault)
  tasks: 3
  completed: 2026-05-22
---

# Phase 06.8 Plan 02: STT fix — live-pod validation + promotion Summary

The CLAUDE.md anti-blind-commit gate satisfied: the STT fix (whisper HF-hub-cache
tarball + `HF_HUB_CACHE=/weights/whisper`) was proven on live hardware before any
shared-state mutation or develop commit.

## What happened

### Task 1 — candidate upload + validation image (off-develop)
- Forked `validate/06.8-stt` from `gsd/06.8-stt-fix-candidate` (Plan-01 commits c6d7c3b + 454b3f9).
- Candidate whisper tarball (HF-cache layout, real hash `edaa852…`) → MinIO candidate key `whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz`, sha `d8e8f6e489c9…`. Canonical untouched.
- Validation image built via `workflow_dispatch --ref validate/06.8-stt`: `ghcr.io/ifixtelecom/converseai-primary-pod:dev-validate-06.8-stt-150c040` @ `sha256:b57179d4…`.

### Task 2 — live STT validation (PASS)
Instance `37309227` on machine 43803 (2x3090, driver 580.95). All 7 checks green:
- transcription `POST /v1/audio/transcriptions` → **HTTP 200** `{"text":"you"…}` (pre-fix was 500 `CacheNotFound`).
- whisper loaded **on GPU** (GPU0 +4.0GB VRAM during transcription, util 38%).
- `/v1/models` lists whisper (local resolution), `/health` OK, silero v5 ONNX present, refs/main = real hash `edaa852…`.
Pod destroyed immediately after pass (~$0.17). (First host machine 57777 destroyed on persistent ghcr.io pull EOF — host defect.)

### Task 3 — promotion + propagate + develop commit (ordered, Convergent #1)
- (a) candidate → canonical `mc cp` (canonical sha now `d8e8f6e…`).
- (b) GH Secrets `PRIMARY_WHISPER_WEIGHTS_SHA256` + `WEIGHTS_WHISPER_SHA256` → `d8e8f6e…`.
- (c) dev-stack `ai-gateway-dev` `.env` whisper SHA → `d8e8f6e…`; gateway recreated, healthy, inspect-confirmed.
- (d) `gatewayctl primary force-down` (no pod running; clean).
- No SHA drift: canonical key == GH Secrets == gateway env == `d8e8f6e…`.
- develop: cherry-picked `5635502` (upload-weights.sh) + `999e290` (supervisord.conf HF_HUB_CACHE). The `pod/primary/**` change triggers `build-primary-pod.yml` → rebuilds `converseai-primary-pod:develop` (the tag the primary reconciler provisions) with the fix.

## Key outputs for Plan 03
- New whisper SHA: `d8e8f6e489c9eae7836a2faebc06af6fa6c631248fc1c644ca695d9bef3c34f0`
- Validated image digest: `sha256:b57179d43a89e3ad989add0abc71227c4979a371cff7ab91aa4f2ade4236ad8e`
- A1: real hash, zero-risk.
- develop commit (STT fix): `999e290` (+ `5635502`). Plan 03's live-UAT asserts STT 200 against the promoted `:develop` image once build-primary-pod completes.

## A2 early signal (relevant to Plan 03)
Filtered 2x3090 search (gateway gates: cuda_max_good≥12.6 AND inet≥300 AND reliability≥0.98 AND disk≥65) returned **0** offers at validation time — verified 2x3090 inventory was 2-3 deep and each failed at least one gate (reliability 0.977, old driver/cuda<12.6, or disk<65). Manual create (bypassing gates) on machine 43803 succeeded. Plan 03 must confirm whether the GATEWAY's filtered search path finds qualifying 2x3090 offers.
