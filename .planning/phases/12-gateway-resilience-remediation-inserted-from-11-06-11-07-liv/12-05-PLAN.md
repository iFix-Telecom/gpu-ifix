---
phase: 12-gateway-resilience-remediation
plan: 05
type: execute
wave: 4
depends_on: [12-04]
files_modified:
  - docs/CAP-01-saturation-decision.md
  - .planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-05-PROD-CHAOS-GATE.md
autonomous: false
requirements: [RES-13, CAP-01]
must_haves:
  truths:
    - "Prod gateway (n8n-ia-vm /opt/ai-gateway-prod) is deployed on the new image before the gate"
    - "The 11-07 chaos recipe re-runs in prod against a 1x5090 primary with the cheapest qualified chaos pod (D-17)"
    - "Zero connection-class 502 (upstream_unreachable) during T+0..end of the chaos window (D-18 HARD GATE)"
    - "503 sensitive_block remains expected and present for sensitive tenants (RES-08)"
    - "CAP-01 saturation decision doc exists analyzing the 11-06 baseline (chat p95 21.7s @ concurrency 50 on 1x5090)"
  artifacts:
    - path: "docs/CAP-01-saturation-decision.md"
      provides: "Saturation/capacity decision (queue-depth/concurrency-cap/shape) — doc-only D-19"
      contains: "21.7"
    - path: ".planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-05-PROD-CHAOS-GATE.md"
      provides: "Prod chaos gate sheet + signed zero-502 result + spend"
      contains: "upstream_unreachable"
  key_links:
    - from: "ops-claude Vast API DELETE (prod chaos)"
      to: "prod gateway audit_log upstream_unreachable count"
      via: "11-07 recipe re-run, zero-502 gate"
      pattern: "upstream_unreachable"
---

<objective>
Final prod gate (D-16): only AFTER the dev chaos passes, re-run the exact 11-07 chaos recipe against the PROD gateway (n8n-ia-vm, 1x5090 primary) and assert the D-18 hard gate — zero connection-class 502 `upstream_unreachable` in the kill window — the precise failure the 100x502 of 11-07 produced. Also ship the CAP-01 saturation decision doc (D-19 doc-only): analyze the 11-06 load-test baseline and write the queue-depth/concurrency-cap/shape decision (implementation, if any, becomes a future phase).

Purpose: This is the phase's acceptance gate. The prod re-run with zero-502 is the live proof that RES-11+RES-12+RES-13 made the gateway survive a primary death autonomously. CAP-01 closes the stretch requirement as a decision document.

Output: A signed prod chaos gate sheet (zero-502 attested) and `docs/CAP-01-saturation-decision.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-CONTEXT.md
@.planning/phases/11-prod-hardening/11-06-EVIDENCE.md
@.planning/phases/11-prod-hardening/11-07-EVIDENCE.md
@.planning/phases/11-prod-hardening/11-07-PLAN.md
@scripts/chaos/vast-delete.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the CAP-01 saturation decision doc (D-19, doc-only)</name>
  <read_first>
    - .planning/phases/11-prod-hardening/11-06-EVIDENCE.md (load-test saturation numbers — chat p95 21.7s @ concurrency 50 on 1x5090, billing-stop saga)
    - .planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-CONTEXT.md (D-19 scope: doc-only; out-of-scope: implementation)
    - .planning/REQUIREMENTS.md (LSH-01..05 load-shedding requirements for cross-reference)
  </read_first>
  <action>
    Create `docs/CAP-01-saturation-decision.md` analyzing the 11-06 load-test baseline. Include: (1) the measured saturation point — chat p95 21.7s @ concurrency 50 on 1x5090, with the supporting numbers from 11-06-EVIDENCE.md (VRAM, util, tok/s under load, where the knee is). (2) The decision: choose among queue-depth limiting, a concurrency cap, or a GPU shape upgrade — state the recommendation with rationale grounded in the 11-06 data (NOT speculative language — cite the evidence numbers). (3) The explicit scope fence: D-19 is doc-only this phase; if the chosen option requires code, it becomes a FUTURE phase (note the cross-reference to LSH-01..05 load-shedding which already covers inflight/P95/VRAM thresholds — the decision should say whether CAP-01 is subsumed by load-shedding config or needs a distinct mechanism). (4) Out-of-scope confirmation: no shape change to prod this phase (1x5090 cap $1.50 maintained per CONTEXT out-of-scope). The doc is the complete CAP-01 deliverable — acceptance is the document existing with a concrete decision, not an implementation.
  </action>
  <verify>
    <automated>test -f docs/CAP-01-saturation-decision.md && grep -c "21.7" docs/CAP-01-saturation-decision.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/CAP-01-saturation-decision.md` exists.
    - It cites the 11-06 baseline (chat p95 21.7s @ concurrency 50 on 1x5090) with supporting evidence numbers.
    - It states a concrete decision (queue-depth / concurrency-cap / shape) with rationale grounded in evidence, no speculative language.
    - It declares the doc-only scope fence (implementation -> future phase) and cross-references LSH-01..05.
    - It confirms no prod shape change this phase.
  </acceptance_criteria>
  <done>CAP-01 is delivered as a decision document analyzing the 11-06 saturation baseline with a concrete, evidence-grounded recommendation and an explicit doc-only scope fence.</done>
</task>

<task type="auto">
  <name>Task 2: Author the prod chaos gate sheet</name>
  <read_first>
    - .planning/phases/11-prod-hardening/11-07-PLAN.md (the exact chaos recipe)
    - .planning/phases/11-prod-hardening/11-07-EVIDENCE.md (the 100x502 baseline this re-run must beat, audit_log evidence pattern, --max-concurrency 20)
    - scripts/chaos/vast-delete.sh
    - .planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-04-DEV-CHAOS-UAT.md (dev rehearsal — mirror structure)
    - .planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-CONTEXT.md (D-16/D-17/D-18)
  </read_first>
  <action>
    Create `12-05-PROD-CHAOS-GATE.md` re-running the 11-07 chaos recipe against the PROD gateway (n8n-ia-vm, `/opt/ai-gateway-prod` operator-managed docker compose — prod deploy = pull new image + `up`, NOT Portainer). Numbered operator checklist with sign-off slots: (Pre-flight) Vast credit check FIRST; deploy the new image to prod (`docker compose pull && up` on n8n-ia-vm) + `docker inspect` digest confirm; dev chaos PASS is a prerequisite (D-16 — do not run prod until 12-04 signed). (Provision) provision the cheapest qualified chaos pod (D-17 — price first; kill is destructive, no premium shape needed). (Load) re-run the 11-07 load recipe at ~20 concurrency (NOT 50 — Pitfall 6: 1x5090 saturates at ~50; the chaos signal must stay clean; D-18 gates connection-class only). (Kill) `scripts/chaos/vast-delete.sh` during load. (Gate) THE HARD GATE: query `audit_log` for `error_code='upstream_unreachable'` over T+0..end of the chaos window — the count MUST be 0 (D-18). 503 `sensitive_block` for sensitive tenants is EXPECTED and present (RES-08). Degraded latency during failover is acceptable (no p95 gate). (Cleanup) destroy the chaos pod; verify count=0; record prod spend (target ~$0.80-1.50 total dev+prod per D-16). Include the explicit attestation pattern from 11-07 evidence (no raw keys/DSNs/tokens in the committed sheet). Each step gets a PASS/FAIL slot; the zero-502 gate gets a dedicated attestation line.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-05-PROD-CHAOS-GATE.md && grep -c "upstream_unreachable" .planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-05-PROD-CHAOS-GATE.md    </automated>
  </verify>
  <acceptance_criteria>
    - `12-05-PROD-CHAOS-GATE.md` exists targeting the PROD gateway (n8n-ia-vm /opt/ai-gateway-prod, operator-managed docker compose).
    - The sheet documents the prod image deploy + digest confirm as a pre-flight, with dev-chaos PASS (12-04) as a hard prerequisite (D-16).
    - The zero-502 hard gate is expressed as an audit_log `error_code='upstream_unreachable'` count == 0 over the kill window (D-18).
    - 503 `sensitive_block` is documented as expected/present (RES-08), and degraded latency is documented as acceptable (no p95 gate).
    - Price-first chaos pod selection (D-17) and ~20 concurrency (Pitfall 6) are specified; spend target ~$0.80-1.50 recorded.
  </acceptance_criteria>
  <done>The prod chaos gate sheet exists, re-runs the 11-07 recipe against prod, and defines the zero connection-class 502 hard gate read authoritatively from audit_log, ready for operator execution.</done>
  </task>

  <task type="checkpoint:human-verify" gate="blocking">
    <name>Task 3: Execute the prod chaos gate (blocking human-verify)</name>
    <action>Operator runs the live prod chaos kill per 12-05-PROD-CHAOS-GATE.md and signs the zero-502 hard gate. Blocking checkpoint: autonomous mode cannot satisfy it (real prod Vast spend + destructive kill).</action>
    <what-built>CAP-01 decision doc shipped (docs/CAP-01-saturation-decision.md). The prod chaos gate sheet (12-05-PROD-CHAOS-GATE.md) is ready. Dev chaos (12-04) is signed PASS.</what-built>
    <how-to-verify>
      Execute `12-05-PROD-CHAOS-GATE.md` end-to-end on the PROD gateway:
      1. Pre-flight: Vast credit check; deploy the new image to /opt/ai-gateway-prod (docker compose pull and up on n8n-ia-vm); confirm digest; confirm 12-04 dev chaos is signed PASS.
      2. Provision the cheapest qualified chaos pod (D-17); reach FSM Ready.
      3. Drive the 11-07 load recipe at ~20 concurrency including a sensitive-tenant stream.
      4. Kill the pod via scripts/chaos/vast-delete.sh during load.
      5. THE HARD GATE: query audit_log for error_code='upstream_unreachable' over T+0..end; the count MUST be 0 (D-18). Confirm 503 sensitive_block present for sensitive tenants (RES-08). Degraded latency OK.
      6. Cleanup: destroy chaos pod; verify Vast count=0; record prod spend.
      Any non-zero connection-class 502 -> FAIL -> capture evidence -> /gsd:plan-phase 12 --gaps.
    </how-to-verify>
    <resume-signal>Paste the signed gate result (zero-502 attestation + sensitive 503 confirmation + spend) into 12-05-PROD-CHAOS-GATE.md and type "prod-chaos approved" — or describe the failure for gap planning.</resume-signal>
  </task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude -> Vast API DELETE (prod) | destructive live kill of a prod-billed instance |
| prod gateway -> tier-1 (OpenRouter/OpenAI) | fallthrough sends prod-tenant payload to a 3rd party (non-sensitive only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Information Disclosure | sensitive prod-tenant data leaks to tier-1 during the prod kill | mitigate | gate confirms sensitive -> 503 sensitive_block present (RES-08); the dev rehearsal (12-04 S3) already proved the invariant before prod spend |
| T-12-15 | Denial of Service | orphaned prod chaos pod (runaway spend) | mitigate | cleanup verifies Vast count=0; BestEffortDestroy + manual DELETE fallback; spend recorded against the ~$0.80-1.50 budget |
| T-12-16 | Information Disclosure | secrets/DSNs leak into the committed gate evidence | mitigate | 11-07 attestation pattern: no raw keys/DSNs/tokens in the committed sheet; evidence is counts + attestations |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | Zero package installs; doc + UAT plan only |
</threat_model>

<verification>
- docs/CAP-01-saturation-decision.md exists with a concrete evidence-grounded decision (D-19).
- 12-05-PROD-CHAOS-GATE.md exists and is executed end-to-end.
- D-18 HARD GATE: zero connection-class 502 (upstream_unreachable) in the prod kill window, read from audit_log.
- 503 sensitive_block present (RES-08); degraded latency acceptable.
- Vast spend recorded; prod chaos pod count returns to 0.
</verification>

<success_criteria>
- RES-13 proven live in prod: zero-502 budget met under chaos (the 100x502 of 11-07 is closed).
- CAP-01 delivered as a decision doc (D-19 doc-only).
- D-16 sequence honored: prod gate runs only after the dev chaos PASS.
- D-17 price-first pod selection; spend within ~$0.80-1.50 total.
</success_criteria>

<output>
Create `.planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-05-SUMMARY.md` when done
</output>
