---
phase: 12-gateway-resilience-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/internal/upstreams/probe.go
  - gateway/internal/upstreams/health.go
  - gateway/internal/upstreams/loader.go
  - gateway/internal/upstreams/probe_test.go
  - gateway/internal/upstreams/health_test.go
  - gateway/internal/breaker/force_override.go
  - gateway/internal/breaker/breaker.go
  - gateway/internal/breaker/force_override_test.go
  - gateway/internal/obs/metrics.go
autonomous: true
requirements: [RES-12]
must_haves:
  truths:
    - "Probe tick resolves tier-0 per role via Resolve(role,0), honoring tier0Override"
    - "When tier0Override is active, the dead static tier-0 row is NOT probed and its breaker does not flap"
    - "/v1/health/upstreams reports the effective tier-0 with an override flag; replaced static row appears as overridden without dragging the aggregate to failed"
    - "A programmatic force-CLOSE breaker write exists and EffectiveState honors State==\"closed\" by short-circuiting to StateClosed"
    - "obs.DialFallthroughTotal and obs.PrimaryDeathDetectedTotal counters are defined and registered"
  artifacts:
    - path: "gateway/internal/upstreams/probe.go"
      provides: "doTick resolves tier-0 via Resolve(role,0)"
      contains: "Resolve("
    - path: "gateway/internal/upstreams/health.go"
      provides: "buildHealthResponse override-aware tier-0 + override flag payload"
      contains: "override"
    - path: "gateway/internal/breaker/force_override.go"
      provides: "force-CLOSE write + State==closed honored in read path"
      contains: "closed"
    - path: "gateway/internal/obs/metrics.go"
      provides: "DialFallthroughTotal + PrimaryDeathDetectedTotal counters"
      contains: "gateway_dial_fallthrough_total"
  key_links:
    - from: "gateway/internal/upstreams/probe.go"
      to: "gateway/internal/upstreams/loader.go Resolve(role,0)"
      via: "per-role tier-0 resolution in doTick"
      pattern: "Resolve\\("
    - from: "gateway/internal/breaker/breaker.go EffectiveState"
      to: "gateway/internal/breaker/force_override.go State==closed"
      via: "force-close honor in routing-state read path"
      pattern: "closed"
---

<objective>
RES-12: make the prober and the `/v1/health/upstreams` handler resolve tier-0 through the SAME override-honoring `Resolve(role, 0)` path the dispatcher already uses, so `local-*` breakers stop flapping in prod and the health endpoint tells the truth with a live pod. Also ship the two infrastructure primitives that Wave-2 plans depend on: a programmatic breaker force-CLOSE write (D-13, currently open-only — Pitfall 4) and the two new Prometheus counters (single-owner here to avoid Wave-2 file conflicts).

Purpose: This is the smallest fix and it unblocks the chaos-gate evidence surface (health endpoint + breaker truthfulness) that Plans 04/05 read. It also lands the force-close mechanism and metrics that Plans 02 and 03 call into.

Output: Prober/health tier-0 parity, override-flagged health payload, breaker force-close write + read-honor, two new obs counters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-CONTEXT.md
@.planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-PATTERNS.md
@.planning/seeds/SEED-012-prober-ignores-tier0-override.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase; use directly, no exploration. -->

loader.Resolve (gateway/internal/upstreams/loader.go:222) — the override-honoring path the dispatcher uses at dispatcher.go:200:
  func (l *Loader) Resolve(role string, tier int) (UpstreamConfig, bool)
  // Returns the emergency_pod_<role> synthetic config (live pod URL, IsEmergency=true) when tier0Override active.

loader.All (gateway/internal/upstreams/loader.go:358) — the raw snapshot the bug uses; returns []UpstreamConfig with Role/Tier/Name fields.

probe.go:156-188 doTick — CURRENT BUG at probe.go:160 (`all := p.loader.All()`); tier-1 gating map `tier0Closed[role]` at probe.go:170-182 gates external probes (D-15 must stay).

health.go:121-145 buildHealthResponse — CURRENT BUG at health.go:128 (`all := loader.All()`); already honors breaker force-override via `bs.EffectiveStateSnapshot()` at health.go:140; aggregate logic at health.go:108-115.

breaker force-override (gateway/internal/breaker/force_override.go):
  const ForceOverrideKeyPrefix = "gw:breaker:force:"
  func ForceOverrideKey(upstreamName string) string
  type ForceOverrideValue struct { State string; TTLSec int; SetBy string; SetAt time.Time }  // State today always "open"
  func ReadForceOverride(ctx, rdb, upstreamName) (state string, ttl, set bool, err error)
breaker.EffectiveState (gateway/internal/breaker/breaker.go:231) — routing-relevant state; today honors State=="open" via CheckForceOverride.

obs/metrics.go promauto pattern (metrics.go:17-95) — `promauto.NewCounterVec(prometheus.CounterOpts{Name,Help}, []string{...})`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RES-12 prober + health tier-0 parity via Resolve(role,0)</name>
  <read_first>
    - gateway/internal/upstreams/probe.go (full doTick 156-188 + tier-1 gating)
    - gateway/internal/upstreams/health.go (full buildHealthResponse + aggregate 108-145)
    - gateway/internal/upstreams/loader.go (Resolve L222-254, All L358, IsEmergency field on UpstreamConfig)
    - gateway/internal/upstreams/probe_test.go (existing table-driven style)
    - gateway/internal/upstreams/health_test.go (existing style)
    - .planning/seeds/SEED-012-prober-ignores-tier0-override.md (exact mechanism)
  </read_first>
  <behavior>
    - TestProbe_HonorsTier0Override: with tier0Override active for "llm", doTick probes the emergency_pod_llm URL (Resolve(role,0) result), NOT the static row URL; the static row's breaker is not touched/flapped.
    - TestProbe_TierGatingPreserved: with tier-0 effective state CLOSED, tier-1 (OpenRouter/OpenAI) external probes are gated OFF (D-15 intent); tier-1 probed only when effective tier-0 not closed.
    - TestHealth_OverrideEffectiveTier0: buildHealthResponse with override active reports role tier-0 as the effective (pod) upstream with override_active=true + override_source set; the replaced static row is reported as overridden/standby and does NOT push the aggregate status to "failed" when the effective tier-0 is healthy.
  </behavior>
  <action>
    Replace the `loader.All()` enumeration in BOTH probe.go doTick (line 160) AND health.go buildHealthResponse (line 128) with per-role tier-0 resolution through `p.loader.Resolve(role, 0)` / `loader.Resolve(role, 0)` over roles `{"llm","stt","tts","embed"}` (D-11). When `Resolve(role,0)` returns a config with `IsEmergency==true`, probe/report the EFFECTIVE tier-0 (the pod URL) and SKIP the underlying static tier-0 row for that role this tick (D-12) — do not enumerate the dead static row separately. Preserve tier-1 enumeration (the static tier-1 rows are still probed per the existing `tier0Closed[role]` gating logic at probe.go:170-182, but compute `tier0Closed[role]` from the RESOLVED tier-0 upstream name, not from the All() snapshot — D-15 gating intent unchanged). In health.go, add per-role `override_active bool` + `override_source string` fields to the upstreamStatus payload (field names at Claude's discretion D); when override is active, mark the replaced static row entry as `overridden`/`standby` and exclude it from the aggregate-status computation (health.go:108-115) so a healthy pod does not yield aggregate `failed` (D-14). Keep the existing breaker-side `bs.EffectiveStateSnapshot()` honor at health.go:140 unchanged. Add a `ResolveTier0Roles()` helper to loader.go if it reduces duplication (Claude's discretion) — Resolve already exists, do not reimplement resolution. Do NOT alter loader.Resolve's logic. Mirror Pitfall 3: BOTH call sites must change in this task; a partial fix leaves health 503 with a healthy pod.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/upstreams/ -run 'TestProbe_HonorsTier0Override|TestProbe_TierGatingPreserved|TestHealth_OverrideEffectiveTier0' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "loader.All()" gateway/internal/upstreams/probe.go` returns no tier-0 enumeration line in doTick (All may remain only if used for tier-1 rows, not tier-0).
    - `grep -c "Resolve(" gateway/internal/upstreams/probe.go` is ≥1 and references role-loop resolution.
    - health.go payload includes an override flag field (grep for the chosen field name returns ≥1).
    - The three named tests pass; `cd gateway && go test ./internal/upstreams/ -count=1` is green.
    - With override active, the static tier-0 row is excluded from aggregate-status failure (asserted by TestHealth_OverrideEffectiveTier0).
  </acceptance_criteria>
  <done>Prober and health both resolve tier-0 via Resolve(role,0); dead static tier-0 rows are not probed/flapped under override; health payload carries an override flag and the aggregate stays healthy with a live pod.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Breaker programmatic force-CLOSE (D-13) + force-OPEN reuse contract</name>
  <read_first>
    - gateway/internal/breaker/force_override.go (full — ForceOverrideKey, ForceOverrideValue, ReadForceOverride, CheckForceOverride read path)
    - gateway/internal/breaker/breaker.go (EffectiveState L221-251, EffectiveStateSnapshot L265-300, Set methods)
    - gateway/cmd/gatewayctl/breaker.go (lines ~150-170 — the existing open-only writer pattern)
    - gateway/internal/breaker/force_override_test.go (existing style)
  </read_first>
  <behavior>
    - TestForceOverride_CloseShortCircuits: a force-override with State=="closed" makes EffectiveState(name) return gobreaker.StateClosed regardless of observed/raw FSM state.
    - TestForceOverride_OpenStillWorks: State=="open" still returns gobreaker.StateOpen (no regression).
    - TestForceOverride_WriteCloseRoundTrips: a programmatic WriteForceOverride(name, "closed", ttl, setBy) followed by ReadForceOverride returns state=="closed", set==true.
    - TestForceOverride_DeleteClearsOverride: clearing the force key returns set==false and EffectiveState falls back to observed state.
  </behavior>
  <action>
    Add a programmatic force-override writer usable from the reconciler goroutine (Plan 02 calls it). Implement `WriteForceOverride(ctx, rdb, upstreamName, state, ttl, setBy)` in force_override.go that writes `ForceOverrideValue{State: state, TTLSec: int(ttl.Seconds()), SetBy: setBy, SetAt: time.Now().UTC()}` to `ForceOverrideKey(upstreamName)` with Redis EX=ttl — reusing the exact key + value shape the gatewayctl open-only writer uses (cmd/gatewayctl/breaker.go ~155-167). Accept state values "open" and "closed". Extend the READ-honor path so EffectiveState (breaker.go:231) and EffectiveStateSnapshot honor `State=="closed"` by SHORT-CIRCUITING to `gobreaker.StateClosed` (symmetric to today's `State=="open"` → StateOpen, force_override.go read path + breaker.go:231-251). Add a `ClearForceOverride(ctx, rdb, upstreamName)` helper that deletes the key (Plan 02 D-13 uses write-closed-then-let-TTL-expire OR clear — provide both; choose write-"closed" with short TTL as the markReady mechanism for symmetry with D-04 force-open per research Open Q1). Do NOT change the open path semantics. Per Pitfall 4: this is NEW code, not a call into an existing helper — the field was string-typed "for forward-compat" and you are implementing that forward-compat now.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/breaker/ -run 'TestForceOverride_CloseShortCircuits|TestForceOverride_OpenStillWorks|TestForceOverride_WriteCloseRoundTrips|TestForceOverride_DeleteClearsOverride' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "closed" gateway/internal/breaker/force_override.go` ≥1 (state value handled in write + read path).
    - `grep -n "func WriteForceOverride" gateway/internal/breaker/force_override.go` returns a definition.
    - EffectiveState returns StateClosed for a State=="closed" override (TestForceOverride_CloseShortCircuits passes).
    - State=="open" regression test passes (TestForceOverride_OpenStillWorks).
    - `cd gateway && go test ./internal/breaker/ -count=1` is green.
  </acceptance_criteria>
  <done>A programmatic force-CLOSE write exists, EffectiveState/EffectiveStateSnapshot honor State=="closed" → StateClosed, and force-OPEN behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: New obs counters (single-owner for Wave-2 consumers)</name>
  <read_first>
    - gateway/internal/obs/metrics.go (promauto.NewCounterVec pattern lines 17-95, package godoc on cardinality lines 2-3)
  </read_first>
  <action>
    Add two exported counters in obs/metrics.go using the existing `promauto.NewCounterVec` pattern (cardinality bounded per the package godoc): (1) `DialFallthroughTotal` with `Name: "gateway_dial_fallthrough_total"`, Help describing tier-0 dial failures that fell through to tier-1, labels `[]string{"role","outcome"}` where outcome ∈ {tier1_served, chain_exhausted, sensitive_blocked}; (2) `PrimaryDeathDetectedTotal` with `Name: "gateway_primary_death_detected_total"`, Help describing confirmed primary-pod deaths detected on the Ready tick, labels `[]string{"cause"}` where cause ∈ {billing_stopped, host_death, not_found}. Both are referenced by Plans 02 and 03; define them here so a single plan owns metrics.go and the two Wave-2 plans run in parallel without a file conflict. Do not wire increments here — the consuming code in Plan 02 (death detection) and Plan 03 (fallthrough) increments them.
  </action>
  <verify>
    <automated>cd gateway && go build ./internal/obs/ && go vet ./internal/obs/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gateway_dial_fallthrough_total" gateway/internal/obs/metrics.go` == 1.
    - `grep -c "gateway_primary_death_detected_total" gateway/internal/obs/metrics.go` == 1.
    - `cd gateway && go build ./internal/obs/` exits 0.
    - Labels are low-cardinality enums (role/outcome/cause), no unbounded label such as request_id or tenant.
  </acceptance_criteria>
  <done>Both counters are defined, registered via promauto, build-clean, and available as exported symbols for Plans 02 and 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/dashboard → /v1/health/upstreams | health payload is read by operators during incidents; a lying health surface mis-directs incident response |
| reconciler → Redis force-override key | force-close write is a privileged state mutation read by the request-path breaker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | breaker force-CLOSE Redis key | mitigate | TTL-bounded write (force-close expires); same `gw:breaker:force:*` sub-namespace as open-only writer; no new key namespace; read path validates JSON shape and falls back to observed state on malformed value |
| T-12-02 | Information Disclosure | /v1/health/upstreams override payload | mitigate | override_source reports a role/source label only (e.g. "primary pod"), NOT the raw pod URL or any secret; no tenant data in health payload |
| T-12-03 | Repudiation | force-close write provenance | accept | SetBy carries the writer identity (reconciler replica id); low risk — internal-only mutation, audited via existing breaker mirror |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | Zero package installs this phase (go.mod unchanged); RESEARCH Package Legitimacy Audit = not applicable |
</threat_model>

<verification>
- `cd gateway && go build ./... && go test ./internal/upstreams/ ./internal/breaker/ ./internal/obs/ -count=1` green.
- Prober no longer probes overridden static tier-0 rows (TestProbe_HonorsTier0Override).
- Health endpoint reports effective tier-0 + override flag without aggregate-failed (TestHealth_OverrideEffectiveTier0).
- Force-CLOSE short-circuits EffectiveState to StateClosed (TestForceOverride_CloseShortCircuits).
</verification>

<success_criteria>
- RES-12 parity: probe.go and health.go both resolve tier-0 via Resolve(role,0).
- D-12: dead static tier-0 rows not probed under active override.
- D-13 mechanism: programmatic force-CLOSE write + EffectiveState honor exists (consumed by Plan 02 markReady).
- D-14: health payload carries override flag, aggregate stays healthy with live pod.
- obs counters defined for Wave-2 consumers.
</success_criteria>

<output>
Create `.planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-01-SUMMARY.md` when done
</output>
