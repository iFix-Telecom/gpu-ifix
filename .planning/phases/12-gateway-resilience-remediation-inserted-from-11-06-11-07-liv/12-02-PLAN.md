---
phase: 12-gateway-resilience-remediation
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - gateway/internal/primary/reconciler.go
  - gateway/internal/primary/reconciler_test.go
  - gateway/internal/alert/alerter.go
  - gateway/internal/alert/severity.go
  - gateway/internal/alert/severity_test.go
autonomous: true
requirements: [RES-11]
must_haves:
  truths:
    - "evaluateReady polls Vast for the tracked instance every Ready tick"
    - "3-strike confirm on both ErrInstanceNotFound and IsTerminal() before declaring death (no false positive on transient exited)"
    - "On confirmed death: Ready→Draining (startDrain) → Destroying → Asleep + BestEffortDestroy, force-open local-llm/local-stt/local-tts breakers, publish distinct primary_death_confirmed event"
    - "Billing-stop death (intended_status=stopped) does NOT trigger re-provision; host-yank death lets the schedule loop re-provision"
    - "When activeInstanceID==0 the death poll reconciles the tracked id from the open primary_lifecycles row (D-05)"
    - "Alerter subscribes PrimaryEventsChannel and fans out a critical alert with a distinct billing-stop title"
  artifacts:
    - path: "gateway/internal/primary/reconciler.go"
      provides: "Ready-tick death poll + classifyDeath helper + D-05 trackedID repair"
      contains: "GetInstance"
    - path: "gateway/internal/alert/alerter.go"
      provides: "PrimaryEventsChannel subscription"
      contains: "PrimaryEventsChannel"
    - path: "gateway/internal/alert/severity.go"
      provides: "severityForPrimary classifier (critical, distinct billing-stop title)"
      contains: "severityForPrimary"
  key_links:
    - from: "gateway/internal/primary/reconciler.go evaluateReady"
      to: "vast.GetInstance + Instance.IsTerminal()"
      via: "death poll on Ready tick"
      pattern: "GetInstance"
    - from: "gateway/internal/primary/reconciler.go death-confirmed"
      to: "breaker.WriteForceOverride local-* open + publishPrimaryEvent"
      via: "deterministic breaker open + distinct alert event"
      pattern: "publishPrimaryEvent"
    - from: "gateway/internal/alert/alerter.go Subscribe"
      to: "redisx.PrimaryEventsChannel → severityForPrimary"
      via: "critical fan-out on primary death"
      pattern: "PrimaryEventsChannel"
---

<objective>
RES-11: make the primary reconciler detect a dead pod on the steady-state Ready tick (it currently never polls Vast — FINDING 2), port the proven 3-strike `IsTerminal()` + `ErrInstanceNotFound` confirm from `waitForReadyOrDestroy` into `evaluateReady`, advance Ready→Draining→Asleep with BestEffortDestroy, force-open the `local-*` breakers deterministically (D-04), and fire a distinct critical alert for billing-stop vs host-yank (D-03). Fix the empty-trackedID bug (D-05) FIRST in this plan — it is the load-bearing prerequisite that defeated the 11-07 reproduction (Pitfall 1).

Purpose: This is the core autonomous-failover fix. Without it, the prod gateway stays `ready` for 25+ minutes pointing at a dead address. With it, death is confirmed in ~15s, the breakers open, RES-13 (Plan 03) instantly falls traffic to tier-1, and the operator gets an actionable alert.

Output: Ready-tick death detection, FSM-driven breaker force-open, distinct billing-stop alert, D-05 trackedID repair, schedule-driven re-provision policy (no billing-stop loop).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-CONTEXT.md
@.planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-PATTERNS.md
@.planning/seeds/SEED-011-fsm-ready-with-stopped-instance.md
@.planning/phases/11-prod-hardening/11-07-EVIDENCE.md

<interfaces>
<!-- Contracts extracted from codebase + Plan 01 outputs. Use directly. -->

evaluateReady (reconciler.go:403-434) — TODAY: re-asserts tier-0 override for {llm,stt,tts} (Pitfall #11 re-assert, lines 415-422), checks PrimaryPodScheduleDisabled (424), checks IsInPeak→startDrain (427). NO GetInstance call. This is the additive target.

3-strike confirm to PORT (reconciler.go:945-1061, waitForReadyOrDestroy):
  const terminalConfirmStrikes = 3
  inst, err := r.deps.Vast.GetInstance(ctx, instanceID)
  // ErrInstanceNotFound → notFoundStrikes++ (confirm at 3); reset on transient GET err / healthy
  // inst.IsTerminal() → terminalStrikes++ (confirm at 3); reset on non-terminal obs
  // status_msg early-abort at reconciler.go:1031-1046

Instance (gateway/internal/emerg/vast/types.go:138-188):
  IsTerminal() bool  // "exited"|"unknown"|"offline" — the billing-stop shape (types.go:182)
  IntendedStatus string (types.go:141)  // "stopped" candidate billing-stop signal — A1 UNCONFIRMED, inspect live JSON
  ActualStatus string

startDrain (reconciler.go:501) — advances Ready→Draining AND RestoreTier0s the slots. Reason arg string.
evaluateDestroying (reconciler.go:476-499) — reads r.activeInstanceID.Load() (line 477), calls vastutil.BestEffortDestroy, closes lifecycle, Destroying→Asleep.
recoverOpenLifecycle (reconciler.go:1220-1291) — q.GetOpenPrimaryLifecycle(ctx) → open.VastInstanceID.Int64 → r.activeInstanceID.Store(...) at line 1291. The DB-row read to MIRROR for D-05.
publishPrimaryEvent (reconciler.go:1335) — best-effort publish wrapper (nil-Redis safe).

redisx.PrimaryEvent (gateway/internal/redisx/primary.go:74):
  type PrimaryEvent struct { Type, State string; LifecycleID int64; Reason string; SinceUnix int64; ReplicaID string }
redisx.PrimaryEventsChannel = "gw:primary:events" (primary.go:42).

breaker.WriteForceOverride(ctx, rdb, name, state, ttl, setBy) — NEW from Plan 01 (12-01 Task 2); state "open"|"closed".

alerter.go:139-143 — Subscribe lists ONLY BreakerEventsChannel(), ShedEventsChannel, EmergEventsChannel. PrimaryEventsChannel NOT subscribed (FINDING 1).
severity.go:55-66 severityFor — switch on channel; no PrimaryEventsChannel case today.
severity.go:161-187 severityForEmerg — the analog to mirror for severityForPrimary.
severity.go:200-208 channelsFor(SeverityCritical) → [chatwoot, clickup, brevo] (reuse, no change).

obs.PrimaryDeathDetectedTotal (from Plan 01) — CounterVec labels [cause]: billing_stopped|host_death|not_found.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-05 trackedID repair + Ready-tick death poll with 3-strike confirm (RES-11)</name>
  <read_first>
    - gateway/internal/primary/reconciler.go (evaluateReady 403-434; waitForReadyOrDestroy 931-1061 incl. status_msg abort 1031-1046; startDrain 501; evaluateDestroying 476-499; recoverOpenLifecycle 1220-1291)
    - gateway/internal/emerg/vast/types.go (Instance 138-188, IsTerminal 182, IntendedStatus 141)
    - gateway/internal/primary/reconciler_test.go (existing table-driven + fakeVast harness; alwaysInPeakRule helper)
    - .planning/seeds/SEED-011-fsm-ready-with-stopped-instance.md (timeline + display bug)
    - .planning/phases/11-prod-hardening/11-07-EVIDENCE.md (empty trackedID finding)
  </read_first>
  <behavior>
    - TestEvaluateReady_EmptyTrackedIDReconciles: when r.activeInstanceID==0 but an open primary_lifecycles row exists, the death poll reads open.VastInstanceID.Int64, repairs r.activeInstanceID, and polls that id (no silent no-op).
    - TestEvaluateReady_DeathDetection: a tracked instance returning IsTerminal()==true for 3 consecutive ticks → startDrain called with a death reason; force-open written for local-llm/local-stt/local-tts; primary_death_confirmed event published; PrimaryDeathDetectedTotal incremented.
    - TestEvaluateReady_TransientExitedDoesNotDrain: IsTerminal()==true for 1-2 ticks then a non-terminal observation → strike counter resets, FSM stays Ready, no startDrain.
    - TestEvaluateReady_NotFound3StrikeDrains: ErrInstanceNotFound for 3 consecutive ticks → death-confirmed (cause=not_found/host_death) → startDrain.
    - TestEvaluateReady_HealthyNoop: a healthy (non-terminal, found) instance → no strike accumulation, no drain.
  </behavior>
  <action>
    In `evaluateReady` (reconciler.go:403), AFTER the existing Pitfall #11 re-assert block (lines 415-422) and BEFORE / alongside the schedule checks, add a Vast death poll. First, D-05 repair: read `instanceID := r.activeInstanceID.Load()`; if it is 0 AND `r.activePodURLs.Load() != nil` (a pod is routing but the id was lost on a force-up), reconcile by reading the open lifecycle row exactly as recoverOpenLifecycle does — `q.GetOpenPrimaryLifecycle(ctx)` → `open.VastInstanceID.Int64` → `r.activeInstanceID.Store(...)` — then use that id (Pitfall 1; also repair the force-up write path at reconciler.go:891 region so future force-ups always Store the id — do BOTH per research Open Q3). Then port the 3-strike confirm from waitForReadyOrDestroy (reconciler.go:945-1061): call `r.deps.Vast.GetInstance(ctx, instanceID)`; on `errors.Is(err, vast.ErrInstanceNotFound)` increment a notFound strike counter, reset on transient GET error or healthy; on `inst.IsTerminal()` increment a terminal strike counter, reset on non-terminal. Use `const terminalConfirmStrikes = 3` (same value, D-02). The strike counters must persist across ticks — store them on the Reconciler struct (e.g. atomic/guarded fields), NOT as function locals, since each tick is a separate call (unlike the in-loop waitForReadyOrDestroy). Extract a shared `classifyDeath` helper (Claude's discretion A; research recommends extraction) callable from evaluateReady; the recover/wait paths may continue to use their inline copies or be refactored to call it. On confirmed death do NOT yet wire the drain/alert here — that is Task 2 — but the poll + strike + classification (return a struct/enum: {dead bool, cause string}) must be complete and tested. Reset both strike counters whenever the FSM leaves Ready (so a re-provisioned pod starts clean). Honor the existing PrimaryPodScheduleDisabled gate semantics — the death poll runs regardless of DISABLED (a Ready pod under DISABLED is just as mortal, mirroring the Pitfall #11 re-assert which also runs under DISABLED).
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/primary/ -run 'TestEvaluateReady_EmptyTrackedIDReconciles|TestEvaluateReady_DeathDetection|TestEvaluateReady_TransientExitedDoesNotDrain|TestEvaluateReady_NotFound3StrikeDrains|TestEvaluateReady_HealthyNoop' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GetInstance" gateway/internal/primary/reconciler.go` shows a call inside evaluateReady's call graph (not only in waitForReadyOrDestroy/recover).
    - Strike counters survive across ticks (struct fields, not locals) — asserted by TestEvaluateReady_TransientExitedDoesNotDrain over multiple evaluateReady calls.
    - D-05: empty activeInstanceID with an open row reconciles (TestEvaluateReady_EmptyTrackedIDReconciles passes).
    - 3-strike confirm value is 3 (`grep -c "terminalConfirmStrikes = 3" gateway/internal/primary/reconciler.go` ≥1).
    - `cd gateway && go test ./internal/primary/ -count=1` green.
  </acceptance_criteria>
  <done>evaluateReady polls Vast every Ready tick, confirms death via 3-strike on both signals, classifies cause, and reconciles an empty trackedID from the open lifecycle row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Death-confirmed path — startDrain + force-open breakers + billing-stop policy + distinct alert event (D-01/D-03/D-04)</name>
  <read_first>
    - gateway/internal/primary/reconciler.go (startDrain 501, evaluateDestroying 476, publishPrimaryEvent 1335, status_msg abort 1031-1046)
    - gateway/internal/redisx/primary.go (PrimaryEvent struct 74, PrimaryEventsChannel 42)
    - gateway/internal/breaker/force_override.go (WriteForceOverride from Plan 01 12-01 Task 2)
    - gateway/internal/obs/metrics.go (PrimaryDeathDetectedTotal from Plan 01)
    - gateway/internal/primary/reconciler_test.go
  </read_first>
  <behavior>
    - TestDeath_HostYankDrainsAndForceOpens: confirmed host-yank death → startDrain(reason "primary_instance_dead"/host_death) called; WriteForceOverride("local-llm"/"local-stt"/"local-tts","open",...) called for all three; primary_death_confirmed event published with Reason="host_death"; PrimaryDeathDetectedTotal{cause=host_death} incremented.
    - TestDeath_BillingStopNoReprovision: confirmed billing-stop death (IntendedStatus=="stopped") → startDrain + force-open + event with Reason="billing_stopped"; and the death path does NOT trigger a re-provision call (no provisionLifecycle invocation attributable to this death). Host-yank, by contrast, leaves re-provision to the existing schedule loop (assert no explicit re-provision suppression for host_death).
    - TestDeath_BreakersForceOpenedBeforeDestroy: force-open is written at drain-start, before BestEffortDestroy, eliminating the dead-address window.
  </behavior>
  <action>
    In `evaluateReady`, on a confirmed-death classification from Task 1's helper: (1) call `r.startDrain(ctx, reason, log)` where reason encodes the cause (e.g. "primary_instance_dead_host_death" / "primary_instance_dead_billing_stopped") — startDrain advances Ready→Draining and RestoreTier0s the slots; the existing Draining→Destroying→evaluateDestroying path then reaches `vastutil.BestEffortDestroy` for free (D-01). (2) D-04: deterministically force-open the local breakers by calling `breaker.WriteForceOverride(ctx, rdb, name, "open", ttl, setBy)` for `local-llm`, `local-stt`, `local-tts` BEFORE the destroy completes, so requests stop hitting the dead address while observation would otherwise accumulate. Use a TTL long enough to cover the drain+destroy window; the breakers re-close via D-13 force-close on the next markReady (Plan 01 Task 2 mechanism) when a new pod goes Ready. (3) D-01 billing-stop policy: distinguish billing-stop (IntendedStatus=="stopped", or the status_msg/balance signal — A1 UNCONFIRMED, leave a clearly-commented classification point and inspect a live billing-stopped instance JSON during execution before locking the exact field; default to IntendedStatus=="stopped" as the billing signal) from host-yank (404 / IsTerminal without stopped-intent). For billing-stop, do NOT enter a re-provision loop (Pitfall 5 — no credit means every bid fails and burns nothing-but-attempts); rely on the existing cooldown/abort gate pattern at reconciler.go:1031-1046 as the model. For host-yank, take NO special action — the existing schedule loop re-provisions naturally inside the peak window (D-01). (4) D-03: publish a distinct event via `r.publishPrimaryEvent(ctx, redisx.PrimaryEvent{Type:"primary_death_confirmed", State:"draining", LifecycleID:r.activeLifecycleID.Load(), Reason: reason /* "billing_stopped" | "host_death" */, SinceUnix:time.Now().Unix(), ReplicaID:r.deps.ReplicaID}, log)`. (5) Increment `obs.PrimaryDeathDetectedTotal.WithLabelValues(cause).Inc()`. Do NOT add a new FSM state (anti-pattern — reuse Ready→Draining→Asleep).
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/primary/ -run 'TestDeath_HostYankDrainsAndForceOpens|TestDeath_BillingStopNoReprovision|TestDeath_BreakersForceOpenedBeforeDestroy' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WriteForceOverride" gateway/internal/primary/reconciler.go` ≥1 and targets local-llm/local-stt/local-tts.
    - `grep -c "primary_death_confirmed" gateway/internal/primary/reconciler.go` ≥1.
    - Billing-stop classified by IntendedStatus=="stopped" (or documented live-inspected field) and does NOT re-provision (TestDeath_BillingStopNoReprovision passes).
    - No new FSM state added (`grep -c "StateDead\|StateFailed" gateway/internal/primary/fsm.go` == 0 for any new state).
    - `cd gateway && go test ./internal/primary/ -count=1` green.
  </acceptance_criteria>
  <done>Confirmed death drains the FSM, force-opens local-* breakers before destroy, suppresses billing-stop re-provision, and publishes a distinct cause-tagged death event.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the Alerter to consume PrimaryEvents — severityForPrimary critical fan-out (D-03, FINDING 1)</name>
  <read_first>
    - gateway/internal/alert/alerter.go (Subscribe block 139-143)
    - gateway/internal/alert/severity.go (severityFor switch 55-66; severityForEmerg analog 161-187; channelsFor 200-208)
    - gateway/internal/alert/severity_test.go (existing classifier test style)
    - gateway/internal/redisx/primary.go (PrimaryEvent struct, PrimaryEventsChannel)
  </read_first>
  <behavior>
    - TestSeverityForPrimary_BillingStopCritical: a primary_death_confirmed event with Reason="billing_stopped" → SeverityCritical with Title containing "Vast account sem crédito" (operator-actionable distinct title).
    - TestSeverityForPrimary_HostDeathCritical: Reason="host_death" → SeverityCritical with a distinct host-death title (e.g. "Primary pod morto").
    - TestSeverityForPrimary_Malformed: malformed payload → returns an error (not a panic), mirroring severityForEmerg.
    - TestSeverityFor_RoutesPrimaryChannel: severityFor(PrimaryEventsChannel, payload) dispatches to severityForPrimary.
  </behavior>
  <action>
    Per FINDING 1 (RESEARCH A4 was wrong — PrimaryEvents is consumed by nobody today): (1) add `redisx.PrimaryEventsChannel` to the `a.rdb.Subscribe(ctx, ...)` list in alerter.go:139-143 (the constant exists at redisx/primary.go:42). (2) add `case redisx.PrimaryEventsChannel: return severityForPrimary(payload)` to the `severityFor` switch (severity.go:55-66). (3) write `severityForPrimary(payload []byte) (Severity, Message, error)` mirroring `severityForEmerg` (severity.go:161): json.Unmarshal into `redisx.PrimaryEvent`; when `ev.Type=="primary_death_confirmed"` return `SeverityCritical` with Title chosen by Reason — `"billing_stopped"` → "Vast account sem crédito — primary billing-stopped" (D-03 distinct, operator-actionable: action is ADD CREDIT, not debug pod), default/`"host_death"` → "Primary pod morto (host-yank/404)"; set a Fingerprint like `"primary:death:"+ev.Reason` for dedup; non-death event types return SeverityInfo. `channelsFor(SeverityCritical)` already returns [chatwoot, clickup, brevo] (severity.go:200-208) — reuse unchanged. Do NOT add new alerting infra (no new channels, no new dedup store). Ensure the alert body carries NO tenant payload / no secrets (V7 — only the cause + lifecycle id).
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/alert/ -run 'TestSeverityForPrimary_BillingStopCritical|TestSeverityForPrimary_HostDeathCritical|TestSeverityForPrimary_Malformed|TestSeverityFor_RoutesPrimaryChannel' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PrimaryEventsChannel" gateway/internal/alert/alerter.go` ≥1 (added to Subscribe).
    - `grep -c "func severityForPrimary" gateway/internal/alert/severity.go` == 1.
    - `grep -c "Vast account sem crédito" gateway/internal/alert/severity.go` == 1 (distinct billing-stop title).
    - billing_stopped and host_death both map to SeverityCritical (the two named tests pass).
    - `cd gateway && go test ./internal/alert/ -count=1` green.
  </acceptance_criteria>
  <done>The Alerter subscribes PrimaryEventsChannel, severityForPrimary maps death events to critical with distinct billing-stop vs host-death titles, and the existing critical fan-out (Chatwoot+ClickUp+Brevo) delivers them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vast API → reconciler death poll | untrusted external status drives an FSM transition + spend (destroy) |
| reconciler → Redis PrimaryEvents → Alerter | death event triggers external alert fan-out (Chatwoot/ClickUp/Brevo) |
| reconciler → schedule re-provision | a death can trigger a paid Vast bid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Denial of Service (self-inflicted financial) | billing-stop re-provision loop | mitigate | D-01: billing-stop (IntendedStatus=="stopped") does NOT re-provision; distinct alert prompts operator to add credit (Pitfall 5); TestDeath_BillingStopNoReprovision is the gate |
| T-12-05 | Tampering | false-positive death on transient Vast `exited` | mitigate | 3-strike confirm (D-02) ~15s latency; strike resets on any non-terminal obs; TestEvaluateReady_TransientExitedDoesNotDrain is the gate |
| T-12-06 | Information Disclosure | alert body leaks tenant payload/secrets | mitigate | severityForPrimary body carries only cause + lifecycle id; no request body, no key; reuses dedup'd alert path |
| T-12-07 | Spoofing | forged PrimaryEvent on Redis channel | accept | internal Redis (no external pub access); same trust model as existing EmergEvents/BreakerEvents consumers |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | Zero package installs (go.mod unchanged) |
</threat_model>

<verification>
- `cd gateway && go build ./... && go test ./internal/primary/ ./internal/alert/ -count=1` green.
- Ready-tick death detection confirmed via 3-strike (RES-11).
- Billing-stop does not re-provision; host-yank lets schedule re-provision (D-01).
- local-* breakers force-open on death before destroy (D-04).
- Critical billing-stop alert delivered via existing fan-out (D-03).
- Manual-only (Plan 04/05 UAT): live alert delivery + gatewayctl primary state coherence (D-05).
</verification>

<success_criteria>
- RES-11 fully wired: poll + 3-strike confirm + drain + destroy + force-open + alert.
- D-05 trackedID repair lands in the same plan (prerequisite, not cosmetic).
- D-01/D-03/D-04 satisfied with zero new infra.
- No new FSM state; reuses Ready→Draining→Asleep.
</success_criteria>

<output>
Create `.planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-02-SUMMARY.md` when done
</output>
