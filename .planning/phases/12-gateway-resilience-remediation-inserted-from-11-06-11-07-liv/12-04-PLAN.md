---
phase: 12-gateway-resilience-remediation
plan: 04
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - .planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-04-DEV-CHAOS-UAT.md
autonomous: false
requirements: [RES-11, RES-12, RES-13]
must_haves:
  truths:
    - "Dev gateway is running the new image (Plans 01-03 merged + Portainer redeploy)"
    - "A live cheap Vast pod is provisioned (price-first selection, D-17) and reaches FSM Ready on dev"
    - "Killing the pod via Vast API DELETE during load triggers Ready->Draining->Asleep death detection (RES-11)"
    - "During the kill window, normal-tenant traffic falls through to tier-1 with zero connection-class 502 (RES-13)"
    - "Sensitive-tenant traffic returns 503 sensitive_block during the kill (RES-08/D-10)"
    - "/v1/health/upstreams and breaker state tell the truth before/during/after the kill (RES-12)"
  artifacts:
    - path: ".planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-04-DEV-CHAOS-UAT.md"
      provides: "Dev chaos UAT scenario sheet + signed results + spend"
      contains: "PASS"
  key_links:
    - from: "ops-claude Vast API DELETE"
      to: "dev gateway FSM death detection + dispatcher fallthrough"
      via: "scripts/chaos/vast-delete.sh on dev"
      pattern: "vast-delete"
---

<objective>
Dev-first chaos validation (D-16): with Plans 01-03 merged and deployed to the dev gateway, provision the cheapest qualified Vast pod (D-17 — price is the 1st selection factor), drive moderate load, kill the pod via Vast API DELETE, and verify all three fixes work TOGETHER on a cheap dev kill BEFORE spending on the prod gate.

Purpose: Catch integration bugs (death poll + force-open + fallthrough interplay) on a cheap dev pod before the prod gate. The dev kill is the first time RES-11+RES-12+RES-13 run against a real Vast death end-to-end.

Output: A signed dev chaos UAT sheet with PASS/FAIL per scenario, evidence from audit_log + health endpoint + logs, and Vast spend recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-CONTEXT.md
@.planning/phases/11-prod-hardening/11-07-EVIDENCE.md
@.planning/phases/11-prod-hardening/11-07-PLAN.md
@scripts/chaos/vast-delete.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the dev chaos UAT scenario sheet</name>
  <read_first>
    - .planning/phases/11-prod-hardening/11-07-PLAN.md (the chaos recipe to mirror)
    - .planning/phases/11-prod-hardening/11-07-EVIDENCE.md (deviations, --max-concurrency 20, audit_log evidence pattern)
    - scripts/chaos/vast-delete.sh (the kill mechanism + Vast-list fallback)
    - .planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-CONTEXT.md (D-16/D-17/D-18 gates)
  </read_first>
  <action>
    Create `12-04-DEV-CHAOS-UAT.md` adapting the 11-07 chaos recipe for the DEV gateway (`ai-gateway-dev` on vps-ifix-vm, Portainer GitOps stack). Document, as a numbered operator checklist with PASS/FAIL sign-off slots, these sections: (Pre-flight) confirm Vast credit BEFORE provisioning (per MEMORY: resilience recovery starts with a credit check); confirm dev gateway is on the new image (Plans 01-03 merged to develop + webhook redeploy + `docker inspect` digest check). (Provision) provision the CHEAPEST qualified pod (D-17 — price first; allowlist/blocklist still apply but cost decides); wait for FSM Ready and check `gatewayctl primary state` shows pod_url + lifecycle_id coherent (D-05 check). (Load) drive load at moderate concurrency (~20, mirroring 11-07's deliberate drop to keep the chaos signal clean — Pitfall 6) with a mix including one sensitive-tenant stream. (Kill) run `scripts/chaos/vast-delete.sh` against the dev pod's Vast instance id during load. Define the scenarios with their evidence sources:
      S1 RES-11 death detection: after the DELETE, logs show 3-strike confirm -> startDrain -> force-open local-* -> Destroying -> Asleep; `gateway_primary_death_detected_total` increments; distinct alert fired (billing-stop title if billing-stop simulated, host-death otherwise). Evidence: gateway logs + Prometheus + Redis primary event.
      S2 RES-13 zero-502: during T+0..end, normal-tenant requests return 200 via tier-1 (or degraded latency), ZERO `upstream_unreachable` connection-class 502. Evidence: `audit_log.error_code` query (system of record — NOT breaker state), `gateway_dial_fallthrough_total{outcome=tier1_served}`.
      S3 RES-08/D-10 sensitive: a sensitive-tenant request during the kill returns 503 `upstream_unavailable_for_sensitive_tenant`, never routed to tier-1. Evidence: audit_log + response body.
      S4 RES-12 health truth: `/v1/health/upstreams` reports effective tier-0 + override flag while the pod is Ready, does not 503 with a healthy pod, and `local-*` breakers do not flap pre-kill; AFTER recovery provisions a new pod, the local-* breakers are force-closed (D-13) and traffic returns to the live pod. Evidence: curl health endpoint + breaker snapshot.
      S5 cleanup: pod destroyed (Vast instance count returns to 0); record total dev spend.
    Each scenario gets an explicit PASS/FAIL marker + evidence-paste slot. Include the exact audit_log query (filter `error_code='upstream_unreachable'` over the kill window) so the operator reads connection-class 502s authoritatively, not aggregate latency (Pitfall 6 — D-18 gates connection-class only; degraded p95 is OK).
  </action>
  <verify>
    <!-- Existence check ONLY. The sheet ships with template placeholders (S1-S5 with blank PASS/FAIL slots), so a content grep on "PASS" passes trivially and proves nothing. The REAL gate is Task 2 (checkpoint:human-verify, blocking): the operator runs the live kill and signs the results. This automated check only confirms the sheet was authored with the five scenarios. -->
    <automated>test -f .planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-04-DEV-CHAOS-UAT.md && grep -Ec '^#+ *S[1-5]\b|S[1-5][ :)-]' .planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-04-DEV-CHAOS-UAT.md | awk '$1>=5{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `12-04-DEV-CHAOS-UAT.md` exists with the 5 scenarios (S1-S5) each carrying a PASS/FAIL slot.
    - The sheet names the dev gateway target (`ai-gateway-dev`, vps-ifix-vm) and the price-first pod selection (D-17).
    - The audit_log `upstream_unreachable` query is documented as the authoritative zero-502 evidence (not breaker state, not aggregate latency).
    - The sensitive-tenant 503 scenario (S3) is present and uses a sensitive tenant key (telefonia/cobrancas).
    - Pre-flight includes a Vast credit check before any provisioning.
    - The automated verify is an existence + scenario-count check only; signed PASS results are gated by Task 2 (the blocking human-verify), NOT by this autonomous task.
  </acceptance_criteria>
  <done>The dev chaos UAT sheet exists, mirrors the 11-07 recipe against the dev gateway, defines RES-11/RES-12/RES-13 + sensitive scenarios with authoritative audit_log evidence, and is ready for the operator to execute. The real PASS/FAIL gate is Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the dev chaos UAT (blocking human-verify)</name>
  <action>Operator runs the live dev chaos kill per 12-04-DEV-CHAOS-UAT.md and signs S1-S5. Blocking checkpoint: autonomous mode cannot satisfy it (real Vast spend + destructive kill). This is the authoritative gate for the dev chaos rehearsal — Task 1's automated check only confirmed the sheet exists.</action>
  <what-built>Plans 01-03 (RES-11 death detection, RES-12 prober/health parity + D-13 markReady force-close, RES-13 dial-failure fallthrough) merged to develop and deployed to the dev gateway. The dev chaos UAT sheet (12-04-DEV-CHAOS-UAT.md) is ready.</what-built>
  <how-to-verify>
    Execute `12-04-DEV-CHAOS-UAT.md` end-to-end on the DEV gateway:
    1. Pre-flight: check Vast credit; confirm `ai-gateway-dev` is on the new image digest.
    2. Provision the cheapest qualified pod (D-17); wait for FSM Ready; verify `gatewayctl primary state` shows coherent pod_url + lifecycle_id (D-05).
    3. Drive load at ~20 concurrency including one sensitive-tenant stream.
    4. Kill the pod via `scripts/chaos/vast-delete.sh` during load.
    5. Sign S1 (death detection Ready->Draining->Asleep + force-open + distinct alert), S2 (zero connection-class 502 via audit_log query), S3 (sensitive 503 never tier-1), S4 (health truth + no breaker flap pre-kill + D-13 force-close on recovery), S5 (cleanup, count=0, record spend).
    Any FAIL -> stop, capture evidence, run `/gsd:plan-phase 12 --gaps`.
  </how-to-verify>
  <resume-signal>Paste the signed S1-S5 results (PASS/FAIL each + spend) into 12-04-DEV-CHAOS-UAT.md and type "dev-chaos approved" — or describe the failure for gap planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude -> Vast API DELETE | destructive live kill of a billed instance |
| dev gateway -> tier-1 (OpenRouter/OpenAI) | fallthrough sends dev-tenant payload to a 3rd party (non-sensitive only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Denial of Service | orphaned Vast pod after the kill (runaway spend) | mitigate | S5 cleanup verifies Vast instance count returns to 0; BestEffortDestroy + manual DELETE fallback in vast-delete.sh; record spend |
| T-12-13 | Information Disclosure | sensitive-tenant data leaks to tier-1 during the dev kill | mitigate | S3 asserts sensitive -> 503, never tier-1; uses a sensitive tenant key to prove the invariant live |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | Zero package installs; doc-only plan |
</threat_model>

<verification>
- Dev chaos UAT sheet exists and is executed end-to-end.
- S1-S5 all signed PASS (any FAIL routes to `/gsd:plan-phase 12 --gaps`).
- Zero connection-class 502 in audit_log over the kill window (RES-13/D-18 dev rehearsal).
- Vast spend recorded; pod count returns to 0.
</verification>

<success_criteria>
- D-16 dev-first gate passed: all three fixes validated together on a cheap dev kill before the prod gate.
- D-17 price-first pod selection used.
- D-18 zero-502 budget met on dev (rehearsal for prod).
- Sensitive 503 invariant (RES-08/D-10) proven live.
</success_criteria>

<output>
Create `.planning/phases/12-gateway-resilience-remediation-inserted-from-11-06-11-07-liv/12-04-SUMMARY.md` when done
</output>
</content>
