---
phase: 06.6
plan: 03
type: execute
wave: 2
depends_on: [06.6-01, 06.6-02]
files_modified:
  - gateway/internal/config/config.go
  - gateway/internal/config/config_test.go
autonomous: true
requirements: []
tags: [config, wave-2, env-vars, primary-pod, schedule]

must_haves:
  truths:
    - "gateway/internal/config/config.go expõe 23 novos campos PrimaryXxx + PrimaryPodScheduleXxx (D-08.1 + Decisions Resolved #5 RESEARCH.md:656-681 + ProvisionFailureCooldownSeconds parity emerg per checker revision)"
    - "Defaults dos 23 fields correspondem exatamente aos valores LOCKED em 06.6-WAVE0-GATES.md (Decisions 1, 3, 4, 6) — image SHA pins de Decision 1 (per D-02 upstream tag-pinned policy + D-01 4-service stack), Jinja key/sha256 de Decision 3, Disk implicito 50GB do build, coldstart budget 2400s de Decision 6, cooldown 300s mirror emerg's 60s scaled-up para schedule cadence"
    - "PRIMARY_WHISPER_WEIGHTS_SHA256 + PRIMARY_BGEM3_WEIGHTS_SHA256 NÃO têm defaults shipped (operator-set per RESEARCH.md:683 mesma política que WEIGHTS_QWEN_SHA256 do emerg)"
    - "PrimaryQwenJinjaKey + PrimaryQwenJinjaSHA256 default empty SE WAVE0-GATES Decision 3 = B1 (GGUF-embedded); ELSE default = MinIO key + sha256 do upload Wave 0 Task 2 (B2 fallback)"
    - "PrimaryLlamaArgs é csvOr default nil — empty CSV → lifecycle.go usa hardcoded primaryLlamaArgsDefault const (mesma convention que EmergencyLlamaArgs Phase 6)"
    - "PrimaryProvisionFailureCooldownSeconds: default 300 (5min); mirror emerg's ProvisionFailureCooldownSeconds (default 60s, line 141 of existing config.go) mas scaled-up because primary cadence is hours-scale (schedule windows) vs emerg minutes-scale (event-driven). Plan 06 reconciler.evaluateAsleep enforces: refuse re-provision if (now - lastFailureAt) < PrimaryProvisionFailureCooldownSeconds"
    - "Helpers envOr/atoiOr/csvOr/boolOr/floatOr existentes em config.go:340-396 são REUTILIZADOS — zero novo helper criado"
  artifacts:
    - path: "gateway/internal/config/config.go"
      provides: "23 new env var fields under Phase 6.6 header block"
      contains: "PrimaryTemplateImage|PrimarySpeachesImage|PrimaryInfinityImage|PrimaryDCGMImage|PrimaryQwenWeightsKey|PrimaryQwenWeightsSHA256|PrimaryQwenJinjaKey|PrimaryQwenJinjaSHA256|PrimaryLlamaArgs|PrimaryWhisperWeightsKey|PrimaryWhisperWeightsSHA256|PrimaryBGEM3WeightsKey|PrimaryBGEM3WeightsSHA256|PrimaryVastPriceCapDPH|PrimaryProvisionColdStartBudgetSeconds|PrimaryProvisionFailureCooldownSeconds|MonthlyPrimaryBudgetBRL|PrimaryPodScheduleTimezone|PrimaryPodScheduleUpHour|PrimaryPodScheduleDownHour|PrimaryPodScheduleDays|PrimaryPodScheduleGraceRampDownSeconds|PrimaryPodScheduleDisabled"
    - path: "gateway/internal/config/config_test.go"
      provides: "Coverage for 23 new fields — defaults + override-via-env"
      contains: "TestConfig_PrimaryPod_DefaultsLoaded|TestConfig_PrimaryPod_EnvOverride|TestConfig_PrimaryPod_FailureCooldownDefault"
  key_links:
    - from: "config.go Load() block (new lines)"
      to: "primary/lifecycle.go buildCreateRequest (Wave 2 Plan 06.6-04 consumer)"
      via: "cfg.PrimaryXxx field reads"
      pattern: "cfg\\.Primary[A-Z]"
    - from: "config.go PrimaryProvisionFailureCooldownSeconds field"
      to: "primary/reconciler.go evaluateAsleep cooldown check (Plan 06.6-06 consumer)"
      via: "cfg.PrimaryProvisionFailureCooldownSeconds read"
      pattern: "cfg\\.PrimaryProvisionFailureCooldownSeconds"

threat_refs: [T-06.6-03, T-06.6-04, T-06.6-SC]
---

<objective>
Adicionar 23 novos campos de configuração ao gateway/internal/config/config.go correspondentes às decisões D-08.1 (env vars schedule) + Decisions Resolved #5 (env var naming) + Plan 06 threat_model T-06.6-04 mitigation (PrimaryProvisionFailureCooldownSeconds — checker revision). Os defaults são determinados por 06.6-WAVE0-GATES.md (Wave 0) — image SHA pins, Jinja path strategy, soak gate.

Purpose: Plans Wave 2+ (lifecycle.go, schedule.go, reconciler.go) leem cfg.PrimaryXxx via dependency injection — config é o ponto único de wiring entre operator env vars e código. Sem este plan, todos os Wave 2 plans seriam blocked. PrimaryProvisionFailureCooldownSeconds em particular é referenciado por Plan 06's threat_model T-06.6-04 mitigation — checker (revision 2026-05-17) flagged isso como phantom feature antes desta adição.

Output: 23 fields no struct Config + 23 linhas no Load() block + coverage test verificando defaults batem com WAVE0-GATES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md
@gateway/internal/config/config.go

<interfaces>
<!-- Existing Phase 6 emergency block at config.go lines 119-149 (struct fields) + 252-288 (Load reads). Pattern to mirror VERBATIM, renaming Emergency→Primary. -->

Helpers already exist in config.go:340-396:
- `envOr(k, def string) string`
- `atoiOr(s string, def int) int`
- `csvOr(s string, def []string) []string`
- `boolOr(s string, def bool) bool`
- `floatOr(s string, def float64) float64`

Existing emerg analog (line 141): `ProvisionFailureCooldownSeconds int // PROVISION_FAILURE_COOLDOWN_SECONDS (default 60 ...)` — Phase 6.6 primary mirrors with `PrimaryProvisionFailureCooldownSeconds` (default 300 — scaled-up for schedule cadence).

23 new env var defaults (verbatim from RESEARCH.md §Decisions Resolved #5, lines 656-681, plus PrimaryProvisionFailureCooldownSeconds added per checker revision):
| Env | Type | Default |
|-----|------|---------|
| PRIMARY_POD_SCHEDULE_TIMEZONE | string | "America/Sao_Paulo" |
| PRIMARY_POD_SCHEDULE_UP_HOUR | int | 8 |
| PRIMARY_POD_SCHEDULE_DOWN_HOUR | int | 22 |
| PRIMARY_POD_SCHEDULE_DAYS | []string | ["mon","tue","wed","thu","fri"] |
| PRIMARY_POD_SCHEDULE_GRACE_RAMP_DOWN_SECONDS | int | 300 |
| PRIMARY_POD_SCHEDULE_DISABLED | bool | true (per WAVE0-GATES Decision 5 soak gate) |
| PRIMARY_TEMPLATE_IMAGE | string | "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128" (SHA pin from WAVE0-GATES Decision 1) |
| PRIMARY_SPEACHES_IMAGE | string | "ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3" (SHA pin from WAVE0-GATES Decision 1) |
| PRIMARY_INFINITY_IMAGE | string | "michaelf34/infinity:0.0.77" (SHA pin from WAVE0-GATES Decision 1) |
| PRIMARY_DCGM_IMAGE | string | "nvcr.io/nvidia/k8s/dcgm-exporter:4.5.3-4.8.2-distroless" (SHA pin from WAVE0-GATES Decision 1) |
| PRIMARY_QWEN_WEIGHTS_KEY | string | "qwen3.6-27b-Q4_K_M/v1.0.0/model.gguf" (CONTEXT.md D-04) |
| PRIMARY_QWEN_WEIGHTS_SHA256 | string | "a7cbd3ecc0e3f9b333edee61ae66bc87ed713c5d49587a8355814722ed329e0f" (CONTEXT.md D-04) |
| PRIMARY_QWEN_JINJA_KEY | string | "" (B1) OR MinIO key (B2 fallback) per WAVE0-GATES Decision 3 |
| PRIMARY_QWEN_JINJA_SHA256 | string | "" (B1) OR sha256 (B2 fallback) per WAVE0-GATES Decision 3 |
| PRIMARY_LLAMA_ARGS | []string | nil (lifecycle.go uses hardcoded const) |
| PRIMARY_WHISPER_WEIGHTS_KEY | string | "whisper-large-v3/v1.0.0/model.tar.gz" (Phase 1 default) |
| PRIMARY_WHISPER_WEIGHTS_SHA256 | string | "" (operator-set; NO default shipped per RESEARCH.md:683) |
| PRIMARY_BGEM3_WEIGHTS_KEY | string | "bge-m3/v1.0.0/model.tar.gz" (Phase 1 default) |
| PRIMARY_BGEM3_WEIGHTS_SHA256 | string | "" (operator-set; NO default shipped) |
| PRIMARY_VAST_PRICE_CAP_DPH | float64 | 0.40 |
| PRIMARY_PROVISION_COLDSTART_BUDGET_SECONDS | int | 2400 (WAVE0-GATES Decision 6) |
| PRIMARY_PROVISION_FAILURE_COOLDOWN_SECONDS | int | 300 (NEW per checker revision; mirror emerg's PROVISION_FAILURE_COOLDOWN_SECONDS=60 scaled-up because primary cadence is hours-scale vs emerg minutes-scale; consumer = Plan 06.6-06 reconciler.evaluateAsleep) |
| MONTHLY_PRIMARY_BUDGET_BRL | float64 | 800.0 (Pitfall #12 separate from emergency) |

POD_DEBUG_SSH_PUBLIC_KEY: REUSE existing field (config.go:137 — verified existing). No new field needed per D-10.
MINIO_*, VAST_AI_API_KEY, USD_TO_BRL_RATE: REUSE existing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 23 Primary* struct fields + Load() block to gateway/internal/config/config.go</name>
  <files>gateway/internal/config/config.go</files>
  <read_first>
    - gateway/internal/config/config.go lines 119-149 (Phase 6 emergency block — pattern to mirror; specifically line 141 `ProvisionFailureCooldownSeconds int` field + line 270 `ProvisionFailureCooldownSeconds: atoiOr(os.Getenv("PROVISION_FAILURE_COOLDOWN_SECONDS"), 60)`) + lines 252-288 (existing Phase 6 Load() reads — pattern to mirror) + lines 340-396 (envOr/atoiOr/csvOr/boolOr/floatOr helpers — reuse, do not redefine)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md (operator-locked defaults — Decision 1, 3, 5, 6)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 681-721 (MODIFIED config.go section spec)
  </read_first>
  <action>Localizar fim do bloco Phase 6 emergency em config.go (~line 149 do struct + ~line 288 do Load). Adicionar SOB header comment `// Phase 6.6 — primary pod auto-provisioning (schedule-driven; D-08 + Decisions Resolved #5)`: (1) 23 struct fields no order da tabela <interfaces>, each one-line `FieldName Type // ENV_VAR_NAME (default ...; brief purpose)`. Tipos:  são `string`; PrimaryPodScheduleUpHour/PrimaryPodScheduleDownHour/PrimaryPodScheduleGraceRampDownSeconds/PrimaryProvisionColdStartBudgetSeconds/PrimaryProvisionFailureCooldownSeconds são `int`; PrimaryPodScheduleDays/PrimaryLlamaArgs são `[]string`; PrimaryPodScheduleDisabled é `bool`; PrimaryVastPriceCapDPH/MonthlyPrimaryBudgetBRL são `float64`. (2) 23 Load() reads no order da tabela usando helpers existentes: `PrimaryPodScheduleTimezone: envOr("PRIMARY_POD_SCHEDULE_TIMEZONE", "America/Sao_Paulo")`, `PrimaryPodScheduleUpHour: atoiOr(os.Getenv("PRIMARY_POD_SCHEDULE_UP_HOUR"), 8)`, `PrimaryPodScheduleDays: csvOr(os.Getenv("PRIMARY_POD_SCHEDULE_DAYS"), []string{"mon","tue","wed","thu","fri"})`, `PrimaryPodScheduleDisabled: boolOr(os.Getenv("PRIMARY_POD_SCHEDULE_DISABLED"), true)` [NOTA: default `true` = soak-gate kill-switch per WAVE0-GATES Decision 5; difere de RESEARCH.md sugestão de `false`], `PrimaryTemplateImage: envOr("PRIMARY_TEMPLATE_IMAGE", "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128")`, demais imagens análogas com defaults da tabela, `PrimaryQwenWeightsKey/SHA256`: defaults da CONTEXT D-04, `PrimaryQwenJinjaKey: envOr("PRIMARY_QWEN_JINJA_KEY", "<WAVE0-GATES Decision 3 value>")` (lê de WAVE0-GATES.md — se Decision 3 = B1, default empty string; se B2, default key), `PrimaryQwenJinjaSHA256` análogo, `PrimaryLlamaArgs: csvOr(os.Getenv("PRIMARY_LLAMA_ARGS"), nil)`, Whisper/BGE keys de RESEARCH (defaults Phase 1), Whisper/BGE SHA256: `os.Getenv("PRIMARY_WHISPER_WEIGHTS_SHA256")` direto (sem envOr — no default), `PrimaryVastPriceCapDPH: floatOr(os.Getenv("PRIMARY_VAST_PRICE_CAP_DPH"), 0.40)`, `PrimaryProvisionColdStartBudgetSeconds: atoiOr(os.Getenv("PRIMARY_PROVISION_COLDSTART_BUDGET_SECONDS"), 2400)`, `PrimaryProvisionFailureCooldownSeconds: atoiOr(os.Getenv("PRIMARY_PROVISION_FAILURE_COOLDOWN_SECONDS"), 300)` [NOVO per checker revision — mirror emerg's ProvisionFailureCooldownSeconds at line 270 scaled-up 60s→300s for schedule cadence; consumed by Plan 06.6-06 evaluateAsleep], `MonthlyPrimaryBudgetBRL: floatOr(os.Getenv("MONTHLY_PRIMARY_BUDGET_BRL"), 800.0)`. Comentar a primeira linha do bloco com `// Phase 6.6 (06.6-CONTEXT.md D-08 + 06.6-RESEARCH.md §Decisions Resolved #5 + 06.6-WAVE0-GATES.md Decision 5 soak-gate)`. SHA pins literais (não @sha256 forma) — operator pode strengthen via override env.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./... && grep -q 'PrimaryProvisionFailureCooldownSeconds' gateway/internal/config/config.go</automated>
  </verify>
  <done>Build succeeds. 23 fields exportados (capitalized) no Config struct, incluindo PrimaryProvisionFailureCooldownSeconds (mirror emerg). Load() retorna struct populado pelos defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config_test.go — coverage for 23 Primary defaults + env override</name>
  <files>gateway/internal/config/config_test.go</files>
  <behavior>
    - TestConfig_PrimaryPod_DefaultsLoaded: com env limpo (todos PRIMARY_* unset, exceto operator-set SHAs que ficam empty), Load() retorna cfg com 23 fields populados com defaults expected (timezone=America/Sao_Paulo, UpHour=8, DownHour=22, Days=mon..fri, GraceRampDown=300, Disabled=true [soak gate], TemplateImage=ghcr.io/ggml-org/llama.cpp:server-cuda-b9128, demais conforme WAVE0-GATES; PrimaryWhisperWeightsSHA256="" e PrimaryBGEM3WeightsSHA256="")
    - TestConfig_PrimaryPod_EnvOverride: t.Setenv("PRIMARY_POD_SCHEDULE_UP_HOUR", "7") + t.Setenv("PRIMARY_POD_SCHEDULE_DISABLED", "false") + t.Setenv("PRIMARY_VAST_PRICE_CAP_DPH", "0.50") + t.Setenv("PRIMARY_LLAMA_ARGS", "--host,0.0.0.0,--port,8000") → Load retorna UpHour=7, Disabled=false, PriceCap=0.50, LlamaArgs=["--host","0.0.0.0","--port","8000"]
    - TestConfig_PrimaryPod_DaysCSVParse: t.Setenv("PRIMARY_POD_SCHEDULE_DAYS", "tue,wed,thu") → cfg.PrimaryPodScheduleDays == ["tue","wed","thu"]
    - TestConfig_PrimaryPod_WhisperSHANoDefault: env limpo → cfg.PrimaryWhisperWeightsSHA256 == "" (operator must set explicitly)
    - TestConfig_PrimaryPod_FailureCooldownDefault: env limpo → cfg.PrimaryProvisionFailureCooldownSeconds == 300 (scaled-up vs emerg 60s); t.Setenv("PRIMARY_PROVISION_FAILURE_COOLDOWN_SECONDS", "120") → Load retorna 120
  </behavior>
  <read_first>
    - gateway/internal/config/config_test.go (existing structure — Phase 6 emergency tests para mirror pattern)
    - gateway/internal/config/config.go (Task 1 output — confirm field names + types)
  </read_first>
  <action>Adicionar 5 testes no fim de config_test.go (após bloco Phase 6). Cada test usa `t.Setenv` para isolar env mutations (Go 1.17+ auto-cleanup). TestConfig_PrimaryPod_DefaultsLoaded: t.Setenv("PRIMARY_POD_SCHEDULE_TIMEZONE", "") etc para reset (ou usar t.Setenv só nos campos relevantes; outros default já garantem) + chamar Load() + require.Equal each field. Para slices usar require.ElementsMatch. Para empty SHA fields usar require.Empty. TestConfig_PrimaryPod_EnvOverride mostra o caminho operator-override. TestConfig_PrimaryPod_DaysCSVParse valida o csvOr helper behavior preservado. TestConfig_PrimaryPod_WhisperSHANoDefault valida explicitamente que operator deve setar (cobre regression risk: alguém adicionar default acidentalmente). TestConfig_PrimaryPod_FailureCooldownDefault cobre o NOVO field PrimaryProvisionFailureCooldownSeconds (Plan 06 dependency).</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./internal/config/... -count=1 -race -run TestConfig_PrimaryPod</automated>
  </verify>
  <done>5+ tests passam. Confirma defaults batem com WAVE0-GATES.md decisions + override path funciona + PrimaryProvisionFailureCooldownSeconds default 300s coberto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Portainer stack env UI → gateway process env vars | Operator-controlled; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-03 | Information Disclosure | POD_DEBUG_SSH_PUBLIC_KEY accidental commit | mitigate | Field REUSED from Phase 6 (no new field — confirmed config.go line 137 already exposes PodDebugSSHPublicKey) — existing CI check (Phase 6 19a69a3 commit) protects against git accidental commit. Production env empty enforced by ops runbook. |
| T-06.6-04 | DoS | Schedule env-var DoS (operator sets bad UpHour/DownHour causing rapid up/down loop) | mitigate | Reconciler will enforce min 5-min between provisions via cfg.PrimaryProvisionFailureCooldownSeconds (default 300s — added in this plan; consumed by Plan 06.6-06); operator kill-switch via PRIMARY_POD_SCHEDULE_DISABLED=true (default true per soak gate). Config layer only validates types (atoiOr falls back to default on parse error) — no business logic gates here. |
| T-06.6-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new Go deps. Pure config refactor. |
</threat_model>

<verification>
- `cd gateway && go build ./...` succeeds
- `cd gateway && go test ./internal/config/... -count=1 -race` all green (existing + 5 new)
- `cd gateway && grep -c 'Primary[A-Z]\\w*\\s\\+\\(string\\|int\\|bool\\|float64\\|\\[\\]string\\)' gateway/internal/config/config.go >= 23` (23 fields present)
- `cd gateway && grep -q 'PrimaryProvisionFailureCooldownSeconds' gateway/internal/config/config.go` (cooldown field present)
</verification>

<success_criteria>
- 23 fields exposed in Config struct (22 from D-08.1 + 1 new PrimaryProvisionFailureCooldownSeconds per checker revision)
- Load() populates all 23 from env or defaults
- WAVE0-GATES.md decisions are the ONLY authoritative source for defaults (no code-side override of operator decisions)
- D-08.1 + Decisions Resolved #5 fully implemented
- Plan 06 threat_model T-06.6-04 mitigation no longer references phantom field
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-03-SUMMARY.md`.
</output>
