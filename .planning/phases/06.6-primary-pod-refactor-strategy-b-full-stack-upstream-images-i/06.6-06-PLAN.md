---
phase: 06.6
plan: 06
type: execute
wave: 3
depends_on: [06.6-04, 06.6-05, 06.6-07]
files_modified:
  - gateway/internal/primary/reconciler.go
  - gateway/internal/primary/reconciler_test.go
  - gateway/internal/primary/budget.go
  - gateway/internal/primary/budget_test.go
  - gateway/internal/dcgm/scraper.go
  - gateway/internal/shed/inflight.go
  - gateway/internal/upstreams/loader.go
  - gateway/internal/upstreams/loader_test.go
autonomous: true
requirements: []
tags: [primary-pod, wave-3, reconciler, drain, budget, dcgm, inflight, tier0-extension, large-plan]
scope_note: "Plan 06 has 4 tasks + 8 files modified (exceeds standard 2-3 task target). Checker WARNING 3 (revision 2026-05-17) flagged context-budget risk and suggested split into 06.6-06a (reconciler + drain) + 06.6-06b (budget + 3-role tier0 + DCGM.SetURL + Inflight.Count). DECISION: keep as one plan. Rationale: (1) splitting would cascade depends_on renumbering across Plans 08/09/10/11 (4+ plans churned for marginal benefit); (2) the 4 tasks are highly cohesive — Task 1 extensions (loader/dcgm/inflight) are direct inputs consumed by Task 2 reconciler.markReady; Task 3 reconciler_test depends on Task 2 reconciler.go; Task 4 budget is independent but small enough to coexist; (3) context budget mitigation: executor should checkpoint after each task (commit + verify before proceeding) — natural compression checkpoints prevent runaway context spend. Operator-acceptable trade-off per orchestrator guidance."

must_haves:
  truths:
    - "gateway/internal/primary/reconciler.go expõe Reconciler (extends struct de Plan 06.6-04) com evaluateTick(ctx, now, log) dispatcher cobrindo 5 estados (Asleep/Provisioning/Ready/Draining/Destroying) + Start(ctx) goroutine 1Hz que chama evaluateTick em loop"
    - "evaluateAsleep: se !cfg.PrimaryPodScheduleDisabled && rule.IsInPeak(now) && cooldownElapsed(now) → emit gw:primary:events provisioning_started + transition Asleep→Provisioning via FSM CAS + spawnProvisioningGoroutine async; cooldownElapsed = (now - lastProvisionFailureAt) > cfg.PrimaryProvisionFailureCooldownSeconds (per BLOCKER 4 revision — wires cfg field added in Plan 06.6-03)"
    - "Reconciler struct adiciona field `lastProvisionFailureAt atomic.Pointer[time.Time]` populated on provisioning failure (offer_race_lost, health_timeout, vast_create_error); zero-value (nil) interpretado como `cooldown elapsed` (no prior failure)"
    - "evaluateProvisioning: poll 4 pod health endpoints (LLM /v1/models + STT /health + Embed /health + DCGM /metrics) via deps.HealthCheck; quando todos 4 retornam true → markReady + transition Provisioning→Ready; se coldstart budget (cfg.PrimaryProvisionColdStartBudgetSeconds) elapsed → close lifecycle reason=health_timeout + lastProvisionFailureAt.Store(&now) + transition Provisioning→Asleep + Sentry alert"
    - "evaluateReady: se !rule.IsInPeak(now) → start drain → transition Ready→Draining; OU se all 3 breakers (local-llm/local-stt/local-embed) OPEN > 60s → transition Ready→Draining (pod went unhealthy unexpected)"
    - "evaluateDraining (NEW; per Decisions Resolved #10 RESEARCH.md:744-765): RestoreTier0 já chamado na entrada; inflight := dispatcher.InflightForRole(local-llm) + ...stt + ...embed; se inflight==0 OR elapsed >= grace_s → transition Draining→Destroying"
    - "evaluateDestroying: chamar vastutil.BestEffortDestroy(ctx, deps.Vast, log, activeInstanceID); on success → close lifecycle + clear activePodURLs + transition Destroying→Asleep"
    - "markReady (analog emerg markHealthy:429-462): OverrideTier0 para 3 roles (llm/stt/embed; per Architecture Pattern 3 RESEARCH:337-348) + DCGMScraper.SetURL(podDCGMURL) + Loader notify via existing LISTEN/NOTIFY pattern (Plan 06.6-08 wiring)"
    - "closeLifecycle (analog emerg:488-525): RestoreTier0 para 3 roles + DCGMScraper.SetURL(\"\") defensive idempotent + ClosePrimaryLifecycle SQL query (Plan 06.6-07 created) + clear activePodURLs"
    - "cancelActiveLifecycle (analog emerg:1001-1042) triple-layer: ctx.Cancel + redisx.PublishPrimaryEvent cancel_in_flight + vastutil.BestEffortDestroy fresh background ctx"
    - "leader-election via redsync (parity emerg; RESEARCH §Schedule Reconciler Design lines 976-982): only leader runs evaluateTick; non-leader replicas hot-standby"
    - "primary/budget.go define MonthlyPrimaryBudgetBRL aggregator analog emerg/budget.go com SELECT SUM(total_cost_brl) WHERE event_kind='primary_lifecycle_close' (Pitfall #12 RESEARCH.md:558-568); Sentry alert on overshoot"
    - "gateway/internal/dcgm/scraper.go expõe novo Scraper.SetURL(url string) method para runtime override (Decisions Resolved #8 RESEARCH.md:725-726)"
    - "gateway/internal/shed/inflight.go expõe novo InflightRegistry.Count(upstream string) int64 method para drain polling (Decisions Resolved #10 RESEARCH.md:768)"
    - "gateway/internal/upstreams/loader.go newTier0OverrideMap retorna map com 3 chaves (llm + stt + embed; era apenas llm em Phase 6) per Architecture Pattern 3 — OverrideTier0/RestoreTier0 SÃO role-agnostic, NÃO precisam mudar"
  artifacts:
    - path: "gateway/internal/primary/reconciler.go"
      provides: "Reconciler.Start + evaluateTick + 5 state-branch evaluate* methods + markReady + closeLifecycle + cancelActiveLifecycle + leader-election + cooldown enforcement"
      contains: "Start|evaluateTick|evaluateAsleep|evaluateProvisioning|evaluateReady|evaluateDraining|evaluateDestroying|markReady|closeLifecycle|cancelActiveLifecycle|redsync|lastProvisionFailureAt|PrimaryProvisionFailureCooldownSeconds"
    - path: "gateway/internal/primary/reconciler_test.go"
      provides: "12+ unit tests cobrindo evaluate* + leader + cancel + cooldown paths"
      contains: "TestEvaluateAsleep_TransitionsToProvisioningInPeak|TestEvaluateAsleep_NoopWhenDisabled|TestEvaluateAsleep_NoopDuringCooldown|TestEvaluateReady_TransitionsToDrainingOutOfPeak|TestEvaluateDraining_DestroysWhenInflightZero|TestEvaluateDraining_DestroysOnGraceElapsed|TestDrain_RestoreTier0CalledBeforeDestroy|TestMarkReady_OverridesTier0_3Roles|TestEvaluateProvisioning_TimeoutTransitionsToAsleep|TestProvisioningFailure_SetsLastFailureTimestamp"
    - path: "gateway/internal/primary/budget.go"
      provides: "MonthlyPrimaryBudgetBRL aggregator + Sentry alert"
      contains: "MonthlyPrimaryBudgetBRL|checkBudget|primary_lifecycle_close"
    - path: "gateway/internal/primary/budget_test.go"
      provides: "Budget separation from emergency assertion"
      contains: "TestBudget_PrimarySeparateFromEmerg"
    - path: "gateway/internal/dcgm/scraper.go"
      provides: "Scraper.SetURL runtime override method"
      contains: "func (s \\*Scraper) SetURL"
    - path: "gateway/internal/shed/inflight.go"
      provides: "InflightRegistry.Count public read method"
      contains: "func (r \\*InflightRegistry) Count"
    - path: "gateway/internal/upstreams/loader.go"
      provides: "3-role override map (llm/stt/embed)"
      contains: "\"stt\":\\s*new\\(atomic\\.Pointer\\[string\\]\\)|\"embed\":\\s*new\\(atomic\\.Pointer\\[string\\]\\)"
  key_links:
    - from: "Reconciler.markReady"
      to: "upstreams.Loader.OverrideTier0"
      via: "deps.Loader interface field"
      pattern: "Loader\\.OverrideTier0\\(\"llm\"\\|\"stt\"\\|\"embed\""
    - from: "Reconciler.evaluateDraining"
      to: "shed.InflightRegistry.Count"
      via: "deps.Inflight field"
      pattern: "Inflight\\.Count\\(\"local-(llm|stt|embed)\"\\)"
    - from: "Reconciler.markReady"
      to: "dcgm.Scraper.SetURL"
      via: "deps.DCGMScraper field"
      pattern: "DCGMScraper\\.SetURL"
    - from: "Reconciler.evaluateAsleep cooldown check"
      to: "cfg.PrimaryProvisionFailureCooldownSeconds (Plan 06.6-03 added field)"
      via: "cfg field read in reconciler"
      pattern: "PrimaryProvisionFailureCooldownSeconds"

threat_refs: [T-06.6-04, T-06.6-SC]
---

<objective>
Plan central de Phase 6.6 — implementar o reconciler completo com 5-state FSM dispatcher + leader election + drain ramp-down + 3-role tier-0 override extension + provision-failure cooldown enforcement. Este plan consolida 4 modificações em arquivos externos ao primary package (`dcgm/scraper.go`, `shed/inflight.go`, `upstreams/loader.go`) + 2 arquivos novos no primary package (`reconciler.go`, `budget.go`).

Purpose: Sem reconciler, primary pod nunca seria provisionado/destruído. Sem 3-role tier-0 extension, gateway requests STT/embed nunca seriam roteadas para o primary pod (sempre cairiam em OpenAI tier-1). Sem Inflight.Count public + DCGMScraper.SetURL public, drain ramp-down e Phase 5 VRAM signal não funcionam para pods dynamic. Sem cooldown enforcement, T-06.6-04 (schedule oscillation) seria mitigated apenas em paper.

**Context budget note** (per checker WARNING 3 revision): este plan tem 4 tasks + 8 files modificados — acima do standard 2-3 task target. Executor DEVE checkpoint após cada task (git commit + verify pass) antes de proceed, para evitar runaway context spend. Tasks são cohesivas (split cascade penalty was deemed worse than budget risk). Veja `scope_note` em frontmatter para rationale.

Output: 8 arquivos modificados/criados. ~12+ unit tests cobrindo todos branches do evaluateTick + drain + markReady + cooldown. Reconciler é STARTABLE mas ainda não WIRED — Plan 06.6-08 faz o main.go wiring final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@gateway/internal/emerg/reconciler.go
@gateway/internal/emerg/lifecycle.go
@gateway/internal/emerg/budget.go
@gateway/internal/upstreams/loader.go
@gateway/internal/dcgm/scraper.go
@gateway/internal/shed/inflight.go
@gateway/internal/redisx/emerg.go

<interfaces>
<!-- emerg/reconciler.go pattern references (verbatim from PATTERNS.md lines 489-543) -->

- evaluateTick dispatcher at emerg/reconciler.go:453-474 (5-case switch by FSM.State())
- markHealthy at emerg/lifecycle.go:429-462 (Override + state transition + DB update)
- closeLifecycle at emerg/lifecycle.go:488-525 (DB close + RestoreTier0)
- cancelActiveLifecycle triple-layer at emerg/lifecycle.go:1001-1042
- bestEffortDestroy fresh background ctx (now in vastutil.BestEffortDestroy per Plan 06.6-02)

<!-- emerg cooldown analog at gateway/internal/config/config.go line 141 + reconciler.go (search for cfg.ProvisionFailureCooldownSeconds) — pattern to mirror with cfg.PrimaryProvisionFailureCooldownSeconds -->

<!-- Existing interfaces consumed -->

upstreams/loader.go:
- newTier0OverrideMap() at lines 76-85 (CURRENTLY 1 role "llm" — extend to 3)
- OverrideTier0(role, url) at lines 237-248 (role-agnostic — no change needed)
- RestoreTier0(role) at lines 258-... (role-agnostic — no change needed)

dcgm/scraper.go:
- type Scraper struct { url string; ... } at line 84
- New(url, interval, timeout, log) *Scraper at line 101
- ADD: SetURL(url string) method protected by atomic.Pointer[string] or mutex

shed/inflight.go:
- InflightRegistry type at line ~27 with RWMutex + per-upstream maps
- GlobalInflight(upstream string) int64 at line 155 (existing public reader — Count is just a clearer alias OR new public method semantic-identical; choose: ADD Count as wrapper of GlobalInflight, or document GlobalInflight as the official drain-polling API)

redisx/emerg.go:
- NewEmergRedsync(rdb) at line 156 (pattern to mirror in primary; key gw:primary:lock added in Plan 06.6-07)

<!-- New Deps fields for Reconciler (extension of Plan 06.6-04 Deps struct) -->

```go
type Deps struct {
    Cfg            config.Config
    Log            *slog.Logger
    Vast           VastAPI                       // same interface as emerg.VastAPI; primary uses subset
    HealthCheck    func(ctx context.Context, url string) bool
    Loader         *upstreams.Loader              // for OverrideTier0/RestoreTier0
    DCGMScraper    *dcgm.Scraper                  // for SetURL on Ready/RestoreOnClose
    Inflight       *shed.InflightRegistry         // for drain polling Count("local-llm"/"local-stt"/"local-embed")
    FSM            *FSM                           // primary package FSM (Plan 06.6-05)
    Rule           ScheduleRule                   // pre-parsed; lifetime = reconciler lifetime
    DB             *pgxpool.Pool                  // for primary_lifecycles SQL ops (Plan 06.6-07 created queries)
    Redis          *redis.Client                  // for redsync lock + Pub/Sub gw:primary:events
    ReplicaID      string                         // for leader claim identity + events.ReplicaID
}

// VastAPI subset interface (primary needs Search/Create/Get/Destroy; mirror emerg.VastAPI shape)
type VastAPI interface {
    SearchOffers(ctx context.Context, filter vast.SearchFilter) ([]vast.Offer, error)
    CreateInstance(ctx context.Context, req vast.CreateRequest) (int64, error)
    GetInstance(ctx context.Context, id int64) (vast.Instance, error)
    DestroyInstance(ctx context.Context, id int64) error
}

// Reconciler struct extension (Plan 06.6-04 base + cooldown field):
// lastProvisionFailureAt atomic.Pointer[time.Time]  // populated on provisioning failure; nil = no prior failure → cooldown elapsed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend gateway/internal/upstreams/loader.go (3-role override map) + gateway/internal/dcgm/scraper.go (SetURL) + gateway/internal/shed/inflight.go (Count public method) + tests</name>
  <files>gateway/internal/upstreams/loader.go, gateway/internal/upstreams/loader_test.go, gateway/internal/dcgm/scraper.go, gateway/internal/dcgm/scraper_test.go, gateway/internal/shed/inflight.go, gateway/internal/shed/inflight_test.go</files>
  <read_first>
    - gateway/internal/upstreams/loader.go lines 76-85 (newTier0OverrideMap — extend to 3 roles)
    - gateway/internal/dcgm/scraper.go lines 84-130 (Scraper struct + constructor)
    - gateway/internal/shed/inflight.go lines 27-200 (InflightRegistry + GlobalInflight)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 725-741 (loader.go modification spec) + RESEARCH.md §Decisions Resolved #8 + #10 (DCGM SetURL + Inflight.Count specs)
  </read_first>
  <action>(a) loader.go: localizar newTier0OverrideMap() em lines 76-85; substituir return statement por map com 3 entries: `return map[string]*atomic.Pointer[string]{"llm": new(atomic.Pointer[string]), "stt": new(atomic.Pointer[string]), "embed": new(atomic.Pointer[string])}`. Adicionar inline comment `// Phase 6.6 — primary pod overrides 3 roles (was LLM-only in Phase 6). // OverrideTier0/RestoreTier0 são role-agnostic; só o map precisa crescer.`. loader_test.go: adicionar TestNewTier0OverrideMap_Has3Roles asserting all 3 keys + TestOverrideTier0_RestoreTier0_AllRoles iterando 3 roles. (b) dcgm/scraper.go: o campo url atualmente é set via constructor New. Para runtime override: substituir `url string` por `url atomic.Pointer[string]` (lockless) OU adicionar mutex.RWMutex protecting url + add method `func (s *Scraper) SetURL(url string)` que atomic-stores OR mutex-set. `scrape` method (line 148) precisa Load() o url atual em vez de field direto. Empty url → fail-open (Phase 5 design preservado). scraper_test.go: adicionar TestScraper_SetURL_RuntimeOverride asserting that SetURL("http://new") changes subsequent scrape target URL. (c) shed/inflight.go: adicionar `func (r *InflightRegistry) Count(upstream string) int64 { if r == nil { return 0 }; return r.GlobalInflight(upstream) }` (delegation wrapper para keep API surface explicit). inflight_test.go: adicionar TestInflightRegistry_Count_DelegatesToGlobalInflight asserting Count and GlobalInflight return same value.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./internal/upstreams/... ./internal/dcgm/... ./internal/shed/... -count=1 -race</automated>
  </verify>
  <done>3 files extended without breaking existing tests. New tests verde. Plan 06.6-06 Task 2 pode chamar Loader.OverrideTier0("stt"|"embed"), DCGMScraper.SetURL, Inflight.Count("local-llm"/...). **CHECKPOINT**: commit + verify pass antes de proceder ao Task 2 (context budget mitigation per scope_note).</done>
</task>

<task type="auto">
  <name>Task 2: Create gateway/internal/primary/reconciler.go — full 5-state dispatcher + leader-election + drain + cooldown enforcement</name>
  <files>gateway/internal/primary/reconciler.go, gateway/internal/primary/lifecycle.go</files>
  <read_first>
    - gateway/internal/emerg/reconciler.go full file (1000+ LOC; main analog — focus on evaluateTick dispatcher, leader-election via redsync, Run loop 1Hz, evaluate* state branches; também grep para cfg.ProvisionFailureCooldownSeconds usage pattern)
    - gateway/internal/emerg/lifecycle.go lines 429-462 (markHealthy), 488-525 (closeLifecycle), 1001-1042 (cancelActiveLifecycle)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 489-543 (reconciler.go pattern spec) + lines 316-357 (markReady + closeLifecycle 3-role extension)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md §Schedule Reconciler Design (lines 922-982)
    - gateway/internal/redisx/emerg.go (PublishEmergEvent + NewEmergRedsync pattern — analog para primary)
    - gateway/internal/redisx/primary.go (Plan 06.6-07 output — PublishPrimaryEvent + NewPrimaryRedsync)
    - gateway/db/queries/primary_lifecycles.sql.go (Plan 06.6-07 sqlc generated — Insert/MarkHealthy/MarkDraining/Close)
    - gateway/internal/config/config.go (Plan 06.6-03 output — PrimaryProvisionFailureCooldownSeconds field; default 300s)
  </read_first>
  <action>(a) Extender Deps struct em primary/lifecycle.go (Plan 06.6-04 stub) para incluir Loader, DCGMScraper, Inflight, FSM, Rule, DB, Redis, ReplicaID — types citados em <interfaces>. Adicionar VastAPI interface no primary package (subset emerg.VastAPI). Extender Reconciler struct para incluir `lastProvisionFailureAt atomic.Pointer[time.Time]` (per BLOCKER 4 revision — zero-value=nil means no prior failure → cooldown elapsed). (b) Criar primary/reconciler.go com `package primary`. Imports: stdlib (context, errors, fmt, log/slog, sync/atomic, time), config, vast, vastutil, upstreams, dcgm, shed, redisx, primary's own FSM/ScheduleRule, sqlc/db (gateway/internal/db/sqlc), goredsync. Adicionar fields na Reconciler struct: `activeLifecycle atomic.Int64; activeInstance atomic.Int64; lifecycleCancel atomic.Pointer[context.CancelFunc]; isLeader atomic.Bool`. `func (r *Reconciler) Start(ctx context.Context)`: leader-election loop via redsync mutex Lock(ctx, gw:primary:lock); on lock acquired isLeader.Store(true), spawn evaluateTick goroutine ticking 1Hz. On context cancel: Unlock fresh ctx (mirror emerg Pitfall 8 separate ctx pattern). `func (r *Reconciler) evaluateTick(ctx, now, log)`: switch r.deps.FSM.State() { Asleep: evaluateAsleep; Provisioning: evaluateProvisioning; Ready: evaluateReady; Draining: evaluateDraining; Destroying: evaluateDestroying }. Each evaluate* method follows the PATTERNS.md pseudocode + RESEARCH.md state transition table lines 946-951. **evaluateAsleep COOLDOWN ENFORCEMENT (per BLOCKER 4)**: helper `func (r *Reconciler) cooldownElapsed(now time.Time) bool { lastFail := r.lastProvisionFailureAt.Load(); if lastFail == nil { return true }; cooldown := time.Duration(r.cfg.PrimaryProvisionFailureCooldownSeconds) * time.Second; return now.Sub(*lastFail) >= cooldown }`. evaluateAsleep guard: `if r.cfg.PrimaryPodScheduleDisabled || !r.rule.IsInPeak(now) || !r.cooldownElapsed(now) { return }`. Implementar `markReady(ctx, lifecycleID, urls primaryPodURLs, acceptedDPH float64) error`: INSERT audit event via vastutil.MustEventJSON; MarkPrimaryLifecycleHealthy SQL; activePodURLs.Store(&urls); 3× Loader.OverrideTier0("llm"|"stt"|"embed", urls.LLM|STT|Embed); DCGMScraper.SetURL(urls.DCGM); FSM.Transition(Provisioning, Ready, time.Now(), "all_probes_passed"); PublishPrimaryEvent type=primary_ready. Implementar `closeLifecycle(ctx, id, reason, dph) error`: ClosePrimaryLifecycle SQL; 3× Loader.RestoreTier0(role); DCGMScraper.SetURL("") (defensive); clear activePodURLs.Store(nil); FSM transition para Asleep (com SetState se from estava em Destroying/Draining). Implementar `cancelActiveLifecycle(ctx, reason)` triple-layer (PATTERNS.md:539): Layer 1 invoca lifecycleCancel.Swap(nil) callable; Layer 2 PublishPrimaryEvent cancel_in_flight; Layer 3 vastutil.BestEffortDestroy fresh ctx. evaluateDraining (PATTERNS.md:524-535): inflight := r.deps.Inflight.Count("local-llm") + Count("local-stt") + Count("local-embed"); elapsed := now.Sub(*r.drainStartedAt.Load()); grace := time.Duration(cfg.PrimaryPodScheduleGraceRampDownSeconds) * time.Second; if inflight==0 || elapsed >= grace → FSM.Transition(Draining, Destroying, now, "drain_complete"). evaluateAsleep com Pitfall #11 awareness: NÃO inspect emerg FSM diretamente (cross-package coupling); apenas provision + publish primary_ready event; emerg subscriber em Plan 06.6-08 reage. **evaluateProvisioning FAILURE PATHS (per BLOCKER 4 — wire cooldown timestamp)**: spawn-goroutine pattern para chamar Vast.SearchOffers + CreateInstance + waitForReadyOrDestroy (cold-start budget polling 4 endpoints via deps.HealthCheck); transição Provisioning→Ready disparada por markReady success; Provisioning→Asleep se cold-start budget elapsed OU schedule out of window (cancel-in-flight); **EM TODO failure path (offer_race_lost / vast_create_error / health_timeout), CALL r.lastProvisionFailureAt.Store(&now) ANTES de transition Asleep — isso é o que arma o cooldown gate em evaluateAsleep next tick**. evaluateDestroying: vastutil.BestEffortDestroy(activeInstance); on ack closeLifecycle. Helpers podLLMURL/podSTTURL/podEmbedURL/podDCGMURL já adicionados em Plan 06.6-04.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./internal/primary/...</automated>
  </verify>
  <done>reconciler.go compila. 5 evaluate* methods + markReady + closeLifecycle + cancelActiveLifecycle + Start (com leader-election) + cooldown enforcement defined. lastProvisionFailureAt field populated em todos failure paths. Build verde standalone. **CHECKPOINT**: commit + verify antes de Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: reconciler_test.go — 12+ unit tests cobrindo evaluate* + drain + markReady + cooldown</name>
  <files>gateway/internal/primary/reconciler_test.go</files>
  <behavior>
    - TestEvaluateAsleep_TransitionsToProvisioningInPeak: FSM=Asleep + rule=peak + Disabled=false + lastProvisionFailureAt=nil → after evaluateTick, FSM.State() == Provisioning + spawnProvisioning called
    - TestEvaluateAsleep_NoopWhenDisabled: cfg.PrimaryPodScheduleDisabled=true → evaluateTick from Asleep keeps Asleep
    - TestEvaluateAsleep_NoopOutOfPeak: rule.IsInPeak=false + Disabled=false → stays Asleep
    - TestEvaluateAsleep_NoopDuringCooldown: rule=peak + Disabled=false + lastProvisionFailureAt set to (now - 60s) + cfg.PrimaryProvisionFailureCooldownSeconds=300 → evaluateTick keeps Asleep (cooldown 60s < 300s); after wait/jump now+300s → next evaluateTick transitions to Provisioning (cooldown elapsed)
    - TestEvaluateAsleep_CooldownElapsed_AfterFailure: simulate provision failure path (lastProvisionFailureAt set via direct r.lastProvisionFailureAt.Store(&pastTime)); pastTime older than cooldown → cooldownElapsed returns true → next evaluateAsleep transitions to Provisioning
    - TestProvisioningFailure_SetsLastFailureTimestamp: simulate evaluateProvisioning failure (cold-start budget elapsed OR fake VastAPI returns error from CreateInstance); assert r.lastProvisionFailureAt.Load() != nil AND value close to time.Now() (within 1s) — this is the wiring proof per BLOCKER 4
    - TestEvaluateReady_TransitionsToDrainingOutOfPeak: FSM=Ready + rule.IsInPeak=false → drainStartedAt set + FSM transitions Ready→Draining + 3× RestoreTier0 called
    - TestEvaluateDraining_DestroysWhenInflightZero: FSM=Draining + Inflight.Count returns 0 → FSM transitions Draining→Destroying
    - TestEvaluateDraining_DestroysOnGraceElapsed: FSM=Draining + Inflight>0 + drainStartedAt before now - grace → FSM transitions Draining→Destroying anyway
    - TestEvaluateDraining_WaitsIfInflightAndWithinGrace: FSM=Draining + Inflight>0 + within grace → FSM stays Draining
    - TestDrain_RestoreTier0CalledBeforeDestroy: assert RestoreTier0 fires na entrada do Draining state, ANTES de BestEffortDestroy (order: RestoreTier0 → Destroying state → Destroy)
    - TestMarkReady_OverridesTier0_3Roles: chamar markReady com fake Loader; assert Loader.OverrideTier0 chamado 3 vezes com roles ["llm","stt","embed"] + corresponding URLs
    - TestMarkReady_SetsDCGMScraperURL: assert DCGMScraper.SetURL chamado com primaryPodURLs.DCGM
    - TestEvaluateProvisioning_TimeoutTransitionsToAsleep: FSM=Provisioning + elapsed > cfg.PrimaryProvisionColdStartBudgetSeconds → closeLifecycle reason=health_timeout + lastProvisionFailureAt.Store(&now) + FSM.SetState(Asleep) + Sentry alert (capture via fake)
    - TestLeaderElection_OnlyOneLeaderEvaluatesTick: 2 reconcilers compartilhando mesma Redis; só 1 ganha lock; outro permanece isLeader=false + evaluateTick never called
  </behavior>
  <read_first>
    - gateway/internal/primary/reconciler.go (Task 2 output — confirm method signatures + fake-able interfaces + lastProvisionFailureAt field)
    - gateway/internal/emerg/reconciler_test.go (251 LOC; analog test harness with leader-election + tick branches)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 489-543 (evaluate* pseudocode for asserting behavior)
  </read_first>
  <action>Criar reconciler_test.go com `package primary`. Imports: stdlib + miniredis + testify/require. Helpers: `type fakeLoader struct { overrides map[string]string; restores []string }` with OverrideTier0/RestoreTier0 stubs recording calls. `type fakeDCGMScraper struct { url atomic.Pointer[string] }` with SetURL. `type fakeInflight struct { counts map[string]int64 }` with Count. `type fakeVast struct { searchResult []vast.Offer; createInstanceID int64; destroyed []int64; createInstanceErr error }` implementing VastAPI (createInstanceErr para forçar failure path em TestProvisioningFailure_SetsLastFailureTimestamp). Para leader-election test usar real miniredis + redsync. Para schedule rule, construir manualmente com BRT timezone (Brazil sem DST) + UpHour/DownHour adequados ao test. Para timeout test, drainStartedAt ou provisioning starter timestamp manipulado via clock injection — passar `now time.Time` direto em evaluateTick (já é parâmetro per Task 2 design). Para cooldown tests: r.lastProvisionFailureAt.Store(&pastTime) direto para simular failure history; assert behavior gate. 15 testes seguindo behavior table (12 originais + 3 novos de cooldown).</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./internal/primary/... -count=1 -race -run TestEvaluate -run TestDrain -run TestMarkReady -run TestLeaderElection -run TestProvisioningFailure</automated>
  </verify>
  <done>15+ tests passam. Reconciler comportamento determinístico em todos branches do evaluateTick. Leader election validated com miniredis + redsync real. Cooldown gate provado mecanicamente (BLOCKER 4 resolved). **CHECKPOINT**: commit + verify antes de Task 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create gateway/internal/primary/budget.go + budget_test.go (separate from emergency per Pitfall #12)</name>
  <files>gateway/internal/primary/budget.go, gateway/internal/primary/budget_test.go</files>
  <behavior>
    - TestBudget_PrimarySeparateFromEmerg: tabela primary_lifecycles tem 3 closes com total_cost_brl=100, 200, 250 (sum 550); audit_log tem ainda 1 emergency_lifecycle_close com total_cost_brl=500. checkBudget consulta APENAS event_kind='primary_lifecycle_close' (NÃO 'emergency_lifecycle_close'); resultado: monthly_spent_brl == 550, NOT 1050; alert fires somente se 550 > cfg.MonthlyPrimaryBudgetBRL (default 800)
    - TestBudget_UnderBudget_NoAlert: spent=400 < budget 800 → NO Sentry alert fired
    - TestBudget_OverBudget_AlertFires: spent=900 > budget 800 → 1 Sentry alert fired (capture via sentry hub mock or breadcrumb counter)
    - TestBudget_DedupeAlert: 60s tick wrapper; alert fires apenas uma vez por mês mesmo se checkBudget chamado múltiplas vezes (analog emerg/budget.go dedupe Pitfall 11)
  </behavior>
  <read_first>
    - gateway/internal/emerg/budget.go (158 LOC analog — verbatim adapt)
    - gateway/internal/emerg/budget_test.go (271 LOC test pattern)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md §Pitfall #12 (lines 558-568) — event_kind separation rationale
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 545-549 (budget.go pattern spec)
  </read_first>
  <action>Criar gateway/internal/primary/budget.go: copy-adapt verbatim emerg/budget.go renomeando MonthlyEmergencyBudgetBRL → MonthlyPrimaryBudgetBRL (consultando cfg.MonthlyPrimaryBudgetBRL ao invés de cfg.MonthlyEmergencyBudgetBRL), event_kind filter `emergency_lifecycle_close` → `primary_lifecycle_close`, queries reference primary_lifecycles table (Plan 06.6-07 created). Função `checkBudget(ctx, cfg, db, now) (spentBRL float64, overshoot bool, err error)` + Sentry alert dedupe lógica (60s tick wrapper); função `Start(ctx)` rodando ticker para periodic check. Criar budget_test.go com 4 testes da tabela <behavior>. Para dedupe usar `lastAlertAt atomic.Int64` field; alerts fire apenas se now - lastAlertAt > 24h within same month.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./internal/primary/... -count=1 -race -run TestBudget</automated>
  </verify>
  <done>budget.go compila + 4 tests verdes. Demonstrado que aggregator NÃO mistura primary com emergency costs (Pitfall #12 mitigated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reconciler goroutine → Vast.ai API | Outbound; CreateInstance + DestroyInstance via cfg.VastAPIKey |
| Reconciler goroutine → Postgres (primary_lifecycles table) | DB INSERT/UPDATE via pgxpool; trusted internal |
| Reconciler goroutine → Redis (gw:primary:lock + gw:primary:events) | Cluster-internal; trusted |
| evaluateDraining → shed.InflightRegistry.Count | In-process; no boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-04 | DoS | Schedule oscillation (provision/destroy loop) | mitigate | Reconciler enforces cfg.PrimaryProvisionFailureCooldownSeconds (default 300s — added in Plan 06.6-03; wired into evaluateAsleep cooldownElapsed gate in this plan Task 2) — após failure, lastProvisionFailureAt is set and evaluateAsleep refuses to re-arm provisioning until cooldown elapses. Schedule changes require gateway restart (no hot-reload). PRIMARY_POD_SCHEDULE_DISABLED=true kill-switch always honored. Tests TestEvaluateAsleep_NoopDuringCooldown + TestProvisioningFailure_SetsLastFailureTimestamp prove mechanical enforcement. |
| T-06.6-SC | Tampering | Reconciler imports | mitigate | All imports from go.mod (no new external deps). vastutil + emerg/vast already audited Phase 6 + Plan 06.6-02. |
</threat_model>

<verification>
- `cd gateway && go build ./...` succeeds
- `cd gateway && go test ./internal/primary/... ./internal/upstreams/... ./internal/dcgm/... ./internal/shed/... -count=1 -race` all green
- `cd gateway && grep -c 'OverrideTier0(\"stt\"\\|OverrideTier0(\"embed\"' gateway/internal/primary/reconciler.go >= 2` (3-role override wired)
- `cd gateway && grep -c 'Inflight\\.Count' gateway/internal/primary/reconciler.go >= 3` (3-role drain polling wired)
- `cd gateway && grep -c 'DCGMScraper\\.SetURL' gateway/internal/primary/reconciler.go >= 2` (markReady + closeLifecycle both call)
- `cd gateway && grep -c 'PrimaryProvisionFailureCooldownSeconds\\|lastProvisionFailureAt' gateway/internal/primary/reconciler.go >= 2` (cooldown wired per BLOCKER 4)
</verification>

<success_criteria>
- 5-state FSM evaluateTick is the canonical entry point for schedule-driven lifecycle
- 3-role tier-0 override extension allows STT + embed routing to primary pod (was LLM-only in Phase 6)
- DCGMScraper.SetURL allows Phase 5 VRAM signal to track dynamic primary pod URL
- InflightRegistry.Count enables drain ramp-down without changing breaker semantics
- Budget separation prevents Pitfall #12 (emergency budget exhaustion by primary regular use)
- Provision-failure cooldown enforcement prevents T-06.6-04 schedule oscillation (BLOCKER 4 resolved)
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-06-SUMMARY.md`.
</output>
