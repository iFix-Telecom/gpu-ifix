---
phase: 06.6
plan: 02
type: execute
wave: 1
depends_on: [06.6-01]
files_modified:
  - gateway/internal/vastutil/helpers.go
  - gateway/internal/vastutil/helpers_test.go
  - gateway/internal/emerg/lifecycle.go
  - gateway/internal/emerg/lifecycle_test.go
autonomous: true
requirements: []
tags: [refactor, wave-1, vastutil, shared-helpers, extract]

must_haves:
  truths:
    - "gateway/internal/vastutil/ é um pacote Go novo que expõe FilterBelowCap, ExcludeHost, MustEventJSON, PgInt8, PgNumericFromFloat, CaptureBreadcrumb, BestEffortDestroy como free functions reutilizáveis pelo emerg E pelo novo primary package (D-08.3 + Decisions Resolved #4 RESEARCH.md:638-646)"
    - "gateway/internal/emerg/lifecycle.go compila e passa todos os testes existentes após substituir chamadas locais por vastutil.* imports — zero regressão Phase 6/6.5"
    - "VastDestroyer é uma interface mínima de 1 método (DestroyInstance) que isola BestEffortDestroy de emerg.VastAPI completo (avoids import cycle se primary depender ambos)"
    - "vastutil.CaptureBreadcrumb aceita category string prefixed pelo caller (ex: \"emerg.\"+category ou \"primary.\"+category) — receiver-bound captureBreadcrumb removido"
  artifacts:
    - path: "gateway/internal/vastutil/helpers.go"
      provides: "7 pure helper functions + VastDestroyer interface"
      contains: "FilterBelowCap|ExcludeHost|MustEventJSON|PgInt8|PgNumericFromFloat|CaptureBreadcrumb|BestEffortDestroy|VastDestroyer"
    - path: "gateway/internal/vastutil/helpers_test.go"
      provides: "7+ unit tests replicating emerg/lifecycle_test.go assertions"
      contains: "TestFilterBelowCap|TestExcludeHost|TestMustEventJSON|TestPgInt8|TestPgNumericFromFloat"
  key_links:
    - from: "gateway/internal/emerg/lifecycle.go"
      to: "gateway/internal/vastutil"
      via: "import statement"
      pattern: "vastutil\\.(FilterBelowCap|ExcludeHost|MustEventJSON|PgInt8|PgNumericFromFloat|CaptureBreadcrumb|BestEffortDestroy)"

threat_refs: [T-06.6-SC]
---

<objective>
Extrair 7 helpers puros de gateway/internal/emerg/lifecycle.go para um novo pacote shared `gateway/internal/vastutil/` ANTES de criar primary package. Sem esta extração, o package primary teria que copy-paste os mesmos 7 helpers, gerando 2 cópias divergentes ao longo do tempo (D-08.3 anti-pattern).

Purpose: Code reuse without coupling. Primary package importa vastutil para shared helpers; emerg package também importa vastutil; ambos pacotes ficam independentes (no cross-imports emerg ⟷ primary).

Output: Novo pacote vastutil (helpers.go + helpers_test.go); emerg/lifecycle.go atualizado para usar vastutil.* (zero behavior change, testes verdes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@gateway/internal/emerg/lifecycle.go
@gateway/internal/emerg/lifecycle_test.go
@gateway/internal/emerg/vast/types.go

<interfaces>
<!-- Source of helpers (extracted from). Source: PATTERNS.md table lines 16-17 + lines 99-130 -->

From gateway/internal/emerg/lifecycle.go (extracted symbols, lines from PATTERNS.md verified):
- `filterBelowCap(offers []vast.Offer, cap float64) []vast.Offer` at lifecycle.go:833-842 (Pitfall 5 epsilon cap+0.0001)
- `excludeHost(offers []vast.Offer, hostID int64) []vast.Offer` at lifecycle.go:847-859 (host_id != filter, D-A2)
- `mustEventJSON(eventType string, payload map[string]any) []byte` at lifecycle.go:865-880 (events JSONB row marshaller)
- `pgInt8(v int64) pgtype.Int8` at lifecycle.go:883-885 (sqlc BIGINT mapper)
- `pgNumericFromFloat(v float64) pgtype.Numeric` at lifecycle.go:890-897 (sqlc NUMERIC(6,4) mapper)
- `(*Reconciler).captureBreadcrumb(category string, data map[string]any)` at lifecycle.go:903-911 (Sentry breadcrumb; receiver-bound — strip receiver, take category prefix as arg)
- `(*Reconciler).bestEffortDestroy(instanceID int64)` at lifecycle.go:634-647 (30s background ctx destroy; receiver-bound — strip receiver, take VastDestroyer + log + instanceID)

vast.Offer type from gateway/internal/emerg/vast/types.go (DTO already shared; vastutil imports it but doesn't redefine).

New VastDestroyer interface (Phase 6.6 net-new minimal contract):
```go
type VastDestroyer interface {
    DestroyInstance(ctx context.Context, instanceID int64) error
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gateway/internal/vastutil/ package with 7 extracted helpers + VastDestroyer interface</name>
  <files>gateway/internal/vastutil/helpers.go</files>
  <read_first>
    - gateway/internal/emerg/lifecycle.go lines 1-22 (imports), 634-647 (bestEffortDestroy), 833-911 (5 pure helpers + receiver-bound captureBreadcrumb)
    - gateway/internal/emerg/vast/types.go (vast.Offer struct definition)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 91-132 (vastutil section spec)
  </read_first>
  <action>Criar gateway/internal/vastutil/helpers.go com `package vastutil`. Imports (mirror lifecycle.go:1-19 mas só os usados pelos 7 helpers): "context", "encoding/json", "log/slog", "math/big", "time", "github.com/getsentry/sentry-go", "github.com/jackc/pgx/v5/pgtype", "github.com/ifixtelecom/gpu-ifix/gateway/internal/emerg/vast". Definir interface `VastDestroyer { DestroyInstance(ctx context.Context, instanceID int64) error }`. Implementar 7 free functions copy-adapt verbatim dos sources listados em <interfaces>: FilterBelowCap (era filterBelowCap), ExcludeHost (era excludeHost), MustEventJSON (era mustEventJSON), PgInt8 (era pgInt8), PgNumericFromFloat (era pgNumericFromFloat). Para CaptureBreadcrumb: copiar bloco de lifecycle.go:903-911, remover receiver `(r *Reconciler)`, adicionar parâmetro `category string` (caller já passa prefix tipo "emerg." ou "primary."), corpo da função permanece chamando sentry.AddBreadcrumb com Category=category. Para BestEffortDestroy: copiar bloco de lifecycle.go:634-647, remover receiver, signature vira `func BestEffortDestroy(ctx context.Context, vastClient VastDestroyer, log *slog.Logger, instanceID int64)`, manter 30s background timeout const interno + best-effort error log (não panic).</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./internal/vastutil/...</automated>
  </verify>
  <done>vastutil package compila standalone. 7 free functions + 1 interface exportadas (capitalizadas). Zero referência a emerg.Reconciler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: vastutil unit tests — port 5 emerg helper tests + cover BestEffortDestroy/CaptureBreadcrumb</name>
  <files>gateway/internal/vastutil/helpers_test.go</files>
  <behavior>
    - TestFilterBelowCap_Epsilon: offers com DPH = cap, cap+0.00005, cap+0.001 → primeiros 2 incluídos (cap+0.0001 epsilon per Pitfall 5), terceiro excluído
    - TestExcludeHost: lista de offers com HostID variado → resultado exclui apenas matched hostID, preserva ordem dos demais
    - TestMustEventJSON: payload {"foo":"bar"} + eventType "test_event" → JSON bytes contém "event":"test_event" + "foo":"bar"
    - TestPgInt8: input 123 → pgtype.Int8{Int64:123, Valid:true}
    - TestPgNumericFromFloat: tabela com 0.0, 0.35, 0.4001, 200.0, 200.1234 → round-trip via Float64Value retorna mesmo valor within 0.0001 delta
    - TestBestEffortDestroy_CallsDestroyInstance: fake VastDestroyer captura instanceID; helper retorna sem panic mesmo se fake retornar error
    - TestCaptureBreadcrumb_NoOp_WhenNoSentryHub: sem Sentry inicializado, helper não panica (defensive coverage)
  </behavior>
  <read_first>
    - gateway/internal/emerg/lifecycle_test.go lines 11-21 (imports) + 26-118 (5 helper test patterns to replicate verbatim)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 136-176 (helpers_test.go pattern spec)
    - gateway/internal/vastutil/helpers.go (just created in Task 1 — for symbol names + signatures)
  </read_first>
  <action>Criar gateway/internal/vastutil/helpers_test.go com `package vastutil`. Imports: "encoding/json", "testing", "context", "log/slog", "io", "github.com/stretchr/testify/require", "github.com/ifixtelecom/gpu-ifix/gateway/internal/emerg/vast". Copy-adapt 5 testes de lifecycle_test.go:26-118 trocando filterBelowCap → FilterBelowCap, excludeHost → ExcludeHost, mustEventJSON → MustEventJSON, pgInt8 → PgInt8, pgNumericFromFloat → PgNumericFromFloat. Para PgNumericFromFloat usar exatamente o table-driven block citado em PATTERNS.md:156-176. Para TestBestEffortDestroy_CallsDestroyInstance criar `type fakeVastDestroyer struct{ called int64 }; func (f *fakeVastDestroyer) DestroyInstance(ctx context.Context, id int64) error { f.called = id; return nil }` + verify após BestEffortDestroy(context.Background(), &fake, slog.New(slog.NewTextHandler(io.Discard, nil)), 42), fake.called == 42. Para TestCaptureBreadcrumb_NoOp_WhenNoSentryHub: simplesmente chama CaptureBreadcrumb("test.", map[string]any{"k":"v"}) sem inicializar sentry → assert require.NotPanics(t, func() { CaptureBreadcrumb("test.", map[string]any{"k":"v"}) }).</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./internal/vastutil/... -count=1 -race</automated>
  </verify>
  <done>Todos os 7+ testes vastutil passam com -race. Coverage cobre os 7 helpers exportados.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor emerg/lifecycle.go to import vastutil — remove 7 local helper definitions + update callers</name>
  <files>gateway/internal/emerg/lifecycle.go, gateway/internal/emerg/lifecycle_test.go</files>
  <read_first>
    - gateway/internal/emerg/lifecycle.go full file (will modify references throughout)
    - gateway/internal/emerg/lifecycle_test.go lines 26-118 (5 helper tests — DECIDE: deletar (vastutil já tem) OU manter como integration smoke; recomendação: deletar para evitar duplicação, vastutil tests cobrem)
    - gateway/internal/vastutil/helpers.go (Task 1 output — para confirmar API)
  </read_first>
  <action>Modificar gateway/internal/emerg/lifecycle.go: (1) Adicionar import `"github.com/ifixtelecom/gpu-ifix/gateway/internal/vastutil"`. (2) Deletar definições locais filterBelowCap (lines 833-842), excludeHost (847-859), mustEventJSON (865-880), pgInt8 (883-885), pgNumericFromFloat (890-897), bestEffortDestroy receiver method (634-647), captureBreadcrumb receiver method (903-911). (3) Atualizar todos call sites no mesmo arquivo: `filterBelowCap(...)` → `vastutil.FilterBelowCap(...)`; `excludeHost(...)` → `vastutil.ExcludeHost(...)`; `mustEventJSON(...)` → `vastutil.MustEventJSON(...)`; `pgInt8(...)` → `vastutil.PgInt8(...)`; `pgNumericFromFloat(...)` → `vastutil.PgNumericFromFloat(...)`; `r.bestEffortDestroy(id)` → `vastutil.BestEffortDestroy(ctx, r.vastAPI(), r.deps.Log, id)` (passar context.Background() se chamado fora de um ctx — preserve current behavior); `r.captureBreadcrumb("category", data)` → `vastutil.CaptureBreadcrumb("emerg."+category, data)`. (4) emerg.VastAPI já tem DestroyInstance method → satisfaz vastutil.VastDestroyer interface implicitamente; nenhum cast explicit necessário. (5) Em lifecycle_test.go: deletar tests duplicados (TestFilterBelowCap_*, TestExcludeHost, TestMustEventJSON, TestPgInt8, TestPgNumericFromFloat) — já cobertos por vastutil; manter testes de buildCreateRequest, provisionLifecycle, podHealthURL, etc. (que dependem de helpers via emerg internals + estarão verdes pelo wrapping chamadas vastutil.*). Verify suite emerg verde fim a fim.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./internal/emerg/... ./internal/vastutil/... -count=1 -race</automated>
  </verify>
  <done>emerg package compila + todos tests passam + zero referência local aos 7 helpers em lifecycle.go (verify: grep -n "^func filterBelowCap\|^func excludeHost\|^func mustEventJSON\|^func pgInt8\|^func pgNumericFromFloat\|bestEffortDestroy(\|r\\.captureBreadcrumb" gateway/internal/emerg/lifecycle.go retorna apenas calls a vastutil.*, sem definições locais). Phase 6/6.5 regression risk: zero (behavior preservado via wrapping).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma nova trust boundary introduzida — refactor puro de código interno. emerg package continua a única consumidora de vastutil neste plan (primary será adicionado em Wave 2).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-SC | Tampering | npm/pip/cargo installs | mitigate | Refactor não adiciona deps; vastutil reusa imports já em go.mod (pgx, sentry-go, slog stdlib). RESEARCH.md §Don't Hand-Roll line 363 enforce zero new external deps. |
| (none new) | — | — | — | — |
</threat_model>

<verification>
- `cd gateway && go build ./...` succeeds
- `cd gateway && go test ./internal/vastutil/... ./internal/emerg/... -count=1 -race` all green
- `cd gateway && grep -v '^#' gateway/internal/emerg/lifecycle.go | grep -c 'func filterBelowCap\|func excludeHost\|func mustEventJSON\|func pgInt8\|func pgNumericFromFloat' == 0` (definitions removed)
- `cd gateway && grep -c 'vastutil\\.' gateway/internal/emerg/lifecycle.go >= 7` (all 7 helpers consumed via import)
</verification>

<success_criteria>
- vastutil é um pacote consumível por qualquer subsystem (emerg, primary, future)
- emerg behavior é byte-equivalent (tests provam regressão zero)
- Wave 2 plans (06.6-04..06.6-07) podem importar vastutil sem cross-import com emerg
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-02-SUMMARY.md`.
</output>
