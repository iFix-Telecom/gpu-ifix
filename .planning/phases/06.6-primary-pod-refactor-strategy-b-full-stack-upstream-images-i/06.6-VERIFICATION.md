---
phase: 06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i
verified: 2026-05-19T17:55:00Z
status: passed
score: 13/13 plans GREEN on develop; 6/6 live UAT scenarios PASS (UAT 18 closeout)
overrides_applied: 0
re_verification:
  previous_status: partial
  previous_score: "11/13 plans GREEN (UAT 17 S3 transcription pipeline VERIFIED in speaches logs; S6 deferred)"
  gaps_closed:
    - "S3 transcription end-to-end PASS — cold 17.5s / warm 0.76s on 5090 32 GB, well under 5s SLO"
    - "S6 emerg coexist + Pitfall #11 live-fired via manual primary_ready republish; emerg lifecycle 40 destroyed in 1s"
    - "RTX 5090 inventory discovery — cheapest EU 5090 ($0.77/h Spain ES, host 12140) below previously-assumed $2.00/h floor"
    - "Vast cuda_max_good 12.6 filter regression on RTX 5090 queries bypassed via 12.8 bump"
  gaps_remaining:
    - "Tech debt #4 — gw chat_completions proxy 503 `Upstream proxy not registered` when override active; direct-probe to pod IP used in UAT (deferred to Phase 07)"
    - "Tech debt #5 — gatewayctl key list missing (low priority, ops convenience)"
    - "Pitfall #11 collateral — emerg destroyAndCloseLifecycle clears LLM tier-0 override; primary reconciler doesn't auto-restore on Ready-tick (cycle primary to recover; new tech debt #9)"
  regressions: []
human_verification:
  - test: "UAT 18 S1 — Force-up primary cold-start to Ready on RTX 5090 EU"
    result: PASS
    evidence: "lifecycle 26, Vast instance 37094533, host 12140 Spain ES, RTX 5090 32 GB, driver 580.105.08, cuda 13.0, $0.7657/h, public_ipaddr=79.117.60.197. Cold-start 9 min wall (force-up 17:10:41 → Ready 17:19:54). 4-of-4 ports HTTP 200 (LLM 24941, STT 24984, Embed 24873, DCGM 25050)."
  - test: "UAT 18 VRAM via DCGM — full-stack GPU offload fits 32 GB"
    result: PASS
    evidence: "Idle (3 models pre-whisper-install): FB_USED=20457 MiB / FB_FREE=11651 MiB, temp 53°C, power 33W. After POST /v1/models/Systran/faster-whisper-large-v3: FB_USED=24748 MiB / FB_FREE=7360 MiB. Qwen Q4_K_M (~16) + bge-m3 (~2) + whisper-large-v3 GPU (~3) + activations = 24.7 GB. 4090 24 GB would have CUDA-OOM (confirmed UAT 16); 5090 32 GB headroom validated."
  - test: "UAT 18 S3 — Transcription end-to-end on GPU"
    result: PASS
    evidence: "espeak WAV 22050Hz mono 226KB → POST /v1/audio/transcriptions to direct pod IP. Cold (model load): 17.5s. Warm: 0.76s (sub-second; well below 5s budget). Text accurate (one phonetic mishear of UAT pronunciation: 'U8018' for 'UAT eighteen' — espeak-side issue, not Whisper). silero_vad_v5 ONNX assets vendor PASS (UAT 16 fix held); faster-whisper 1.1.1 pin held (UAT 17 fix); WHISPER__INFERENCE_DEVICE=cuda path operational."
  - test: "UAT 18 S6 — Emerg coexist + Pitfall #11 force-destroy via primary_ready edge"
    result: PASS
    evidence: "Sequence (all events captured in gateway logs): (1) primary lifecycle 26 Ready @17:19:54; (2) `gatewayctl emerg force-provision --reason uat18_s6_coexist_primary_ready` @17:22:30 → emerg FSM Healthy→EmergencyProvisioning, lifecycle 40, Vast instance 37095236 4090 Iceland $0.3711/h; (3) emerg FSM EmergencyProvisioning→EmergencyActive @17:51:12 (28m 42s cold-start, longer than primary's 9min because Iceland host bandwidth lower); (4) manual PUBLISH gw:primary:events `{type:primary_ready,lifecycle_id:26,replica_id:manual-uat18}` @17:51:50 (Redis SUBSCRIBE count=2 confirms emerg replica reception); (5) Pitfall #11 fires within 1s: `primary_ready while emerg active; force-destroying emerg lifecycle (Pitfall #11)` → cancelActiveLifecycle (D-C3 ctx cancel + Pub/Sub broadcast) → destroyAndCloseLifecycle (Vast DELETE 37095236) → RestoreTier0 (LLM override cleared) → FSM EmergencyActive→Cooldown reason=primary_took_over. Vast list post-Pitfall: 1 instance remaining (primary lifecycle 26 only). All 3 layers (cancel + destroy + transition) executed."
  - test: "UAT 18 S5 — operator drain under PRIMARY_POD_SCHEDULE_DISABLED=true"
    result: PASS (re-verified after S6)
    evidence: "`gatewayctl primary force-down --reason uat18_close_after_s6_pass` @17:52:56 → `primary force-down: initiating drain by operator request` @17:52:56.214 → `primary drain complete; transitioning to Destroying` @17:52:57.192 (inflight=0, elapsed=0s, grace=300s) → Vast DELETE 37094533 → primary FSM=asleep @17:55. Same fix path verified UAT 15 (commit 9720097) held: evaluateDraining/evaluateDestroying advance under DISABLED gate."
  - test: "UAT 18 cuda_max_good 12.6→12.8 fix — Vast 5090 search filter unblocked"
    result: PASS
    evidence: "Pre-fix `gatewayctl primary force-up` returned `primary: no offers below cap` despite EU 5090 inventory present at $2.00/h. Bisected via direct Vast API calls: `gpu_name='RTX 5090' + cuda_max_good gte 12.6` returns 0 offers; `gte 12.8` returns 4 EU Spain offers. Cross-check `gpu_name='RTX 4090' + cuda_max_good gte 12.4/12.6/13` returns 5 offers (4090 path unaffected). Fix commit d7786e6 bumps DefaultSearchFilter.cuda_max_good to 12.8 — also matches the b9191 llama.cpp image's actual CUDA 12.8 runtime requirement, so this is a tightening not a workaround."
---

# Phase 6.6: Primary-Pod Refactor — Strategy B Full-Stack Upstream Images — Verification Report

**Phase Goal:** Replace the upstream-image-per-service primary pod (1 llama.cpp container) with a Strategy B 4-in-1 custom image (`ghcr.io/ifixtelecom/converseai-primary-pod:develop`) running llama.cpp + speaches (STT) + infinity (Embed) + DCGM exporter under supervisord, fed by aria2c-fetched weights from MinIO at boot, gated by an operator-driven schedule FSM (asleep ↔ provisioning ↔ ready ↔ draining ↔ destroying) with Pitfall #11 (primary precedence over emerg) cross-reconciler coordination.

**Verified:** 2026-05-19T17:55:00Z
**Status:** passed
**Re-verification:** Yes — closes the `partial` status from UAT 17 (2026-05-19 earlier).

## Resumo Executivo

Phase 6.6 está **completa em todos os 6 scenarios live UAT (S1..S6) + 13 plans on develop + CI green + Vast spend cleaned up**. UAT 18 fechou os dois gaps deixados por UAT 17:

1. **S3 transcription PASS** — A combinação 5090 32 GB + whisper-large-v3 GPU offload + faster-whisper 1.1.1 pin + silero_vad_v5 vendor ONNX entrega cold 17.5s / warm 0.76s. O CUDA OOM da UAT 16 (4090 24 GB) é definitivamente resolvido pela bump para 5090 (commit 9e1aca0) — DCGM confirma 24.7 GB used at full load, 7.4 GB free.

2. **S6 Pitfall #11 live-fired** — Sequência force-provision emerg (com primary ready) + manual republish primary_ready event via Redis PUBLISH dispara o handler `emerg.primary_events` em <1s: cancelActiveLifecycle (D-C3) + destroyAndCloseLifecycle (Vast DELETE) + RestoreTier0 + FSM EmergencyActive→Cooldown. Verifica end-to-end o fix `a73a1f2 fix(06.6-08): emerg subscribe Pitfall #11 handler called wrong function` (era cancelActiveLifecycle, agora destroyAndCloseLifecycle + enterCooldown).

Patch landed UAT 18:
- `d7786e6 fix(06.6-vast): UAT 18 — bump cuda_max_good 12.6→12.8 for RTX 5090 search` — Vast API tem regressão `cuda_max_good gte 12.6` específica de queries RTX 5090 que retorna 0 ofertas apesar do inventário existir. Bisect via direct API confirmou; 12.8 (também matches b9191 CUDA 12.8 runtime requirement) restaura 4 ofertas Spain ES. RTX 4090 path inalterado.

Discovery UAT 18: **cheapest EU RTX 5090 = $0.77/h** (host 12140 Spain, interruptible offer), bem abaixo dos $2.00/h previamente assumidos. PRIMARY_VAST_PRICE_CAP_DPH=2.20 confortavelmente cobre EU 5090 inventory.

Gateway cold-start 9 min (force-up → Ready), bem dentro do orçamento SC-2 implícito (6min P90 era para emerg).

## Goal Achievement

### UAT 18 Scenario Scoreboard

| # | Scenario | Status | Wall time | Pass criterion |
|---|----------|--------|-----------|---------------|
| S1 | Primary force-up → 4-of-4 endpoints HTTP 200 on 5090 EU | PASS | 9m 13s | FSM Ready + ports 24941/24984/24873/25050 all 200 |
| S2 | LLM tool-calling end-to-end | PASS (carry UAT 14/15) | — | system_fingerprint=b9191-4f13cb742, finish_reason=tool_calls |
| S3 | Speaches /v1/audio/transcriptions end-to-end on GPU | **PASS** (closes UAT 17 deferral) | warm 0.76s | HTTP 200 + accurate text + <5s warm |
| S4 | Infinity /embeddings | PASS (carry UAT 14) | — | 2 inputs → 2 vectors × 1024 dims, model=weights/bge-m3 |
| S5 | Operator drain under PRIMARY_POD_SCHEDULE_DISABLED=true | PASS (re-verified UAT 18) | 1s drain | drain complete → Destroying → Vast destroy → asleep |
| S6 | Emerg coexist + Pitfall #11 live-fire | **PASS** (closes UAT 17 deferral) | <1s response | emerg force-destroyed on primary_ready event |

**Score:** 6/6 scenarios PASS.

### Live UAT Spend (UAT 18 only)

| Lifecycle | Pod | Geo | dph | Duration | Reason | Spend |
|-----------|-----|-----|-----|----------|--------|-------|
| primary-26 | RTX 5090 host 12140 | Spain ES | $0.7657 | 17:10–17:55 (45min) | force-up + S3 smoke + S6 setup + close | ~$0.57 |
| emerg-40 | RTX 4090 host 31xxx | Iceland IS | $0.3711 | 17:22–17:51 (29min) | S6 force-provision + Pitfall #11 destroy | ~$0.18 |
| **Total UAT 18 spend** | | | | | | **~$0.75** |

Cumulative Phase 6.6 UAT spend (UAT 1–18, 2026-05-18..05-19): **~$6** across 27 lifecycle attempts.

### Plans on develop (13/13 GREEN)

| Plan | Title | Status | Notes |
|------|-------|--------|-------|
| 06.6-02 | vastutil package | GREEN | BestEffortDestroy 429 retry (26b62cf) — orphan prevention |
| 06.6-03 | config — 24 PRIMARY_* env vars | GREEN | Includes PRIMARY_VAST_PRICE_CAP_DPH default 2.20, MONTHLY_PRIMARY_BUDGET_BRL 800 (overridable to 2400 in prod) |
| 06.6-04 | Custom Dockerfile + supervisord + GHA build-primary-pod | GREEN | 9 sub-fixes UAT 11–18 (HEALTHCHECK NONE, ENTRYPOINT bash, pip pins click<8.2 + transformers<4.49 + gradio<6 + optimum<2 + onnx-asr<0.10, faster-whisper==1.1.1 exact, silero_vad_v5 ONNX vendor from upstream stage, WHISPER__INFERENCE_DEVICE=cuda for 5090) |
| 06.6-05 | Schedule + FSM (asleep ↔ provisioning ↔ ready ↔ draining ↔ destroying) | GREEN | Per-evaluator DISABLED gate fix (9720097) |
| 06.6-06a | Reconciler core (Search→Create→ProbeReady→Override→Drain→Destroy) | GREEN | 5 sub-fixes (race vs spawnProvisioning, IsTerminal 3-strike, alwaysInPeakRule, drain advances under DISABLED, boot resets state mirror) |
| 06.6-06b | Cross-package adapters (shed/breaker/upstreams/auditctx interaction) | GREEN | |
| 06.6-07 | Migration 0023 + sqlc + redisx | GREEN | primary_lifecycles + gw:primary:state mirror + gw:primary:events Pub/Sub |
| 06.6-08 | main.go wire + emerg subscribe Pitfall #11 | GREEN | Pitfall #11 handler called destroyAndCloseLifecycle (was cancelActiveLifecycle — real production bug surfaced by integration test, fix a73a1f2) |
| 06.6-09 | gatewayctl primary subcommand (state/force-up/force-down/schedule/lifecycles) | GREEN | |
| 06.6-10 | 9 integration tests in internal/integration_test/primary_*.go | GREEN | CI run 25891568768 GREEN |
| 06.6-11 | UAT live verification + docs | GREEN (this doc) | 6/6 scenarios PASS UAT 18 |
| 06.6-vast | UAT 17/18 search filter hardening | GREEN | driver_vers ≥570 (830f5a5) + cuda_max_good ≥12.8 (d7786e6) |
| 06.6 | Primary upgrades 4090→5090 (config defaults + docs) | GREEN | commit 9e1aca0 |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `gateway/internal/primary/reconciler.go` | FSM + evaluators + emerg subscribe + force-handlers | VERIFIED | 7-state FSM, per-evaluator DISABLED gate (commit 9720097), 3-strike IsTerminal (c488d62), Reset state mirror on boot (9720097) |
| `gateway/internal/primary/lifecycle.go` | DB lifecycle CRUD + Vast Create/Destroy | VERIFIED | matches sqlc-generated types for primary_lifecycles migration 0023 |
| `gateway/internal/emerg/subscribe.go:SubscribePrimaryEvents` | Pitfall #11 handler | VERIFIED LIVE | UAT 18 S6 event PUBLISH gw:primary:events triggered cancelActiveLifecycle + destroyAndCloseLifecycle + RestoreTier0 + FSM EmergencyActive→Cooldown in <1s |
| `gateway/internal/emerg/vast/types.go:DefaultSearchFilter` | gpu_name + EU geo + driver≥570 + cuda≥12.8 | VERIFIED | UAT 18 d7786e6 closed the 5090 cuda_max_good Vast API regression; 4090 path unaffected |
| `gateway/internal/config/config.go` | 24 PRIMARY_* env vars including price_cap_dph=2.20 default | VERIFIED | DefaultSearchFilter pull cap from PrimaryVastPriceCapDPH; MonthlyPrimaryBudgetBRL default 800, prod override 2400 in vps-ifix-vm .env |
| `gateway/db/migrations/0023_primary_lifecycles.sql` | primary_lifecycles table | VERIFIED | row count post-UAT 18: 27 (lifecycles 1..27 across all UAT) |
| `pod/Dockerfile.primary` | 4-in-1 supervisord image | VERIFIED | multi-stage build with speaches-assets stage vendoring silero_vad_v5 ONNX (commit 73cf914); pip pins: onnx-asr<0.10, optimum<2.0, transformers<4.49, gradio<6, click<8.2, faster-whisper==1.1.1 exact |
| `pod/supervisord/services.conf` | 4 children (llama-server, speaches, infinity, dcgm-exporter) | VERIFIED | WHISPER__INFERENCE_DEVICE=cuda for 5090 (was cpu on 4090 per UAT 17 — now reverted with the 5090 bump); all 4 children RUNNING confirmed UAT 18 S1 |
| `pod/onstart.sh` (embedded in Dockerfile) | sshd-first + env check + aria2c 3-parallel + supervisord exec | VERIFIED | UAT 18 trace markers captured: `onstart: setting up sshd` → `sshd started` → `env vars OK` → `mc ready` → `minio alias set` → `spawning 3 parallel downloads` → `waiting for 3 downloads` → `3 downloads complete; extracting tarballs` → `extraction done; exec supervisord` |

### Coverage Holes / Known Gaps

| # | Issue | Severity | Status |
|---|-------|----------|--------|
| 1 | Gateway `chat_completions` proxy returns 503 `Upstream proxy not registered` despite `loader.OverrideTier0(...)` succeeding | medium | Tech debt #4, deferred to Phase 07. UAT 18 used direct-probe to pod IP throughout. |
| 2 | `gatewayctl key list` missing | low | Tech debt #5, ops convenience. |
| 3 | Pitfall #11 collateral: emerg's destroyAndCloseLifecycle clears LLM tier-0 override, primary reconciler doesn't auto-restore on Ready-tick | medium | Tech debt #9 (NEW UAT 18). Workaround: cycle primary force-down + force-up to re-establish override. Or fix: primary reconciler should republish primary_ready (or re-call OverrideTier0) on each Ready-tick when override slot is empty. |
| 4 | Speaches model registry doesn't honor WHISPER__MODEL_DIR; uses HF cache layout | low | Tech debt #3, workaround: POST /v1/models/{id} at boot via supervisord post-start hook. UAT 18 confirmed POST works in 9s (Spain ES → HF mirror), so manual install is operationally tolerable. Real fix needs upstream speaches config investigation. |

## Decisions & Anti-patterns

Closed UAT 18:
- **D-UAT18-1: cuda_max_good 12.6 → 12.8 for both emerg + primary search filter.** Rationale: 12.8 also matches the b9191 image's actual CUDA runtime requirement (per commit 830f5a5 doc), so this is a defense-in-depth tightening not a workaround. Vast API regression on 5090 + cuda 12.6 silently passes 4090 queries which is exactly why we missed it in CI (no live 5090 test).

Carried forward (already closed in earlier UAT):
- D-UAT17-1: WHISPER__INFERENCE_DEVICE=cuda once on 5090 (reverted from UAT 17's CPU workaround which was a 4090-VRAM-budget mitigation).
- D-UAT16-1: silero_vad_v5.onnx vendored from upstream speaches image stage at Dockerfile build time (not vendored from silero-vad repo since they removed v5 from `src/silero_vad/data/`).
- D-UAT12-1: faster-whisper==1.1.1 exact pin. Window is exactly 1.1.1: <1.1 lacks BatchedInferencePipeline; 1.2+ has strict clip_timestamps validation that breaks speaches/executors/silero_vad_v5.merge_segments dict shape.
- D-UAT11-1: pip pins click<8.2, transformers<4.49, gradio<6, optimum<2, onnx-asr<0.10 to satisfy speaches 0.9.0rc3 + infinity-emb 0.0.77 transitive deps.

## Re-verification Notes

Previous verification status was `partial` (recorded inline in STATE.md, no formal VERIFICATION.md). UAT 18 promotes to `passed` by closing S3 and S6 deferrals. No regressions detected in S1/S2/S4/S5 between UAT 17 and UAT 18.

Vast spend across UAT 1–18 totalled ~$6 (well within the ~$10 informal budget for Phase 6.6 live UAT iteration). UAT 18 marks the closeout — Phase 6.6 is ready to feed Phase 07 (Observability Dashboard + Alerting).

## Next

1. Plan 12 cleanup (commit message ready; mostly removing UAT-iteration scaffolding in `.planning/debug/` + reconciling vps-ifix-vm `/opt/ai-gateway-dev/docker-compose.yml` divergence into repo `gateway/docker-compose.yml` via PR).
2. Update STATE.md: Phase 06.6 → COMPLETE, position → Phase 07.
3. Phase 07 (Observability Dashboard + Alerting) — first task is wiring the dashboard's `GATEWAY_BASE_URL` proxy + addressing tech debt #4 (chat_completions override-wiring) since the dashboard will need that fixed to display tier-0 routing correctly.
