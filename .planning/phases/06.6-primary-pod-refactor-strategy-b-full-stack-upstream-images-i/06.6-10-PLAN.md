---
phase: 06.6
plan: 10
type: execute
wave: 4
depends_on: [06.6-06, 06.6-07, 06.6-08, 06.6-09]
files_modified:
  - gateway/internal/integration_test/primary_probe_test.go
  - gateway/internal/integration_test/primary_leader_test.go
  - gateway/internal/integration_test/primary_cancel_inflight_test.go
  - gateway/internal/integration_test/primary_emerg_coexist_test.go
  - gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go
  - gateway/internal/primary/export_test.go
autonomous: true
requirements: []
tags: [integration, wave-4, tests, primary-pod, pitfall-11, leader-election, cli-db-coverage]

must_haves:
  truths:
    - "5 novos arquivos de integration test em gateway/internal/integration_test/ (4) + gateway/cmd/gatewayctl/ (1) usando build tag `//go:build integration` (parity emerg_*_test.go pattern)"
    - "primary_probe_test.go cobre Plan 06.6-06 markReady + 3-role OverrideTier0 + DCGMScraper.SetURL via testcontainers Postgres + miniredis"
    - "primary_leader_test.go cobre 2-replicas-1-leader scenario via redsync (parity emerg_leader_test.go)"
    - "primary_cancel_inflight_test.go cobre Layer 1+2+3 cancel-in-flight triple-layer via fake Vast + miniredis Pub/Sub; usa export_test.go test-only setter para schedule rule mutation (WARNING 4 revision — ScheduleRule é immutable em produção)"
    - "primary_emerg_coexist_test.go cobre Pitfall #11 (RESEARCH.md:546-555): start both reconcilers; emerg in StateEmergencyActive; primary fires primary_ready; emerg cancelActiveLifecycle called within 30s — NO ANALOG (PATTERNS.md:45) — net-new Phase 6.6 risk coverage"
    - "primary_lifecycles_integration_test.go cobre gatewayctl primary lifecycles DB fetch path via testcontainers Postgres + apply migration 0023 + INSERT 3 rows + invoke runPrimaryLifecycles + assert stdout has 3 data rows (per BLOCKER 3 revision — closes coverage gap from Plan 09 Task 2's t.Skip integration test)"
    - "export_test.go em gateway/internal/primary/ expõe test-only setter `SetScheduleRuleForTest(t *testing.T, r *Reconciler, rule ScheduleRule)` para safely mutate ScheduleRule em integration tests SEM violar production immutability invariant (WARNING 4 revision)"
    - "Todos 5 testes rodam via `go test -tags=integration ./internal/integration_test/... ./cmd/gatewayctl/...`"
  artifacts:
    - path: "gateway/internal/integration_test/primary_probe_test.go"
      provides: "markReady + 3-role tier-0 override + DCGM SetURL E2E coverage"
      contains: "OverrideTier0|primaryPodURLs|DCGMScraper\\.SetURL"
    - path: "gateway/internal/integration_test/primary_leader_test.go"
      provides: "Leader-election 2-replicas-1-leader via redsync"
      contains: "redsync|isLeader|NewPrimaryRedsync"
    - path: "gateway/internal/integration_test/primary_cancel_inflight_test.go"
      provides: "Triple-layer cancel-in-flight coverage"
      contains: "cancelActiveLifecycle|cancel_in_flight|BestEffortDestroy|SetScheduleRuleForTest"
    - path: "gateway/internal/integration_test/primary_emerg_coexist_test.go"
      provides: "Pitfall #11 coexistence race resolved"
      contains: "primary_ready|StateEmergencyActive|primary_took_over"
    - path: "gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go"
      provides: "gatewayctl primary lifecycles DB-fetch CLI coverage (BLOCKER 3)"
      contains: "TestRunPrimaryLifecycles_FetchesFromDB|testcontainers|migration 0023|primary_lifecycles"
    - path: "gateway/internal/primary/export_test.go"
      provides: "Test-only ScheduleRule setter (WARNING 4)"
      contains: "SetScheduleRuleForTest"
  key_links:
    - from: "primary_emerg_coexist_test.go"
      to: "emerg.Reconciler.SubscribePrimaryEvents (Plan 06.6-08)"
      via: "miniredis Pub/Sub event flow"
      pattern: "PrimaryEvent\\{.*Type:.*primary_ready"
    - from: "primary_lifecycles_integration_test.go"
      to: "gateway/cmd/gatewayctl/primary.go runPrimaryLifecycles (Plan 06.6-09 Task 1)"
      via: "direct function invocation with testcontainers DB"
      pattern: "runPrimaryLifecycles"

threat_refs: [T-06.6-04]
---

<objective>
Adicionar 5 integration tests cobrindo paths que unit tests não conseguem (cross-process interactions: testcontainers Postgres + miniredis Pub/Sub + redsync leader election + cross-package emerg+primary coexistence + gatewayctl CLI DB-fetch). Per BLOCKER 3 revision: o 5º teste (primary_lifecycles_integration_test.go) move o coverage do Plan 09 Task 2's t.Skip integration test para este plan, fechando o gap zero-coverage da DB-fetch CLI path. Per WARNING 4 revision: adicionar export_test.go com test-only ScheduleRule setter para evitar mutation of immutable production type em tests.

Purpose: Mitigation final dos cenários reais antes do live UAT em Plan 06.6-11. Sem estes tests, Pitfall #11 (emerg + primary race) só seria detectado em produção. Leader-election bug poderia causar double-provision silenciosamente. Cancel-in-flight Layer 2 (Pub/Sub) só verificável via real Redis Pub/Sub. gatewayctl primary lifecycles DB-fetch path teria zero automated coverage (operator-only manual smoke).

Output: 6 arquivos novos (4 integration tests + 1 CLI integration test + 1 export_test.go). Plan 06.6-11 (Live UAT) depende destes — se algum falha, NÃO escala para Vast.ai live spend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@gateway/internal/integration_test
@gateway/internal/primary/reconciler.go
@gateway/internal/emerg/subscribe.go
@gateway/internal/redisx/primary.go
@gateway/internal/redisx/emerg.go
@gateway/cmd/gatewayctl/primary.go
@gateway/cmd/gatewayctl/emerg_test.go

<interfaces>
<!-- Existing emerg integration tests at gateway/internal/integration_test/emerg_*.go -->

Existing test files (analog to mirror):
- emerg_provision_happy_test.go: testcontainers Postgres + miniredis + fake Vast + assert markHealthy + tier-0 override + DB insert
- emerg_leader_test.go: 2 reconcilers + redsync + assert only one wins lock
- emerg_cancel_pre_create_test.go + emerg_cancel_post_create_test.go: cancelActiveLifecycle Layer 1+2+3 coverage

Note: gateway/cmd/gatewayctl/emerg_test.go (lines 1-40) explicitly states "The DB-backed `lifecycles` subcommand is exercised via flag parsing only here — the full SQL round-trip is integration-test territory (testcontainers, deferred to CI per Plan 06-09 convention)." For primary, we close that gap in THIS plan (BLOCKER 3 revision).

Build tag pattern (first 2 lines of each integration test):
```go
//go:build integration
// +build integration
```

Test runner command:
```bash
cd gateway && go test -tags=integration ./internal/integration_test/... ./cmd/gatewayctl/... -count=1 -timeout=15m
```

<!-- 5 new test files specs (PATTERNS.md lines 42-51 + RESEARCH.md §Pitfall #11 + BLOCKER 3 revision) -->

1. primary_probe_test.go (POD-03 + Phase 5 DCGM wiring):
   - Setup: testcontainers Postgres (with migration 0023 applied) + miniredis + fake VastAPI returning offer with mapped ports 8000/tcp→33000, 8001/tcp→33001, 8002/tcp→33002, 9400/tcp→33400 + fake HealthCheck returning true for all 4 endpoints + real upstreams.Loader + real dcgm.Scraper
   - Action: construct primary.Reconciler + manually advance FSM Asleep→Provisioning + call markReady(ctx, lifecycleID=42, urls{LLM:"http://host:33000", STT:"http://host:33001", ...}, dph=0.40)
   - Assert: Loader.tier0Override["llm"].Load() == "http://host:33000"; same for stt/embed; dcgmScraper internal url field == "http://host:33400"; SQL row in primary_lifecycles has first_health_pass_at populated; FSM.State() == StateReady

2. primary_leader_test.go (parity emerg_leader_test):
   - Setup: 2 primary.Reconciler instances sharing same miniredis + Postgres
   - Action: start both reconcilers (go r1.Start(ctx) + go r2.Start(ctx))
   - Assert: within 5s, exactly 1 has isLeader.Load() == true; other has isLeader == false

3. primary_cancel_inflight_test.go (analog emerg_cancel_post_create) — WARNING 4 revision:
   - Setup: testcontainers + miniredis + fake VastAPI with CreateInstance returning ID=999; HealthCheck returning false (never marks ready); cold-start budget cfg.PrimaryProvisionColdStartBudgetSeconds=3 (short for test)
   - Action: advance FSM Asleep→Provisioning + spawn provisionLifecycle goroutine + after 1s, **construct fresh ScheduleRule with Days[currentWeekday]=false** (NEW rule object, NOT mutating original — ScheduleRule is immutable) + call `primary.SetScheduleRuleForTest(t, reconciler, freshRule)` (export_test.go test-only setter — replaces the whole r.rule reference atomically) → trigger evaluateTick → triggers cancelActiveLifecycle
   - Assert: Layer 1 ctx.Done() fires (goroutine exits); Layer 2 miniredis Pub/Sub published 1 event with Type="cancel_in_flight"; Layer 3 fakeVast.destroyed contains 999 (BestEffortDestroy called)

4. primary_emerg_coexist_test.go (Pitfall #11 NEW; PATTERNS.md:45):
   - Setup: testcontainers + miniredis + fake VastAPI + 2 reconcilers: emerg.Reconciler (with FSM=StateEmergencyActive — manually seeded) + primary.Reconciler (with FSM=StateProvisioning → markReady fires)
   - Action: spawn emerg.SubscribePrimaryEvents goroutine (Plan 06.6-08); primary publishes PrimaryEvent{Type:"primary_ready"} via PublishPrimaryEvent
   - Assert: within 5s, emerg.cancelActiveLifecycle called → fakeVast.destroyed contains the emerg's instance ID; emerg.FSM.State() transitions away from EmergencyActive

5. primary_lifecycles_integration_test.go (BLOCKER 3 revision — closes coverage gap from Plan 09 Task 2 t.Skip):
   - Setup: testcontainers Postgres + apply gateway/db/migrations/0023_primary_lifecycles.sql (Plan 06.6-07 created) + INSERT 3 rows com started_at staggered (now-3h, now-2h, now-1h), trigger_reason values varying, total_cost_brl populated, shutdown_reason mix of "drain_complete"/"schedule_window_exited"/null
   - Action: capture os.Stdout to bytes.Buffer; invoke runPrimaryLifecycles(ctx, []string{"--limit", "10"}, log) directly (Plan 06.6-09 Task 1 function); capture exit code
   - Assert: exit code == 0; stdout contains 3 data rows; output ordered by started_at DESC (most recent first); each row contains expected columns (id, started_at, trigger_reason, total_cost_brl)
   - Note: this validates the full DB round-trip Plan 09 Task 2 t.Skip'd — analog to what gateway/cmd/gatewayctl/emerg_test.go intentionally defers per its module-level comment lines 14-19

6. gateway/internal/primary/export_test.go (WARNING 4):
   - `package primary` + provides `SetScheduleRuleForTest(t *testing.T, r *Reconciler, rule ScheduleRule)` that does `t.Helper(); r.rule = rule` (direct field assign — works because export_test.go is in the same package and tests inherit unexported field access; the t.Helper requirement signals this is test-only)
   - Alternative if r.rule cannot be directly assigned (e.g., it's stored in atomic.Pointer): expose via `r.ruleAtomic.Store(&rule)` — choose pattern matching the actual Plan 06.6-06 reconciler.go implementation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: export_test.go + primary_probe_test.go + primary_leader_test.go</name>
  <files>gateway/internal/primary/export_test.go, gateway/internal/integration_test/primary_probe_test.go, gateway/internal/integration_test/primary_leader_test.go</files>
  <behavior>
    - export_test.go: package primary + `func SetScheduleRuleForTest(t *testing.T, r *Reconciler, rule ScheduleRule) { t.Helper(); r.rule = rule }` (or atomic.Pointer store variant matching reconciler.go field type). NOT exported via build tag — just `_test.go` suffix means it's only compiled in test mode.
    - primary_probe_test.go (1 test): TestPrimaryProbe_MarkReady_OverridesTier03Roles — setup full stack (testcontainers Postgres + migration 0023 + miniredis + real Loader + real dcgm.Scraper + fake VastAPI + fake HealthCheck always-true); call reconciler.markReady manually; assert 3 OverrideTier0 calls (one per role) + DCGMScraper.SetURL fired + DB row marked healthy
    - primary_leader_test.go (1 test): TestPrimaryLeader_OnlyOneActiveLeader — 2 reconcilers; Start both; within 5s assert exactly 1 isLeader; kill leader → other promotes within next 30s (redsync TTL expires)
  </behavior>
  <read_first>
    - gateway/internal/integration_test/emerg_provision_happy_test.go (full file — testcontainers + miniredis harness pattern)
    - gateway/internal/integration_test/emerg_leader_test.go (full file — redsync leader-election test pattern)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 42-44 (integration test specs)
    - gateway/internal/primary/reconciler.go (Plan 06.6-06 — markReady signature; Start; isLeader field; AND confirm r.rule field type for export_test.go setter)
  </read_first>
  <action>(a) Criar gateway/internal/primary/export_test.go com `package primary` + import "testing" + `func SetScheduleRuleForTest(t *testing.T, r *Reconciler, rule ScheduleRule) { t.Helper(); r.rule = rule }`. Se Plan 06.6-06 implementou r.rule como atomic.Pointer[ScheduleRule] (não direct field), ajustar para `r.rule.Store(&rule)` ou similar. Comentário acima do func: `// SetScheduleRuleForTest é test-only helper para safely substituir r.rule em integration tests. // Production code NUNCA chama esta function — ScheduleRule é immutable em produção. // Per checker WARNING 4 revision (2026-05-17).`. (b) Criar primary_probe_test.go com `//go:build integration` + `// +build integration` header + `package integration_test`. Imports: stdlib + testcontainers-go + miniredis + testify/require + primary + redisx + upstreams + dcgm + config. Helper `setupPrimaryTestStack(t *testing.T) (...)` que retorna pgxpool + miniredis client + Loader + Scraper + fake VastAPI. Test usa helper + invoca markReady; usa `l, _ := loader.Tier0Override("llm"); require.Equal(t, "http://host:33000", l.Load())` (assuming `Tier0Override(role)` is a public test helper — if not, expose via internal export_test.go pattern). (c) Criar primary_leader_test.go análogo emerg_leader_test.go com Reconciler instâncias 2x compartilhando same miniredis. Para leader promotion após kill: cancelar ctx1 do leader + esperar ~30s redsync TTL → require.Eventually(t, func() bool { return r2.IsLeader() }, 60s, 1s).</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./internal/primary/... && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=5m -run TestPrimaryProbe -run TestPrimaryLeader</automated>
  </verify>
  <done>2 integration tests verdes + export_test.go compila. Build tag isolation funciona (sem -tags=integration os arquivos não são compilados). SetScheduleRuleForTest disponível para Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: primary_cancel_inflight_test.go + primary_emerg_coexist_test.go (Pitfall #11)</name>
  <files>gateway/internal/integration_test/primary_cancel_inflight_test.go, gateway/internal/integration_test/primary_emerg_coexist_test.go</files>
  <behavior>
    - primary_cancel_inflight_test.go (1 test): TestPrimaryCancelInflight_TripleLayer — Setup com fakeVast CreateInstance returns 999; HealthCheck always false; cold-start budget cfg=3s; reconciler advances Asleep→Provisioning; após 1s, **construct fresh ScheduleRule** (NEW object, not mutation) com Days[currentWeekday]=false + chamar `primary.SetScheduleRuleForTest(t, reconciler, freshRule)` (atomically replaces r.rule reference) + trigger evaluateTick → assert ctx.Done fires + 1 Pub/Sub event published com Type="cancel_in_flight" + fakeVast.destroyed contains 999
    - primary_emerg_coexist_test.go (1 test): TestEmergCoexist_PrimaryReadyForceDestroysEmerg — Setup com 2 reconcilers; emerg seeded em StateEmergencyActive com fakeVast.activeID=888; spawn emerg.SubscribePrimaryEvents(ctx); primary publishes PrimaryEvent{Type:"primary_ready", LifecycleID:42}; require.Eventually(t, func() bool { return emerg.FSM.State() != StateEmergencyActive && contains(fakeVast.destroyed, 888) }, 30s, 500ms)
  </behavior>
  <read_first>
    - gateway/internal/integration_test/emerg_cancel_post_create_test.go (analog cancel-in-flight pattern with 3 layers)
    - gateway/internal/primary/reconciler.go (cancelActiveLifecycle implementation)
    - gateway/internal/primary/export_test.go (Task 1 output — SetScheduleRuleForTest signature)
    - gateway/internal/primary/schedule.go (Plan 06.6-05 — ScheduleRule struct shape, confirm Days field type [7]bool ou map[string]bool)
    - gateway/internal/emerg/subscribe.go (Plan 06.6-08 added SubscribePrimaryEvents)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md §Pitfall #11 (lines 545-555)
  </read_first>
  <action>Criar primary_cancel_inflight_test.go com mesmo header build tag. Test usa fakeVast.CreateInstance returning ID 999 + fakeHealthCheck always false. Helper para listen miniredis Pub/Sub gw:primary:events into buffered channel. Action: r.startProvisioning(ctx) → after 1s in Provisioning, **construct fresh rule via:** `originalRule := r.GetScheduleRuleForTest()  // OR read r.rule directly via integration test helper; var freshRule = originalRule  // value copy; freshRule.Days[int(time.Now().Weekday())] = false  // mutate the COPY, original untouched; primary.SetScheduleRuleForTest(t, r, freshRule)` → r.evaluateTick(ctx, time.Now(), log) triggers cancelActiveLifecycle. Assert via 3 channels separados: (1) ctx of provisioning goroutine cancelled (via WithCancel sentinel), (2) Pub/Sub event count == 1 with Type="cancel_in_flight", (3) fakeVast.destroyed slice contém 999. Criar primary_emerg_coexist_test.go análogo. Setup: testcontainers Postgres + emerg + primary; manually set emerg FSM via testing helper SetState(StateEmergencyActive); spawn `go emerg.SubscribePrimaryEvents(ctx)`. Then primary publishes redisx.PublishPrimaryEvent(ctx, miniredis, PrimaryEvent{Type:"primary_ready"}). require.Eventually para emerg state transition + destroy call.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=10m -run TestPrimaryCancelInflight -run TestEmergCoexist</automated>
  </verify>
  <done>2 integration tests verdes. Triple-layer cancel-in-flight provado mecanicamente SEM violar ScheduleRule immutability. Pitfall #11 mitigation provado mecanicamente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: primary_lifecycles_integration_test.go — gatewayctl primary lifecycles DB-fetch CLI coverage (BLOCKER 3)</name>
  <files>gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go</files>
  <behavior>
    - TestRunPrimaryLifecycles_FetchesFromDB: testcontainers Postgres started + apply migration 0023 (gateway/db/migrations/0023_primary_lifecycles.sql) + INSERT 3 rows com staggered started_at + varied trigger_reason + populated total_cost_brl + mix shutdown_reason; redirect os.Stdout → bytes.Buffer; invoke `runPrimaryLifecycles(ctx, []string{"--limit", "10"}, log)` (Plan 06.6-09 Task 1 function); read buffer; assert exit code == 0 + stdout contains 3 data rows + rows ordered by started_at DESC + each contains expected columns (id, started_at, trigger_reason, total_cost_brl, shutdown_reason)
    - TestRunPrimaryLifecycles_RespectsLimitFlag: INSERT 5 rows; invoke with `--limit 2`; assert only 2 rows in stdout
    - TestRunPrimaryLifecycles_EmptyTable_NoRows: empty primary_lifecycles table; invoke; assert exit code 0 + stdout shows headers only (no data rows)
  </behavior>
  <read_first>
    - gateway/cmd/gatewayctl/emerg_test.go (full file — note module-level comment lines 14-19 that explicitly defers DB-fetch CLI test to integration territory; THIS plan closes the analog gap for primary)
    - gateway/cmd/gatewayctl/primary.go (Plan 06.6-09 Task 1 output — runPrimaryLifecycles function signature + flag.IntVar limit handling)
    - gateway/db/migrations/0023_primary_lifecycles.sql (Plan 06.6-07 — schema to apply in testcontainers)
    - gateway/db/queries/primary_lifecycles.sql.go (Plan 06.6-07 sqlc generated — for direct INSERT helper if needed)
    - gateway/internal/integration_test/emerg_provision_happy_test.go (testcontainers Postgres harness pattern — adapt for cmd/gatewayctl/ package context)
  </read_first>
  <action>Criar gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go com `//go:build integration` + `// +build integration` headers + `package main`. Imports: "bytes", "context", "io", "log/slog", "os", "testing", "time", testcontainers-go (testcontainers/postgres module ou pgx/pgxpool direct connection), pgxpool, testify/require. Helper `setupTestPostgresWithPrimaryLifecycles(t *testing.T) *pgxpool.Pool`: start testcontainers postgres container; run migration 0023 via raw SQL exec; return pgxpool.Pool conectado. Helper `seedPrimaryLifecycles(t *testing.T, pool *pgxpool.Pool, n int)`: INSERT n rows com staggered timestamps (each one 1h apart, going back from now). Helper `captureStdout(t *testing.T, fn func()) string`: redirect os.Stdout via os.Pipe trick (analog emerg_test.go pattern) — buffer + return string. Test 1 (TestRunPrimaryLifecycles_FetchesFromDB): setup pool + seed 3 rows + set env `t.Setenv("DATABASE_URL", pool's connection string)` so config.Load() (called inside runPrimaryLifecycles) picks up the test DB + invoke runPrimaryLifecycles via captureStdout + require.Contains(stdout, expectedRowMarkers). Test 2 (TestRunPrimaryLifecycles_RespectsLimitFlag): seed 5 rows + invoke com args `[]string{"--limit", "2"}` + require.Equal(2, countDataRows(stdout)). Test 3 (TestRunPrimaryLifecycles_EmptyTable_NoRows): zero seed + invoke + assert "no lifecycles found" message OR header-only stdout. Documentar no top-of-file comment: `// Phase 6.6 — closes BLOCKER 3 coverage gap from Plan 09 Task 2's t.Skip integration test. // Analog to what gateway/cmd/gatewayctl/emerg_test.go lines 14-19 intentionally defers.`. **NOTA importante**: depende da existência de Plan 06.6-09 (que cria gateway/cmd/gatewayctl/primary.go runPrimaryLifecycles function) — depends_on declared in frontmatter includes 06.6-09. Plan 06.6-09 Task 2's t.Skip comment deve ser updated em Plan 09 revisão para apontar para este test file (`// TODO: integration test moved to gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go per Plan 06.6-10 BLOCKER 3 revision`).</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -tags=integration ./cmd/gatewayctl/... -count=1 -timeout=10m -run TestRunPrimaryLifecycles</automated>
  </verify>
  <done>3 integration tests verdes. DB-fetch CLI path coberta mecanicamente — operator não precisa mais manual smoke pós-deploy para confirmar que `gatewayctl primary lifecycles` consegue ler from primary_lifecycles. BLOCKER 3 resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(Integration tests run in CI; same trust boundaries as unit tests; no new prod surface.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-04 | DoS | emerg + primary double-provision race | mitigate | primary_emerg_coexist_test.go provides mechanical proof of Pitfall #11 mitigation via integration test. Without this test, Option B (primary precedence) is only architecturally specified, not verified at runtime. |
</threat_model>

<verification>
- `cd gateway && go test -tags=integration ./internal/integration_test/... ./cmd/gatewayctl/... -count=1 -timeout=15m -run TestPrimary -run TestEmergCoexist -run TestRunPrimaryLifecycles` all green
- `cd gateway && grep -c '//go:build integration' gateway/internal/integration_test/primary_*.go == 4` (all 4 integration test files have build tag)
- `cd gateway && grep -q 'SetScheduleRuleForTest' gateway/internal/primary/export_test.go` (test-only setter exists)
- `cd gateway && grep -q '//go:build integration' gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go` (CLI integration test has build tag)
- `cd gateway && ! grep -q 'r\\.rule\\.Days\\[.*\\]\\s*=\\s*false' gateway/internal/integration_test/primary_cancel_inflight_test.go` (NO direct mutation of ScheduleRule.Days — WARNING 4 compliance)
</verification>

<success_criteria>
- 5 integration tests cover paths that unit tests can't (cross-process, multi-replica, Pub/Sub, CLI DB-fetch)
- Pitfall #11 is mechanically proven via primary_emerg_coexist_test.go
- Cancel-in-flight triple-layer fully covered (Layer 1 ctx cancel, Layer 2 Pub/Sub, Layer 3 BestEffortDestroy) WITHOUT violating ScheduleRule immutability (WARNING 4)
- Leader-election prevents double-provision (mechanical proof)
- gatewayctl primary lifecycles DB-fetch path covered (BLOCKER 3 — Plan 09 t.Skip closure)
- Plan 06.6-11 live UAT can proceed knowing these failure modes are tested
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-10-SUMMARY.md`.
</output>
