---
phase: 06.6
plan: 12
type: execute
wave: 6
depends_on: [06.6-11]
files_modified:
  - pod/onstart.sh
  - pod/docker-compose.yml
  - pod/health-bridge
  - pod/scripts/download-weights.sh
  - pod/README.md
  - .github/workflows/smoke.yml
autonomous: true
requirements: []
tags: [cleanup, pr3, wave-6, delete, post-uat]

must_haves:
  truths:
    - "pod/onstart.sh (137 lines) DELETED — substituído por gateway/internal/primary/onstart.go primaryOnstartHead raw-string Go const (Plan 06.6-04)"
    - "pod/docker-compose.yml (183 lines) DELETED — substituído por inline docker run blocks dentro de primaryOnstartHead bash script (RESEARCH §Onstart Script Anatomy)"
    - "pod/health-bridge/ subdir (entire tree) DELETED — droped per D-05; gateway Phase 3 probe loop (ProbeIntervalSeconds=10) substituiu — D-05 traceability: health-bridge service no longer exists in repo"
    - "pod/scripts/download-weights.sh DELETED — consumido apenas pelo pod/onstart.sh (deletado); inline `mc cp` block agora vive em primaryOnstartHead"
    - "pod/README.md REWRITE para 1 paragraph pointer redirecting para gateway/docs/RUNBOOK-PRIMARY-POD.md (referências antigas a Dockerfile + build-pod.yml já obsoletas em Phase 6 PR2 commit 064702e)"
    - ".github/workflows/smoke.yml DELETED — Phase 1 vestige tied to deleted artifacts (lines 163 `--onstart pod/onstart.sh`, 197 `cp pod/docker-compose.yml`, 199 `cp pod/onstart.sh`, 200 `cp pod/scripts/download-weights.sh`, 213 `pod/onstart.sh` invocation). Strategy B primary pod is now reconciler-driven (gateway/internal/primary + gatewayctl primary force-up CLI); the GHA-driven smoke flow cannot survive deletion of its dependencies. UAT moves to Plan 06.6-11 live operator-run script."
    - "Files KEPT: pod/scripts/upload-weights.sh (operator weight rotation pipeline ainda usado), pod/templates/qwen3.5-27b-tool-calling.jinja (still consumed by emerg pod), pod/smoke/ (Phase 1 smoke fixtures reused for UAT POD-07 VRAM validation), pod/scripts/vast-ai.sh (operator manual provisioning fallback)"
    - "git diff stats: ~700-900 lines deleted (PATTERNS.md:895 estimate) + ~290 lines from smoke.yml; zero gateway/ regression (verified by Plan 06.6-10 integration test suite green)"
  artifacts:
    - path: "pod/README.md"
      provides: "Redirect pointer to RUNBOOK-PRIMARY-POD.md"
      contains: "RUNBOOK-PRIMARY-POD.md|gateway/docs"
  key_links:
    - from: "pod/README.md"
      to: "gateway/docs/RUNBOOK-PRIMARY-POD.md"
      via: "markdown link"
      pattern: "gateway/docs/RUNBOOK-PRIMARY-POD\\.md"

threat_refs: []
---

<objective>
PR3 cleanup — burnt-bridge final delete dos artefatos do Phase 1 primary pod arch (HOST-mode docker-compose + custom onstart bash + health-bridge Go service + GHA smoke workflow) que foram substituídos pela Phase 6.6 Strategy B Full-Stack architecture. Este plan corre APENAS APÓS Plan 06.6-11 live UAT GREEN, mirror Phase 6 PR2 sequence (PR1 inline → PR2 delete pós-UAT).

Purpose: Sem este cleanup, o repo carrega dual-implementation arch (legacy pod/ + new gateway/internal/primary/) + um workflow `.github/workflows/smoke.yml` que referencia arquivos deletados (CI quebra silenciosamente quando alguém tentar workflow_dispatch). Phase 6.6 termina apenas quando legacy é removido E quebra de CI é prevenida.

Output: 5 files/subdirs deleted (4 pod/ + 1 .github/workflows/) + 1 file rewritten (pod/README.md). ~1000-1200 lines deleted no diff. ZERO impact runtime (Plan 06.6-08 main.go já é o único path em produção; UAT Plan 06.6-11 confirma).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@pod/onstart.sh
@pod/docker-compose.yml
@pod/health-bridge
@pod/scripts/download-weights.sh
@pod/scripts/upload-weights.sh
@pod/README.md
@.github/workflows/smoke.yml
@gateway/docs/RUNBOOK-PRIMARY-POD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete pod/onstart.sh + pod/docker-compose.yml + pod/health-bridge/ + pod/scripts/download-weights.sh + .github/workflows/smoke.yml</name>
  <files>pod/onstart.sh, pod/docker-compose.yml, pod/health-bridge, pod/scripts/download-weights.sh, .github/workflows/smoke.yml</files>
  <read_first>
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 67-77 (DELETED files table — confirm what stays vs goes)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-11-SUMMARY.md (verify Live UAT signed off GREEN before destruction)
    - pod/scripts/upload-weights.sh (verify this is KEPT — operator pipeline still in use)
    - .github/workflows/smoke.yml lines 155-215 (confirm hard refs ao pod/onstart.sh + pod/docker-compose.yml + pod/scripts/download-weights.sh — these prove the workflow cannot survive without the deleted artifacts)
    - .github/workflows/ (ls remaining workflows to confirm CI surface after delete — build-pod.yml já obsoleto Phase 6 PR2; smoke.yml é o último Phase 1 vestige)
  </read_first>
  <action>Antes de deletar, ASSERT que Plan 06.6-11 UAT sign-off é GREEN. Se 06.6-11-SUMMARY.md indicar FAIL ou pending, STOP. Caso contrário: (1) `git rm pod/onstart.sh`; (2) `git rm pod/docker-compose.yml`; (3) `git rm -r pod/health-bridge/` (entire subdir incluindo .go files + Dockerfile se houver + go.mod local se houver); (4) `git rm pod/scripts/download-weights.sh`; (5) `git rm .github/workflows/smoke.yml`. Decisão DELETE (não rewrite) para smoke.yml: o workflow é Phase 1 GHA-driven smoke baseado em `pod/scripts/vast-ai.sh create --onstart pod/onstart.sh` + scp do compose + onstart re-invoke; com Strategy B, primary pod é provisioned pelo reconciler (gateway/internal/primary) e validado por Plan 06.6-11 live operator script (06.6-HUMAN-UAT.md) que cobre os mesmos cenários (4 services GREEN, tool-calling, STT, embed, schedule, Pitfall #11 coexist). Rewrite would require parallel maintenance burden sem ganho — operator UAT é mais aderente à realidade Strategy B. NÃO deletar pod/scripts/upload-weights.sh, pod/scripts/vast-ai.sh, pod/templates/qwen3.5-27b-tool-calling.jinja, pod/templates/qwen3.5-27b-tool-calling.jinja.sha256, pod/smoke/, pod/weights/.gitkeep. Verificar `git status` shows exatamente 5 deletes. Commitar com mensagem `chore(06.6): PR3 — delete pod/ legacy + smoke.yml (Phase 1 GHA workflow tied to deleted artifacts)` — referencia 06.6-11-SUMMARY.md sign-off no body + cita linhas 163/197/199/200/213 do smoke.yml como prova das refs quebradas.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && [ ! -f pod/onstart.sh ] && [ ! -f pod/docker-compose.yml ] && [ ! -d pod/health-bridge ] && [ ! -f pod/scripts/download-weights.sh ] && [ ! -f .github/workflows/smoke.yml ] && [ -f pod/scripts/upload-weights.sh ] && [ -d pod/smoke ] && ! grep -rE "pod/(onstart\.sh|docker-compose\.yml|scripts/download-weights\.sh)" .github/ 2>/dev/null && echo "delete-set OK"</automated>
  </verify>
  <done>5 paths deleted. KEPT files preserved. .github/ has zero refs aos pod/ artifacts deletados. git diff stats show ~1000-1200 lines removed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite pod/README.md as pointer to gateway/docs/RUNBOOK-PRIMARY-POD.md</name>
  <files>pod/README.md</files>
  <read_first>
    - pod/README.md current content (legacy references to Dockerfile + build-pod.yml + onstart.sh — all obsolete)
    - gateway/docs/RUNBOOK-PRIMARY-POD.md (Plan 06.6-11 Task 1 output — the new canonical doc)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md line 76 (rewrite spec)
  </read_first>
  <action>Substituir TODO o conteúdo de pod/README.md por: `# pod/` (H1) + 1 parágrafo: `Esta pasta contém artefatos operacionais de provisioning do primary pod ifix-ai-gateway (weights upload pipeline, manual Vast.ai provisioning fallback, smoke-test fixtures Phase 1). A arquitetura de runtime do pod (Strategy B Full-Stack — upstream images + inline bash bootstrap via Vast.ai args runtype) vive em \`gateway/internal/primary/\` e é orchestrada via \`gateway/cmd/gatewayctl/primary*\` CLI. Documentação operator-facing canonical: [gateway/docs/RUNBOOK-PRIMARY-POD.md](../gateway/docs/RUNBOOK-PRIMARY-POD.md).` + Seção `## Files`: list of KEPT files com 1-line description cada: `pod/scripts/upload-weights.sh` (operator weight rotation pipeline), `pod/scripts/vast-ai.sh` (operator manual provisioning fallback), `pod/templates/qwen3.5-27b-tool-calling.jinja` (Qwen3.5 Jinja consumed by emerg pod; Qwen3.6 Jinja optional via MinIO key per WAVE0-GATES Decision 3), `pod/smoke/` (Phase 1 smoke-test fixtures reused for UAT POD-07 VRAM validation), `pod/weights/.gitkeep` (placeholder for weights directory). Seção `## See Also`: links to .planning/phases/06.6-... + Phase 1 history (`.planning/phases/01-gpu-pod-image-smoke-test/`). Mencionar explicitamente `## CI Workflow History`: `.github/workflows/smoke.yml` foi REMOVED no PR3 (06.6-12-PLAN.md) — UAT migrated para operator-run live script em 06.6-HUMAN-UAT.md. `.github/workflows/build-pod.yml` foi REMOVED em Phase 6 PR2 (commit 064702e). Footer com `_Last updated: 2026-05-17 — Phase 6.6 PR3 cleanup (06.6-12-PLAN.md)_`.</action>
  <verify>
    <automated>grep -q "RUNBOOK-PRIMARY-POD.md" /home/pedro/projetos/pedro/gpu-ifix/pod/README.md && grep -q "gateway/internal/primary" /home/pedro/projetos/pedro/gpu-ifix/pod/README.md && grep -q "smoke.yml" /home/pedro/projetos/pedro/gpu-ifix/pod/README.md</automated>
  </verify>
  <done>pod/README.md reescrito. Operator pode chegar via README e ser redirected à canonical documentação em gateway/docs/. CI history transparent.</done>
</task>

<task type="auto">
  <name>Task 3: Final regression check — full build + test suite must remain GREEN after deletes</name>
  <files>(verification-only, no files modified)</files>
  <read_first>
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-VALIDATION.md (Quick run + Full suite commands)
  </read_first>
  <action>Executar comandos de regression check: (1) `cd gateway && go build ./...` — sucesso obrigatório; (2) `cd gateway && go test ./... -count=1 -race -timeout=10m` — todos green; (3) `cd gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=15m` — todos green incluindo primary_emerg_coexist_test.go (Pitfall #11); (4) `find .github/ -type f -name '*.yml' -o -name '*.yaml' | xargs grep -lE "pod/(onstart\\.sh|docker-compose\\.yml|scripts/download-weights\\.sh|health-bridge)" || true` — output deve ser empty (zero stale refs). Documentar exit codes e wall-clock time em commit message. Se algum falhar: STOP, criar 06.6-12-FAILURE-NOTE.md descrevendo qual file delete causou regression + propor restoration; NÃO PROCEED com Phase 6.6 close.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./... && go test ./... -count=1 -race -timeout=10m && cd /home/pedro/projetos/pedro/gpu-ifix && ! grep -rE "pod/(onstart\.sh|docker-compose\.yml|scripts/download-weights\.sh|health-bridge)" .github/ 2>/dev/null</automated>
  </verify>
  <done>Build + unit tests green pós-deletes. Zero stale refs em .github/. Confirma que zero código gateway depende dos arquivos pod/ deletados E nenhum CI workflow ficou órfão. Phase 6.6 código está auto-suficiente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(Cleanup plan; only deletes files. No new trust boundaries.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none new) | — | — | — | Cleanup is delete-only; reduces attack surface (less custom code to audit; no more custom GHCR image dependency post-this-plan; one fewer CI workflow with stale broken refs). |
</threat_model>

<verification>
- `[ ! -f pod/onstart.sh ] && [ ! -f pod/docker-compose.yml ] && [ ! -d pod/health-bridge ] && [ ! -f pod/scripts/download-weights.sh ] && [ ! -f .github/workflows/smoke.yml ]` (5 paths deleted)
- `[ -f pod/scripts/upload-weights.sh ] && [ -d pod/smoke ] && [ -f pod/templates/qwen3.5-27b-tool-calling.jinja ]` (3 KEPT preserved)
- `grep -q "RUNBOOK-PRIMARY-POD.md" pod/README.md` (redirect in place)
- `! grep -rE "pod/(onstart\.sh|docker-compose\.yml|scripts/download-weights\.sh|health-bridge)" .github/` (zero stale CI refs)
- `cd gateway && go build ./... && go test ./... -count=1 -race -timeout=10m` all green
</verification>

<success_criteria>
- Phase 1 + Phase 6.6 dual-implementation eliminated; only Strategy B Full-Stack remains
- ~1000-1200 lines deleted (PATTERNS.md estimate + smoke.yml ~290 lines)
- README updates make navigation operator-friendly
- Regression check confirms gateway code is independent of deleted pod/ artifacts
- CI surface clean: no broken refs to deleted artifacts; UAT scope owned by Plan 06.6-11 operator script
- Phase 6.6 ready for SUMMARY.md + VERIFICATION.md + ROADMAP close
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-12-SUMMARY.md`. After this plan completes, the orchestrator should run `/gsd:verify-work 06.6` to produce 06.6-VERIFICATION.md + close Phase 6.6 in ROADMAP.md + update STATE.md.
</output>
