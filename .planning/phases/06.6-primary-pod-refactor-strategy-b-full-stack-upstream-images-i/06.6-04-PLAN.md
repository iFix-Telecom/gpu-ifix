---
phase: 06.6
plan: 04
type: execute
wave: 2
depends_on: [06.6-02, 06.6-03]
files_modified:
  - gateway/internal/primary/onstart.go
  - gateway/internal/primary/lifecycle.go
  - gateway/internal/primary/lifecycle_test.go
  - gateway/internal/primary/doc.go
autonomous: true
requirements: []
tags: [primary-pod, wave-2, lifecycle, onstart, dind, qwen3.6, strategy-b]

must_haves:
  truths:
    - "gateway/internal/primary/onstart.go expõe primaryOnstartHead raw-string Go const (~3.3KB inline bash) com ZERO fmt.Sprintf (Pitfall #9 enforced) e termina com exec /app/llama-server (Pitfall #2 PID 1 invariant); 4 sidecars (LLM + STT + Embed + DCGM) all from upstream tag-pinned images per D-01 + D-02; 3 parallel weight downloads per lifecycle per D-09"
    - "D-03 traceability: cfg.PrimaryTemplateImage default == \"ghcr.io/ggml-org/llama.cpp:server-cuda-b9128\" (engine LLM upstream image — locked decision per CONTEXT.md D-03 + WAVE0-GATES Decision 1); enforced via TestBuildPrimaryCreateRequest_D03_EngineImagePinned asserting req.Image equals this value when cfg has defaults"
    - "D-07 traceability: cfg.PrimaryDCGMImage default == \"nvcr.io/nvidia/k8s/dcgm-exporter:4.5.3-4.8.2-distroless\" + onstart bash includes `-p 9400:9400` port mapping + DCGM sidecar docker run block uses ${PRIMARY_DCGM_IMAGE} (DCGM exporter port 9400 preserved per CONTEXT.md D-07 + Pitfall #8); enforced via TestPrimaryOnstart_DCGMPort9400Preserved asserting onstart text contains '9400:9400' AND '${PRIMARY_DCGM_IMAGE'"
    - "Script inline cobre: optional sshd (D-10), mc install (curl, no apt — Pitfall hot-fix 4896004), 3 parallel weight downloads + sha256 verify (Qwen + Whisper + BGE-M3), optional Jinja fetch se PRIMARY_QWEN_JINJA_KEY non-empty, dockerd start + 30s wait loop (DinD per WAVE0-GATES Decision 2), 3 sidecar docker run -d (speaches + infinity + dcgm), exec llama-server final line"
    - "buildPrimaryCreateRequest produz vast.CreateRequest com Image=cfg.PrimaryTemplateImage, Runtype=\"args\", Onstart=\"/bin/bash\", Args=[\"-c\", onstart], Disk=50, Label=ifix-primary-lifecycle-<id>, TargetState=\"running\""
    - "Env map inclui 4 port forwards (-p 8000:8000, -p 8001:8001, -p 8002:8002, -p 9400:9400 per Pitfall #8) + 4 MINIO_* (Endpoint, Bucket, AccessKey, SecretKey — ALL four must be non-empty per WARNING 2 revision) + 3 PRIMARY_*_WEIGHTS_KEY/SHA256 pairs + 3 PRIMARY_*_IMAGE refs + condicional POD_DEBUG_SSH_PUBLIC_KEY + -p 22:22 quando key non-empty + condicional PRIMARY_QWEN_JINJA_KEY/SHA256 quando key non-empty"
    - "buildPrimaryOnstart(llamaArgs []string) string retorna primaryOnstartHead + \"exec /app/llama-server \" + strings.Join(llamaArgs, \" \") + \"\\n\" (último-line invariant)"
    - "primaryLlamaArgsDefault var contém slice com --host 0.0.0.0 --port 8000 -m /weights/qwen/model.gguf -ngl 99 -np 2 --ctx-size 16384 --jinja (sem --chat-template-file se WAVE0-GATES Decision 3 = B1)"
    - "Quando cfg.PrimaryQwenJinjaKey != \"\", onstart bash conditional fetch Jinja para /app/templates/qwen3.6.jinja com sha256 verify; llamaArgs append --chat-template-file /app/templates/qwen3.6.jinja"
  artifacts:
    - path: "gateway/internal/primary/doc.go"
      provides: "Package-level doc comment for gateway/internal/primary"
      contains: "Package primary"
    - path: "gateway/internal/primary/onstart.go"
      provides: "primaryOnstartHead raw-string const + buildPrimaryOnstart + primaryLlamaArgsDefault"
      contains: "primaryOnstartHead|buildPrimaryOnstart|primaryLlamaArgsDefault|exec /app/llama-server|dockerd|set -euxo"
    - path: "gateway/internal/primary/lifecycle.go"
      provides: "buildPrimaryCreateRequest + Reconciler skeleton (struct fields only; methods minimally stubbed for compile)"
      contains: "buildCreateRequest|primaryPodURLs|Reconciler"
    - path: "gateway/internal/primary/lifecycle_test.go"
      provides: "11+ unit tests covering buildCreateRequest assembly + onstart length + bash content invariants + D-03/D-07 traceability + MinIO non-empty"
      contains: "TestBuildPrimaryCreateRequest_DinD_4Services|TestPrimaryOnstartLengthBelowLimit|TestPrimaryOnstart_StartsWithSetEuxo|TestPrimaryOnstart_NoLegacyImage|TestPrimaryOnstart_ExecLlamaServerIsLastLine|TestBuildPrimaryCreateRequest_Has4PortMappings|TestPrimaryOnstart_QwenWeightsInjected|TestBuildPrimaryCreateRequest_JinjaEmbeddedMode|TestBuildPrimaryCreateRequest_JinjaMinIOFallback|TestBuildPrimaryCreateRequest_D03_EngineImagePinned|TestPrimaryOnstart_DCGMPort9400Preserved|TestBuildPrimaryCreateRequest_MinIOCredentialsNotEmpty"
  key_links:
    - from: "primary.buildPrimaryCreateRequest"
      to: "vast.CreateRequest DTO (gateway/internal/emerg/vast/types.go)"
      via: "DTO shape"
      pattern: "vast\\.CreateRequest\\{"
    - from: "primary.buildPrimaryCreateRequest Env map"
      to: "primaryOnstartHead bash expansion ($VAR references)"
      via: "Vast.ai env-var injection"
      pattern: "\\$(MINIO_|PRIMARY_|POD_DEBUG_)"

threat_refs: [T-06.6-01, T-06.6-02, T-06.6-SC]
---

<objective>
Criar pacote gateway/internal/primary/ com 3 artefatos centrais: (1) `onstart.go` raw-string Go const com bash inline ~3.3KB (4 sidecars via DinD + 3 paralelos weight downloads), (2) `lifecycle.go` com `buildCreateRequest` Strategy B payload + Reconciler struct skeleton (fields only — methods stubbed para compile; full implementation em Plans 06.6-06+), (3) `lifecycle_test.go` com 11+ testes invariants (length, PID 1, port mappings, JSON shape, D-03/D-07 traceability, MinIO credentials non-empty).

Purpose: Plans 06.6-05/06.6-06 (schedule + reconciler) consomem este lifecycle.go. Plan 06.6-08 (main.go wiring) precisa do Reconciler struct público. Sem este plan, demais Wave 2 plans não compilam.

Output: 3 arquivos go novos + 1 doc.go. Build verde; 11+ testes verdes. Behavior diferenças vs emerg: Image upstream (não custom GHCR), 4 services (não 1), Disk=50 (não 40), Label prefix ifix-primary-lifecycle (não ifix-emerg-lifecycle), --jinja sem --chat-template-file por default (B1 strategy per WAVE0-GATES Decision 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md
@gateway/internal/emerg/lifecycle.go
@gateway/internal/emerg/lifecycle_test.go
@gateway/internal/emerg/vast/types.go
@gateway/internal/config/config.go

<interfaces>
<!-- vast.CreateRequest DTO (consumed) — from gateway/internal/emerg/vast/types.go lines 130-160 -->

```go
type CreateRequest struct {
    ClientID    string            `json:"client_id"`
    Image       string            `json:"image"`
    Env         map[string]string `json:"env"`
    Onstart     string            `json:"onstart_cmd"`     // "/bin/bash" for Strategy B
    Runtype     string            `json:"runtype"`         // "args" for Strategy B
    Args        []string          `json:"args"`            // []string{"-c", <onstart_script>}
    Disk        int               `json:"disk"`
    Label       string            `json:"label"`
    TargetState string            `json:"target_state"`
}
```

<!-- emergencyOnstartHead pattern from gateway/internal/emerg/lifecycle.go:694-731 — REFERENCE only, do not copy verbatim; primary onstart is structurally similar but with 4-service DinD layer -->

<!-- buildCreateRequest analog from gateway/internal/emerg/lifecycle.go:770-826 — pattern to follow with renames -->

<!-- primaryPodURLs new struct (consumed later by reconciler in Plan 06.6-06) -->
```go
type primaryPodURLs struct {
    LLM   string // http://<vast_ip>:<8000-host-port>
    STT   string
    Embed string
    DCGM  string
}
```

<!-- Reconciler struct fields needed for buildCreateRequest call site — minimal viable shape -->
```go
type Reconciler struct {
    deps          Deps                                 // config + log + vast client + DB pool + loader + dcgm + dispatcher + fsm
    activePodURLs atomic.Pointer[primaryPodURLs]      // populated on Ready
    rule          ScheduleRule                         // resolved by Plan 06.6-05 schedule.go
    drainStartedAt atomic.Pointer[time.Time]
    cfg           config.Config
}

type Deps struct {
    Cfg         config.Config
    Log         *slog.Logger
    Vast        vastutil.VastDestroyer // minimal interface; full vast.Client wired in main.go
    // Loader, DCGMScraper, Dispatcher, FSM, DB pool — fields wired in Plan 06.6-08 main.go;
    // for THIS plan, declare them as interface{} or specific types as needed (placeholders OK
    // — Plan 06.6-06 will refine when reconciler.go methods land)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gateway/internal/primary/doc.go + onstart.go (raw-string Go const + buildPrimaryOnstart)</name>
  <files>gateway/internal/primary/doc.go, gateway/internal/primary/onstart.go</files>
  <read_first>
    - gateway/internal/emerg/lifecycle.go lines 650-742 (emergencyLlamaArgsDefault + emergencyOnstartHead + buildEmergencyOnstart — analog shape)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md §Onstart Script Anatomy (lines 772-892) — full 7-section inline bash spec
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 180-258 (onstart.go pattern spec with raw-string + buildPrimaryOnstart signature)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md Decision 3 (Jinja strategy B1 vs B2 — defines whether --chat-template-file appears)
  </read_first>
  <action>Criar gateway/internal/primary/doc.go com block comment `// Package primary implements the primary pod lifecycle for ifix-ai-gateway. // Phase 6.6 introduces a schedule-driven 5-state FSM (Asleep|Provisioning|Ready|Draining|Destroying) // distinct from the emergency-event-driven emerg package. See // .planning/phases/06.6-.../06.6-CONTEXT.md and 06.6-RESEARCH.md.` + `package primary`. Criar gateway/internal/primary/onstart.go com `package primary` + import "strings". Definir `var primaryLlamaArgsDefault = []string{"--host", "0.0.0.0", "--port", "8000", "-m", "/weights/qwen/model.gguf", "-ngl", "99", "-np", "2", "--ctx-size", "16384", "--jinja"}` (NÃO incluir --chat-template-file por default — B1 strategy per WAVE0-GATES Decision 3; código condicional adiciona quando Jinja env set). Definir `const primaryOnstartHead = \`...\`` raw-string com EXATAMENTE as 7 seções de RESEARCH.md §Onstart Script Anatomy linhas 776-882: (1) header `#!/bin/bash` + `set -euxo pipefail` + `export DEBIAN_FRONTEND=noninteractive` + `mkdir -p /weights/qwen /weights/whisper /weights/bge-m3 /app/templates /run/sshd /root/.ssh`; (2) optional sshd if POD_DEBUG_SSH_PUBLIC_KEY set (mesma block que emergencyOnstartHead:702-712); (3) mc curl install if not present; (4) mc alias + 3 parallel `( ... ) &` weight download blocks com sha256sum -c verify, capturando 3 PIDs, optional Jinja fetch conditional if PRIMARY_QWEN_JINJA_KEY non-empty, then `wait $QWEN_PID $WHISPER_PID $BGE_PID`; (5) dockerd start `dockerd --host=unix:///var/run/docker.sock &>/var/log/dockerd.log &` + 30s wait loop `for i in {1..30}; do if docker info &>/dev/null; then break; fi; sleep 1; done`; (6) 3 sidecar `docker run -d --restart unless-stopped --name speaches/infinity/dcgm` blocks com flags exatas de RESEARCH.md §Onstart Script Anatomy linhas 854-873 (--gpus all + -p forwards + -v volume mounts + image env-var refs `${PRIMARY_SPEACHES_IMAGE:-...}` etc; DCGM block exposes -p 9400:9400 explicitly per D-07); (7) **NO final exec line aqui** — buildPrimaryOnstart appende dinamicamente. Definir `func buildPrimaryOnstart(llamaArgs []string, jinjaPath string) string { args := strings.Join(llamaArgs, " "); if jinjaPath != "" { args += " --chat-template-file " + jinjaPath }; return primaryOnstartHead + "exec /app/llama-server " + args + "\n" }`. ZERO fmt.Sprintf no raw-string ou no exec append — strings.Join é safe (não interpola arbitrário). Comentário sobre Pitfall #9 acima da const.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./internal/primary/...</automated>
  </verify>
  <done>onstart.go + doc.go compilam standalone. primaryOnstartHead é const (não var). buildPrimaryOnstart é function. Conteúdo bash inline tem ZERO fmt.Sprintf e termina sem exec (exec adicionado por buildPrimaryOnstart).</done>
</task>

<task type="auto">
  <name>Task 2: Create gateway/internal/primary/lifecycle.go — buildPrimaryCreateRequest + Reconciler skeleton</name>
  <files>gateway/internal/primary/lifecycle.go</files>
  <read_first>
    - gateway/internal/emerg/lifecycle.go lines 1-22 (imports), 70-105 (VastAPI + HealthChecker + const block), 770-826 (buildCreateRequest pattern)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 262-357 (lifecycle.go pattern spec with full buildCreateRequest body)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md §Pitfall #8 (lines 494-525) — 4 port mappings invariant
    - gateway/internal/config/config.go (Plan 06.6-03 output — confirm 23 Primary* field names including PrimaryProvisionFailureCooldownSeconds)
  </read_first>
  <action>Criar gateway/internal/primary/lifecycle.go com `package primary`. Imports: "context", "fmt", "log/slog", "sync/atomic", "time", "github.com/ifixtelecom/gpu-ifix/gateway/internal/config", "github.com/ifixtelecom/gpu-ifix/gateway/internal/emerg/vast", "github.com/ifixtelecom/gpu-ifix/gateway/internal/vastutil". Definir struct `primaryPodURLs { LLM, STT, Embed, DCGM string }`. Definir struct `Deps { Cfg config.Config; Log *slog.Logger; Vast vastutil.VastDestroyer; HealthCheck func(ctx context.Context, url string) bool }` (campos mínimos para THIS plan; Plan 06.6-06 adiciona Loader/DCGMScraper/Dispatcher/FSM/DB). Definir struct `Reconciler { deps Deps; cfg config.Config; activePodURLs atomic.Pointer[primaryPodURLs]; drainStartedAt atomic.Pointer[time.Time] }` — schedule rule e FSM são fields adicionados nos plans seguintes; OK ser parcial agora. Constructor `func NewReconciler(deps Deps) *Reconciler { return &Reconciler{deps: deps, cfg: deps.Cfg} }`. Function `func (r *Reconciler) buildCreateRequest(offer vast.Offer, lifecycleID int64) vast.CreateRequest`: corpo seguir verbatim PATTERNS.md:267-313 com renames Emergency→Primary, EmergencyJinjaTemplateKey→PrimaryQwenJinjaKey/SHA256 (condicional empty-check), Disk=50 (WAVE0-GATES Decision 4), Label `fmt.Sprintf("ifix-primary-lifecycle-%d", lifecycleID)` (fmt.Sprintf OK fora do onstart raw-string — só argumento de Label). Build env map: 4 port forwards `"-p 8000:8000": "1"`, `"-p 8001:8001": "1"`, `"-p 8002:8002": "1"`, `"-p 9400:9400": "1"` (Pitfall #8 invariant + D-07 DCGM port preservation), 4 MINIO_* (Endpoint, Bucket, AccessKey, SecretKey — ALL FOUR required for `mc cp` block to work; tests must assert all non-empty), 3 PRIMARY_*_WEIGHTS_KEY + 3 PRIMARY_*_WEIGHTS_SHA256 (qwen + whisper + bge), 3 PRIMARY_*_IMAGE (speaches + infinity + dcgm), condicional adicionar `env["PRIMARY_QWEN_JINJA_KEY"]/`"PRIMARY_QWEN_JINJA_SHA256"]` se `cfg.PrimaryQwenJinjaKey != ""`, condicional adicionar `env["POD_DEBUG_SSH_PUBLIC_KEY"]/"-p 22:22"` se `cfg.PodDebugSSHPublicKey != ""`. Selecionar llamaArgs: `llamaArgs := primaryLlamaArgsDefault; if len(cfg.PrimaryLlamaArgs) > 0 { llamaArgs = cfg.PrimaryLlamaArgs }`. jinjaPath: `jinjaPath := ""; if cfg.PrimaryQwenJinjaKey != "" { jinjaPath = "/app/templates/qwen3.6.jinja" }`. onstart := buildPrimaryOnstart(llamaArgs, jinjaPath). Return CreateRequest{ClientID:"me", Image:cfg.PrimaryTemplateImage, Env:env, Onstart:"/bin/bash", Runtype:"args", Args:[]string{"-c", onstart}, Disk:50, Label:..., TargetState:"running"}. Adicionar helpers stub `func (r *Reconciler) podLLMURL(inst vast.Instance) string`/podSTTURL/podEmbedURL/podDCGMURL extraindo mapped host port from inst.Ports map (analog emerg lifecycle.go:564-577 podHealthURL pattern — para "8000/tcp", "8001/tcp", "8002/tcp", "9400/tcp"). Adicionar ActivePodURLs() getter retornando `*primaryPodURLs` (uses activePodURLs.Load()).</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./internal/primary/...</automated>
  </verify>
  <done>lifecycle.go compila. Reconciler struct + buildCreateRequest method exportadas (capitalized onde precisa) ou unexported (lower-case onde só usadas dentro do pacote). Build agreement com config.go field names verificada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lifecycle_test.go — 11+ unit tests covering buildCreateRequest + onstart invariants + D-03/D-07 traceability + MinIO non-empty</name>
  <files>gateway/internal/primary/lifecycle_test.go</files>
  <behavior>
    - TestBuildPrimaryCreateRequest_DinD_4Services: cfg com defaults (INCLUI MinioAccessKey + MinioSecretKey non-empty test values) + 1 offer fake → req.Image == cfg.PrimaryTemplateImage; req.Runtype == "args"; req.Onstart == "/bin/bash"; require.Len(req.Args, 2); req.Args[0] == "-c"; req.Args[1] contém "exec /app/llama-server", "--jinja", "/weights/qwen/model.gguf", "dockerd", "speaches", "infinity", "dcgm"; req.Disk == 50; req.Label == "ifix-primary-lifecycle-7" (para lifecycleID=7); req.TargetState == "running"; require.NotEmpty(req.Env["MINIO_ACCESS_KEY"]); require.NotEmpty(req.Env["MINIO_SECRET_KEY"]); require.NotEmpty(req.Env["MINIO_ENDPOINT"]); require.NotEmpty(req.Env["MINIO_BUCKET"]) — per WARNING 2 revision (cfgWithDefaults previously omitted Access/Secret keys; tests silently asserted against empty values)
    - TestBuildPrimaryCreateRequest_Has4PortMappings: 4 keys "-p 8000:8000", "-p 8001:8001", "-p 8002:8002", "-p 9400:9400" presentes em req.Env com valor "1"
    - TestBuildPrimaryCreateRequest_MinIOCredentialsNotEmpty: separar test focado NO MinIO contract — cfg with all 4 MINIO_* set → all 4 keys present + values match cfg fields; cfg with empty MinioAccessKey → still builds req but assert req.Env["MINIO_ACCESS_KEY"] == "" to document the empty-passthrough behavior + log warning in implementation (downstream pod onstart `mc alias set` will fail visibly) — NÃO mascarar empty values silently
    - TestBuildPrimaryCreateRequest_JinjaEmbeddedMode: cfg.PrimaryQwenJinjaKey="" → req.Args[1] NÃO contém "--chat-template-file" + req.Env não tem PRIMARY_QWEN_JINJA_KEY
    - TestBuildPrimaryCreateRequest_JinjaMinIOFallback: cfg.PrimaryQwenJinjaKey="qwen-templates/...jinja" + cfg.PrimaryQwenJinjaSHA256="abc..." → req.Args[1] contém "--chat-template-file /app/templates/qwen3.6.jinja" + req.Env tem ambos PRIMARY_QWEN_JINJA_KEY e PRIMARY_QWEN_JINJA_SHA256
    - TestBuildPrimaryCreateRequest_LlamaArgsOverride: cfg.PrimaryLlamaArgs=["--host","127.0.0.1","--port","9000"] → req.Args[1] termina com "exec /app/llama-server --host 127.0.0.1 --port 9000\n"
    - TestPrimaryOnstartLengthBelowLimit: len(req.Args[1]) < 14000 (Vast 16KB hard limit margin per Pitfall #1)
    - TestPrimaryOnstart_StartsWithSetEuxo: req.Args[1] começa com "#!/bin/bash\n" OU "set -euxo pipefail" no início (first 100 chars contains "set -euxo pipefail")
    - TestPrimaryOnstart_NoLegacyImage: req.Args[1] NÃO contém substring "ifix-ai-pod" (proves migration away from custom GHCR image)
    - TestPrimaryOnstart_ExecLlamaServerIsLastLine: strings.TrimSpace(req.Args[1]) termina com "/app/llama-server <flags>" (Pitfall #2 PID 1 invariant — última linha do script)
    - TestPrimaryOnstart_QwenWeightsInjected: req.Args[1] contém "$PRIMARY_QWEN_WEIGHTS_SHA256" OU literal sha256 + "$PRIMARY_QWEN_WEIGHTS_KEY"
    - TestBuildPrimaryCreateRequest_SSHDebugConditional: cfg.PodDebugSSHPublicKey="ssh-ed25519 AAA..." → req.Env tem POD_DEBUG_SSH_PUBLIC_KEY + "-p 22:22"; cfg empty → ambos ausentes
    - TestBuildPrimaryCreateRequest_D03_EngineImagePinned: cfg com defaults → req.Image == "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128" (D-03 traceability + WAVE0-GATES Decision 1 — checker revision WARNING 1)
    - TestPrimaryOnstart_DCGMPort9400Preserved: req.Args[1] contém "9400:9400" AND "${PRIMARY_DCGM_IMAGE" (or literal default) — DCGM exporter port 9400 preserved per CONTEXT.md D-07 + Pitfall #8; checker revision WARNING 1
  </behavior>
  <read_first>
    - gateway/internal/emerg/lifecycle_test.go lines 226-417 (TestBuildCreateRequest_StrategyB_args + variants — pattern to mirror)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 361-415 (lifecycle_test pattern spec with test naming table)
    - gateway/internal/primary/lifecycle.go (Task 2 output — confirm public/private symbols)
    - gateway/internal/primary/onstart.go (Task 1 output — confirm const + builder)
  </read_first>
  <action>Criar gateway/internal/primary/lifecycle_test.go com `package primary`. Imports: "testing", "strings", "github.com/stretchr/testify/require", "github.com/ifixtelecom/gpu-ifix/gateway/internal/config", "github.com/ifixtelecom/gpu-ifix/gateway/internal/emerg/vast". Definir helper `func newReconcilerForBuildTest(t *testing.T, cfg config.Config) *Reconciler` que retorna `NewReconciler(Deps{Cfg: cfg, Log: slog.Default()})`. Para defaults usar `cfgWithDefaults := config.Config{ PrimaryTemplateImage: "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128", PrimarySpeachesImage: "ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3", PrimaryInfinityImage: "michaelf34/infinity:0.0.77", PrimaryDCGMImage: "nvcr.io/nvidia/k8s/dcgm-exporter:4.5.3-4.8.2-distroless", PrimaryQwenWeightsKey: "qwen3.6-27b-Q4_K_M/v1.0.0/model.gguf", PrimaryQwenWeightsSHA256: "a7cbd3ec...", PrimaryWhisperWeightsKey: "whisper-large-v3/v1.0.0/model.tar.gz", PrimaryBGEM3WeightsKey: "bge-m3/v1.0.0/model.tar.gz", MinioEndpoint: "https://s3.ifixtelecom.com.br", MinioBucket: "ai-gateway", MinioAccessKey: "test-access-key", MinioSecretKey: "test-secret-key" }` — **CRÍTICO per WARNING 2 revision: MinioAccessKey + MinioSecretKey DEVEM ser non-empty test values; previously omitted, masking empty-credential bugs**. Escrever 14 testes da tabela <behavior> com nomes exatos da PATTERNS.md:365-376 table + 3 novos per checker revision: TestBuildPrimaryCreateRequest_MinIOCredentialsNotEmpty (WARNING 2), TestBuildPrimaryCreateRequest_D03_EngineImagePinned (WARNING 1), TestPrimaryOnstart_DCGMPort9400Preserved (WARNING 1). Para TestPrimaryOnstart_ExecLlamaServerIsLastLine usar `lines := strings.Split(strings.TrimRight(req.Args[1], "\n"), "\n"); lastLine := lines[len(lines)-1]; require.True(t, strings.HasPrefix(lastLine, "exec /app/llama-server"), "PID 1 invariant: last line must exec llama-server, got: %s", lastLine)`. Para TestPrimaryOnstartLengthBelowLimit usar `require.Less(t, len(req.Args[1]), 14000, "leave 2KB margin under Vast 16KB hard limit (Pitfall #1)")`. Para TestBuildPrimaryCreateRequest_DinD_4Services adicionar 4 require.NotEmpty asserts: `require.NotEmpty(t, req.Env["MINIO_ACCESS_KEY"], "MINIO_ACCESS_KEY must be non-empty for mc alias to succeed"); require.NotEmpty(t, req.Env["MINIO_SECRET_KEY"], ...); require.NotEmpty(t, req.Env["MINIO_ENDPOINT"], ...); require.NotEmpty(t, req.Env["MINIO_BUCKET"], ...)`.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./internal/primary/... -count=1 -race -run TestBuildPrimary -run TestPrimaryOnstart</automated>
  </verify>
  <done>14 testes verdes. Invariants enforced: PID 1 last-line, length < 14KB, 4 port mappings, Jinja conditional, no legacy image refs, D-03 engine pin, D-07 DCGM port preservation, MinIO 4 credentials non-empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway process → Vast.ai API | CreateInstance payload travels operator-controlled cfg values (no untrusted input) |
| Vast.ai pod (runtime) → MinIO (`s3.ifixtelecom.com.br`) | Pod onstart bash uses MINIO_* env (operator-supplied); sha256 verify on each download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-01 | Tampering | inline bash onstart (script injection via env var) | mitigate | All dynamic values flow via $VAR shell expansions populated from CreateRequest.Env map. Zero fmt.Sprintf in raw-string Go const (Pitfall #9). Env values come from cfg.PrimaryXxx fields (operator-controlled), not user input. TestPrimaryOnstart_NoLegacyImage proves migration completeness. |
| T-06.6-02 | Tampering | MinIO weight download (GGUF tampering) | mitigate | sha256sum -c verify on every download (Qwen, Whisper, BGE-M3, optional Jinja). Operator sets SHA256 env vars at deploy time; carry from CONTEXT.md D-04 immutable values. MinIO credentials enforced non-empty by TestBuildPrimaryCreateRequest_MinIOCredentialsNotEmpty (empty credentials would silently fail mc alias). |
| T-06.6-SC | Tampering | OCI image references | mitigate | cfg.Primary*Image fields default to SHA-pinned tags from WAVE0-GATES.md Decision 1. Tests assert defaults match (TestBuildPrimaryCreateRequest_D03_EngineImagePinned for D-03 engine image specifically). |
</threat_model>

<verification>
- `cd gateway && go build ./internal/primary/...` succeeds
- `cd gateway && go test ./internal/primary/... -count=1 -race` all green (14 tests)
- `cd gateway && grep -c 'fmt\\.Sprintf' gateway/internal/primary/onstart.go == 0` (Pitfall #9 invariant)
- `cd gateway && grep -c 'ifix-ai-pod' gateway/internal/primary/onstart.go == 0` (no legacy image)
- `cd gateway && grep -c '\"exec /app/llama-server \"' gateway/internal/primary/onstart.go == 1` (PID 1 invariant via builder)
- `cd gateway && grep -c '9400:9400' gateway/internal/primary/onstart.go >= 1` (D-07 DCGM port preserved)
</verification>

<success_criteria>
- gateway/internal/primary/ package is the foundation for schedule.go (06.6-05) + reconciler.go (06.6-06)
- buildCreateRequest produces a Strategy B payload structurally identical to Phase 6 emergency (Runtype=args + Onstart=/bin/bash + Args=[-c, script])
- 4-service DinD orchestration embedded in inline bash (per WAVE0-GATES Decision 2)
- Jinja strategy flexible (B1 default, B2 fallback) via cfg.PrimaryQwenJinjaKey conditional
- D-03 engine image pinned + asserted (TestBuildPrimaryCreateRequest_D03_EngineImagePinned)
- D-07 DCGM port preserved + asserted (TestPrimaryOnstart_DCGMPort9400Preserved)
- MinIO 4 credentials non-empty asserted (TestBuildPrimaryCreateRequest_MinIOCredentialsNotEmpty + TestBuildPrimaryCreateRequest_DinD_4Services)
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-04-SUMMARY.md`.
</output>
