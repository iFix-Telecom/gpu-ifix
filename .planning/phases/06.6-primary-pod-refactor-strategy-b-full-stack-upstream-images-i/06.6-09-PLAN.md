---
phase: 06.6
plan: 09
type: execute
wave: 4
depends_on: [06.6-06, 06.6-07]
files_modified:
  - gateway/cmd/gatewayctl/primary.go
  - gateway/cmd/gatewayctl/primary_test.go
  - gateway/cmd/gatewayctl/main.go
autonomous: true
requirements: []
tags: [cli, wave-4, gatewayctl, primary, operator]

must_haves:
  truths:
    - "gateway/cmd/gatewayctl/primary.go expõe subcommand router `gatewayctl primary [state|force-up|force-down|schedule|lifecycles]` espelhando emerg.go pattern (lines 1-80) + new `schedule` subcommand sem analog emerg"
    - "gatewayctl primary state — lê redisx.PrimaryStateKey() HGETALL + imprime state, lifecycle_id, pod_url, instance_id, entered_at em formato human-readable"
    - "gatewayctl primary force-up — emite redisx.PublishPrimaryEvent type=force_up_request (operator manual override); reconciler subscriber em Plan 06.6-06 trata"
    - "gatewayctl primary force-down — emite redisx.PublishPrimaryEvent type=force_down_request"
    - "gatewayctl primary schedule [show] — chama primary.ParseScheduleEnv(cfg) + imprime ScheduleRule fields + ScheduleRule.NextTransition(time.Now()) timestamp (operator pre-flight check)"
    - "gatewayctl primary lifecycles [--limit N] — SELECT FROM primary_lifecycles ORDER BY started_at DESC LIMIT N + imprime tabela (parity emerg lifecycles command)"
    - "main.go gatewayctl dispatcher adiciona case \"primary\" chamando runPrimary"
  artifacts:
    - path: "gateway/cmd/gatewayctl/primary.go"
      provides: "5 subcommands: state | force-up | force-down | schedule | lifecycles"
      contains: "runPrimary|runPrimaryState|runPrimaryForceUp|runPrimaryForceDown|runPrimarySchedule|runPrimaryLifecycles"
    - path: "gateway/cmd/gatewayctl/primary_test.go"
      provides: "6 unit tests cobrindo cada subcommand happy path; DB-fetch lifecycles test t.Skip pointing to Plan 06.6-10 integration coverage"
      contains: "TestRunPrimaryState|TestRunPrimaryForceUp|TestRunPrimaryForceDown|TestRunPrimarySchedule|TestRunPrimary_NoArgs|TestRunPrimary_UnknownSubcommand"
    - path: "gateway/cmd/gatewayctl/main.go"
      provides: "Updated subcommand dispatcher with primary case"
      contains: "case \"primary\":"
  key_links:
    - from: "gatewayctl primary force-up"
      to: "primary.Reconciler subscriber (Plan 06.6-06)"
      via: "redisx.PublishPrimaryEvent"
      pattern: "force_up_request"
    - from: "gatewayctl primary schedule show"
      to: "primary.ParseScheduleEnv + IsInPeak + NextTransition"
      via: "shared primary package"
      pattern: "primary\\.(ParseScheduleEnv|NextTransition)"

threat_refs: []
---

<objective>
Adicionar CLI operacional `gatewayctl primary <subcommand>` para dar ao operator controle manual sobre o primary pod fora do schedule. 5 subcomandos: state (read mirror), force-up/force-down (manual override), schedule show (pre-flight verify), lifecycles (historical audit).

Purpose: Operator precisa ferramentas para (a) verificar estado atual sem ler logs, (b) forçar provision/destroy quando schedule está errado ou em emergência, (c) inspecionar histórico de lifecycles para debug. Sem este CLI, operator depende de raw SQL queries + Redis HGETALL, error-prone.

Output: 1 novo arquivo Go (primary.go) + 1 test file + 1-line dispatcher addition em main.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@gateway/cmd/gatewayctl/emerg.go
@gateway/cmd/gatewayctl/emerg_test.go
@gateway/cmd/gatewayctl/main.go

<interfaces>
<!-- gatewayctl emerg.go pattern (gateway/cmd/gatewayctl/emerg.go:1-80) -->

```go
// Existing emerg subcommand dispatch pattern
func runEmerg(ctx context.Context, args []string, log *slog.Logger) int {
    if len(args) == 0 {
        fmt.Fprintln(os.Stderr, "Usage: gatewayctl emerg state|force-provision|force-destroy|lifecycles [flags]")
        return 2
    }
    switch args[0] {
    case "state":              return runEmergState(ctx, args[1:], log)
    case "force-provision":    return runEmergForceProvision(ctx, args[1:], log)
    case "force-destroy":      return runEmergForceDestroy(ctx, args[1:], log)
    case "lifecycles":         return runEmergLifecycles(ctx, args[1:], log)
    default:
        fmt.Fprintf(os.Stderr, "unknown emerg subcommand: %s\n", args[0])
        return 2
    }
}
```

<!-- Primary CLI mirrors with schedule subcommand NEW -->

```go
func runPrimary(ctx context.Context, args []string, log *slog.Logger) int {
    if len(args) == 0 {
        fmt.Fprintln(os.Stderr,
            "Usage: gatewayctl primary state|force-up|force-down|schedule|lifecycles [flags]")
        return 2
    }
    switch args[0] {
    case "state":         return runPrimaryState(ctx, args[1:], log)
    case "force-up":      return runPrimaryForceUp(ctx, args[1:], log)
    case "force-down":    return runPrimaryForceDown(ctx, args[1:], log)
    case "schedule":      return runPrimarySchedule(ctx, args[1:], log)
    case "lifecycles":    return runPrimaryLifecycles(ctx, args[1:], log)
    default:
        fmt.Fprintf(os.Stderr, "unknown primary subcommand: %s\n", args[0])
        return 2
    }
}
```

<!-- Existing gatewayctl/main.go subcommand dispatcher — pattern shows how to add "primary" case -->

```go
// Inside main() dispatcher
switch cmd {
case "emerg":       return runEmerg(ctx, args[1:], log)
// ... other cases
case "primary":     return runPrimary(ctx, args[1:], log)  // NEW Phase 6.6
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gateway/cmd/gatewayctl/primary.go with 5 subcommands</name>
  <files>gateway/cmd/gatewayctl/primary.go, gateway/cmd/gatewayctl/main.go</files>
  <read_first>
    - gateway/cmd/gatewayctl/emerg.go full file (~80-200 LOC; verbatim pattern for runEmergState/ForceProvision/ForceDestroy/Lifecycles)
    - gateway/cmd/gatewayctl/main.go (dispatcher pattern — find switch on cmd; insert "primary" case)
    - gateway/internal/primary/schedule.go (Plan 06.6-05 — ParseScheduleEnv + NextTransition)
    - gateway/internal/redisx/primary.go (Plan 06.6-07 — PublishPrimaryEvent + PrimaryStateKey)
    - gateway/db/queries/primary_lifecycles.sql (Plan 06.6-07 sqlc — for ListPrimaryLifecycles query; may need to add a `:many` query)
  </read_first>
  <action>(a) gatewayctl/primary.go: `package main`. Imports: stdlib + config + redisx + primary + db sqlc queries + go-redis + pgx. Implementar `runPrimary` dispatcher como mostrado em <interfaces>. Implementar 5 subcommands: (1) runPrimaryState: lê cfg via config.Load() + cria redis client + HGetAll redisx.PrimaryStateKey() + print formatado "State: <state>\nLifecycle ID: <id>\nPod URL: <url>\nInstance: <id>\nEntered at: <unix→time>"; (2) runPrimaryForceUp: cria redis client + redisx.PublishPrimaryEvent(ctx, rdb, redisx.PrimaryEvent{Type:"force_up_request", Reason:"manual_gatewayctl", ReplicaID:"gatewayctl-cli", SinceUnix:time.Now().Unix()}); print "force-up request published"; (3) runPrimaryForceDown: análogo com Type:"force_down_request"; (4) runPrimarySchedule: chama primary.ParseScheduleEnv(cfg); se err != nil print error + return 1; print rule.Timezone.String(), rule.UpHour, rule.DownHour, rule.Days (sorted), rule.GraceRampDownS, rule.Disabled; chama rule.NextTransition(time.Now()) + print next transition time + kind; suporta `--show` ou no flags (default action é show); (5) runPrimaryLifecycles: cria pgxpool + executa SQL `SELECT id, started_at, first_health_pass_at, drain_started_at, ended_at, trigger_reason, vast_instance_id, accepted_dph, total_cost_brl, shutdown_reason FROM ai_gateway.primary_lifecycles ORDER BY started_at DESC LIMIT 20` (limit configurable via flag.IntVar); print tabela tab-separated com headers. Implementar `--limit N` flag via `flag.NewFlagSet("primary lifecycles", flag.ContinueOnError)` + flag.Int("limit", 20). (b) gatewayctl/main.go: localizar dispatcher switch (provavelmente em main() function) + adicionar `case "primary": return runPrimary(ctx, args[1:], log)`. Confirmar imports atualizados se necessário.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./cmd/gatewayctl/...</automated>
  </verify>
  <done>gatewayctl primary binary compila. 5 subcommands disponíveis via dispatcher.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gateway/cmd/gatewayctl/primary_test.go — 6 unit tests covering each subcommand (DB-fetch lifecycles deferred to Plan 06.6-10)</name>
  <files>gateway/cmd/gatewayctl/primary_test.go</files>
  <behavior>
    - TestRunPrimary_NoArgs_UsageHelp: runPrimary(ctx, []string{}, log) → exit code 2 + stderr contains "Usage:"
    - TestRunPrimary_UnknownSubcommand: runPrimary(ctx, []string{"xxx"}, log) → exit code 2 + stderr contains "unknown primary subcommand"
    - TestRunPrimaryState_HappyPath: miniredis seed HSET gw:primary:state state=ready lifecycle_id=42 → runPrimaryState → stdout contains "ready" + "42"
    - TestRunPrimaryForceUp_PublishesEvent: miniredis + Subscribe gw:primary:events; chamar runPrimaryForceUp → received event has Type="force_up_request"
    - TestRunPrimaryForceDown_PublishesEvent: análogo
    - TestRunPrimarySchedule_PrintsResolvedRule: cfg defaults → runPrimarySchedule → stdout contains "America/Sao_Paulo", "UpHour: 8", "DownHour: 22", "Disabled: true" (soak gate per WAVE0-GATES Decision 5)
    - TestRunPrimaryLifecycles_FetchesFromDB: t.Skip with explicit pointer to Plan 06.6-10 — integration coverage lives in gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go (created by Plan 06.6-10 Task 3 per BLOCKER 3 revision); this skip is documented and intentional, analog to gateway/cmd/gatewayctl/emerg_test.go lines 14-19 deferral pattern
  </behavior>
  <read_first>
    - gateway/cmd/gatewayctl/emerg_test.go (analog test harness with miniredis + capturing stdout/stderr; especially lines 14-19 module comment that explicitly defers DB-fetch CLI test to integration territory — same pattern this plan adopts)
    - gateway/cmd/gatewayctl/primary.go (Task 1 output — confirm function signatures)
  </read_first>
  <action>Criar primary_test.go com `package main`. Imports: stdlib + miniredis + testify/require + bytes + io. Module-level doc comment espelhando emerg_test.go:1-19 pattern: `// Package main: unit tests for the gatewayctl primary subcommand family (Plan 06.6-09). // Tests focus on dispatcher exit codes, miniredis-backed state/force-up/force-down/schedule paths. // The DB-backed lifecycles subcommand is exercised via flag parsing only here — full SQL round-trip // is integration-test territory (testcontainers); see gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go // created by Plan 06.6-10 Task 3 per BLOCKER 3 revision (2026-05-17).` Helper: redirect os.Stdout/os.Stderr para bytes.Buffer via os.Pipe trick (analog emerg_test.go pattern). Implementar 6 tests da tabela <behavior>. **CRITICAL — TestRunPrimaryLifecycles_FetchesFromDB:** explicit t.Skip with msg `t.Skip("integration test — see gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go (Plan 06.6-10 Task 3 closes BLOCKER 3 coverage gap)")` — NÃO usar generic message; pointer should be specific to file path. Pode opcionalmente still validate flag parsing (no DB call) via subtest like TestRunPrimaryLifecycles_FlagParsing — invoke with `[]string{"--limit", "5"}` against nil pgxpool + expect graceful error (not panic) → exit code 1; this is flag-parsing-only sub-coverage, NOT a DB round-trip substitute.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./cmd/gatewayctl/... -count=1 -race -run TestRunPrimary</automated>
  </verify>
  <done>6 tests verdes (TestRunPrimaryLifecycles_FetchesFromDB t.Skip aponta explicitamente para Plan 06.6-10's gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go). Coverage gap fechado em Wave 4 via Plan 10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation (gatewayctl binary) → Redis (gw:primary:events) | Outbound; operator-controlled invocation; force-up/force-down require Redis access (cluster-internal in production) |
| operator workstation (gatewayctl binary) → Postgres (primary_lifecycles SELECT) | Outbound; read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (no new threats) | — | — | — | gatewayctl CLI runs only on ops-claude host with operator credentials; same trust model as existing gatewayctl emerg subcommand. |
</threat_model>

<verification>
- `cd gateway && go build ./cmd/gatewayctl/...` succeeds
- `cd gateway && go test ./cmd/gatewayctl/... -count=1 -race -run TestRunPrimary` all green (6 tests; 1 skip with pointer to Plan 10)
- `cd gateway && ./gatewayctl primary --help 2>&1 | grep -c 'state\\|force-up\\|force-down\\|schedule\\|lifecycles' >= 5` (all 5 subcommands listed in usage)
- `cd gateway && grep -q 'primary_lifecycles_integration_test.go' gateway/cmd/gatewayctl/primary_test.go` (Skip message points to Plan 10's test file)
</verification>

<success_criteria>
- Operator has CLI tools to verify state, force lifecycle, inspect schedule, audit history
- Tests cover all 5 subcommands at unit level (lifecycles fetch DB round-trip lives in Plan 06.6-10 Task 3 integration test per BLOCKER 3 revision)
- Pattern parity with existing gatewayctl emerg makes operator UX consistent
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-09-SUMMARY.md`.
</output>
