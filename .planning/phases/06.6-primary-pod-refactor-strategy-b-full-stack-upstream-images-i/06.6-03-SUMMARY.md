---
phase: 06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i
plan: 03
subsystem: infra
tags: [config, env-vars, primary-pod, wave0-locked, supervisord, qwen3.6, b9191, sha-pinned, schedule, soak-gate]

# Dependency graph
requires:
  - phase: 06.6-01
    provides: WAVE0-GATES.md operator-locked decisions (image SHA pins, B1 Jinja, soak gate, cold-start budget)
  - phase: 06.6-02
    provides: vastutil/ shared helpers extracted from emerg lifecycle (reused by Plan 04 buildPrimaryCreateRequest consumer)
provides:
  - 24 PrimaryXxx config fields exposed via gateway/internal/config/config.go Config struct
  - Wave 0 LOCKED defaults enforced at config layer (4 image @sha256 digests, B1 Jinja empty, Disabled=true, ColdStart=2400, FailureCooldown=300, ProvisionLead=1800)
  - 12 TestConfig_PrimaryPod_* tests asserting defaults match WAVE0-GATES literals + override paths
affects:
  - 06.6-04 (primary lifecycle.go buildPrimaryCreateRequest — consumes cfg.PrimaryTemplateImage/Speaches/Infinity/DCGM/QwenWeightsKey/QwenWeightsSHA256/QwenJinjaKey/QwenJinjaSHA256/LlamaArgs/Whisper*/BGEM3*/VastPriceCapDPH/MinIO*)
  - 06.6-05 (primary schedule.go ShouldBeProvisioned — consumes cfg.PrimaryPodScheduleTimezone/UpHour/DownHour/Days/Disabled/ProvisionLeadSeconds + GraceRampDownSeconds)
  - 06.6-06a (reconciler.evaluateAsleep — consumes cfg.PrimaryProvisionFailureCooldownSeconds + ColdStartBudgetSeconds)
  - 06.6-08 (gateway boot wire-up — surfaces cfg.PrimaryXxx via DI to lifecycle/schedule/reconciler)
  - 06.6-12 (PR3 cleanup — operator runbook documents soak-gate flip after Live UAT GREEN)

# Tech tracking
tech-stack:
  added: []  # No new deps — reuses existing envOr/atoiOr/csvOr/boolOr/floatOr helpers
  patterns:
    - "Wave 0 LOCKED defaults enforced at config layer (literal SHA digest match in tests)"
    - "FAIL-FAST SHA policy at config: Whisper/BGE SHA fields use direct os.Getenv (no envOr default) — consumer must reject empty"
    - "Soak gate via boolean default (Disabled=true; operator manual flip after UAT GREEN)"
    - "Pre-warm offset (ProvisionLeadSeconds) honors schedule semantics vs cold-start reality"

key-files:
  created: []
  modified:
    - gateway/internal/config/config.go
    - gateway/internal/config/config_test.go

key-decisions:
  - "Image defaults SHA-pinned via @sha256:<digest> suffix in envOr default literals (T-06.6-SC tampering mitigation enforced at config layer)"
  - "b9191 UPGRADED from Phase 6's prior build for the primary-pod path — Phase 6 emergency pod continues on its own image (separate EmergencyTemplateImage field, untouched)"
  - "PrimaryQwenJinjaKey + SHA256 default empty (B1 GGUF-embedded LOCKED) — `--jinja` flag alone extracts PEG-native parser; override via env permitted for future B2 MinIO fallback"
  - "PrimaryPodScheduleDisabled default true (soak gate) — same pattern as Phase 6 PR2 EMERGENCY_POD_DISABLED=true initial deploy"
  - "PrimaryProvisionColdStartBudgetSeconds default 2400 (40min) — generous margin accommodating slow inet hosts + multi-stage image pull + aria2c weight download + 4-service supervisord startup"
  - "PrimaryProvisionFailureCooldownSeconds default 300 (mirror emerg's 60s scaled-up for schedule cadence)"
  - "Whisper/BGE SHA fields use direct os.Getenv (no envOr default) — empty passthrough so Plan 06.6-04 buildPrimaryCreateRequest rejects at build time with ErrMissingWhisperSHA"
  - "Standard library testing (t.Errorf) used instead of testify — preserves consistency with existing Phase 6 test pattern in config_test.go"

patterns-established:
  - "Phase 6.6 primary-pod config block: 24 fields under explicit header comment citing WAVE0-GATES.md decisions; mirrors Phase 6 emergency-pod block structure (struct field doc → Load() read with default literal → test default + override)"
  - "Wave 0 fidelity testing: TestConfig_PrimaryPod_TemplateImageIsB9191SHAPinned + TestConfig_PrimaryPod_AllUpstreamImagesSHAPinned assert literal SHA digest prefixes — image bumps require WAVE0-GATES amendment + operator sign-off (no silent drift)"
  - "Soak gate enforcement at config layer: TestConfig_PrimaryPod_DisabledDefaultsTrue_SoakGate guards Decision 5 — prevents accidental production scheduling before UAT GREEN"

requirements-completed: []  # PLAN frontmatter requirements: []

# Metrics
duration: ~12 min
completed: 2026-05-18
---

# Phase 06.6 Plan 03: gateway/internal/config/config.go 24 Primary* fields + Wave 0 LOCKED defaults Summary

**24 PrimaryXxx config fields wired into gateway Config struct with Wave 0 LOCKED defaults — 4 upstream OCI images SHA-pinned (b9191/speaches/infinity/dcgm), B1 GGUF-embedded Jinja LOCKED (empty key+sha256), soak gate default Disabled=true, 40min cold-start budget, 30min pre-warm offset, FAIL-FAST Whisper/BGE SHA policy.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-18T (worktree spawn time)
- **Completed:** 2026-05-18T
- **Tasks:** 2 (both auto, Task 2 with tdd="true" semantics)
- **Files modified:** 2

## Accomplishments

- 24 PrimaryXxx fields added to `gateway/internal/config/config.go` Config struct under explicit `Phase 6.6 — primary pod auto-provisioning` header comment, citing WAVE0-GATES.md Decisions 1+3+5+6 and Reviews #6 + #8.
- 24 Load() reads added with Wave 0 LOCKED literal defaults:
  - PrimaryTemplateImage = `ghcr.io/ggml-org/llama.cpp:server-cuda-b9191@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3` (UPGRADED from Phase 6's prior b9128 build for Qwen3.6 SSM tensor support)
  - PrimarySpeachesImage = `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3@sha256:5c6206a349e90b9a6782342917e72f84fc7cb60e8afd540f6aa625831ac1fd0f`
  - PrimaryInfinityImage = `michaelf34/infinity:0.0.77@sha256:11e8b3921b9f1a58965afaad4a844c435c9807cbc82c51e47cb147b7d977fc88`
  - PrimaryDCGMImage = `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.3-4.8.2-distroless@sha256:60d3b00ac80b4ae77f94dae2f943685605585ad9e92fdccda3154d009ae317cc`
  - PrimaryQwenJinjaKey + PrimaryQwenJinjaSHA256 = "" (B1 GGUF-embedded LOCKED)
  - PrimaryPodScheduleDisabled = true (soak gate)
  - PrimaryProvisionColdStartBudgetSeconds = 2400 (40min)
  - PrimaryProvisionFailureCooldownSeconds = 300 (5min scaled-up vs emerg 60s)
  - PrimaryPodScheduleProvisionLeadSeconds = 1800 (30min pre-warm offset)
- 12 TestConfig_PrimaryPod_* tests added to `gateway/internal/config/config_test.go` validating defaults, env overrides, and Wave 0 fidelity (literal SHA digest assertions, soak gate, B1 embedded Jinja).
- `go build ./...` succeeds. `go test ./gateway/internal/config/... -count=1 -race` all green (existing + 12 new = 17 PASS).
- Zero docker-in-docker env vars introduced — Wave 0 spike REJECTED nested-runtime orchestration; supervisord lives inside Plan 06.6-04 custom multi-stage image as PID 1 (no user-facing env required).
- Existing helpers (`envOr`, `atoiOr`, `csvOr`, `boolOr`, `floatOr`) at `config.go:340-396` reused verbatim — zero new helper created.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add 24 Primary* struct fields + Load() block** — `3927648` (feat)
2. **Task 2: config_test.go — coverage for 24 Primary defaults + Wave 0 LOCKED assertions** — `aaa94f0` (test)

## Files Created/Modified

- `gateway/internal/config/config.go` — Added 24 new `Primary*` exported fields under Phase 6.6 header comment block; added 24 corresponding Load() reads with Wave 0 LOCKED literal defaults. +80 lines.
- `gateway/internal/config/config_test.go` — Added `phase6_6OptionalEnv` slice (24 env vars), `clearPhase6_6` helper, and 12 `TestConfig_PrimaryPod_*` tests. +412 lines.

## Decisions Made

- **Image SHA-pinned defaults literal in envOr second argument** (not extracted to package-level const). Rationale: matches Phase 6 EmergencyTemplateImage convention at `config.go:262`; image bumps then require operator sign-off via PR review of the config.go default change, observable in `git blame` (T-06.6-SC tampering mitigation enforced via PR review trail).
- **Standard library `t.Errorf`/`t.Fatalf` in tests (NOT testify `require`)**. PLAN's Behavior spec showed `require.Contains` and `require.Equal` examples, but the existing `config_test.go` (Phase 3/4/6/7 test blocks) uses standard library exclusively, and testify is only an `indirect` dep in `go.mod`. Consistency with the existing test pattern in the same file was prioritized over PLAN snippet syntax. Tests assert the same semantic checks (strings.Contains for digest prefixes, equality for empty defaults).
- **Phase 6 emergency pod's b9128 reference left UNTOUCHED** at `config.go:133, 303, 307` (`EmergencyTemplateImage` default). The Phase 6.6 plan's global `<verification>` block at line 207-208 included `grep -c 'b9128' gateway/internal/config/config.go == 0`, but this conflicts with WAVE0-GATES.md explicit statement: "Phase 6 production emergency pod can continue on b9128 (uses Qwen3.5, pure Transformer). Phase 6.6 primary pod MUST use b9191+." Treated as scope-boundary: Plan 06.6-03 only modifies PRIMARY-path defaults. See deviation note below.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] PLAN global `<verification>` `grep b9128 == 0` was over-broad**
- **Found during:** Task 1 verification pass after initial commit attempt.
- **Issue:** Plan line 207-208 specifies `grep -c 'b9128' gateway/internal/config/config.go == 0` as a global verification. This is impossible to satisfy without also editing Phase 6 emergency pod's `EmergencyTemplateImage` default (out-of-scope for Plan 06.6-03 — Phase 6 emergency pod must remain on its prior build per WAVE0-GATES.md Decision 1 "Phase 6 production emergency pod can continue on b9128").
- **Fix:** Interpreted the verification intent as scoped to NEW primary-pod fields (Task 1 explicit acceptance criteria at line 139-144 does NOT include this grep). Removed only the descriptive textual mention of "b9128" from the new Phase 6.6 doc comment (changed "UPGRADED from Phase 6 b9128" → "UPGRADED from Phase 6's prior build") to avoid introducing a NEW b9128 reference via my own changes. Phase 6 production code (lines 133, 303, 307) intentionally untouched.
- **Files modified:** gateway/internal/config/config.go (doc comment only)
- **Verification:** `grep -c 'server-cuda-b9191@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3'` = 1 (PRIMARY default). `grep -c 'DOCKERD_\|INNER_DOCKER_\|DIND_\|NESTED_'` = 0 (NO DinD env vars introduced).
- **Committed in:** `3927648` (Task 1 commit)

**2. [Rule 1 - Bug] PLAN Task 1 `<verify>` `grep DOCKERD_|...|NESTED_` initially triggered false positive on descriptive comment**
- **Found during:** Task 1 verification (first pass).
- **Issue:** Initial doc comment explicitly enumerated rejected env-var prefixes (`NO DinD env vars (DOCKERD_*, INNER_DOCKER_*, DIND_*, NESTED_*)`) for self-documenting clarity. This matched the grep pattern even though no actual env vars with those prefixes were introduced.
- **Fix:** Rephrased the comment to "NO docker-in-docker env vars" without listing the literal prefixes. Same semantic clarity; satisfies the grep literally.
- **Files modified:** gateway/internal/config/config.go (doc comment only)
- **Verification:** `grep -c 'DOCKERD_\|INNER_DOCKER_\|DIND_\|NESTED_' gateway/internal/config/config.go` = 0.
- **Committed in:** `3927648` (Task 1 commit, after fix-and-amend cycle resolved before commit was finalized)

---

**Total deviations:** 2 auto-fixed (both Rule 1 — verification spec over-broad vs intent; both fixed inline within the Task 1 commit cycle before staging)
**Impact on plan:** Zero scope creep. Both deviations resolved cosmetic over-specification in the plan's verification criteria; the underlying acceptance criteria (Task 1 done lines 137-145) was satisfied literally. No PRIMARY-path defaults changed. No Phase 6 emergency code touched.

## Issues Encountered

None — both tasks executed on first build/test pass after the doc-comment cosmetic fixes noted above. No runtime test failures, no compile errors, no import drift.

## Self-Check

- [x] gateway/internal/config/config.go modified (FOUND).
- [x] gateway/internal/config/config_test.go modified (FOUND).
- [x] Commit `3927648` exists (FOUND via `git log --oneline -3`).
- [x] Commit `aaa94f0` exists (FOUND via `git log --oneline -3`).
- [x] `go build ./...` succeeds (verified).
- [x] `go test ./gateway/internal/config/... -count=1 -race` passes (verified).
- [x] 12 `TestConfig_PrimaryPod_*` functions present (verified via `grep -c '^func TestConfig_PrimaryPod' = 12`).
- [x] PrimaryTemplateImage default contains literal `server-cuda-b9191@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3` (verified via grep — count 1).
- [x] 4 image @sha256: defaults present (verified via grep — count 5, including 1 from descriptive comment block; 4 in actual envOr defaults).
- [x] No docker-in-docker env vars (verified via grep `DOCKERD_|INNER_DOCKER_|DIND_|NESTED_` — count 0).

## Self-Check: PASSED

## Next Plan Readiness

- **Plan 06.6-04 (primary onstart.go + lifecycle.go):** Can now consume `cfg.PrimaryTemplateImage`, `cfg.PrimarySpeachesImage`, `cfg.PrimaryInfinityImage`, `cfg.PrimaryDCGMImage` in the Dockerfile `FROM` lines and `buildPrimaryCreateRequest` payload assembly. The fail-fast SHA gate (`ErrMissingWhisperSHA` / `ErrMissingBGEM3SHA` per reviews #6) must be enforced in Plan 04's build step — config layer passes empty values through intentionally.
- **Plan 06.6-05 (schedule.go + fsm.go):** Can consume `cfg.PrimaryPodScheduleTimezone`, `cfg.PrimaryPodScheduleUpHour`, `cfg.PrimaryPodScheduleDownHour`, `cfg.PrimaryPodScheduleDays`, `cfg.PrimaryPodScheduleDisabled`, `cfg.PrimaryPodScheduleProvisionLeadSeconds`, `cfg.PrimaryPodScheduleGraceRampDownSeconds` to evaluate the schedule rule and emit FSM state transitions.
- **Plan 06.6-06a (reconciler core):** Can consume `cfg.PrimaryProvisionFailureCooldownSeconds` (`evaluateAsleep` cooldown check) and `cfg.PrimaryProvisionColdStartBudgetSeconds` (40min provision-failure threshold).
- **Plan 06.6-08 (boot wire):** Plain DI — pass `cfg` to lifecycle/schedule/reconciler constructors; no new boot-time validation required at this layer.
- **Plan 06.6-12 (PR3 cleanup):** Operator runbook must document the soak-gate manual flip (`PRIMARY_POD_SCHEDULE_DISABLED=false`) gated on Plan 06.6-11 Live UAT GREEN.

No blockers. Phase 6 emergency pod path is unaffected (separate EmergencyXxx fields, separate b9128 image, separate ProvisionFailureCooldownSeconds=60s).

---
*Phase: 06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i*
*Completed: 2026-05-18*
