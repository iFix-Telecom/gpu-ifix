---
wave: 4
depends_on: [20-04, 20-05]
autonomous: false
requirements: [FF-01, FF-02, FF-03, BL-01, AL-01, OBS-11]
files_modified: []
---

# Plan 20-06 — HUMAN-UAT: live coldstart fast-fail on real Vast (regimes 1 & 3)

Fast-fail is timing logic against real paid infra; the unit tests use the `budget=0` idiom
but CANNOT prove the loop kills a REAL pod at ~budget (not at the `coldstart_budget_s`
ceiling) nor that the Vast logs API (FF-03) actually surfaces the onstart heartbeat. This is
a blocking human-verify checkpoint. Budget: ~$1-2 of Vast spend, ~2-3 pod cycles.

**This plan also RESOLVES the two open questions the code deferred:**
- **Q2 — Vast logs API `result_url` shape:** capture the real `PUT /instances/request_logs/{id}/`
  response body + the `GET result_url` payload on the first live cycle; confirm the 20-03
  defensive decode matches, and tighten the JSON keys / add a result_url-host allowlist if
  needed.
- **Q3 — SSH-tail fallback:** decide from live evidence whether the Vast logs API is reliable
  enough to remain the SOLE progress source (leave SSH-tail deferred) OR whether it drops
  heartbeats badly enough to justify building the SSH-tail leg (follow-up quick-task).

**Pre-req:** the primary-pod image must include the OBS-11 heartbeat change (20-02) — rebuild
+ publish the pod image before this UAT, or the download will be silent and FF-02 cannot be
observed. Confirm the deployed gateway carries 20-04.

## Tasks

<task type="human">
<name>Regime 1 (host morto → created_state_timeout)</name>
<action>
Set `created_budget_s` low (~120s) via the dashboard slider (20-05). Force a primary
provision biased toward a host that gets stuck at `actual_status=created` (a known-flaky
host, or set the allowlist to a machine that historically hangs at created; the SEED cites
Norway 24953). Watch `gatewayctl primary` state + the gateway logs. ASSERT: the lifecycle
closes with `shutdown_reason=created_state_timeout` at roughly `created_budget_s` (±one poll
interval), the Vast instance is DESTROYED (Vast UI shows 0 instances), and the elapsed time
is FAR below `coldstart_budget_s`. Record the actual elapsed vs budget.
</action>
<acceptance_criteria>
- A primary_lifecycles row closes with `shutdown_reason=created_state_timeout`.
- Wall-clock from first `created` observation to destroy is within ~1 poll interval of `created_budget_s`, and well under `coldstart_budget_s`.
- Vast instance count returns to 0 (no orphan; confirm via Vast UI / API).
- If the machine failed and this was the 2nd consecutive failure, its machine_id appears in `pod_config.vast_machine_blocklist` (BL-01) and is absent from the allowlist.
</acceptance_criteria>
</task>

<task type="human">
<name>Regime 3 (download stall → progress_stall_timeout, exercises FF-03)</name>
<action>
Set `progress_stall_budget_s` low (~120s). Force a coldstart where the weights download
STALLS mid-file with the transfer connection OPEN but BYTES FROZEN — e.g. point `MINIO_*` at
an endpoint that accepts the connection then hangs (a tarpit / firewall-drop after connect),
so `mc cp` opens the file and the dest-file size stops growing (the OBS-11 `progress bytes=`
line goes FLAT while telemetry stays `Available`). The pod is UP but never health-passes.
Watch the gateway logs for the OnstartLog fetch (FF-03) + the byte-progress stall detector.
ASSERT: the lifecycle closes with `shutdown_reason=progress_stall_timeout` at roughly
`progress_stall_budget_s`, the pod is destroyed, and the gateway logged an OnstartLog fetch
returning `Status=Available` with a FLAT `bytes=` (proving FF-03 leg (a) + the byte-frozen
detection work live). Capture the raw `result_url` PUT response + GET payload for Q2 + the
result_url HOST (feeds the REQUIRED host-allowlist follow-up, 20-03 finding #8). **Capture the
REAL `actual_status` during the download window (do NOT infer).**

**GLOBAL-FAILURE / no-blocklist assertion (Codex #9):** breaking the shared weights store is a
GLOBAL failure, not a bad host. Snapshot `pod_config.vast_machine_blocklist` BEFORE this run
and AFTER: ASSERT the stalled machine_id was NOT added — `progress_stall_timeout` is
deliberately EXCLUDED from auto-blocklist (a global R2 outage must not cascade-poison the
market). This run PROVES the classifier's exclusion works.
</action>
<acceptance_criteria>
- A primary_lifecycles row closes with `shutdown_reason=progress_stall_timeout`.
- Gateway logs show an `OnstartLog` fetch returning `Status=Available` with a FLAT `bytes=` (byte-frozen stall verified live; FF-03 leg (a) verified).
- The kill fires at ~`progress_stall_budget_s`, not at `coldstart_budget_s`.
- Vast instance destroyed, 0 orphans.
- **`vast_machine_blocklist` is UNCHANGED across the run — the stalled machine_id is NOT blocklisted (proves progress_stall_timeout exclusion / global-failure safety).**
- The captured `result_url` response shape + HOST are recorded (Q2 resolved; host feeds the required allowlist follow-up).
- The REAL `actual_status` during the download window is recorded (running vs loading).
</acceptance_criteria>
</task>

<task type="human">
<name>Happy path (auto-allowlist) + Q3 decision</name>
<action>
Let a HEALTHY coldstart complete (all 4 endpoints pass, `markReady`). ASSERT the successful
host's machine_id is auto-added to `pod_config.vast_machine_allowlist` (AL-01, deduped, cap
20) and removed from the blocklist if it was there. Then, from the two failure cycles + this
success, JUDGE Q3: did the Vast logs API reliably surface the heartbeat every time FF-02
needed it? If YES → leave SSH-tail deferred (close the ponytail marker as "not needed"). If
NO (logs API lagged/dropped past `progress_stall_budget_s`, causing a false or missed stall)
→ file a follow-up quick-task to add the SSH-tail leg and note the observed failure mode.
</action>
<acceptance_criteria>
- After a healthy provision, the host machine_id is present in `pod_config.vast_machine_allowlist` and absent from the blocklist.
- Allowlist respects cap-20 FIFO + dedup on the live row.
- A written Q3 verdict (logs-API-sufficient OR SSH-tail-needed) with the live evidence, recorded in the phase VERIFICATION notes.
</acceptance_criteria>
</task>

<verification>
- Regime 1 kills at ~created_budget_s with reason `created_state_timeout`; regime 3 kills at ~progress_stall_budget_s with reason `progress_stall_timeout` (byte-frozen, telemetry Available); neither burns to `coldstart_budget_s`.
- FF-03 Vast logs API leg (a) verified surfacing the byte-progress heartbeat on a live pod; Q2 result_url shape + HOST recorded (host feeds the required allowlist follow-up).
- Auto-blocklist observed ONLY on the machine-attributable regime-1 failure (2nd consecutive); the regime-3 (global weights-store) stall does NOT blocklist; auto-allowlist observed on success; no machine in both lists.
- Zero Vast orphans after all cycles (BestEffortDestroy fired every close).
- Q3 decision recorded (SSH-tail stays deferred unless live evidence demands it).

must_haves:
  - "on real Vast, a doomed coldstart is detected and destroyed in ~2 min (at its budget), not at the 40-90 min coldstart ceiling — for BOTH regime 1 (stuck created) and regime 3 (byte-frozen download stall)"
  - "the Vast logs API actually delivers the byte-progress heartbeat to the reconciler on a live pod (FF-03 leg a proven; Q2 shape + host confirmed)"
  - "the picker learns SAFELY: a machine-attributable failure (regime 1) parks the host; a global weights-store stall (regime 3) does NOT blocklist; a proven host is allowlisted — verified live on pod_config"
  - "no paid Vast instances are left running after the UAT"
</verification>

<threat_model>
Attack surface: this UAT spins REAL paid Vast pods and deliberately induces failures.
- **Billing-burn / orphans:** the whole point is to prove fast-fail SAVES money, but a UAT
  bug (or a `BestEffortDestroy` 429, per the STATE.md attempt-9 orphan that burned ~$2.17
  over 3.5h) could leave a paid instance running. Mitigation: after EACH cycle, the operator
  independently confirms 0 instances via the Vast UI/API (not just the gateway's view) and
  manually `curl DELETE`s any orphan; keep the session's cumulative spend visible.
- **Cred handling for the regime-3 stall:** breaking MinIO/R2 read creds to induce the stall
  must NOT leak a real secret into logs or the phase notes — use a scoped/temporary bad cred
  or an unreachable endpoint, not a redacted-real-key. The Vast API key + MinIO creds live in
  the pod env / gateway env, never pasted into the plan or VERIFICATION.
- **Blocklist pollution of prod picker:** auto-blocklisting during UAT writes REAL machine_ids
  into the live `pod_config.vast_machine_blocklist`, which the production picker reads. After
  UAT, review the blocklist and prune any machine parked purely by the induced-failure test
  (the blocklist has no auto-expiry yet — ponytail-marked). Record which machine_ids the UAT
  added so they can be pruned.
- **result_url SSRF surface (finding #8 — REQUIRED follow-up):** the 20-03 GET already
  enforces `scheme==https` + no-auth. This UAT MUST record the real `result_url` host; adding a
  HOST allowlist (e.g. vast.ai / *.cloudflarestorage) to the 20-03 GET is a REQUIRED follow-up
  quick-task once the host shape is observed — promoted from "maybe later" to a must-do before
  FF-03 is trusted in steady-state prod.
</threat_model>
