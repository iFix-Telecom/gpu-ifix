---
wave: 2
depends_on: [20-01, 20-02, 20-03]
autonomous: true
requirements: [FF-01, FF-02, BL-01, AL-01]
files_modified:
  - gateway/internal/primary/reconciler.go
  - gateway/internal/primary/reconciler_test.go
---

# Plan 20-04 — Reconciler: coldstart fast-fail (FF-01/FF-02) + outcome-driven lists (BL-01/AL-01)

All four requirements land in ONE plan because all four edit `gateway/internal/primary/
reconciler.go` — FF-01/FF-02 inside `waitForReadyOrDestroy` (the coldstart poll loop) and
BL-01/AL-01 at the `provisionLifecycle` return (line 1438). Splitting them into parallel
plans would put two agents into the SAME file in the same wave and conflict; the suggested
"FF ‖ BL/AL" parallelism is deliberately merged here for that reason. FF-03's `OnstartLog`
(20-03) and the new budget fields (20-01) are dependencies; OBS-11 (20-02) makes FF-02's
signal real in production.

**Test idiom (no injectable clock — RESEARCH):** drive timing with `budget=0 fires on the
first poll` (the `>=`-not-`>` trick at reconciler.go:1614) + the package var
`primaryInstancePollIntervalForTest` + scripted `fakeVast.getInstanceFn`/`onstartLogFn`
sequences. Do NOT add a fake clock.

**Decision resolutions baked in:**
- **BL-01 off-by-one (Q1) → ban when `failStreak >= 1` at the failure hook.** `failStreak`
  is read at the TOP of `provisionLifecycle` (line 1292, BEFORE this attempt is counted; the
  open lifecycle row is excluded from `CountConsecutiveFailedPrimaryProvisions`). So after
  THIS attempt fails, the effective consecutive-failure count is `failStreak + 1`. To ban on
  the 2nd consecutive failure (decision #4 "ban when fail_streak≥2"), gate the blocklist
  append on `failStreak >= 1`. The `failStreak` local is already in scope at line 1438.
- **New close reasons (Q4) are free-form DB strings** (`created_state_timeout`,
  `progress_stall_timeout`) — `closeLifecycle` writes `shutdown_reason` as `pgtype.Text` to
  the DB; there is NO bounded prometheus label for primary close reasons (confirmed:
  `GatewayEmergencyLifecyclesTotal` is emerg-only; primary has none). No alerter-fingerprint
  wiring needed.
- **FF-02 cadence (Q6):** the Vast logs API is async (~seconds); do NOT fetch every ~5s
  poll. Fetch `OnstartLog` on a slower sub-cadence (every ~30s, gated by a
  `lastLogFetchAt` check inside the loop) so it never stretches the poll tick.
- **FF-02 stall = frozen BYTES, phase-scoped, telemetry-aware (Codex HIGH #1/#2/#3):** the
  stall fires ONLY on `Status=Available` telemetry with the dest-file byte SUM provably not
  advancing past budget, and ONLY between the download `fetching` (arm) and all-files-`ok`
  (disarm) brackets. `NotReady`/`FetchError`/`Empty` telemetry = UNKNOWN → never fire → ride
  to the `coldstart_budget_s` ceiling (the safe default; the deferred SSH-tail leg would
  close that gap). A healthy slow startup AFTER a good download is out of scope (disarmed).
- **BL-01 failure-classification (Codex HIGH #4 + #9):** blocklist ONLY on a machine-attributable
  close reason (allowlist: created_state_timeout, instance_terminal_state(_confirmed),
  public_port_bind_timeout) — never on cancelled_in_flight / health_timeout / create/config/
  status_msg errors, and never on `progress_stall_timeout` (download stalls are potentially
  GLOBAL — R2/network — so blocklisting risks cascade-poisoning the whole market during an R2
  outage; #9). AL-01 (success → allowlist) is unaffected.

## Tasks

<task type="tdd">
<name>RED: FF-01 created-budget + FF-02 progress-stall + BL-01/AL-01 outcome tests</name>
<action>
Add tests to `gateway/internal/primary/reconciler_test.go`:

FF-01 (created-budget): script `fakeVast.getInstanceFn` to always return
`vast.Instance{ActualStatus:"created"}` (never advances). Set the captured snapshot's
`CreatedBudgetS = 0` (via the provision-cfg seed the test controls) so the created-budget
fires on the FIRST `created` poll. Assert `waitForReadyOrDestroy` returns an error, the
lifecycle is closed with reason `created_state_timeout`, and `DestroyInstance` was called
(fakeVast.destroyCalls == 1). NEGATIVE test A: with `CreatedBudgetS` large and the instance
flipping to `running` before the budget, the created path does NOT fire. **NEGATIVE test B
(regime 2 — REQUIRED):** script `getInstanceFn` to return `created` once then `loading`
forever; set `CreatedBudgetS = 0`. Assert NO `DestroyInstance`, NO `created_state_timeout`
close — a created→loading pod is a healthy image-pull and must ride to the coldstart ceiling,
so the created-budget must have RESET on the loading transition.

FF-02 (progress-stall, BYTES-based + download-phase-scoped + telemetry-aware — Codex HIGH
#1/#2/#3). The stall is defined as **dest-file bytes provably frozen while the log telemetry
is Available**, scoped to the download phase, with telemetry-unavailable treated as UNKNOWN
(ride, never kill). Drive via `onstartLogFn` returning `vast.OnstartLogResult`:
- **STALL fires:** `onstartLogFn` returns `{Status:Available, Text:"…[download-weights] fetching qwen…\n…[download-weights] progress key=qwen bytes=1000\n"}` then keeps returning the SAME `bytes=1000` (frozen) forever; `ProgressStallBudgetS=0`. Assert close reason `progress_stall_timeout` + destroy. (The `fetching` line ARMS the phase; frozen bytes = stall.)
- **NEGATIVE (healthy slow download):** `onstartLogFn` returns Available with `bytes=` INCREASING each fetch (1000→2000→…) → stall does NOT fire (each byte advance resets the anchor), even past `ProgressStallBudgetS`.
- **TELEMETRY-UNAVAILABLE (REQUIRED, replaces the removed round-2 backstop):** `getInstanceFn` returns `running` (health failing), `onstartLogFn` returns `{Status:FetchError}` forever (Vast logs API down), `ProgressStallBudgetS=0` → the stall does **NOT** fire, NO destroy, NO `progress_stall_timeout` (UNKNOWN ⇒ ride to the coldstart ceiling — the SAFE default). Add the same assertion for `{Status:Empty}` and `{Status:NotReady}`.
- **DOWNLOAD-PHASE SCOPE (REQUIRED):** `onstartLogFn` returns Available with all files logging `fetching`→`progress`→`ok` (download COMPLETE), then the log advances into docker-compose/model-startup lines with NO further `progress bytes=` (a healthy slow STARTUP after a good download); `ProgressStallBudgetS=0`. Assert the stall does NOT fire — the download-stall timer must DISARM once every expected file logged `ok`.
- **REGIME-2 non-arm:** `loading`, `onstartLogFn` returns `{Status:NotReady}` (no `fetching` line yet) → not armed → rides.

Note the removal: the earlier "running + empty-telemetry → fires" backstop is DELETED — Codex
#3 shows it false-kills on a logs-API outage; empty/error telemetry is now UNKNOWN.

BL-01 (failure-CLASSIFIED auto-blocklist — Codex HIGH #4 + #9). Blocklisting on the wrong
failure class poisons the picker. Test a `machineAttributableReason(reason string)` gate:
- **MACHINE-ATTRIBUTABLE reasons → blocklist on `failStreak>=1`:** `created_state_timeout`,
  `instance_terminal_state`, `instance_terminal_state_confirmed`, `public_port_bind_timeout`.
  For each: `failStreak=1` + that close reason → assert `UpdatePodConfigFieldBlocklist`
  includes `offer.MachineID` + removed from allowlist; `failStreak=0` + same reason → NO
  blocklist (1st-failure grace).
- **EXCLUDED reasons → NEVER blocklist (no list mutation), even at `failStreak>=1`:**
  `progress_stall_timeout` (download stalls are potentially-GLOBAL — R2/network — so
  blocklisting risks cascading market poisoning during an R2 outage; Codex #9), `cancelled_in_flight`,
  `health_timeout`, schedule-driven `destroyed`, `create_error`, `build_create_request_failed:*`,
  `audit_write_failed`, `vast_status_msg_error:*` (image/config/auth-class). One test per
  excluded reason: failure + `failStreak=1` → assert ZERO `UpdatePodConfigFieldBlocklist` call.

AL-01: drive a provision that SUCCEEDS (health passes) and assert `offer.MachineID` was
written to the allowlist (dedup + FIFO cap 20) AND removed from the blocklist. Test the cap:
a pre-seeded 20-entry allowlist + a new success drops the OLDEST and appends the new (len
stays 20); a duplicate machine is not double-added.
</action>
<read_first>
- gateway/internal/primary/reconciler_test.go (fakeVast :82-135, fakeDBTX + SetQueriesForTest :494, budget=0/poll-interval idiom used by the existing port-bind timeout tests; export_test.go for exposed internals + any provision-cfg seed helper)
- gateway/internal/primary/reconciler.go (waitForReadyOrDestroy :1453-1680; the `>=` budget=0 comment :1614; provisionLifecycle failStreak :1292-1304; the line-1438 return)
- gateway/db/queries/pod_config.sql (UpdatePodConfigFieldBlocklist/Allowlist signatures — []int64)
</read_first>
<acceptance_criteria>
- New tests compile and FAIL (the created-budget, stall detector, and outcome hook don't exist yet).
- `cd gateway && go test ./internal/primary/ -run 'CreatedBudget|ProgressStall|Blocklist|Allowlist'` runs and reports failures for the new tests only.
</acceptance_criteria>
</task>

<task type="tdd">
<name>GREEN FF-01: created-state budget anchor in waitForReadyOrDestroy</name>
<action>
In `waitForReadyOrDestroy` (reconciler.go), add a SECOND budget anchor mirroring
`firstRunningAt`/`portBindBudget`, gated SPECIFICALLY on the `created` (host-morto) window —
NOT the whole non-running branch. **CRITICAL (regime 1 vs regime 2):** the bare
`inst.ActualStatus != "running"` branch at :1587-1593 fires for `created`, `loading` AND
`scheduling`. Anchoring the created-budget on that whole branch would kill a HEALTHY slow
image-pull (regime 2 = `actual_status=loading`, "Pulling" 15min, host 43503) — which LOCKED
CONTEXT (20-CONTEXT.md:15-16,33) says must RIDE to `coldstart_budget_s`, no early kill.
So gate the created-budget on `actual_status ∈ {"created","scheduling"}` ONLY (both are the
pre-loading handshake window per vast/types.go:171), and hand off entirely once actual_status
advances to `loading` or `running`.

Add `var firstCreatedAt time.Time` + `createdBudget := time.Duration(hot.CreatedBudgetS) * time.Second`
near the existing anchors (:1515-1516). Inside the non-running branch (:1587-1593): if
`inst.ActualStatus == "created" || inst.ActualStatus == "scheduling"` → anchor
`firstCreatedAt = time.Now()` on the first such observation, emit a per-poll `log.Warn` with
`elapsed_since_created_s` + `budget_s` (mirror the port-bind Warn at :1606-1613), and if
`time.Since(firstCreatedAt) >= createdBudget` (`>=` for budget=0 determinism) →
`BestEffortDestroy` + `closeLifecycle(..., "created_state_timeout", 0)` + return error.
For ANY OTHER non-running status (i.e. `loading`) RESET `firstCreatedAt = time.Time{}` — a
created→loading transition means the image is pulling (regime 2) and the created-budget must
never fire again for this lifecycle; it rides the `coldstart_budget_s` ceiling. Keep the
existing `firstRunningAt = time.Time{}` reset in this branch (unchanged, for the port-bind
anchor). Also reset `firstCreatedAt = time.Time{}` once the instance reaches `running`.
</action>
<read_first>
- gateway/internal/primary/reconciler.go (the anchors block :1515-1516, the non-running branch :1587-1593, the port-bind Warn+`>=` pattern :1605-1620 to mirror)
- gateway/internal/emerg/vast/types.go (:140 actual_status enum `running|exited|unknown|offline|loading|scheduling`; :171 IsActive treats created/scheduling as the pre-loading handshake — confirm which token the pod reports while "host morto")
- gateway/internal/podconfig/types.go (PodConfig.CreatedBudgetS — added in 20-01)
- 20-CONTEXT.md:15-16,33 (regime 1 `created` = kill at created_budget_s; regime 2 `loading` = ride to coldstart_budget_s)
</read_first>
<acceptance_criteria>
- The FF-01 RED tests pass: stuck-`created` with `CreatedBudgetS=0` → `created_state_timeout` + destroy; created→running before budget → no fire.
- **Regime-2 negative test GREEN:** a lifecycle that goes `created`→`loading` and STAYS `loading` past `created_budget_s` does NOT fire — asserts NO `DestroyInstance` call and NO `created_state_timeout` close (it must ride to the coldstart ceiling / test-end).
- `go test ./internal/primary/ -run CreatedBudget` GREEN.
- No change to the port-bind / terminal / not-found paths (their existing tests stay green).
</acceptance_criteria>
</task>

<task type="tdd">
<name>GREEN FF-02: byte-progress stall detector, download-phase-scoped, telemetry-aware</name>
<action>
In `waitForReadyOrDestroy`, add a DOWNLOAD-phase byte-progress stall detector. State:
`var downloadArmed bool`, `var downloadDone bool`, `var maxBytes int64` (highest total bytes
seen), `var lastProgressAt time.Time` (anchor of the last byte ADVANCE), `var lastLogFetchAt time.Time`
(sub-cadence gate). (No bytes-frozen flag needs to escape the loop — `progress_stall_timeout`
does NOT auto-blocklist, so the close reason string alone is sufficient for the classifier.)

Add a parser `parseDownloadProgress(text string) (fetching bool, okCount int, totalBytes int64)`:
scans the onstart log text for `[download-weights]` lines — `fetching` presence, count of `ok`
lines, and the SUM of the newest `bytes=<N>` per `key=` (3 parallel files → sum their latest
byte counts). Small assert-based self-check on this parser.

Sub-cadence fetch (~30s, gated by `time.Since(lastLogFetchAt) >= logFetchInterval`, a package
var like `primaryInstancePollIntervalForTest` so tests shrink it): call
`res := r.deps.Vast.OnstartLog(ctx, instanceID)`. **Branch on `res.Status`:**
- `NotReady` / `FetchError` / `Empty` → **UNKNOWN**: skip this cadence NON-FATALLY — do NOT
  arm, do NOT advance, do NOT fire. (Codex #3: telemetry-unavailable ≠ pod-stuck; ride to the
  `coldstart_budget_s` ceiling — the safe default.)
- `Available` → parse `res.Text`:
  - **Arm** the download phase the first time a `fetching` line is present: `downloadArmed=true`,
    `lastProgressAt=now`, `maxBytes` = current total. (Codex #2: scope to the download phase —
    arm on `fetching`.)
  - **Disarm** once `okCount >= <expected files>` (3, or the count of configured weights) →
    `downloadDone=true`. After disarm the download-stall detector NEVER fires again — a healthy
    slow docker-compose/model-startup after a good download must not trip it. (Codex #2.)
  - **Progress** while armed & not done: if `totalBytes > maxBytes` → bytes advanced →
    `maxBytes=totalBytes`, `lastProgressAt=now`.
- **Fire** ONLY while `downloadArmed && !downloadDone`, on `Available` telemetry, if
  `time.Since(lastProgressAt) >= progressStallBudget` (`>=` for budget=0) with `totalBytes`
  NOT above `maxBytes` (bytes provably frozen): `BestEffortDestroy`
  + `closeLifecycle(..., "progress_stall_timeout", 0)` + return error.
Skip the whole block when `r.deps.Vast == nil`. `// ponytail:` (1) the 30s sub-cadence throttles
the async Vast logs fetch so it never stretches the 5s poll; (2) telemetry-unavailable rides to
the ceiling — the SSH-tail follow-up (FF-03 leg b) is what would fast-fail regime 3 during a
Vast-logs outage.
</action>
<read_first>
- gateway/internal/primary/reconciler.go (the poll loop body :1518-1678; the `running` branch :1594-1653; primaryInstancePollIntervalForTest package var :117 as the tunable-cadence idiom)
- gateway/internal/emerg/vast/client.go (OnstartLog returns vast.OnstartLogResult{Status,Text} from 20-03 — Available/NotReady/FetchError/Empty)
- pod/scripts/download-weights.sh (the line formats from 20-02 — `fetching …` arms, `progress key=<k> bytes=<N>` advances, `ok …` disarms)
- 20-CONTEXT.md:16-17 (regime 3 = download stall inside onstart; heartbeat = the bytes signal)
</read_first>
<acceptance_criteria>
- FF-02 RED tests pass: Available + frozen `bytes=` + `ProgressStallBudgetS=0` → `progress_stall_timeout` + destroy; Available + rising `bytes=` → no fire; `{FetchError|Empty|NotReady}` forever on a running pod → NO fire / NO destroy (rides); post-`ok` startup lines (download done) → no fire (disarmed); `loading`+`NotReady` (no `fetching`) → not armed.
- `go test ./internal/primary/ -run ProgressStall` GREEN.
- `parseDownloadProgress` has an assert-based self-check (fetching/ok/summed-bytes parse; junk → zero).
- The OnstartLog fetch is throttled — assert via a call-count on `onstartLogFn` that it fires at the sub-cadence, not once per poll tick.
</acceptance_criteria>
</task>

<task type="tdd">
<name>GREEN BL-01 + AL-01: classified outcome hook at the provisionLifecycle return</name>
<action>
**Plumb the close REASON to the hook.** The classifier gates on the close reason, but
`waitForReadyOrDestroy` currently returns only an error and swallows the reason internally.
Change its signature to `(reason string, err error)` — it already computes `reason` at every
`closeLifecycle(...)` call site; return that same string (and `"ready"`/`""` + nil on success).
Update the sole caller (the :1438 return) to `reason, werr := r.waitForReadyOrDestroy(...)` →
`r.recordProvisionOutcome(ctx, offer.MachineID, failStreak, reason, werr, log)` → `return werr`.

Add a **classifier** `machineAttributableReason(reason string) bool` — a small ALLOWLIST of
close reasons that indict the HOST (returns true), everything else false:
`created_state_timeout`, `instance_terminal_state`, `instance_terminal_state_confirmed`,
`public_port_bind_timeout`. EXCLUDED (return false, no blocklist):
- `progress_stall_timeout` — **DELIBERATELY EXCLUDED (reconciling Codex #4 vs #9).** A download
  stall's root cause is the shared weights store (MinIO/R2) + network, NOT necessarily the host.
  If R2 is globally down, EVERY provision stalls → blocklisting on stall would cascade-poison
  the ENTIRE market within a few retry cycles (the exact catastrophe #4 guards against). Codex
  #9 makes this explicit: a broken-R2 run must NOT blocklist. `// ponytail:` promote
  progress_stall_timeout to machine-attributable only with cross-lifecycle correlation (this
  machine stalled while OTHER machines succeeded in the same window ⇒ host-attributable);
  single-signal bytes-frozen cannot distinguish host from global.
- `cancelled_in_flight` (primary recovered — finding #6), `health_timeout` (ambiguous ceiling —
  global R2/telemetry outage or slow host, not attributable), `create_error`,
  `build_create_request_failed:*`, `audit_write_failed`, `vast_status_msg_error:*`
  (image/config/auth-class + free-form), schedule `destroyed` (never reaches this hook anyway).
Assert-based self-check on the classifier (each machine reason → true; each excluded, incl.
`progress_stall_timeout`, → false).

Add `recordProvisionOutcome(ctx, machineID, failStreak int64, reason string, provErr error, log)`:
- `machineID <= 0` → no-op.
- Read the CURRENT lists FRESH via `r.queries().GetPodConfig(ctx)`.
  `// ponytail: fresh read + 2 writes; single primary leader (PRV-03) so no real contention — per-field CAS if that ever changes.`
- On `provErr == nil` (SUCCESS / first_health_pass): append `machineID` to the allowlist
  (dedup; FIFO cap 20 — drop oldest on overflow) AND remove it from the blocklist. Write each
  changed list. (AL-01 is UNGATED by reason — success is always allowlist-worthy.)
- On `provErr != nil` AND `failStreak >= 1` AND `machineAttributableReason(reason)`: append
  `machineID` to the blocklist (dedup) AND remove it from the allowlist. If `failStreak == 0`
  (1st-failure grace) OR the reason is NOT machine-attributable → do NOTHING to either list.
- A DB read/write error → `log.Warn` + return (NEVER block/crash the reconciler on a list
  write — matches the failStreak-count "never block the pod" rule).
- Helpers `appendDedupCap(list, id, cap) []int64` + `removeFromList(list, id) []int64` with
  assert self-checks (dedup, cap-drops-oldest, remove-absent-noop). `// ponytail:` no
  TTL/expiry on the blocklist — manual prune; upgrade path = 24h expiry column if it rots.
</action>
<read_first>
- gateway/internal/primary/reconciler.go (provisionLifecycle :1271-1439, failStreak :1292-1304, offer pick :1387-1402, the :1438 return; waitForReadyOrDestroy :1453-1680 — EVERY closeLifecycle call site whose reason must be returned: cancelled_in_flight :1522, health_timeout :1526, instance_terminal_state_confirmed :1541, vast_status_msg_error :1568, instance_terminal_state :1579, public_port_bind_timeout :1618/1649, + the new created_state_timeout/progress_stall_timeout; r.queries() :1851)
- gateway/db/queries/pod_config.sql (UpdatePodConfigFieldBlocklist :41, UpdatePodConfigFieldAllowlist :44 — []int64; GetPodConfig :1)
- .planning/quick/260702-nse-primary-pod-provision-cheapest-market-na/260702-nse-SUMMARY.md (the failStreak<2/>=2 policy this hook complements)
</read_first>
<acceptance_criteria>
- Classifier tests: each of {created_state_timeout, instance_terminal_state, instance_terminal_state_confirmed, public_port_bind_timeout} + `failStreak>=1` → machine in blocklist + removed from allowlist; each of {progress_stall_timeout, cancelled_in_flight, health_timeout, create_error, build_create_request_failed:*, audit_write_failed, vast_status_msg_error:*} + `failStreak>=1` → ZERO blocklist mutation.
- 1st-failure grace: any machine reason + `failStreak==0` → NOT blocklisted.
- AL-01: success → machine in allowlist (deduped, cap 20 FIFO) + removed from blocklist, regardless of prior reason.
- Pre-pick failures (search_failed / no_offers_below_cap / build_create_request_failed / create_error before :1438) do NOT reach the hook — assert an all-error-search provision writes NEITHER list.
- A list-write error is swallowed (log.Warn) and does NOT change the provision's returned error.
- `machineAttributableReason` + `appendDedupCap` + `removeFromList` self-checks pass.
- `go test ./internal/primary/` fully GREEN (waitForReadyOrDestroy signature change compiles at all call sites/tests).
</acceptance_criteria>
</task>

<task type="auto">
<name>Integration + fmt gate</name>
<action>
Run the FULL primary suite + the integration suite (the STATE.md LESSON: integration tests
+ gofmt do NOT run in the executor's default `go test ./internal/primary/...`). Fix any
fallout. Confirm no existing close-reason/terminal/port-bind test regressed.
</action>
<read_first>
- STATE.md Phase 14 LESSON (run `go test -tags integration ./internal/integration_test/` + `gofmt -l` before pushing a reconciler change)
- gateway/internal/integration_test/primary_helpers_test.go (the fixture that must still build)
</read_first>
<acceptance_criteria>
- `cd gateway && go test ./internal/primary/` GREEN.
- `cd gateway && sudo env PATH=$PATH go test -tags integration ./internal/integration_test/` GREEN (or the repo's canonical integration command).
- `gofmt -l gateway/internal/primary` prints nothing.
- `go vet ./internal/primary/` clean.
</acceptance_criteria>
</task>

<verification>
- `cd gateway && go build ./... && go test ./internal/primary/` green; integration suite green; `gofmt -l gateway/internal/primary` empty.
- A stuck-`created` provision (unit) closes with `created_state_timeout` at ~`created_budget_s`, NOT at `coldstart_budget_s`; a created→loading pod rides (regime 2).
- A frozen-BYTES download (Available telemetry) closes with `progress_stall_timeout` at ~`progress_stall_budget_s`; a rising-bytes download rides; `FetchError`/`Empty`/`NotReady` telemetry rides (never fires); a post-download startup rides (disarmed).
- Failure with `failStreak>=1` AND a machine-attributable reason blocklists the machine; excluded reasons (cancelled_in_flight/health_timeout/create/status_msg) never blocklist; success allowlists; the two lists never hold the same machine.

must_haves:
  - "a pod stuck at actual_status=created is destroyed at ~created_budget_s (FF-01), while a healthy created→loading image-pull (regime 2) rides to the coldstart ceiling — the created-budget arms ONLY on created/scheduling and resets on the loading transition"
  - "the stall signal is progress-BEARING: FF-02 fires only when dest-file BYTES are provably frozen (Available telemetry) within the download phase; a healthy download (rising bytes) and a healthy post-download startup (disarmed) never trip it"
  - "telemetry-unavailable ≠ pod-stuck: FetchError/Empty/NotReady from OnstartLog rides to the coldstart ceiling (safe default), never a false kill — the SSH-tail follow-up would close that gap"
  - "auto-blocklist is CLASSIFIED: only machine-attributable close reasons park a host on the 2nd consecutive failure; cancelled_in_flight / health_timeout / create/config/status_msg errors never poison the picker; success always allowlists (dedup, cap-20 FIFO); a machine is never in both lists"
  - "the failStreak>=1 ban predicate is the correct off-by-one (failStreak is pre-attempt; +1 makes it the 2nd consecutive)"
  - "a list-write failure never blocks or crashes a provision"
</verification>

<threat_model>
Attack surface: this plan decides when to DESTROY a paid Vast pod and which real Vast
machine_ids get auto-parked/preferred — a wrong predicate wastes money or bricks provisioning.
- **Billing-burn (false-negative fast-fail):** a bug that fails to anchor `firstCreatedAt`
  or mis-parses the heartbeat would let a dead pod burn to `coldstart_budget_s` (up to 90 min
  × ~$0.30-0.77/h). Mitigation: the `coldstart_budget_s` ceiling is untouched (still the
  absolute backstop); FF-01/FF-02 only ADD earlier exits; budget=0 unit tests prove the exit
  fires.
- **Over-aggressive kill (false-positive fast-fail) — regime-2 + telemetry hazards:** killing
  a legitimately slow-but-live cold start wastes the re-provision AND poisons the blocklist.
  Mitigations: FF-01 arms ONLY on `created`/`scheduling` and RESETS on the `loading` transition,
  so a healthy 15-min image pull (regime 2) is never touched (regression-tested by the
  created→loading negative test). FF-02 fires ONLY on `Status=Available` telemetry with the
  dest-file BYTE SUM provably frozen, within the `fetching`→all-`ok` download phase; a
  rising-bytes download resets the anchor, a completed download disarms, and — critically —
  `FetchError`/`Empty`/`NotReady` telemetry (Vast logs API down/delayed) is treated as UNKNOWN
  and RIDES to the ceiling rather than false-killing (the earlier "empty-telemetry backstop"
  was REMOVED per Codex #3 precisely because it false-killed on a logs-API outage). Residual
  cost: regime-3 during a full Vast-logs outage fails slow (rides to ceiling) instead of fast —
  the deferred SSH-tail leg closes it.
- **Blocklist poisoning / allowlist starvation:** auto-blocklisting on the WRONG failure class
  parks good hosts and shrinks the market. Mitigations: (1) the reason CLASSIFIER — only
  machine-attributable close reasons (created_state_timeout, terminal/offline, port-bind)
  ever blocklist; global/our-side/ambiguous reasons — including progress_stall_timeout
  (DELIBERATELY EXCLUDED: a global R2/network stall hits ALL hosts → blocklisting on stall
  cascade-poisons the market; FF-02 still kills the pod, it just doesn't park the host),
  cancelled_in_flight, health_timeout, create/config/status_msg/R2-auth — NEVER do, so a global
  R2 outage or an in-flight cancel can't poison the picker (Codex #4/#6/#9). (2) `failStreak>=1`
  grace (never ban on the 1st failure). (3) a success removes the machine from the blocklist so
  a degraded-then-recovered host self-heals. (4) allowlist cap-20 FIFO bounds growth. No TTL on
  the blocklist yet (manual prune, ponytail-marked) — a permanently flaky-then-fixed host stays
  parked until pruned; accepted, upgrade path noted.
- **Lost-update race on the two lists:** the hook reads fresh + writes; the primary is
  leader-gated (single reconciler, PRV-03) so concurrent writes are not expected. ponytail
  comment names the ceiling.
- **No secrets in this plan** (machine_ids are non-sensitive integers; the Vast key stays in
  the 20-03 client).
</threat_model>
