---
wave: 1
depends_on: []
autonomous: true
requirements: [OBS-11]
files_modified:
  - pod/scripts/download-weights.sh
---

# Plan 20-02 — OBS-11: download-weights.sh mid-file heartbeat (enables FF-02)

> **Requirement ID note:** OBS-01 is already a COMPLETE v1 requirement (Phase 7,
> REQUIREMENTS.md:79). This phase's download-heartbeat requirement is **OBS-11**
> (OBS-09/10 are taken by Phase 15). ROADMAP.md already reconciled.

Today `download_and_verify()` runs `mc cp --quiet` (line 55). With `--quiet`, a single
16 GB Qwen file emits exactly TWO log lines — `fetching …` (start) and `ok …` (end) — with
NOTHING in between. During that window the onstart log (`/var/log/onstart.log`, tee'd to the
Vast console) is silent, so a download STALL mid-file is invisible. FF-02 (plan 20-04) keys
its regime-3 stall detector on the newest ISO-8601-timestamped `[download-weights]`
heartbeat line advancing; without a mid-file heartbeat it can never fire during a large
file. This plan makes the download emit a periodic progress heartbeat.

**Laziest fix that works:** drop `--quiet` so `mc cp` prints its own periodic transfer
progress line. If `mc`'s default progress is not reliably line-buffered to the tee'd log
(it uses a carriage-return-updated TTY progress bar that may not flush to a non-TTY pipe),
fall back to a lightweight background heartbeat loop that logs the destination file's
growing size every ~15s while the `mc cp` runs, killed when the copy returns. Prefer the
one-line `--quiet`-drop; only add the heartbeat loop if the dropped-quiet output does not
produce timestamped lines to the file in a non-TTY context (the pod runs `mc` under
`exec > >(tee -a …)`, i.e. a pipe, not a TTY — so the progress bar likely won't flush;
plan for the heartbeat loop). ponytail: heartbeat loop is ~6 lines of bash; no new tool.

## Tasks

<task type="auto">
<name>Emit a periodic download heartbeat per file</name>
<action>
In `pod/scripts/download-weights.sh` `download_and_verify()` (lines 51-69): remove `--quiet`
from the `mc cp` invocation. Because the pod pipes stdout through `tee` (not a TTY), `mc`'s
CR-updated progress bar will not line-flush — so ALSO wrap the copy with a background
heartbeat: before the `mc cp`, start a subshell loop that every 15s logs
`log "downloading ${key}: $(stat -c%s "${dest}" 2>/dev/null || echo 0) bytes"` (via the
existing `log()` helper so it carries the `[download-weights]` prefix + ISO-8601 timestamp
FF-02 parses); capture its PID; run `mc cp` (without `--quiet`); on return (success OR
failure) `kill` the heartbeat PID (guard with `2>/dev/null`) so it never leaks past the copy.
Keep the existing sha256 verify + return codes unchanged. The heartbeat must be per-download
so all 3 parallel downloads each emit their own advancing line. Add a `# ponytail:` comment
noting the heartbeat exists solely to feed FF-02 stall detection and the upgrade path is
`mc --json` progress parsing if byte-size polling proves too coarse.
</action>
<read_first>
- pod/scripts/download-weights.sh (log() helper :32; download_and_verify :51-69; the `mc cp --quiet` line :55; parallel launch :78-83 — confirm heartbeat is per-file so 3 concurrent downloads each advance)
- pod/onstart.sh (lines 20-22 confirm `exec > >(tee -a /var/log/onstart.log) 2>&1` — the log IS a pipe, not a TTY; this is why the mc progress bar won't flush)
</read_first>
<acceptance_criteria>
- `grep -c -- "--quiet" pod/scripts/download-weights.sh` returns 0 (the flag is gone).
- A heartbeat loop logs a `[download-weights]` line via `log()` at a fixed interval (~15s) while each `mc cp` runs, and is killed when the copy returns (no orphaned background loop after the function exits).
- `bash -n pod/scripts/download-weights.sh` passes (syntax).
- A local dry-run self-check (mock `mc` as a `sleep 40; touch dest` shim in a scratch dir) shows ≥2 timestamped `[download-weights] downloading …` heartbeat lines emitted between `fetching` and the copy return — proving the mid-file heartbeat exists. Provide this as a runnable one-off check in the task notes; it must assert ≥1 heartbeat line appears mid-copy.
- Exit codes 0/2/3/4/5 semantics unchanged (heartbeat never alters the copy's return).
</acceptance_criteria>
</task>

<verification>
- `bash -n pod/scripts/download-weights.sh` clean; `shellcheck` (if available) surfaces no new errors on the changed function.
- Mock-`mc` scratch run: heartbeat lines appear mid-copy AND no background process survives the function return (`jobs` empty / the trap-or-kill fired).

must_haves:
  - "a large weight download emits at least one timestamped [download-weights] heartbeat line mid-file, so FF-02 can detect a mid-file stall"
  - "the heartbeat is killed when each copy returns — no leaked background loops, no change to the copy's exit code or the sha256 verify"
</verification>

<threat_model>
Attack surface: this script runs INSIDE the paid pod as root during onstart, before the
health-bridge exists. It handles MinIO creds (`MINIO_ACCESS_KEY`/`MINIO_SECRET_KEY`) already.
- **Cred leak via verbose output:** dropping `--quiet` makes `mc cp` more verbose. `mc cp`
  prints the object path + progress, NOT the alias creds (the creds live in the
  `mc alias set` line at :43, unchanged). Verify the removed-quiet output does not echo the
  endpoint credential — it prints `ifix/<bucket>/<key>` only. No new cred surface.
- **Heartbeat DoS on the log:** a 15s interval over a worst-case 60-min download is ~240
  lines/file — trivial. Do NOT log every byte or poll sub-second (would flood the tee'd
  console and the Vast logs API FF-03 fetches). Interval fixed at ~15s.
- **Orphaned background process:** a heartbeat loop that outlives the copy would keep logging
  forever and hold a PID. Mitigated by killing the captured PID on every return path
  (success and failure) — asserted by the scratch self-check.
- **False stall signal:** byte-size polling on a sparse/preallocated file could show
  no growth even while healthy. Acceptable — FF-02 treats "no heartbeat advance" as the
  stall signal and the byte-size line advances for a streaming `mc cp`; the ponytail comment
  names `mc --json` as the upgrade if this proves too coarse in UAT.
</threat_model>
