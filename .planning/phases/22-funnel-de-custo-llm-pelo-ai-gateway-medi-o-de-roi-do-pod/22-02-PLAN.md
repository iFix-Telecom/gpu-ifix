---
phase: 22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - "/home/pedro/projetos/pedro/converseai-v4/agents/src/config.py (se re-land necessário)"
  - "/home/pedro/projetos/pedro/converseai-v4/agents/src/llm/provider.py (se re-land necessário)"
  - "converseai-v4 origin/develop (cherry-pick commits 113 dangling, se necessário)"
  - "Portainer stack 15 converseai-v4-dev — redeploy (se re-land necessário)"
autonomous: false
requirements: [CV-01]
user_setup: []

must_haves:
  truths:
    - "Sabe-se com FATO se a imagem em execução no stack 15 (converseai-v4-dev, endpoint 3) contém o código de roteamento Phase 113 (config.Settings tem stt_gateway_key/agent_*_gateway_key)"
    - "Se ausente: o código 113 foi re-landado em origin/develop, a imagem :develop rebuildada pelo CI e o stack 15 redeployado com a imagem nova"
    - "Após este plano, setar STT_GATEWAY_KEY no stack 15 NÃO é mais no-op (o consumidor existe no runtime)"
  artifacts:
    - path: ".planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-02-GATE.md"
      provides: "resultado do gate (código 113 presente/ausente na imagem rodando) + evidência do comando"
      min_lines: 10
  key_links:
    - from: "imagem stack 15 (:develop, buildada de origin/develop)"
      to: "config.Settings.stt_gateway_key / agent_classifier_gateway_key"
      via: "python -c 'from src.config import get_settings; hasattr(...)' no container em execução"
      pattern: "stt_gateway_key"
---

<objective>
GATE de runtime do CV-01. O CONTEXT assume "código 113 mergeado+inerte, só ativar env" — a pesquisa PROVOU que isso é FALSO: os commits 113 (386494ba, 6a00de74, b205bd6d, 0d038b3b, 680accfa) estão DANGLING (`git branch --contains`=vazio), não em `origin/develop`. Setar `STT_GATEWAY_KEY` no stack 15 seria no-op silencioso se a imagem rodando não tiver o código. Este plano PRIMEIRO verifica o runtime e, se o código estiver ausente, re-landa o código (sob confirmação humana) antes de qualquer ativação de env.

Purpose: impedir o pior modo de falha do CONTEXT — "setei a key e não aconteceu nada". Pré-requisito bloqueante de CV-01 STT (22-03) e secundárias (22-06).
Output: gate documentado; se necessário, código 113 de volta em develop + stack 15 redeployado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-CONTEXT.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-RESEARCH.md

<interfaces>
<!-- Fatos da pesquisa (22-RESEARCH.md §CV-01 + Open Question 1) -->
converseai-v4 repo: /home/pedro/projetos/pedro/converseai-v4 (HEAD == origin/develop == c6fd7b6c à época da pesquisa).
Commits 113 DANGLING (fora de develop): 386494ba, 6a00de74, b205bd6d, 0d038b3b, 680accfa (+config).
Env keys 113 (pydantic aliases, default ""): stt_gateway_key, agent_classifier_gateway_key,
  agent_format_hint_gateway_key, agent_ai_match_gateway_key, agent_gateway_base_url
  (default https://ai-gateway.converse-ai.app/v1), agent_gateway_timeout_seconds (10.0).
Deploy: Portainer stack 15 converseai-v4-dev, endpoint 3 (socket vps-ifix-vm .30).
  Fluxo CI: push develop → GHA runner builda ghcr.io/ifixtelecom/converseai-*:develop → webhook → Portainer pull+recreate.
Portainer API: portainer3.ifixtelecom.com.br, X-API-Key <ver `/home/pedro/.claude/CLAUDE.md` seção Portainer; NÃO inline, plano é commitado>
GATE (Open Question 1): ssh vps-ifix-vm 'docker inspect <converseai-api|agents container>' + no container
  python -c "from src.config import get_settings; s=get_settings(); print(hasattr(s,'stt_gateway_key'))"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate de runtime — a imagem do stack 15 contém o código 113?</name>
  <read_first>
    - 22-RESEARCH.md §CV-01 "🔴 BLOQUEIO CRÍTICO" + Open Question 1
    - saída de `ssh vps-ifix-vm 'docker ps --format "{{.Names}}\t{{.Image}}"'` (identificar o container de agents/api da converseai)
  </read_first>
  <files>.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-02-GATE.md</files>
  <action>
    Descobrir o container Python (agents) do stack 15 na vps-ifix-vm e sua imagem/digest (`docker ps` + `docker inspect --format '{{.Config.Image}}'`). Registrar o digest da imagem em execução em 22-02-GATE.md — é a âncora de rollback (redeploy dessa imagem restaura o estado pré-fase). Prova em DOIS níveis (concern codex/gemini HIGH — `hasattr` prova config, NÃO prova que os paths STT/LLM consomem a config em runtime): (1) **config existe**: `docker exec <container> python -c "from src.config import get_settings; s=get_settings(); print(hasattr(s,'stt_gateway_key'), hasattr(s,'agent_classifier_gateway_key'))"` (ajustar import path ao layout real do agents); (2) **consumidor existe** (prova FUNCIONAL): grep nos módulos que DEVEM ler a key e montar o client do gateway — `docker exec <container> grep -rn "stt_gateway_key\|agent_.*_gateway_key\|ifix-gateway\|agent_gateway_base_url" /app/src 2>/dev/null` DEVE mostrar o branch que constrói o client apontando pro gateway (não só a definição no config). Se só o config aparece e nenhum consumidor → tratar como AUSENTE (código inerte não landado). Registrar em 22-02-GATE.md: nome/imagem/digest do container, saída literal dos dois níveis, e VEREDITO: PRESENTE (config + consumidor no runtime → pular Task 2/3, seguir direto pro 22-03) ou AUSENTE (re-land obrigatório).
  </action>
  <acceptance_criteria>
    22-02-GATE.md registra o container+imagem+digest (âncora de rollback) e a saída literal dos DOIS níveis (hasattr do config E grep do consumidor que monta o client do gateway), com veredito explícito PRESENTE (config+consumidor) ou AUSENTE.
  </acceptance_criteria>
  <done>Gate resolvido com evidência de comando (config + consumidor runtime); digest anterior registrado; caminho (pular vs re-land) decidido por FATO.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: aprovar re-land do código Phase 113 (se gate AUSENTE)</name>
  <action>Pausar para o Pedro decidir re-land vs pular, conforme o veredito do 22-02-GATE.md. Ver <decision>/<options> abaixo. Re-land NÃO é autônomo.</action>
  <decision>Se o gate deu AUSENTE: aprovar re-land do código Phase 113 em origin/develop (cherry-pick dos commits dangling) antes de qualquer ativação de env.</decision>
  <context>Re-landar código toca o repo de PRODUÇÃO dev (converseai-v4) + dispara CI + redeploy do stack 15 (api/agents/worker/web reais). O downstream_consumer proíbe re-land autônomo — exige confirmação humana. Se o gate deu PRESENTE, este checkpoint é dispensado (responder "gate presente, pular").</context>
  <options>
    <option id="reland">
      <name>Re-landar 113 via PR/branch temporária (cherry-pick 6a00de74 b205bd6d 0d038b3b 680accfa + config → branch → PR → merge em develop)</name>
      <pros>Desbloqueia CV-01; código já revisado (foi mergeado e caiu por rebase/force); PR dá diff revisável + merge commit atômico revertível (rollback de código)</pros>
      <cons>Pode ter conflito com c6fd7b6c; toca prod dev; requer CI verde antes de redeploy</cons>
    </option>
    <option id="skip">
      <name>Gate deu PRESENTE — pular re-land, seguir pro 22-03</name>
      <pros>Sem mudança de código; ativação é só env</pros>
      <cons>Só válido se Task 1 provou o código no runtime</cons>
    </option>
  </options>
  <resume-signal>Responder "re-landar 113" OU "gate presente, pular"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Re-land do código 113 + CI + redeploy stack 15 (só se AUSENTE e aprovado)</name>
  <read_first>
    - 22-02-GATE.md (veredito)
    - /home/pedro/projetos/pedro/converseai-v4 (branch develop; `git log --oneline --all | grep -i 113` p/ localizar os dangling)
    - 22-RESEARCH.md §CV-01 (commits a cherry-pickar)
  </read_first>
  <files>/home/pedro/projetos/pedro/converseai-v4 (develop) + agents/src/config.py + agents/src/llm/provider.py + Portainer stack 15 redeploy</files>
  <action>
    SOMENTE se Task 1 = AUSENTE e checkpoint aprovou. No repo converseai-v4, criar branch temporária (ex.: `phase22-reland-113`) a partir de develop e cherry-pickar os commits 113 dangling na ordem (6a00de74, b205bd6d, 0d038b3b, 680accfa e o commit de config 386494ba se separado), resolvendo conflitos contra c6fd7b6c. Abrir **PR → develop** (concern codex HIGH — preferir PR revisável a push direto; o merge commit é a unidade de rollback de código). NÃO alterar comportamento: todas as `*_gateway_key` default "" mantêm o fornecedor direto (roteamento inerte até env setada — isso é intencional, é o opt-in do rollout gradual). Antes do merge, rodar os testes ESPECÍFICOS dos fluxos 113 (não só `pytest` genérico) — os testes que cobrem o branch gateway do STT (commit 680accfa) e do LLM (b205bd6d); rodar via `pytest -k "gateway or stt or provider"` (ajustar aos nomes reais dos testes 113). Merge → aguardar CI GHA buildar `:develop` verde → confirmar webhook Portainer OU forçar pull+recreate do stack 15 (endpoint 3). Re-rodar o check DE DOIS NÍVEIS do Task 1 no container novo (config `hasattr(s,'stt_gateway_key')==True` E grep do consumidor presente). **Rollback de código explícito**, se CI passar mas runtime falhar: `git revert` do merge commit da PR (ou reset da branch) → push → CI rebuilda; E redeploy da imagem no digest anterior (registrado no 22-02-GATE.md) para restauração imediata enquanto o rebuild roda.
  </action>
  <acceptance_criteria>
    PR mergeada em develop; `git log origin/develop --oneline` inclui os commits 113 (não mais dangling); testes 113-específicos verdes (não só pytest genérico); CI build `:develop` verde; re-check DE DOIS NÍVEIS no container do stack 15 → `hasattr(s,'stt_gateway_key')==True` E grep do consumidor do gateway presente; procedimento de rollback de código (revert do merge + redeploy do digest anterior) documentado no SUMMARY. (Se Task 1 = PRESENTE: esta task é no-op documentado.)
  </acceptance_criteria>
  <done>Código 113 no runtime do stack 15 (provado por hasattr==True), comportamento inerte (keys vazias = fornecedor direto).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → converseai-v4 origin/develop | push de código a prod dev; cherry-pick pode introduzir regressão |
| CI → Portainer stack 15 | imagem nova recria api/agents/worker/web reais |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering | cherry-pick com conflito mal-resolvido | mitigate | pytest local + CI verde ANTES de redeploy; keys default "" mantêm comportamento direto (inerte) |
| T-22-05 | Denial of Service | redeploy stack 15 derruba converseai-dev | mitigate | redeploy fora de pico; rollback = re-deploy da imagem :develop anterior (digest registrado no 22-02-GATE.md) |
| T-22-06-SC | Tampering | cherry-pick de commit dangling não-auditado | mitigate | commits já foram mergeados e revisados uma vez (Phase 113); diff conferido no cherry-pick; checkpoint humano bloqueante |
</threat_model>

<verification>
- 22-02-GATE.md com veredito + evidência
- Se re-land: `git log origin/develop` contém 113 + CI verde + hasattr==True no container
</verification>

<success_criteria>
FATO estabelecido sobre a presença do código 113 no runtime. Se ausente, código re-landado e provado no container, sem mudar o comportamento inerte. CV-01 STT (22-03) desbloqueado.
</success_criteria>

<output>
Create `.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-02-SUMMARY.md` when done
</output>
