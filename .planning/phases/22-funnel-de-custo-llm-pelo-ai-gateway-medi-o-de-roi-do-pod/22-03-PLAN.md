---
phase: 22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - "Portainer stack 15 converseai-v4-dev — env STT_GATEWAY_KEY + redeploy (sem arquivo no repo)"
autonomous: false
requirements: [CV-01]
user_setup: []

must_haves:
  truths:
    - "STT_GATEWAY_KEY (tenant converseai-stt — valor em CLAUDE.md) está setada no stack 15 e a converseia roteia STT pelo gateway (não mais OpenAI/Gemini direto)"
    - "tenant converseai-stt sai de 0 rows para N rows em gatewayctl usage report"
    - "billing_events da tenant converseai-stt (tenant_id 11bfafdf-…) mostra upstream gemini-stt|openai-whisper (não unknown) com cost_external_brl coerente (preços do 22-01)"
    - "existe caminho de rollback provado: esvaziar STT_GATEWAY_KEY + redeploy volta ao STT direto"
  artifacts:
    - path: ".planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-03-STT-UAT.md"
      provides: "evidência antes/depois (usage 0→N, billing upstream, log gateway_enabled=True) + passo de rollback"
      min_lines: 15
  key_links:
    - from: "STT_GATEWAY_KEY no stack 15"
      to: "billing_events tenant_id=11bfafdf-… upstream=gemini-stt|openai-whisper"
      via: "agents roteia /v1/audio/transcriptions pro gateway com Bearer da tenant converseai-stt"
      pattern: "converseai-stt"
---

<objective>
Primeiro passo do funnel real (CV-01): ativar o roteamento de STT da converseia pelo gateway setando UMA env key no stack 15. STT é o alvo de baixo risco que valida o funnel end-to-end e ataca os ~$38 OpenAI. Rollout gradual LOCKED: só esta key, com UAT + rollback documentado, antes de tocar classifier/format-hint/ai-match (22-06).

Purpose: provar o funnel com o menor blast-radius; capturar STT em billing_events com preço correto (22-01).
Output: STT roteado pelo gateway; UAT antes/depois; rollback provado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-CONTEXT.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-RESEARCH.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-02-GATE.md

<interfaces>
<!-- Fatos: 22-RESEARCH.md §CV-01 "Passo-a-passo" + Validation Architecture -->
Env a setar no stack 15: STT_GATEWAY_KEY = <key da tenant converseai-stt — ver `/home/pedro/.claude/CLAUDE.md` seção API Keys, bloco Phase 113; NÃO inline aqui, plano é commitado>
  (tenant converseai-stt, tenant_id 11bfafdf-00a7-4b8e-aade-9d0946839e41).
Portainer stack 15 (converseai-v4-dev, endpoint 3); env na UI/API + redeploy.
  API: portainer3.ifixtelecom.com.br, X-API-Key <ver CLAUDE.md seção Portainer>
Fallback anti-DoS (concern codex HIGH — PROVAR no código, não assumir): CLAUDE.md documenta "gateway como primary do chain via `.with_fallbacks()` nativo, fallback fornecedor direto, timeout curto AGENT_GATEWAY_TIMEOUT_SECONDS=10s". CONFIRMAR isso no código 113 re-landado (grep `.with_fallbacks`/fallback no path STT) — se o fallback NÃO existir, a mitigação T-22-07 é falsa e o UAT precisa tratar falha do gateway como risco real.
Prova de ativação:
  Antes: docker exec $GW /gatewayctl usage report --tenant converseai-stt --from <hoje> --to <hoje> = 0 rows
  Depois: mesma query > 0 rows; psql SELECT DISTINCT upstream FROM billing_events WHERE tenant_id='11bfafdf-…' → gemini-stt|openai-whisper (não unknown)
  Log agents: campo REAL a confirmar no código (RESEARCH aponta `provider="ifix-gateway"` no branch gateway do 113; CONTEXT/CLAUDE.md chamam de gateway_enabled=True — usar o literal que o código realmente emite, checar no grep do 22-02).
GW=$(docker ps -q -f name=ai-gateway-prod_gateway|head -1) no worker-vm.
Rollback: esvaziar STT_GATEWAY_KEY (="") no stack 15 + redeploy → STT volta ao fornecedor direto (default "" = comportamento antigo).
Não regredir Phase 21: local-stt tier0 + cascade tier1 (gemini-stt/openai-whisper) intactos.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline antes da ativação</name>
  <read_first>
    - 22-02-GATE.md (confirmar código 113 PRESENTE no runtime — pré-condição dura)
    - 22-RESEARCH.md §CV-01 Validation Architecture (linha CV-01)
  </read_first>
  <files>.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-03-STT-UAT.md</files>
  <action>
    Registrar o estado ANTES em 22-03-STT-UAT.md: `docker exec $GW /gatewayctl usage report --tenant converseai-stt --from <hoje> --to <hoje>` (esperado 0 rows) e o env atual do stack 15 (confirmar STT_GATEWAY_KEY vazia/ausente). Confirmar via 22-02-GATE.md que o gate deu PRESENTE (senão ABORTAR — este plano depende do runtime 113). **Confirmar por FATO (concern codex HIGH) que o path STT do 113 tem fallback pro fornecedor direto**: grep no código re-landado por `.with_fallbacks`/fallback no fluxo de transcrição; registrar o trecho. Se o fallback NÃO existir, marcar no doc que a mitigação anti-DoS T-22-07 é FALSA e o UAT deve tratar falha do gateway como quebra real de STT (não degradação graciosa). Anotar o digest da imagem em execução (para rollback).
  </action>
  <acceptance_criteria>
    22-03-STT-UAT.md tem o baseline: usage converseai-stt = 0 rows + STT_GATEWAY_KEY não-setada + digest da imagem; gate 113 confirmado PRESENTE.
  </acceptance_criteria>
  <done>Baseline capturado; pré-condição do gate confirmada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: ativar STT pelo gateway + UAT</name>
  <action>Setar STT_GATEWAY_KEY no stack 15, redeploy e verificar o funnel STT conforme <how-to-verify>. Prod dev — verificação humana antes de propagar.</action>
  <what-built>Setar STT_GATEWAY_KEY (key da tenant converseai-stt — ver CLAUDE.md) no env do stack 15 (Portainer converseai-v4-dev) e redeploy, ativando o roteamento de STT pelo gateway. É prod dev com clientes reais — o Pedro verifica antes de propagar.</what-built>
  <how-to-verify>
    1. Após o redeploy, disparar um **teste ATIVO controlado (concern codex MEDIUM — não depender só de tráfego natural)**: postar 1 áudio curto conhecido em `/v1/audio/transcriptions` model=whisper via a tenant converseai-stt, OU gerar 1 transcrição real de teste na converseia. Critério mínimo de UAT: HTTP 200, transcrição NÃO-vazia e correta pro áudio, latência dentro do budget (< AGENT_GATEWAY_TIMEOUT_SECONDS=10s), e cost_external_brl coerente com o preço do 22-01. Complementar com tráfego natural para volume.
    2. `ssh worker-vm 'docker exec $(docker ps -q -f name=ai-gateway-prod_gateway|head -1) /gatewayctl usage report --tenant converseai-stt --from <hoje> --to <hoje>'` → deve mostrar > 0 rows.
    3. `psql "$AI_GATEWAY_PG_DSN" -c "SELECT DISTINCT upstream, count(*), round(sum(cost_external_brl),4) FROM ai_gateway.billing_events WHERE tenant_id='11bfafdf-00a7-4b8e-aade-9d0946839e41' AND ts>=date_trunc('day',now()) GROUP BY 1"` → upstream gemini-stt|openai-whisper (NÃO unknown), custo coerente com os preços do 22-01.
    4. Log do agents mostra o campo real de roteamento (provider="ifix-gateway" ou gateway_enabled=True — o literal confirmado no grep do 22-02) nas transcrições.
    5. UAT qualitativo: as transcrições continuam corretas (comparar 1-2 saídas antes/depois).
    6. Confirmar Phase 21 intacta: local-stt tier0 + cascade tier1 sem mudança (gatewayctl upstreams list).
  </how-to-verify>
  <resume-signal>Responder "STT aprovado" (funnel provado) OU "rollback" (esvaziar key + redeploy)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Documentar UAT + rollback no 22-03-STT-UAT.md</name>
  <read_first>
    - 22-03-STT-UAT.md (baseline)
  </read_first>
  <files>.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-03-STT-UAT.md</files>
  <action>
    Registrar o DEPOIS: saída do usage report (0→N), o SELECT de billing por upstream, o log provider="ifix-gateway", e o resultado do UAT qualitativo. Documentar explicitamente o procedimento de ROLLBACK provado: setar STT_GATEWAY_KEY="" no stack 15 + redeploy → STT volta ao fornecedor direto (confirmar que o default vazio restaura comportamento). Se o checkpoint resultou em "rollback", registrar o motivo e o estado restaurado.
  </action>
  <acceptance_criteria>
    22-03-STT-UAT.md contém antes/depois (usage 0→N, billing upstream≠unknown, provider="ifix-gateway") e o passo de rollback com a env exata a esvaziar.
  </acceptance_criteria>
  <done>UAT e rollback documentados com evidência; funnel STT provado end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → Portainer stack 15 env | key de tenant em env de prod dev; redeploy afeta converseia real |
| converseia agents → gateway | STT de call/WhatsApp (áudio de cliente) passa a rotear pelo gateway |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Denial of Service | STT quebra pós-repoint (gateway 5xx) | mitigate | mitigação DEPENDE de PROVAR o fallback fornecedor direto no código 113 (Task 1 grep `.with_fallbacks`) + timeout curto (agent_gateway_timeout_seconds=10s); se o fallback NÃO existir a mitigação é falsa → rollback (esvaziar key + redeploy) vira a única defesa; rollback sempre disponível (Task 2) |
| T-22-08 | Information Disclosure | áudio de cliente rota nova | accept | tenant converseai-stt é normal por design (áudio de call já ia p/ provider externo — CLAUDE.md); gateway não muda a classe de dado |
| T-22-09 | Tampering | regressão Phase 21 STT local | mitigate | não tocar upstreams/aliases STT; conferir local-stt tier0 + cascade intactos (Task checkpoint passo 6) |
</threat_model>

<verification>
- gatewayctl usage report --tenant converseai-stt: 0 → N rows
- billing_events tenant 11bfafdf-…: upstream gemini-stt|openai-whisper, custo coerente
- log agents provider="ifix-gateway"
- rollback documentado e reversível
</verification>

<success_criteria>
STT da converseia roteado pelo gateway, capturado em billing_events com preço correto, transcrições intactas, Phase 21 sem regressão, rollback provado. Funnel end-to-end validado no menor blast-radius.
</success_criteria>

<output>
Create `.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-03-SUMMARY.md` when done
</output>
