---
phase: 22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - "prod DB ai_gateway.model_aliases (via gatewayctl model-alias set) — e/ou ai_gateway.upstreams (migration, se caminho A)"
autonomous: false
requirements: [CV-02]
user_setup: []

must_haves:
  truths:
    - "O gateway consegue servir chat Gemini (google/gemini-2.5-flash-lite) via um alias/upstream — pré-requisito para classifier/format-hint (22-06) roteárem o R$238 pelo gateway sem quebrar"
    - "O caminho escolhido (alias sobre openrouter-chat vs upstream nativo gemini-chat) foi decidido por PROVA empírica do path OpenAI-compat do Gemini, não por suposição"
    - "tool_choice/structured-output passa pelo gateway (classifier/format-hint usam with_structured_output) — testado com o MODEL LITERAL real (google/gemini-2.5-flash-lite) antes de prod, não alias sintético"
    - "ESCOPO declarado: CV-02 cobre só Gemini chat OpenAI-compat; media pipeline Gemini (visão/generate_content nativo) fica FORA do gateway e é reportado como gasto off-gateway no 22-07 (não vendido como parte do 100%)"
  artifacts:
    - path: ".planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-04-GEMINI-CHAT.md"
      provides: "prova do path Gemini OpenAI-compat, decisão A/B, e o teste tools+tool_choice via gateway"
      min_lines: 15
  key_links:
    - from: "alias google/gemini-2.5-flash-lite (nome LITERAL enviado pelo app; ou upstream gemini-chat)"
      to: "billing_events upstream com custo (preço de gemini-2.5-flash-lite do 22-01)"
      via: "gatewayctl model-alias set -alias -upstream -target; resolver 60s poll"
      pattern: "google/gemini-2.5-flash-lite"
---

<objective>
CV-02: dar ao gateway a capacidade de servir chat Gemini, PRÉ-REQUISITO para funnelar classifier/format-hint (o grosso do R$238) em 22-06. O CONTEXT diz "criar upstream de fallback gemini de CHAT"; a pesquisa PROVOU que (a) não existe director gemini-chat (só gemini-STT), (b) o caminho nativo (caminho A) tem alto risco de quebra por path.Join (`/v1beta/openai/v1/chat/completions`), e (c) o caminho PROVADO (spec 113) é o alias `gemini-flash-lite → openrouter-chat → google/gemini-2.5-flash-lite`. Este plano decide por evidência e aplica.

Purpose: habilitar o funnel do maior gasto (Gemini) sem quebrar comportamento; garantir tool-calling/structured-output pelo gateway.
Output: alias/upstream gemini-chat funcional; decisão A/B com prova; validação de tools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-CONTEXT.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-RESEARCH.md

<interfaces>
<!-- Fatos: 22-RESEARCH.md §CV-02 (dois caminhos) + Pitfalls 3,4,8 -->
Upstreams: tabela ai_gateway.upstreams; gatewayctl SÓ list|update|enable|disable (NÃO create — novo upstream = migration).
  url resolvida em runtime: os.Getenv(url_env); env ausente = row SKIPPED (graceful).
Director de chat = genérico OpenAI-compat (BuildDirector path.Join(upstream.Path, inboundPath)). SEM director gemini-chat.
Alias: gatewayctl model-alias set -alias <a> -upstream <u> -target <t>; resolver pega por POLL 60s (sem pg_notify p/ alias).
  Ex atual: qwen → local-llm(tier0) + openrouter-chat(tier1).
CAMINHO A (nativo, redação literal CONTEXT — ALTO RISCO):
  migration INSERT upstreams(name='gemini-chat',role='llm',tier=1,url_env='UPSTREAM_LLM_FALLBACK_1_URL',
  auth_bearer_env='UPSTREAM_LLM_FALLBACK_1_AUTH_BEARER') + env no stack. RISCO: endpoint OpenAI-compat do Gemini
  é /v1beta/openai/chat/completions; director genérico produz /v1beta/openai/v1/chat/completions → 404. Precisaria director dedicado.
CAMINHO B (PROVADO, o que o spec 113 fez p/ secundários):
  gatewayctl model-alias set -alias google/gemini-2.5-flash-lite -upstream openrouter-chat -target google/gemini-2.5-flash-lite
  ⚠ CRÍTICO (concern gemini+codex HIGH — falha silenciosa): o alias DEVE ter o nome do LITERAL que o app envia como `model`.
  O código da converseia envia `google/gemini-2.5-flash-lite` (classifier/format-hint default). Um alias chamado só `gemini-flash-lite`
  NÃO casaria o request → falha silenciosa. Criar o alias com nome = literal enviado (+ opcionalmente o curto `gemini-flash-lite` como conveniência).
  Sem novo upstream/director. OpenRouter serve o Gemini (markup); provider vira openrouter-fireworks (casar preço 22-01).
Pitfall 4: testar tools+tool_choice pelo gateway ANTES de prod (bug histórico Phase 70.2 structured-output) — com o MODEL LITERAL real (`google/gemini-2.5-flash-lite`), não um alias sintético.
Pitfall 8 / ESCOPO HONESTO: CV-02 cobre APENAS Gemini chat OpenAI-compat. O media pipeline Gemini (visão, `generate_content` nativo multimodal) NÃO é funnelável pelo gateway atual — fica FORA do "100%" e é reportado como gasto conhecido off-gateway no 22-07.
Rollback do alias: capturar o estado de `gatewayctl model-alias list` ANTES (Task 1); reverter = remover o alias criado (`gatewayctl model-alias` delete/rm se existir, senão re-set pro estado anterior). NÃO tocar o alias `qwen` existente.
GW=$(docker ps -q -f name=ai-gateway-prod_gateway|head -1) no worker-vm.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provar o path OpenAI-compat do Gemini + testar tools pelo gateway</name>
  <read_first>
    - 22-RESEARCH.md §CV-02 "Dois caminhos" + Open Questions 3 e 4
    - saída de `gatewayctl model-alias list` e `gatewayctl upstreams list`
  </read_first>
  <files>.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-04-GEMINI-CHAT.md</files>
  <action>
    Antes de tudo, capturar o estado atual `gatewayctl model-alias list` (baseline de rollback). Resolver a Open Question 3 por FATO: `curl` no endpoint real que o director genérico produziria — `https://generativelanguage.googleapis.com/v1beta/openai/v1/chat/completions` (path.Join) vs o correto `/v1beta/openai/chat/completions`. Se o primeiro der 404, caminho A exige director dedicado (fora do ponytail — evitar). Registrar em 22-04-GEMINI-CHAT.md o resultado dos dois paths. Depois testar o CAMINHO B como preferido: criar o alias temporário com nome = **literal enviado pelo app** (`google/gemini-2.5-flash-lite`) e mandar 1 request `/v1/chat/completions` com esse `model` literal (NÃO um alias sintético — concern HIGH), `tools`+`tool_choice` via a tenant converseai — provar que retorna `tool_calls` (Pitfall 4/structured-output). Critério mínimo: HTTP 200 + `tool_calls` presente + `finish_reason=tool_calls`. Registrar a resposta.
  </action>
  <acceptance_criteria>
    22-04-GEMINI-CHAT.md registra: status HTTP dos dois paths Gemini (prova A vs B) e a resposta do request com tools (200 + tool_calls presente ou o erro).
  </acceptance_criteria>
  <done>Path Gemini provado; tool-calling pelo gateway testado; caminho recomendado com evidência.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: escolher caminho A (upstream nativo) ou B (alias)</name>
  <action>Pausar para o Pedro escolher A vs B com base na prova de path do Task 1 (22-04-GEMINI-CHAT.md). Ver <decision>/<options> abaixo.</action>
  <decision>Escolher CAMINHO A (upstream nativo gemini-chat via migration+director) ou CAMINHO B (alias gemini-flash-lite → openrouter-chat).</decision>
  <context>O CONTEXT travou "criar upstream de fallback gemini de CHAT" (redação = caminho A). A pesquisa e a Task 1 mostram que A quebra por path (precisa director dedicado) enquanto B é proven, uma linha, zero migration/director. Decisão do Pedro backing pela prova do Task 1.</context>
  <options>
    <option id="caminho-b">
      <name>CAMINHO B — alias google/gemini-2.5-flash-lite → openrouter-chat (recomendado)</name>
      <pros>Proven (spec 113); 1 comando; sem migration/director; tool-calling já testável</pros>
      <cons>OpenRouter serve o Gemini (markup pequeno); provider vira openrouter-fireworks (não Google direto). Se escolhido, o funnel/ROI (22-07) reporta este gasto como "via gateway usando OpenRouter como provider", NÃO "Gemini/Google direto" — semântica de custo muda.</cons>
    </option>
    <option id="caminho-a">
      <name>CAMINHO A — upstream nativo gemini-chat (redação literal do CONTEXT)</name>
      <pros>Google direto (sem markup OpenRouter); é o que o CONTEXT pediu ao pé da letra</pros>
      <cons>Task 1 mostra path.Join quebra → exige migration + director dedicado (código Go novo, escopo maior/risco)</cons>
    </option>
  </options>
  <resume-signal>Responder "caminho B" OU "caminho A" (ciente do custo de director dedicado)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Aplicar o caminho escolhido + validar billing/comportamento</name>
  <read_first>
    - 22-04-GEMINI-CHAT.md (prova + decisão)
    - 22-RESEARCH.md §CV-02 (comando do alias / molde de migration 0029 se caminho A)
    - 22-01-PRICE-AUDIT.md (preço de gemini-2.5-flash-lite provider openrouter-fireworks já existe)
  </read_first>
  <files>prod DB ai_gateway.model_aliases (caminho B) ou nova migration ai_gateway.upstreams + director (caminho A)</files>
  <action>
    CAMINHO B: `docker exec $GW /gatewayctl model-alias set -alias google/gemini-2.5-flash-lite -upstream openrouter-chat -target google/gemini-2.5-flash-lite` (alias com nome = LITERAL enviado pelo app — concern HIGH); aguardar o poll de 60s do resolver; confirmar em `gatewayctl model-alias list`. CAMINHO A (só se decidido): criar migration aditiva (molde 0029, ON CONFLICT DO NOTHING, swap constraint se colidir (role,tier,tier_priority)) inserindo o upstream gemini-chat + env UPSTREAM_LLM_FALLBACK_1_URL/_AUTH_BEARER no stack do gateway + director dedicado de reescrita de path — e rodar `gatewayctl migrate up`. Em AMBOS: garantir que o preço (22-01) casa a chave (model,provider,unit) do upstream resolvido (Pitfall 5). Mandar 1 request final com o model literal e confirmar billing_events com o upstream esperado e cost_external_brl > 0. **Rollback documentado**: remover o alias criado (voltar ao baseline capturado na Task 1). Não regredir Phase 20 (não tocar pod_config/reconciler) nem o alias qwen existente.
  </action>
  <acceptance_criteria>
    `gatewayctl model-alias list` mostra o alias `google/gemini-2.5-flash-lite`→upstream; request de prova `/v1/chat/completions model=google/gemini-2.5-flash-lite` (o LITERAL que o app envia) → 200; `psql` billing_events registra o request com upstream esperado e cost_external_brl > 0.
  </acceptance_criteria>
  <done>Gateway serve chat Gemini pelo caminho decidido; billing captura com preço correto; comportamento (tools) preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → gateway model_aliases/upstreams | config de roteamento prod; erro afeta resolução de chat |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Denial of Service | caminho A com path errado → 404 em chat | mitigate | Task 1 prova o path ANTES; default caminho B (proven) |
| T-22-11 | Tampering | alias/upstream regride qwen ou Phase 20 | mitigate | migration aditiva (ON CONFLICT DO NOTHING); não tocar pod_config/reconciler; conferir alias qwen intacto |
| T-22-12 | Information Disclosure | tool_choice/structured não repassado → resposta errada | mitigate | testar tools+tool_choice via gateway (Task 1) antes de 22-06 ligar em prod |
</threat_model>

<verification>
- gatewayctl model-alias list mostra google/gemini-2.5-flash-lite (nome literal)
- request com tools → tool_calls
- billing_events registra upstream com custo coerente
- qwen + Phase 20 intactos
</verification>

<success_criteria>
Gateway serve chat Gemini pelo caminho decidido por prova; tool-calling validado; billing correto. Pré-requisito de 22-06 (classifier/format-hint) pronto sem quebrar comportamento.
</success_criteria>

<output>
Create `.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-04-SUMMARY.md` when done
</output>
