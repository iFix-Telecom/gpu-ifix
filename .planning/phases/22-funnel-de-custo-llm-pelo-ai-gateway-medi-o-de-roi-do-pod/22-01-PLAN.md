---
phase: 22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "prod DB ai_gateway.prices (via gatewayctl prices set/set-fx — sem arquivo no repo)"
autonomous: false
requirements: [PRICE-01]
user_setup: []

must_haves:
  truths:
    - "ai_gateway.prices tem 1 linha active (valid_to IS NULL) por (model,provider,unit) com unit_cost_usd realista para todos os upstreams que a converseai vai passar a usar (gemini-stt, openai-whisper, openrouter-chat p/ gemini-flash-lite, deepseek)"
    - "fx_rates active reflete o USD/BRL corrente (não um valor stale)"
    - "após 1 request real, billing_events.cost_external_brl bate com audio_s|tokens × unit_cost_usd × fx (não R$0, não valor irreal)"
    - "gateway_prices_missing_total NÃO incrementa após o request de prova (chave (model,provider,unit) casou)"
  artifacts:
    - path: ".planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-01-PRICE-AUDIT.md"
      provides: "tabela de preços reais decididos vs vigentes, cruzada com faturas Google/OpenAI"
      min_lines: 20
  key_links:
    - from: "ai_gateway.prices (model,provider,unit,unit_cost_usd)"
      to: "billing_events.cost_external_brl"
      via: "interceptor_usage.go priceTokens -> billing/cost.go ComputeCostBRL (units × unit_cost_usd × fx)"
      pattern: "ComputeCostBRL"
    - from: "providerForUpstream(upstream) + sttBillingModel(upstream)"
      to: "chave de lookup na tabela prices"
      via: "runtime — preço p/ provider errado = R$0 silencioso + gateway_prices_missing_total++"
      pattern: "providerForUpstream"
---

<objective>
PRICE-01 é o GATE de toda a fase: sem preços reais em `ai_gateway.prices`, qualquer conclusão de ROI (MEASURE-01) sai errada a favor do pod. Corrigir `unit_cost_usd` por (model,provider,unit) e o câmbio USD/BRL para que `cost_external_brl` reflita o custo externo REAL. Tudo é hot-reload (NOTIFY `prices_changed`/`fx_changed`), sem deploy.

Purpose: destravar a medição honesta. Nenhuma outra conclusão de ROI vale antes deste plano fechar.
Output: preços corrigidos no DB prod (via `gatewayctl`), documento de auditoria de preços, prova empírica de que um request real bila coerente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-CONTEXT.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-RESEARCH.md

<interfaces>
<!-- Fatos da pesquisa (22-RESEARCH.md §PRICE-01) — usar diretamente, sem re-explorar -->
Tabela: ai_gateway.prices(model, provider, unit, unit_cost_usd numeric(12,8), valid_from, valid_to, notes)
  UNIQUE(model,provider,unit,valid_from); active = valid_to IS NULL; unit ∈ {input_token,output_token,audio_second,embed_request}
cost_external_brl NÃO está em prices — é coluna de billing_events, calculada por request.
Chave de lookup em runtime (interceptor_usage.go):
  provider = providerForUpstream(upstream): openrouter-chat→openrouter-fireworks; openai-embed/openai-whisper→openai;
             DEFAULT→o próprio nome do upstream (gemini-stt→provider "gemini-stt").
  model p/ STT (resp só {"text":...}) = sttBillingModel(upstream): gemini-stt→"gemini-2.5-flash-lite",
             openai-whisper→"whisper-1", groq-whisper→"whisper-large-v3".
Preços vigentes hoje (gatewayctl prices list, 2026-07-19):
  gemini-2.5-flash-lite | gemini-stt | audio_second | 0.00000960
  whisper-1 | openai | audio_second | 0.00010000
  qwen3.5-27b | openrouter-fireworks | input_token 0.00000020 / output_token 0.00000156
FX ativo: USD/BRL = 5.082509
CLI: docker exec $GW /gatewayctl prices {list|set|set-fx}; set = INSERT nova valid_from (não há -update).
  set-fx -usd-brl <n>; set -model -provider -unit -usd [-notes]
Acesso: GW=$(docker ps -q -f name=ai-gateway-prod_gateway|head -1) no worker-vm (imagem scratch, só /gatewayctl).
psql: via container postgres do worker-vm com $AI_GATEWAY_PG_DSN (SELECT), DB bd_ai_gateway.
Métrica: gateway_prices_missing_total em /admin/metrics (X-Admin-Key ifix_admin_49e46cb79de69ec59af89a48e828613f).
Faturas brutas: /sync/completions_usage_2026-*.{json,csv} (OpenAI) + /sync/Minha conta de faturamento_Relatórios, 2026-07-01 — 2026-07-31.csv (Google).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar preços vigentes vs custo real das faturas</name>
  <read_first>
    - 22-RESEARCH.md §PRICE-01 (DDL, chave de lookup, preços vigentes, HIPÓTESES 1-2)
    - /sync/completions_usage_2026-07*.csv (uso OpenAI — isolar áudio/whisper)
    - "/sync/Minha conta de faturamento_Relatórios, 2026-07-01 — 2026-07-31.csv" (Google, discriminado por serviço)
    - saída de `ssh worker-vm 'docker exec $(docker ps -q -f name=ai-gateway-prod_gateway|head -1) /gatewayctl prices list'`
  </read_first>
  <files>.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-01-PRICE-AUDIT.md</files>
  <action>
    Levantar TODOS os upstreams externos que a converseia vai passar a rotear (gemini-stt, openai-whisper, openrouter-chat servindo gemini-2.5-flash-lite e deepseek-v4-flash) e para cada um a chave real de lookup (model,provider,unit) usando providerForUpstream/sttBillingModel (ver interfaces). Cruzar o custo modelado hoje (`SUM(audio_seconds) × 0.0000096 × fx` para gemini-stt; tokens × preço para chat) contra a fatura Google (R$238) e o export OpenAI ($38), resolvendo a HIPÓTESE do CONTEXT: "gemini-stt R$1,33 é subvalorizado OU é só volume off-gateway?". Registrar em 22-01-PRICE-AUDIT.md uma tabela: coluna (model,provider,unit) | USD vigente | USD real proposto (com fonte: preço de tabela do provider em USD/1M tok ou USD/min) | delta. Incluir o câmbio USD/BRL corrente a usar (fonte). Marcar `HIPÓTESE:` onde a fatura não discriminar o suficiente e dizer qual dado resolveria. NÃO alterar nada ainda — só auditar.
  </action>
  <acceptance_criteria>
    Arquivo 22-01-PRICE-AUDIT.md existe com >=1 linha por upstream externo alvo, cada uma com USD vigente, USD proposto e fonte; a pergunta "gemini-stt subvalorizado vs volume" respondida com FATO (fatura) ou marcada HIPÓTESE explícita.
  </acceptance_criteria>
  <done>Auditoria completa; valores propostos têm fonte; ambiguidades marcadas HIPÓTESE.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: aprovar valores de preço reais + câmbio</name>
  <action>Pausar para o Pedro aprovar os USD por (model,provider,unit) e o USD/BRL da 22-01-PRICE-AUDIT.md antes da Task 2 gravar em prod. Ver <decision>/<options> abaixo.</action>
  <decision>Aprovar os valores de preço reais (USD por model/provider/unit) e o câmbio USD/BRL a gravar em prod.</decision>
  <context>PRICE-01 muta preços em prod (afeta cost_external_brl de todo request externo dali pra frente — retroativo NÃO, é calculado no ingest). Erro de valor = ROI enviesado. O Pedro precisa confirmar os números da auditoria (Task 1) antes de escrever no DB.</context>
  <options>
    <option id="approve-audit">
      <name>Aprovar valores da 22-01-PRICE-AUDIT.md como estão</name>
      <pros>Baseado em fatura real; destrava a fase</pros>
      <cons>Depende da fatura discriminar bem (algumas linhas podem ser HIPÓTESE)</cons>
    </option>
    <option id="adjust">
      <name>Ajustar valores específicos antes de gravar</name>
      <pros>Corrige qualquer preço que o Pedro saiba estar errado</pros>
      <cons>Requer input manual dos valores</cons>
    </option>
  </options>
  <resume-signal>Responder "aprovar preços" OU listar os (model,provider,unit)=USD a ajustar</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Aplicar preços + câmbio via gatewayctl e provar bilagem coerente</name>
  <read_first>
    - 22-01-PRICE-AUDIT.md (valores aprovados no checkpoint)
    - 22-RESEARCH.md §PRICE-01 "Passo-a-passo de execução" + Pitfall 5 (provider errado = R$0 silencioso)
  </read_first>
  <files>prod DB ai_gateway.prices + ai_gateway.fx_rates (via gatewayctl, sem arquivo no repo)</files>
  <action>
    No container do gateway (worker-vm, `GW=$(docker ps -q -f name=ai-gateway-prod_gateway|head -1)`): rodar `docker exec $GW /gatewayctl prices set-fx -usd-brl <valor aprovado>` e, para cada linha aprovada, `docker exec $GW /gatewayctl prices set -model <m> -provider <p> -unit <u> -usd <v> -notes "revalidado jul/2026 phase 22 PRICE-01"`. CRÍTICO (Pitfall 5): usar EXATAMENTE o provider que `providerForUpstream` produz (ex.: gemini-2.5-flash-lite servido via openrouter-chat → provider `openrouter-fireworks`, NÃO `gemini-stt`; STT gemini-stt → provider `gemini-stt`). Cada `set` cria nova `valid_from`; o loader ativa a linha `valid_to IS NULL` mais recente por hot-reload (sem deploy). Depois confirmar via `gatewayctl prices list` que só há 1 linha active por (model,provider,unit) alvo. Disparar 1 request real de STT (áudio curto) pelo gateway com a key da tenant converseai-stt e um request de chat, e conferir o billing.
  </action>
  <acceptance_criteria>
    `gatewayctl prices list` mostra os USD novos e 1 linha active por (model,provider,unit) alvo; `psql "$AI_GATEWAY_PG_DSN" -c "SELECT upstream,cost_external_brl,audio_seconds,tokens_in,tokens_out FROM ai_gateway.billing_events ORDER BY ts DESC LIMIT 3"` mostra cost_external_brl > 0 e coerente com (units × unit_cost_usd × fx); `/admin/metrics` mostra gateway_prices_missing_total SEM incremento após os requests de prova.
  </acceptance_criteria>
  <done>Preços e fx aplicados em prod; request real bila valor coerente; nenhuma chave faltando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → prod DB (gatewayctl prices) | escrita em tabela de billing prod; valor errado enviesa ROI |
| admin key → /admin/metrics | X-Admin-Key ifix_admin_****613f exposta em comando |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | prices set com provider/unit errado | mitigate | casar chave exata via providerForUpstream/sttBillingModel; conferir gateway_prices_missing_total==inalterado após prova (Task 2) |
| T-22-02 | Repudiation | mudança de preço sem trilha | mitigate | notes="revalidado jul/2026 phase 22 PRICE-01" em cada set; nova valid_from preserva histórico (linhas antigas viram valid_to, não deletadas) |
| T-22-03 | Information Disclosure | admin key em histórico de shell | accept | key já é operacional/documentada; escopo admin restrito ao gateway |
</threat_model>

<verification>
- `gatewayctl prices list` — 1 active por chave alvo, USD realista
- `SELECT` em billing_events pós-request — cost_external_brl coerente
- `/admin/metrics` gateway_prices_missing_total sem incremento
- 22-01-PRICE-AUDIT.md documenta a decisão de preços
</verification>

<success_criteria>
Preços e câmbio corrigidos em prod (hot-reload, sem deploy); um request real prova cost_external_brl coerente; zero chaves faltando. GATE liberado para MEASURE-01.
</success_criteria>

<output>
Create `.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-01-SUMMARY.md` when done
</output>
