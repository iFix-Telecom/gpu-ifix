# 22-02 GATE — código Phase 113 presente na imagem do stack 15?

Data: 2026-07-19 · Requirement: CV-01 (gate)

## Container + imagem (âncora de rollback)
- Container: `converseai-dev-agents` (vps-ifix-vm, stack 15, endpoint 3)
- Image: `localhost:5000/converseai-agents:develop`
- **Digest (rollback anchor): `sha256:95c9aa22c4f8f3d8caf975962f65c0e13676137fecf913c330be31e90cb7241b`**
  - Redeploy dessa imagem restaura o estado pré-fase.

## Prova de 2 níveis

### Nível 1 — config define as keys 113? (`grep /app/src/config.py`)
Alvo: `stt_gateway_key|agent_classifier_gateway_key|agent_format_hint_gateway_key|agent_ai_match_gateway_key|agent_gateway_base_url`
**Saída literal: VAZIA.** Nenhum campo 113 no config.

### Nível 2 — consumidor monta client do gateway? (`grep /app/src --include='*.py'`, exceto config.py)
Alvo: `stt_gateway_key|agent_.*_gateway_key|agent_gateway_base_url|gateway_enabled`
**Saída literal: VAZIA.** Nenhum consumidor.

### Grep amplo `gateway` (falso-negativo?)
O único roteamento pelo gateway na imagem é **RAG HyDE** (`rag_hyde_gateway_key`, `rag/retrieval.py` — tenant converseai, pré-existente). NÃO é 113.

### Caminho STT hoje (FATO)
`src/media/transcription.py`: `AsyncOpenAI(api_key=settings.openai_api_key)` **sem base_url** → bate **OpenAI direto** (`api.openai.com`), model `gpt-4o-mini-transcribe` (config.py:133). É gasto OpenAI direto (parte dos $38), fora do gateway.

## VEREDITO: **AUSENTE**
O código de roteamento Phase 113 (STT/secundárias pelo gateway) **NÃO está na imagem em execução**. Confirma a pesquisa (commits 113 dangling, fora de origin/develop).

**Implicação:** setar `STT_GATEWAY_KEY` no stack 15 seria **no-op silencioso**. CV-01 STT (22-03) e secundárias (22-06) estão BLOQUEADOS até re-land (Task 3, requer aprovação humana).

**Independentes de 113 (não bloqueados):** 22-04 (gemini chat capability, muta model_aliases prod), 22-05 (followup, worker TS, spec próprio), 22-07 (measure do que estiver funnelado).

---

## RE-LAND executado (Task 3, checkpoint aprovado "re-landar 113 agora")

Cherry-pick de **6** commits (o plano listava 5 — faltava o TDD `1d8825aa` que cria `test_provider_gateway.py`) sobre develop (c6fd7b6c), branch `phase22-reland-113`:
`5959ebac` config → `64eaeb0c` create_llm → `989ccbb3` RED tests → `abec4bc8` route secundárias → `63bb81dc` RED STT → `ef0eba0a` STT gateway.

- Conflito: `test_provider_gateway.py` (DU) — resolvido incluindo o commit criador `1d8825aa` na ordem.
- Testes 113: **28 passed** (test_provider_gateway + test_transcription_gateway + test_ai_match_provider), venv uv local (CI py roda `|| true`, não valida).
- PR **#16** → merge commit **`66fcf05c`** em origin/develop (113 não-dangling).
- Deploy Dev run 29698406637 (dispatch, o push-merge não disparou run) → build+recreate ✅.

### Re-check 2 níveis no container novo — VEREDITO: **PRESENTE**
- Novo digest: `sha256:b19252ba22eb81bbb79aea5dd0b2cd90ef6946cce25cf39c17c5ee16b584832e`
- Nível 1: `hasattr(s,'stt_gateway_key')==True`, `hasattr(s,'agent_classifier_gateway_key')==True` (5 campos no config).
- Nível 2: consumidores presentes — `llm/provider.py` (gateway clients classifier/format-hint/ai-match + base_url) e `media/transcription.py:90` (`if settings.stt_gateway_key:` → gateway hop).
- Comportamento **inerte** (keys default "" → fornecedor direto). Ativação = env (22-03/22-06).

### Rollback de código
`git revert 66fcf05c` (revert do merge, 1 unidade) + push → CI rebuilda; imediato = redeploy imagem digest anterior **`sha256:95c9aa22c4f8f3d8caf975962f65c0e13676137fecf913c330be31e90cb7241b`**.
