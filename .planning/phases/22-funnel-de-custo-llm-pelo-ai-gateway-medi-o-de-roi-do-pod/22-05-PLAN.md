---
phase: 22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod
plan: 05
type: execute
wave: 4
depends_on: ["22-03"]
files_modified:
  - "/home/pedro/projetos/pedro/converseai-v4/apps/worker/src/shared/followup-llm.ts"
  - "Portainer stack 15 converseai-v4-dev — env FOLLOWUP_GATEWAY_KEY + redeploy do worker"
autonomous: false
requirements: [CV-03]
user_setup: []

must_haves:
  truths:
    - "getFollowupClient tem um branch gateway: quando FOLLOWUP_GATEWAY_KEY setada, o followup aponta baseURL pro gateway e usa model=qwen (alias já resolve deepseek-v4-flash)"
    - "com FOLLOWUP_GATEWAY_KEY vazia, o comportamento é idêntico ao atual (fornecedor direto) — opt-in por env"
    - "após ativar, tenant converseai-followup sai de 0 para N rows em gatewayctl usage report"
  artifacts:
    - path: "/home/pedro/projetos/pedro/converseai-v4/apps/worker/src/shared/followup-llm.ts"
      provides: "branch gateway em getFollowupClient"
      contains: "FOLLOWUP_GATEWAY_KEY"
  key_links:
    - from: "FOLLOWUP_GATEWAY_KEY no stack 15 (worker)"
      to: "billing_events tenant converseai-followup (faf8d8a8-…)"
      via: "getFollowupClient new OpenAI({apiKey, baseURL: gateway}) model=qwen"
      pattern: "getFollowupClient"
---

<objective>
CV-03: rotear o followup (worker TS) pelo gateway. É caminho PRÓPRIO (spec `docs/route-secondary-llms-through-gateway.md`), NÃO Phase 113 — o followup vive em `apps/worker` (TS), separado dos agents Python, e NÃO depende dos commits 113 dangling. Implementável do zero pelo spec.

Purpose: fechar mais uma fonte de gasto no funnel (followup = deepseek/OpenRouter direto hoje).
Output: branch gateway em getFollowupClient; env opt-in; UAT usage > 0.

**Nota de ordenação (desvio intencional da sequência LOCKED do CONTEXT):** o CONTEXT numera CV-03 (followup) DEPOIS de CV-01 completo (item 4 após item 2 = classifier/format-hint/ai-match em 22-06). Este plano ativa em wave 4, ANTES de 22-06 (wave 5). O desvio é SEGURO e deliberado: (a) o followup é codebase própria (`apps/worker` TS), NÃO depende dos commits 113 nem do alias literal de CV-02; (b) cada ativação preserva seu próprio UAT + rollback isolado (key vazia = comportamento antigo); (c) `depends_on: [22-03]` garante que o funnel já foi validado end-to-end pelo STT antes. A "sequência" LOCKED é ordem de raciocínio/risco, não uma barreira transacional — o safeguard (uma key por vez + rollback) é mantido. Nenhum revisor cross-AI sinalizou a ordem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-CONTEXT.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-RESEARCH.md

<interfaces>
<!-- Fatos: 22-RESEARCH.md §CV-03 + Open Question 5 -->
Spec: /home/pedro/projetos/pedro/converseai-v4/docs/route-secondary-llms-through-gateway.md (§Worker TS + §Implementação item 3).
Alvo: apps/worker/src/shared/followup-llm.ts::getFollowupClient (~linha 85) — monta new OpenAI({apiKey, baseURL})
  escolhendo provider por prefixo (openrouter/gemini/openai).
Mudança: adicionar branch gateway — quando process.env.FOLLOWUP_GATEWAY_KEY setado → baseURL pro gateway + model=qwen
  (alias qwen já resolve deepseek-v4-flash; SEM alias novo p/ followup).
  ⚠ model=qwen MUDA o modelo efetivo do followup (concern codex MEDIUM): documentar o modelo ATUAL do followup (grep no getFollowupClient/config) e a diferença esperada ao trocar pra qwen→deepseek. Confirmar ANTES de ativar que o alias `qwen` resolve (fallback externo correto) E que o preço/billing do deepseek está correto pra essa tenant (22-01).
Env stack 15 (worker): FOLLOWUP_GATEWAY_KEY = <key da tenant converseai-followup — ver `/home/pedro/.claude/CLAUDE.md` seção API Keys; NÃO inline, plano é commitado> (tenant converseai-followup, faf8d8a8-74d7-4d87-95eb-1c256773c0f6).
  base_url default do gateway: https://ai-gateway.converse-ai.app/v1
Prova: gatewayctl usage report --tenant converseai-followup > 0 rows; log do worker com base_url do gateway SANITIZADO (só provider/baseURL — NUNCA logar a API key — concern codex MEDIUM).
Confirmar assinatura/linha atual antes de editar: grep -n "FOLLOWUP_GATEWAY_KEY\|getFollowupClient" apps/worker/src/shared/followup-llm.ts (HEAD).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch gateway em getFollowupClient (opt-in por env)</name>
  <behavior>
    - FOLLOWUP_GATEWAY_KEY setada → cliente usa baseURL do gateway (default https://ai-gateway.converse-ai.app/v1) + apiKey=a key + model resolvido para "qwen"
    - FOLLOWUP_GATEWAY_KEY vazia/ausente → comportamento idêntico ao atual (branch de provider por prefixo inalterado)
  </behavior>
  <read_first>
    - /home/pedro/projetos/pedro/converseai-v4/docs/route-secondary-llms-through-gateway.md (§Worker TS + item 3)
    - /home/pedro/projetos/pedro/converseai-v4/apps/worker/src/shared/followup-llm.ts (assinatura real de getFollowupClient no HEAD + MODELO ATUAL usado pelo followup — registrar no SUMMARY p/ comparar com qwen)
  </read_first>
  <files>/home/pedro/projetos/pedro/converseai-v4/apps/worker/src/shared/followup-llm.ts</files>
  <action>
    Seguindo o spec §Implementação item 3: adicionar em getFollowupClient um branch `gateway` que, quando `process.env.FOLLOWUP_GATEWAY_KEY` está setado, retorna `new OpenAI({ apiKey: process.env.FOLLOWUP_GATEWAY_KEY, baseURL: process.env.FOLLOWUP_GATEWAY_BASE_URL ?? 'https://ai-gateway.converse-ai.app/v1' })` e força `model=qwen` no ponto onde o modelo é escolhido. Branch tem precedência quando a key existe; senão cai no comportamento por-prefixo atual INALTERADO (opt-in). Qualquer log do branch deve ser SANITIZADO — logar provider/baseURL, NUNCA a API key (concern codex MEDIUM). Não mudar assinaturas exportadas além do necessário. Adicionar/rodar o teste unit do worker cobrindo os dois casos (key setada → baseURL gateway + model qwen; key vazia → cliente direto atual).
  </action>
  <acceptance_criteria>
    `grep -n FOLLOWUP_GATEWAY_KEY apps/worker/src/shared/followup-llm.ts` mostra o branch; teste unit do worker passa nos dois casos (`bun test` do worker verde); `tsc`/build limpo.
  </acceptance_criteria>
  <done>Branch gateway implementado e testado; comportamento inerte com key vazia.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: ativar followup pelo gateway + UAT</name>
  <action>Setar FOLLOWUP_GATEWAY_KEY no worker do stack 15, redeploy e verificar o funnel de followup conforme <how-to-verify>. Prod dev — verificação humana.</action>
  <what-built>Código do branch gateway em getFollowupClient (mergeado em develop, CI buildou :develop do worker) + setar FOLLOWUP_GATEWAY_KEY (key da tenant converseai-followup — ver CLAUDE.md) no env do worker no stack 15 e redeploy. Prod dev — Pedro verifica antes de considerar ativo.</what-built>
  <how-to-verify>
    0. PRÉ: confirmar que o alias `qwen` resolve com fallback externo correto e preço/billing correto pra tenant converseai-followup (`gatewayctl model-alias list` + preço 22-01) ANTES de ativar.
    1. Confirmar CI build `:develop` do worker verde e stack 15 redeployado (webhook OU pull+recreate).
    2. Gerar/aguardar tráfego de followup real.
    3. `ssh worker-vm 'docker exec $(docker ps -q -f name=ai-gateway-prod_gateway|head -1) /gatewayctl usage report --tenant converseai-followup --from <hoje> --to <hoje>'` → > 0 rows.
    4. Log do worker mostra o baseURL do gateway (SANITIZADO, sem key) na chamada de followup.
    5. UAT antes/depois (concern codex MEDIUM — model muda de <modelo atual> pra qwen→deepseek): comparar a saída de followup em 2-3 casos reais antes vs depois; confirmar que a qualidade/tom das mensagens continua aceitável (não só "continua correto").
    6. Rollback disponível: esvaziar FOLLOWUP_GATEWAY_KEY + redeploy → followup volta ao provider direto.
  </how-to-verify>
  <resume-signal>Responder "followup aprovado" OU "rollback"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → apps/worker código + stack 15 env | edit de prod dev + key de tenant em env |
| worker → gateway | followup de cliente passa a rotear pelo gateway |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-13 | Denial of Service | followup quebra pós-repoint | mitigate | branch opt-in; key vazia = comportamento atual; rollback = esvaziar key + redeploy |
| T-22-14 | Tampering | branch altera caminho direto existente | mitigate | teste unit prova key-vazia == comportamento atual inalterado |
</threat_model>

<verification>
- grep FOLLOWUP_GATEWAY_KEY na followup-llm.ts (branch existe)
- bun test worker verde (dois casos)
- gatewayctl usage --tenant converseai-followup > 0 pós-deploy
</verification>

<success_criteria>
Followup roteado pelo gateway via opt-in por env, testado nos dois casos, capturado em billing_events, rollback disponível.
</success_criteria>

<output>
Create `.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-05-SUMMARY.md` when done
</output>
