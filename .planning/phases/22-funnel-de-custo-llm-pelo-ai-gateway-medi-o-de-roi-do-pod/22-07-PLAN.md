---
phase: 22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod
plan: 07
type: execute
wave: 6
depends_on: ["22-01", "22-03", "22-04", "22-05", "22-06"]
files_modified:
  - ".planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-07-ROI-QUERY.sql"
  - ".planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-07-ROI.md"
autonomous: false
requirements: [MEASURE-01]
user_setup: []

must_haves:
  truths:
    - "Existe uma query SQL mensal, runnable, que responde 'pod compensa?': sum(cost_external_brl) por upstream/tenant vs custo do pod (primary_lifecycles.total_cost_brl no mês)"
    - "GET /admin/economy retorna roi_multiplier / phantom_brl / vast_brl / custo_openrouter_brl NÃO-nulos, coerentes com os preços corrigidos (22-01) e com a query SQL"
    - "A resposta ROI só é declarada válida DEPOIS de PRICE-01 (22-01) — preços realistas — e com o funnel ligado (22-03/04/05/06)"
  artifacts:
    - path: ".planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-07-ROI-QUERY.sql"
      provides: "query mensal ROI por upstream/tenant vs custo pod"
      min_lines: 15
    - path: ".planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-07-ROI.md"
      provides: "resultado atual + cross-check /admin/economy vs SQL + veredito 'pod compensa?'"
      min_lines: 15
  key_links:
    - from: "billing_events.cost_external_brl (por upstream/tenant)"
      to: "veredito ROI vs primary_lifecycles.total_cost_brl"
      via: "query SQL mensal + GET /admin/economy (economy.go)"
      pattern: "primary_lifecycles.total_cost_brl"
---

<objective>
MEASURE-01 fecha a fase: transformar o funnel em resposta objetiva "alugar pod compensa?". A pesquisa mostrou que o `GET /admin/economy` (economy.go, Phase 15) JÁ computa phantom_brl/vast_brl/roi_multiplier/custo_openrouter_brl + série diária, e o custo do pod já está persistido em `primary_lifecycles.total_cost_brl`. Ponytail: MEASURE-01 é REUSO + uma query mensal por upstream/tenant, NÃO construção do zero. Só é válido APÓS PRICE-01 (preços reais) e com o funnel ligado — daí a última wave.

Purpose: entregar o número que motivou a fase, agora confiável (preços certos + gasto passando pelo gateway).
Output: query SQL mensal committed + validação do endpoint /admin/economy + veredito documentado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-CONTEXT.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-RESEARCH.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-01-PRICE-AUDIT.md

<interfaces>
<!-- Fatos: 22-RESEARCH.md §MEASURE-01 + Open Question 6 -->
Query ROI (validada ao vivo na pesquisa):
  SELECT b.upstream, t.slug, count(*) reqs,
         round(sum(b.cost_external_brl),4) externo_brl,
         round(sum(b.cost_local_phantom_brl),4) phantom_brl
  FROM ai_gateway.billing_events b JOIN ai_gateway.tenants t ON t.id=b.tenant_id
  WHERE b.ts >= date_trunc('month', now())
  GROUP BY b.upstream, t.slug ORDER BY externo_brl DESC NULLS LAST;
Custo pod: ai_gateway.primary_lifecycles.total_cost_brl (accrual accepted_dph × horas up); started_at/ended_at.
Endpoint pronto: GET /admin/economy (X-Admin-Key ifix_admin_49e46cb79de69ec59af89a48e828613f) computa:
  phantom_brl, vast_brl, economia_liquida_brl=phantom−vast, roi_multiplier=phantom/vast,
  custo_openrouter_brl=SUM(cost_external_brl), + Series[] diária. Consumido pelo painel "Economia" (Phase 15/OBS-09).
Open Question 6: se o painel só mostra agregado, MEASURE-01 pede o recorte por upstream/tenant (a query SQL acima entrega isso;
  o "opcional expor no dashboard" do CONTEXT/roadmap permanece opcional — não obrigatório neste plano).
psql via container postgres do worker-vm com $AI_GATEWAY_PG_DSN, DB bd_ai_gateway.
GW=$(docker ps -q -f name=ai-gateway-prod_gateway|head -1) no worker-vm.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query ROI mensal por upstream/tenant vs custo pod (runnable) + rodar</name>
  <read_first>
    - 22-RESEARCH.md §MEASURE-01 (query validada + primary_lifecycles.total_cost_brl)
    - 22-01-PRICE-AUDIT.md (confirmar que os preços já foram corrigidos — GATE)
  </read_first>
  <files>.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-07-ROI-QUERY.sql + 22-07-ROI.md</files>
  <action>
    Escrever em 22-07-ROI-QUERY.sql uma query mensal committed que entregue: (1) a tabela por upstream/tenant de `sum(cost_external_brl)` e `sum(cost_local_phantom_brl)` (a query validada nas interfaces); (2) o custo do pod no mês via `SELECT round(sum(total_cost_brl),4) FROM ai_gateway.primary_lifecycles WHERE started_at >= date_trunc('month', now())`; (3) o veredito: total externo funnelado vs custo pod (e vs phantom, que mede a economia do pod). Rodar a query no postgres do worker-vm (`psql "$AI_GATEWAY_PG_DSN" -f ...` ou -c) e colar o resultado atual em 22-07-ROI.md. Marcar como GATE: só é resposta válida porque 22-01 corrigiu os preços — anotar isso explicitamente.
  </action>
  <acceptance_criteria>
    22-07-ROI-QUERY.sql existe e roda sem erro; 22-07-ROI.md contém o resultado atual (breakdown por upstream/tenant + custo pod do mês + veredito preliminar).
  </acceptance_criteria>
  <done>Query mensal committed e executada; resultado registrado; dependência de PRICE-01 anotada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: validar ROI e o veredito "pod compensa?"</name>
  <action>Cruzar GET /admin/economy com a query SQL do Task 1 e registrar o veredito do Pedro conforme <how-to-verify>. Só válido após 22-01 (PRICE-01).</action>
  <what-built>Cross-check do endpoint /admin/economy (que o painel Economia da Phase 15 consome) contra a query SQL do Task 1, com os preços já corrigidos (22-01) e o funnel ligado (22-03/04/05/06). Entrega o veredito objetivo "pod compensa?" para o Pedro confirmar.</what-built>
  <how-to-verify>
    1. `curl -H "X-Admin-Key: ifix_admin_49e46cb79de69ec59af89a48e828613f" "https://ai-gateway.converse-ai.app/admin/economy?from=<1º do mês>&to=<hoje>"` → JSON com phantom_brl, vast_brl, roi_multiplier, custo_openrouter_brl NÃO-nulos.
    2. Conferir que `custo_openrouter_brl` (SUM cost_external_brl do endpoint) bate com o `externo_brl` somado da query SQL (Task 1) — mesma ordem de grandeza.
    3. Confirmar que o custo externo mensal agora reflete o funnel (subiu vs os ~R$13 pré-fase, porque STT/secundárias/followup passam pelo gateway) e que os preços são realistas (22-01).
    4. Ler o veredito: com o gasto externo agora medido, o pod (vast_brl / total_cost_brl) compensa? Registrar a conclusão do Pedro em 22-07-ROI.md.
    5. Se o painel Economia só mostra agregado e o Pedro quiser o recorte por upstream/tenant no dashboard, capturar como follow-up (opcional, fora do escopo obrigatório).
  </how-to-verify>
  <resume-signal>Responder "ROI validado" + a conclusão pod-compensa (sim/não/depende) OU apontar discrepância a corrigir</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| admin key → /admin/economy | X-Admin-Key exposta em curl; endpoint read-only |
| operator → psql SELECT bd_ai_gateway | leitura de billing/lifecycles prod |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-18 | Information Disclosure | conclusão ROI errada por preço stale | mitigate | GATE: só validar após 22-01 (PRICE-01); cross-check endpoint vs SQL (Task checkpoint) |
| T-22-19 | Repudiation | número ROI sem lastro | mitigate | query committed (22-07-ROI-QUERY.sql) reproduzível; resultado datado em 22-07-ROI.md |
</threat_model>

<verification>
- 22-07-ROI-QUERY.sql roda; breakdown por upstream/tenant + custo pod
- /admin/economy retorna roi_multiplier/phantom_brl/vast_brl/custo_openrouter_brl não-nulos
- custo_openrouter_brl do endpoint bate com a soma da query SQL
- veredito "pod compensa?" registrado
</verification>

<success_criteria>
Resposta objetiva e lastreada para "alugar pod compensa?": query mensal committed + endpoint validado, com preços reais (22-01) e o funnel ligado. MEASURE-01 fecha a fase.
</success_criteria>

<output>
Create `.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-07-SUMMARY.md` when done
</output>
