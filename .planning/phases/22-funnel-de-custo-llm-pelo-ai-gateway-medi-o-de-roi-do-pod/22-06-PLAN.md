---
phase: 22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod
plan: 06
type: execute
wave: 5
depends_on: ["22-03", "22-04", "22-05"]
files_modified:
  - "Portainer stack 15 converseai-v4-dev — env AGENT_CLASSIFIER_GATEWAY_KEY / AGENT_FORMAT_HINT_GATEWAY_KEY / AGENT_AI_MATCH_GATEWAY_KEY + redeploys sucessivos (sem arquivo no repo)"
autonomous: false
requirements: [CV-01]
user_setup: []

must_haves:
  truths:
    - "classifier, format-hint e ai-match da converseia roteiam pelo gateway, cada um ativado por sua env key, UMA POR VEZ com UAT entre cada"
    - "cada tenant (converseai-classifier, converseai-format-hint, converseai-ai-match) sai de 0 para N rows em gatewayctl usage report após sua ativação"
    - "classifier/format-hint (que usam google/gemini-2.5-flash-lite) resolvem pelo alias gemini-flash-lite do 22-04, com tool_choice/structured-output preservado"
    - "cada passo tem rollback provado (esvaziar a key específica + redeploy)"
  artifacts:
    - path: ".planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-06-SECONDARIES-UAT.md"
      provides: "log do rollout gradual (3 passos), usage 0→N por tenant, UAT qualitativo, rollback por passo"
      min_lines: 20
  key_links:
    - from: "AGENT_CLASSIFIER_GATEWAY_KEY / AGENT_FORMAT_HINT_GATEWAY_KEY / AGENT_AI_MATCH_GATEWAY_KEY"
      to: "billing_events das 3 tenants"
      via: "agents 113 roteia cada função pelo gateway; classifier/format-hint via alias gemini-flash-lite"
      pattern: "converseai-classifier"
---

<objective>
CV-01 (continuação): completar o rollout gradual das LLMs secundárias da converseia pelo gateway — classifier, format-hint, ai-match — UMA POR VEZ, com UAT e rollback entre cada. Isto ataca o grosso funnelável do R$238 Gemini (classifier + format-hint usam gemini-2.5-flash-lite). LOCKED: NÃO ligar tudo de uma vez.

Purpose: fechar o funnel das secundárias com o menor risco por passo; capturar Gemini/secundárias em billing_events.
Output: 3 keys ativadas sequencialmente, cada uma com prova usage 0→N + rollback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-CONTEXT.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-RESEARCH.md
@.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-04-GEMINI-CHAT.md

<interfaces>
<!-- Fatos: 22-RESEARCH.md §CV-01 (keys→tenant) + §CV-02 (classifier/format-hint = gemini-2.5-flash-lite) + Pitfall 4 -->
Env keys no stack 15 (ativar 1 por vez, nesta ordem):
  AGENT_CLASSIFIER_GATEWAY_KEY  = ifix_sk_hu3x6754mhf6j2fa6wso2gqrspmibr56  (converseai-classifier, bcdf82d0-3461-49c0-b6c7-b97510c8ce63)
  AGENT_FORMAT_HINT_GATEWAY_KEY = ifix_sk_2h7iv23niyxnsfdnntgtzyaft2rvurxq  (converseai-format-hint, 25d67e96-9dac-4924-aea1-547f30982e92)
  AGENT_AI_MATCH_GATEWAY_KEY    = ifix_sk_4u3dtfqcxkbs7kfwtyfo5jm2m4ctkm5v  (converseai-ai-match, 6e8a258b-818d-47bd-af9f-b5ca83cb9e21)
classifier default model = google/gemini-2.5-flash-lite; format-hint idem → dependem do alias gemini-flash-lite (22-04).
  Se a converseia passa o nome literal google/gemini-2.5-flash-lite ao gateway, garantir que resolve (alias/target casa) — conferir no 22-04.
ai-match: modelo próprio (ver config.py agent) — conferir se precisa alias adicional ou já resolve.
Prova por passo: gatewayctl usage report --tenant <slug> 0→N; billing_events upstream≠unknown.
Pitfall 4: classifier/format-hint usam with_structured_output → confirmar tool_choice pelo gateway (já testado em 22-04) por tenant.
Portainer stack 15 API: X-API-Key ptr_jBtR69HsSXBT4hL4RPdMKe6hQ17q7jQOyuF3dGZhKHQ= (endpoint 3).
Rollback por passo: esvaziar a key específica + redeploy → aquela função volta ao provider direto.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint passo 1/3: ativar classifier pelo gateway + UAT</name>
  <action>Setar AGENT_CLASSIFIER_GATEWAY_KEY no stack 15, redeploy e verificar conforme <how-to-verify>. Só este passo — não ligar os outros.</action>
  <what-built>Passo 1/3 do rollout — setar AGENT_CLASSIFIER_GATEWAY_KEY=ifix_sk_hu3x… no stack 15 e redeploy, roteando o classifier pelo gateway (via alias gemini-flash-lite do 22-04). Prod dev; Pedro verifica antes de seguir ao próximo passo.</what-built>
  <how-to-verify>
    1. Baseline: `gatewayctl usage report --tenant converseai-classifier` = 0 rows (registrar).
    2. Setar a key + redeploy stack 15.
    3. Gerar/aguardar tráfego de classificação; `gatewayctl usage report --tenant converseai-classifier --from <hoje> --to <hoje>` → > 0 rows.
    4. `psql "$AI_GATEWAY_PG_DSN" -c "SELECT DISTINCT upstream FROM ai_gateway.billing_events WHERE tenant_id='bcdf82d0-3461-49c0-b6c7-b97510c8ce63' AND ts>=date_trunc('day',now())"` → upstream esperado (não unknown), cost_external_brl coerente.
    5. UAT qualitativo: classificações continuam corretas (structured-output/tool_choice OK).
    6. Rollback disponível: esvaziar AGENT_CLASSIFIER_GATEWAY_KEY + redeploy.
    Registrar tudo em 22-06-SECONDARIES-UAT.md.
  </how-to-verify>
  <resume-signal>Responder "classifier aprovado" (seguir p/ format-hint) OU "rollback"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint passo 2/3: ativar format-hint pelo gateway + UAT</name>
  <action>Só após classifier aprovado: setar AGENT_FORMAT_HINT_GATEWAY_KEY no stack 15, redeploy e verificar conforme <how-to-verify>.</action>
  <what-built>Passo 2/3 — setar AGENT_FORMAT_HINT_GATEWAY_KEY=ifix_sk_2h7i… no stack 15 e redeploy (format-hint via alias gemini-flash-lite). Só após classifier aprovado.</what-built>
  <how-to-verify>
    1. Baseline: `gatewayctl usage report --tenant converseai-format-hint` = 0 rows.
    2. Setar a key + redeploy.
    3. `gatewayctl usage report --tenant converseai-format-hint --from <hoje> --to <hoje>` → > 0 rows.
    4. `psql` billing_events tenant 25d67e96-… → upstream≠unknown, custo coerente.
    5. UAT qualitativo: format-hint continua correto.
    6. Rollback: esvaziar AGENT_FORMAT_HINT_GATEWAY_KEY + redeploy.
    Registrar em 22-06-SECONDARIES-UAT.md.
  </how-to-verify>
  <resume-signal>Responder "format-hint aprovado" (seguir p/ ai-match) OU "rollback"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint passo 3/3: ativar ai-match pelo gateway + UAT</name>
  <action>Só após format-hint aprovado: confirmar alias do ai-match (criar se faltar), setar AGENT_AI_MATCH_GATEWAY_KEY no stack 15, redeploy e verificar conforme <how-to-verify>.</action>
  <what-built>Passo 3/3 — setar AGENT_AI_MATCH_GATEWAY_KEY=ifix_sk_4u3d… no stack 15 e redeploy (ai-match via gateway). Só após format-hint aprovado. Confirmar se ai-match precisa de alias próprio (conferir config.py do agent) — se sim, criar antes (molde 22-04).</what-built>
  <how-to-verify>
    1. Baseline: `gatewayctl usage report --tenant converseai-ai-match` = 0 rows.
    2. Confirmar que o modelo do ai-match resolve no gateway (alias/target). Se faltar alias, criar via model-alias set antes de setar a key.
    3. Setar a key + redeploy.
    4. `gatewayctl usage report --tenant converseai-ai-match --from <hoje> --to <hoje>` → > 0 rows.
    5. `psql` billing_events tenant 6e8a258b-… → upstream≠unknown, custo coerente.
    6. UAT qualitativo: ai-match continua correto. Rollback: esvaziar a key + redeploy.
    Registrar em 22-06-SECONDARIES-UAT.md o log final dos 3 passos.
  </how-to-verify>
  <resume-signal>Responder "ai-match aprovado" (CV-01 completo) OU "rollback"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → Portainer stack 15 env | 3 keys de tenant em env prod dev; cada redeploy afeta a converseia |
| agents → gateway | classifier/format-hint/ai-match de cliente passam a rotear pelo gateway |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-15 | Denial of Service | structured-output/tool_choice quebra em classifier/format-hint | mitigate | tool_choice já provado em 22-04; UAT qualitativo por passo; rollback por key |
| T-22-16 | Denial of Service | 3 ativações de uma vez amplificam falha | mitigate | LOCKED: uma key por vez, UAT entre cada, checkpoints separados (3 tasks) |
| T-22-17 | Tampering | preço p/ provider errado nas secundárias → R$0 | mitigate | 22-01 casa a chave; monitorar gateway_prices_missing_total após cada passo |
</threat_model>

<verification>
- usage report 0→N para cada tenant (classifier, format-hint, ai-match)
- billing_events por tenant: upstream≠unknown, custo coerente
- UAT qualitativo por passo; rollback por key documentado
</verification>

<success_criteria>
As 3 LLMs secundárias roteadas pelo gateway, uma por vez, cada uma provada (usage 0→N + billing) e com rollback. CV-01 completo; grosso funnelável do Gemini capturado.
</success_criteria>

<output>
Create `.planning/phases/22-funnel-de-custo-llm-pelo-ai-gateway-medi-o-de-roi-do-pod/22-06-SUMMARY.md` when done
</output>
