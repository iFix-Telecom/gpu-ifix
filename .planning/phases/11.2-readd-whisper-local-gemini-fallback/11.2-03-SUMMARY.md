---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 03
subsystem: database
tags: [migration, goose, sql, schema-cascade, stt, tier-priority, circuit-config, reconciler, lifecycle]

requires:
  - phase: 11.2-01
    provides: RED stubs for migration 0029 + loader cascade + reconciler/lifecycle 4-endpoint
  - phase: 11.2-02
    provides: UpstreamSTTFallback{1,2}{URL,AuthBearer,Model} Config fields wired in container env
provides:
  - DB migration 0029 (additive cascade): re-add local-stt + add gemini-stt (tier_priority=10, cooldown_s=120) + add groq-whisper (tier_priority=15) + promote openai-whisper (tier_priority=20)
  - UpstreamConfig.TierPriority int field + Loader.ResolveAllTier1(role) ASC ordering
  - Primary reconciler restored 3-role tier-0 override {llm,stt,tts} + 4-endpoint health gate (llm/stt/tts/dcgm)
  - Lifecycle restored primaryPodURLs.STT + -p 8001:8001 + PRIMARY_WHISPER_WEIGHTS env injection
  - migration 0029 applied live in DigitalOcean managed `bd_ai_gateway`
affects: [11.2-06 (dispatcher cascade loop uses ResolveAllTier1), 11.2-08 (UAT live cenários)]

tech-stack:
  added: [tier_priority INT NOT NULL DEFAULT 0]
  patterns: [Loader.ResolveAllTier1 stable-sort by TierPriority ASC]

key-files:
  created:
    - gateway/db/migrations/0029_readd_whisper_add_gemini_groq.sql
  modified:
    - gateway/internal/upstreams/types.go (TierPriority field)
    - gateway/internal/upstreams/loader.go (ResolveAllTier1 + snapshot SELECT)
    - gateway/internal/upstreams/loader_test.go (3 new tests unskipped)
    - gateway/internal/primary/reconciler.go (3-role startDrain/markReady/closeLifecycle + 4-endpoint health gate)
    - gateway/internal/primary/lifecycle.go (STT field + port 8001 + WHISPER env)
    - gateway/internal/primary/{reconciler,lifecycle}_test.go (6 unit tests unskipped)
    - gateway/internal/db/queries/upstreams.sql + sqlc regen
    - gateway/internal/integration_test/* (cascading test count fixes — see follow-up commits)

key-decisions:
  - "tier_priority column is INT NOT NULL DEFAULT 0 — existing rows non-breaking after migration."
  - "UNIQUE swap: drop (role, tier) → add (role, tier, tier_priority) atomic in single migration step."
  - "DB row `name` stays provider-specific (`gemini-stt`, `groq-whisper`) per replan — director pick depends on it. env vars are FALLBACK_{1,2} (operator-friendly slot naming)."
  - "circuit_config column ALREADY EXISTS per 0007:14 — migration 0029 uses UPDATE not ADD COLUMN (avoids redundant DDL)."
  - "AI_GATEWAY_MIGRATE_ON_BOOT=true on ai-gateway-dev — container restart applies 0029 automatically; no manual psql needed."

patterns-established:
  - "Multi-tier-1 ordering by tier_priority ASC: Loader.ResolveAllTier1(role) returns stable-sorted slice."

requirements-completed: [D-B5′, D-B6′, D-B11, D-B12, UAT-COLD-START]

duration: 45min (subagent) + 25min (CI cascade fixes by gsd-debugger)
completed: 2026-06-07
---

# Phase 11.2 Plan 03: Migration 0029 + Loader cascade + Primary 3-role restore

**DB schema cascade live: 4 STT upstreams ordered by tier_priority (local-stt|0, gemini-stt|10, groq-whisper|15, openai-whisper|20) + 4 whisper aliases. Primary reconciler back to 3-role tier-0 override.**

## What was built

**SQL migration 0029** — additive 6-step UP block:
1. `ALTER TABLE upstreams ADD COLUMN tier_priority INT NOT NULL DEFAULT 0`
2. UNIQUE constraint swap `(role, tier)` → `(role, tier, tier_priority)` atomic
3. Re-INSERT `local-stt` (tier=0, tier_priority=0) + alias `(whisper, local-stt) → Systran/faster-whisper-large-v3`
4. INSERT `gemini-stt` (tier=1, tier_priority=10) + alias + `circuit_config = '{"cooldown_s":120}'::jsonb`
5. INSERT `groq-whisper` (tier=1, tier_priority=15) + alias
6. UPDATE `openai-whisper` tier_priority=20
DOWN: symmetric DELETE.

**Go code** — UpstreamConfig.TierPriority + Loader.ResolveAllTier1 + reconciler 3-role + lifecycle WHISPER env.

**Live verification (DigitalOcean managed `bd_ai_gateway`):**

```
$ ssh vps-ifix-vm docker logs ai-gateway-dev | grep goose
2026/06/07 14:16:51 OK   0029_readd_whisper_add_gemini_groq.sql (498.3ms)
2026/06/07 14:16:51 goose: successfully migrated database to version: 29

$ gatewayctl upstreams list (role=stt rows):
local-stt        stt  0  true   UPSTREAM_STT_URL              -
gemini-stt       stt  1  true   UPSTREAM_STT_FALLBACK_1_URL   UPSTREAM_STT_FALLBACK_1_AUTH_BEARER
groq-whisper     stt  1  true   UPSTREAM_STT_FALLBACK_2_URL   UPSTREAM_STT_FALLBACK_2_AUTH_BEARER
openai-whisper   stt  1  true   UPSTREAM_STT_OPENAI_URL       UPSTREAM_STT_OPENAI_AUTH_BEARER

$ gatewayctl model-alias list (whisper aliases):
whisper  gemini-stt       stt  gemini-2.5-flash-lite
whisper  groq-whisper     stt  whisper-large-v3
whisper  local-stt        stt  Systran/faster-whisper-large-v3
whisper  openai-whisper   stt  whisper-1
```

All 4 probe `failed` (expected: pod-OFF for local-stt; Plan 06 dispatcher wireup pending for gemini-stt/groq-whisper directors).

## Commits

- 173022a `feat(11.2-03): migration 0029 — cascade STT 3 upstreams + tier_priority + UNIQUE swap`
- 1772bd6 `feat(11.2-03): add UpstreamConfig.TierPriority + Loader.ResolveAllTier1 + tier_priority snapshot read`
- d101d13 `chore(11.2-03): sync querier.go + symbols test after sqlc regen for tier_priority`
- 2c79803 `feat(11.2-03): restore role=stt in primary reconciler/lifecycle (revert 11.1-01)`
- 59f42ea `fix(11.2): gofmt cleanup for Plan 02 + Plan 03 files`
- 7938f4b `fix(11.2-03): update integration test counts post-0029`
- 883c103 `test(11.2): bump migration_0026 + primary_probe assertions for 0029 cascade`
- d12f49d `test(11.2): skip migration_0028 intermediate-state tests pending rework`
- 7d256fe `test(11.2): restore whisper weights + 3-role STT assertions in primary fixtures`

## Integration test cascade (Plan 03 follow-up)

Migration 0029 cascade required test updates across the integration suite:
- 5→8 model_aliases count assertions (migrate_test, migration_0026_test)
- 5→6 upstream count in upstream_e2e (local-stt loads because UPSTREAM_STT_URL is set in fixture; gemini/groq rows skipped because FALLBACK_{1,2}_URL unset in CI)
- migration_0026_test Down(3)→Down(4) + R3 guard count revisions
- migration_0028_test intermediate Up/Down/Roundtrip SKIPPED with TODO note — schema correctness covered by migration_0029_test
- primary_probe_test: 2-role → 3-role STT restore + :33001/health probe assertion flipped
- primary_helpers_test: cfgWithDefaults restored PrimaryWhisperWeights{Key,SHA256} (lifecycle now fails fast without SHA256)

## CI green

Run 27099088086 — unit + integration testcontainers-go + build-gateway all PASS.

## Operator gate D-B6′ (Task 4) — satisfied

Container `ai-gateway-dev` on vps-ifix-vm restarted via Portainer webhook after develop push. `AI_GATEWAY_MIGRATE_ON_BOOT=true` triggered goose Up to version 29. Verified via `gatewayctl upstreams list` + `gatewayctl model-alias list`.
