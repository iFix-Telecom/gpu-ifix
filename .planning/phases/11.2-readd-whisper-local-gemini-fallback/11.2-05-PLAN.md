---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 05
type: execute
wave: 4
depends_on: ["11.2-04"]
files_modified:
  - pod/health-bridge/main.go
  - pod/health-bridge/probes.go
  - pod/health-bridge/probes_test.go
  - pod/health-bridge/main_test.go
  - pod/health-bridge/state.go
  - pod/health-bridge/handlers.go
autonomous: true
requirements: [RES-HEALTHBRIDGE-RESTORE]
threat_refs: []

must_haves:
  truths:
    - "pod/health-bridge tem probeSTT + sttProbeTimeout + generateSilentWAV helpers restored"
    - "main.go probe slice volta a 3 entries {LLM, STT, Embed} (Embed permanece como Phase 06.7 D-03 — não removido)"
    - "state.go UpstreamSTT const restored + NewState seed entry"
    - "handlers.go /health/stt route restored em mux"
    - "probes_test.go TestProbeSTT_Success restored (vide git show 39bec50^:pod/health-bridge/probes_test.go)"
    - "main_test.go testes restauram UpstreamSTT em aggregate fixtures"
  artifacts:
    - path: pod/health-bridge/probes.go
      provides: probeSTT + 1s silent WAV builder
    - path: pod/health-bridge/handlers.go
      provides: /health/stt route
    - path: pod/health-bridge/state.go
      provides: UpstreamSTT const
---

<objective>
Wave 4 — reverter Phase 11.1 Plan 04 (commit 39bec50): restaurar probeSTT no health-bridge para que reconciler health-gate (4 endpoints) consiga discovery do /health/stt da pod. Depende de Plan 04 ter Speaches rodando na :8001 do pod.

Output: 6 arquivos pod/health-bridge revertidos; suite go test ./pod/health-bridge/ verde com TestProbeSTT_Success de volta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-04-SUMMARY.md
@pod/health-bridge/main.go
@pod/health-bridge/probes.go
@pod/health-bridge/state.go
@pod/health-bridge/handlers.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restaurar probeSTT + sttProbeTimeout + helpers em probes.go</name>
  <files>pod/health-bridge/probes.go, pod/health-bridge/probes_test.go</files>
  <read_first>
    - git show 39bec50^:pod/health-bridge/probes.go — código pré-shrink completo (probeSTT + sttProbeTimeout + generateSilentWAV + writeU16LE + writeU32LE)
    - git show 39bec50^:pod/health-bridge/probes_test.go — TestProbeSTT_Success
    - pod/health-bridge/probes.go (estado atual)
  </read_first>
  <behavior>
    - TestProbeSTT_Success: monta httptest.Server que retorna `{"text": "..."}` 200 OK em POST multipart → probeSTT retorna ProbeResult{Healthy: true}
    - TestProbeSTT_Timeout: server delay > sttProbeTimeout → ProbeResult{Healthy: false}
    - TestProbeSTT_5xx: server retorna 503 → Healthy false
  </behavior>
  <action>
    1. Extract texto via `git show 39bec50^:pod/health-bridge/probes.go > /tmp/pre.probes.go`. Apply diff invertido: restaurar probeSTT, sttProbeTimeout (= 10*time.Second), generateSilentWAV, writeU16LE, writeU32LE helpers.
    2. Re-adicionar imports `os` (env SPEACHES_MODEL) + `mime/multipart` (form builder).
    3. probeSTT default model = "Systran/faster-whisper-large-v3" se SPEACHES_MODEL env unset.
    4. Restaurar probes_test.go: TestProbeSTT_Success + reverter `TestState_ConcurrentSet` rotation de 2-item back to 3-item + TestAggregateStatus restaurar UpstreamSTT.
  </action>
  <verify>
    <automated>go test ./pod/health-bridge/ -count=1 -run "TestProbeSTT|TestState_ConcurrentSet|TestAggregateStatus"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "func probeSTT" pod/health-bridge/probes.go == 1
    - grep -c "sttProbeTimeout" pod/health-bridge/probes.go ≥ 2
    - grep -c "generateSilentWAV" pod/health-bridge/probes.go ≥ 2
    - grep -c "TestProbeSTT_Success" pod/health-bridge/probes_test.go == 1
  </acceptance_criteria>
  <done>probeSTT funcional; tests verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restaurar UpstreamSTT + /health/stt route + SpeachesURL config field</name>
  <files>pod/health-bridge/main.go, pod/health-bridge/state.go, pod/health-bridge/handlers.go, pod/health-bridge/main_test.go</files>
  <read_first>
    - git show 39bec50^:pod/health-bridge/main.go — Config.SpeachesURL field + loadConfig + probe slice registration
    - git show 39bec50^:pod/health-bridge/state.go — UpstreamSTT const + NewState seed
    - git show 39bec50^:pod/health-bridge/handlers.go — /health/stt mux route
  </read_first>
  <behavior>
    - TestLoadConfig_HasSpeachesURL: env SPEACHES_URL setado → cfg.SpeachesURL == valor
    - TestHandleAggregate_AllHealthy_200: 3 upstreams {LLM, STT, Embed} all healthy → 200
    - TestHandleAggregate_OneFailed_503: qualquer um failed → 503
    - TestHealthReady_IsAggregate: same shape com 3 upstreams
  </behavior>
  <action>
    1. **main.go**: restaurar `SpeachesURL string` em Config struct; restaurar atribuição em loadConfig (env SPEACHES_URL); restaurar `"speaches"` key no startup-log; restaurar `{UpstreamSTT, cfg.SpeachesURL, probeSTT}` na probe slice (entre LLM e Embed).
    2. **state.go**: restaurar `UpstreamSTT Upstream = "stt"` const + NewState seed entry para UpstreamSTT. Atualizar doc comment para citar Phase 11.2 D-B5 reverso de SEED-010 Mudança 7.
    3. **handlers.go**: restaurar `mux.HandleFunc("/health/stt", ...)` route; atualizar godoc do mux com 6 routes: /health /health/live /health/ready /health/llm /health/stt /health/embed.
    4. **main_test.go**: restaurar UpstreamSTT em TestHandleAggregate_AllHealthy_200, TestHandleAggregate_OneFailed_503 (failed-bucket volta para UpstreamSTT), TestHealthReady_IsAggregate.
  </action>
  <verify>
    <automated>go build ./pod/health-bridge/... && go test ./pod/health-bridge/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "UpstreamSTT" pod/health-bridge/state.go ≥ 2
    - grep -c "SpeachesURL" pod/health-bridge/main.go ≥ 2
    - grep -c "/health/stt" pod/health-bridge/handlers.go == 1
    - go test ./pod/health-bridge/ verde
  </acceptance_criteria>
  <done>Health-bridge volta a 3-probe shape; /health/stt responde.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none new) | — | health-bridge restore | accept | Adição reversiva; probeSTT path foi UAT-validado Phase 06.8 |
</threat_model>

<verification>
go build ./pod/health-bridge/...
go test ./pod/health-bridge/ -count=1
</verification>

<success_criteria>
- 6 arquivos pod/health-bridge revertidos
- Suite go test ./pod/health-bridge verde
- TestProbeSTT_Success restored e passa
</success_criteria>

<output>
Create `.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-05-SUMMARY.md` when done.
</output>
