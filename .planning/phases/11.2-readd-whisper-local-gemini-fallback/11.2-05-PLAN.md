---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 05
type: execute
wave: 3
depends_on: [11.2-01]
files_modified:
  - pod/health-bridge/probes.go
  - pod/health-bridge/handlers.go
  - pod/health-bridge/state.go
  - pod/health-bridge/main.go
  - pod/health-bridge/probes_test.go
autonomous: true
requirements: [D-B2]
threat_refs: [T-11.2-HB]

must_haves:
  truths:
    - "Pod health-bridge expõe /health/stt que posta WAV 1s para speaches:8001/v1/audio/transcriptions e retorna 200 quando OK"
    - "UpstreamSTT const + sttProbeTimeout 10s + probeSTT function restaurados verbatim de 39bec50^"
  artifacts:
    - path: pod/health-bridge/probes.go
      provides: "probeSTT function + sttProbeTimeout const (10s)"
    - path: pod/health-bridge/handlers.go
      provides: "/health/stt HTTP route handler"
    - path: pod/health-bridge/state.go
      provides: "UpstreamSTT const + STT slot in state machine"
  key_links:
    - from: pod health-bridge probe loop
      to: speaches:8001/v1/audio/transcriptions
      via: 1s silent 16kHz mono WAV multipart POST
      pattern: "audio/wav"
---

<objective>
Wave 3b (parallel com Plan 04) — Restaurar 4 arquivos health-bridge (probes/handlers/state/main) verbatim de commit 39bec50^ para reativar probe STT do pod. Plan 01 já restaurou probes_test.go com TestProbeSTT skip; aqui implementa o test também.

Purpose: Sem probeSTT loop, primary reconciler nunca confirma readiness do speaches → nunca markReady role=stt → tier-0 fica perpetually fall-through para tier-1.
Output: 4 health-bridge files restored + test unskipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-PATTERNS.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore probes.go + handlers.go + state.go + main.go (verbatim 39bec50^)</name>
  <files>pod/health-bridge/probes.go, pod/health-bridge/handlers.go, pod/health-bridge/state.go, pod/health-bridge/main.go</files>
  <read_first>
    - PATTERNS.md rows pod/health-bridge/* (source git show 39bec50^)
    - 11.2-RESEARCH.md Health-bridge restoration section
    - Current files via cat (estado pós-11.1-04 que removeu STT)
  </read_first>
  <action>
    Para cada arquivo, executar restoration cirúrgica:

    probes.go: extrair probeSTT function (linhas ~22-180 do commit pre-39bec50) + const sttProbeTimeout = 10 * time.Second + 1s silent 16kHz mono WAV body builder. Comando: git show 39bec50^:pod/health-bridge/probes.go > /tmp/probes-restore.go; diff vs current; splice probeSTT + const sem perder probes LLM/TTS/DCGM atualizados.

    handlers.go: restaurar /health/stt route handler de git show 39bec50^:pod/health-bridge/handlers.go. Merge com handlers atuais para LLM/TTS/DCGM.

    state.go: restaurar UpstreamSTT const + STT slot no state machine de git show 39bec50^:pod/health-bridge/state.go.

    main.go: restaurar probeSTT registration na probe loop (chamada periódica) de git show 39bec50^:pod/health-bridge/main.go.

    Em todos os 4: PRESERVAR fixes pós-11.1 (Phase 06.7 TTS + Phase 06.8 DCGM probes) que não existiam em 39bec50^.
  </action>
  <verify>
    <automated>cd pod/health-bridge && go vet ./... && go build ./... && grep -q probeSTT probes.go && grep -q UpstreamSTT state.go && grep -q /health/stt handlers.go && grep -q sttProbeTimeout probes.go</automated>
  </verify>
  <done>
    4 arquivos compilam; probeSTT/UpstreamSTT/sttProbeTimeout/handlers /health/stt todos presentes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar TestProbeSTT (unskip Plan 01 stub)</name>
  <files>pod/health-bridge/probes_test.go</files>
  <read_first>
    - PATTERNS.md row pod/health-bridge/probes_test.go (source git show 39bec50^)
    - pod/health-bridge/probes.go (atual, post-Task-1)
  </read_first>
  <behavior>
    - TestProbeSTT mounts httptest.Server retornando 200 + JSON {"text":"hello"}
    - probeSTT chama POST /v1/audio/transcriptions com multipart contendo file part audio/wav
    - Retorna ProbeResult com Status OK e latency observada
    - Header Content-Type contém multipart/form-data
    - 4xx/5xx respondem ProbeResult Status Fail
  </behavior>
  <action>
    Extrair TestProbeSTT verbatim de git show 39bec50^:pod/health-bridge/probes_test.go. Remover t.Skip do Plan 01 stub. Asserções: ProbeResult.Status, multipart boundary parsing no fake server, latency capturada.
  </action>
  <verify>
    <automated>cd pod/health-bridge && go test -count=1 -run TestProbeSTT</automated>
  </verify>
  <done>
    TestProbeSTT PASS (não SKIP); cobertura cobre 200 + 4xx + timeout paths.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-HB | Denial of Service | Health bridge probe loop | mitigate | sttProbeTimeout 10s (audio decode latency); probe failure não bloqueia outros probes (per-role isolation já no main.go pattern) |
</threat_model>

<verification>
- go build em pod/health-bridge/ OK
- TestProbeSTT PASS
- 4 arquivos restored; LLM/TTS/DCGM probes preservados (não regredidos)
</verification>

<success_criteria>
Wave 3b done; pod cold-start scenario 1 do UAT pode confirmar /health/stt 200 antes de reconciler.markReady.
</success_criteria>

<output>
Create .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-05-SUMMARY.md when done
</output>
