---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 03
type: execute
wave: 2
depends_on: [11.2-02]
files_modified:
  - gateway/db/migrations/0029_readd_whisper_add_gemini_groq.sql
  - gateway/internal/integration_test/migration_0029_test.go
  - gateway/internal/upstreams/types.go
  - gateway/internal/upstreams/loader.go
  - gateway/internal/upstreams/loader_test.go
  - gateway/internal/primary/reconciler.go
  - gateway/internal/primary/reconciler_test.go
  - gateway/internal/primary/lifecycle.go
  - gateway/internal/primary/lifecycle_test.go
autonomous: true
requirements: [D-B5′, D-B6′, D-B11, D-B12, UAT-COLD-START]
threat_refs: [T-11.2-MIG, T-11.2-RT]

must_haves:
  truths:
    - "Migration 0029 aplicada limpa em DB integration (upstreams cascade gemini-stt + groq-whisper + local-stt presentes)"
    - "UpstreamConfig.TierPriority int parseado do snapshot"
    - "Loader.ResolveAllTier1(role) retorna candidatos ordered ASC por tier_priority"
    - "CircuitConfig.CooldownS parseado para gemini-stt = 120 (D-B11/D-B12)"
    - "Reconciler restaurado role=stt em startDrain/markReady/closeLifecycle"
    - "Lifecycle restaurado STT field + -p 8001:8001 + WHISPER env injection"
  artifacts:
    - path: gateway/db/migrations/0029_readd_whisper_add_gemini_groq.sql
      provides: "Schema cascade STT 3 upstreams + tier_priority col + UNIQUE swap"
    - path: gateway/internal/upstreams/loader.go
      provides: "ResolveAllTier1 helper + tier_priority read into snapshot"
  key_links:
    - from: dispatcher.go (Plan 06)
      to: Loader.ResolveAllTier1
      via: method call signature `func(role string) []UpstreamConfig`
      pattern: "ResolveAllTier1\\(cfg.Role\\)"
    - from: gateway boot AI_GATEWAY_MIGRATE_ON_BOOT
      to: migration 0029
      via: goose driver
      pattern: "0029_readd_whisper"
---

<objective>
Wave 2 — Aplicar migration DB 0029 (additive schema cascade) + restaurar role=stt no primary reconciler (revert 11.1-01 trio call sites) + restaurar STT field + porta 8001 + WHISPER env em lifecycle. Bloqueante: rodar `migrate up` para aplicar o schema realmente.

Purpose: Sem schema cascade no DB e sem reconciler chamando OverrideTier0("stt"), pod-ON local-stt nunca roteia.
Output: 1 nova migration SQL + tests integration + 2 files Go restored + tests unit + DB schema actualmente aplicada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-CONTEXT.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-PATTERNS.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
@gateway/db/migrations/0028_remove_local_stt_upstream.sql
@gateway/db/migrations/0007_create_upstreams.sql
@gateway/internal/upstreams/types.go
@gateway/internal/upstreams/loader.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0029 — cascade STT 3 upstreams + tier_priority col + UNIQUE swap</name>
  <files>gateway/db/migrations/0029_readd_whisper_add_gemini_groq.sql, gateway/internal/integration_test/migration_0029_test.go</files>
  <read_first>
    - PATTERNS.md row migration 0029 (verbatim source map; circuit_config column ALREADY EXISTS per 0007:14)
    - 11.2-RESEARCH.md §Code Examples linhas 587-661 (full UP/DOWN draft)
    - gateway/db/migrations/0028_remove_local_stt_upstream.sql (DOWN block linhas 64-70 = revert template para local-stt)
    - gateway/db/migrations/0007_create_upstreams.sql linha 23 (UNIQUE origin) + linha 14 (circuit_config JSONB already)
    - PATTERNS.md linhas 130-156 (6-step Up/Down draft)
  </read_first>
  <action>
    Criar `gateway/db/migrations/0029_readd_whisper_add_gemini_groq.sql` com header goose + statementBegin/End, `SET search_path = ai_gateway, public;`, e 6 passos UP:

    1. `ALTER TABLE upstreams ADD COLUMN IF NOT EXISTS tier_priority INT NOT NULL DEFAULT 0`
    2. `ALTER TABLE upstreams DROP CONSTRAINT IF EXISTS upstreams_role_tier_key` + `ADD CONSTRAINT upstreams_role_tier_priority_key UNIQUE (role, tier, tier_priority)`
    3. Re-INSERT `local-stt` (tier=0, tier_priority=0) + alias `(whisper, local-stt) → Systran/faster-whisper-large-v3` (ambos ON CONFLICT DO NOTHING — verbatim de 0028 DOWN lines 64-70)
    4. INSERT `gemini-stt` (tier=1, tier_priority=10, url_env=`UPSTREAM_STT_GEMINI_URL`, auth_bearer_env=`UPSTREAM_STT_GEMINI_AUTH_BEARER`) + alias `(whisper, gemini-stt) → gemini-2.5-flash-lite` + `UPDATE upstreams SET circuit_config = '{"cooldown_s":120}'::jsonb WHERE name='gemini-stt'` (D-B11; NÃO ADD COLUMN — já existe per 0007:14)
    5. INSERT `groq-whisper` (tier=1, tier_priority=15, url_env=`UPSTREAM_STT_GROQ_URL`, auth_bearer_env=`UPSTREAM_STT_GROQ_AUTH_BEARER`) + alias `(whisper, groq-whisper) → whisper-large-v3`
    6. `UPDATE upstreams SET tier_priority = 20 WHERE name = 'openai-whisper'`
    + COMMENT ON COLUMN tier_priority

    DOWN: symmetric DELETE de aliases (gemini-stt, groq-whisper, local-stt) + DELETE upstreams (gemini-stt, groq-whisper, local-stt) + `UPDATE openai-whisper tier_priority=0`. Manter coluna `tier_priority` + UNIQUE swap (additive).

    Implementar todos os 6 testes integration em `migration_0029_test.go` (remover skips do Plan 01) — usar `freshSchema` helper já presente. Cada teste assert linhas/colunas/aliases presentes ou ausentes conforme nome.
  </action>
  <verify>
    <automated>cd gateway && go test -tags=integration -count=1 ./internal/integration_test/... -run TestIntegration_Migration0029</automated>
  </verify>
  <done>
    - Arquivo 0029_readd_whisper_add_gemini_groq.sql existe + goose headers válidos
    - 6 testes integration PASS (não SKIP)
    - `psql ai_gateway -c "SELECT name, tier, tier_priority FROM upstreams WHERE role='stt' ORDER BY tier_priority"` (após migrate up no Task 4) retorna: local-stt|0|0, gemini-stt|1|10, groq-whisper|1|15, openai-whisper|1|20
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: types.go add TierPriority + loader.go ResolveAllTier1 + snapshot query read tier_priority</name>
  <files>gateway/internal/upstreams/types.go, gateway/internal/upstreams/loader.go, gateway/internal/upstreams/loader_test.go</files>
  <read_first>
    - gateway/internal/upstreams/types.go linhas 24-42 (UpstreamConfig struct)
    - gateway/internal/upstreams/types.go linhas 63-78 + 97 (CircuitConfig + parser — D-B12 wired confirm)
    - gateway/internal/upstreams/loader.go linha 205 (Resolve method analog)
    - PATTERNS.md linhas 213-247 (ResolveAllTier1 pattern)
    - 11.2-RESEARCH.md §Pattern 3 (helper code)
  </read_first>
  <behavior>
    - `UpstreamConfig.TierPriority int json:"tier_priority"` field added após `Tier int`
    - Snapshot SELECT query inclui `tier_priority` coluna
    - `Loader.ResolveAllTier1(role string) []UpstreamConfig` retorna tier-1 enabled rows ordered ASC por TierPriority (stable sort)
    - Roles com 1 tier-1 (llm/embed/tts): slice len=1 → backward compat
    - Roles com 3 tier-1 (stt após 0029): slice len=3 ordered [gemini-stt, groq-whisper, openai-whisper]
    - CircuitConfig.CooldownS = 120 para gemini-stt (parsed via existing parser; verify gap)
  </behavior>
  <action>
    1. `types.go:24` — adicionar `TierPriority int \`json:"tier_priority"\`` após `Tier int`.

    2. `loader.go` — adicionar método `ResolveAllTier1(role string) []UpstreamConfig` (verbatim conforme PATTERNS linhas 230-244) + import `sort` se ausente. Atualizar SELECT query do snapshot (procurar pelo SQL do Loader que carrega upstreams; pode estar em sqlc-generated ou inline) para incluir `tier_priority` coluna.

    3. `loader_test.go` — unskip + implementar `TestPostMigration0029_RestoresLocalSTT_AddsGeminiSTT_AddsGroqWhisper`, `TestResolveAllTier1_OrderByTierPriority_ASC` (asserta `[gemini-stt(10), groq-whisper(15), openai-whisper(20)]`), `TestCircuitConfigParsed_GeminiCooldownS120` (asserta `cfg.CircuitConfig.CooldownS == 120` para gemini-stt row).
  </action>
  <verify>
    <automated>cd gateway && go test -count=1 ./internal/upstreams/... -run 'TestResolveAllTier1|TestPostMigration0029|TestCircuitConfigParsed'</automated>
  </verify>
  <done>
    - 3 tests PASS (não SKIP)
    - `grep -n 'TierPriority' gateway/internal/upstreams/types.go` retorna ≥1 linha
    - `grep -n 'ResolveAllTier1' gateway/internal/upstreams/loader.go` retorna 1 linha (method def)
  </done>
</task>

<task type="auto">
  <name>Task 3: Restore role=stt no primary reconciler trio + STT field/port/env em lifecycle</name>
  <files>gateway/internal/primary/reconciler.go, gateway/internal/primary/reconciler_test.go, gateway/internal/primary/lifecycle.go, gateway/internal/primary/lifecycle_test.go</files>
  <read_first>
    - PATTERNS.md row reconciler.go (`git log -S 'OverrideTier0("stt"' -- gateway/internal/primary/reconciler.go` para SHA)
    - PATTERNS.md row lifecycle.go (linhas :104/:322/:341 restore)
    - 11.2-RESEARCH.md §Code Examples linhas 666-700
    - reconciler.go current code at :527 (startDrain), :571 (markReady), :636 (closeLifecycle)
    - lifecycle.go current code at :104 (primaryPodURLs struct), :322 (port mapping), :341 (env injection)
  </read_first>
  <action>
    1. `reconciler.go` — restaurar 3 call sites verbatim de `git show <11.1-01-commit>^:gateway/internal/primary/reconciler.go`:
       - linha ~527 (startDrain): adicionar `r.deps.Loader.RestoreTier0("stt")` entre LLM e TTS RestoreTier0 calls
       - linha ~571 (markReady): adicionar `r.deps.Loader.OverrideTier0("stt", stripPrimaryReadinessSuffix(urls.STT))` entre LLM e TTS Override calls
       - linha ~636 (closeLifecycle): adicionar `r.deps.Loader.RestoreTier0("stt")` entre LLM e TTS

    2. `lifecycle.go`:
       - linha ~104: `primaryPodURLs` struct adicionar `STT string` field entre LLM e TTS
       - linha ~322: provision args adicionar `"-p 8001:8001": "1"` entre :8000 e :8003
       - linha ~341: provision env adicionar `"PRIMARY_WHISPER_WEIGHTS_KEY": cfg.PrimaryWhisperWeightsKey` + `"PRIMARY_WHISPER_WEIGHTS_SHA256": cfg.PrimaryWhisperWeightsSHA256` + `"PRIMARY_SPEACHES_IMAGE": cfg.PrimarySpeachesImage`

    3. `reconciler_test.go` + `lifecycle_test.go` — unskip stubs Plan 01 Task 2; implementar asserções (3 reconciler tests + 3 lifecycle tests). Tests usam fake Loader que tracka chamadas OverrideTier0/RestoreTier0 com role param.
  </action>
  <verify>
    <automated>cd gateway && go test -count=1 ./internal/primary/... -run 'RestoreTier0_CalledFor_STT|OverrideTier0_CalledFor_STT|HasSTTField|Port8001|PRIMARY_WHISPER_WEIGHTS'</automated>
  </verify>
  <done>
    - 6 tests PASS
    - `grep -c 'OverrideTier0("stt"' gateway/internal/primary/reconciler.go` == 1
    - `grep -c 'RestoreTier0("stt")' gateway/internal/primary/reconciler.go` == 2
    - `grep -c '8001:8001' gateway/internal/primary/lifecycle.go` ≥ 1
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: BLOCKING — apply migration 0029 to DB (goose up via gateway/cmd/migrate ou AI_GATEWAY_MIGRATE_ON_BOOT)</name>
  <read_first>
    - 11.2-CONTEXT.md (planning_context — schema push BLOCKING)
    - Phase 06.9-01-SUMMARY ou equivalente para convention de migrate up
  </read_first>
  <what-built>Migration 0029 SQL committed + integration tests green. Schema ainda NÃO aplicado em DB live.</what-built>
  <how-to-verify>
    Run em vps-ifix-vm (ai-gateway-dev):
    1. `ssh vps-ifix-vm 'docker exec ifix-ai-gateway /gateway-migrate up'` OU equivalente do projeto (verificar gateway/cmd/migrate/ convention; se boot-time via AI_GATEWAY_MIGRATE_ON_BOOT=true, restart container: `docker compose -f /opt/ai-gateway-dev/docker-compose.yml restart gateway`)
    2. Verificar versão aplicada: `ssh vps-ifix-vm 'docker exec ifix-ai-gateway-postgres psql -U gateway -d ai_gateway -c "SELECT version_id FROM goose_db_version ORDER BY id DESC LIMIT 5;"'` deve mostrar 29 no topo
    3. Verificar cascade: `... psql -c "SELECT name, role, tier, tier_priority, circuit_config FROM ai_gateway.upstreams WHERE role='stt' ORDER BY tier_priority;"` retorna 4 linhas (local-stt|stt|0|0, gemini-stt|stt|1|10|{cooldown_s:120}, groq-whisper|stt|1|15, openai-whisper|stt|1|20)
    4. Verificar aliases: `... psql -c "SELECT alias, upstream_name, target FROM ai_gateway.model_aliases WHERE alias='whisper' ORDER BY upstream_name;"` retorna 4 linhas (whisper|gemini-stt|gemini-2.5-flash-lite, whisper|groq-whisper|whisper-large-v3, whisper|local-stt|Systran/faster-whisper-large-v3, whisper|openai-whisper|whisper-1)
  </how-to-verify>
  <acceptance_criteria>
    - goose_db_version contém 29
    - 4 STT upstream rows ordered correctly
    - 4 whisper aliases presentes
    - DOWN test: aplicar `goose down` + `goose up` end-state idêntico (executado em ambiente integration, não live)
  </acceptance_criteria>
  <resume-signal>Type "migrated" or describe failure</resume-signal>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-MIG | Tampering | DB schema | mitigate | UNIQUE swap atomic; ON CONFLICT DO NOTHING garante idempotência; DOWN block symmetric verificado via Roundtrip test |
| T-11.2-RT | Denial of Service | Loader snapshot read | mitigate | tier_priority é INT NOT NULL DEFAULT 0; existing rows não quebram após migration |
</threat_model>

<verification>
- migration_0029_test.go suite verde (6 tests)
- Schema cascade verificável via psql na vps-ifix-vm
- 6 unit tests reconciler/lifecycle PASS
</verification>

<success_criteria>
DB tem 4 STT upstreams + 4 whisper aliases; Loader.ResolveAllTier1("stt") em snapshot live retorna 3 candidates ordered; primary reconciler chama Override/Restore para role stt.
</success_criteria>

<output>
Create `.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-03-SUMMARY.md` when done
</output>
