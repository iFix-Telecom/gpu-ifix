---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 03
type: execute
wave: 3
depends_on: ["11.2-01", "11.2-02"]
files_modified:
  - gateway/internal/primary/lifecycle.go
  - gateway/internal/primary/reconciler.go
  - gateway/internal/primary/reconciler_test.go
  - gateway/internal/primary/lifecycle_test.go
  - gateway/internal/integration_test/primary_helpers_test.go
autonomous: true
requirements: [D-B5, D-B6, RES-RECON-RESTORE]
threat_refs: []

must_haves:
  truths:
    - "primaryPodURLs struct ganha STT string novamente (revert 11.1-01 Task 1)"
    - "reconciler.markReady chama OverrideTier0(\"stt\", urls.STT) — 3 roles {llm,stt,tts}"
    - "reconciler.drainPrimary + closeLifecycle chamam RestoreTier0(\"stt\") — 3 roles"
    - "reconciler.evaluateReady re-assert loop opera em {llm,stt,tts} (recoverOpenLifecycle similar)"
    - "waitForReadyOrDestroy + recoverOpenLifecycle health checks: 4 endpoints (llm + stt + tts + dcgm)"
    - "buildPodURLs constrói STT field via roleURL(*urls, \"stt\")"
    - "lifecycle.go env map injeta PRIMARY_WHISPER_WEIGHTS_KEY + PRIMARY_WHISPER_WEIGHTS_SHA256 + PRIMARY_SPEACHES_IMAGE no pod env"
    - "Porta -p 8001:8001 já presente no env map permanece (não foi removida por 11.1) — apenas confirmação"
  artifacts:
    - path: gateway/internal/primary/reconciler.go
      provides: "3-role reconciler trio restored"
    - path: gateway/internal/primary/lifecycle.go
      provides: "STT field + PRIMARY_WHISPER_WEIGHTS_* + PRIMARY_SPEACHES_IMAGE injection"
---

<objective>
Wave 3 (lado gateway) — restaurar role=stt ao primary reconciler trio: revert cirúrgico das mudanças do Plan 11.1-01 Task 1. Adicionar STT ao struct primaryPodURLs; restaurar OverrideTier0("stt", urls.STT) em markReady + RestoreTier0("stt") em drain/closeLifecycle; restaurar drain-gate sum local-stt; restaurar 4-endpoint health gate; restaurar injeção de PRIMARY_WHISPER_WEIGHTS_* + PRIMARY_SPEACHES_IMAGE no env do pod.

Plan 04 (paralelo, mesma wave) reverte pod image side. Plan 03 mexe APENAS em arquivos gateway.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-CONTEXT.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-01-SUMMARY.md
@gateway/internal/primary/reconciler.go
@gateway/internal/primary/lifecycle.go

<interfaces>
<!-- Revert anchor commit: 2449461 — strip role=stt from primary reconciler + lifecycle -->
<!-- Pré-shrink shape: `git show 2449461^:gateway/internal/primary/lifecycle.go` -->
<!-- Pré-shrink shape: `git show 2449461^:gateway/internal/primary/reconciler.go` -->
<!-- Lifecycle.go ATUAL ainda tem `-p 8001:8001` em env map (linha 322) — confirmar não-tocado -->
<!-- Recente: fbe2129 dropou PRIMARY_WHISPER_WEIGHTS_* env injection — Task 1 deve restaurar isso ALÉM do 2449461 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restaurar primaryPodURLs.STT + injetar PRIMARY_WHISPER_WEIGHTS_* + PRIMARY_SPEACHES_IMAGE em lifecycle.go</name>
  <files>gateway/internal/primary/lifecycle.go, gateway/internal/primary/lifecycle_test.go</files>
  <read_first>
    - `git show 2449461^:gateway/internal/primary/lifecycle.go` (shape pré-11.1 do struct + env injection)
    - `git show fbe2129^:gateway/internal/primary/lifecycle.go` (shape antes do drop dos WHISPER env vars)
    - `gateway/internal/primary/lifecycle.go:104-110` (struct atual sem STT)
    - `gateway/internal/primary/lifecycle.go:315-355` (env map atual; `-p 8001:8001` JÁ presente)
  </read_first>
  <behavior>
    - TestPrimaryPodURLs_HasSTTField: reflection-based — struct tem campo STT string
    - TestBuildPrimaryProvisionEnv_InjectsWhisperWeights: dado Config com PrimaryWhisperWeightsKey="weights/whisper.tar.gz" + SHA256, env retornado contém ambos
    - TestBuildPrimaryProvisionEnv_InjectsSpeachesImage: env contém PRIMARY_SPEACHES_IMAGE com valor de cfg.PrimarySpeachesImage
    - TestBuildPrimaryProvisionEnv_Has4PortForwards: env keys contêm "-p 8000:8000", "-p 8001:8001", "-p 8003:8003", "-p 9400:9400" (já presente — assert preservation)
  </behavior>
  <action>
    1. Em struct `primaryPodURLs` (~linha 104), adicionar campo `STT string` após LLM. Atualizar comentário godoc removendo a nota "Phase 06.7 (D-03/D-11): ... {llm,stt,tts}" para refletir que STT volta como dynamic role.
    2. Em `buildPrimaryProvisionEnv` (~linha 315-355), no env map adicionar (após PRIMARY_QWEN_WEIGHTS_SHA256):
       ```go
       "PRIMARY_WHISPER_WEIGHTS_KEY":    cfg.PrimaryWhisperWeightsKey,
       "PRIMARY_WHISPER_WEIGHTS_SHA256": cfg.PrimaryWhisperWeightsSHA256,
       ```
       E (no init do env map, perto de outros image envs caso exista, OU adicionar novo line):
       ```go
       "PRIMARY_SPEACHES_IMAGE": cfg.PrimarySpeachesImage,
       ```
    3. APAGAR comentário "Phase 11.1 D-A4: PRIMARY_WHISPER_WEIGHTS_* removed (STT shrunk to tier-1-only)" linha 341.
    4. APAGAR comentário "Phase 11.1 D-A4: ErrMissingWhisperSHA removed" linha 86.
    5. Confirmar `-p 8001:8001` permanece intacto (Plan 11.1 nunca removeu — verificar por grep).
    6. Atualizar `lifecycle_test.go` — adicionar 4 tests acima + ajustar fixtures que constroem Config para incluir `PrimarySpeachesImage` e `PrimaryWhisperWeights{Key,SHA256}` (espelhar fixtures de PrimaryBGEM3*).
  </action>
  <verify>
    <automated>go test ./gateway/internal/primary/ -count=1 -run "TestPrimaryPodURLs_HasSTTField|TestBuildPrimaryProvisionEnv"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'STT  string' gateway/internal/primary/lifecycle.go` ≥ 1
    - `grep -c 'PRIMARY_WHISPER_WEIGHTS_KEY' gateway/internal/primary/lifecycle.go` == 1
    - `grep -c 'PRIMARY_SPEACHES_IMAGE' gateway/internal/primary/lifecycle.go` == 1
    - `grep -c '"-p 8001:8001"' gateway/internal/primary/lifecycle.go` == 1
    - `grep -c "Phase 11.1 D-A4" gateway/internal/primary/lifecycle.go` == 0
  </acceptance_criteria>
  <done>Build verde; tests novos passam; pod recebe weight env + speaches image SHA na próxima provisionada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restaurar OverrideTier0/RestoreTier0("stt") em reconciler.go + drain-gate + 4-endpoint health</name>
  <files>gateway/internal/primary/reconciler.go, gateway/internal/primary/reconciler_test.go</files>
  <read_first>
    - `git show 2449461^:gateway/internal/primary/reconciler.go` — todos call sites pré-11.1
    - `gateway/internal/primary/reconciler.go:527,571,636,1147` (markReady, drainPrimary, closeLifecycle, recoverOpenLifecycle — 4 sites onde STT precisa voltar)
    - RESEARCH "Code Examples — Primary reconciler" (linhas ~666-700)
  </read_first>
  <behavior>
    - TestMarkReady_RestoresAllThreeRoles: markReady chama OverrideTier0 3x (llm, stt, tts) com strip suffix correto
    - TestDrain_RestoreTier0CalledForAllThreeRoles: drainPrimary chama RestoreTier0 3x
    - TestCloseLifecycle_RestoreTier0For3Roles: closeLifecycle chama RestoreTier0 3x
    - TestEvaluateReady_ReassertsTier0ForSTT: re-assert loop inclui stt (espelha existing llm+tts cases)
    - TestEvaluateProvisioning_AllFourEndpointsHealthy_PromotesToReady: 4 endpoints (llm + stt + tts + dcgm) — STT readicionado ao gate
    - TestRoleURL_HandlesSTTCase: roleURL(urls, "stt") retorna urls.STT (cobertura do switch)
    - TestEvaluateDraining_DrainGateIncludesLocalSTT: drain-gate sum inclui local-stt term (revert 11.1-01)
    - TestBuildPodURLs_AssignsSTT: buildPodURLs preenche STT via podSTTURL/podPortURL
  </behavior>
  <action>
    Aplicar revert cirúrgico em 7 locations (referência RESEARCH Code Examples primary reconciler block):

    1. ~linha 411 (re-assert loop `for _, role := range [...]string{...}`): adicionar `"stt"` à lista de roles.
    2. ~linha 522-527 (drainPrimary): adicionar `r.deps.Loader.RestoreTier0("stt")`.
    3. ~linha 571 (markReady): adicionar `r.deps.Loader.OverrideTier0("stt", stripPrimaryReadinessSuffix(urls.STT))` ENTRE llm e tts.
    4. ~linha 636 (closeLifecycle): adicionar `r.deps.Loader.RestoreTier0("stt")`.
    5. ~linha 1053 (buildPodURLs): adicionar `STT: r.podSTTURL(inst),`.
    6. ~linha 1147 (recoverOpenLifecycle markReady-equivalent): adicionar `r.deps.Loader.OverrideTier0("stt", stripPrimaryReadinessSuffix(urls.STT))`.
    7. roleURL func + evaluateDraining drain-gate sum: restaurar "stt" case + local-stt term (revert dos asserts NotContains "stt" da 11.1).
    8. waitForReadyOrDestroy health checks: voltar de 3→4 endpoints (incluir STT URL com `/health`).
    9. Comentário godoc "{llm,stt,tts}" — restaurar onde 11.1 dropou para 2-role.
    10. APAGAR comentário "(11.1 D-A4: local-stt term removed)" linha 68.
    11. reconciler_test.go: ajustar fixtures: `TestMarkReady_OverridesTier0_2Roles` → renomear/expandir para `TestMarkReady_RestoresAllThreeRoles`; restaurar `TestEvaluateReady_ReassertsTier0ForSTT`; flip todos NotContains("stt") → Contains("stt"); 8 fakeVast mocks revisar se urls.STT é nil-safe.
  </action>
  <verify>
    <automated>go test ./gateway/internal/primary/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'OverrideTier0\("stt"' gateway/internal/primary/reconciler.go` == 2 (markReady + recoverOpenLifecycle)
    - `grep -cE 'RestoreTier0\("stt"' gateway/internal/primary/reconciler.go` ≥ 2 (drain + closeLifecycle)
    - `grep -c '"stt"' gateway/internal/primary/reconciler.go` ≥ 5
    - `grep -c "STT:" gateway/internal/primary/reconciler.go` ≥ 1 (buildPodURLs)
    - `grep -c "Phase 11.1 D-A4" gateway/internal/primary/reconciler.go` == 0
  </acceptance_criteria>
  <done>Reconciler trio volta a 3 roles; todos call sites restored; tests verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restaurar PrimarySpeachesImage + WhisperWeights em primary_helpers_test.go (integration_test fixture)</name>
  <files>gateway/internal/integration_test/primary_helpers_test.go</files>
  <read_first>
    - `gateway/internal/integration_test/primary_helpers_test.go` (estado atual com Phase 11.1 D-A4 comments)
    - `git show 88bea84^:gateway/internal/integration_test/primary_helpers_test.go` (shape pré-drop)
  </read_first>
  <behavior>
    - go test -tags integration build do pacote primary_helpers compila (sem usar Config fields que não existem).
    - Fixture provê PrimarySpeachesImage + PrimaryWhisperWeights{Key,SHA256} para downstream tests.
  </behavior>
  <action>
    1. Adicionar (em buildTestConfig OU equivalente):
       ```go
       cfg.PrimarySpeachesImage         = "ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3"
       cfg.PrimaryWhisperWeightsKey     = "weights/Systran-faster-whisper-large-v3.tar.gz"
       cfg.PrimaryWhisperWeightsSHA256  = strings.Repeat("a", 64)  // dummy hex p/ tests
       ```
    2. Apagar comentários "Phase 11.1" mencionando os fields removidos.
  </action>
  <verify>
    <automated>go test -tags integration -run TestIntegration_Migration0029 ./gateway/internal/integration_test/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PrimarySpeachesImage' gateway/internal/integration_test/primary_helpers_test.go` ≥ 1
    - `grep -c 'PrimaryWhisperWeightsKey' gateway/internal/integration_test/primary_helpers_test.go` ≥ 1
  </acceptance_criteria>
  <done>integration_test compila + roda tests Wave 2/3.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none new) | — | reconciler/lifecycle restore | accept | Pure subtraction reversal; risk surface idêntica ao Phase 06.8 shape pré-shrink (já UAT-validado) |
</threat_model>

<verification>
go build ./gateway/...
go test ./gateway/internal/primary/ ./gateway/internal/config/ ./gateway/internal/upstreams/ -count=1
</verification>

<success_criteria>
- Reconciler trio = 3 roles {llm,stt,tts} novamente
- lifecycle.go env map injeta WHISPER weights + SPEACHES_IMAGE no env do pod
- primary_helpers_test.go fixture compatível com Config restaurada
</success_criteria>

<output>
Create `.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-03-SUMMARY.md` when done.
</output>
