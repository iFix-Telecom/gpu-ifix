---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 07
type: execute
wave: 5
depends_on: [11.2-03, 11.2-04, 11.2-05, 11.2-06]
files_modified:
  - docs/RUNBOOK-OPS.md
  - .planning/PROJECT.md
  - gateway/.env.example
autonomous: true
requirements: [D-B3, D-B7, D-B8, D-B9, D-B10, D-B11]
threat_refs: [T-11.2-DOC]

must_haves:
  truths:
    - "RUNBOOK-OPS contém seção Gemini STT Operations (key minting, rotation, quota, cooldown, Pitfall list)"
    - "RUNBOOK-OPS contém seção Groq Whisper Operations (key minting, model whisper-large-v3 fixed)"
    - "RUNBOOK-OPS documenta operator gate D-B10 command verbatim"
    - "gateway/.env.example contém 6 novas STT envs + 2 weights envs (parity com pod side)"
    - "PROJECT.md tech-stack atualizado com Gemini 2.5 Flash Lite + Groq Whisper tier-1 entries"
  artifacts:
    - path: docs/RUNBOOK-OPS.md
      provides: "Operator playbook STT cascade 3-upstream"
    - path: gateway/.env.example
      provides: "Env documentation gateway side (parity pod)"
  key_links:
    - from: D-B10 operator gate
      to: RUNBOOK-OPS verbatim command
      via: docker exec env grep
      pattern: "UPSTREAM_STT_(GEMINI|GROQ)_AUTH_BEARER"
---

<objective>
Wave 5 — Documentation hygiene: RUNBOOK-OPS sections para Gemini + Groq, atualizar gateway/.env.example com 8 novas envs (2 weights + 6 STT), atualizar PROJECT.md tech-stack. Cost ledger row INSERTs para gemini-2.5-flash-lite e whisper-large-v3-groq via comment SQL (executar via gatewayctl em Wave 6).

Purpose: Operator runbook é a única fonte para minting/rotation/troubleshooting; sem ele, key rotation futura quebra prod.
Output: 3 docs files updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-CONTEXT.md
@docs/RUNBOOK-OPS.md
@gateway/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: RUNBOOK-OPS — Gemini STT + Groq Whisper sections + D-B10 gate</name>
  <files>docs/RUNBOOK-OPS.md</files>
  <read_first>
    - docs/RUNBOOK-OPS.md (estilo + structure existente)
    - 11.2-CONTEXT.md D-B3/D-B7/D-B8/D-B9/D-B10/D-B11
    - 11.2-RESEARCH.md Pitfalls 1-6 (operator-actionable)
  </read_first>
  <action>
    Adicionar 2 novas seções em RUNBOOK-OPS.md:

    Seção 1: "Gemini STT Operations" — subsections:
    - Key minting: AI Studio URL, project naming convention (D-B9 — 1 key compartilhada por enquanto; nota "revisita quando novo prod target chosen")
    - Env vars (D-B7): UPSTREAM_STT_GEMINI_URL/AUTH_BEARER/MODEL com defaults
    - Operator gate D-B10 verbatim command: ssh vps-ifix-vm 'docker exec ifix-ai-gateway sh -c "env | grep UPSTREAM_STT_GEMINI_AUTH_BEARER"' — non-empty igual gate green
    - Cooldown tuning D-B11: circuit_config cooldown_s=120 persisted in DB row gemini-stt (alterar via SQL UPDATE)
    - Quota monitoring: AI Studio dashboard URL; Pitfall 1 oscilação warning signs
    - Pitfall list ref (1=cascade race, 2=18MB cap, 3=x-goog-api-key, 6=key scoping)
    - Rotation: revoke + mint new key + update .env + docker compose restart gateway

    Seção 2: "Groq Whisper Operations" — subsections:
    - Key minting: console.groq.com/keys
    - Env vars (D-B8): UPSTREAM_STT_GROQ_URL/AUTH_BEARER/MODEL com defaults; model whisper-large-v3 FIXED (latest stable)
    - Operator gate D-B10 verbatim command (mesmo pattern)
    - REUSE adapter note: Groq usa BuildOpenAIWhisperDirector existente (OpenAI-compatible endpoint) — não há código Groq-specific
    - Cooldown: default breaker (não custom)
    - Pricing reference: console.groq.com/docs/speech-text (~$0.111/h)
    - Rotation: mesmo flow Gemini

    Posicionar as 2 seções após existing Upstream/OpenAI section (procurar pela seção análoga openai-whisper docs).
  </action>
  <verify>
    <automated>grep -q 'Gemini STT Operations' docs/RUNBOOK-OPS.md && grep -q 'Groq Whisper Operations' docs/RUNBOOK-OPS.md && grep -q 'UPSTREAM_STT_GEMINI_AUTH_BEARER' docs/RUNBOOK-OPS.md && grep -q 'UPSTREAM_STT_GROQ_AUTH_BEARER' docs/RUNBOOK-OPS.md && grep -q 'cooldown_s' docs/RUNBOOK-OPS.md</automated>
  </verify>
  <done>
    Ambas seções presentes; D-B10 command verbatim documentado; rotation procedure documentado.
  </done>
</task>

<task type="auto">
  <name>Task 2: gateway/.env.example + PROJECT.md tech-stack update</name>
  <files>gateway/.env.example, .planning/PROJECT.md</files>
  <read_first>
    - gateway/.env.example (current state)
    - .planning/PROJECT.md (tech-stack section)
    - 11.2-CONTEXT.md D-B7/D-B8
  </read_first>
  <action>
    1. gateway/.env.example: adicionar bloco com 8 envs comentadas:
       - PRIMARY_WHISPER_WEIGHTS_KEY= (MinIO key)
       - PRIMARY_WHISPER_WEIGHTS_SHA256=
       - PRIMARY_SPEACHES_IMAGE=ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3 (D-B1 LOCKED)
       - UPSTREAM_STT_GEMINI_URL=https://generativelanguage.googleapis.com/v1beta
       - UPSTREAM_STT_GEMINI_AUTH_BEARER= (paste from AI Studio)
       - UPSTREAM_STT_GEMINI_MODEL=gemini-2.5-flash-lite
       - UPSTREAM_STT_GROQ_URL=https://api.groq.com/openai/v1
       - UPSTREAM_STT_GROQ_AUTH_BEARER= (paste from console.groq.com/keys)
       - UPSTREAM_STT_GROQ_MODEL=whisper-large-v3

    2. PROJECT.md: localizar tech-stack section listando upstreams atuais (procurar STT entries). Adicionar:
       - Gemini 2.5 Flash Lite (tier-1 prio=10, $0.05/h batch, D-B11 cooldown 120s)
       - Groq Whisper-large-v3 (tier-1 prio=15, $0.111/h, REUSE adapter D-B8)
       Atualizar costing notes: tier-1 primary fallback é Gemini, OpenAI safety net continua presente.
  </action>
  <verify>
    <automated>grep -q 'UPSTREAM_STT_GEMINI_AUTH_BEARER' gateway/.env.example && grep -q 'UPSTREAM_STT_GROQ_AUTH_BEARER' gateway/.env.example && grep -q 'PRIMARY_WHISPER_WEIGHTS_KEY' gateway/.env.example && grep -q 'gemini-2.5-flash-lite' .planning/PROJECT.md && grep -q 'whisper-large-v3' .planning/PROJECT.md</automated>
  </verify>
  <done>
    Env example tem 8 entries; PROJECT.md cita Gemini + Groq como upstreams tier-1.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-DOC | Information Disclosure | RUNBOOK published .env values | mitigate | .env.example documenta NAME não VALUE; AUTH_BEARER fields explícitos vazios com instrução "paste from <dashboard>" |
</threat_model>

<verification>
- 2 seções RUNBOOK-OPS publicadas
- gateway/.env.example com 8 novas linhas
- PROJECT.md tech-stack atualizado
</verification>

<success_criteria>
Operator tem playbook completo para mint/rotate/troubleshoot Gemini + Groq keys. Onboarding novo operator não precisa decodificar 11.2-CONTEXT.md.
</success_criteria>

<output>
Create .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-07-SUMMARY.md when done
</output>
