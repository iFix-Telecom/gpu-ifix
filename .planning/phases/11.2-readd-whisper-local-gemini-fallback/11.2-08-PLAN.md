---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 08
type: execute
wave: 7
depends_on: ["11.2-01", "11.2-02", "11.2-03", "11.2-04", "11.2-05", "11.2-06", "11.2-07"]
files_modified:
  - .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-HUMAN-UAT.md
  - .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-VERIFICATION.md
  - pod/smoke/uat-11.2.sh
autonomous: false
requirements: [UAT-CASCADE-LIVE, UAT-COLD-START]
threat_refs: [T-11.2-01, T-11.2-04]

must_haves:
  truths:
    - "Vast pod cold-start com novo image (whisper venv restored) completa em <8min e probeSTT :8001 retorna 200"
    - "Curl pod ON {model=whisper} hits local-stt tier-0 (assertion via /admin/metrics breaker.local-stt + audit_log row)"
    - "Curl pod OFF (drained) {model=whisper} hits gemini-stt tier-1 (audit_log upstream=gemini-stt; response shape OpenAI-compatible {text:...})"
    - "Force-open gemini-stt breaker via gatewayctl; curl pod OFF {model=whisper} fall-through para openai-whisper safety net (audit_log upstream=openai-whisper)"
    - "T-11.2-01 burst test: 35 concurrent requests em <60s contra gemini-stt → quota 429 → breaker OPEN → openai-whisper safety net atende"
    - "11.2-VERIFICATION.md preenchido com truths/artifacts/key_links observados; status: passed | passed_partial | failed"
  artifacts:
    - path: .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-HUMAN-UAT.md
      provides: Operator-driven UAT script S1-S5 com setup + commands + expected outputs
    - path: .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-VERIFICATION.md
      provides: Final phase verification record
    - path: pod/smoke/uat-11.2.sh
      provides: Reproducible bash driver para S1-S3 (Vast pod create + curl cascade)
---

<objective>
Wave 7 — LIVE UAT. Pre-condition Wave 0 (operator-supplied Gemini API key) já satisfeito antes de Plan 01. Author UAT script + reproducible driver + run S1-S5 cenários em vps-ifix-vm/Vast.ai. Document achados em VERIFICATION.md e fecha phase com status final.

Sem essa wave, multi-tier-1 cascade nunca foi exercitada em wire real — risco mais alto da phase (T-11.2-01 quota burst + Pitfall 1 oscillation só observáveis sob carga).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-CONTEXT.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md
@pod/smoke/
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0 (Wave 0 retroactive gate): Confirmar Gemini API key dev-scoped no /opt/ai-gateway-dev/.env</name>
  <what-built>Plans 01-07 já completos. Antes do UAT começar, operator confirma que:
  - UPSTREAM_STT_GEMINI_AUTH_BEARER setado em /opt/ai-gateway-dev/.env em vps-ifix-vm
  - Key é dev-scoped (criada em projeto AI Studio "ifix-ai-gateway-dev" — Pitfall 6)
  - `gatewayctl env show` revela key presente (presence-only — sem revelar valor)
  </what-built>
  <how-to-verify>
    1. SSH vps-ifix-vm: `grep -c UPSTREAM_STT_GEMINI_AUTH_BEARER /opt/ai-gateway-dev/.env` == 1
    2. Verify value setado: `grep '^UPSTREAM_STT_GEMINI_AUTH_BEARER=.\+' /opt/ai-gateway-dev/.env | wc -l` == 1 (não vazio)
    3. Restart stack: `cd /opt/ai-gateway-dev && docker compose restart`
    4. Probe presence: `docker exec ai-gateway-dev /gatewayctl env show 2>&1 | grep -i gemini` retorna entry "UPSTREAM_STT_GEMINI_AUTH_BEARER: present=true" (presence-only — D-06 pattern)
    5. AI Studio dashboard: confirmar key tem nome "ifix-ai-gateway-dev" (não compartilhada com prod)
  </how-to-verify>
  <resume-signal>Type "gemini-key-confirmed dev-scoped" or describe blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Author 11.2-HUMAN-UAT.md (S1-S5 cenários)</name>
  <files>.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md (pattern de S1-S6 estrutura)
    - 11.2-RESEARCH Pitfall 1 (T-11.2-01 burst test recipe)
  </read_first>
  <action>
    Criar 11.2-HUMAN-UAT.md com 5 cenários:

    **S1 — Cold-start com whisper re-added**
    - Pre: Plan 04 image promoted (novo digest em PRIMARY_TEMPLATE_IMAGE env)
    - Steps: trigger Vast pod create via gatewayctl primary spawn; aguardar até FSM transition Ready (timeout 600s); observar /health/stt :8001 retorna 200 OK + JSON `{text:""}` em silent WAV probe
    - Acceptance: cold-start total <8min; probeSTT presente; image size (`docker image inspect`) within ~10-12GB band

    **S2 — STT cascade pod ON (local-stt tier-0)**
    - Pre: pod Ready (S1 completed)
    - Steps: `curl -X POST https://ai-gateway.converse-ai.app/v1/audio/transcriptions -H "Authorization: Bearer ifix_sk_..." -F model=whisper -F file=@sample.wav` → response 200 + `{text:"...transcription..."}`
    - Acceptance: audit_log entry upstream=local-stt; metrics `gateway_dispatcher_total{upstream="local-stt"}` increments

    **S3 — STT tier-1 primary (gemini-stt) pod OFF**
    - Pre: drainPrimary via gatewayctl + aguardar FSM Drained
    - Steps: mesmo curl S2 → response 200 + `{text:"..."}`
    - Acceptance: audit_log upstream=gemini-stt; response shape OpenAI-compatible; latency <5s

    **S4 — STT tier-1 safety net fallback (openai-whisper)**
    - Pre: pod OFF (S3 state) + force-open gemini-stt breaker via `gatewayctl breaker force-open gemini-stt`
    - Steps: mesmo curl → response 200 + `{text:"..."}`
    - Acceptance: audit_log upstream=openai-whisper; metrics breaker.gemini-stt == OPEN

    **S5 — T-11.2-01 quota burst regression test**
    - Pre: pod OFF + gemini-stt breaker CLOSED (revert S4 force-open)
    - Steps: 35 concurrent curls em <60s → ≥1 quota 429 → breaker OPEN → subsequent curls hit openai-whisper safety net
    - Acceptance: audit_log mostra mistura initial gemini-stt → ≥1 quota error → transition para openai-whisper; nenhum 503 retornado ao cliente

    Cada S inclui: setup commands, expected outputs (com regex when nondeterministic), troubleshooting decision tree.
  </action>
  <verify>
    <automated>test -f .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-HUMAN-UAT.md && grep -c "^## S[1-5]" .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File presente
    - 5 cenários S1-S5 documentados
    - grep -c "^## S[1-5]" == 5
  </acceptance_criteria>
  <done>UAT script pronto para operator executar.</done>
</task>

<task type="auto">
  <name>Task 2: Author pod/smoke/uat-11.2.sh — bash driver para S1-S3</name>
  <files>pod/smoke/uat-11.2.sh</files>
  <read_first>
    - pod/smoke/ (outros scripts existentes — espelhar shape se houver)
  </read_first>
  <action>
    Criar `pod/smoke/uat-11.2.sh` que automatiza S1-S3:
    1. `set -euo pipefail; source /opt/ai-gateway-dev/.env`
    2. Pre-checks: gatewayctl reachable, tenant key set, sample.wav presente
    3. S1: `gatewayctl primary spawn` + poll FSM Ready
    4. S2: curl tier-0 → assert audit_log upstream=local-stt
    5. S3: `gatewayctl primary drain` + curl → assert audit_log upstream=gemini-stt
    6. Cleanup: `gatewayctl primary destroy` se exit code non-zero
    7. Exit 0 = all 3 PASS; non-zero = failed scenario.
  </action>
  <verify>
    <automated>bash -n pod/smoke/uat-11.2.sh && chmod +x pod/smoke/uat-11.2.sh && test -x pod/smoke/uat-11.2.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash -n exit 0
    - executable bit set
    - grep -c "gemini-stt" pod/smoke/uat-11.2.sh ≥ 1
  </acceptance_criteria>
  <done>Driver bash reproduzível disponível.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator executa S1-S5 e relata achados</name>
  <what-built>Tasks 0-2 já completos: env confirmed, UAT script + bash driver disponíveis. Operator agora executa cada cenário S1-S5 segundo 11.2-HUMAN-UAT.md.</what-built>
  <how-to-verify>
    1. SSH vps-ifix-vm
    2. Para S1-S3: rodar `bash pod/smoke/uat-11.2.sh` → exit 0 esperado
    3. Para S4-S5: seguir steps manuais do HUMAN-UAT.md
    4. Anotar audit_log SELECTs + metrics deltas para cada cenário
    5. Anotar duration + custos (S5 burst consome quota — confirmar com Google AI Studio dashboard que quota usage incrementou)
  </how-to-verify>
  <resume-signal>Type "uat-passed S1-S5", "uat-passed-partial &lt;cenários&gt;", ou "uat-failed &lt;cenários&gt; &lt;rationale&gt;".</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Author 11.2-VERIFICATION.md + cross-link to phase SUMMARY</name>
  <files>.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/11.1-shrink-pod-remove-whisper/11.1-VERIFICATION.md (template)
  </read_first>
  <action>
    Author 11.2-VERIFICATION.md com:
    1. Phase goal recap
    2. Must-haves por plan (truths/artifacts/key_links) — assert observed vs intended
    3. S1-S5 outcomes (PASS/PARTIAL/FAIL com rationale)
    4. T-11.2-01..T-11.2-05 disposition observed
    5. Cost summary (Vast pod time + Gemini API tokens consumed durante S5)
    6. Status final: passed | passed_partial | failed
    7. Próximos passos: phase 11.3 candidate items (cooldown_s wiring, gatewayctl breaker patch CLI, resumable files.upload para >18MB)
  </action>
  <verify>
    <automated>test -f .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-VERIFICATION.md && grep -cE "^## (Status|Must-haves|S[1-5])" .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md presente
    - Status field setado a passed|passed_partial|failed
    - 5 S sections cobertas
  </acceptance_criteria>
  <done>Phase 11.2 fechada com record formal.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-01 | DoS | Gemini quota burst | mitigate + verify | S5 cenário valida fall-through real; se safety net falha, gatewayctl breaker pattern + RUNBOOK guidance compensa |
| T-11.2-04 | Operational | Image size trade-off | accept + measure | S1 mede image size + cold-start time para SUMMARY; cost analysis Task 4 |
</threat_model>

<verification>
bash -n pod/smoke/uat-11.2.sh
test -f .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-HUMAN-UAT.md
test -f .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-VERIFICATION.md
</verification>

<success_criteria>
- Operator gate Wave 0 cleared (Gemini key dev-scoped present)
- S1-S5 outcomes documentados em VERIFICATION.md
- S2/S3 ambos PASSED (cascade tier-0 + tier-1 primary funcionais end-to-end)
- S4 PASSED OU rationale documentado (safety net acessível)
- S5 PASSED OU graceful degradation documentado (Pitfall 1 observado)
- Phase status final emitido
</success_criteria>

<output>
Create `.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-08-SUMMARY.md` when done + finalizar 11.2-VERIFICATION.md.
</output>
