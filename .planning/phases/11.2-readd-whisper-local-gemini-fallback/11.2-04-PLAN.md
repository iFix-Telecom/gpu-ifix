---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 04
type: execute
wave: 3
depends_on: ["11.2-01"]
files_modified:
  - pod/primary/Dockerfile
  - pod/primary/supervisord.conf
  - pod/onstart.sh
  - pod/scripts/download-weights.sh
  - pod/.env.example
  - pod/docker-compose.yml
  - .github/workflows/build-primary-pod.yml
autonomous: false
requirements: [D-B1, D-B2, RES-POD-RESTORE]
threat_refs: [T-11.2-04]

must_haves:
  truths:
    - "Dockerfile multi-stage volta a usar FROM ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3 AS speaches-assets"
    - "Dockerfile restaura /opt/speaches-venv com speaches@v0.9.0-rc.3 + onnx-asr + gradio + faster-whisper"
    - "Dockerfile restaura /weights/whisper mkdir + silero_*.onnx COPY --from=speaches-assets"
    - "supervisord.conf tem [program:speaches] block com HF_HUB_CACHE=/weights/whisper (Pitfall 4 fix-iii verbatim)"
    - "onstart.sh restaura WEIGHTS_WHISPER_KEY + WEIGHTS_WHISPER_SHA256 preflight assertions"
    - "download-weights.sh restaura branch paralelo para WEIGHTS_WHISPER_KEY (3-way parallel: qwen + bgem3 + whisper)"
    - ".env.example restaura WEIGHTS_WHISPER_KEY + WEIGHTS_WHISPER_SHA256 entries + adiciona 3 entries UPSTREAM_STT_GEMINI_*"
  artifacts:
    - path: pod/primary/Dockerfile
      provides: speaches venv stage + whisper /weights mkdir
    - path: pod/primary/supervisord.conf
      provides: "[program:speaches] block restored verbatim com Phase 06.8 fix-iii commentary"
    - path: pod/onstart.sh
      provides: WEIGHTS_WHISPER_* preflight guards
---

<objective>
Wave 3 (paralelo a Plan 03 — arquivos disjuntos) — reverter Phase 11.1 Plans 03 + 05 no lado do pod. Restaura speaches venv stage no Dockerfile (revert 8acb991), supervisord program:speaches block (revert 0f65da5), onstart WHISPER preflight (revert 16f865f), download-weights whisper branch (revert eda69d2), .env.example + docker-compose whisper entries (revert 09b5a7c), CI workflow comment (revert 16f865f).

Output: pod image rebuildable com whisper venv; cold-start +3-5min (T-11.2-04 trade-off documented). NÃO restaurar Infinity venv (D-03 manda embed off-pod). Plan 03 gateway side já injeta WHISPER env vars quando este completar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-03-SUMMARY.md
@pod/primary/Dockerfile
@pod/primary/supervisord.conf
@pod/onstart.sh
@pod/scripts/download-weights.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restaurar speaches venv stage no Dockerfile (revert 8acb991, exceto infinity-venv)</name>
  <files>pod/primary/Dockerfile</files>
  <read_first>
    - git show 8acb991^:pod/primary/Dockerfile — shape pré-shrink completo (Stage 1b + speaches-venv RUN + silero COPY + /weights/whisper mkdir + HEALTHCHECK ports)
    - pod/primary/Dockerfile (atual post-shrink)
  </read_first>
  <action>
    Comparar shapes via `git show 8acb991^:pod/primary/Dockerfile > /tmp/pre.dockerfile; diff -u pod/primary/Dockerfile /tmp/pre.dockerfile`. Aplicar APENAS hunks que tocam speaches + whisper + silero. NÃO restaurar Stage 1b infinity-venv nem `RUN python3 -m venv /opt/infinity-venv` (CONTEXT: "Re-introducing Infinity venv (D-03 still says embed off-pod) — out of scope"). Hunks a aplicar:
    1. Stage 1b: FROM ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3 AS speaches-assets
    2. RUN python3 -m venv /opt/speaches-venv + pip install speaches@v0.9.0-rc.3 onnx-asr<0.10.0 gradio<6.0 faster-whisper==1.1.1 + UAT pin commentary block (verbatim)
    3. COPY --from=speaches-assets ... silero_encoder_v5.onnx + silero_decoder_v5.onnx
    4. RUN mkdir -p /opt/speaches-data/realtime-console/dist + echo stub HTML + model_aliases.json bootstrap
    5. /weights/whisper no runtime mkdir -p list
    6. LABEL description + head comment + HEALTHCHECK port list (adicionar :8001 de volta)
    7. chatterbox-venv comment — adicionar Phase 11.2 nota sobre coexistência
  </action>
  <verify>
    <automated>grep -c speaches pod/primary/Dockerfile && grep -cE 'infinity-venv|infinity-emb' pod/primary/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - grep -c speaches pod/primary/Dockerfile ≥ 4
    - grep -c /opt/speaches-venv pod/primary/Dockerfile ≥ 1
    - grep -c silero_encoder_v5 pod/primary/Dockerfile ≥ 1
    - grep -c silero_decoder_v5 pod/primary/Dockerfile ≥ 1
    - grep -cE infinity-venv\|infinity-emb pod/primary/Dockerfile == 0
    - grep -c /weights/whisper pod/primary/Dockerfile ≥ 1
    - grep -c :8001 pod/primary/Dockerfile ≥ 1
  </acceptance_criteria>
  <done>Dockerfile multi-stage shape restaurado parcialmente; build smoke deferred to CI per 11.1-03 precedent.</done>
</task>

<task type="auto">
  <name>Task 2: Restaurar [program:speaches] em supervisord.conf (revert 0f65da5) + Phase 06.8 fix-iii commentary verbatim</name>
  <files>pod/primary/supervisord.conf</files>
  <read_first>
    - git show 0f65da5^:pod/primary/supervisord.conf — shape pré-shrink completo do block
    - RESEARCH Pitfall 4 (HF_HUB_CACHE path mismatch — block contém UAT 15-21 archaeology commentary que DEVE voltar verbatim para futuro grep audit)
  </read_first>
  <action>
    1. Comparar `git show 0f65da5^:pod/primary/supervisord.conf` vs estado atual. Restaurar APENAS [program:speaches] block. NÃO restaurar [program:infinity] commentary stub (D-03 out of scope).
    2. CRÍTICO Pitfall 4: block tem comment "UAT 15-21 archaeology" sobre HF_HUB_CACHE=/weights/whisper apontando para cache HF layout extraído pelo onstart. RESTAURAR VERBATIM.
    3. Header comment: refletir Phase 11.2 (programs supervisord-managed: speaches + llama + chatterbox + dcgm; health-bridge sobe out-of-supervisord).
  </action>
  <verify>
    <automated>grep -cE '^\[program:speaches' pod/primary/supervisord.conf && python3 -c "import configparser; c=configparser.ConfigParser(); c.read('pod/primary/supervisord.conf'); print(sorted(c.sections()))"</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE ^\[program:speaches pod/primary/supervisord.conf == 1
    - grep -c /opt/speaches-data pod/primary/supervisord.conf ≥ 1
    - grep -c HF_HUB_CACHE pod/primary/supervisord.conf ≥ 1
    - grep -cE ^\[program:infinity pod/primary/supervisord.conf == 0
    - configparser parse contém [supervisord, program:speaches, program:llama, program:chatterbox, program:dcgm]
  </acceptance_criteria>
  <done>supervisord parsable + speaches block + fix-iii commentary verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Restaurar WHISPER preflight em onstart.sh + branch em download-weights.sh + .env.example/compose + CI workflow comment</name>
  <files>pod/onstart.sh, pod/scripts/download-weights.sh, pod/.env.example, pod/docker-compose.yml, .github/workflows/build-primary-pod.yml</files>
  <read_first>
    - git show 16f865f^:pod/onstart.sh — preflight + banner pré-shrink
    - git show eda69d2^:pod/scripts/download-weights.sh — 3-way parallel branch
    - git show 09b5a7c^:pod/.env.example — WEIGHTS_WHISPER entries
    - git show 09b5a7c^:pod/docker-compose.yml — speaches service block (se aplicável)
  </read_first>
  <action>
    1. **onstart.sh**: restaurar `: "${WEIGHTS_WHISPER_KEY:?WEIGHTS_WHISPER_KEY required}"` + `: "${WEIGHTS_WHISPER_SHA256:?WEIGHTS_WHISPER_SHA256 required}"` preflight. Restaurar `speaches STT: …:8001` na banner final. NÃO restaurar `:8002` embed (D-03 out of scope).
    2. **download-weights.sh**: restaurar 3rd branch parallel para WEIGHTS_WHISPER_KEY (espelha branches WEIGHTS_QWEN + WEIGHTS_BGEM3). Extract target: `/weights/whisper` (Phase 06.8 fix-iii — tarball encoded com models--Systran--faster-whisper-large-v3/{refs,snapshots/<hash>}).
    3. **.env.example**: restaurar `WEIGHTS_WHISPER_KEY` + `WEIGHTS_WHISPER_SHA256` lines com comments + ADICIONAR 3 entries novas Gemini:
       ```
       UPSTREAM_STT_GEMINI_URL=https://generativelanguage.googleapis.com/v1beta
       UPSTREAM_STT_GEMINI_AUTH_BEARER=  # mint em https://aistudio.google.com/app/apikey (key dev-scoped — Pitfall 6)
       UPSTREAM_STT_GEMINI_MODEL=gemini-2.5-flash-lite
       ```
    4. **docker-compose.yml**: se 09b5a7c removeu speaches service block, restaurar.
    5. **.github/workflows/build-primary-pod.yml**: reverter header comment para refletir 4-binary stack (llama+chatterbox+dcgm+speaches). Sem mudança em run: steps.
  </action>
  <verify>
    <automated>bash -n pod/onstart.sh && bash -n pod/scripts/download-weights.sh && grep -c WEIGHTS_WHISPER_KEY pod/onstart.sh pod/scripts/download-weights.sh pod/.env.example | grep -v ':0' && grep -c UPSTREAM_STT_GEMINI_URL pod/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - grep -c WEIGHTS_WHISPER_KEY pod/onstart.sh ≥ 1
    - grep -c WEIGHTS_WHISPER_KEY pod/scripts/download-weights.sh ≥ 1
    - grep -c WEIGHTS_WHISPER_KEY pod/.env.example ≥ 1
    - grep -c UPSTREAM_STT_GEMINI_URL pod/.env.example == 1
    - bash -n exit 0 em ambos scripts
    - grep -ciE 'whisper|speaches' .github/workflows/build-primary-pod.yml ≥ 1 (header reverted)
  </acceptance_criteria>
  <done>Scripts shell sintaxe válida; .env.example documenta WHISPER + Gemini env vars; CI workflow header consistente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator gate — pod image rebuild + push + promote</name>
  <what-built>Tasks 1-3 alteraram fontes do pod image. Rebuild + push do GHCR + promoção do tag em PRIMARY_TEMPLATE_IMAGE env vars são human-managed (CI builda automaticamente em push develop, mas promotion ao .env do gateway é manual).</what-built>
  <how-to-verify>
    1. Verificar que push develop disparou `.github/workflows/build-primary-pod.yml` no GitHub Actions e workflow ficou verde.
    2. Anotar novo digest do tag `ghcr.io/ifixtelecom/converseai-pod-primary:develop-<sha>`.
    3. SSH vps-ifix-vm: `grep PRIMARY_TEMPLATE_IMAGE /opt/ai-gateway-dev/.env` — atualizar para novo digest se ainda aponta para shrunken image.
    4. Confirmar via `docker pull <digest>` em ops-claude que tamanho da imagem voltou a ~10-12GB (delta vs Phase 11.1 ~6-7GB documenta T-11.2-04 trade-off).
    5. Anotar `docker image inspect <digest> --format '{{.Size}}'` para SUMMARY/RUNBOOK.
  </how-to-verify>
  <resume-signal>Type "image-promoted &lt;new-digest&gt;" or describe blockers.</resume-signal>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-04 | Operational (regression trade-off) | pod/primary image size | accept (documented) | Trade-off (+5-6GB pull cost, ~3-5min cold-start regression) reverte ganho do Phase 11.1. CONTEXT motiva: STT cost local = $0 amortiza pull cost rapidamente para qualquer volume STT > ~30s/h. Decision documentado em 11.2-SUMMARY pós-Plan 03/04 com size mensurada via Task 4. |
</threat_model>

<verification>
bash -n pod/onstart.sh pod/scripts/download-weights.sh
python3 -c "import configparser; c=configparser.ConfigParser(); c.read('pod/primary/supervisord.conf'); assert 'program:speaches' in c.sections()"
docker buildx build --target final pod/primary/  # if buildx available, else defer to CI
</verification>

<success_criteria>
- 7 arquivos pod-side modificados (Dockerfile + supervisord + onstart + download-weights + .env.example + docker-compose + CI workflow comment)
- Static checks PASS (grep acceptance + shell -n + configparser)
- Image rebuilt + promoted (Task 4 checkpoint), tamanho medido para SUMMARY
</success_criteria>

<output>
Create `.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-04-SUMMARY.md` when done.
</output>
