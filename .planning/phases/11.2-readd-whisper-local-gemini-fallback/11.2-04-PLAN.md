---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 04
type: execute
wave: 3
depends_on: [11.2-02]
files_modified:
  - pod/primary/Dockerfile
  - pod/primary/supervisord.conf
  - pod/onstart.sh
  - pod/scripts/download-weights.sh
autonomous: false
requirements: [D-B1, D-B2, UAT-COLD-START]
threat_refs: [T-11.2-IMG, T-11.2-CACHE]

must_haves:
  truths:
    - "Pod image restaurado contém speaches venv stage + [program:speaches] supervisord + WHISPER guards no onstart + 3-way parallel weight download"
    - "HF_HUB_CACHE=/weights/whisper alinhado com download-weights.sh extract target (Pitfall 4)"
    - "Image rebuild + push + promote para ghcr.io concluído via GHA"
  artifacts:
    - path: pod/primary/Dockerfile
      provides: "Speaches venv stage restored"
    - path: pod/primary/supervisord.conf
      provides: "[program:speaches] block + HF_HUB_CACHE env + Phase 06.8 fix-iii comment"
    - path: pod/scripts/download-weights.sh
      provides: "3-way parallel download branch para whisper"
  key_links:
    - from: supervisord.conf [program:speaches]
      to: download-weights.sh tar extract target
      via: HF_HUB_CACHE=/weights/whisper path
      pattern: "/weights/whisper"
---

<objective>
Wave 3a (parallel com Plan 05) — Restaurar componentes pod-side (Dockerfile speaches stage, supervisord [program:speaches], onstart WHISPER guards, download-weights.sh whisper branch) verbatim de git history. Inclui checkpoint para rebuild + push da nova image PRIMARY_TEMPLATE_IMAGE.

Purpose: Sem stage speaches no Dockerfile e sem extract target alinhado com HF_HUB_CACHE, pod cold-start fica em crash-loop (Pitfall 4).
Output: 4 arquivos pod restored + new image promoted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-CONTEXT.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-PATTERNS.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore Dockerfile speaches venv stage + supervisord program speaches (verbatim 0f65da5^)</name>
  <files>pod/primary/Dockerfile, pod/primary/supervisord.conf</files>
  <read_first>
    - PATTERNS.md rows pod/primary/Dockerfile e pod/primary/supervisord.conf (sources git show 0f65da5^)
    - 11.2-RESEARCH.md Pitfall 4 (HF_HUB_CACHE path mismatch)
    - Current files post-11.1 via cat
  </read_first>
  <action>
    Dockerfile: extrair stage speaches-venv de git show 0f65da5^ pod/primary/Dockerfile. Splice no Dockerfile atual posicionando o stage entre o stage do llama e o stage final (mesmo lugar pre-11.1). Manter quaisquer fixes pós-Phase 06.8 que não estavam em 0f65da5 — não regredir (faster-whisper==1.1.1 pin, silero v5 vendor, click less than 8.2 pin, gradio less than 6, transformers less than 4.49, optimum less than 2.0 — todos esses bloqueios estão em commits POST 0f65da5; restoração + merge com fixes downstream).

    supervisord.conf: extrair bloco [program:speaches] verbatim de git show 0f65da5^ pod/primary/supervisord.conf incluindo environment com HF_HUB_CACHE igual /weights/whisper + WHISPER__INFERENCE_DEVICE igual cpu (Phase 06.8 fix-iii + Phase 11 UAT 17 CPU pin); directory=/opt/speaches-data; comentário Phase 06.8 fix-iii VERBATIM (Pitfall 4 documentation requirement); command apontando para /opt/speaches-venv/bin/uvicorn ou equivalente do upstream block.

    Comando útil: git show 0f65da5^:pod/primary/supervisord.conf > /tmp/speaches.conf então diff/merge manual.
  </action>
  <verify>
    <automated>grep -q 'speaches-venv' pod/primary/Dockerfile && grep -q 'program:speaches' pod/primary/supervisord.conf && grep -q 'HF_HUB_CACHE' pod/primary/supervisord.conf && grep -q '/weights/whisper' pod/primary/supervisord.conf</automated>
  </verify>
  <done>
    Dockerfile contém stage speaches-venv build + COPY final; supervisord.conf contém [program:speaches] block + HF_HUB_CACHE + Phase 06.8 fix-iii comment; docker build pod/primary/ (em smoke local opcional) parseia sem erro de syntax.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restore onstart WHISPER guards + download-weights 3-way parallel whisper branch</name>
  <files>pod/onstart.sh, pod/scripts/download-weights.sh</files>
  <read_first>
    - PATTERNS.md rows pod/onstart.sh + pod/scripts/download-weights.sh (sources 16f865f^ e eda69d2^)
    - Current files via cat
    - 11.2-RESEARCH.md Pitfall 4 (extract target alinhamento)
  </read_first>
  <action>
    pod/onstart.sh: restaurar bloco env preflight WHISPER verbatim de git show 16f865f^ pod/onstart.sh. Inclui guards do tipo bash colon-quote para WEIGHTS_WHISPER_KEY e WEIGHTS_WHISPER_SHA256 (sintaxe ${VAR:?msg}). Substituir o comentário atual "STT off-pod" (linhas ~9-12) pelo bloco restored. Preservar fixes posteriores (Phase 06.6/06.7/06.8 fixes a llama/tts/dcgm que não vinham em 16f865f).

    pod/scripts/download-weights.sh: restaurar branch whisper na seção parallel downloads verbatim git show eda69d2^ pod/scripts/download-weights.sh. 3-way parallel: qwen + bge-m3 + whisper. Whisper extract target deve ser /weights/whisper (alinhado com HF_HUB_CACHE no supervisord.conf — Pitfall 4 enforcement). SHA-256 verification antes do tar extract.
  </action>
  <verify>
    <automated>grep -q WEIGHTS_WHISPER_KEY pod/onstart.sh && grep -q WEIGHTS_WHISPER_SHA256 pod/onstart.sh && grep -q '/weights/whisper' pod/scripts/download-weights.sh && bash -n pod/onstart.sh && bash -n pod/scripts/download-weights.sh</automated>
  </verify>
  <done>
    onstart.sh tem ambos guards WEIGHTS_WHISPER_*; download-weights.sh tem branch whisper paralelo com extract target /weights/whisper; bash -n parseia ambos.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: BLOCKING — rebuild + push pod image + promote PRIMARY_TEMPLATE_IMAGE</name>
  <read_first>
    - .github/workflows/build-primary-pod.yml (GHA self-hosted runner deploy)
    - CLAUDE.md (deploy section — GHA + ghcr.io)
    - 11.2-RESEARCH.md Runtime State Inventory
  </read_first>
  <what-built>Dockerfile/supervisord/onstart/download-weights restaurados em develop branch.</what-built>
  <how-to-verify>
    1. git add + commit das mudanças Plan 04 Tasks 1-2 + push develop
    2. GHA build-primary-pod.yml dispara no runner self-hosted (vps-ifix-vm)
    3. Aguardar Actions ghcr.io/ifixtelecom/converseai-primary-pod:develop publicar nova digest
    4. ssh vps-ifix-vm 'docker pull ghcr.io/ifixtelecom/converseai-primary-pod:develop' — capture novo digest
    5. Operator atualiza /opt/ai-gateway-dev/.env PRIMARY_TEMPLATE_IMAGE igual ghcr.io/ifixtelecom/converseai-primary-pod@sha256:<novo-digest>
    6. docker compose -f /opt/ai-gateway-dev/docker-compose.yml up -d gateway
    7. ssh vps-ifix-vm 'docker exec ifix-ai-gateway env | grep PRIMARY_TEMPLATE_IMAGE' — confirmar nova digest
  </how-to-verify>
  <acceptance_criteria>
    - GHA workflow build-primary-pod.yml verde em develop tip
    - ghcr.io novo digest publicado
    - .env PRIMARY_TEMPLATE_IMAGE atualizado com digest novo (não tag mutável develop — digest pinned)
    - Container recriado; env confirma
  </acceptance_criteria>
  <resume-signal>Type "promoted" with digest, or describe failure</resume-signal>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-IMG | Tampering | Pod image build chain | mitigate | Image digest pinning (operator promove digest, não tag mutável); GHA build em runner self-hosted (vps-ifix-vm) já controlado |
| T-11.2-CACHE | Denial of Service | speaches HF cache layout | mitigate | Pitfall 4 enforcement: extract target = HF_HUB_CACHE path = /weights/whisper; Phase 06.8 fix-iii comment restaurado VERBATIM como documentation |
</threat_model>

<verification>
- 4 arquivos pod restaurados com greps PASS
- GHA build verde
- Operator promoveu digest (.env atualizado)
- Pod next provision usa nova image (próximo force-up cycle)
</verification>

<success_criteria>
Pod image novo contém speaches + whisper weights download + supervisord program + HF cache layout — pronto para cold-start UAT Wave 6 scenario 1.
</success_criteria>

<output>
Create .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-04-SUMMARY.md when done
</output>
