---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 02
type: execute
wave: 1
depends_on: [11.2-01]
files_modified:
  - gateway/internal/config/config.go
  - gateway/internal/config/config_test.go
  - pod/.env.example
  - pod/docker-compose.yml
autonomous: true
requirements: [D-B1, D-B2, D-B7, D-B8]
threat_refs: [T-11.2-CFG]

must_haves:
  truths:
    - "Gateway Config tipa PRIMARY_WHISPER_WEIGHTS_KEY + PRIMARY_WHISPER_WEIGHTS_SHA256 (revert 11.1)"
    - "Gateway Config tipa UPSTREAM_STT_GEMINI_{URL,AUTH_BEARER,MODEL} + UPSTREAM_STT_GROQ_{URL,AUTH_BEARER,MODEL} (NEW)"
    - "pod/.env.example documenta WEIGHTS_WHISPER_KEY/SHA256 + 6 novas STT envs"
  artifacts:
    - path: gateway/internal/config/config.go
      provides: "Config fields para weights whisper + Gemini + Groq STT"
    - path: pod/.env.example
      provides: "Env documentation restored + extended"
  key_links:
    - from: gateway Config
      to: resolver.upstreamEnvVarMap
      via: env var names string-matched in Plan 06
      pattern: "UPSTREAM_STT_GEMINI_MODEL / UPSTREAM_STT_GROQ_MODEL"
---

<objective>
Wave 1 — Restaurar campos de Config no gateway para weights whisper (revertendo 11.1) e adicionar os 6 novos env vars de STT tier-1 (Gemini + Groq). Atualizar `.env.example` do pod restoring `WEIGHTS_WHISPER_*` + adicionando as 6 novas.

Purpose: Toda implementação downstream (reconciler em Plan 03, director em Plan 06) depende destes campos existirem em Config.
Output: Config code + test + docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-CONTEXT.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-PATTERNS.md
@gateway/internal/config/config.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore PRIMARY_WHISPER_WEIGHTS_* + add UPSTREAM_STT_GEMINI_* + UPSTREAM_STT_GROQ_* em Config</name>
  <files>gateway/internal/config/config.go, gateway/internal/config/config_test.go</files>
  <read_first>
    - gateway/internal/config/config.go (find PRIMARY_LLM_WEIGHTS_* analog block — Gemini/Groq added there)
    - 11.2-CONTEXT.md D-B7 + D-B8 (env var names exatas)
    - PATTERNS.md row pod/.env.example (env var names para parity)
    - Git: `git log -S PrimaryWhisperWeightsKey -- gateway/internal/config/config.go` para achar commit de remoção
  </read_first>
  <behavior>
    - Field restored: `PrimaryWhisperWeightsKey string` env `PRIMARY_WHISPER_WEIGHTS_KEY`
    - Field restored: `PrimaryWhisperWeightsSHA256 string` env `PRIMARY_WHISPER_WEIGHTS_SHA256`
    - Field NEW: `PrimarySpeachesImage string` env `PRIMARY_SPEACHES_IMAGE` default `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3` (D-B1 LOCKED)
    - Field NEW: `UpstreamSTTGeminiURL string` env `UPSTREAM_STT_GEMINI_URL` default `https://generativelanguage.googleapis.com/v1beta`
    - Field NEW: `UpstreamSTTGeminiAuthBearer string` env `UPSTREAM_STT_GEMINI_AUTH_BEARER` (no log; pattern `AuthBearer json:"-"`)
    - Field NEW: `UpstreamSTTGeminiModel string` env `UPSTREAM_STT_GEMINI_MODEL` default `gemini-2.5-flash-lite`
    - Field NEW: `UpstreamSTTGroqURL string` env `UPSTREAM_STT_GROQ_URL` default `https://api.groq.com/openai/v1`
    - Field NEW: `UpstreamSTTGroqAuthBearer string` env `UPSTREAM_STT_GROQ_AUTH_BEARER` (no log)
    - Field NEW: `UpstreamSTTGroqModel string` env `UPSTREAM_STT_GROQ_MODEL` default `whisper-large-v3`
  </behavior>
  <action>
    Restaurar 2 campos whisper weights via git history (find commit que removeu via `git log -S PrimaryWhisperWeightsKey -- gateway/internal/config/config.go`, extrair lines do parent), depois adicionar 7 campos novos (1 image + 6 STT envs) seguindo padrão do bloco PRIMARY_LLM_WEIGHTS_* / UPSTREAM_LLM_OPENROUTER_* já existente (mesmo loader pattern, mesmo log-on-presence pattern do resolver.go:104-108). Update existing Config struct ordering: weights agrupados após existing PrimaryLlmWeights*; STT envs agrupados após existing UPSTREAM_STT_OPENAI_*. Atualizar `config_test.go` adicionando 9 sub-tests `TestConfig_Defaults_*` (1 por novo campo) cobrindo default + override por env.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/config/... -run 'TestConfig_Defaults' -count=1</automated>
  </verify>
  <done>
    - 9 novos campos presentes em Config struct (`grep -c 'UpstreamSTTGemini\|UpstreamSTTGroq\|PrimaryWhisperWeights\|PrimarySpeachesImage' gateway/internal/config/config.go` ≥ 9)
    - Tests verdes
    - `AuthBearer` fields têm tag `json:"-"` (grep para confirmar)
  </done>
</task>

<task type="auto">
  <name>Task 2: Restore pod/.env.example + pod/docker-compose.yml (WEIGHTS_WHISPER + speaches service block + 6 novas envs)</name>
  <files>pod/.env.example, pod/docker-compose.yml</files>
  <read_first>
    - PATTERNS.md row pod/.env.example (source `git show 09b5a7c^:pod/.env.example`)
    - PATTERNS.md row pod/docker-compose.yml (source `git show 09b5a7c^:pod/docker-compose.yml`)
    - 11.2-CONTEXT.md D-B7 + D-B8 (env var names + defaults)
  </read_first>
  <action>
    1. `pod/.env.example` — extrair seção WEIGHTS_WHISPER_* de `git show 09b5a7c^:pod/.env.example` e mergiar no arquivo atual. Adicionar bloco novo no final com as 6 envs UPSTREAM_STT_GEMINI_* + UPSTREAM_STT_GROQ_* (defaults conforme D-B7/D-B8; AUTH_BEARER vazio com comentário "operator: paste Gemini key from AI Studio").

    2. `pod/docker-compose.yml` — extrair bloco `speaches:` service de `git show 09b5a7c^:pod/docker-compose.yml` e re-adicionar (image `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3` D-B1 LOCKED, porta 8001, volume `/weights/whisper`). Manter blocos llm + tts + dcgm como estão (não tocar).
  </action>
  <verify>
    <automated>grep -q 'WEIGHTS_WHISPER_KEY' pod/.env.example && grep -q 'UPSTREAM_STT_GEMINI_AUTH_BEARER' pod/.env.example && grep -q 'UPSTREAM_STT_GROQ_AUTH_BEARER' pod/.env.example && grep -q 'speaches-ai/speaches:0.9.0-rc.3' pod/docker-compose.yml && grep -q '8001:8001' pod/docker-compose.yml</automated>
  </verify>
  <done>
    - .env.example contém WEIGHTS_WHISPER_KEY + WEIGHTS_WHISPER_SHA256 + 6 UPSTREAM_STT_{GEMINI,GROQ}_*
    - docker-compose.yml contém serviço speaches com image D-B1
    - YAML válido: `docker compose -f pod/docker-compose.yml config > /dev/null` (se ambiente docker disponível) OU `python3 -c 'import yaml; yaml.safe_load(open("pod/docker-compose.yml"))'`
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-CFG | Information Disclosure | AuthBearer fields | mitigate | `json:"-"` tag (PATTERNS Shared Pattern §Secret Hygiene) on all 2 new bearer fields |
</threat_model>

<verification>
- 9 campos Config code-present
- 6 env vars + 2 weights vars documented em .env.example
- speaches service block restored em docker-compose.yml
- `grep -E 'json:"-"' gateway/internal/config/config.go | grep -E 'GeminiAuthBearer|GroqAuthBearer'` retorna 2 linhas
</verification>

<success_criteria>
Wave 1 config done; downstream Plans 03 + 06 podem referenciar `cfg.PrimaryWhisperWeightsKey/SHA256/PrimarySpeachesImage` e `cfg.UpstreamSTTGemini*` / `cfg.UpstreamSTTGroq*` sem 502s de campo inexistente.
</success_criteria>

<output>
Create `.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-02-SUMMARY.md` when done
</output>
