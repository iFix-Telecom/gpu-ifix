---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 02
type: execute
wave: 2
depends_on: ["11.2-01"]
files_modified:
  - gateway/db/migrations/0029_readd_whisper_add_gemini.sql
  - gateway/internal/db/migrate_test.go
  - gateway/internal/integration_test/migration_0029_test.go
  - gateway/internal/integration_test/migration_0026_test.go
  - gateway/internal/integration_test/model_alias_test.go
  - gateway/internal/integration_test/upstreams_loader_test.go
  - gateway/internal/integration_test/upstreams_listen_test.go
  - gateway/internal/integration_test/openai_whisper_model_rewrite_test.go
  - gateway/internal/upstreams/types.go
  - gateway/internal/upstreams/loader.go
  - gateway/internal/upstreams/loader_test.go
  - gateway/internal/models/resolver.go
autonomous: true
requirements: [D-B5, D-B6, OR-FIX-PARITY, RES-MIGR-0029]
threat_refs: [T-11.2-03]

must_haves:
  truths:
    - "Migration 0029 UP é idempotente: re-INSERTs local-stt + (whisper, local-stt) + INSERT gemini-stt + (whisper, gemini-stt) + UPDATE openai-whisper.tier_priority=20"
    - "Migration 0029 DOWN é simétrica: DELETE alias gemini-stt + local-stt; DELETE upstream gemini-stt + local-stt; UPDATE openai-whisper.tier_priority=0"
    - "Schema: upstreams ganha coluna tier_priority INT NOT NULL DEFAULT 0; UNIQUE(role,tier) substituída por UNIQUE(role,tier,tier_priority)"
    - "UpstreamConfig struct ganha campo TierPriority int (sqlc + handlers refletem nova coluna)"
    - "Loader.ResolveAllTier1(role) retorna candidatos tier-1 ordenados ASC por TierPriority"
    - "models.upstreamEnvVarMap inclui entry 'gemini-stt' → 'UPSTREAM_STT_GEMINI_MODEL'"
  artifacts:
    - path: gateway/db/migrations/0029_readd_whisper_add_gemini.sql
      provides: "+goose Up/Down com schema additive + 3 INSERTs + 1 UPDATE + 2 alias INSERTs"
    - path: gateway/internal/upstreams/loader.go
      provides: "ResolveAllTier1(role string) []UpstreamConfig"
    - path: gateway/internal/integration_test/migration_0029_test.go
      provides: "Up/Down/Roundtrip + preservation tests (espelha shape 0028_test)"
---

<objective>
Wave 2 schema additive + multi-tier-1 routing primitive. Migration 0029 (a) reverte 0028 re-inserting local-stt + alias, (b) adiciona coluna tier_priority + nova UNIQUE constraint, (c) insere gemini-stt em tier 1 priority 10 + alias, (d) atualiza openai-whisper para tier_priority=20 (safety net). Loader ganha ResolveAllTier1 para dispatcher consumir em Wave 5. Resolver mapeia gemini-stt → UPSTREAM_STT_GEMINI_MODEL (D-06 parity).

Output: 1 migration nova, Loader+types estendidos, resolver+migrate_test atualizados, 5 fixture files revertidos 5→6 upstreams.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-CONTEXT.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-02-SUMMARY.md
@gateway/db/migrations/0028_remove_local_stt_upstream.sql
@gateway/internal/upstreams/loader.go
@gateway/internal/upstreams/types.go

<interfaces>
<!-- Padrão `+goose Up/Down StatementBegin/End` é o canônico do repo (vide 0026/0028) -->
<!-- `INSERT ... ON CONFLICT DO NOTHING` é o pattern idempotente padrão -->
<!-- UpstreamConfig (types.go:24) atual NÃO tem TierPriority — adicionar -->
<!-- Loader.Resolve(role,tier) (loader.go:205) — Loader.ResolveAllTier1(role) será NOVO sibling -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0029 SQL + integration test (RED→GREEN)</name>
  <files>gateway/db/migrations/0029_readd_whisper_add_gemini.sql, gateway/internal/integration_test/migration_0029_test.go, gateway/internal/db/migrate_test.go</files>
  <read_first>
    - `gateway/db/migrations/0028_remove_local_stt_upstream.sql` (template completo para revert template + DOWN/Up shape)
    - `gateway/internal/integration_test/migration_0028_test.go` (espelhar 4 subtests: Up, Up_PreservesOpenAIWhisper, Down, Roundtrip)
    - `gateway/db/migrations/0007_create_upstreams.sql` linha 23 (`UNIQUE (role, tier)` constraint a substituir)
    - `gateway/internal/db/migrate_test.go` (TestEmbedFS_HasAllMigrations want-list — adicionar 0029)
  </read_first>
  <behavior>
    - TestIntegration_Migration0029_Up: após UP, table upstreams tem 7 rows (5 atuais + local-stt + gemini-stt); model_aliases tem (whisper,local-stt) E (whisper,gemini-stt); openai-whisper row tem tier_priority=20.
    - TestIntegration_Migration0029_Up_PreservesOpenAIWhisper: openai-whisper sobrevive ao Up (apenas tier_priority muda 0→20); alias (whisper, openai-whisper) → whisper-1 intact.
    - TestIntegration_Migration0029_Down: após Up→Down: local-stt + gemini-stt DELETEd; aliases removidos; openai-whisper.tier_priority de volta a 0.
    - TestIntegration_Migration0029_Roundtrip: Up→Down→Up idempotente; coluna tier_priority persiste após Down (additive — não dropped).
    - TestIntegration_Migration0029_UniqueConstraintReplaced: tentar `INSERT (role='llm', tier=0, tier_priority=0)` duplicado → conflict; permitir `(role='stt', tier=1, tier_priority=10)` + `(role='stt', tier=1, tier_priority=20)` simultaneamente.
  </behavior>
  <action>
    Criar `gateway/db/migrations/0029_readd_whisper_add_gemini.sql` com SQL EXATO abaixo (do RESEARCH Code Examples):

    ```sql
    -- +goose Up
    -- +goose StatementBegin
    SET search_path = ai_gateway, public;

    -- Phase 11.2 — revert 0028 (local-stt) AND add gemini-stt as second tier-1.
    -- Schema additive: tier_priority column + replace UNIQUE constraint.

    ALTER TABLE ai_gateway.upstreams
        ADD COLUMN IF NOT EXISTS tier_priority INT NOT NULL DEFAULT 0;

    ALTER TABLE ai_gateway.upstreams
        DROP CONSTRAINT IF EXISTS upstreams_role_tier_key;
    ALTER TABLE ai_gateway.upstreams
        ADD CONSTRAINT upstreams_role_tier_priority_key UNIQUE (role, tier, tier_priority);

    -- Step 3 — revert 0028: re-INSERT local-stt + (whisper, local-stt) alias.
    INSERT INTO ai_gateway.upstreams (name, role, tier, tier_priority, url_env, auth_bearer_env)
        VALUES ('local-stt', 'stt', 0, 0, 'UPSTREAM_STT_URL', NULL)
        ON CONFLICT (name) DO NOTHING;

    INSERT INTO ai_gateway.model_aliases (alias, upstream, target, upstream_name)
        VALUES ('whisper', 'stt', 'Systran/faster-whisper-large-v3', 'local-stt')
        ON CONFLICT (alias, upstream_name) DO NOTHING;

    -- Step 4 — promote existing openai-whisper to tier_priority=20 (safety net).
    UPDATE ai_gateway.upstreams
        SET tier_priority = 20
        WHERE name = 'openai-whisper';

    -- Step 5 — INSERT gemini-stt upstream + alias at tier_priority=10 (primary fallback).
    INSERT INTO ai_gateway.upstreams (name, role, tier, tier_priority, url_env, auth_bearer_env)
        VALUES ('gemini-stt', 'stt', 1, 10, 'UPSTREAM_STT_GEMINI_URL', 'UPSTREAM_STT_GEMINI_AUTH_BEARER')
        ON CONFLICT (name) DO NOTHING;

    INSERT INTO ai_gateway.model_aliases (alias, upstream, target, upstream_name)
        VALUES ('whisper', 'stt', 'gemini-2.5-flash-lite', 'gemini-stt')
        ON CONFLICT (alias, upstream_name) DO NOTHING;

    COMMENT ON COLUMN ai_gateway.upstreams.tier_priority IS
        'Phase 11.2: ordering within (role, tier). Lower wins. Tier-0 sempre 0. Multi-tier-1 ex: (stt,1,10)=gemini-stt, (stt,1,20)=openai-whisper.';
    -- +goose StatementEnd

    -- +goose Down
    -- +goose StatementBegin
    SET search_path = ai_gateway, public;

    DELETE FROM ai_gateway.model_aliases
        WHERE (alias, upstream_name) IN
              (('whisper', 'gemini-stt'), ('whisper', 'local-stt'));

    DELETE FROM ai_gateway.upstreams WHERE name = 'gemini-stt';
    DELETE FROM ai_gateway.upstreams WHERE name = 'local-stt';

    UPDATE ai_gateway.upstreams
        SET tier_priority = 0
        WHERE name = 'openai-whisper';

    -- NOTE: deliberately KEEP tier_priority column + new UNIQUE constraint —
    -- aditivo; manual drop disponível em comentário inline.
    -- +goose StatementEnd
    ```

    Criar `migration_0029_test.go` espelhando migration_0028_test.go (5 subtests conforme behavior, usando testcontainers postgres). Goose Up até HEAD; assertion via `SELECT name, tier, tier_priority FROM upstreams WHERE name IN ('local-stt','gemini-stt','openai-whisper')`.

    Em `migrate_test.go` adicionar `0029_readd_whisper_add_gemini.sql` ao want-list de `TestEmbedFS_HasAllMigrations`.

    T-11.2-03 mitigation: Postgres aplica `+goose StatementBegin/End` como transação implícita — se qualquer step falha, transaction abort + rollback automático. Test `Roundtrip` valida que UP é idempotente sob re-execução.
  </action>
  <verify>
    <automated>go test -tags integration -run TestIntegration_Migration0029 ./gateway/internal/integration_test/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '+goose Up\|+goose Down' gateway/db/migrations/0029_readd_whisper_add_gemini.sql` == 2
    - `grep -c "ADD COLUMN IF NOT EXISTS tier_priority" gateway/db/migrations/0029_readd_whisper_add_gemini.sql` == 1
    - `grep -c "UNIQUE (role, tier, tier_priority)" gateway/db/migrations/0029_readd_whisper_add_gemini.sql` == 1
    - 5 subtests TestIntegration_Migration0029_* passam
  </acceptance_criteria>
  <done>Migration aplicável end-to-end em testcontainers; D-A5 (openai-whisper preserved) verificada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar UpstreamConfig.TierPriority + Loader.ResolveAllTier1 + resolver mapping gemini-stt</name>
  <files>gateway/internal/upstreams/types.go, gateway/internal/upstreams/loader.go, gateway/internal/upstreams/loader_test.go, gateway/internal/models/resolver.go</files>
  <read_first>
    - `gateway/internal/upstreams/types.go` linhas 24-50 (struct UpstreamConfig — adicionar TierPriority)
    - `gateway/internal/upstreams/loader.go:205` (Resolve método para shape de retorno)
    - `gateway/internal/upstreams/loader.go` Refresh — onde lê coluna sqlc gerada (adicionar tier_priority)
    - `gateway/internal/models/resolver.go` linha do upstreamEnvVarMap
  </read_first>
  <behavior>
    - TestUpstreamConfig_TierPriorityField: zero value default 0; struct field name == "TierPriority" tipo int
    - TestResolveAllTier1_OrderedAsc: setup 5-upstream snapshot com (stt,1,10)=gemini-stt + (stt,1,20)=openai-whisper → ResolveAllTier1("stt") retorna [gemini-stt, openai-whisper] nesse ordem
    - TestResolveAllTier1_FilterByRole: ResolveAllTier1("llm") NÃO retorna stt upstreams
    - TestResolveAllTier1_EmptyForUnknownRole: ResolveAllTier1("unknown") == nil
    - TestResolveAllTier1_SkipsTier0: tier-0 rows excluídos do resultado
    - TestPostMigration0029_RestoresLocalSTT_AddsGeminiSTT: substitui TestPostMigration0028_NoLocalSTTInSnapshot — fixture seeds 7-upstream shape e asserta `Resolve("stt",0)==local-stt` + `ResolveAllTier1("stt")` retorna 2 candidatos
    - TestResolver_GeminiSTTEnvOverride: env UPSTREAM_STT_GEMINI_MODEL="custom-model" → resolver.Resolve("whisper","gemini-stt")=="custom-model"
  </behavior>
  <action>
    1. **types.go**: adicionar `TierPriority int \`json:"tier_priority"\`` ao struct UpstreamConfig (posicionado após `Tier`).
    2. **loader.go Refresh**: incluir nova coluna no `SELECT name,role,tier,tier_priority,url_env,...` (regenerar sqlc se necessário OU se Refresh usa raw SQL, atualizar string). Atribuir ao UpstreamConfig.TierPriority.
    3. **loader.go ResolveAllTier1**: adicionar método novo (após `Resolve`, antes de `Get`):
       ```go
       // ResolveAllTier1 returns tier-1 candidates for role, ordered ASC by TierPriority (stable).
       // Phase 11.2 D-B5: dispatcher itera essa lista para multi-tier-1 fallthrough chain.
       func (l *Loader) ResolveAllTier1(role string) []UpstreamConfig {
           s := l.snap.Load()
           if s == nil { return nil }
           var out []UpstreamConfig
           for _, u := range s.ordered {
               if u.Role == role && u.Tier == 1 && u.Enabled {
                   out = append(out, u)
               }
           }
           sort.SliceStable(out, func(i, j int) bool { return out[i].TierPriority < out[j].TierPriority })
           return out
       }
       ```
       Add `import "sort"` se ainda não importado.
    4. **resolver.go**: adicionar entry `"gemini-stt": "UPSTREAM_STT_GEMINI_MODEL"` no `upstreamEnvVarMap` (manter ordem alfabética por NAME).
    5. **loader_test.go**: DELETAR `TestPostMigration0028_NoLocalSTTInSnapshot` integralmente; adicionar 6 tests novos (vide behavior). Reusar `NewLoaderInMemory` helper.
  </action>
  <verify>
    <automated>go test ./gateway/internal/upstreams/ ./gateway/internal/models/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TierPriority int' gateway/internal/upstreams/types.go` == 1
    - `grep -c 'func (l \*Loader) ResolveAllTier1' gateway/internal/upstreams/loader.go` == 1
    - `grep -c '"gemini-stt"' gateway/internal/models/resolver.go` == 1
    - `grep -c 'TestPostMigration0028_NoLocalSTTInSnapshot' gateway/internal/upstreams/loader_test.go` == 0 (deletado)
    - `grep -c 'TestPostMigration0029_RestoresLocalSTT' gateway/internal/upstreams/loader_test.go` == 1
  </acceptance_criteria>
  <done>Loader expõe ResolveAllTier1; tests verdes; resolver mapping gemini-stt presente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reverter fixture cascade 5→6 upstreams em integration_test (revert 11.1-02 Task 2)</name>
  <files>gateway/internal/integration_test/upstreams_loader_test.go, gateway/internal/integration_test/upstreams_listen_test.go, gateway/internal/integration_test/model_alias_test.go, gateway/internal/integration_test/openai_whisper_model_rewrite_test.go, gateway/internal/integration_test/migration_0026_test.go</files>
  <read_first>
    - `.planning/phases/11.1-shrink-pod-remove-whisper/11.1-02-SUMMARY.md` (lista exata dos asserts revertidos)
    - `git show 5ddab53^:gateway/internal/integration_test/upstreams_loader_test.go` (shape pré-shrink)
  </read_first>
  <behavior>
    - Reverte 11.1-02 Task 2: contagens 5→6 (e onde 11.2-02 adicionou gemini-stt → contagens vão a 7).
    - Mas SOMENTE para os assertos em fixtures, não para os "TestPostMigration0028_*". Esses já foram deletados em Task 2.
  </behavior>
  <action>
    Aplicar revert direcionado:

    1. **upstreams_loader_test.go**:
       - `RefreshLoadsFiveUpstreams` → `RefreshLoadsSevenUpstreams` (5→7 post-0029)
       - `len(loader.All())` / `len(loader.Names())` asserts: 5→7
       - `Resolve("stt", 0)`: flip de "ok=false" → "ok=true, name=local-stt"
       - Adicionar `UPSTREAM_STT_URL` setenv onde foi removido (4 test methods, vide 11.1-02 SUMMARY)
       - `MissingURLEnv` 2→3 (UPSTREAM_STT_URL + UPSTREAM_STT_GEMINI_URL contam? — ajustar conforme expectation)
       - `resetUpstreamsTable` CASE arm: restaurar local-stt + adicionar gemini-stt
    2. **upstreams_listen_test.go**: restaurar `UPSTREAM_STT_URL` setenv nos 2 NOTIFY tests + adicionar `UPSTREAM_STT_GEMINI_URL`
    3. **model_alias_test.go**: restaurar Resolve case `(whisper, local-stt) → Systran/faster-whisper-large-v3` + adicionar `(whisper, gemini-stt) → gemini-2.5-flash-lite`; comment block "5-upstream post-0028 shape" → "7-upstream post-0029 shape"
    4. **openai_whisper_model_rewrite_test.go**: voltar `resilienceLoader` no lugar de `NewLoaderInMemory(openai-whisper @ tier=0)`; restaurar `driveBreaker(t, bs, "local-stt", 500, 3)`; restaurar `tier0` mock no struct + setup; docstring revisada para Phase 11.2 shape
    5. **migration_0026_test.go**: reverter `Down(3)→Down(2)` em `TestIntegration_Migration0026_UpDownUp`; ajustar countBefore=6 → 7 (5 seeded post-0029 + 1 op + Plan 02 nova alias) — confirmar contagem empiricamente; comentários inline atualizados pelas novas migrations 0029 atop 0028 atop 0027.
  </action>
  <verify>
    <automated>go test -tags integration -run "TestIntegration_05_ModelAlias|TestIntegration_UpstreamsLoader|TestIntegration_UpstreamsListener|TestIntegration_OpenAIWhisperModelRewrite|TestIntegration_Migration0026" ./gateway/internal/integration_test/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "RefreshLoadsFiveUpstreams" gateway/internal/integration_test/` == 0
    - `grep -c "driveBreaker.*local-stt" gateway/internal/integration_test/openai_whisper_model_rewrite_test.go` == 1
    - `grep -rn "Phase 11.1" gateway/internal/integration_test/` resulta apenas em comentários de migrations 0028 (NÃO em fixture assert comments)
  </acceptance_criteria>
  <done>Fixtures consistentes com post-0029 shape (7 upstreams, 6 aliases — 4 herdados + whisper-local-stt + whisper-gemini-stt).</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-03 | Tampering | Migration 0029 partial apply | mitigate | Postgres DDL transactional dentro de `+goose StatementBegin/End`; ON CONFLICT DO NOTHING garante idempotência em UP; Down simétrico explicitamente keeps tier_priority col (additive — manual drop documented inline) |
</threat_model>

<verification>
go build ./gateway/...
go test ./gateway/internal/{upstreams,models,db,config}/ -count=1
go test -tags integration -run "TestIntegration_Migration0029|TestIntegration_05_ModelAlias|TestIntegration_UpstreamsLoader|TestIntegration_UpstreamsListener|TestIntegration_OpenAIWhisperModelRewrite|TestIntegration_Migration0026" ./gateway/internal/integration_test/ -count=1
</verification>

<success_criteria>
- Migration 0029 verde end-to-end (5 subtests)
- ResolveAllTier1 disponível para Wave 5 dispatcher consumir
- Resolver upstreamEnvVarMap inclui gemini-stt
- Fixture cascade revertida (5→7 upstreams); 1 test deletado, 6 adicionados
</success_criteria>

<output>
Create `.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-02-SUMMARY.md` when done.
</output>
