---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 07
subsystem: docs/runbook + env templates + project tech-stack
tags: [docs, runbook, env, stt, cascade, gemini, groq, whisper]
requires: [11.2-03, 11.2-04, 11.2-05, 11.2-06]
provides:
  - "docs/RUNBOOK-OPS.md — operator playbook for Gemini + Groq STT cascade"
  - "gateway/.env.example — gateway-side env parity (8 Phase 11.2 vars)"
  - "PROJECT.md tier-1 table updated with gemini-stt + groq-whisper rows"
affects:
  - "downstream Plan 08 (UAT script + cost ledger INSERT) — can grep RUNBOOK-OPS gate commands verbatim"
  - "operator onboarding — no need to decode 11.2-CONTEXT.md for key minting/rotation"
tech-stack:
  added:
    - "docs/RUNBOOK-OPS.md (new file — canonical STT cascade ops runbook)"
    - "gateway/.env.example (new file — Phase 11.2 env doc parity with pod/.env.example)"
  patterns:
    - "Slot-named env vars documented as the operator interface (provider name in DB row, slot number in env)"
    - "D-B10 verbatim gate command (docker inspect ... | grep AUTH_BEARER) propagated to both runbook + .env.example"
    - "Cooldown tuning documented as SQL UPDATE on upstreams.circuit_config (loader picks up via NOTIFY)"
key-files:
  created:
    - docs/RUNBOOK-OPS.md
    - gateway/.env.example
  modified:
    - .planning/PROJECT.md
key-decisions:
  - "Created docs/RUNBOOK-OPS.md as new file (path canonical in plan; no pre-existing OPS runbook in repo — gateway/docs/RUNBOOK-FAILOVER.md had only the tier-1 env table, not minting/rotation)"
  - "Created gateway/.env.example as the canonical Phase 11.2 doc artifact (gateway has .env.prod.example + .env.portainer.example for context-specific deploys; .env.example is the minimal Phase 11.2 contract that mirrors pod/.env.example)"
  - "PROJECT.md tier-1 STT row split into 3 (gemini prio=10, groq prio=15, openai prio=20) matching D-B5′ cascade order; per-provider hourly cost added to constraint reference"
requirements-completed: [D-B3, D-B7, D-B8, D-B9, D-B10, D-B11]
duration: 8 min
completed: 2026-06-07
---

# Phase 11.2 Plan 07: Documentation hygiene Summary

Wave 5 documentation hygiene — created `docs/RUNBOOK-OPS.md` (operator playbook
for Gemini + Groq STT minting, env wiring, D-B10 verbatim gate command,
cooldown tuning via `circuit_config` JSONB, rotation procedure) and
`gateway/.env.example` (8 Phase 11.2 env vars mirroring pod/.env.example
parity), plus updated `.planning/PROJECT.md` tier-1 table with `gemini-stt`
+ `groq-whisper` rows and per-provider hourly cost reference. Onboarding a
new operator no longer requires decoding `11.2-CONTEXT.md`.

**Duration:** 8 min (2026-06-07T18:02Z → 2026-06-07T18:10Z)
**Tasks completed:** 2/2
**Files created:** 2
**Files modified:** 1
**Commits:** 2

## Commits

| Hash | Message |
|------|---------|
| 82c6f3a | docs(11.2-07): add RUNBOOK-OPS — Gemini STT + Groq Whisper operator sections |
| a875a09 | docs(11.2-07): add gateway/.env.example + update PROJECT.md tech-stack |

## Tasks

### Task 1 — RUNBOOK-OPS Gemini + Groq sections + D-B10 verbatim gate

**Files:** `docs/RUNBOOK-OPS.md` (created, 270 lines)
**Commit:** `82c6f3a`

Structure of the new runbook:

1. **Header + slot-naming preamble table** — slot 1 = `gemini-stt` (Gemini-specific director), slot 2 = `groq-whisper` (REUSES OpenAI-compat director). Cross-references to existing runbooks (FAILOVER, DEPLOY, PRIMARY-POD, QUOTAS-BILLING) so the new file does not duplicate the cascade tier table.
2. **Gemini STT Operations** — AI Studio key minting steps + D-B9 deferred dev/prod split note, three slot 1 env vars, D-B10 verbatim gate command (`docker inspect ai-gateway-dev --format ... | grep UPSTREAM_STT_FALLBACK_1_AUTH_BEARER`), cooldown tuning via SQL `UPDATE ai_gateway.upstreams SET circuit_config = jsonb_set(...)` on the `gemini-stt` row (D-B11), quota dashboard URL, Pitfall reference table (1/2/3/6 from 11.2-RESEARCH.md), 6-step rotation procedure.
3. **Groq Whisper Operations** — console.groq.com key minting, three slot 2 env vars, D-B10 gate command for slot 2, REUSE adapter note (no Groq-specific code; only `upstreamEnvVarMap` mapping in `resolver.go`), default breaker cooldown, Groq pricing reference, rotation procedure (delegated to Gemini § with slot substitution).
4. **Cross-slot operator commands** — `gatewayctl upstream list --role=stt` inspection, `breaker force-open` for D-B13 UAT scenarios, RES-08 sensitive-tenant invariant reminder.

D-B10 command propagated verbatim so Plan 08 UAT driver can string-match.

### Task 2 — gateway/.env.example (parity) + PROJECT.md tech-stack

**Files:** `gateway/.env.example` (created, 71 lines), `.planning/PROJECT.md` (modified)
**Commit:** `a875a09`

`gateway/.env.example` documents the 8 Phase 11.2 vars in two blocks:

- **Pod-image pin + Whisper weights (3 vars):** `PRIMARY_SPEACHES_IMAGE` (D-B1 LOCKED default `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3`), `PRIMARY_WHISPER_WEIGHTS_KEY` (restored from Phase 11.1 revert), `PRIMARY_WHISPER_WEIGHTS_SHA256` (`<64-hex-digits-from-upload>` placeholder — FAIL-FAST passthrough per Plan 02 review action #6).
- **STT tier-1 cascade (6 vars):** Slot 1 (Gemini) + Slot 2 (Groq) — URL + AUTH_BEARER (empty placeholder + inline key minting URL comment) + MODEL (default). Includes inline D-B10 gate command snippet and `circuit_config.cooldown_s=120` reference for slot 1.

PROJECT.md updates:

- **Phase 06.9 tier-1 table** — split the single `openai-whisper` row into 3 rows (`gemini-stt` prio=10, `groq-whisper` prio=15, `openai-whisper` prio=20) matching D-B5′ cascade order. Each row documents the director (dedicated vs REUSE), the env-override key (`UPSTREAM_STT_FALLBACK_{1,2}_MODEL`), and the per-provider hourly cost.
- **Constraints cost reference** — replaced single OpenAI Whisper line with 3 lines (per-h + per-min for each provider).

## Deviations from Plan

### Path drift — created files at canonical paths (Rule 3)

Plan 07 frontmatter listed `docs/RUNBOOK-OPS.md` and `gateway/.env.example`
in `files_modified` even though neither file existed in the repo before
this plan. Other Phase 11.2 plans (Plan 02) targeted `pod/.env.example`
explicitly; Plan 07 implicitly distinguished gateway-side from pod-side.
Three observations resolved the drift without architectural ask:

1. **No existing OPS runbook.** `gateway/docs/RUNBOOK-FAILOVER.md` carries
   the cascade table but lacks key-minting / rotation sections. The
   canonical-path file does not collide with any existing doc.
2. **`gateway/.env.example` is the canonical Phase 11.2 doc artifact.**
   `gateway/.env.prod.example` and `gateway/.env.portainer.example` are
   deploy-context-specific; `.env.example` is the minimal contract
   mirroring `pod/.env.example` (already updated by Plan 02).
3. **Plan 02 used the same pattern.** Plan 02 SUMMARY lists
   `pod/.env.example` as a `modified` file — same template applied here
   for gateway-side parity.

Both files were created at the verbatim paths from the plan frontmatter.
No content collision with existing runbooks. Treated as Rule 3 (blocking
issue resolved without architectural change).

### Auto-fixed issues

None — verification grep checks (`done` criteria) all passed on first
write.

### No auth gates

Documentation-only plan. No external API calls.

## Self-Check: PASSED

- `docs/RUNBOOK-OPS.md` — exists (270 lines)
- `gateway/.env.example` — exists (71 lines)
- `.planning/PROJECT.md` — modified (gemini-stt + groq-whisper rows present; cost lines split)
- Commit `82c6f3a` — present in `git log`
- Commit `a875a09` — present in `git log`
- Task 1 verify grep — all 5 patterns matched
- Task 2 verify grep — all 5 patterns matched

## Threat Flags

None — `T-11.2-DOC` mitigation honored (`.env.example` documents NAME only,
all AUTH_BEARER values are empty placeholders with "operator: paste from
<dashboard>" comment).

## Known Stubs

None — runbook contains no placeholder sections; every documented procedure
is executable verbatim by the operator.
