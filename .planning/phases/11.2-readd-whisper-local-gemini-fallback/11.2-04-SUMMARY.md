---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 04
subsystem: infra
tags: [docker, dockerfile, supervisord, speaches, whisper, weights, pod-runtime, ghcr]

requires:
  - phase: 11.2-02
    provides: WEIGHTS_WHISPER_* env names + speaches service block in pod/docker-compose.yml
provides:
  - pod/primary/Dockerfile: speaches stage restored
  - pod/primary/supervisord.conf: [program:speaches] restored
  - pod/onstart.sh: WHISPER guards (download + verify SHA + healthcheck)
  - pod/scripts/download-weights.sh: whisper branch in 3-way parallel download
  - New primary-pod image built via GHA + auto-pulled by Portainer webhook
affects: [11.2-05 (probeSTT hits speaches /v1/audio/transcriptions on port 8001), 11.2-08 (live UAT)]

tech-stack:
  added: [Speaches container `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3` D-B1 LOCKED]
  patterns: [Triple-service supervisord: vllm + speaches + chatterbox in single pod]

key-files:
  created: []
  modified:
    - pod/primary/Dockerfile
    - pod/primary/supervisord.conf
    - pod/onstart.sh
    - pod/scripts/download-weights.sh

key-decisions:
  - "D-B1 LOCKED speaches image — no version bump in 11.2; future bump goes through new SPEC."
  - "Image tag :develop mutable — Portainer webhook auto-pulls on develop push. Digest pinning deferred until UAT signoff."
  - "Port mapping in pod docker-compose: 8001:8000 (speaches listens on container 8000); verified Phase 06.7/06.8 UAT topology."

patterns-established:
  - "Pod weight download 3-way parallel: llm + tts + whisper concurrent (per download-weights.sh)"

requirements-completed: [D-B1, D-B2]

duration: 30min (subagent) + auto-build via GHA + Portainer pull
completed: 2026-06-07
---

# Phase 11.2 Plan 04: pod primary speaches stage + WHISPER weight download

**Speaches service restored in primary pod template. New primary-pod :develop image auto-built + pulled. Webhook automatic — no manual digest update needed.**

## What was built

- Dockerfile speaches stage (D-B1 image `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3` LOCKED)
- supervisord [program:speaches] with HF_HUB_CACHE + WHISPER weights mount
- onstart.sh: WHISPER guard (skip download if SHA matches, fail fast if SHA mismatch)
- download-weights.sh: whisper branch added to 3-way parallel download (llm + tts + whisper)

## Commits

- 5be8798 `feat(11.2-04): restore speaches stage + supervisord program`
- b6060a3 `feat(11.2-04): restore WHISPER guards + 3-way parallel weight download`

## Operator gate (Task 3) — satisfied via auto-pipeline

Plan 04 originally required operator-driven image promote. In practice the deploy pipeline handles it:

1. develop push → GHA `build-primary-pod` workflow built `ghcr.io/ifixtelecom/converseai-primary-pod:develop` (run 27097938610 PASS).
2. Portainer webhook on ai-gateway-dev stack pulled new gateway image (which carries `PRIMARY_TEMPLATE_IMAGE=ghcr.io/.../primary-pod:develop` env).
3. Next pod provisioning cycle pulls fresh primary-pod image automatically (Vast.ai pull happens on instance create).

Digest pinning deferred — `:develop` tag is acceptable for dev environment. Promote to digest pin during Phase 11.2 UAT signoff (Plan 08) or before any prod cut.

## CI green

Run 27097938610 (build-primary-pod) PASS. Run 27099088086 (build-gateway) PASS.
