---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 08
type: live-uat-sheet
gateway_url: https://ai-gateway-dev.converse-ai.app
primary_gpu_shape: 2×RTX 3090 (Phase 06.8 final — allowlist 43803,55158 cap $0.60)
audit_db: bd_ai_gateway @ DigitalOcean managed (AI_GATEWAY_PG_DSN env)
runbook_ref: docs/RUNBOOK-OPS.md
driver: pod/smoke/uat-11.2.sh
---

# Phase 11.2 — Live UAT Sheet (6 cenários D-B13)

**Operator workflow:** populate each row's PASS/FAIL + timestamp + request_id + notes inline. After all 6 scenarios complete, sign at bottom + transfer outcome to `.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-VERIFICATION.md`.

## Pre-flight checklist

- [ ] `ssh vps-ifix-vm` — confirm `/opt/ai-gateway-dev/.env` carries the standing primary GPU shape (see MEMORY `primary-gpu-shape-06.8-final`): `PRIMARY_GPU_NAME="RTX 3090"`, `PRIMARY_NUM_GPUS=2`, `PRIMARY_VAST_MACHINE_ALLOWLIST=43803,55158`, `PRIMARY_VAST_PRICE_CAP_DPH=0.60`. Restore from `.bak` if drifted.
- [ ] D-B10 gate green on **both** slots: `docker inspect ai-gateway-dev --format '{{range .Config.Env}}{{println .}}{{end}}' | grep -E 'UPSTREAM_STT_FALLBACK_(1|2)_AUTH_BEARER='` returns 2 non-empty values.
- [ ] Cascade upstreams present: `psql $AI_GATEWAY_PG_DSN -c "SELECT name, tier, tier_priority FROM ai_gateway.upstreams WHERE role='stt' ORDER BY tier, tier_priority"` returns 4 rows ordered `local-stt|0|0`, `gemini-stt|1|10`, `groq-whisper|1|15`, `openai-whisper|1|20`.
- [ ] Export env vars to the operator shell: `NORMAL_KEY` (converseai), `SENSITIVE_KEY` (telefonia-uat in dev / telefonia in prod), `ADMIN_KEY`, `AI_GATEWAY_PG_DSN`.

## Execution order

The order minimizes pod cold-start cost (one force-up + one force-down).

1. Force-up primary (15min cold-start window first time).
2. Run cenários **1** + **5** (pod-ON path).
3. Force-down primary.
4. Run cenários **2** + **3** + **4** + **6** (pod-OFF path).
5. Cleanup: ensure all breakers force-closed; choose to keep pod down or up.

## Sign-off table

| # | Scenario | Expected | HTTP | audit_log.upstream | request_id | Operator | UTC ts | Notes |
|---|----------|----------|------|---------------------|------------|----------|--------|-------|
| 1 | pod-on-local-stt | 200 + upstream=local-stt | DEFERRED | DEFERRED | n/a | claude | 2026-06-08T00:28Z | Pod custom image (`converseai-primary-pod:develop`) failed to mark Ready in 9min cold-start window — actual_status stayed at `created` with no port mapping. Known Phase 6.6 issue (custom-image × Vast runtype=args silent bash exit, see STATE.md UAT 1-9). Aborted via force-down to avoid burn; Vast instance 39972397 destroyed clean ($0.025 spend). |
| 2 | pod-off-gemini | 200 + text plausível + upstream=gemini-stt | 200 | gemini-stt | 019ea491-dee7-7f0e-95dc-21a532b17bec | claude | 2026-06-08T00:11Z | PASS — text length 62 chars (plausible). |
| 3 | gemini-open-groq-takeover | 200 + upstream=groq-whisper | 200 | groq-whisper | 019ea498-76cb-751f-a463-5efe28290aca | claude | 2026-06-08T00:18Z | PASS after fix(11.2-08) groq URL strip `/v1`. |
| 4 | gemini-groq-open-openai-takeover | 200 + upstream=openai-whisper | 401 | openai-whisper | 019ea498-ce64-70b8-b897-fd929962980b | claude | 2026-06-08T00:18Z | PARTIAL — cascade routing VERIFIED (audit_log.upstream='openai-whisper' confirms dispatcher fall-through to slot-3). OpenAI key `sk-proj-***zkg6` returned 401 invalid_request_error. Operator must rotate `UPSTREAM_STT_OPENAI_AUTH_BEARER`. Cascade logic itself PASS. |
| 5 | sensitive-pod-on | 200 + upstream=local-stt + data_class=sensitive | DEFERRED | DEFERRED | n/a | claude | 2026-06-08T00:28Z | Same blocker as scenario 1 — pod did not reach Ready. |
| 6 | sensitive-pod-off-503 | 503 + body contains `upstream_unavailable_for_sensitive_tenant` | 503 | n/a (RES-08 hard-block) | n/a | claude | 2026-06-08T00:29Z | PASS — RES-08 hard-block verified for sensitive tenant. |

## Spend

- Vast total: ~$0.025 (1× RTX 3090 Norway @ $0.15/h × ~10min — pod cold-start aborted)
- Gemini quota usage (cenário 2): trivial (1.7s transcription)
- Groq quota usage (cenário 3): trivial (1 request, 5s WAV)
- OpenAI Whisper usage (cenário 4): n/a (401 before billing)

## Cleanup state at end

- [x] All breakers closed (`gatewayctl breaker list`: openai-whisper / local-embed / local-llm / local-stt / local-tts / openrouter-chat / voice-api-piper all OBSERVATION_*; gemini-stt + groq-whisper plain OBSERVATION; no force overrides remaining).
- [x] Pod state: `asleep` (Vast instance 39972397 destroyed; 0 active instances).

## Outcome

- 3/6 PASS (cenários 2, 3, 6) + 1 PARTIAL routing-only (cenário 4) + 2 DEFERRED pod-blocked (cenários 1, 5).
- Cascade routing primary objective achieved: local-stt → gemini-stt → groq-whisper → openai-whisper fall-through verified in audit_log per-scenario; RES-08 sensitive hard-block verified.
- Carry-forward to `/gsd:plan-phase 11.2 --gaps`:
  - Scenarios 1+5 (pod-ON): blocked by Phase 6.6 custom-image cold-start flakiness (out of scope for Phase 11.2 cascade verification, but must run for Phase completion).
  - Scenario 4 (openai-whisper takeover HTTP 200): blocked by operator OpenAI key rotation — once key rotated, scenario will pass without re-deploy.

**Signed:** claude-opus-4-7 (executor agent)
**Date:** 2026-06-08
