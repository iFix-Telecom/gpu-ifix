---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 02
subsystem: gateway/config + pod/env
tags: [config, env, stt, cascade, gemini, groq, whisper]
requires: [11.2-01]
provides:
  - "cfg.PrimaryWhisperWeightsKey / SHA256 (revert 11.1 D-A4)"
  - "cfg.PrimarySpeachesImage (D-B1 LOCKED pin)"
  - "cfg.UpstreamSTTFallback1{URL,AuthBearer,Model} (slot 1 = Gemini)"
  - "cfg.UpstreamSTTFallback2{URL,AuthBearer,Model} (slot 2 = Groq Whisper)"
affects:
  - "downstream Plan 03 (reconciler) — can wire OverrideTier0(\"stt\") via cfg.PrimarySpeachesImage"
  - "downstream Plan 06 (director) — can resolve env-override via cfg.UpstreamSTTFallback{1,2}Model"
tech-stack:
  added: []
  patterns:
    - "envOr loader for slot-named cascade fields (mirrors UPSTREAM_LLM_OPENROUTER_*)"
    - "FAIL-FAST SHA passthrough (Whisper SHA256 has no envOr default)"
    - "Secret hygiene via json:\"-\" tag on AuthBearer fields (T-11.2-CFG)"
key-files:
  created: []
  modified:
    - gateway/internal/config/config.go
    - gateway/internal/config/config_test.go
    - pod/.env.example
    - pod/docker-compose.yml
key-decisions:
  - "Slot-named env vars (UPSTREAM_STT_FALLBACK_{1,2}_*) — provider-agnostic; operator can swap without renaming envs (CONTEXT replan 2026-06-07)"
  - "PrimaryWhisperWeightsSHA256 FAIL-FAST passthrough — no envOr default, downstream buildPrimaryCreateRequest rejects empty at build time (reviews consensus action #6)"
  - "Speaches image pinned via PRIMARY_SPEACHES_IMAGE env override (D-B1 LOCKED default ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3)"
  - "AuthBearer fields tagged json:\"-\" per T-11.2-CFG threat model"
requirements-completed: [D-B1, D-B2, D-B3, D-B7, D-B8]
duration: 6 min
completed: 2026-06-07
---

# Phase 11.2 Plan 02: Config fields + pod env wiring Summary

Restored 2 Whisper weight Config fields reverted in Phase 11.1 + added 7 new fields (PrimarySpeachesImage + 6 UPSTREAM_STT_FALLBACK_{1,2}_* slot fields for Gemini + Groq cascade tier-1) with operator env-overrides; pod/.env.example + pod/docker-compose.yml restored to mirror the new cascade shape.

**Duration:** 6 min (2026-06-07T15:34Z → 2026-06-07T15:40Z)
**Tasks completed:** 2/2
**Files modified:** 4
**Commits:** 3

## Commits

| Hash | Message |
|------|---------|
| 88b4938 | test(11.2-02): add failing tests for whisper weights + STT fallback slots |
| 470b023 | feat(11.2-02): restore PRIMARY_WHISPER_WEIGHTS_* + add 6 UPSTREAM_STT_FALLBACK_{1,2}_* Config fields |
| 28d055a | feat(11.2-02): restore pod speaches service + WEIGHTS_WHISPER + 6 STT fallback envs |

## Tasks

### Task 1 — Restore PRIMARY_WHISPER_WEIGHTS_* + add 9 new Config fields (TDD)

**Files:** `gateway/internal/config/config.go`, `gateway/internal/config/config_test.go`

**RED (88b4938):** Added `TestConfig_Defaults_Phase112` + `TestConfig_Defaults_Phase112_EnvOverride` to `config_test.go` asserting:
- Defaults for restored `PrimaryWhisperWeightsKey` (`whisper-large-v3/v1.0.0/model.tar.gz`) + `PrimaryWhisperWeightsSHA256` (empty, FAIL-FAST passthrough).
- Default for new `PrimarySpeachesImage` (D-B1 `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3`).
- Slot 1 defaults (Gemini): URL `https://generativelanguage.googleapis.com/v1beta`, Model `gemini-2.5-flash-lite`, AuthBearer empty.
- Slot 2 defaults (Groq): URL `https://api.groq.com/openai/v1`, Model `whisper-large-v3`, AuthBearer empty.
- Env-override path for all 9 fields (model swap, URL swap, bearer injection).

RED compile failure confirmed: `cfg.PrimaryWhisperWeightsKey undefined ...` (build failed as expected).

**GREEN (470b023):** Added:
1. 9 struct fields to `Config` (2 restored whisper + 1 image + 6 STT slot fields).
2. Loader entries using existing `envOr` pattern (FAIL-FAST SHA via `os.Getenv` direct).
3. `json:"-"` tag on both AuthBearer fields per T-11.2-CFG (verified `grep -E 'json:"-"' ... | grep UpstreamSTTFallback | wc -l` = 2).

**Verification:**
- `cd gateway && go test ./internal/config/... -run 'TestConfig_Defaults_Phase112' -count=1` → PASS (2 sub-tests).
- Full config suite: `cd gateway && go test ./internal/config/... -count=1` → PASS (no regression).
- `cd gateway && go build ./...` → clean build.
- `grep -c 'UpstreamSTTFallback1\|UpstreamSTTFallback2\|PrimaryWhisperWeights\|PrimarySpeachesImage' gateway/internal/config/config.go` → 19 (≥ 9 required).

### Task 2 — Restore pod/.env.example + pod/docker-compose.yml

**Files:** `pod/.env.example`, `pod/docker-compose.yml`

**Action:**
1. `pod/.env.example`:
   - Updated weights-dir header comment to reverse 11.1 STT-removal language.
   - Restored `WEIGHTS_WHISPER_KEY=whisper-large-v3/v1.0.0/model.tar.gz` + `WEIGHTS_WHISPER_SHA256=<64-hex-digits-from-upload>` lines.
   - Added new Phase 11.2 cascade section (6 envs) with operator comments pointing at AI Studio + Groq console.
2. `pod/docker-compose.yml`:
   - Updated header comment (4-service pod, STT re-added).
   - Inserted `speaches` service block (image `${PRIMARY_SPEACHES_IMAGE:-ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3}`, port `8001:8000`, volume `${WEIGHTS_DIR:-/weights}/whisper:/weights/whisper:rw`, `HF_HUB_CACHE=/weights/whisper` per Phase 06.8 fix-iii Pitfall 4 workaround).
   - Restored `SPEACHES_URL` + `SPEACHES_MODEL` envs + `depends_on: speaches` in health-bridge.

**Verification:**
- `grep -q 'WEIGHTS_WHISPER_KEY' pod/.env.example` → OK.
- `grep -q 'WEIGHTS_WHISPER_SHA256' pod/.env.example` → OK.
- `grep -q 'UPSTREAM_STT_FALLBACK_1_AUTH_BEARER' pod/.env.example` → OK.
- `grep -q 'UPSTREAM_STT_FALLBACK_2_AUTH_BEARER' pod/.env.example` → OK.
- `grep -q 'speaches-ai/speaches:0.9.0-rc.3' pod/docker-compose.yml` → OK.
- `python3 -c 'import yaml; yaml.safe_load(open("pod/docker-compose.yml"))'` → YAML valid.

**Note on `gateway/docker-compose.yml`:** Orchestrator pre-applied UPSTREAM_STT_FALLBACK_{1,2}_* env mappings in commit 8d8c681 (per executor prompt instruction "DO NOT re-edit env mappings; only confirm"). Verified via `grep -c UPSTREAM_STT_FALLBACK gateway/docker-compose.yml` → 6.

## Deviations from Plan

### [Rule 1 - Plan Bug] PLAN done-criteria grep `8001:8001` did not match verbatim source

- **Found during:** Task 2 verification.
- **Issue:** PLAN's `<verify>` block uses `grep -q '8001:8001' pod/docker-compose.yml` but the verbatim source from `git show 09b5a7c^:pod/docker-compose.yml` (which the PLAN's `<action>` instructs to restore verbatim) uses `"8001:8000"` (host 8001 → container 8000; speaches listens on 8000 internally — same convention as the `llama` service).
- **Fix:** Followed the verbatim source `"8001:8000"`. Used `grep -q '8001:8000'` in self-verification instead. This matches the original Phase 06.7/06.8 UAT-validated topology.
- **Files modified:** `pod/docker-compose.yml`.
- **Verification:** `python3 -c 'import yaml; yaml.safe_load(open("pod/docker-compose.yml"))'` → valid; service block compiles with both `PRIMARY_SPEACHES_IMAGE` and the SHA-pinned default.
- **Commit:** 28d055a.

**Total deviations:** 1 auto-fixed (plan verification command vs. verbatim source mismatch).
**Impact:** None on downstream — the pod/.env.example + docker-compose.yml restoration matches the verbatim Phase 06.7/06.8 shape that Plans 03 (reconciler) and Plan 05 (onstart) downstream code expects.

## Authentication Gates

None.

## Known Stubs

None — all fields are wired end-to-end (struct → loader → tests cover both default + override paths). Downstream Plans 03 and 06 will consume `cfg.PrimaryWhisperWeightsKey/SHA256/PrimarySpeachesImage` + `cfg.UpstreamSTTFallback{1,2}{URL,AuthBearer,Model}` directly.

## Threat Flags

None — only T-11.2-CFG (Information Disclosure on AuthBearer) was in scope, and mitigated as planned via `json:"-"` tags on `UpstreamSTTFallback1AuthBearer` + `UpstreamSTTFallback2AuthBearer` (`grep -E 'json:"-"' gateway/internal/config/config.go | grep UpstreamSTTFallback | wc -l` → 2).

## Next Steps

Ready for **Plan 03** — reconciler `OverrideTier0("stt")`/`RestoreTier0("stt")` restoration consuming `cfg.PrimarySpeachesImage` and `cfg.PrimaryWhisperWeights{Key,SHA256}`.

## Self-Check: PASSED

- [x] `gateway/internal/config/config.go` modified (verified via `git log --oneline -- gateway/internal/config/config.go | head -1` shows 470b023)
- [x] `gateway/internal/config/config_test.go` modified (88b4938)
- [x] `pod/.env.example` modified (28d055a)
- [x] `pod/docker-compose.yml` modified (28d055a)
- [x] Commits 88b4938, 470b023, 28d055a present on `develop`
- [x] All `<done>` criteria from Task 1 met (≥9 field grep, tests green, json:"-" present)
- [x] All `<done>` criteria from Task 2 met (4 grep checks, YAML valid; verify-grep `8001:8001` substituted with `8001:8000` per verbatim source — deviation documented above)
- [x] `<verification>` block: 9 fields code-present (19 grep hits), 8 env vars (2 weights + 6 STT) documented in .env.example, speaches service block restored, `grep -E 'json:"-"' ... | grep UpstreamSTTFallback` → 2 lines
- [x] `<success_criteria>` met: downstream Plans 03 + 06 can reference all new Config fields without 502s
