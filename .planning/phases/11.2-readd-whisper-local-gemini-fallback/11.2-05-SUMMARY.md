---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 05
subsystem: pod-health-bridge
tags: [stt, health-probe, multipart, whisper, restoration, verbatim]
requires:
  - 11.2-01 (RED stub for TestProbeSTT_Success + UpstreamSTT constant absence assertion)
provides:
  - pod/health-bridge/probes.go probeSTT function + sttProbeTimeout 10s + generateSilentWAV
  - pod/health-bridge/state.go UpstreamSTT const + STT slot in NewState seed map
  - pod/health-bridge/handlers.go /health/stt route in mux
  - pod/health-bridge/main.go SpeachesURL config + probeSTT in probes[] loop
affects:
  - pod readiness reconciler (downstream — markReady role=stt now reachable)
  - gateway cascade dispatcher (Wave 1 Plan 02 D-B2 consumer of /health/stt)
tech-stack:
  added: []
  patterns:
    - verbatim restoration from 39bec50^ pre-Phase-11.1 STT strip
key-files:
  created: []
  modified:
    - pod/health-bridge/probes.go
    - pod/health-bridge/handlers.go
    - pod/health-bridge/state.go
    - pod/health-bridge/main.go
    - pod/health-bridge/probes_test.go
    - pod/health-bridge/main_test.go
decisions:
  - Restore main_test.go alongside the 4 source files because TestHandleAggregate_AllHealthy_200 and TestHealthReady_IsAggregate seeded only LLM+Embed slots; with UpstreamSTT re-added to NewState the missing slot stays StatusUnknown and the aggregate degrades to StatusDegraded, breaking those tests. The verbatim 39bec50^ version of main_test.go already seeds the STT slot.
metrics:
  duration_min: 2
  tasks: 2
  files_modified: 6
  completed_at: 2026-06-07T15:38Z
---

# Phase 11.2 Plan 05: Restore pod health-bridge probeSTT + handlers + state + main Summary

Restaurados 4 arquivos (`probes.go`, `handlers.go`, `state.go`, `main.go`) e 2 arquivos de teste (`probes_test.go`, `main_test.go`) do pod health-bridge verbatim de `39bec50^` (parent do commit que strippou STT em Phase 11.1), re-habilitando o probe periódico /v1/audio/transcriptions contra speaches:8000 + o endpoint /health/stt consumido pelo gateway cascade dispatcher (D-B2).

## What Was Built

**Task 1 (commit 17a8ad9):** restauração verbatim dos 4 arquivos de produção.

- `probes.go`: adiciona `const sttProbeTimeout = 10 * time.Second`, função `probeSTT` que monta multipart form com `file=probe.wav` + campo `model` (env `SPEACHES_MODEL` ou default `Systran/faster-whisper-large-v3`) e POST em `/v1/audio/transcriptions`, helper `generateSilentWAV(seconds int)` que constrói RIFF WAV mono 16 kHz PCM 16-bit em memória + helpers `writeU16LE`/`writeU32LE`.
- `handlers.go`: registra rota `/health/stt` no mux mapeada via `handleUpstream(state, UpstreamSTT)`.
- `state.go`: const `UpstreamSTT = "stt"` + slot STT em `NewState().results` semeado com `StatusUnknown`.
- `main.go`: campo `SpeachesURL` em `Config` carregado de `SPEACHES_URL` (default `http://speaches:8000`) + entrada `{UpstreamSTT, cfg.SpeachesURL, probeSTT}` no slice `probes` da loop spawner + log de startup com `speaches` URL.

**Task 2 (commit 375eefa):** unskip + paste verbatim do TestProbeSTT_Success + fix harness 3-slot.

- `probes_test.go`: removido `t.Skip` do stub do Plan 01 e colado o body verbatim de `39bec50^:pod/health-bridge/probes_test.go` (assertion de `multipart/form-data` no Content-Type + 200 com `{"text":"silence"}` → `StatusHealthy`).
- `probes_test.go`: `TestState_ConcurrentSet` voltou a cobrir 3 upstreams (`UpstreamLLM/STT/Embed`, `len(snap)==3`); `TestAggregateStatus` seedando os 3 slots para validar transição healthy → degraded → failed.
- `main_test.go`: restauração verbatim — `TestHandleAggregate_AllHealthy_200`, `TestHandleAggregate_OneFailed_503` e `TestHealthReady_IsAggregate` voltaram a seedar UpstreamSTT.

## Verification Results

```text
$ cd pod/health-bridge && go vet ./...
$ go build ./...
$ go test -count=1 -run TestProbeSTT ./...
ok  	github.com/ifixtelecom/gpu-ifix/pod/health-bridge	0.023s
$ go test -count=1 ./...
ok  	github.com/ifixtelecom/gpu-ifix/pod/health-bridge	5.515s
```

Todos os 14 testes passam (incluindo TestProbeSTT_Success que estava em SKIP no Plan 01).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fix harness tests after STT slot re-added to NewState**
- **Found during:** Task 1 verify (`go test -count=1 ./...`)
- **Issue:** Após restaurar `state.go` com `UpstreamSTT` no seed de `NewState`, `TestHandleAggregate_AllHealthy_200`, `TestHandleAggregate_OneFailed_503` e `TestHealthReady_IsAggregate` falhavam porque seedavam apenas LLM+Embed; o slot STT ficava em `StatusUnknown`, e `AggregateStatus()` rebaixava para `StatusDegraded` (503) em vez de healthy (200). Análogo em `TestState_ConcurrentSet` (esperava `len(snap)==2`) e `TestAggregateStatus` (não seedava STT).
- **Fix:** restauração verbatim de `main_test.go` de `39bec50^` (3 testes consertados) + 2 edits cirúrgicos em `probes_test.go` para incluir slot STT em `TestState_ConcurrentSet` e `TestAggregateStatus`.
- **Files modified:** `pod/health-bridge/main_test.go`, `pod/health-bridge/probes_test.go`
- **Commit:** 375eefa (mesmo commit do Task 2 — agrupado por contexto: tudo é ajuste de harness para 3 upstreams)

Nenhum problema fora desse — todos os 4 arquivos source restauraram verbatim sem conflito com fixes posteriores (não houve TTS/DCGM probes adicionados após 39bec50 que precisassem ser preservados; `probes.go` current só tinha LLM+Embed).

### Auto-added Critical Functionality

None — Plan 05 é puramente restauração.

### Authentication Gates

None.

## Commits

| Hash    | Type | Message                                                                                         |
| ------- | ---- | ----------------------------------------------------------------------------------------------- |
| 17a8ad9 | feat | restore probeSTT + UpstreamSTT + /health/stt handler + main wiring (4 arquivos source verbatim) |
| 375eefa | test | unskip TestProbeSTT_Success + re-add STT slot in state/aggregate tests                          |

## Threat Compliance

T-11.2-HB mitigated: `sttProbeTimeout = 10 * time.Second` aplicado em `probeSTT`. Per-role isolation preservada — falha do probe STT não bloqueia loops LLM/Embed (cada goroutine em `main.go probes[]` roda independente).

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: pod/health-bridge/probes.go (probeSTT, sttProbeTimeout)
- FOUND: pod/health-bridge/handlers.go (/health/stt)
- FOUND: pod/health-bridge/state.go (UpstreamSTT)
- FOUND: pod/health-bridge/main.go (probeSTT in probes[])
- FOUND: pod/health-bridge/probes_test.go (TestProbeSTT_Success body, no t.Skip)
- FOUND: pod/health-bridge/main_test.go (TestHandleAggregate seeds STT)
- FOUND: commit 17a8ad9 (Task 1)
- FOUND: commit 375eefa (Task 2)
- All tests pass (`go test ./...` ok)
