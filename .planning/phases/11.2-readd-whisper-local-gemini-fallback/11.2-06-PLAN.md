---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 06
type: execute
wave: 5
depends_on: ["11.2-02", "11.2-03"]
files_modified:
  - gateway/internal/proxy/gemini_stt_director.go
  - gateway/internal/proxy/gemini_stt_director_test.go
  - gateway/internal/proxy/dispatcher.go
  - gateway/internal/proxy/dispatcher_test.go
  - gateway/cmd/gateway/main.go
autonomous: true
requirements: [D-B3, D-B4, D-B5, OR-FIX-PARITY, RES-GEMINI-ADAPTER]
threat_refs: [T-11.2-01, T-11.2-02, T-11.2-05]

must_haves:
  truths:
    - "gemini_stt_director.go expõe BuildGeminiSTTDirector(upstream, apiKey, resolver, upstreamName, log) (Director, ModifyResponse)"
    - "Director rewrites: URL path → /v1beta/models/<model>:generateContent; header Authorization removido + x-goog-api-key setado; body multipart → JSON {contents:[{parts:[text, inline_data{mime_type,data:base64}]}]}; Content-Type → application/json"
    - "ModifyResponse extrai candidates[0].content.parts[*].text concatenated → {\"text\": \"...\"} (OpenAI Whisper shape)"
    - "Gemini error envelope traduzido para OpenAI error envelope com HTTP 502 + type=upstream_error"
    - "Áudio >18MB rejeitado com HTTP 413 (Pitfall 2 inflation headroom)"
    - "MIME type allowlist {audio/wav, audio/mp3, audio/ogg, audio/flac, audio/aiff, audio/aac, audio/x-wav}; demais → HTTP 415 (T-11.2-05 normalization)"
    - "dispatcher.go troca `cfg.Loader.Resolve(cfg.Role, 1)` (single tier-1) por loop iterando ResolveAllTier1 + breakers CLOSED em tier_priority ASC"
    - "main.go wireup: instancia BuildGeminiSTTDirector + ReverseProxy + registra como `gemini-stt` no proxy map"
    - "API key NUNCA aparece em log lines (presence-only logging — T-11.2-02)"
  artifacts:
    - path: gateway/internal/proxy/gemini_stt_director.go
      provides: Gemini STT director adapter (multipart → generateContent inline_data)
    - path: gateway/internal/proxy/dispatcher.go
      provides: Multi-tier-1 fallthrough chain
    - path: gateway/cmd/gateway/main.go
      provides: gemini-stt proxy wireup
---

<objective>
Wave 5 — CORE NEW COMPONENT: Gemini STT director adapter + dispatcher multi-tier-1 chain. Esta é a primeira tier-1 upstream do projeto cujo wire shape difere de OpenAI (JSON generateContent vs multipart whisper). Esta é também a primeira mudança ao dispatcher que itera múltiplos candidatos tier-1 antes de declarar `service_unavailable`.

Output: 1 novo arquivo Director + tests, dispatcher modificado, main.go wireup do novo proxy, breaker chain {gemini-stt → openai-whisper} funciona end-to-end em integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-CONTEXT.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
@gateway/internal/proxy/openai_whisper_director.go
@gateway/internal/proxy/dispatcher.go
@gateway/internal/upstreams/loader.go

<interfaces>
<!-- RESEARCH Pattern 2 contém SKELETON completo do gemini_stt_director.go — usar VERBATIM como starting point -->
<!-- RESEARCH Pattern 3 contém dispatcher.go modificação SKELETON -->
<!-- openai_whisper_director.go (já em context) prove o pattern BuildDirector+wrapper -->
<!-- Pitfall 1: gemini-stt circuit_config.cooldown_s deve ser HIGH (120s) — settear via migration row OU env override; documentar em main.go comment -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gemini STT director — multipart→JSON + response flatten + MIME validation + size cap</name>
  <files>gateway/internal/proxy/gemini_stt_director.go, gateway/internal/proxy/gemini_stt_director_test.go</files>
  <read_first>
    - RESEARCH Pattern 2 (linhas ~194-427 do 11.2-RESEARCH.md — código skeleton completo)
    - gateway/internal/proxy/openai_whisper_director.go (pattern parity)
    - gateway/internal/proxy/director.go (BuildDirector + clientAuthHeaders strip — verificar x-goog-api-key NÃO está stripado, Pitfall 3)
  </read_first>
  <behavior>
    - TestGeminiSTTDirector_RewritesURL: input r.URL.Path="/v1/audio/transcriptions", model="gemini-2.5-flash-lite" via resolver → r.URL.Path == "/v1beta/models/gemini-2.5-flash-lite:generateContent"
    - TestGeminiSTTDirector_SetsAPIKeyHeader: r.Header.Get("x-goog-api-key") == apiKey AND r.Header.Get("Authorization") == "" (Pitfall 3)
    - TestGeminiSTTDirector_MultipartToInlineData: multipart com part `file`={raw_bytes} mime="audio/wav" → r.Body JSON contém `inline_data.mime_type="audio/wav"` + `data=base64(raw_bytes)`; prompt text="Transcribe this audio. Return only the transcription text, no commentary."
    - TestGeminiSTTDirector_FlattenResponse: Gemini response `{"candidates":[{"content":{"parts":[{"text":"oi mundo"}]}}]}` → resp body == `{"text":"oi mundo"}`
    - TestGeminiSTTDirector_FlattenResponse_MultiPart: multi-part response → text concatenated (Assumption A5)
    - TestGeminiSTTDirector_ErrorEnvelopeTranslate: Gemini `{"error":{"code":429,"message":"quota","status":"RESOURCE_EXHAUSTED"}}` → resp 502 com body `{"error":{"message":"quota","type":"upstream_error","code":"RESOURCE_EXHAUSTED"}}`
    - TestGeminiSTTDirector_OversizeReject: audio > 18MB → director seta sentinel/wrapper retorna 413 com OpenAI envelope (Pitfall 2)
    - TestGeminiSTTDirector_UnsupportedMIME: part Content-Type="audio/m4a" (not in allowlist) → 415
    - TestGeminiSTTDirector_NormalizeAudioXWav: mime "audio/x-wav" rewrite para "audio/wav" antes do POST (T-11.2-05 normalization)
    - TestGeminiSTTDirector_ResolverWiresModelEnvOverride: env UPSTREAM_STT_GEMINI_MODEL="gemini-2.5-flash" → URL path usa esse model (cita OR-FIX SEED-004 — esse director MUST wire resolver desde D1)
    - TestGeminiSTTDirector_NoLeakedAPIKeyInLogs: log.Output captured contém substring "apiKey" como `"present":true` mas NÃO contém o valor cru (T-11.2-02)
  </behavior>
  <action>
    Criar `gemini_stt_director.go` usando o SKELETON RESEARCH Pattern 2 como base. Modificações sobre o skeleton:

    1. **MIME allowlist (T-11.2-05 + ASVS V5)**: definir `var geminiAllowedMIME = map[string]string{ "audio/wav":"audio/wav", "audio/x-wav":"audio/wav", "audio/mp3":"audio/mp3", "audio/mpeg":"audio/mp3", "audio/ogg":"audio/ogg", "audio/flac":"audio/flac", "audio/aiff":"audio/aiff", "audio/aac":"audio/aac" }`. Em extractAudioFromMultipart, após detectar mimeType, lookup no map; rejeitar se ausente; normalize via valor do map antes do POST.

    2. **Oversize handling (Pitfall 2)**: definir `geminiMaxAudioPreBase64 = 18 * 1024 * 1024` (18MB safe pre-base64 cutoff). NO director, se `len(audioBytes) > geminiMaxAudioPreBase64`, NOT forward — sinalizar via wrapper (mesmo pattern WhisperAbortGuard linha 290 openai_whisper_director.go). Adicionar `GeminiSizeGuard(inner http.Handler, log) http.Handler` que pre-flight le body, detecta oversize/MIME violation, retorna HTTP 413 (oversize) ou 415 (MIME) com OpenAI envelope sem chamar inner.

    3. **Resolver wireup (OR-FIX-PARITY — Open Question 3)**: model = resolver.Resolve("whisper", upstreamName) — env override UPSTREAM_STT_GEMINI_MODEL wins (resolver upstreamEnvVarMap from Plan 02 Task 2). Se vazio, default "gemini-2.5-flash-lite". **Test integration-style** valida que URL path contém o resolved model NÃO `{model}` literal — prevenção SEED-004 bug pattern.

    4. **Header security (T-11.2-02 + Pitfall 3)**:
       - `r.Header.Del("Authorization")` ANTES de set x-goog-api-key
       - Comentário inline: `// CRITICAL: header name "x-goog-api-key" NOT "X-API-Key" — esta última seria strippada por clientAuthHeaders em director.go:32`
       - log calls usam `slog.Info("gemini_stt_director request", "upstream", name, "model", model, "api_key_present", apiKey != "")` — NUNCA logam apiKey value

    5. **Pitfall 1 (breaker chain)**: documentação inline mencionando que `gemini-stt` circuit_config.cooldown_s deve ser HIGH (120s) via migration row OU UPSTREAM_STT_GEMINI_COOLDOWN_S env override em Phase 11.3+. Para 11.2, anotar como `// TODO Phase 11.3 — Pitfall 1 cooldown tuning`. NÃO bloqueia esse plan.

    6. Tests acima — 12 tests cobrindo behavior. Usar `httptest.NewRecorder` para director (passa Request, observa rewrites); fake `httputil.ReverseProxy` para response modify path.
  </action>
  <verify>
    <automated>go test ./gateway/internal/proxy/ -count=1 -run "TestGeminiSTTDirector"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "func BuildGeminiSTTDirector" gateway/internal/proxy/gemini_stt_director.go == 1
    - grep -c "func GeminiSizeGuard" gateway/internal/proxy/gemini_stt_director.go == 1
    - grep -c "x-goog-api-key" gateway/internal/proxy/gemini_stt_director.go ≥ 2 (set + tests)
    - grep -c 'r.Header.Del("Authorization")' gateway/internal/proxy/gemini_stt_director.go == 1
    - grep -cE "log\\.(Info|Debug|Warn).*apiKey[^_]" gateway/internal/proxy/gemini_stt_director.go == 0 (no leak)
    - 12 testes TestGeminiSTTDirector_* passam
  </acceptance_criteria>
  <done>Director compila + 12 tests verdes; Gemini wire-shape conversion bidirectional funcional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatcher multi-tier-1 chain — iterar ResolveAllTier1 + breaker CLOSED</name>
  <files>gateway/internal/proxy/dispatcher.go, gateway/internal/proxy/dispatcher_test.go</files>
  <read_first>
    - gateway/internal/proxy/dispatcher.go:255-275 (single tier-1 lookup atual)
    - RESEARCH Pattern 3 (dispatcher modification skeleton)
  </read_first>
  <behavior>
    - TestDispatcher_TierOneFallthrough_GeminiClosed: tier-0 OPEN, gemini-stt CLOSED, openai-whisper CLOSED → dispatchTo("gemini-stt") chamado UMA vez; openai-whisper NÃO chamado
    - TestDispatcher_TierOneFallthrough_GeminiOpen: tier-0 OPEN, gemini-stt OPEN, openai-whisper CLOSED → dispatchTo("openai-whisper") (safety net hits)
    - TestDispatcher_TierOneFallthrough_AllOpen: tier-0 OPEN, gemini-stt OPEN, openai-whisper OPEN → 503 service_unavailable
    - TestDispatcher_TierOneFallthrough_NoCandidates: ResolveAllTier1("stt") retorna [] (degenerado role) → 503 com "no fallback configured"
    - TestDispatcher_BackwardCompat_LLM: role=llm tem single tier-1 (openrouter-chat) → dispatchTo("openrouter-chat") quando tier-0 OPEN — chain degenera para single-candidate path sem regressão
    - TestDispatcher_TierOneFallthrough_IterationOrder: assert order é ASC por TierPriority; flip TierPriority via mock loader (gemini=20, openai=10) → openai-whisper agora wins
  </behavior>
  <action>
    Em dispatcher.go ~linha 257-272 substituir:
    ```go
    t1, ok := cfg.Loader.Resolve(cfg.Role, 1)
    if !ok { /* 503 */ }
    if cfg.Breaker.EffectiveState(t1.Name) != gobreaker.StateClosed { /* 503 */ }
    cfg.dispatchTo(w, r, t1.Name, streaming, log)
    ```
    Por (RESEARCH Pattern 3 verbatim):
    ```go
    candidates := cfg.Loader.ResolveAllTier1(cfg.Role)
    if len(candidates) == 0 {
        httpx.WriteOpenAIError(w, http.StatusServiceUnavailable,
            "service_unavailable", "upstream_unavailable",
            "Primary upstream unavailable and no fallback configured for role.")
        return
    }
    for _, t1 := range candidates {
        if cfg.Breaker.EffectiveState(t1.Name) == gobreaker.StateClosed {
            cfg.dispatchTo(w, r, t1.Name, streaming, log)
            return
        }
    }
    httpx.WriteOpenAIError(w, http.StatusServiceUnavailable,
        "service_unavailable", "upstream_unavailable",
        "All inference upstreams are unavailable.")
    return
    ```

    Atualizar dispatcher_test.go com 6 testes acima. fakeLoader já existente — adicionar mock ResolveAllTier1 method.

    Threading: backward compat (Test 5) garante que llm/embed/tts paths (single tier-1 row tier_priority=0) continuam funcionando — chain de length 1 degrade graceful.
  </action>
  <verify>
    <automated>go test ./gateway/internal/proxy/ -count=1 -run "TestDispatcher_TierOneFallthrough|TestDispatcher_BackwardCompat"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ResolveAllTier1" gateway/internal/proxy/dispatcher.go == 1
    - grep -c "cfg.Loader.Resolve(cfg.Role, 1)" gateway/internal/proxy/dispatcher.go == 0 (substituído)
    - 6 testes acima passam
  </acceptance_criteria>
  <done>Dispatcher itera tier-1 chain; backward compat preserved; chain logic CLOSED→502/503 envelope correto.</done>
</task>

<task type="auto">
  <name>Task 3: Wire gemini-stt no main.go — proxy registration + GeminiSizeGuard handler chain</name>
  <files>gateway/cmd/gateway/main.go</files>
  <read_first>
    - gateway/cmd/gateway/main.go — onde openai-whisper proxy é montado + adicionado ao sttDispatcher.Proxies map
    - WhisperAbortGuard registration pattern (handler chain wrap)
  </read_first>
  <behavior>
    - gateway boots com UPSTREAM_STT_GEMINI_URL setado: log entry "gemini-stt proxy initialized" presente em startup logs
    - gateway boots SEM UPSTREAM_STT_GEMINI_AUTH_BEARER: proxy registrado mesmo assim mas log WARN "gemini-stt API key absent — director will pass empty key, requests will 401 — dispatcher falls through to safety net"
    - sttDispatcher.Proxies map contém entries ["openai-whisper", "gemini-stt"] (parity with new schema row)
  </behavior>
  <action>
    Em main.go, perto da seção que monta openai-whisper proxy:

    ```go
    // Phase 11.2 Plan 06 — gemini-stt director adapter wireup.
    geminiURL, _ := url.Parse(cfg.UpstreamSTTGeminiURL)
    geminiDir, geminiMod := proxy.BuildGeminiSTTDirector(geminiURL, cfg.UpstreamSTTGeminiAuthBearer, resolver, "gemini-stt", log)
    geminiProxy := &httputil.ReverseProxy{
        Director:       geminiDir,
        ModifyResponse: geminiMod,
        ErrorHandler:   standardErrorHandler,
    }
    geminiHandler := proxy.GeminiSizeGuard(geminiProxy, log)
    sttDispatcher.Proxies["gemini-stt"] = geminiHandler
    if cfg.UpstreamSTTGeminiAuthBearer == "" {
        log.Warn("gemini-stt API key absent — director passes empty key; dispatcher fall-through to openai-whisper safety net expected")
    } else {
        log.Info("gemini-stt proxy initialized", "url_present", true, "api_key_present", true)
    }
    ```

    Verificar que `sttDispatcher` (ou equivalent) já estava sendo construído para openai-whisper; se proxy map shape diferir, ajustar registration pattern conforme padrão existente.

    Test pattern: opcional — main_test (se existir) carrega cfg com 3 Gemini env vars set; verifica que startup completa sem panic; verifica via log probe que entry presente.
  </action>
  <verify>
    <automated>go build ./gateway/... && go test ./gateway/cmd/gateway/ -count=1 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "BuildGeminiSTTDirector" gateway/cmd/gateway/main.go == 1
    - grep -c "GeminiSizeGuard" gateway/cmd/gateway/main.go == 1
    - grep -c "gemini-stt" gateway/cmd/gateway/main.go ≥ 2
    - go build verde
  </acceptance_criteria>
  <done>Gemini proxy plug-in wireup pronto; dispatcher pode rotear via map.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-01 | Denial of Service | Gemini API quota exhaustion | mitigate | dispatcher chain fall-through → openai-whisper safety net (Task 2); circuit_config.cooldown_s HIGH (120s) doc as TODO Phase 11.3 (Pitfall 1) |
| T-11.2-02 | Information Disclosure | API key in director logs | mitigate | Task 1 presence-only log pattern; acceptance grep proíbe `log.*apiKey[^_]` literal; `r.Header.Del("Authorization")` antes set x-goog-api-key impede leak via proxy logs |
| T-11.2-05 | Spoofing/Validation | Audio MIME-type mismatch | mitigate | Task 1 mime allowlist + audio/x-wav→audio/wav normalization + HTTP 415 reject para tipos não suportados |
</threat_model>

<verification>
go build ./gateway/...
go test ./gateway/internal/proxy/ -count=1
go test ./gateway/cmd/gateway/ -count=1
</verification>

<success_criteria>
- gemini_stt_director.go novo + 12 tests verdes
- dispatcher.go multi-tier-1 chain + 6 tests verdes (incluindo backward compat)
- main.go wireup integrado
- Token leak grep clean
</success_criteria>

<output>
Create `.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-06-SUMMARY.md` when done.
</output>
