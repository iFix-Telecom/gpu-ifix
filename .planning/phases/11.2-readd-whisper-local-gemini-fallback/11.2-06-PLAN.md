---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 06
type: execute
wave: 4
depends_on: [11.2-03]
files_modified:
  - gateway/internal/proxy/gemini_stt_director.go
  - gateway/internal/proxy/gemini_stt_director_test.go
  - gateway/internal/proxy/openai_whisper_director.go
  - gateway/internal/proxy/dispatcher.go
  - gateway/internal/proxy/dispatcher_test.go
  - gateway/internal/models/resolver.go
  - gateway/internal/models/resolver_test.go
  - cmd/gateway/main.go
autonomous: true
requirements: [D-B4, D-B5′, D-B7, D-B8, D-B11, D-B14]
threat_refs: [T-11.2-DIR, T-11.2-CASCADE, T-11.2-AUTH]

must_haves:
  truths:
    - "POST /v1/audio/transcriptions com pod-OFF + gemini-stt breaker CLOSED roteia para Gemini API, response flatten para OpenAI envelope"
    - "Com gemini-stt OPEN + groq-whisper CLOSED roteia para Groq via OpenAI-compatible director (REUSE BuildOpenAIWhisperDirector D-B8)"
    - "Com gemini-stt + groq-whisper OPEN + openai-whisper CLOSED roteia para OpenAI safety net"
    - "Com all-OPEN retorna 503 upstream_unavailable"
    - "Header x-goog-api-key sempre presente em request a Gemini; Authorization sempre ausente (Pitfall 3)"
  artifacts:
    - path: gateway/internal/proxy/gemini_stt_director.go
      provides: "Director adapter multipart→JSON + ModifyResponse flatten Gemini→OpenAI envelope"
    - path: gateway/internal/proxy/dispatcher.go
      provides: "Multi-tier-1 fallthrough loop iterando ResolveAllTier1 + breaker check"
    - path: cmd/gateway/main.go
      provides: "Wireup proxy registry para gemini-stt + groq-whisper"
  key_links:
    - from: dispatcher tier-1 loop
      to: Loader.ResolveAllTier1
      via: breaker EffectiveState gating
      pattern: "gobreaker.StateClosed"
    - from: gemini_stt_director
      to: BuildDirector (parent)
      via: r.Header.Del Authorization THEN r.Header.Set x-goog-api-key
      pattern: "x-goog-api-key"
---

<objective>
Wave 4 — NEW director adapter gemini_stt_director.go (multipart→JSON + flatten resp), dispatcher.go multi-tier-1 loop, resolver.go upstreamEnvVarMap add 2 entries, openai_whisper_director.go canonicalAliasForUpstream add groq-whisper (D-B8 REUSE — NO new groq adapter), e wireup em cmd/gateway/main.go com 2 novos proxies.

Purpose: Core gateway code que materializa a cascade STT 3-upstream tier-1. Implementa D-B4 (Gemini wire), D-B5′ (cascade loop), D-B8 (Groq REUSE).
Output: 6 arquivos Go modificados; suite unit verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-CONTEXT.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-PATTERNS.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
@gateway/internal/proxy/openai_whisper_director.go
@gateway/internal/proxy/dispatcher.go
@gateway/internal/models/resolver.go
</context>

<interfaces>
From gateway/internal/proxy/openai_whisper_director.go:
  func BuildOpenAIWhisperDirector(upstream *url.URL, authBearer string, resolver *models.Resolver, upstreamName string, log *slog.Logger) func(*http.Request)
  var canonicalAliasForUpstream = map[string]string{"openai-whisper": "whisper"}

From gateway/internal/models/resolver.go:
  var upstreamEnvVarMap = map[string]string{"openrouter-chat":"UPSTREAM_LLM_OPENROUTER_MODEL", "openai-whisper":"UPSTREAM_STT_OPENAI_MODEL", "openai-embed":"UPSTREAM_EMBED_OPENAI_MODEL"}
  func (r *Resolver) Resolve(alias, upstreamName string) string

From gateway/internal/upstreams/loader.go (added Plan 03):
  func (l *Loader) ResolveAllTier1(role string) []UpstreamConfig
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create gemini_stt_director.go (Director + ModifyResponse adapter)</name>
  <files>gateway/internal/proxy/gemini_stt_director.go, gateway/internal/proxy/gemini_stt_director_test.go</files>
  <read_first>
    - gateway/internal/proxy/openai_whisper_director.go (analog: multipart extraction lines 174-268, BuildDirector wrap pattern lines 86-92)
    - 11.2-RESEARCH.md Pattern 2 (full director code lines 194-426)
    - PATTERNS.md gemini_stt_director.go section (header hygiene + breaker IsSuccessful contract)
    - 11.2-CONTEXT.md D-B4 + D-B7
    - 11.2-RESEARCH.md Pitfall 3 (x-goog-api-key NOT X-API-Key)
  </read_first>
  <behavior>
    - BuildGeminiSTTDirector(upstream *url.URL, apiKey string, resolver *models.Resolver, upstreamName string, log *slog.Logger) returns (Director func, ModifyResponse func)
    - Director: r.Header.Del Authorization, r.Header.Set x-goog-api-key <key>; parse multipart, extract file part bytes + MIME; base64 encode; build geminiRequest JSON contents parts (prompt text + inline_data); rewrite r.URL.Path to /v1beta/models/<model>:generateContent (model via resolver.Resolve whisper upstreamName, default gemini-2.5-flash-lite); set Content-Type application/json; r.Method POST
    - ModifyResponse: read body; unmarshal geminiResponse; if Error not nil → 502 with OpenAI envelope; else extract candidates[0].content.parts[0].text → write {"text":"<extracted>"}; update Content-Length
    - Audio >18MB pre-base64 (geminiMaxInlineBytes warning log + pass-through; 413 wrapper deferido a follow-up — director só warn)
    - Tests: header swap (x-goog-api-key set + Authorization deleted); multipart byte-fidelity round-trip; env override (UPSTREAM_STT_GEMINI_MODEL wins); flatten response; error envelope translation
  </behavior>
  <action>
    Implementar gemini_stt_director.go conforme code completo em 11.2-RESEARCH.md linhas 194-426. Estrutura: package proxy; types geminiInlineData/geminiPart/geminiContent/geminiRequest/geminiResponse; const geminiMaxInlineBytes = 20*1024*1024; const geminiTranscribePrompt; function BuildGeminiSTTDirector retorna pair (director, modifyResponse); helper extractAudioFromMultipart. CRITICAL inline comment: "header name is x-goog-api-key NOT X-API-Key — the latter is stripped by BuildDirector" (Pitfall 3).

    Tests gemini_stt_director_test.go: unskip Plan 01 stubs, implementar 6 sub-tests usando httptest.Server fake Gemini upstream: TestBuildGeminiSTTDirector_SetsXGoogApiKeyHeader, _StripsAuthorizationHeader, _MultipartToJSON_AudioBytesPreserved (base64 decode round-trip == original bytes), _ResolvesModelViaEnvOverride (set UPSTREAM_STT_GEMINI_MODEL via fake resolver, assert URL.Path contém model), _FlattenResponse (input candidates JSON, output {"text":"..."}), _TranslatesGeminiErrorEnvelope (Gemini error → 502 OpenAI envelope).
  </action>
  <verify>
    <automated>cd gateway && go test -count=1 ./internal/proxy/... -run TestBuildGeminiSTTDirector</automated>
  </verify>
  <done>
    6 sub-tests PASS; arquivo compila; pattern x-goog-api-key documented inline; geminiMaxInlineBytes constant presente.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolver upstreamEnvVarMap + openai_whisper_director canonicalAliasForUpstream extension</name>
  <files>gateway/internal/models/resolver.go, gateway/internal/models/resolver_test.go, gateway/internal/proxy/openai_whisper_director.go</files>
  <read_first>
    - gateway/internal/models/resolver.go lines 56-60 (upstreamEnvVarMap)
    - gateway/internal/proxy/openai_whisper_director.go line 66 (canonicalAliasForUpstream)
    - PATTERNS.md Shared Patterns Resolver Env-Override Contract
    - 11.2-CONTEXT.md D-B8 (Groq REUSES BuildOpenAIWhisperDirector via canonical alias map)
  </read_first>
  <behavior>
    - resolver.upstreamEnvVarMap ganha 2 entries: "gemini-stt" → "UPSTREAM_STT_GEMINI_MODEL", "groq-whisper" → "UPSTREAM_STT_GROQ_MODEL"
    - openai_whisper_director.canonicalAliasForUpstream ganha entry: "groq-whisper" → "whisper" (D-B8 — Groq REUSES adapter; canonical alias mapping é o gate)
    - Tests resolver_test.go unskip 2 cases (Plan 01 stubs)
  </behavior>
  <action>
    1. resolver.go linhas 56-60: adicionar 2 entries no map (gemini-stt + groq-whisper) mantendo ordem alfabética ou após existing entries.

    2. openai_whisper_director.go linha 66: adicionar entry "groq-whisper": "whisper" no canonicalAliasForUpstream map. Atualizar comentário existente sobre new tier-1 upstreams.

    3. resolver_test.go: implementar TestUpstreamEnvVarMap_GeminiSTT_MapsToGeminiModelEnv + _GroqWhisper_MapsToGroqModelEnv. Tests setam env var, chamam resolver.Resolve, asseguram env value wins sobre schema default.
  </action>
  <verify>
    <automated>cd gateway && go test -count=1 ./internal/models/... -run 'TestUpstreamEnvVarMap_GeminiSTT|TestUpstreamEnvVarMap_GroqWhisper' && grep -q 'groq-whisper' gateway/internal/proxy/openai_whisper_director.go</automated>
  </verify>
  <done>
    2 tests PASS; resolver.go contém 2 novas entries; openai_whisper_director.go contém groq-whisper entry.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dispatcher multi-tier-1 loop + main.go wireup gemini-stt + groq-whisper proxies</name>
  <files>gateway/internal/proxy/dispatcher.go, gateway/internal/proxy/dispatcher_test.go, cmd/gateway/main.go</files>
  <read_first>
    - gateway/internal/proxy/dispatcher.go linhas 257-272 (single-tier-1 lookup atual)
    - 11.2-RESEARCH.md Pattern 3 dispatcher modification linhas 462-480
    - PATTERNS.md dispatcher.go section (backward-compat note: roles com 1 tier-1 = slice len 1)
    - cmd/gateway/main.go (procurar wireup atual de openai-whisper proxy — analog para gemini-stt + groq-whisper)
  </read_first>
  <behavior>
    - dispatcher.go substitui Loader.Resolve(role, 1) single-lookup por loop iterando ResolveAllTier1(cfg.Role) ASC tier_priority
    - Dispatcher dispatcha no primeiro candidato com EffectiveState igual gobreaker.StateClosed
    - Se nenhum candidato CLOSED ou slice vazio → 503 upstream_unavailable (OpenAI envelope)
    - main.go registra 2 novos proxies: gemini-stt usa BuildGeminiSTTDirector + ModifyResponse + ErrorHandler; groq-whisper usa BuildOpenAIWhisperDirector (REUSE D-B8) com URL+bearer apontando para Groq env vars
    - Tests dispatcher_test.go unskip 3 cases (Plan 01)
  </behavior>
  <action>
    1. dispatcher.go linhas 257-272: substituir bloco single-tier-1 por loop conforme RESEARCH Pattern 3 (candidates igual cfg.Loader.ResolveAllTier1(cfg.Role); if len 0 → 503; for range candidates if Breaker.EffectiveState(t1.Name) igual gobreaker.StateClosed → dispatchTo + return; senão → 503 final).

    2. cmd/gateway/main.go: localizar wireup atual de openai-whisper proxy (provavelmente registra map sttDispatcher.Proxies). Adicionar 2 entries:
       - gemini-stt: BuildGeminiSTTDirector(cfg.UpstreamSTTGeminiURL parsed, cfg.UpstreamSTTGeminiAuthBearer, resolver, "gemini-stt", log); ReverseProxy Director+ModifyResponse+standardErrorHandler
       - groq-whisper: BuildOpenAIWhisperDirector(cfg.UpstreamSTTGroqURL parsed, cfg.UpstreamSTTGroqAuthBearer, resolver, "groq-whisper", log); ReverseProxy Director+standardErrorHandler (REUSE — D-B8; mesmo padrão openai-whisper só URL+bearer diferentes)

    3. dispatcher_test.go: implementar 3 sub-tests usando fake Loader retornando 3-candidate slice + fake Breaker com EffectiveState configurável. Asseguram:
       - TestDispatcher_TierOneFallthrough_GeminiOpen_GroqClosed_Routes200: gemini OPEN, groq CLOSED → dispatchTo("groq-whisper")
       - TestDispatcher_TierOneFallthrough_GeminiAndGroqOpen_OpenAIWhisperClosed_Routes200: ambos OPEN, openai CLOSED → dispatchTo("openai-whisper")
       - TestDispatcher_TierOneFallthrough_AllOpen_Returns503: todos OPEN → 503 + OpenAI envelope
  </action>
  <verify>
    <automated>cd gateway && go build ./... && go test -count=1 ./internal/proxy/... -run TestDispatcher_TierOneFallthrough</automated>
  </verify>
  <done>
    Build OK; 3 tests PASS; main.go registra gemini-stt + groq-whisper proxies; dispatcher loop usa ResolveAllTier1.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-DIR | Spoofing | Gemini director header swap | mitigate | r.Header.Del Authorization THEN r.Header.Set x-goog-api-key — Pitfall 3 inline comment + dedicated test |
| T-11.2-CASCADE | Denial of Service | Multi-tier-1 cascade | mitigate | D-B11 cooldown_s=120 no DB (Plan 03 migration) limita oscilação gemini breaker; cascade fall-through automático |
| T-11.2-AUTH | Information Disclosure | Bearer logging | mitigate | AuthBearer json:"-" pattern (Plan 02 Config); presence-only log no resolver.go:104 pattern reused |
</threat_model>

<verification>
- Unit suite verde para proxy + models + dispatcher
- main.go compila com 2 novos proxies
- grep "x-goog-api-key" gemini_stt_director.go retorna pelo menos 2 occurrences (set + comment)
- grep "ResolveAllTier1" dispatcher.go retorna 1 occurrence
</verification>

<success_criteria>
Gateway code path completo da cascade STT 3-upstream tier-1 implementado e unit-tested. Após Plan 06 + Plan 03 deploy, request /v1/audio/transcriptions com pod-OFF gemini CLOSED routes para Gemini; cascade fall-through ativo.
</success_criteria>

<output>
Create .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-06-SUMMARY.md when done
</output>
