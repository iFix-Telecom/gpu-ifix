---
phase: 11.2-readd-whisper-local-gemini-fallback
mode: plan-phase --gaps (evaluation, no plan emitted)
evaluated: 2026-06-07
phase_status: Complete / passed_partial
gate_override: --force (rationale below)
decision: NO_GAP_PLAN
output: GAPS-DECISION (this document)
---

# Phase 11.2 — Gap-closure evaluation (no plan emitted)

`/gsd:plan-phase 11.2 --gaps` ran in evaluation mode. The 3 carry-forward
items from `11.2-VERIFICATION.md` were assessed individually against the
Phase 11.2 scope boundary in `11.2-CONTEXT.md` (`<domain>` section + `Out of
scope` list). **None are in-scope for a Phase 11.2 fix plan.**

`11.2-09-PLAN.md` was **NOT created**.

## Closed-phase gate override

Phase 11.2 status is `Complete / passed_partial`. The `plan-phase --gaps`
workflow normally blocks closed phases. Override rationale: replan
evaluation on a `passed_partial` phase is a legitimate gap-triage activity
(VERIFICATION explicitly recommended `/gsd:plan-phase 11.2 --gaps` in its
Conclusion §). Outcome of this evaluation is a decision document, not a
re-opening of the phase.

## Per-item disposition

### Item 1 — Scenarios 1 + 5 pod-ON local-stt — OUT OF SCOPE

**Verdict:** Belongs to Phase 6.6 pod-cold-start hardening (or a follow-up
pod-image phase). **Not** a Phase 11.2 cascade defect.

**Rationale:**
- Phase 11.2 scope (`11.2-CONTEXT.md` `<domain>`) is *cascade STT routing
  + audit distinguishing* — `local-stt` (tier-0) + `gemini-stt` /
  `groq-whisper` / `openai-whisper` (tier-1). All 4 wire-shape paths and
  the dispatcher fall-through logic are VERIFIED via cenários 2/3/4/6
  (`audit_log.upstream` distinct per request).
- Pod cold-start failure (`actual_status=created` permanent, no port
  mapping after image-load) is identical in symptom to Phase 6.6 UAT 1-9
  attempts (pre-e309756 trace-markers + sshd-first fix). Phase 6.6
  carries this debt; Phase 11.2 cannot fix it without inheriting Phase 6.6
  scope.
- VERIFICATION confirms cascade primary objective verified end-to-end
  without pod-ON path. Cenários 1+5 only exercise *which upstream wins
  when local-stt breaker is CLOSED* — that wiring is the same code path
  tested by `local-stt → fallback` behavior in cenário 3 (gemini OPEN
  → groq takeover proves dispatcher iteration; cenário 4 proves
  tier-1 cascade).
- No Phase 11.2 D-Bx requirement is unverified by deferring 1+5. D-B13
  scenarios 1+5 acceptance is *pod custom-image works → local-stt
  responds 200* — orthogonal to cascade objective.

**Recommendation:**
- Replay cenários 1+5 piggybacked on the *next* Phase 06.7+ TTS Live UAT
  round (already invokes the same custom-image cold-start path once
  Phase 11.2 Plan 04 whisper weights bake lands).
- If pod cold-start does **not** stabilize within 2-3 such piggyback
  attempts, open a dedicated Phase 6.6.X hardening phase for
  custom-image cold-start triage (existing pattern: trace markers,
  sshd-first, image-load timeout audit).

### Item 2 — Scenario 4 OpenAI key rotation — OPERATOR RUNBOOK

**Verdict:** Operator action. **Not** a plan-worthy item. **Cascade
routing already VERIFIED** by `audit_log.upstream='openai-whisper'` row
captured during UAT (VERIFICATION § Cascade dispatch evidence).

**Rationale:**
- HTTP 401 is `invalid_request_error` from OpenAI — credential
  `UPSTREAM_STT_OPENAI_AUTH_BEARER` is invalid (`sk-proj-***zkg6`).
- Zero code paths to change. Rotating env var + recreating container
  flips cenário 4 from PARTIAL → PASS without any commit.
- Operator runbook action only (env edit + container restart).

**Recommendation — operator action items (no plan, no PR):**
1. Generate new OpenAI API key via https://platform.openai.com/api-keys
   (Whisper / Audio category).
2. Patch in vps-ifix-vm:
   ```
   ssh vps-ifix-vm 'sed -i s/UPSTREAM_STT_OPENAI_AUTH_BEARER=.*/UPSTREAM_STT_OPENAI_AUTH_BEARER=<new>/ \
     /var/lib/docker/volumes/infra_portainer_data/_data/compose/34/stack.env'
   ```
   (or edit via Portainer stack UI if preferred).
3. Recreate container: `ssh vps-ifix-vm 'docker compose -f /opt/ai-gateway-dev/docker-compose.yml up -d ai-gateway --force-recreate'`
4. Re-run `bash pod/smoke/uat-11.2.sh --scenario gemini-groq-open-openai-takeover`
   from ops-claude or vps-ifix-vm.
5. Expected: HTTP 200 + `audit_log.upstream='openai-whisper'`. Append
   PASS row to `11.2-HUMAN-UAT.md` sign-off table when complete.

Track as a single line in operator runbook; do **not** create a phase or
plan for it.

### Item 3 — Primary pod shape drift — OUT OF SCOPE

**Verdict:** Belongs to Phase 6.6 env precedence audit. Not a Phase 11.2
cascade concern.

**Rationale:**
- Env vars `PRIMARY_NUM_GPUS=2` and `PRIMARY_VAST_NUM_GPUS_PRIMARY=1` are
  competing inputs to pod-shape resolution. The latter won the Vast
  search during UAT, contradicting MEMORY `primary-gpu-shape-06.8-final`
  (2×3090 standing config, cap $0.60).
- Pod shape selection is the *primary pod lifecycle* concern — Phase 6.6
  domain. Phase 11.2 only consumes the resulting pod regardless of
  1×3090 vs 2×3090 (whisper-large-v3 fits in 1× VRAM).
- CONTEXT.md `Out of scope`: "2×3090 fallback shape change (Phase 11.1
  D-A6 pivot stays)" — the planner explicitly excluded shape work.
- Cascade behavior is shape-agnostic — none of D-B1..D-B14 reference
  GPU count.

**Recommendation:**
- Open a Phase 6.6 hardening follow-up entry: *"Audit dual-shape env
  precedence (`PRIMARY_NUM_GPUS` vs `PRIMARY_VAST_NUM_GPUS_PRIMARY`).
  Decide canonical var, deprecate the loser, document in RUNBOOK."*
- Until then, operator must manually align both vars before any UAT that
  requires the 2×3090 standing config (this is already noted in MEMORY
  `primary-gpu-shape-06.8-final` — "ALWAYS verify vps-ifix-vm .env before
  primary UAT").

## Roadmap recommendation (not applied here)

Per task instruction, ROADMAP.md is **not** modified by this evaluation.
Suggested update (apply via separate user action):

- Add a **Phase 6.6.X — Pod cold-start hardening & env precedence audit**
  follow-up phase (or append items to an existing Phase 6.6 backlog
  section) covering:
  - Custom-image cold-start triage (Item 1) — diagnose
    `actual_status=created` permanence, image-load → supervisord boot
    gap, re-validate trace-markers + sshd-first pattern (parity with
    e309756 fix).
  - Dual-shape env precedence audit (Item 3) — decide canonical
    `PRIMARY_NUM_GPUS` source-of-truth, deprecate competing var,
    update RUNBOOK + MEMORY.
- Item 2 (OpenAI key) does **not** need a roadmap entry — capture in
  operator runbook only (see Item 2 § above).

## Conclusion

Phase 11.2 cascade STT 3-upstream tier-1 routing primary objective is
VERIFIED. The 3 carry-forward items are scoped to (a) Phase 6.6 pod /
env hardening (items 1, 3) and (b) operator runbook action (item 2).
No `11.2-09-PLAN.md` is justified. Phase 11.2 stays closed at
`passed_partial`.

**Next steps:**
- User: decide whether to open Phase 6.6.X follow-up phase per roadmap
  recommendation above.
- Operator: action OpenAI key rotation per Item 2 § (single runbook
  action, no PR).
- No further GSD plan work on Phase 11.2.
