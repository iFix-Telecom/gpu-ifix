---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/internal/config/config.go
  - gateway/internal/config/config_test.go
  - gateway/cmd/gatewayctl/model_alias.go
  - gateway/cmd/gatewayctl/model_alias_test.go
autonomous: true
requirements: [D-B1, D-B2, D-B7, RES-STT-CONFIG]
threat_refs: [T-11.2-02, T-11.2-04]
user_setup:
  - service: google-ai-studio
    why: "Tier-1 primary STT (Gemini 2.5 Flash Lite). Operator deve provisionar API key dev-scoped (vide Pitfall 6 RESEARCH) ANTES de Wave 0."
    env_vars:
      - name: UPSTREAM_STT_GEMINI_AUTH_BEARER
        source: "https://aistudio.google.com/app/apikey (create project ifix-ai-gateway-dev)"
    dashboard_config:
      - task: "Mint API key dev-scoped + salvar em /opt/ai-gateway-dev/.env em vps-ifix-vm"
        location: "Google AI Studio → Get API key"

must_haves:
  truths:
    - "config.Config tem PrimarySpeachesImage restaurado (string, env PRIMARY_SPEACHES_IMAGE) — Phase 06.7 pin como default"
    - "config.Config tem PrimaryWhisperWeightsKey + PrimaryWhisperWeightsSHA256 restaurados com required-env enforcement"
    - "config.Config tem 3 novos campos: UpstreamSTTGeminiURL, UpstreamSTTGeminiAuthBearer, UpstreamSTTGeminiModel"
    - "gatewayctl upstreamNameRole map inclui local-stt + gemini-stt (6 entries total)"
  artifacts:
    - path: gateway/internal/config/config.go
      provides: "PrimarySpeaches/Whisper restore + UpstreamSTTGemini* fields"
    - path: gateway/cmd/gatewayctl/model_alias.go
      provides: "upstreamNameRole map: 7 canonical upstreams post-11.2"
---

<objective>
Wave 1 config restore + Gemini env vars novas. Reverte Phase 11.1 Plan 01 Task 3 (struct + env binding de PrimarySpeachesImage + PrimaryWhisperWeights*) e adiciona 3 campos novos para gemini-stt (URL/AuthBearer/Model). Também restaura `local-stt` ao map `upstreamNameRole` em gatewayctl e adiciona entry `gemini-stt`. Fundação para Waves 2-7 (migration, reconciler, director).

Purpose: gateway precisa conhecer (a) imagem speaches + sha256 whisper para injetar no env do pod (Wave 3) e (b) credenciais Gemini para o director (Wave 5).
Output: 4 arquivos modificados, build/tests verdes, sem novos pacotes Go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-CONTEXT.md
@.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-RESEARCH.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-01-SUMMARY.md
@gateway/internal/config/config.go
@gateway/cmd/gatewayctl/model_alias.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restaurar PrimarySpeachesImage + PrimaryWhisperWeights{Key,SHA256} em config.Config</name>
  <files>gateway/internal/config/config.go, gateway/internal/config/config_test.go</files>
  <read_first>
    - `git show 0f73659^:gateway/internal/config/config.go` — shape pré-11.1 dos 3 campos
    - `git show fbe2129^:gateway/internal/config/config.go` — shape antes do CR-03 drop (post-shrink mas com Whisper ainda)
    - `gateway/internal/config/config.go:46` (UpstreamSTTURL atual — promover de deprecated/optional para required novamente)
    - `gateway/internal/config/config.go:190` (comentário phase 11.1 D-A4 — substituir)
  </read_first>
  <behavior>
    - TestPrimarySpeachesImage_Default: env unset + outros required setados → Config.PrimarySpeachesImage == "ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3"
    - TestPrimaryWhisperWeights_Required: env unset → Load() retorna erro contendo "PRIMARY_WHISPER_WEIGHTS_KEY" OU "PRIMARY_WHISPER_WEIGHTS_SHA256"
    - TestPrimaryWhisperWeights_SHA256Format: SHA256 com chars inválidos → erro "sha256: must be 64 lowercase hex chars" (parity com Qwen/BGE-M3)
    - TestUpstreamSTTURL_RestoredRequired: env unset → erro contendo "UPSTREAM_STT_URL" (reverte CR-02 f06c777 demote)
  </behavior>
  <action>
    Aplicar revert dirigido em `gateway/internal/config/config.go`:

    1. Restaurar struct fields (insert após Qwen* fields, antes BGEM3*):
       ```go
       PrimarySpeachesImage         string // PRIMARY_SPEACHES_IMAGE (Phase 11.2 D-B1: restored Phase 06.7 pin)
       PrimaryWhisperWeightsKey     string // PRIMARY_WHISPER_WEIGHTS_KEY (Phase 11.2 D-B2)
       PrimaryWhisperWeightsSHA256  string // PRIMARY_WHISPER_WEIGHTS_SHA256
       ```
    2. Restaurar Load() bindings com defaults + sha256 hex validation (espelhar pattern do PrimaryBGEM3WeightsSHA256).
       Default PrimarySpeachesImage = `"ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3"`.
    3. Adicionar `PRIMARY_SPEACHES_IMAGE`, `PRIMARY_WHISPER_WEIGHTS_KEY`, `PRIMARY_WHISPER_WEIGHTS_SHA256` na lista de required-env validation.
    4. Em `UpstreamSTTURL` (linha 46): remover phrasing "optional/deprecated"; voltar a required (Load() error se vazio) — reverte CR-02 commit f06c777.
    5. Apagar comentário "Phase 11.1 D-A4: PrimaryWhisperWeightsKey/SHA256 removed" (linha 190).
    6. Atualizar `config_test.go`: adicionar os 4 TestXxx (vide behavior) + ajustar fixtures helpers que setam env para incluir as 3 vars restauradas.

    NÃO acrescentar nada de Gemini neste task (Task 2 separa concerns).
  </action>
  <verify>
    <automated>go test ./gateway/internal/config/ -count=1 -run "TestPrimarySpeachesImage|TestPrimaryWhisperWeights|TestUpstreamSTTURL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PrimarySpeachesImage' gateway/internal/config/config.go` ≥ 2 (struct + Load)
    - `grep -c 'PrimaryWhisperWeightsKey' gateway/internal/config/config.go` ≥ 2
    - `grep -c 'PrimaryWhisperWeightsSHA256' gateway/internal/config/config.go` ≥ 2
    - `grep -c "Phase 11.1 D-A4: PrimaryWhisperWeights" gateway/internal/config/config.go` == 0
  </acceptance_criteria>
  <done>Build verde; novos 4 tests passam; required-env enforcement uniforme com Qwen/BGE-M3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar UpstreamSTTGemini{URL,AuthBearer,Model} em config + auditar redaction</name>
  <files>gateway/internal/config/config.go, gateway/internal/config/config_test.go</files>
  <read_first>
    - `gateway/internal/upstreams/types.go` (UpstreamConfig.AuthBearer json:"-" pattern — para parity em logs)
    - `gateway/internal/config/config.go` linhas onde UPSTREAM_LLM_OPENROUTER_AUTH_BEARER é lido (espelhar shape)
  </read_first>
  <behavior>
    - TestGemini_Defaults: env unset → URL="https://generativelanguage.googleapis.com/v1beta", Model="gemini-2.5-flash-lite", AuthBearer="" (opcional em dev sem key — gateway boota; director fica desativado).
    - TestGemini_EnvOverride: `UPSTREAM_STT_GEMINI_MODEL=gemini-2.5-flash` → Config.UpstreamSTTGeminiModel == "gemini-2.5-flash"
    - TestGemini_AuthBearer_NeverLoggedRaw: invocar `fmt.Sprintf("%+v", cfg)` quando AuthBearer setado a valor sentinela → substring "Gemini_AUTH_BEARER_LEAKED" NÃO aparece (use struct tag `json:"-"` OU custom String()).
  </behavior>
  <action>
    1. Adicionar 3 struct fields (após PrimaryWhisperWeightsSHA256):
       ```go
       UpstreamSTTGeminiURL        string // UPSTREAM_STT_GEMINI_URL — Gemini AI Studio REST root (must NOT end /v1; director appends /v1beta/models/<model>:generateContent)
       UpstreamSTTGeminiAuthBearer string // UPSTREAM_STT_GEMINI_AUTH_BEARER — Google AI API key. Director envia como header x-goog-api-key (NÃO Authorization).
       UpstreamSTTGeminiModel      string // UPSTREAM_STT_GEMINI_MODEL — env override per-instance; resolver.upstreamEnvVarMap["gemini-stt"] aponta aqui
       ```
    2. Em Load(): defaults conforme behavior. `UpstreamSTTGeminiURL` validado por reuse do checker que já recusa /v1 sufixo (Phase 06.9 Plan 04 fail-fast) — adicionar `gemini-stt` à lista checada.
    3. CR-fix antecipado T-11.2-02 (API key leak): garantir que se config.Config tem ToString() OR Marshal customizado, AuthBearer seja redacted. Se não tem, adicionar comentário sobre `slog` log calls: NUNCA logar `cfg.UpstreamSTTGeminiAuthBearer` cru — usar `"present", cfg.UpstreamSTTGeminiAuthBearer != ""`.
    4. Tests conforme behavior.
  </action>
  <verify>
    <automated>go test ./gateway/internal/config/ -count=1 -run "TestGemini"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'UpstreamSTTGeminiURL\|UpstreamSTTGeminiAuthBearer\|UpstreamSTTGeminiModel' gateway/internal/config/config.go` ≥ 6
    - `grep -nE 'log\.(Info|Debug|Warn).*UpstreamSTTGeminiAuthBearer' gateway/internal/config/config.go` == 0 (sem log raw)
  </acceptance_criteria>
  <done>3 campos novos legíveis; build verde; redaction garantido em logs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restaurar local-stt + adicionar gemini-stt em gatewayctl upstreamNameRole</name>
  <files>gateway/cmd/gatewayctl/model_alias.go, gateway/cmd/gatewayctl/model_alias_test.go</files>
  <read_first>
    - `git show 0f73659^:gateway/cmd/gatewayctl/model_alias.go` (pre-11.1 map com 6 entries incluindo local-stt)
    - `gateway/cmd/gatewayctl/model_alias.go` (estado atual com 5 entries)
  </read_first>
  <behavior>
    - TestUpstreamNameRole_HasLocalSTT: map["local-stt"] == "stt"
    - TestUpstreamNameRole_HasGeminiSTT: map["gemini-stt"] == "stt"
    - TestUpstreamNameRole_Count: len(map) == 7 (local-stt, gemini-stt, openai-whisper, openrouter-chat, openai-embed, local-llm, chatterbox-tts — confirmar exact roster contra schema Phase 11.2)
  </behavior>
  <action>
    1. Adicionar `"local-stt": "stt"` e `"gemini-stt": "stt"` ao map `upstreamNameRole`.
    2. Atualizar comentário do map: "7 canonical upstreams after Phase 11.2 (3 STT cascade: local-stt tier-0 → gemini-stt tier-1 primary → openai-whisper tier-1 safety net)".
    3. Atualizar 3 tests acima.
  </action>
  <verify>
    <automated>go test ./gateway/cmd/gatewayctl/ -count=1 -run "TestUpstreamNameRole"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"local-stt": "stt"' gateway/cmd/gatewayctl/model_alias.go` == 1
    - `grep -c '"gemini-stt": "stt"' gateway/cmd/gatewayctl/model_alias.go` == 1
  </acceptance_criteria>
  <done>Map atualizado; 3 tests passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → gateway process | UPSTREAM_STT_GEMINI_AUTH_BEARER atravessa fronteira via /opt/ai-gateway-dev/.env (Portainer/compose) |
| gateway process → log/audit sinks | Risco de leak de API key em log lines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.2-02 | Information Disclosure | gateway/internal/config/config.go log calls | mitigate | Task 2 enforça presence-only logging; grep gate em acceptance impede regressão; espelhar pattern UpstreamConfig.AuthBearer `json:"-"` |
| T-11.2-04 | Operational (cost regression) | image trade-off | accept (documented) | Trade-off (+5-6GB pull custo, ~3-5min cold-start) documentado em 11.2-SUMMARY após Plan 03/06; rebuild + promote sequence em Plan 03 Task |
</threat_model>

<verification>
go build ./gateway/...
go test ./gateway/internal/config/ ./gateway/cmd/gatewayctl/ -count=1
</verification>

<success_criteria>
- 4 arquivos modificados (config.go + config_test.go + model_alias.go + model_alias_test.go)
- 7 testes novos verdes (4 whisper/speaches + 3 gemini + 3 upstreamNameRole; sobreposição com fixtures permite ajustar contagem)
- Build verde; nenhum pacote externo novo
</success_criteria>

<output>
Create `.planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-01-SUMMARY.md` when done.
</output>
