---
phase: 11.2-readd-whisper-local-gemini-fallback
plan: 01
subsystem: testing
tags: [tdd, red-stubs, nyquist-gate, uat-skeleton, stt-cascade]

requires:
  - phase: 11.2-discuss
    provides: CONTEXT.md / VALIDATION.md / PATTERNS.md (D-B4..D-B14 contract)
provides:
  - 20 RED test stubs covering D-B4 (Gemini director), D-B5′ (cascade), D-B6′ (migration 0029), D-B7/D-B8 (env vars), D-B11/D-B12 (cooldown)
  - TestProbeSTT_Success skip stub in pod/health-bridge (OWNER Plan 05)
  - pod/smoke/uat-11.2.sh 6-scenario D-B13 driver skeleton (OWNER Plan 08)
  - Operator gate D-B10 validated GREEN (FALLBACK_1/2 envs present in ai-gateway-dev)
affects: [Plan 02, Plan 03, Plan 05, Plan 06, Plan 08]

tech-stack:
  added: []
  patterns: [t.Skip("OWNER: Plan NN — ...") RED-stub annotation]

key-files:
  created:
    - gateway/internal/proxy/gemini_stt_director_test.go
    - gateway/internal/integration_test/migration_0029_test.go
    - pod/smoke/uat-11.2.sh
  modified:
    - gateway/internal/proxy/dispatcher_test.go
    - gateway/internal/upstreams/loader_test.go (DELETE TestPostMigration0028_NoLocalSTTInSnapshot + ADD 3 new)
    - gateway/internal/primary/reconciler_test.go
    - gateway/internal/primary/lifecycle_test.go
    - gateway/internal/models/resolver_test.go
    - pod/health-bridge/probes_test.go (TestProbeSTT_Success stub)
    - gateway/docker-compose.yml (FALLBACK_1/2 env mapping)

key-decisions:
  - "Env naming generic FALLBACK_1/FALLBACK_2 (slot 1 = Gemini, slot 2 = Groq) — operator can swap provider without env rename. DB row name stays provider-specific (gemini-stt, groq-whisper) because director pick depends on it."
  - "D-B10 simplified gate: 6 FALLBACK envs (was 6 named GEMINI/GROQ) verified once in ai-gateway-dev container; no functional smoke."
  - "TestProbeSTT_Success remains skip-only (not verbatim restore) because probeSTT/UpstreamSTT symbols are absent — Plan 05 unskips after restore."

patterns-established:
  - "RED-stub: every new test is t.Skip('OWNER: Plan NN — implements ...') + expected-shape comment + PATTERNS.md line reference"
  - "Generic slot env naming: UPSTREAM_STT_FALLBACK_{1,2}_{URL,AUTH_BEARER,MODEL}"

requirements-completed: [D-B4, D-B5′, D-B6′, D-B7, D-B8, D-B10, D-B11, D-B12, D-B13]

duration: 90min
completed: 2026-06-07
---

# Phase 11.2 Plan 01: Wave 0 RED stubs + UAT skeleton + D-B10 gate

**Nyquist gate green: every downstream behavior (D-B4–D-B14) has a failing/skipped test stub before any Plan 02+ implementation begins.**

## What was built

- **20 RED stubs** across gateway directors/dispatcher/loader/migration/resolver/primary tests — each `t.Skip("OWNER: Plan NN — ...")` with expected assertion shape + PATTERNS.md line ref.
- **1 SKIP stub** in pod/health-bridge/probes_test.go (TestProbeSTT_Success) — Plan 05 restores function + pastes verbatim test body.
- **1 bash UAT driver** pod/smoke/uat-11.2.sh — 6 D-B13 scenarios stubbed `TODO + exit 0`; Plan 08 fills curl/jq logic. CLI supports `--all`, `--scenario <name>`, `--list`.
- **D-B10 operator gate** verified green: container `ai-gateway-dev` on vps-ifix-vm has all 6 `UPSTREAM_STT_FALLBACK_{1,2}_{URL,AUTH_BEARER,MODEL}` envs set (verified via `docker inspect`).

## Replan applied (pre-execution)

- Env naming changed from `UPSTREAM_STT_{GEMINI,GROQ}_*` → `UPSTREAM_STT_FALLBACK_{1,2}_*` (commit 98e10bb).
- gateway/docker-compose.yml updated to forward new env names (commit 8d8c681) — was wiring only old GEMINI envs.
- Go Config field names mirror: `UpstreamSTTFallback{1,2}{URL,AuthBearer,Model}`. DB row `name` (`gemini-stt`, `groq-whisper`) stays provider-specific (director pick depends).
- Naming-convention note appended to CONTEXT.md preamble.

## Verification

- `go vet ./...` PASS (no output)
- `go test -count=1 -short ./gateway/internal/{proxy,upstreams,primary,models}/... ./pod/health-bridge/...` — all new tests SKIP cleanly
- `grep -rn NoLocalSTTInSnapshot gateway/` → 0 occurrences
- `bash -n pod/smoke/uat-11.2.sh` PASS; `--all` enumerates 6 scenarios
- `ssh vps-ifix-vm docker inspect ai-gateway-dev | grep FALLBACK_` → 6 entries set

## Commits

- 73df07a `test(11.2-01): add RED stubs for gateway directors + cascade dispatcher + migration 0029`
- 1b61ed3 `test(11.2-01): add TestProbeSTT RED stub + UAT driver skeleton`
- 02a90db `security(11.2-01): remove hardcoded tenant API keys from uat-11.2.sh`

## Security follow-up REQUIRED (out-of-band)

Commits 73df07a and 1b61ed3 included tenant keys `ifix_sk_ysohb3l6xgq3wxeac4ftkabz6ubnkfwv`
(converseai) and `ifix_sk_hu6h3ggws6sfqwqgsfhbyonkq72jzbnj` (telefonia) as `${VAR:-literal}`
defaults in pod/smoke/uat-11.2.sh. Repo is PUBLIC (IfixTelecom/gpu-ifix). 02a90db switches
to `${VAR:?msg}` fail-fast but the literals remain in commit history. Operator must:

1. **Rotate both keys** via `gatewayctl key revoke --id <id>` + `gatewayctl key create --tenant <slug>`
2. **Scrub git history**: `git filter-repo --replace-text` (see [[github-push-protection-cf-token-incident]] memory for recipe)
3. **Force-push develop** + notify any team member with a clone

## Downstream gates unblocked

- Plan 02 (Config fields) — RED tests in config_test.go scope already exist analogously; symbols Fallback{1,2} now expected.
- Plan 03 (migration 0029 + reconciler/lifecycle) — 6 integration + 6 unit stubs awaiting impl.
- Plan 05 (health-bridge probeSTT) — 1 skip awaiting restore.
- Plan 06 (directors + dispatcher) — 11 stubs awaiting impl.
- Plan 08 (live UAT) — 6 scenario stubs awaiting curl/jq + sign-off.
