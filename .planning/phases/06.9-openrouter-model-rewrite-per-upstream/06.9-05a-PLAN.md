---
phase: 06.9-openrouter-model-rewrite-per-upstream
plan: 05a
type: execute
wave: 3
depends_on: [06.9-03, 06.9-04]
files_modified:
  - gateway/internal/integration_test/resilience_helpers_test.go
  - gateway/internal/integration_test/openrouter_model_rewrite_test.go
  - gateway/internal/integration_test/openai_whisper_model_rewrite_test.go
  - gateway/internal/integration_test/openai_embed_model_rewrite_test.go
autonomous: true
requirements: [OR-FIX, STT-OAI-FIX, EMBED-OAI-FIX]
tags: [integration-tests, body-capture, mock, r6-whisper-edges, r8-helper-gate]

must_haves:
  truths:
    - "R8 gate cleared: freshSchema helper signature exposes *pgxpool.Pool (or equivalent) for resolver instantiation"
    - "newSuccessMock (or a new newSuccessMockCapturing variant) exposes captured request body via mock.LastBody() []byte"
    - "newSelectiveMock variant added for R13 (Plan 05b consumes; helper lives here)"
    - "TestIntegration_OpenRouterModelRewrite: trips local-llm breaker, posts {model:qwen}, asserts tier-1 mock saw forwarded body with model=qwen/qwen3.5-27b (not qwen)"
    - "TestIntegration_OpenAIWhisperModelRewrite: trips local-stt breaker, posts multipart with model=whisper + WAV, asserts tier-1 mock saw multipart with model=whisper-1 AND audio bytes byte-identical"
    - "TestIntegration_OpenAIWhisperModelRewrite_R6_MissingModelInjectsTarget / _R6_DuplicateModelRejects / _R6_ResolverMissPassesThrough: 3 R6 edge-case integration tests — duplicate-model test MUST PASS (not SKIP) because WARNING-3 wires the abort handler in Plan 03"
    - "TestIntegration_OpenAIEmbedModelRewrite: trips local-embed breaker, posts {model:bge-m3,input:[x]}, asserts tier-1 mock saw forwarded body with model=text-embedding-3-small AND dimensions=1024"
  artifacts:
    - path: "gateway/internal/integration_test/resilience_helpers_test.go"
      provides: "Extended upstreamMock with body-capture + selective-reject + R8 freshSchema signature gate"
      contains: "LastBody"
    - path: "gateway/internal/integration_test/openrouter_model_rewrite_test.go"
      provides: "End-to-end OR-FIX integration test (body capture proves rewrite)"
      contains: "TestIntegration_OpenRouterModelRewrite"
    - path: "gateway/internal/integration_test/openai_whisper_model_rewrite_test.go"
      provides: "End-to-end STT-OAI-FIX with binary audio preservation + R6 edge cases (all PASS — duplicate-model abort wired in Plan 03)"
      contains: "TestIntegration_OpenAIWhisperModelRewrite"
    - path: "gateway/internal/integration_test/openai_embed_model_rewrite_test.go"
      provides: "End-to-end EMBED-OAI-FIX consistency check"
      contains: "TestIntegration_OpenAIEmbedModelRewrite"
  key_links:
    - from: "gateway/internal/integration_test/openrouter_model_rewrite_test.go"
      to: "gateway/internal/integration_test/resilience_helpers_test.go"
      via: "newSuccessMockCapturing(t) + mock.LastBody()"
      pattern: "mock\\.LastBody\\(\\)"
---

<objective>
Wave 3a (split from prior Plan 05 per WARNING-2): Helper extension + 3 model-rewrite integration tests + 3 R6 Whisper edge-case tests. Plan 05b (Wave 3b) covers local-tier byte-identical + R13 historical-bug + R1 breaker TTL restore + R3 migration round-trip + PROJECT.md doc convergence.

This plan closes the test-shaped gap that hid the bug for months: the existing `newSuccessMock` returns 200 regardless of forwarded body, so tier-1 routing tests passed while production OpenRouter 404'd.

This plan:
1. **R8 gate (Task 1):** Wave 3a entry gate — verify `freshSchema` helper signature exposes `*pgxpool.Pool` (or equivalent) for resolver instantiation. If only Redis is exposed, extend the helper FIRST.
2. **Task 1:** Extends `newSuccessMock` (or adds a new `newSuccessMockCapturing` variant) so tests can assert on the FORWARDED body. Also adds `newSelectiveMock` (for Plan 05b R13 consumption).
3. **Task 2:** Adds 3 new model-rewrite integration tests — one per requirement (OR-FIX, STT-OAI-FIX, EMBED-OAI-FIX) — that trip the tier-0 breaker, POST through the gateway, and assert the tier-1 mock saw the rewritten model slug. Also adds 3 R6 Whisper edge-case integration tests (missing model field → inject target; duplicate model field → reject 400; resolver miss → pass-through alias). **WARNING-3 (from Plan 03 revision): the duplicate-model abort is WIRED in Plan 03 — R6 Test B is required to PASS, NOT SKIP.**

Purpose: After Plan 05a, the 3 model-rewrite requirements (OR-FIX, STT-OAI-FIX, EMBED-OAI-FIX) have CI-enforced integration regression coverage with full body assertion (closing the "fake mock returns 200 regardless of body" anti-pattern). R6 Whisper edge cases are CI-locked end-to-end.

**WARNING-2 split rationale:** Prior Plan 05 had 5 tasks + 9 files modified — exceeded the ~50% context budget. Split:
- **05a (this plan, Wave 3a):** Tasks 1-2 — helper extension + 3 model-rewrite + 3 R6 Whisper tests. Touches 4 files.
- **05b (next plan, Wave 3b, depends_on 05a):** Tasks 3-5 — R4 local-tier byte-identical + R13 historical-bug + R1 breaker TTL restore + R3 migration round-trip + PROJECT.md doc convergence. Touches 5 files.

Output: 1 helper file extended + 3 new integration test files. `cd gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=300s -run 'TestIntegration_(OpenRouter|OpenAIWhisper|OpenAIEmbed)'` PASS including R6 edge cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-CONTEXT.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-PATTERNS.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-REVIEWS.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-03-SUMMARY.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-04-SUMMARY.md

@gateway/internal/integration_test/resilience_helpers_test.go
@gateway/internal/integration_test/fallback_routing_test.go
@gateway/internal/upstreams/testdata/probe.wav

<interfaces>
<!-- Current upstreamMock shape (gateway/internal/integration_test/resilience_helpers_test.go:29-66) — to EXTEND: -->
```go
type upstreamMock struct {
    server *httptest.Server
    hits   *atomic.Int64
}

func newSuccessMock(t *testing.T) *upstreamMock { /* returns 200 always; body discarded */ }
```

<!-- Phase 06.9 extension (this plan adds): -->
```go
type upstreamMock struct {
    server   *httptest.Server
    hits     *atomic.Int64
    lastBody atomic.Value // []byte — captured forwarded body
    lastCT   atomic.Value // string — captured Content-Type
}

// NEW: capturing variant for body-shape assertions
func newSuccessMockCapturing(t *testing.T) *upstreamMock
func (m *upstreamMock) LastBody() []byte
func (m *upstreamMock) LastContentType() string

// NEW (R13 — used by Plan 05b): variant that REJECTS specific model values, ACCEPTS others
// Returns HTTP 404 on configured-reject models, HTTP 200 on others.
func newSelectiveMock(t *testing.T, acceptedModels []string) *upstreamMock
```

<!-- R8 GATE: freshSchema signature must expose Postgres pool for resolver instantiation. -->
<!-- Current signature (verify in Task 1 R8 gate): -->
<!-- func freshSchema(t *testing.T, ctx context.Context) (Postgres *pgxpool.Pool, Redis *redis.Client) -->
<!-- If only Redis is returned, Task 1 EXTENDS the helper FIRST before writing model-rewrite tests. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: R8 freshSchema gate + extend upstreamMock with body capture + selective-reject variant (for Plan 05b)</name>
  <files>gateway/internal/integration_test/resilience_helpers_test.go</files>
  <read_first>
    - gateway/internal/integration_test/resilience_helpers_test.go (full file — find `freshSchema` helper signature, identify return tuple)
    - gateway/internal/integration_test/fallback_routing_test.go (how callers use freshSchema; what they receive)
    - gateway/internal/models/resolver.go (NewResolver signature; needs *pgxpool.Pool or *gen.Queries)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-REVIEWS.md (R8 helper signature gate + R13 selective-reject)
  </read_first>
  <acceptance_criteria>
    - **R8 GATE (BLOCKING for downstream Plan 05a Task 2 + Plan 05b):** Inspect `freshSchema` signature. If it does NOT return Postgres pool (e.g. only returns Redis client), EXTEND it to return `*pgxpool.Pool` (or `*gen.Queries`) as an additional value. Update all existing callers in `fallback_routing_test.go` and any other integration test file to consume the new return value (or ignore via `_`). Document the new signature in the SUMMARY.
    - `resilience_helpers_test.go` extends `upstreamMock` with `lastBody atomic.Value` + `lastCT atomic.Value` + accessor methods `LastBody() []byte` + `LastContentType() string`
    - New constructor `newSuccessMockCapturing(t *testing.T) *upstreamMock` returns 200 + captures body bytes via `io.ReadAll(r.Body)` BEFORE writing the response
    - **R13 NEW constructor `newSelectiveMock(t *testing.T, acceptedModels []string) *upstreamMock`** — parses JSON body, extracts `model` field, returns 200 if value is in `acceptedModels` slice, returns 404 + HTML-shaped body (mimicking real OpenRouter rejection of unknown slug) otherwise. Captures body like newSuccessMockCapturing. (Plan 05b consumes this; the helper lives here so the file is touched only once.)
    - Existing `newSuccessMock` callers in other integration tests UNCHANGED (the new variants are opt-in to avoid touching unrelated tests)
    - `cd gateway && go build ./...` succeeds (extending freshSchema must not break existing callers; if it does, fix them)
    - `cd gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=180s` PASS — verify R8 extension didn't regress anything
  </acceptance_criteria>
  <action>
    First, the R8 GATE: run `grep -n 'func freshSchema' gateway/internal/integration_test/*.go` to find the helper. Read its signature + body. Determine whether it returns `*pgxpool.Pool` (or `*sql.DB` or `*gen.Queries`).

    If YES (already exposes Postgres) → proceed to mock extension.
    If NO (only Redis or only schema-setup side-effect) → EXTEND it:
    - Add `*pgxpool.Pool` to the return tuple
    - Modify all callers to receive (or ignore via `_`) the new value
    - Test that `cd gateway && go test -tags=integration ./internal/integration_test/... -count=1` still passes
    - Document the change in SUMMARY

    Then extend `resilience_helpers_test.go`:
    - Add `lastBody atomic.Value` + `lastCT atomic.Value` fields to `upstreamMock`
    - Add `LastBody() []byte` + `LastContentType() string` getters
    - Add `newSuccessMockCapturing(t *testing.T) *upstreamMock` — reads request body via `io.ReadAll(r.Body)` + stores via `Store(body)` BEFORE responding 200
    - **R13:** Add `newSelectiveMock(t *testing.T, acceptedModels []string) *upstreamMock`. Inside the handler:
      ```go
      body, _ := io.ReadAll(r.Body)
      mock.lastBody.Store(body)
      mock.lastCT.Store(r.Header.Get("Content-Type"))
      var m map[string]any
      _ = json.Unmarshal(body, &m)
      requestedModel, _ := m["model"].(string)
      for _, accepted := range acceptedModels {
          if requestedModel == accepted {
              w.WriteHeader(200)
              json.NewEncoder(w).Encode(map[string]any{"id": "gen-test", "model": requestedModel, "choices": []any{}})
              return
          }
      }
      // Not in accepted list — mimic OpenRouter 404
      w.Header().Set("Content-Type", "text/html")
      w.WriteHeader(404)
      w.Write([]byte(`<html><body>Not Found</body></html>`))
      ```

    Run the integration test suite — verify NO regressions in existing tests.
  </action>
  <verify>
    <automated>cd gateway && go build ./... && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=180s</automated>
  </verify>
  <done>R8 gate cleared (freshSchema exposes Postgres pool); upstreamMock has body-capture + selective-reject variants (selective consumed by Plan 05b); existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: 3 model-rewrite integration tests (OR-FIX + STT-OAI-FIX + EMBED-OAI-FIX) + 3 R6 Whisper edge cases (all PASS, NO SKIP per WARNING-3)</name>
  <files>gateway/internal/integration_test/openrouter_model_rewrite_test.go, gateway/internal/integration_test/openai_whisper_model_rewrite_test.go, gateway/internal/integration_test/openai_embed_model_rewrite_test.go</files>
  <read_first>
    - gateway/internal/integration_test/resilience_helpers_test.go (post-Task-1 — body capture + selective mock + freshSchema signature)
    - gateway/internal/integration_test/fallback_routing_test.go (full file — copy the freshSchema/breaker/dispatcher boilerplate verbatim into new test files)
    - gateway/internal/models/resolver.go (NewResolver signature; Start + Refresh)
    - gateway/internal/proxy/openrouter_director.go (post-Plan-03 signature with resolver+upstreamName+log)
    - gateway/internal/proxy/openai_whisper_director.go (post-Plan-03 signature; R6 multipart helper + whisperAbortGuard wired)
    - gateway/internal/proxy/openai_embed_director.go (post-Plan-03 signature)
    - gateway/internal/upstreams/testdata/probe.wav (file path + size for binary fixture)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-PATTERNS.md (lines 480-528 — integration test additions)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md (lines 604-636 — capturing mock pattern with verbatim shape)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-REVIEWS.md (R6 Whisper edge-case behaviors)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-03-SUMMARY.md (confirm WARNING-3 wired the duplicate-model abort — R6 Test B MUST PASS)
  </read_first>
  <acceptance_criteria>
    - New file `openrouter_model_rewrite_test.go` with `//go:build integration` tag; defines `TestIntegration_OpenRouterModelRewrite` that:
      1. `freshSchema(t, ctx)` to bootstrap DB with migration 0026 applied
      2. Instantiates `models.NewResolver(pool, log)` + Refresh
      3. Wires `tier0 := newFailMock(t)`, `tier1 := newSuccessMockCapturing(t)`
      4. Builds OpenRouter director against `tier1.server.URL` with resolver + `"openrouter-chat"` upstream name + discard logger
      5. Constructs dispatcher with proxies map (Plan 03 wiring shape)
      6. Trips `driveBreaker(t, bs, "local-llm", 500, 3)`
      7. Sends `POST /v1/chat/completions {"model":"qwen","messages":[{"role":"user","content":"ping"}]}`
      8. Assert: `tier1.hits.Load() >= 1`, `var body map[string]any; json.Unmarshal(tier1.LastBody(), &body); body["model"] == "qwen/qwen3.5-27b"`
    - New file `openai_whisper_model_rewrite_test.go` with:
      - `TestIntegration_OpenAIWhisperModelRewrite` — multipart with `model=whisper` + WAV → tier1 sees `model=whisper-1` + audio bytes byte-identical
      - **R6 Test A — `TestIntegration_OpenAIWhisperModelRewrite_MissingModelInjectsTarget`:** multipart WITHOUT model field → tier1 receives multipart with synthetic `model=whisper-1` (or appropriate canonical) field injected; audio bytes preserved
      - **R6 Test B — `TestIntegration_OpenAIWhisperModelRewrite_DuplicateModelRejects` (MUST PASS, NOT SKIP per WARNING-3):** multipart with TWO `model` fields → gateway returns HTTP 400 to client with `{"error":{"type":"invalid_request_error"}}`; tier1 hits == 0 (request never forwarded — `whisperAbortGuard` from Plan 03 prevents forwarding)
      - **R6 Test C — `TestIntegration_OpenAIWhisperModelRewrite_ResolverMissPassesThrough`:** seed DB without the whisper/openai-whisper row → multipart `model=whisper` → tier1 sees `model=whisper` unchanged (pass-through)
    - New file `openai_embed_model_rewrite_test.go` with `TestIntegration_OpenAIEmbedModelRewrite`:
      1. Same setup
      2. tier1 = newSuccessMockCapturing for embed
      3. Build Embed director with resolver + `"openai-embed"`
      4. Trip `local-embed` breaker
      5. POST `/v1/embeddings {"model":"bge-m3","input":["test"]}`
      6. Assert: forwarded body has `"model":"text-embedding-3-small"` AND `"dimensions":1024` AND `"input":["test"]`
    - All 6 tests pass — **NO SKIPs allowed for the R6 duplicate-model test (WARNING-3 wires the abort in Plan 03)**: `cd gateway && go test -tags=integration ./internal/integration_test/... -run 'TestIntegration_(OpenRouter|OpenAIWhisper|OpenAIEmbed)' -count=1 -timeout=300s`
  </acceptance_criteria>
  <action>
    Create 3 new test files (one per requirement). Each file:
    - Uses `//go:build integration` build tag
    - Imports the testcontainers/Redis/Postgres helpers from `resilience_helpers_test.go`
    - Copies the `fallback_routing_test.go:30-92` skeleton (freshSchema + loader + breaker + proxies + dispatcher) and adapts to wire the appropriate Director with resolver + upstream-name

    For the Whisper test file: construct multipart body via `mime/multipart.NewWriter` in the test setup; embed `gateway/internal/upstreams/testdata/probe.wav` bytes via `os.ReadFile`. Parse the captured body back as multipart in the assertion using `mime.ParseMediaType` on `LastContentType()` to extract the new boundary.

    For R6 Test B (duplicate model fields) — **MUST PASS, NOT SKIP**: Plan 03 wires `whisperAbortGuard` around the Whisper proxy. The integration test sets up the same wrapper (or registers the Whisper handler via the same code path the production wiring uses) and asserts: (a) `responseRecorder.Code == 400`; (b) response body contains the expected JSON error envelope; (c) `tier1.hits.Load() == 0` (proves the abort blocked forwarding). Do NOT add `t.Skip()` — Plan 03 owns the wire-up so this test MUST run GREEN.

    For R6 Test C (resolver miss): bypass the normal seed by deleting the specific (whisper, openai-whisper) row before Refresh, OR use an in-memory resolver fixture map directly bypassing DB.

    Run tests — all 6 must PASS. NO SKIPs.
  </action>
  <verify>
    <automated>cd gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=300s -run 'TestIntegration_(OpenRouter|OpenAIWhisper|OpenAIEmbed)' -v</automated>
  </verify>
  <done>3 model-rewrite integration tests + 3 R6 Whisper edge-case tests; all 6 PASS — no SKIPs (WARNING-3 wires the duplicate-model abort in Plan 03); body-capture mock variant proven across all 3 directors.</done>
</task>

</tasks>

<verification>
- `cd gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=300s -run 'TestIntegration_(OpenRouter|OpenAIWhisper|OpenAIEmbed)' -v` ALL PASS (3 model-rewrite tests + 3 R6 Whisper edge tests — all 6 PASS, no SKIPs)
- `cd gateway && go test ./... -count=1 -race -timeout=120s` (full unit suite) still PASS
- `grep -c "lastBody\|LastBody" gateway/internal/integration_test/resilience_helpers_test.go` returns at least 2
- `grep -c "newSelectiveMock" gateway/internal/integration_test/resilience_helpers_test.go` returns at least 1 (Plan 05b consumes)
- `ls gateway/internal/integration_test/*model_rewrite_test.go` shows 3 files
- `! grep -q "t.Skip" gateway/internal/integration_test/openai_whisper_model_rewrite_test.go` (no SKIPs allowed)
</verification>

<success_criteria>
- R8 helper signature gate cleared; freshSchema exposes Postgres pool for resolver instantiation
- Body-capturing mock + selective-reject mock variants in place; selective mock ready for Plan 05b consumption
- 3 model-rewrite integration tests prove per-upstream model rewriting end-to-end with body assertion
- R6: 3 Whisper edge-case tests cover missing-model / duplicate-model / resolver-miss behaviors — all PASS, no SKIPs (WARNING-3 wires the abort in Plan 03)
- Plan 05b unlocked (R4 local-tier + R13 + R1 + R3 + PROJECT.md)
</success_criteria>

<output>
Create `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-05a-SUMMARY.md` when done. Capture: diff of `resilience_helpers_test.go` body-capture extension + R8 freshSchema extension if needed + newSelectiveMock source; full source of each new integration test file (3 files); R-finding mapping for tests in this plan (OR-FIX, STT-OAI-FIX, EMBED-OAI-FIX, R6 A/B/C); explicit confirmation that R6 Test B (duplicate-model) PASSES (does not SKIP) thanks to Plan 03 WARNING-3 wire-up.
</output>
