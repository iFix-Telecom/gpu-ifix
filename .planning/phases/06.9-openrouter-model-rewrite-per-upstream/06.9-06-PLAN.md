---
phase: 06.9-openrouter-model-rewrite-per-upstream
plan: 06
type: execute
wave: 5
depends_on: [06.9-05b]
files_modified:
  - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md
  - .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md
  - .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md
  - .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md
autonomous: false
requirements: [SC1-CLOSE]
user_setup:
  - service: openai
    why: "S2 (Whisper) + S3 (Embed) live UAT scenarios require a real OpenAI API key"
    env_vars:
      - name: UPSTREAM_STT_OPENAI_AUTH_BEARER
        source: "OpenAI Platform > API keys (https://platform.openai.com/api-keys); create a project-scoped secret key"
      - name: UPSTREAM_EMBED_OPENAI_AUTH_BEARER
        source: "Same OpenAI key (one key serves both whisper + embed scopes)"
    dashboard_config:
      - task: "Apply both env vars to /opt/ai-gateway-dev/.env on vps-ifix-vm"
        location: "SSH vps-ifix-vm; edit /opt/ai-gateway-dev/.env; docker compose -f /opt/ai-gateway-dev/docker-compose.yml up -d --force-recreate gateway"
  - service: openrouter
    why: "S1 (Chat) live UAT scenario; key already applied this session 2026-05-24 — re-verify before UAT"
    env_vars:
      - name: UPSTREAM_LLM_OPENROUTER_AUTH_BEARER
        source: "OpenRouter > Keys; existing key in /opt/ai-gateway-dev/.env per memory entry openrouter-token-and-stack-location.md"
    dashboard_config:
      - task: "Re-verify key still valid by direct curl ops-claude → openrouter.ai/api/v1/chat/completions before S1"
        location: "Same key + verify mid-UAT-day"

tags: [live-uat, human-checkpoint, cascade-close, sc1, phase-02, phase-03, phase-05, r2-uat-guardrails, d-06-coequal-paths]

must_haves:
  truths:
    - "06.9-HUMAN-UAT.md authored with 6 scenarios + Wave 0 pre-req + R2 hardened guardrails (OpenAI key hard gate, spend cap, breaker-restore on abort, migration-rollback procedure, token-redaction instructions) + cleanup section"
    - "Pre-UAT preconditions task (operator-driven, autonomous: false) BLOCKS S1 entry until passed"
    - "S1 (Chat OpenRouter), S2 (Whisper OpenAI), S3 (Embed OpenAI) operator-signed PASS with HTTP 200 + real tier-1 completion/transcription/embedding"
    - "S1 setup documents BOTH operator-override paths per D-06: (a) gatewayctl model-alias set (schema route); (b) export UPSTREAM_LLM_OPENROUTER_MODEL=... + restart (env route). Both supported; operator may use either."
    - "S1 fallback breaker driver documented: `docker stop ai-gateway-primary-llm` (or equivalent real-failure driver) — operator may choose this over `gatewayctl breaker force-open` for S1; S2/S3 require force-open (no real-failure driver for external upstreams). Expected time-to-OPEN documented (3 consecutive failures × eval-tick)."
    - "S4 (Phase 03 SC-1 cascade), S5 (Phase 02 step 7 cascade), S6 (Phase 05 SC-1 vegeta cascade) operator-signed PASS"
    - "Phase 02 02-VERIFICATION.md SC-5 step 7 flipped from passed_partial → passed with cross-reference to 06.9-HUMAN-UAT.md S5 evidence (verified via positive-assertion grep)"
    - "Phase 03 03-VERIFICATION.md SC-1 flipped from human_needed → passed with cross-reference to 06.9-HUMAN-UAT.md S4 evidence (verified via positive-assertion grep)"
    - "Phase 05 05-VERIFICATION.md SC-1 flipped from passed_partial/PARTIAL → passed with cross-reference to 06.9-HUMAN-UAT.md S6 evidence (verified via positive-assertion grep)"
  artifacts:
    - path: ".planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md"
      provides: "6-scenario operator scoresheet + R2 hardened pre-UAT block + D-06 coequal-paths setup options + signed evidence"
      contains: "S1 — Chat tier-1"
    - path: ".planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md"
      provides: "SC-5 step 7 closure"
      contains: "06.9-HUMAN-UAT.md S5"
    - path: ".planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md"
      provides: "SC-1 closure"
      contains: "06.9-HUMAN-UAT.md S4"
    - path: ".planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md"
      provides: "SC-1 closure"
      contains: "06.9-HUMAN-UAT.md S6"
  key_links:
    - from: ".planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md"
      to: ".planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md"
      via: "S4 cascade evidence reference"
      pattern: "06.9-HUMAN-UAT.md"
---

<objective>
Wave 5 (final): Author the live HUMAN-UAT scenario sheet with R2 hardened guardrails + D-06 coequal-paths setup options + WARNING-6 dual breaker drivers (real-failure for S1, force-open for S2/S3), drive operator through Pre-UAT preconditions + 6 scenarios on the live dev stack, capture sign-off evidence, and cascade-close 3 prior phase VERIFICATIONs whose deferrals this phase resolves.

**R2 (review-mode HIGH revision):** Both reviewers wanted sharper guardrails on the operator-driven S1–S6 sequence. This plan now includes an explicit "Pre-UAT preconditions and operator safeguards" task BEFORE S1, with HARD gates (not checklist items):
- OpenAI key availability hard gate (direct curl validates 200 before S2/S3 can start)
- OpenRouter spend cap with abort criteria
- Breaker-restore-on-abort requirement (cleanup checklist for every scenario that uses force-open)
- Migration-rollback procedure (exact `goose down -1` invocation + production-shaped-data behavior)
- Token-redaction guidance (UAT evidence boxes pre-instruct operator to redact Authorization + keys)

**BLOCKER-1 / D-06 (revision iteration 2):** S1 setup now documents BOTH operator-override paths coequally — `gatewayctl model-alias set ...` (schema route, multi-instance-consistent) AND `export UPSTREAM_LLM_OPENROUTER_MODEL=...` + container restart (env route, per-instance escape hatch). Operator may use either; both are supported permanently per D-06. The env var is NOT deprecated.

**WARNING-6 (revision iteration 2):** S1 (local-llm primary breaker OPEN) now has a FALLBACK breaker driver — `docker stop ai-gateway-primary-llm` or equivalent — to drive the breaker via real failure observations rather than CLI force. Documented expected time-to-OPEN (3 consecutive failures × eval tick) so operator knows when to proceed. S2/S3 (Whisper + Embed primary breaker OPEN) explicitly REQUIRE `gatewayctl breaker force-open` because they have no real-failure driver without breaking OpenAI keys. Documented explicitly.

**WARNING-5 (revision iteration 2):** Task 4 cascade-closure verify shell uses POSITIVE assertions (each cascade SC must `status: passed` AFTER the commits land), not the fragile `grep -L ... | wc -l | grep -q '^3$'` shape. Grep patterns calibrated to the actual YAML/markdown shape of each VERIFICATION.md (Phase 02 + 03 + 05) — read each VERIFICATION.md first to confirm the exact format.

This is the FINAL plan in Phase 06.9. After this plan signs:
- Phase 06.9 status: complete (SC1-CLOSE met, all 4 requirement IDs verified end-to-end)
- Phase 02 SC-5 step 7 chat E2E deferral CLOSED (was waiting for tier-1 to actually return 200 instead of 404)
- Phase 03 SC-1 live UAT deferral CLOSED (was waiting for full failover chain to function)
- Phase 05 SC-1 deferral CLOSED (was waiting for tier-1 to actually serve overflow traffic instead of 502/404)

**This plan is autonomous: false.** Task 2 + Task 3 are BLOCKING human-verify checkpoints requiring real spend (OpenRouter + OpenAI API calls, no GPU/Vast spend).

Estimated cost: ~$0.05 in OpenRouter + OpenAI usage (a few completions + 1 short transcription + 1 embedding). Abort if exceeds $1.00 cumulative (R2 spend cap). ZERO Vast / GPU spend.

Estimated wall: 45-60 minutes operator-driven, including pre-UAT preconditions task.

Output: 6 signed scenarios + 3 cascade-closure VERIFICATION.md edits (verified via positive-assertion grep) + Pre-UAT preconditions evidence captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-CONTEXT.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-VALIDATION.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-REVIEWS.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-04-SUMMARY.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-05a-SUMMARY.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-05b-SUMMARY.md

@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-HUMAN-UAT.md
@.planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md
@.planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md
@.planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md
@.planning/phases/02-gateway-core-multi-tenant-auth/02-UAT-2026-05-23.md
@.planning/phases/05-load-shedding-saturation-aware-routing/05-UAT-2026-05-13.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 06.9-HUMAN-UAT.md scoresheet with R2 hardened Pre-UAT preconditions + 6 scenarios + D-06 coequal-paths setup + WARNING-6 dual breaker drivers + cleanup</name>
  <files>.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-HUMAN-UAT.md (style template — 6-scenario sign-off shape; copy headers + sign-off line conventions)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-VALIDATION.md (S1-S6 already drafted at the bottom; rewrite as operator-facing scenarios with exact curl commands + expected responses)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-04-SUMMARY.md (read Plan 04's breaker decision — `gatewayctl breaker force-open --ttl` is the primary breaker driver; ALSO read Plan 04 R1 acceptance to understand the audit + TTL constraints)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md (lines 488-538 — curl recipes for S1/S2/S3 verbatim)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-REVIEWS.md (R2 hardened guardrails: OpenAI key hard gate, spend cap, breaker-restore on abort, migration-rollback, token-redaction)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-CONTEXT.md (D-06 — env-override-wins coequal-paths)
    - .planning/phases/02-gateway-core-multi-tenant-auth/02-UAT-2026-05-23.md (step 7 chat E2E format for S5 re-run)
    - .planning/phases/05-load-shedding-saturation-aware-routing/05-UAT-2026-05-13.md (vegeta recipe for S6 re-run)
  </read_first>
  <acceptance_criteria>
    - File `06.9-HUMAN-UAT.md` exists with the following sections, in order:
      1. **Header** — Phase 06.9, date, status=in_progress, operator, expected wall time, expected $ spend (target ≤ $0.05; abort criteria $1.00 cumulative per R2)
      2. **Pre-UAT preconditions and operator safeguards (NEW per R2 — task-shaped block, with 6 HARD gates that must each be checked before S1 can start):**
         - **Gate A — OpenAI key availability (HARD GATE for S2/S3):** Operator runs `curl -sS -H "Authorization: Bearer $UPSTREAM_STT_OPENAI_AUTH_BEARER" https://api.openai.com/v1/models -o /dev/null -w "%{http_code}"` from ops-claude AND from inside ai-gateway-dev container. Both must return `200`. If `401` or `404`: STOP. Operator obtains a working OpenAI key from https://platform.openai.com/api-keys, applies it to `/opt/ai-gateway-dev/.env` (both `UPSTREAM_STT_OPENAI_AUTH_BEARER` + `UPSTREAM_EMBED_OPENAI_AUTH_BEARER`), recreates gateway container, re-runs Gate A. **DO NOT proceed to S2 or S3 until both curls return 200.**
         - **Gate B — OpenRouter key still valid:** Operator runs `curl -sS -X POST https://openrouter.ai/api/v1/chat/completions -H "Authorization: Bearer $UPSTREAM_LLM_OPENROUTER_AUTH_BEARER" -H "Content-Type: application/json" -d '{"model":"qwen/qwen3.5-27b","provider":{"order":["novita"]},"messages":[{"role":"user","content":"ping"}],"max_tokens":5}'`. Must return HTTP 200 + JSON body. If `401` or `402`: operator refreshes key on https://openrouter.ai/keys and updates env + recreates container.
         - **Gate C — Migration 0026 applied:** Operator runs `ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl migrate status'` (or psql query against schema). Must show `0026_evolve_model_aliases_per_upstream.sql` applied (or whichever number Plan 01 landed). If not applied: operator runs migrate Up + recreates container.
         - **Gate D — Latest image deployed:** Operator runs `ssh vps-ifix-vm 'docker inspect ai-gateway-dev | grep Image'`. Compare image SHA to develop tip after Phase 06.9 plans 01-05b merge. If stale: `docker compose -f /opt/ai-gateway-dev/docker-compose.yml pull && docker compose -f /opt/ai-gateway-dev/docker-compose.yml up -d --force-recreate gateway`.
         - **Gate E — Container healthy:** `docker compose -f /opt/ai-gateway-dev/docker-compose.yml ps` shows ai-gateway-dev Up + healthy. `docker logs ai-gateway-dev --tail 100` shows the `Phase 06.9` boot lines (resolver refreshed with 6 rows; force_override read path active; optionally INFO `env override active for upstream` lines if any UPSTREAM_*_MODEL env vars are set).
         - **Gate F — Spend cap acknowledged:** Operator pre-sets a mental abort criterion: "If cumulative OpenRouter + OpenAI spend in S1-S6 exceeds $1.00, ABORT and triage" (per R2 + per `OPENROUTER_HTTP_REFERER` set to identifying value `ifix-uat-069-${OPERATOR_INITIALS}` for traceability on the OpenRouter dashboard). Also acknowledges abort criterion: "If any single S1/S4 OpenRouter call exceeds $0.02 (per response.usage.total_tokens × pricing) STOP and investigate before continuing."
         - Each gate has a sign-off line: `Gate A: [ ] PASS [ ] FAIL  Evidence: ___  Operator: ___  Date: ___`
         - **AUTONOMOUS: false** — this is the FIRST checkpoint of Plan 06; orchestrator pauses here. Operator confirms ALL 6 gates passed before proceeding.
      3. **R2 Token-redaction reminder** (banner at the top of every evidence box):
         > **REDACT BEFORE PASTING:** Replace all `Authorization: Bearer sk-...` strings with `Authorization: Bearer <REDACTED>`. Replace all real OpenAI/OpenRouter/Cloudflare tokens with `<REDACTED-OPENAI-KEY>` / `<REDACTED-OPENROUTER-KEY>`. NEVER paste a real key into this markdown. UAT evidence is committed to git — leaked tokens require rotation.
      4. **S1 — Chat tier-1 (OpenRouter):** exact curl command (use env vars in shell, NOT inline keys), expected response shape (HTTP 200 + Qwen content + provider Novita or DeepInfra), PASS/FAIL sign-off line + evidence box (paste redacted response + timestamp + operator initials)
         - **BLOCKER-1 / D-06 — Setup options (BOTH supported coequally):** S1 must document BOTH operator-override paths:
           - **Option A (schema route, multi-instance-consistent):** `ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl model-alias set --alias=qwen --upstream=openrouter-chat --target=qwen/qwen3.5-27b'` (verify via `model-alias list`). This is the default; the seeded migration row already does this so no action needed unless operator wants to test the CLI.
           - **Option B (env route, per-instance escape hatch):** edit `/opt/ai-gateway-dev/.env`, set `UPSTREAM_LLM_OPENROUTER_MODEL=qwen/qwen3.5-27b`, run `docker compose -f /opt/ai-gateway-dev/docker-compose.yml up -d --force-recreate gateway`, verify boot log includes `env override active for upstream upstream=openrouter-chat env_var=UPSTREAM_LLM_OPENROUTER_MODEL`. **Operator may use either path; both supported per D-06.**
         - **WARNING-6 — Breaker driver options (S1 has TWO options):**
           - **Primary driver (recommended, fast):** `ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl breaker force-open --upstream local-llm --ttl 5m'`. Immediate effect on next eval tick (~1-5s per Plan 04 Task 2 cadence).
           - **Fallback driver (alternative, real-failure-driven):** `ssh vps-ifix-vm 'docker stop ai-gateway-primary-llm'` (or equivalent — verify the actual container name via `docker ps`). The breaker observes real failures and trips OPEN after `ConsecutiveFailures=3` × eval-tick observations (~3-15s typical). After UAT: `docker start ai-gateway-primary-llm` to restore. This option exercises the real failover path WITHOUT requiring the operator CLI; useful for verifying the breaker observation logic alongside the override CLI.
         - **R2 Cleanup-on-abort:** if S1 FAILs or operator interrupts: `ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl breaker force-close --upstream local-llm'` MUST run before exit (covers Option-A path). If Option-B driver was used (docker stop): `docker start ai-gateway-primary-llm` MUST also run.
      5. **S2 — STT tier-1 (OpenAI Whisper):** exact curl command using `/tmp/probe.wav` (operator scp's from local `gateway/internal/upstreams/testdata/probe.wav`), expected response (HTTP 200 + `{"text":"..."}`), sign-off + evidence box
         - **WARNING-6 — Breaker driver:** `gatewayctl breaker force-open --upstream local-stt --ttl 5m` **REQUIRED**. No real-failure driver is documented for S2 — the local Whisper pod can't be stopped without breaking other UAT scenarios, and stopping the external OpenAI upstream would break the OpenAI key validity gate. Explicitly note: "S2 REQUIRES `gatewayctl breaker force-open` — no equivalent fallback driver is documented for external upstreams."
         - **R2 Cleanup-on-abort:** `gatewayctl breaker force-close --upstream local-stt`
      6. **S3 — Embed tier-1 (OpenAI):** exact curl command, expected response (HTTP 200 + 1024-dim embedding array), sign-off + evidence box
         - **WARNING-6 — Breaker driver:** `gatewayctl breaker force-open --upstream local-embed --ttl 5m` **REQUIRED**. Same rationale as S2 (no real-failure driver for external upstreams).
         - **R2 Cleanup-on-abort:** `gatewayctl breaker force-close --upstream local-embed`
      7. **S4 — Phase 03 SC-1 cascade re-run:** cross-reference to `03-HUMAN-UAT.md` SC-1 scenario steps (re-run identically); operator confirms PASS; sign-off + evidence
      8. **S5 — Phase 02 SC-5 step 7 cascade:** re-run the step 7 chat E2E from `02-UAT-2026-05-23.md`; sign-off + evidence
      9. **S6 — Phase 05 SC-1 cascade:** re-run the vegeta burst from `05-UAT-2026-05-13.md` (5 RPS × 30s); expected: tier-1 200s (NOT 502/404); sign-off + evidence
      10. **R2 Migration-rollback procedure (NEW):**
         > If migration 0026 must be reverted mid-UAT:
         > 1. STOP all active S-scenarios; run breaker force-close for any upstream still forced open.
         > 2. `ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl migrate down -n 1'` (single step Down). The R3 Down guard will ABORT with an explicit error if operator-created duplicate-alias rows exist (e.g., a `gatewayctl model-alias set` call landed during UAT). Recovery: `DELETE FROM ai_gateway.model_aliases WHERE upstream_name NOT IN ('local-llm','local-stt','local-embed','openrouter-chat','openai-whisper','openai-embed');` then re-run `migrate down -n 1`.
         > 3. After Down succeeds: `docker compose -f /opt/ai-gateway-dev/docker-compose.yml restart gateway`. The resolver Refresh on boot will reload the 3-row pre-Phase-06.9 state; tier-1 chain will revert to producing HTTP 404 (the original bug).
         > 4. To re-apply Phase 06.9 after rollback: `gatewayctl migrate up` + container restart.
      11. **Cleanup section (R2 hardened):**
         - **MANDATORY:** Run `gatewayctl breaker force-close --upstream X` for EVERY upstream forced open during S1-S3 (verify with `gatewayctl breaker list` — no FORCED_OPEN rows remain)
         - **If S1 fallback driver used (docker stop):** Run `docker start ai-gateway-primary-llm` to restore primary
         - Restore primary pod state via `gatewayctl primary force-up` if S1-S3 inadvertently affected primary state (S1-S3 with force-open should NOT touch primary FSM; verify `gatewayctl primary state` shows pre-UAT value)
         - Confirm `docker compose ps` healthy + no orphan resources
         - **D-06 cleanup (only if Option B / env route was exercised in S1 setup):** If `UPSTREAM_LLM_OPENROUTER_MODEL` was set in env to test the env path, decide whether to keep it or remove it. If removing: edit `.env`, remove the line, recreate container, verify boot log no longer shows `env override active for upstream`.
         - Final spend check: query OpenRouter dashboard for total spend on `ifix-uat-069-<operator>` referer; query OpenAI dashboard usage; record exact $ in the final sign-off block
      12. **Final sign-off block:** "All 6 scenarios PASS — Phase 06.9 ready for cascade closure" + operator initials + final timestamp + total spend
    - Each scenario includes:
      - Exact shell commands ready to copy-paste (using shell env vars, NOT inline keys per R2 token redaction)
      - Exact expected HTTP status + response shape
      - "Common failure modes" callout (e.g., S1: if HTTP 404 received, verify migration 0026 applied via `gatewayctl model-alias list` shows 6 rows; verify `docker logs | grep "dispatching upstream=openrouter-chat"`)
      - Sign-off line: `**Status:** [ ] PASS [ ] FAIL    **Operator:** ___    **Date:** ___    **Evidence (REDACTED):** [paste response with keys/tokens replaced]`
      - **R2 cleanup-on-abort instruction** at the bottom of every scenario that runs force-open
    - Estimated time per scenario in the header (target: S1=5min, S2=10min including key application if not done, S3=5min, S4=5min cascade-confirm, S5=5min cascade-confirm, S6=10min vegeta run; Pre-UAT preconditions=10min)
    - Total target wall: 55-70min
  </acceptance_criteria>
  <action>
    Use `06.7-HUMAN-UAT.md` as the styling template. Substitute the 6 Phase 06.9 scenarios using the recipes from `06.9-VALIDATION.md` and `06.9-RESEARCH.md`.

    **R2 hardened content:** add the Pre-UAT preconditions section at the very top (after the header) with 6 hard gates (A-F) each with its own sign-off line. Make the section visually distinct (e.g., box-drawn callout via `> ` quotes + bold headers). The operator must check off ALL gates before S1.

    For S1, the exact recipe is in RESEARCH.md gap evidence section (verified live 2026-05-24 — the failing case). Adapt: **document BOTH operator-override paths per D-06 (Option A — CLI schema row; Option B — env var + restart) and BOTH breaker drivers per WARNING-6 (primary `gatewayctl breaker force-open`; fallback `docker stop ai-gateway-primary-llm`).** Expected result CHANGES from HTTP 404 HTML to HTTP 200 + Qwen JSON.

    For S2, use `gateway/internal/upstreams/testdata/probe.wav` (50KB existing fixture). Instruct operator to scp it from the dev box: `scp ./gateway/internal/upstreams/testdata/probe.wav vps-ifix-vm:/tmp/probe.wav` BEFORE running the curl. **Explicit note: S2 REQUIRES `gatewayctl breaker force-open` — no real-failure fallback for external upstreams.**

    For S3, send 2-input batch to verify list-response shape; assert `data[].embedding` length == 1024. **Same explicit note as S2: REQUIRES force-open.**

    For S4/S5/S6 (cascade scenarios), the operator simply re-runs the previously-failing scenario from each prior phase's UAT doc and confirms PASS this time. Each cascade scenario links to the original UAT doc — operator copies the exact prior failing-curl from the prior doc and runs it now.

    **R2 token redaction banner** at the top of every evidence box. Pre-format the curl commands to use shell variables (`-H "Authorization: Bearer $UPSTREAM_LLM_OPENROUTER_AUTH_BEARER"`) — operator never types the real key, never has to redact.

    **R2 cleanup-on-abort** at the bottom of every force-open-using scenario. Bold + explicit: "Always run on abort: `gatewayctl breaker force-close --upstream X`". For S1 if fallback driver used: also `docker start ai-gateway-primary-llm`.

    **R2 migration-rollback procedure** as a self-contained section near the end, separate from the scenarios.

    Cleanup section MUST include the inverse of every state change S1-S6 made. Operator confirms `docker compose ps` + `gatewayctl primary state` + `gatewayctl breaker list` return clean baseline state. If Option B / env route was used in S1, document the env-var cleanup choice.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "Pre-UAT preconditions" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "Gate A — OpenAI key availability" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S1 — Chat tier-1" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S2 — STT tier-1" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S3 — Embed tier-1" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S4 — Phase 03 SC-1" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S5 — Phase 02" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S6 — Phase 05" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "Migration-rollback procedure" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "REDACT BEFORE PASTING" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "Cleanup" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -qE 'Option A.*schema|model-alias set' .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -qE 'Option B.*env|UPSTREAM_LLM_OPENROUTER_MODEL' .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -qE 'docker stop ai-gateway-primary-llm|fallback driver' .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "REQUIRES \`gatewayctl breaker force-open\`" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md</automated>
  </verify>
  <done>06.9-HUMAN-UAT.md authored with R2 hardened Pre-UAT preconditions + token-redaction banners + cleanup-on-abort + migration-rollback procedure + 6 scenarios + D-06 coequal-paths setup options (S1 Option A schema + Option B env) + WARNING-6 dual breaker drivers (S1 primary + fallback; S2/S3 REQUIRE force-open with explicit rationale) + cleanup + sign-off blocks; ready for operator execution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING — operator runs Pre-UAT preconditions (Gates A-F) — R2 mandatory hard-gate task</name>
  <what-built>
    - 06.9-HUMAN-UAT.md authored with R2 hardened Pre-UAT preconditions section (Task 1 output)
    - Phase 06.9 Plans 01-05b fully shipped on develop
    - Dev stack at /opt/ai-gateway-dev/ on vps-ifix-vm received the new gateway image
  </what-built>
  <how-to-verify>
    Operator follows `06.9-HUMAN-UAT.md` Pre-UAT preconditions section, gates A-F:

    1. **Gate A — OpenAI key availability (HARD GATE):** Run direct OpenAI API curl from both ops-claude AND from inside ai-gateway-dev container. Both must return 200. If FAIL: apply working OpenAI key to `/opt/ai-gateway-dev/.env`, recreate gateway container, re-run Gate A. Do NOT proceed to S2/S3 until both curls return 200.

    2. **Gate B — OpenRouter key still valid:** Direct curl ops-claude → OpenRouter chat completions endpoint. Must return 200 + JSON body. If 401/402: refresh key.

    3. **Gate C — Migration 0026 applied:** `gatewayctl migrate status` shows 0026 applied. If not: apply + recreate.

    4. **Gate D — Latest image deployed:** Compare image SHA to develop tip. If stale: pull + recreate.

    5. **Gate E — Container healthy:** `docker compose ps` healthy + Phase 06.9 boot lines visible in logs (resolver refreshed 6 rows; force_override active; optional env-override INFO lines if any UPSTREAM_*_MODEL set).

    6. **Gate F — Spend cap acknowledged:** Operator confirms abort criteria ($1.00 cumulative, $0.02 per call) + OPENROUTER_HTTP_REFERER set.

    Sign each gate PASS/FAIL with evidence + initials + date.

    **Resume only when ALL 6 gates show PASS.**
  </how-to-verify>
  <resume-signal>
    Operator types "all gates passed" to acknowledge sign-off and proceed to Task 3 (S1-S6 execution). If any gate FAIL: operator addresses the underlying issue (apply key, run migration, etc.) and re-runs that gate before resuming.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING — operator runs 06.9-HUMAN-UAT.md S1-S6 + cleanup + signs evidence</name>
  <what-built>
    - Task 2 Pre-UAT preconditions ALL PASSED (Gates A-F signed off)
    - 06.9-HUMAN-UAT.md authored with R2 hardened guardrails + D-06 coequal-paths + WARNING-6 dual breaker drivers (Task 1)
    - All unit + integration tests GREEN; develop tip carries the full Phase 06.9 fix
    - Dev stack at /opt/ai-gateway-dev/ on vps-ifix-vm received the new gateway image
  </what-built>
  <how-to-verify>
    Operator follows `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md` end-to-end:

    1. **S1 (Chat OpenRouter, ~5min)**: **Operator chooses ONE override path AND ONE breaker driver:**
       - Override path: Option A (`gatewayctl model-alias set ...` — schema route) OR Option B (`export UPSTREAM_LLM_OPENROUTER_MODEL=... + restart` — env route). Both supported per D-06.
       - Breaker driver: Primary `gatewayctl breaker force-open --upstream local-llm --ttl 5m` OR Fallback `docker stop ai-gateway-primary-llm` (real failure, ~3-15s to OPEN).
       - Then: curl POST /v1/chat/completions with model=qwen + ping message; expect HTTP 200 + Qwen JSON with provider Novita preferred (DeepInfra acceptable); paste REDACTED response + sign PASS.
       - **On abort:** if Option A primary driver used → `gatewayctl breaker force-close --upstream local-llm`. If Option B env route used → restore env file. If Fallback driver used → `docker start ai-gateway-primary-llm`.

    2. **S2 (Whisper OpenAI, ~10min)**: scp probe.wav to vps-ifix-vm; **`gatewayctl breaker force-open --upstream local-stt --ttl 5m` (REQUIRED — no fallback driver)**; curl POST /v1/audio/transcriptions with file=@/tmp/probe.wav + model=whisper; expect HTTP 200 + {"text":"..."}; paste REDACTED response + sign PASS. **On abort: `gatewayctl breaker force-close --upstream local-stt`.**

    3. **S3 (Embed OpenAI, ~5min)**: **`gatewayctl breaker force-open --upstream local-embed --ttl 5m` (REQUIRED — no fallback driver)**; curl POST /v1/embeddings with bge-m3 + 2 inputs; expect HTTP 200 + 2 embeddings × 1024 dims; sign PASS. **On abort: `gatewayctl breaker force-close --upstream local-embed`.**

    4. **S4 (Phase 03 SC-1 cascade, ~5min)**: re-run 03-HUMAN-UAT.md SC-1; sign PASS.

    5. **S5 (Phase 02 SC-5 step 7 cascade, ~5min)**: re-run 02-UAT-2026-05-23.md step 7; sign PASS.

    6. **S6 (Phase 05 SC-1 cascade, ~10min)**: re-run 05-UAT-2026-05-13.md vegeta burst; assert tier-1 200s instead of prior 502/404; sign PASS.

    7. **Cleanup (R2 mandatory)**:
       - Run `gatewayctl breaker force-close` for EVERY upstream forced open during S1-S3 (verify `gatewayctl breaker list` shows no FORCED_OPEN rows)
       - If S1 fallback driver was used (docker stop): also `docker start ai-gateway-primary-llm`
       - If S1 Option B / env route was used: decide whether to keep env override or remove it
       - Confirm `gatewayctl primary state` returns pre-UAT value (S1-S3 should not have affected primary)
       - Confirm `docker compose ps` healthy

    8. **Final sign-off** in 06.9-HUMAN-UAT.md: "All 6 scenarios PASS — Phase 06.9 ready for cascade closure" + operator initials + date + TOTAL spend recorded.

    **R2 abort criteria:**
    - Total cumulative OpenRouter + OpenAI spend exceeds $1.00 → STOP, triage, write FAIL evidence
    - Any single S1/S4 call exceeds $0.02 (per response.usage.total_tokens × pricing) → STOP, investigate
    - Migration rollback required mid-UAT → follow the R2 migration-rollback procedure section verbatim

    **Failure handling:**
    - If S1 returns HTTP 404 + HTML body: migration 0026 didn't apply OR resolver didn't refresh OR director constructor didn't get the resolver+name. Inspect: `gatewayctl model-alias list` (should show 6 rows); `docker logs ai-gateway-dev | grep 'model aliases refreshed'` (should show count=6); `docker logs ai-gateway-dev | grep 'dispatching upstream=openrouter-chat'` (should appear after the request). Report finding back via `/gsd:plan-phase 06.9 --gaps`.
    - If S2 returns HTTP 400 from OpenAI: model field rewrite failed OR audio bytes corrupted. Run TestIntegration_OpenAIWhisperModelRewrite locally to isolate.
    - If S6 vegeta shows tier-1 errors but S1 PASSes: load-shedding interaction; capture vegeta detailed report + open follow-up.
  </how-to-verify>
  <resume-signal>
    Operator types "approved" to acknowledge sign-off OR describes a specific FAIL with the affected scenario(s). If FAIL: do NOT proceed to Task 4 (cascade closure); instead run `/gsd:plan-phase 06.9 --gaps` to plan the gap-closure fix. R2 cleanup steps MUST run regardless of pass/fail.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Cascade-close Phase 02, 03, 05 VERIFICATION.md to flip deferred SCs to PASS (WARNING-5 positive-assertion grep)</name>
  <files>.planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md, .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md, .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md — full file. Confirm the EXACT frontmatter / SC-table shape for SC-5 step 7. From the prior planner read: file has `status: passed_partial` at line 4 + a `score:` line at 5 + a `previous_status: human_needed` field + a markdown table row for SC-5 step 7 referenced inline. Identify the EXACT line(s) that must flip.
    - .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md — full file. Confirm: `status: human_needed` at line 4 + `must_haves_passed: 32` + `SC-1: PARTIAL` at line 8 (frontmatter sub-key). Identify the EXACT line that must flip to `SC-1: passed` (or `status: passed` at file level).
    - .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md — full file. Confirm: `status: passed_partial` at line 4 + `score: 8/8 must-haves verified ...; SC-1 PARTIAL` at line 5. Identify the EXACT line(s) that must flip.
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md (post-Task-3: read the signed S4/S5/S6 evidence blocks for verbatim quotation in the cascade closures)
  </read_first>
  <acceptance_criteria>
    - **Phase 02 02-VERIFICATION.md SC-5 step 7**: the frontmatter `status: passed_partial` flips to `status: passed`; the inline reference to "step 7 chat E2E deferred to Phase 03/06.6" updates to "step 7 chat E2E closed 2026-MM-DD via Phase 06.9 — see 06.9-HUMAN-UAT.md S5 evidence". Add a Phase 06.9 closure note quoting S5 evidence.
    - **Phase 03 03-VERIFICATION.md SC-1**: the frontmatter `status: human_needed` flips to `status: passed`; the `SC-1: PARTIAL` sub-key flips to `SC-1: passed`. Add a Phase 06.9 closure note quoting S4 evidence: "Closed 2026-MM-DD via Phase 06.9 — full failover chain (local-llm breaker OPEN → dispatcher picks openrouter-chat → director rewrites model → OpenRouter returns 200 Qwen) verified end-to-end on dev stack. Evidence: see `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md` S4 sign-off."
    - **Phase 05 05-VERIFICATION.md SC-1**: the frontmatter `status: passed_partial` flips to `status: passed`; the inline `SC-1 PARTIAL` reference updates to `SC-1 passed`. Add a Phase 06.9 closure note quoting S6 evidence: "Closed 2026-MM-DD via Phase 06.9 — vegeta 5 RPS × 30s burst now returns tier-1 200s (was 502/404 pre-OR-FIX because model name was forwarded as 'qwen' literal). Evidence: see `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md` S6 sign-off."
    - Each VERIFICATION.md edit is SURGICAL: change ONLY the SC status line + add the closure paragraph. Do NOT touch other SCs in those docs.
    - Each edit committed in a separate small commit (3 commits total) so cascade attribution is clean:
      - `docs(02): close SC-5 step 7 via Phase 06.9 cascade`
      - `docs(03): close SC-1 via Phase 06.9 cascade`
      - `docs(05): close SC-1 via Phase 06.9 cascade`
  </acceptance_criteria>
  <action>
    For each VERIFICATION.md:
    1. Locate the relevant SC section + frontmatter status line.
    2. Update the status marker:
       - **Phase 02:** `status: passed_partial` → `status: passed`. Inline reference "step 7 chat E2E deferred to Phase 03/06.6" → "step 7 chat E2E closed 2026-MM-DD via Phase 06.9 — see 06.9-HUMAN-UAT.md S5 evidence".
       - **Phase 03:** `status: human_needed` → `status: passed`. The frontmatter sub-key `SC-1: PARTIAL` → `SC-1: passed`.
       - **Phase 05:** `status: passed_partial` → `status: passed`. Inline `SC-1 PARTIAL` → `SC-1 passed`.
    3. Insert a closure note paragraph referencing the Task 3 signed evidence and the date of operator sign-off (read from 06.9-HUMAN-UAT.md).
    4. Stage + commit with the scoped message above.

    Use direct git invocations for each commit. Capture each commit SHA into 06.9-06-SUMMARY.md cascade table:

    ```bash
    # Phase 02 cascade close
    git add .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md
    git commit -m "docs(02): close SC-5 step 7 chat E2E via Phase 06.9 model rewrite (06.9-06)"
    git rev-parse HEAD  # capture SHA into 06.9-06-SUMMARY.md cascade table

    # Phase 03 cascade close
    git add .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md
    git commit -m "docs(03): close SC-1 failover chain via Phase 06.9 model rewrite (06.9-06)"
    git rev-parse HEAD  # capture SHA

    # Phase 05 cascade close
    git add .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md
    git commit -m "docs(05): close SC-1 saturation overflow via Phase 06.9 model rewrite (06.9-06)"
    git rev-parse HEAD  # capture SHA
    ```

    Three SHAs MUST be recorded into 06.9-06-SUMMARY.md in a "Cascade Commits" table (columns: Phase | SC | Commit SHA | Date).

    After all 3 commits land, write `06.9-06-SUMMARY.md` AND `06.9-VERIFICATION.md` (phase-level VERIFICATION) capturing the full Phase 06.9 status: all 4 requirement IDs verified (OR-FIX + STT-OAI-FIX + EMBED-OAI-FIX + SC1-CLOSE), 6 scenarios PASS in 06.9-HUMAN-UAT.md, 3 cascade closures landed (with SHAs captured), no open Tech Debt from this phase, Pre-UAT preconditions evidence preserved.
  </action>
  <verify>
    <automated>set -e; \
echo "Phase 02 — verify SC-5 step 7 closure (positive assertion)"; \
grep -qE '^status:\s*passed\s*$' .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md || (echo "FAIL: Phase 02 status not flipped to passed" && exit 1); \
grep -q "Phase 06.9" .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md || (echo "FAIL: Phase 02 missing 06.9 closure note" && exit 1); \
grep -q "06.9-HUMAN-UAT.md" .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md || (echo "FAIL: Phase 02 missing UAT cross-ref" && exit 1); \
\
echo "Phase 03 — verify SC-1 closure (positive assertion)"; \
grep -qE '^status:\s*passed\s*$' .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md || (echo "FAIL: Phase 03 status not flipped to passed" && exit 1); \
grep -qE 'SC-1:\s*passed\s*$' .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md || (echo "FAIL: Phase 03 SC-1 sub-key not flipped" && exit 1); \
grep -q "Phase 06.9" .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md || (echo "FAIL: Phase 03 missing 06.9 closure note" && exit 1); \
grep -q "06.9-HUMAN-UAT.md" .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md || (echo "FAIL: Phase 03 missing UAT cross-ref" && exit 1); \
\
echo "Phase 05 — verify SC-1 closure (positive assertion)"; \
grep -qE '^status:\s*passed\s*$' .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md || (echo "FAIL: Phase 05 status not flipped to passed" && exit 1); \
grep -q "Phase 06.9" .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md || (echo "FAIL: Phase 05 missing 06.9 closure note" && exit 1); \
grep -q "06.9-HUMAN-UAT.md" .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md || (echo "FAIL: Phase 05 missing UAT cross-ref" && exit 1); \
\
echo "all 3 cascade SCs flipped to passed"</automated>
  </verify>
  <done>Phase 02/03/05 VERIFICATION.md cascade-closed for the 3 SCs Phase 06.9 was promised to resolve; 3 small docs commits land; positive-assertion grep (WARNING-5) confirms each `status: passed` AND each cross-ref to 06.9-HUMAN-UAT.md is present; phase-level 06.9-VERIFICATION.md authored.</done>
</task>

</tasks>

<verification>
- `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md` exists with R2 Pre-UAT preconditions section (Gates A-F) + 6 signed scenarios (each Status: [x] PASS line filled in by operator) + D-06 coequal-paths setup options + WARNING-6 dual breaker drivers
- Pre-UAT preconditions task (Task 2) signed off — ALL 6 gates PASS
- Operator total spend < $1.00 (OpenRouter + OpenAI combined; zero Vast/GPU spend) — per R2 spend cap
- 3 cascade VERIFICATION.md edits landed in 3 distinct commits
- WARNING-5 positive-assertion verify shell passes — each phase has `status: passed` AND `Phase 06.9` closure note AND `06.9-HUMAN-UAT.md` cross-ref
- Phase-level `06.9-VERIFICATION.md` exists with status=passed
- `06.9-06-SUMMARY.md` exists and references all 6 PASS scenarios + Pre-UAT preconditions evidence + cascade evidence (3 SHAs)
- Phase 06.9 STATE.md update reflects completion (handled by orchestrator post-plan)
</verification>

<success_criteria>
- R2 Pre-UAT preconditions hardened (OpenAI key hard gate, spend cap, OpenRouter referer, container health, migration check, image SHA) — operator-driven, blocking
- R2 token-redaction guidance in every evidence box
- R2 cleanup-on-abort instructions in every force-open scenario (covering both breaker-driver options for S1)
- R2 migration-rollback procedure documented in self-contained section
- BLOCKER-1 / D-06: S1 setup documents BOTH operator-override paths (CLI schema + env var) coequally
- WARNING-5: cascade closure verify uses positive-assertion grep (no fragile `grep -L | wc -l` shell)
- WARNING-6: S1 has TWO breaker driver options (force-open primary + docker stop fallback); S2/S3 explicitly REQUIRE force-open with rationale
- All 4 phase requirement IDs verified end-to-end:
  - OR-FIX: S1 PASS + integration TestIntegration_OpenRouterModelRewrite PASS + R13 historical-bug regression PASS + D-06 env-override-wins integration PASS
  - STT-OAI-FIX: S2 PASS + integration TestIntegration_OpenAIWhisperModelRewrite (+ R6 edge cases — no SKIPs) PASS
  - EMBED-OAI-FIX: S3 PASS + integration TestIntegration_OpenAIEmbedModelRewrite PASS
  - SC1-CLOSE: S4 (Phase 03 SC-1) + S5 (Phase 02 step 7) + S6 (Phase 05 SC-1) all PASS
- 3 prior phase deferrals cascade-closed in their VERIFICATION.md files
- Phase 06.9 marked complete; ready for Phase 07 (Observability) to begin
- Net result: gateway returns real tier-1 completions when primary is down; the original gap from Phase 03 Wave 0 Gate A (2026-04-20) is closed; operators have BOTH the env-var override path AND the CLI schema-row override path
</success_criteria>

<output>
Create `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-06-SUMMARY.md` AND `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-VERIFICATION.md` when done. Both files reference the operator sign-off evidence in 06.9-HUMAN-UAT.md.

SUMMARY captures: Pre-UAT preconditions evidence (Gates A-F sign-offs); operator sign-off dates per scenario; final spend tally; the chosen S1 override path (Option A or B) + breaker driver (primary or fallback) — operator records which was tested; any tech debt surfaced during UAT (e.g., if S5 cleanup needs a manual workaround); the 3 cascade-close commit SHAs.

VERIFICATION captures: status=passed for all 4 requirement IDs; cross-references to 06.9-HUMAN-UAT.md scenario evidence per ID; cross-references to the 3 cascade-closed VERIFICATION.md files.
</output>
