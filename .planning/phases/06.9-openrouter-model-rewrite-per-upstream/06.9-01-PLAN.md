---
phase: 06.9-openrouter-model-rewrite-per-upstream
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - gateway/db/migrations/0026_evolve_model_aliases_per_upstream.sql
  - gateway/db/queries/model_aliases.sql
  - gateway/internal/db/gen/model_aliases.sql.go
  - gateway/internal/db/gen/models.go
  - gateway/internal/db/migrate_test.go
  - .planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md
autonomous: true
requirements: [OR-FIX]
tags: [migration, sqlc, postgres, model_aliases, wave0-gate-a]

must_haves:
  truths:
    - "model_aliases table PK is composite (alias, upstream_name)"
    - "Six rows seeded: 3 tier-0 backfilled + 3 tier-1 new (qwen+openrouter-chat → qwen/qwen3.5-27b; whisper+openai-whisper → whisper-1; bge-m3+openai-embed → text-embedding-3-small)"
    - "sqlc-generated AiGatewayModelAlias struct has UpstreamName field"
    - "Boot order is migrate Up → sqlc cache → Resolver.NewResolver (verified by inspection of main.go init sequence)"
    - "03-WAVE0-GATES.md no longer documents UPSTREAM_LLM_OPENROUTER_URL=/api/v1 (corrected to /api with explicit rationale)"
    - "03-WAVE0-GATES.md documents UPSTREAM_LLM_OPENROUTER_MODEL as the runtime fallback override per D-06 (env wins over schema row when set); not deprecated"
    - "Down migration aborts with explicit operator-actionable error when operator-created rows share an alias across multiple upstreams (R3 guard); clean Up→Down→Up succeeds when only seeded rows are present"
  artifacts:
    - path: "gateway/db/migrations/0026_evolve_model_aliases_per_upstream.sql"
      provides: "PK widening + tier-1 seed + backfill + R3 Down guard"
      contains: "ADD COLUMN IF NOT EXISTS upstream_name"
    - path: "gateway/internal/db/gen/models.go"
      provides: "Regenerated struct with UpstreamName field"
      contains: "UpstreamName string"
    - path: "gateway/internal/db/migrate_test.go"
      provides: "Migration ordering list includes 0026"
      contains: "0026_evolve_model_aliases_per_upstream.sql"
  key_links:
    - from: "gateway/db/migrations/0026_evolve_model_aliases_per_upstream.sql"
      to: "gateway/internal/db/gen/model_aliases.sql.go"
      via: "sqlc generate"
      pattern: "UpstreamName"
    - from: "gateway/internal/db/migrate_test.go"
      to: "gateway/db/migrations/0026_evolve_model_aliases_per_upstream.sql"
      via: "ordering slice"
      pattern: "0026_evolve_model_aliases_per_upstream.sql"
---

<objective>
Wave 0: Schema migration + sqlc regeneration + Wave 0 Gate A doc correction.

Purpose: Widen `ai_gateway.model_aliases` PK from `(alias)` to `(alias, upstream_name)` so the resolver can return per-upstream targets in subsequent plans. Seed the 3 tier-1 rows (qwen/openrouter-chat → qwen/qwen3.5-27b; whisper/openai-whisper → whisper-1; bge-m3/openai-embed → text-embedding-3-small). Backfill existing 3 tier-0 rows with their upstream NAME (local-llm/local-stt/local-embed). Regenerate sqlc-generated Go. Add the new migration filename to `migrate_test.go`. Correct `03-WAVE0-GATES.md` URL convention error (was `/api/v1`; must be `/api` because BuildDirector preserves `/v1/...` path).

Output: Migration 0026 applies cleanly Up + Down. sqlc-generated `AiGatewayModelAlias` struct carries `UpstreamName`. `go test ./gateway/internal/db/...` passes. `03-WAVE0-GATES.md` reads `/api` (not `/api/v1`) AND documents the env-override-wins semantic per D-06.

Scope: schema + regen + 1 doc fix only — no Go source changes outside `db/gen/`.

**R3 (review-mode revision):** Down migration MUST include an explicit guard against operator-created duplicate-alias rows (rows added via `gatewayctl model-alias set` after Up). Down behavior on production-shaped data: (a) `upstream_name` column STAYS (never dropped) so tier-0 backfilled data + any operator-edited rows survive Down; (b) only the 3 seeded tier-1 rows (`upstream_name IN ('openrouter-chat','openai-whisper','openai-embed')`) are deleted; (c) BEFORE restoring `PRIMARY KEY (alias)`, an explicit guard `SELECT` checks for remaining duplicate-alias rows and RAISES EXCEPTION with operator-actionable message if any exist (option (b) per replan mandate — explicit failure beats silent data loss).

**BLOCKER-1 revision (revision iteration 2 — honor D-06):** Earlier wording marked `UPSTREAM_LLM_OPENROUTER_MODEL` as DEPRECATED. CONTEXT.md D-06 actually states the env var "stays as a fallback override (env value wins over schema row if set)." Wave 0 Gate A doc text must reflect this: schema row is the default; env var (when set) takes precedence at resolver-lookup time. Plan 02 implements the env-override-wins layer; this plan's only job here is to update the doc to read the correct semantic. No WARN-on-presence behavior — env presence is supported, not pressured to be migrated away from.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-CONTEXT.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-PATTERNS.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-REVIEWS.md

@gateway/db/migrations/0024_upstreams_tts_role.sql
@gateway/db/migrations/0005_create_model_aliases.sql
@gateway/db/queries/model_aliases.sql
@gateway/internal/db/migrate_test.go
@.planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md

<interfaces>
<!-- Current sqlc-generated struct (from gateway/internal/db/gen/models.go:123-128) -->
<!-- After regen this gains an UpstreamName string field -->

Pre-regen:
```go
type AiGatewayModelAlias struct {
    Alias    string
    Upstream string  // ROLE: 'llm' | 'stt' | 'embed' (CHECK constraint, unchanged)
    Target   string
}
```

Post-regen (this plan adds):
```go
type AiGatewayModelAlias struct {
    Alias        string
    Upstream     string  // ROLE — unchanged
    Target       string
    UpstreamName string  // NEW: actual upstream NAME ('local-llm' | 'openrouter-chat' | ...)
}
```

Existing sqlc queries to update (gateway/db/queries/model_aliases.sql):
```sql
-- name: ListModelAliases :many
SELECT alias, upstream, target FROM ai_gateway.model_aliases;

-- name: GetModelAlias :one
SELECT alias, upstream, target FROM ai_gateway.model_aliases WHERE alias = $1;
```

After this plan:
```sql
-- name: ListModelAliases :many
SELECT alias, upstream, target, upstream_name FROM ai_gateway.model_aliases ORDER BY alias, upstream_name;

-- name: GetModelAlias :one
SELECT alias, upstream, target, upstream_name FROM ai_gateway.model_aliases WHERE alias = $1 AND upstream_name = $2;
```

Wave 0 Gate A line to correct (from .planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md):
- Line ~88 documents `UPSTREAM_LLM_OPENROUTER_URL=https://openrouter.ai/api/v1` — change to `https://openrouter.ai/api` and add inline note explaining BuildDirector preserves `/v1/...` request path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0026 + R3 Down guard + R11 column comment + correct Wave 0 Gate A URL convention doc + document D-06 env-override-wins</name>
  <files>gateway/db/migrations/0026_evolve_model_aliases_per_upstream.sql, .planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md</files>
  <read_first>
    - gateway/db/migrations/0024_upstreams_tts_role.sql (goose convention: SET search_path + -- +goose StatementBegin/End + IF EXISTS guards + ON CONFLICT idempotency)
    - gateway/db/migrations/0005_create_model_aliases.sql (current schema: PK on alias, CHECK upstream IN ('llm','stt','embed'), 3 seed rows)
    - gateway/db/queries/model_aliases.sql (current queries shape — Task 2 will update)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-PATTERNS.md (lines 42-86 — verbatim migration template)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md (lines 173-258 — full migration recipe + Down rationale)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-REVIEWS.md (R3 section — Down behavior on production-shaped data)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-CONTEXT.md (D-06 — env-override-wins semantic; lines 122-124)
    - .planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md (find the line documenting UPSTREAM_LLM_OPENROUTER_URL=/api/v1 + the line documenting UPSTREAM_LLM_OPENROUTER_MODEL)
  </read_first>
  <acceptance_criteria>
    - File `gateway/db/migrations/0026_evolve_model_aliases_per_upstream.sql` exists with both `-- +goose Up` and `-- +goose Down` blocks
    - Up block: adds `upstream_name TEXT` column with `IF NOT EXISTS`, backfills 3 existing rows (llm→local-llm, stt→local-stt, embed→local-embed), sets `NOT NULL`, drops old PK, adds composite PK `(alias, upstream_name)`, INSERTs the 3 tier-1 rows with `ON CONFLICT (alias, upstream_name) DO NOTHING`
    - Tier-1 INSERT rows are EXACTLY: `('qwen','llm','qwen/qwen3.5-27b','openrouter-chat')`, `('whisper','stt','whisper-1','openai-whisper')`, `('bge-m3','embed','text-embedding-3-small','openai-embed')`
    - **R3 Down block (REVISED per REVIEWS.md HIGH concern — explicit guard against operator data loss):**
      1. DELETEs the 3 seeded tier-1 rows: `DELETE FROM ai_gateway.model_aliases WHERE (alias, upstream_name) IN (('qwen','openrouter-chat'), ('whisper','openai-whisper'), ('bge-m3','openai-embed'))`.
      2. **GUARD step (NEW):** before restoring PK on `(alias)`, runs a `DO $$ ... $$` PL/pgSQL block that queries `SELECT alias, array_agg(upstream_name) FROM ai_gateway.model_aliases GROUP BY alias HAVING count(*) > 1`. If any rows returned, raises `EXCEPTION 'Phase 06.9 Down aborted: duplicate-alias rows exist across upstreams. Manual cleanup required: %', dup_list` with the offending alias + upstream list. This errors loudly instead of silently dropping data when operators added rows via `gatewayctl model-alias set` between Up and Down.
      3. DROP CONSTRAINT model_aliases_pkey + ADD CONSTRAINT model_aliases_pkey PRIMARY KEY (alias) (only runs if guard passed).
      4. **DOES NOT DROP COLUMN upstream_name** — preserve per-upstream data for re-Up idempotency + safety; column stays as nullable / NOT NULL-with-default with PK reverted to (alias).
      5. All with `IF EXISTS` guards on constraint operations.
      6. SQL comments inside the Down block documenting: (a) Down behavior on production-shaped data (which rows preserved, which deleted); (b) the guard rationale (operator-friendly explicit failure over silent data loss); (c) recovery procedure (manually delete duplicate rows, then re-run Down).
    - **R11 column comment:** `COMMENT ON COLUMN ai_gateway.model_aliases.upstream_name IS 'Phase 06.9: upstream NAME (not role). Canonical values: local-llm, local-stt, local-embed (tier-0); openrouter-chat, openai-whisper, openai-embed (tier-1). New tier-1 providers add new values as schema rows.'` added in Up block.
    - Verify next available number AT EXEC TIME via: `ls gateway/db/migrations/00*.sql | sort -V | tail -1` — if last is `0025_*`, this file is `0026_*`. If concurrent commit bumped to 0026, name this `0027_*` instead and update all downstream references (migrate_test.go entry + any prose mentioning 0026).
    - `psql -c "SELECT alias, upstream_name, target FROM ai_gateway.model_aliases ORDER BY alias, upstream_name"` (run via docker compose after migration applies) returns exactly 6 rows
    - `03-WAVE0-GATES.md` no longer contains the string `openrouter.ai/api/v1` in the operator env section; replaced with `openrouter.ai/api` + a short paragraph: "Phase 06.9 correction — URL ends at `/api` (not `/api/v1`) because the gateway preserves the client's `/v1/chat/completions` request path; concatenation produces the correct endpoint. Boot-time fail-fast (Plan 04) rejects URLs ending in `/v1`."
    - **BLOCKER-1 / D-06 env-override-wins doc:** `03-WAVE0-GATES.md` reads the following amendment for `UPSTREAM_LLM_OPENROUTER_MODEL` (verbatim, replacing any prior "DEPRECATED" wording introduced by earlier revisions): *"`UPSTREAM_LLM_OPENROUTER_MODEL` remains the runtime fallback override per D-06. Schema row in `model_aliases` provides the default; env var (when set) takes precedence at resolver-lookup time. New deployments SHOULD prefer schema rows for multi-instance consistency, but env overrides remain honored for backward compatibility and operator escape hatches. Plan 02 implements the env-override-wins precedence inside the resolver."*
    - `goose status` (run inside dev container against ai_gateway schema) shows new migration applied; no errors
  </acceptance_criteria>
  <action>
    Author the migration file using the PATTERNS.md verbatim template (lines 42-86) as base, with the exact 6 row shape from CONTEXT D-02 and RESEARCH lines 173-251. The header block MUST include the line: "Migration number = NNNN (computed at exec time per PATTERNS.md anti-pattern guidance — verify via `ls gateway/db/migrations/00*.sql | sort -V | tail -1`)." Backfill UPDATE clauses go BEFORE re-adding PK so uniqueness constraint never violates mid-statement.

    **R3 Down block (REVISED).** Use the following shape (inside `-- +goose Down` + `-- +goose StatementBegin`):

    Step 1 — delete the seeded tier-1 rows:
    `DELETE FROM ai_gateway.model_aliases WHERE (alias, upstream_name) IN (('qwen','openrouter-chat'), ('whisper','openai-whisper'), ('bge-m3','openai-embed'));`

    Step 2 — guard block (REVIEWS R3 option (b) — error rather than silent loss): a `DO $$ DECLARE dup_aliases TEXT; BEGIN SELECT string_agg(alias || ' across {' || array_to_string(arr, ',') || '}', '; ') INTO dup_aliases FROM (SELECT alias, array_agg(upstream_name ORDER BY upstream_name) AS arr FROM ai_gateway.model_aliases GROUP BY alias HAVING count(*) > 1) d; IF dup_aliases IS NOT NULL THEN RAISE EXCEPTION 'Phase 06.9 migration 0026 Down aborted: duplicate-alias rows exist across upstreams. Manual cleanup required for: %. To recover: DELETE the unwanted rows, then re-run goose down -1.', dup_aliases; END IF; END $$;`

    Step 3 — restore PK on `(alias)`:
    `ALTER TABLE ai_gateway.model_aliases DROP CONSTRAINT IF EXISTS model_aliases_pkey;`
    `ALTER TABLE ai_gateway.model_aliases ADD CONSTRAINT model_aliases_pkey PRIMARY KEY (alias);`

    Do NOT drop the `upstream_name` column — it stays for re-Up idempotency and to preserve any operator-edited tier-0 data.

    Wrap the entire Down block with SQL comments explaining:
    - Down preserves `upstream_name` column (only PK reverts).
    - Down deletes only the 3 seeded tier-1 rows.
    - The guard catches operator-created duplicate-alias rows (e.g., added via `gatewayctl model-alias set --alias=foo --upstream=openrouter-chat ...` then Down attempt) and aborts with actionable message.
    - Recovery: operator deletes the offending rows manually, then re-runs `goose down -1`.

    **R11 column comment.** Add at the end of Up block (after the INSERT):
    `COMMENT ON COLUMN ai_gateway.model_aliases.upstream_name IS 'Phase 06.9: upstream NAME (not role). Canonical values: local-llm, local-stt, local-embed (tier-0); openrouter-chat, openai-whisper, openai-embed (tier-1). New tier-1 providers add new values as schema rows.';`

    Then edit `03-WAVE0-GATES.md`: locate the operator env block that names `UPSTREAM_LLM_OPENROUTER_URL`. Replace `https://openrouter.ai/api/v1` with `https://openrouter.ai/api` and append a "Phase 06.9 amendment" callout (matches the existing "D-C1 → D-C1'" amendment style already in the file). Do NOT delete the historical context — annotate, don't rewrite.

    **BLOCKER-1 / D-06 env-override-wins doc.** In the same `03-WAVE0-GATES.md` edit, also reword the `UPSTREAM_LLM_OPENROUTER_MODEL` entry to reflect D-06 (env wins over schema). REPLACE any earlier "DEPRECATED" wording from a prior revision iteration with this verbatim Phase 06.9 amendment paragraph:

    "`UPSTREAM_LLM_OPENROUTER_MODEL` remains the runtime fallback override per D-06. Schema row in `model_aliases` provides the default; env var (when set) takes precedence at resolver-lookup time. New deployments SHOULD prefer schema rows for multi-instance consistency, but env overrides remain honored for backward compatibility and operator escape hatches. Plan 02 implements the env-override-wins precedence inside the resolver — see Plan 02 acceptance criteria for the resolver precedence chain (env → schema → unchanged passthrough)."
  </action>
  <verify>
    <automated>cd gateway && (ls db/migrations/00*.sql | sort -V | tail -1 | grep -E '_evolve_model_aliases_per_upstream\.sql$') && grep -q "ADD COLUMN IF NOT EXISTS upstream_name" db/migrations/*_evolve_model_aliases_per_upstream.sql && grep -q "qwen/qwen3.5-27b" db/migrations/*_evolve_model_aliases_per_upstream.sql && grep -q "openai-whisper" db/migrations/*_evolve_model_aliases_per_upstream.sql && grep -q "openai-embed" db/migrations/*_evolve_model_aliases_per_upstream.sql && grep -q "RAISE EXCEPTION 'Phase 06.9" db/migrations/*_evolve_model_aliases_per_upstream.sql && grep -q "COMMENT ON COLUMN ai_gateway.model_aliases.upstream_name" db/migrations/*_evolve_model_aliases_per_upstream.sql && ! grep -q "openrouter.ai/api/v1" ../.planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md && grep -q "remains the runtime fallback override per D-06" ../.planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md && ! grep -qi "UPSTREAM_LLM_OPENROUTER_MODEL.*DEPRECATED\|DEPRECATED.*UPSTREAM_LLM_OPENROUTER_MODEL" ../.planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md</automated>
  </verify>
  <done>Migration file exists with correct PK + seed shape (6 rows after apply); R3 Down guard present (explicit RAISE EXCEPTION on duplicate-alias rows); R11 column comment present; 03-WAVE0-GATES.md no longer documents the `/api/v1` URL convention error AND documents `UPSTREAM_LLM_OPENROUTER_MODEL` per D-06 (env-override-wins, NOT deprecated).</done>
</task>

<task type="auto">
  <name>Task 2: Update sqlc queries + regenerate + add migration to migrate_test.go</name>
  <files>gateway/db/queries/model_aliases.sql, gateway/internal/db/gen/model_aliases.sql.go, gateway/internal/db/gen/models.go, gateway/internal/db/migrate_test.go</files>
  <read_first>
    - gateway/db/queries/model_aliases.sql (current 2-query file)
    - gateway/internal/db/gen/models.go (lines 123-128 — current AiGatewayModelAlias struct)
    - gateway/internal/db/gen/model_aliases.sql.go (lines 16, 32 — GetModelAlias + ListModelAliases generated funcs)
    - gateway/internal/db/migrate_test.go (lines 39-65 — hard-coded slice of migration filenames; currently ends at 0025)
    - gateway/sqlc.yaml (verify which queries file is included)
  </read_first>
  <acceptance_criteria>
    - `gateway/db/queries/model_aliases.sql` exports two queries with the post-Phase-06.9 shape (`SELECT ... upstream_name`; `WHERE alias = $1 AND upstream_name = $2`)
    - **R7 sqlc consistency (NEW per REVIEWS.md MEDIUM concern):** ALSO add the following queries for use by Plan 04's `gatewayctl model-alias` CLI:
      - `-- name: UpsertModelAlias :exec` — `INSERT INTO ai_gateway.model_aliases (alias, upstream, target, upstream_name) VALUES ($1, $2, $3, $4) ON CONFLICT (alias, upstream_name) DO UPDATE SET target = EXCLUDED.target;`
      - `-- name: DeleteModelAlias :exec` — `DELETE FROM ai_gateway.model_aliases WHERE alias = $1 AND upstream_name = $2;`
    - `sqlc generate` (from `gateway/` directory) regenerates `internal/db/gen/model_aliases.sql.go` and `internal/db/gen/models.go` without manual edits
    - Regenerated `AiGatewayModelAlias` struct contains exactly four fields: `Alias`, `Upstream`, `Target`, `UpstreamName` (all `string`)
    - Regenerated `GetModelAlias` query function signature accepts `(ctx, db, alias string, upstream_name string)` (or sqlc-canonical `GetModelAliasParams` struct with both fields)
    - Regenerated `UpsertModelAlias` + `DeleteModelAlias` query functions exist with appropriate param structs
    - `gateway/internal/db/migrate_test.go` contains an additional entry in the migration-list slice for the new migration filename (matching the actual filename chosen in Task 1)
    - `cd gateway && go build ./...` succeeds with no compile errors (callers of `GetModelAlias` that don't pass upstream_name will break — fix call sites by updating them to pass the correct upstream name OR replace with `ListModelAliases` + in-memory filter; Plan 02 owns the resolver path so changes here are limited to making code compile)
    - `cd gateway && go test ./internal/db/... -count=1 -timeout=60s` passes
  </acceptance_criteria>
  <action>
    Edit `gateway/db/queries/model_aliases.sql`: change `ListModelAliases :many` SELECT list to include `upstream_name` and add `ORDER BY alias, upstream_name` for deterministic Refresh; change `GetModelAlias :one` to filter on both `alias = $1 AND upstream_name = $2`.

    **R7:** Append the two new queries (`UpsertModelAlias :exec` and `DeleteModelAlias :exec`) per acceptance criteria. Rationale: sqlc is the established data-access pattern in this codebase, and Plan 04's CLI needs to write to this table; rather than have Plan 04 emit ad-hoc SQL (inconsistent with the rest of the codebase per Codex review), bake the queries here. Plan 04 imports the generated functions. This keeps validation logic in one place (the SQL itself) and avoids drift.

    Save.

    Run `cd gateway && sqlc generate` to regenerate `internal/db/gen/model_aliases.sql.go` and `internal/db/gen/models.go`. Do NOT hand-edit the generated files. If sqlc emits a `GetModelAliasParams` struct (it does when a query has >1 parameter), accept it.

    Update `gateway/internal/db/migrate_test.go`: locate the slice of migration filenames (currently ending at `0025_create_voices.sql`) and append the new filename from Task 1. Preserve slice ordering exactly (lexicographic by filename).

    Find compile-break call sites: `grep -rn 'GetModelAlias' gateway/ --include='*.go'` outside `internal/db/gen/`. Any non-gen caller must pass `(alias, upstream_name)`. If the only caller is the resolver (which Plan 02 owns), it's OK if it temporarily passes a placeholder upstream name — Plan 02 will fix to the real per-upstream lookup. The build must compile cleanly at the end of this task.

    Per pitfall #7 (RESEARCH.md): if `grep -rn 'GetModelAlias' gateway/internal/ gateway/cmd/ --include='*.go' | grep -v gen/` shows zero non-gen callers, that's the simpler outcome — the regen-only path. If callers exist (likely only in resolver tests), patch them to pass the canonical tier-0 upstream name (`local-llm` for `llm` role, etc.) as a temporary fix; Plan 02 will rework the resolver to use ListModelAliases properly.
  </action>
  <verify>
    <automated>cd gateway && sqlc generate && go build ./... && grep -q "UpstreamName string" internal/db/gen/models.go && grep -q "ORDER BY alias, upstream_name" db/queries/model_aliases.sql && grep -q "UpsertModelAlias" db/queries/model_aliases.sql && grep -q "DeleteModelAlias" db/queries/model_aliases.sql && grep -q "UpsertModelAlias" internal/db/gen/model_aliases.sql.go && grep -q "_evolve_model_aliases_per_upstream.sql" internal/db/migrate_test.go && go test ./internal/db/... -count=1 -timeout=60s</automated>
  </verify>
  <done>sqlc-generated struct has UpstreamName field; UpsertModelAlias + DeleteModelAlias queries generated; build compiles; migrate_test.go lists the new migration; db tests green.</done>
</task>

</tasks>

<verification>
- Migration 0026 (or 0027 if number bumped) applies Up cleanly against fresh testcontainers Postgres
- `psql ... -c "SELECT count(*) FROM ai_gateway.model_aliases"` returns 6
- Down + duplicate-alias row + Down again triggers the RAISE EXCEPTION guard (test in Plan 05a)
- `cd gateway && sqlc diff` returns clean (no drift between SQL and generated Go)
- `cd gateway && go test ./internal/db/... -count=1 -race -timeout=60s` PASS
- `grep -c "openrouter.ai/api/v1" .planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md` returns 0
- `grep -q "remains the runtime fallback override per D-06" .planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md` matches (D-06 env-override-wins documented, not deprecated)
</verification>

<success_criteria>
- Migration file authored with correct shape (PK widening + 3 seed rows + backfill + reversible Down + R3 guard + R11 column comment)
- sqlc regen produces `UpstreamName` field on generated struct + `UpsertModelAlias` / `DeleteModelAlias` queries for Plan 04 use
- migrate_test.go ordering slice includes the new migration
- Wave 0 Gate A doc corrected from `/api/v1` to `/api` with explicit Phase 06.9 amendment paragraph; `UPSTREAM_LLM_OPENROUTER_MODEL` documented per D-06 (env-override-wins — NOT deprecated)
- Build green; db package tests green
- Phase 06.9 Wave 0 unlocked for Plan 02 (resolver refactor — owns the runtime env-override-wins implementation)
</success_criteria>

<output>
Create `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-01-SUMMARY.md` when done. Capture the actual migration number chosen (0026 vs 0027 if concurrent bump), `sqlc diff` output, the diff-snippet for the corrected Wave 0 Gate A paragraph (BOTH the URL fix AND the D-06 env-override-wins paragraph), AND the exact Down block including the RAISE EXCEPTION guard.
</output>
