---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 05
type: execute
wave: 3
depends_on: [06.6.X-04]
files_modified:
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md
autonomous: true
requirements: [PC-COLD-START-FIX]

must_haves:
  truths:
    - "Each of the 4 D-08 suspects (onstart exit, weights presence, supervisord boot, port mapping) is rated by evidence strength from iter-1 logs"
    - "Per D-15, a suspect is labelled CONFIRMED ROOT CAUSE only if ≥2 independent signal families support it"
    - "Decision to run iter-2 (and which hypothesis to test) is recorded with rationale"
  artifacts:
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md"
      provides: "Ranked suspects + iter-2 go/no-go decision"
      contains: "CONFIRMED|REMAINING HYPOTHESIS|REFUTED"
  key_links:
    - from: "06.6.X-ITER1-ANALYSIS.md"
      to: "06.6.X-RESEARCH-COLD-START.md (plan 08)"
      via: "evidence-cited root-cause-or-hypothesis section"
      pattern: "lifecycle_id|signal family"
---

<objective>
Rank the 4 D-08 cold-start suspects against iter-1 evidence and decide whether iter-2 is needed.

Purpose: Apply D-15 (confirmed = ≥2 signal families); produce a ranked suspect table feeding plan 08 RESEARCH-COLD-START synthesis.
Output: 06.6.X-ITER1-ANALYSIS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read iter-1 logs and build evidence-ranked suspect table</name>
  <read_first>
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md (D-08 4 suspects, D-09 iter-2 trigger, D-15 confirmation criterion)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-vast-timeline.log
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-pod-snapshot.log
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-gateway-logs.log
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-network-probe.log
  </read_first>
  <files>.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md</files>
  <action>
    Build the analysis doc with these sections:

    **1. Suspect ranking table:**

    | suspect (D-08) | supporting signals (file:line refs) | refuting signals | verdict |
    |---|---|---|---|
    | onstart exit code / timing | vast-timeline.log:LL, pod-snapshot.log:LL | ... | CONFIRMED \| REMAINING HYPOTHESIS \| REFUTED |
    | weights presence (qwen/whisper/bge-m3) | ... | ... | ... |
    | supervisord boot order + per-program status | ... | ... | ... |
    | Vast port mapping / sshd reachability | ... | ... | ... |

    Apply D-15 strictly: CONFIRMED requires ≥2 independent signal families (e.g., "weights missing" needs both pod-snapshot `ls /weights/whisper` empty AND supervisord `speaches` restart loop). Otherwise REMAINING HYPOTHESIS.

    **2. Iter-2 decision:**
    - If exactly one suspect is CONFIRMED with ≥2 signal families → no iter-2 needed; rationale: "evidence sufficient per D-15".
    - If 0 confirmed or ≥2 ambiguous → iter-2 recommended; pick top-1 by signal weight and define the variant to test per D-09 (example: if weights-missing is top suspect, iter-2 tests with pre-baked or mocked whisper to isolate).
    - State the chosen variant + the specific configuration change for iter-2 (e.g., "pod with /weights/whisper pre-populated via mc cp before onstart").

    **3. Evidence inventory list:**
    Bulletted list mapping each cited file:line back to the suspect it supports. Functions as plan 08's source map.

    Per CLAUDE.md: every cell must cite log-file + line ref or "no evidence in iter-1". No "probably caused by" language.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md && grep -cE 'CONFIRMED|REMAINING HYPOTHESIS|REFUTED' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md | awk '{ if ($1 < 4) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all 4 D-08 suspects rated
    - Each verdict cell cites log file:line for supporting evidence
    - Iter-2 go/no-go decision present with rationale and (if go) the variant + config change specified
  </acceptance_criteria>
  <done>06.6.X-ITER1-ANALYSIS.md committed with 4 suspects rated + iter-2 decision</done>
</task>

</tasks>

<verification>
Analysis applies D-15 confirmation criterion; iter-2 decision explicit.
</verification>

<success_criteria>
Plan 06 (iter-2) knows exactly which variant to test (or is skipped). Plan 08 (RESEARCH-COLD-START) can cite this doc + raw logs for root cause section.
</success_criteria>

<output>Create `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-05-SUMMARY.md` when done.</output>
