---
iter: 1
lifecycle_id: 101
vast_instance_id: 40271309
vast_offer_id: 40097654
vast_machine_id: 51096
vast_host_id: 348505
vast_geo: "Brazil, BR"
dph_usd: 0.1303
trigger: operator_force_up:gatewayctl-cli:manual_gatewayctl
started_at: 2026-06-09T16:28:36Z
ended_at: 2026-06-09T16:47:00Z
elapsed_minutes: 18.4
operator_approval_ts: 2026-06-09T13:28:30-03:00 (user message: "delego pra você, autorizado até $1.20")
trigger_cmd: ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary force-up'
destroy_cmd: ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary force-down'
shutdown_reason: cancelled_in_flight
---

# Iter-1 Signals — Phase 06.6.X Plan 04

## Raw log files (15-min concurrent capture window)

- `iter1-vast-timeline.log` — Vast API poll every 5s (Plan-prescribed Bearer auth returned all-null fields; **manual correction appended** with corrected `?api_key=` snapshot mid-flight)
- `iter1-pod-snapshot.log` — Pod ssh probe every 30s (POD SSH UNREACHABLE entire window; manual correction explains why this is truthful)
- `iter1-gateway-logs.log` — `ai-gateway-dev` docker logs follow (full primary subsys activity 16:28:35Z → 16:47:00Z + final DB row)
- `iter1-network-probe.log` — port reachability every 10s (NO PUBLIC PORT/IP ever assigned; manual correction confirms)

## Rollup paragraph (factual, no speculation)

At `T+1.1s` (`iter1-gateway-logs.log:1-4`) the gateway primary.reconciler picked offer `40097654` (machine `51096`, host `348505`, geo `Brazil BR`, `dph=0.1303`, `shape=0 gpu_shape="1xRTX 3090"`) after logging `"primary allowlist exhausted for shape; broadening to full qualified search"` — operator allowlist `[43803 55158]` returned ZERO matches for shape `1xRTX 3090`, so reconciler broadened to qualified search and picked an out-of-allowlist Brazilian host. From `T+1.1s` through `T+17m` the Vast instance state per corrected `?api_key=` snapshot (`iter1-vast-timeline.log` MANUAL CORRECTION block) was: `actual_status=running`, `intended_status=running`, `cur_state=running`, `status_msg="success, running ghcr.io/ifixtelecom/converseai-primary-pod:develop"`, `public_ipaddr="192.168.1.8"` (RFC1918 private, not public-routable), `ssh_host=null`, `ssh_port=null`, `direct_port_count=250` (none allocated), `gpu_util=0`, `gpu_temp=34.99°C` (no model loaded — `disk_util=23/50GB` reflects extracted image only). Gateway-side from `T+1.4s` to `T+18m` emitted ZERO additional primary-subsys log lines (only `module=MODELS model aliases refreshed` heartbeats every 60s) — reconciler was looping in `waitForReadyOrDestroy` waiting for endpoint reachability that NEVER materialized. Operator-issued `force-down` at `T+18m 24s` closed lifecycle 101 with `shutdown_reason=cancelled_in_flight`.

## Confirmed signals (D-15: ≥2 independent sources each)

### Cold-start root cause (PC-COLD-START-FIX)
**Hypothesis: pod runs but host never binds public ports → reconciler waits forever.**
- Vast API (corrected snapshot): `ssh_host=null`, `ssh_port=null`, `public_ipaddr=192.168.1.8` (private), `direct_port_count=250` unallocated, despite `actual_status=running` + `status_msg=success`.
- Pod-side probe: `POD SSH UNREACHABLE for entire 900s window` (consistent with above — same root truth via different observation channel).
- Network probe: `ip=null ports=` for every 10s tick (consistent third signal).
- Gateway-side: zero `primary` log lines after offer-pick (consistent with reconciler stuck in silent endpoint-poll loop).
✅ D-15 satisfied (4 signal families agree).

### Env-precedence drift (PC-ENV-PRECEDENCE)
**Hypothesis: operator-set `PRIMARY_VAST_NUM_GPUS_PRIMARY=2` + `PRIMARY_NUM_GPUS=2` (per Plan 02) NOT honored at runtime → gateway uses default 1.**
- Plan 02 runtime env (`06.6.X-AUDIT-RUNTIME-ENV.md`): both `PRIMARY_NUM_GPUS=2` and `PRIMARY_VAST_NUM_GPUS_PRIMARY=2` set in container `Config.Env`.
- Gateway live log (`iter1-gateway-logs.log:2`): `shape=0 gpu_shape="1xRTX 3090"` — runtime treated primary slot as NUM_GPUS=1.
- Vast instance returned: `num_gpus: 1`, `gpu_name: "RTX 3090"` (consistent with runtime config, NOT operator-set env).
- Plan 01 code audit (`06.6.X-AUDIT-CODE-MAP.md`): `PRIMARY_VAST_NUM_GPUS_PRIMARY` is NOT read anywhere in HEAD code; `PRIMARY_NUM_GPUS` IS read at `config.go:416` (default 1) but appears the field maps to FALLBACK slot (since shape=0 PRIMARY still shows 1) — to be confirmed by Plan 05 analysis.
✅ D-15 satisfied (≥2 signals: runtime env capture vs runtime log + Vast API truth).

### Allowlist scope mismatch (NEW finding — not in original carry-forward)
**Operator allowlist `[43803, 55158]` does not contain any machine offering `1xRTX 3090`** — gateway log explicitly says `"primary allowlist exhausted for shape; broadening to full qualified search"`. Two possibilities (Plan 05 must resolve): (a) allowlisted machines only offer 2×3090 and shape was wrong (NUM_GPUS=2 expected per MEMORY `primary-gpu-shape-06.8-final`), so the env-precedence drift cascades into allowlist failure; OR (b) allowlist itself is stale.

## Spend

```
spend:
  hourly_rate_usd: 0.1303
  elapsed_minutes: 18.4
  total_usd: 0.0399    # 0.1303 * (18.4/60)
  budget_cap_usd: 0.60
  budget_remaining_usd: 0.5601
  phase_budget_cap_usd: 1.20
  phase_budget_remaining_usd: 1.16
```

## Data-quality caveats

- Plan-prescribed `Authorization: Bearer $VAST_AI_API_KEY` auth header is **not honored by `/api/v0/instances/<id>/`** — endpoint silently returns object with all-null fields instead of 401. Correct auth: `?api_key=$VAST_AI_API_KEY` query param. Three of four signal collectors (vast-timeline, pod-snapshot, network-probe) initially captured nulls due to this; manual mid-flight `?api_key=` curl rescued the truth. Plan 06 / future iters must use query-param auth.
- Pod was destroyed via `gatewayctl primary force-down` (preferred path per Plan 04 D-03) → published to `gw:primary:events` channel → reconciler picked up at next tick → lifecycle ENDED 16:47:00Z with `shutdown_reason=cancelled_in_flight`. Vast list endpoint confirms zero active instances post-destroy.

## Hand-off to Plan 05

Plan 05 should:
1. **Suspect ranking for cold-start (PC-COLD-START-FIX)** based on the 4-signal agreement — primary suspect = "Vast host fails to assign public ports despite `actual_status=running`". Cross-reference: `reconciler.go:920-1020 waitForReadyOrDestroy` 3-strike behavior — current capture shows reconciler waited 18min with NO strike progress visible in logs. Investigate: (a) does host 348505 / machine 51096 have a known port-allocation quirk? (b) is there a Vast-side "container started but host networking not bound" condition specific to certain offers? (c) does the offer-filter logic exclude hosts with insufficient `direct_port_count` headroom?
2. **Env-precedence resolution (PC-ENV-PRECEDENCE)**: Plan 01 audit + this live signal together prove that `PRIMARY_VAST_NUM_GPUS_PRIMARY` env var is **dead code** at runtime (no reader). Plan 07 synthesis must document this as canonical-name-not-yet-wired finding and recommend whether (i) wire the new canonical names, or (ii) deprecate the new names and document `PRIMARY_NUM_GPUS` as canonical.
3. **Iter-2 go/no-go**: Given 4-signal agreement on cold-start root cause = "no public ports" AND env-drift fully explained without further evidence — iter-2 may be SKIPPED if the goal is "≥2 signals for confirmed root cause"; iter-2 IS warranted only if Plan 05 wants to test a specific fix-side hypothesis (e.g., re-run with `direct_port_count` minimum filter, or with operator-corrected allowlist that contains 2×3090 hosts).
