---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 08
subsystem: primary-pod / cold-start diagnosis
tags: [research, diagnosis-only, cold-start, PC-COLD-START-FIX, D-14, D-15]
dependency_graph:
  requires:
    - 06.6.X-ITER1-SIGNALS.md (Plan 04 — raw 4-family capture)
    - 06.6.X-ITER1-ANALYSIS.md (Plan 05 — suspect ranking)
    - 06.6.X-ITER2-ANALYSIS.md (Plan 06 — skip rationale)
    - 06.6.X-AUDIT-CODE-MAP.md (Plan 01)
    - 06.6.X-AUDIT-RUNTIME-ENV.md (Plan 02)
  provides:
    - 06.6.X-RESEARCH-COLD-START.md (PC-COLD-START-FIX research deliverable)
  affects:
    - Phase 6.6.Y discuss-phase (canonical input)
tech_stack:
  added: []
  patterns:
    - "Evidence-cited research synthesis (D-15 ≥2-signal-family rule for CONFIRMED verdict)"
    - "Fix options table with operator-impact tradeoff columns (image-size, cold-start, complexity)"
key_files:
  created:
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-08-SUMMARY.md
  modified: []
decisions:
  - "Verdict CONFIRMED ROOT CAUSE: Vast host machine_id=51096 / host_id=348505 (Brazil BR) accepted pod with actual_status=running but never bound public ports (ssh_host=null, public_ipaddr=192.168.1.8 RFC1918, direct_port_count=250 unallocated). 4-of-4 signal-family agreement satisfies D-15 with margin."
  - "Recommended fix: Option A (offer-side RFC1918 / port-count filter at picker stage) + Option B (reconciler health-gate with PrimaryPublicPortBindBudgetSeconds fail-fast in waitForReadyOrDestroy) bundled."
  - "Option C (pre-bake whisper or 3-way onstart download) scheduled AFTER A+B in Phase 6.6.Y so the next UAT actually reaches the supervisord/weights stage to validate suspect #2."
  - "Option D (allowlist hard-fail) paired with env-precedence fix from sibling RESEARCH-ENV-PRECEDENCE doc."
  - "REFUTED CONTEXT.md D-10/D-11 premise: config.go:622-642 is csvOr helper, not alias mapping (per Plan 01 audit)."
  - "Carry forward to Phase 6.6.Y: Vast API auth bug — Bearer header returns null fields on /api/v0/instances/<id>/; only ?api_key= query-param works."
metrics:
  duration_minutes: 15
  completed: 2026-06-09
  tasks_completed: 1
  files_created: 2
  files_modified: 0
  spend_usd: 0  # synthesis-only, no live pod
---

# Phase 6.6.X Plan 08: Synthesize iter-1 evidence into RESEARCH-COLD-START Summary

**One-liner:** Wrote `06.6.X-RESEARCH-COLD-START.md` (PC-COLD-START-FIX deliverable) with CONFIRMED root cause backed by 4-of-4 signal families plus 5 fix options with full tradeoff columns and a Phase 6.6.Y hand-off addressing 6 discuss-phase questions.

## Objective (recap)

Synthesize iter-1 evidence (iter-2 was SKIPPED — see `06.6.X-ITER2-ANALYSIS.md`) into the final RESEARCH-COLD-START doc per D-13. Sections: Root Cause (CONFIRMED, ≥2 signal families per D-15), Fix Options (≥2 with operator-impact tradeoffs per MUST-HAVES truth #2), Hand-off to Phase 6.6.Y. Honor D-14 (no `gateway/` / `pod/` edits).

## What was built

### Artifact: `06.6.X-RESEARCH-COLD-START.md`

Eight-section research doc:

1. **Executive Summary** — one-paragraph root cause statement tied to Phase 11.2 carry-forward source.
2. **Repro Method** — D-01..D-03 quoted; iter-1 + iter-2 (skip) summaries with `lifecycle_id=101`, spend `$0.0399`.
3. **Evidence Summary by Signal Family (D-04..D-07)** — Vast / Pod / Gateway / Network sub-sections with per-row log file:line citations; subsection 3.5 documents the 4-family agreement.
4. **Root Cause Verdict** — explicit `CONFIRMED ROOT CAUSE` label with 4 cited signal-family sources; 3 `REMAINING HYPOTHESES` table (onstart timing, weights presence, supervisord boot order) all blocked behind the confirmed cause; 1 `REFUTED` sub-premise (CONTEXT D-10/D-11 alias mapping); cascading-finding paragraph on env-precedence chain.
5. **Fix Options** — 5 options (A–E) in a single tradeoff table with columns: Description, Pros, Cons, Image-size delta, Cold-start delta, Operator complexity. Deferred-item note for "boot-time resolved-shape log dump" cross-referenced to sibling env-precedence doc.
6. **Recommended Option** — A+B bundled, with evidence-tied rationale citing `iter1-vast-timeline.log:324, 327`, `iter1-gateway-logs.log:6, 8-64`. Explicit "why not C/D/E alone" justifications.
7. **Open Items / Out of Scope** — 6-row table including suspects #1/#2/#3 disposition, host-specific-vs-universal sample-size gap, Vast API auth bug as a hard requirement.
8. **Hand-off to Phase 6.6.Y** — single-paragraph spec for `gsd-discuss-phase 6.6.Y` prompt, listing 6 explicit discuss-phase questions including the Plan 05 hand-off question on host-specific vs universal port-binding failure.

### Citation discipline (per CLAUDE.md CONVENTIONS — no speculative language)

Every CONFIRMED/REMAINING/REFUTED claim cites a specific log line range or audit doc section. Where evidence is single-family, doc explicitly labels REMAINING HYPOTHESIS with "unresolved-because" column populated. No "probably" / "likely" / "may" language anywhere in the deliverable.

## Verification

```
$ test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md && echo FILE OK
FILE OK
$ grep -cE 'Root Cause|Fix Options|Hand-off|Phase 6.6.Y' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md
18    # plan requires >=4
$ git status gateway/ pod/ --porcelain
(empty)   # D-14 compliance: zero source edits
```

Plan automated verify gate passes (`>=4` keyword matches; zero gateway/pod changes).

## Acceptance criteria check

- ✅ File exists with all 8 sections (1 Executive Summary, 2 Repro Method, 3 Evidence by Family, 4 Root Cause Verdict, 5 Fix Options, 6 Recommended Option, 7 Open Items, 8 Hand-off).
- ✅ Section 4 explicitly labels `CONFIRMED ROOT CAUSE` per D-15 (4-of-4 signal families cited).
- ✅ Section 5 has 5 fix options (≥2 required) with full tradeoff columns including image-size delta, cold-start delta, and operator complexity (MUST-HAVES truth #2 met).
- ✅ Section 6 recommends Option A+B bundle with evidence-tied rationale.
- ✅ `git status gateway/ pod/` shows zero unstaged changes (D-14 compliance).

## Deviations from Plan

None — plan executed exactly as written. No bugs, no missing functionality, no architectural changes. The plan's read-first list was followed; iter-2 documents were honored as SKIP (per `06.6.X-ITER2-ANALYSIS.md`) and the research doc records "iter-2: skipped" with cross-reference; the optional `06.6.X-ITER2-SIGNALS.md` is intentionally absent (no signals to roll up).

## Commits

| Commit | Subject |
|---|---|
| 4b9379c | docs(06.6.X-08): synthesize iter-1 evidence into RESEARCH-COLD-START |
| (this) | docs(06.6.X-08): complete plan 08 — RESEARCH-COLD-START synthesis |

## Self-Check: PASSED

- ✅ File `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md` exists (`git ls-files` will confirm post-commit).
- ✅ Commit `4b9379c` exists in `git log`.
- ✅ Per plan instructions: STATE.md / ROADMAP.md NOT updated in this plan (deferred to phase-rollup plan 09 / phase-close).
