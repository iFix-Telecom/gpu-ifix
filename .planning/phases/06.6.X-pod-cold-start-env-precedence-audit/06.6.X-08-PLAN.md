---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 08
type: execute
wave: 5
depends_on: [06.6.X-05, 06.6.X-06]
files_modified:
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md
autonomous: true
requirements: [PC-COLD-START-FIX]

must_haves:
  truths:
    - "Root cause is either CONFIRMED (≥2 signal families per D-15) OR explicitly listed as REMAINING HYPOTHESES"
    - "2-3 fix options with tradeoffs are listed per D-13 (e.g., pre-bake whisper +3GB image vs onstart 3-way download vs supervisord priority change)"
    - "Every citation traces to iter-1/iter-2 logs or ITER*-ANALYSIS docs"
  artifacts:
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md"
      provides: "Final RESEARCH doc for PC-COLD-START-FIX; feeds Phase 6.6.Y fix planning"
      contains: "Root Cause|Remaining Hypotheses|Fix Options"
  key_links:
    - from: "06.6.X-RESEARCH-COLD-START.md"
      to: "Phase 6.6.Y (future)"
      via: "fix options block with tradeoffs"
      pattern: "Option|tradeoff"
---

<objective>
Synthesize iter-1 + iter-2 evidence into the final RESEARCH-COLD-START doc per D-13.

Purpose: Second phase deliverable. Honors D-14 (no code edits) and D-15 (evidence-or-hypothesis labelling).
Output: 06.6.X-RESEARCH-COLD-START.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-ANALYSIS.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-SIGNALS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RESEARCH-COLD-START with root cause or hypotheses + fix options</name>
  <read_first>
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md (D-13, D-14, D-15)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-ANALYSIS.md
    - .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-GAPS-DECISION.md (item 1 — original carry-forward)
    - .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-VERIFICATION.md (cenários 1+5 PARTIAL evidence)
    - gateway/internal/primary/onstart.go lines 141-154 (current onstart: 2 parallel downloads, whisper NOT in list)
    - pod/primary/Dockerfile line 203 (/weights/whisper empty)
  </read_first>
  <files>.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md</files>
  <action>
    Sections:

    **1. Executive Summary** (~5 lines): one-sentence statement of either confirmed root cause OR top remaining hypothesis. Carry-forward source = 11.2 cenários 1+5 PARTIAL.

    **2. Repro Method**: Quote D-01..D-03 + summary of iter-1 (and iter-2 if run). Cite lifecycle_ids.

    **3. Evidence Summary by Signal Family** (D-04..D-07):
    - Vast-side: key timeline events with timestamps + log refs
    - Pod-side: key findings (weights state, supervisord state, dmesg) + log refs
    - Gateway-side: FSM decisions, closure_reason from primary_pod_lifecycle
    - Network: when (if) ports became reachable

    **4. Root Cause Verdict** (apply D-15):
    Either:
    - **CONFIRMED ROOT CAUSE**: <name>. Supporting evidence: signal family A (cite file:line) + signal family B (cite file:line). [≥2 families required]
    OR
    - **NO CONFIRMED ROOT CAUSE**. **Remaining Hypotheses** (ranked):
      1. <hypothesis>: evidence-for (cite), evidence-against (cite), unresolved-because (e.g., "iter-2 INCONCLUSIVE per 06.6.X-ITER2-ANALYSIS.md")
      2. ...

    **5. Fix Options** (D-13, 2-3 options with tradeoffs):
    Use a table:

    | Option | Description | Pros | Cons | Image size delta | Cold-start delta | Operator complexity |
    |---|---|---|---|---|---|---|

    Examples (refine based on findings):
    - A: Pre-bake whisper weights into pod image (+~3GB image, eliminates 1 download dependency, no onstart code change)
    - B: Onstart 3-way download (add whisper to onstart.go:141-154 alongside qwen + bge-m3, ~minor onstart time delta)
    - C: Supervisord boot-order priority change (if root cause is timing, not weights)
    - D: Vast offer geo/host pin (if root cause is host-specific image-load failure)

    Per D-13 also note "Boot-time resolved-shape log dump" deferred item if env precedence visibility surfaces as related (cross-reference 06.6.X-RESEARCH-ENV-PRECEDENCE.md).

    **6. Recommended Option**: pick one with rationale tying back to evidence.

    **7. Open Items / Out of Scope**: list what remains unresolved (e.g., "cold-start budget seconds drift — secondary finding, not investigated"); mark as candidates for 6.6.Y or future phases.

    **8. Hand-off to Phase 6.6.Y**: Single-paragraph spec for the discuss-phase prompt of 6.6.Y, citing this doc + 06.6.X-RESEARCH-ENV-PRECEDENCE.md as inputs.

    Per D-14: NO code edits — confirm via `git status gateway/ pod/` showing clean before commit. Per CLAUDE.md: cite every claim. Per D-15: never call something a root cause without 2 signal-family sources.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md && grep -cE 'Root Cause|Fix Options|Hand-off|Phase 6.6.Y' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md | awk '{ if ($1 < 4) exit 1 }' && git status gateway/ pod/ --porcelain | grep -v '^$' | wc -l | awk '{ if ($1 > 0) { print "ERROR: gateway/pod/ has unstaged changes — diagnosis-only phase violated"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all 8 sections
    - Section 4 explicitly labels CONFIRMED or NO CONFIRMED ROOT CAUSE per D-15
    - Section 5 has ≥2 fix options with full tradeoff columns
    - Section 6 recommends one option with evidence-tied rationale
    - `git status gateway/ pod/` shows zero unstaged changes (D-14 compliance)
  </acceptance_criteria>
  <done>06.6.X-RESEARCH-COLD-START.md committed; gateway/ and pod/ untouched</done>
</task>

</tasks>

<verification>
- D-14 compliance: gateway/ pod/ untouched
- D-15 compliance: root cause label requires ≥2 signal families
- Fix options have tradeoff columns
</verification>

<success_criteria>
Phase 6.6.Y discuss-phase can use this + RESEARCH-ENV-PRECEDENCE as canonical inputs.
</success_criteria>

<output>Create `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-08-SUMMARY.md` when done.</output>
