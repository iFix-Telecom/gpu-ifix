---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 01
subsystem: env-precedence-audit
tags: [audit, diagnose-only, shape-vars, code-map]
requires: []
provides: ["06.6.X-AUDIT-CODE-MAP.md"]
affects: []
tech-stack:
  added: []
  patterns: [grep-then-cite, evidence-only-no-speculation]
key-files:
  created:
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-CODE-MAP.md
  modified: []
decisions:
  - "CONTEXT.md cites symbols not present in HEAD code; documented as a synthesis row plan 07 must reconcile (drop or rewrite the 'Fallback slot' hypothesis)."
  - "Verbatim quote of config.go:622-642 included; block is csvOr() helper, not a legacy alias mapping."
metrics:
  duration_min: 6
  completed: 2026-06-09
---

# Phase 06.6.X Plan 01: Code-side shape var audit — Summary

One-liner: Grep + read of gateway/internal/config/config.go produced a code map of 5 primary-pod shape env vars (NUM_GPUS, GPU_NAME, PRICE_CAP_DPH, MACHINE_ALLOWLIST, MACHINE_BLOCKLIST) with file:line for declaration, default, reader, and downstream consumer chain to the Vast search filter.

## What was built

`06.6.X-AUDIT-CODE-MAP.md` — code column of the env precedence divergence table feeding plan 07 synthesis.

Contents:
- Critical finding: 7 symbols/line-ranges cited in CONTEXT.md do not exist in HEAD code (notably `PRIMARY_VAST_NUM_GPUS_{PRIMARY,FALLBACK}` and the "legacy alias mapping at 622-642").
- Shape var table: 5 vars, each with declaration file:line, default value, reader file:line, consumer chain to `vast.DefaultSearchFilter`.
- Verbatim quote of `config.go:622-642` showing the block is `csvOr()` helper (not alias mapping).
- pod/ side coverage: `pod/scripts/vast-ai.sh:54` `num_gpus:1` hardcode (manual path, NOT live-tick).
- Test coverage table per var.
- Hand-off section pre-filling the code column of the plan 07 divergence table.

## Tasks executed

| # | Task | Commit |
|---|------|--------|
| 1 | Grep all shape vars, build code map (read config.go:195-199 declarations + 412-416 reads + 622-642 verbatim) | pending (this commit) |

## Verification (acceptance criteria from plan)

- File exists at expected path — PASS
- Table includes ≥4 shape vars — PASS (5 vars: PRICE_CAP_DPH, MACHINE_BLOCKLIST, MACHINE_ALLOWLIST, GPU_NAME, NUM_GPUS)
- Each row has file:line for read_at — PASS (config.go:412, 413, 414, 415, 416)
- Legacy alias block quoted verbatim — PASS (verbatim at requested range 622-642, with explicit note that it is the `csvOr` helper, not an alias block)
- Doc states which slot `PRIMARY_NUM_GPUS` writes to — PASS: explicitly documented "no slot routing exists; read directly into single `PrimaryNumGPUs` field at config.go:416"
- Automated gate: `grep -cE 'PRIMARY_(VAST_)?(NUM_GPUS|GPU_NAME|PRICE_CAP|ALLOWLIST)' >= 4` — PASS (18 matches)
- No speculative language (`probably|likely|provavelmente|talvez|geralmente`) — PASS (zero matches)

## Deviations from Plan

### Scope finding (not a deviation; surfaced for plan 07)

The plan's `<read_first>` and acceptance-criteria block presumed a "legacy alias mapping in config.go:622-642" plus split `PRIMARY_VAST_NUM_GPUS_PRIMARY/FALLBACK` fields. Neither exists in HEAD (commit `1311a25`). Per CLAUDE.md ("validate claims with evidence"), the audit documents the absence directly with grep output and a verbatim read of the cited line range, rather than inventing the missing symbols. This is the most important downstream input to plan 07 (env precedence synthesis): the divergence-table hypothesis must be reformulated because the assumed alias-misrouting mechanism is not in code.

No code was modified (D-14 compliance: `git status gateway/ pod/` returns empty).

### Auto-fixed Issues

None — diagnose-only plan, no fix actions taken.

## D-14 compliance (diagnose-only)

```
$ git status gateway/ pod/ --short
(empty)
```

Zero edits to `gateway/` or `pod/`. Only `.planning/phases/06.6.X-*/` writes.

## Hand-off

- Plan 02 (runtime env capture) — confirm whether `PRIMARY_NUM_GPUS=2` is actually present in `vps-ifix-vm:/opt/ai-gateway-dev/.env` and exported to the `ai-gateway-dev` container (`docker exec ai-gateway-dev env | grep PRIMARY_`).
- Plan 03 (docs audit) — read `gateway/.env.example`, `gateway/.env.prod.example`, `RUNBOOK-OPS.md`, `RUNBOOK-PRIMARY-POD.md` for the 5 shape vars in this map.
- Plan 07 (synthesis) — merge this doc + plan 02 runtime + plan 03 docs into the divergence table. **Reformulate the "Fallback alias misrouting" hypothesis** before writing the table — the code premise it relied on is not in HEAD.

## Self-Check: PASSED

- File `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-CODE-MAP.md` — exists (verified `test -f`).
- Commit hash — to be recorded post-commit.
- Scope guard (`git status gateway/ pod/`) — empty (verified).
- Shape var coverage gate (`grep -cE >= 4`) — 18 matches (verified).
- Speculation gate (`probably|likely|maybe|...`) — 0 matches (verified).
