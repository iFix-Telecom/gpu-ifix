---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-CODE-MAP.md
autonomous: true
requirements: [PC-ENV-PRECEDENCE]

must_haves:
  truths:
    - "Every PRIMARY_* shape var (NUM_GPUS, GPU_NAME, PRICE_CAP, ALLOWLIST) has its reader location, default, and consumer documented"
    - "Legacy alias mapping in config.go:622-642 is captured verbatim with which slot each legacy var maps to"
  artifacts:
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-CODE-MAP.md"
      provides: "Code-side var map for env precedence audit (intermediate artifact for plan 07 synthesis)"
      contains: "PRIMARY_VAST_NUM_GPUS_PRIMARY"
  key_links:
    - from: "06.6.X-AUDIT-CODE-MAP.md"
      to: "06.6.X-RESEARCH-ENV-PRECEDENCE.md (plan 09)"
      via: "divergence table input — code column"
      pattern: "config.go:[0-9]+"
---

<objective>
Build the code-side half of the env precedence audit: enumerate every shape var (NUM_GPUS, GPU_NAME, PRICE_CAP, ALLOWLIST family), capture its default, reader location, consumer, and the legacy alias mapping in config.go:622-642.

Purpose: Feed plan 07 synthesis with authoritative code-side values so the divergence table (runtime vs prod-example vs runbook vs code) can be built without re-grepping.
Output: 06.6.X-AUDIT-CODE-MAP.md — intermediate artifact, NOT the final RESEARCH doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md
@gateway/internal/config/config.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep all shape vars across gateway/ and pod/, build code map</name>
  <read_first>
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md (D-10, D-11, D-12 — audit scope restricted to shape family only)
    - gateway/internal/config/config.go lines 200-250 (field declarations, deprecation comments)
    - gateway/internal/config/config.go lines 450-475 (env var read with defaults)
    - gateway/internal/config/config.go lines 615-650 (legacy alias mapping block)
  </read_first>
  <files>.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-CODE-MAP.md</files>
  <action>
    Run these greps from repo root (capture line numbers — required for divergence table traceability):

    ```
    grep -rn -E 'PRIMARY_[A-Z_]*(NUM_GPUS|GPU_NAME|PRICE|ALLOWLIST)' gateway/ pod/ --include='*.go' --include='*.sh' --include='*.yml' --include='*.env*'
    ```

    For each unique env var name returned, record in 06.6.X-AUDIT-CODE-MAP.md the following columns:

    | var_name | declared_field (struct + type) | default_value | read_at (file:line) | consumers (file:line[]) | legacy_alias_target | notes |

    Then extract the legacy alias mapping block (config.go:622-642) verbatim into a fenced code block in the doc — this is the canonical "what maps to what" reference. Per D-10/D-11, explicitly call out which slot `PRIMARY_NUM_GPUS` maps to today (per phase CONTEXT this currently lands in `Fallback`, not `Primary` — confirm via direct read, do not assume).

    Per CLAUDE.md communication rule: every claim in the doc must cite a file:line. No "probably", no "likely". If a consumer is not found via grep, write "consumer: NONE FOUND via grep <pattern>" — do not speculate.

    Scope per D-12: shape family only (`*NUM_GPUS*`, `*GPU_NAME*`, `*PRICE_CAP*`, `*ALLOWLIST*`). Skip other ~26 PRIMARY_* vars.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-CODE-MAP.md && grep -v '^#' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-CODE-MAP.md | grep -cE 'PRIMARY_(VAST_)?(NUM_GPUS|GPU_NAME|PRICE_CAP|ALLOWLIST)' | awk '{ if ($1 < 4) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File 06.6.X-AUDIT-CODE-MAP.md exists in phase dir
    - Table includes at minimum: PRIMARY_VAST_NUM_GPUS_PRIMARY, PRIMARY_VAST_NUM_GPUS_FALLBACK, PRIMARY_NUM_GPUS (legacy), PRIMARY_GPU_NAME, PRIMARY_VAST_PRICE_CAP_DPH, PRIMARY_VAST_OFFER_ALLOWLIST (or actual names found via grep — DO NOT invent)
    - Each row has a file:line reference for read_at column
    - Legacy alias block from config.go:622-642 is quoted verbatim in fenced code block
    - Doc explicitly states which slot `PRIMARY_NUM_GPUS` legacy var currently writes to (Primary or Fallback)
  </acceptance_criteria>
  <done>06.6.X-AUDIT-CODE-MAP.md committed with ≥4 shape vars mapped + legacy alias block quoted</done>
</task>

</tasks>

<verification>
- File exists and contains shape var table with file:line citations
- Legacy alias mapping quoted verbatim
- No speculative language (manual scan for "probably", "likely", "geralmente")
</verification>

<success_criteria>
Plan 07 (env precedence synthesis) can build the divergence table using ONLY this doc + the runtime capture (plan 02) + docs audit (plan 03), with no need to re-grep code.
</success_criteria>

<output>
Create `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-01-SUMMARY.md` when done.
</output>
