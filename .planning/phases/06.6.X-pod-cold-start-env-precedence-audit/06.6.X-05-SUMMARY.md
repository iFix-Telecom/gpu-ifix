---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 05
subsystem: diagnosis
tags: [cold-start, env-precedence, analysis, iter1, vast-ai]
requires:
  - 06.6.X-04 (iter-1 live signals)
  - 06.6.X-01 (code-side audit)
  - 06.6.X-02 (runtime env audit)
provides:
  - "ITER1-ANALYSIS doc with 4 D-08 suspects ranked under D-15"
  - "Iter-2 SKIP decision with rationale"
  - "Hand-off notes for Plan 07 (RESEARCH-COLD-START) + Plan 08 (RESEARCH-ENV-PRECEDENCE)"
affects: []
tech-stack:
  added: []
  patterns: []
key-files:
  created:
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md
  modified: []
decisions:
  - "Cold-start root cause CONFIRMED (D-15 ≥2-signal bar met with 4-of-4 families): Vast host machine 51096 / host_id 348505 accepted pod, reported running, never bound public ports (ssh_host=null, ssh_port=null, public_ipaddr=192.168.1.8 RFC1918, direct_port_count=250 unallocated)"
  - "Iter-2 SKIP: top-1 suspect already confirmed by 4 signal families; D-13 forbids code fixes this phase so a fix-variant repro adds zero diagnostic value; remaining hypotheses (onstart, weights, supervisord) all sit behind reachability and require port-bound pod to instrument — a distinct repro setup, not iter-2 of same hypothesis"
  - "CONTEXT D-10 premise REFUTED: no 'legacy alias mapping PRIMARY_NUM_GPUS → Fallback slot' exists at config.go:622-642 in HEAD; PrimaryNumGPUs is a single field with direct env read at config.go:416"
  - "Unresolved sub-question for Plan 08 RESEARCH: container env has PRIMARY_NUM_GPUS=2 and code reads it, yet iter-1 Vast search filtered num_gpus=1 — implies a second reader or shape-dispatch override not surfaced by Plan 01 grep"
metrics:
  duration_minutes: 10
  completed_date: 2026-06-09
  tasks_completed: 1
  files_created: 2
---

# Phase 06.6.X Plan 05: Iter-1 Signal Analysis Summary

Ranks the 4 D-08 cold-start suspects against iter-1 evidence under D-15 (≥2 independent signal families), decides iter-2 SKIP with explicit rationale, and produces hand-off notes that let Plan 07/08 RESEARCH docs cite source evidence directly.

## What was delivered

- **`06.6.X-ITER1-ANALYSIS.md`** with five sections:
  1. **Suspect ranking table** (4 D-08 suspects × supporting-signals / refuting-signals / D-15 status / fix-complexity columns).
  2. **Iter-2 decision: SKIP** with D-09 rationale (top-1 suspect already CONFIRMED by 4 signal families; remaining hypotheses blocked behind reachability; D-13 forbids fix code this phase).
  3. **Evidence inventory** mapping every supporting file:line back to the suspect it supports (acts as Plan 07/08 source map).
  4. **Hand-off notes** for Plan 06 (optional deeper env-precedence grep), Plan 07 (RESEARCH-COLD-START with 3 fix-option seeds), Plan 08 (RESEARCH-ENV-PRECEDENCE with refuted premise + unresolved sub-question).
  5. **D-15 compliance check** summarising signal-family count per suspect.

## Verdicts (D-15 strict)

| D-08 suspect | Signal-family count | Verdict |
|---|---|---|
| #1 Onstart exit code / timing | 1 (Vast API only — `status_msg=success` + `disk_util/gpu_util` idle) | REMAINING HYPOTHESIS |
| #2 Weights presence | 1 (Vast API `disk_util=23 GB` ≈ image-only) | REMAINING HYPOTHESIS |
| #3 Supervisord boot order | 0 (no signal — pod ssh unreachable) | REMAINING HYPOTHESIS |
| #4 Vast port mapping / sshd reachability | **4 of 4** (Vast API + pod-ssh probe + network probe + gateway silent-wait) | **CONFIRMED ROOT CAUSE** |
| (CONTEXT D-10 sub-premise: legacy alias mapping) | n/a — code referent absent in HEAD | **REFUTED** |

## Iter-2 decision rationale (one-liner)

SKIP — D-09 trigger requires ambiguous top-1 OR a fix-variant to test; iter-1 produced 4-of-4 signal-family agreement on suspect #4 and D-13 bars fix code this phase, so iter-2 would re-confirm what is already confirmed.

## Hand-off to downstream plans

- **Plan 06 (env-precedence-deeper, optional):** re-grep `gateway/` for any `os.Getenv("PRIMARY_NUM_GPUS")` reader beyond `config.go:416` and any shape-table that overrides per-shape `num_gpus`. Goal: reconcile `iter1-gateway-logs.log:5-7` `shape=0 gpu_shape="1xRTX 3090"` against runtime env having `PRIMARY_NUM_GPUS=2`.
- **Plan 07 (RESEARCH-COLD-START.md):** root cause + 3 enumerated fix options seeded — (1) offer-side filter excluding RFC1918 `public_ipaddr`, (2) reconciler health-gate with new `PrimaryPublicPortBindBudgetSeconds`, (3) operator-curated allowlist enforcement (remove broaden-fallback). Plus secondary finding: Vast API auth must use `?api_key=` query param, not Bearer header.
- **Plan 08 (RESEARCH-ENV-PRECEDENCE.md):** divergence table seeded; canonical-var recommendation = keep `PRIMARY_NUM_GPUS` (HEAD reader), deprecate `PRIMARY_VAST_NUM_GPUS_PRIMARY/FALLBACK` from compose.yml + runbook; boot-time gateway log dump of resolved shape as operator visibility primitive; allowlist scope mismatch needs hard-fail vs intentional-broaden decision.

## Deviations from plan

None — plan executed exactly as written. Plan called for 4 verdict cells minimum (per automated verify); delivered 4 D-08 suspect verdicts + 1 additional REFUTED verdict for the CONTEXT D-10 sub-premise (refuted by Plan 01 code audit, surfaced here for Plan 08 input).

## Verification

- `test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md` → PASS
- `grep -cE 'CONFIRMED|REMAINING HYPOTHESIS|REFUTED' 06.6.X-ITER1-ANALYSIS.md` → 20 (≥4 required) → PASS
- Every supporting-signal cell cites log file:line or audit-file section → manually verified
- Iter-2 decision present with rationale → present (section 2)
- D-14 zero-source-edits invariant → respected (only `.planning/phases/06.6.X-...` touched)

## Self-Check: PASSED

- File exists: `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md` → FOUND
- D-15 verdict-count gate: 20 matches (≥4 required) → PASS
- No gateway/ or pod/ source edits in this commit (verified via staged-files list)
