# Phase 6.6.X: Pod cold-start hardening + env precedence audit — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-09
**Phase:** 6.6.X-pod-cold-start-env-precedence-audit
**Areas discussed:** Repro method (cold-start), Diagnostic scope (cold-start suspects), Env precedence audit method, Deliverable + success criterion

**User steering up-front:** Pediu explicitamente "diagnóstico primeiro antes de qualquer fix". Reformulou as 4 áreas de fix-choice → método/escopo de investigação.

---

## Área 1 — Método repro cold-start

| Option | Description | Selected |
|--------|-------------|----------|
| Spike vivo em pod fresco com trace markers | Provisionar pod novo via gateway live-tick; trace markers + Vast events + supervisord + ssh. ~$0.30-0.60 spend, ~15-30min | ✓ |
| Forensics passiva de 11.2 UAT | Ler logs Portainer + audit_log + Vast event log. Zero spend, evidência mais fraca | |
| Híbrido — forensics → spike se inconclusivo | Tentar forensics primeiro, promover pra spike sob demanda | |

**Notes:** Spike escolhido por reproduzir path real do FSM.

### Sub-pergunta — onde provisionar

| Option | Description | Selected |
|--------|-------------|----------|
| Stack ai-gateway-dev em vps-ifix-vm | Path real, env vars reais (reproduz drift se existir) | ✓ |
| Binário gateway local em ops-claude | Env hardcoded, determinístico mas isola path real | |
| Vast UI manual | Sem FSM no loop | |

### Sub-pergunta — sinais a instrumentar (multiSelect)

| Option | Description | Selected |
|--------|-------------|----------|
| Vast lifecycle (actual_status, status_msg, ports) | Polling GET /instances/<id> a cada 5s | ✓ |
| Pod-side (supervisord, onstart, dmesg, df, ps) | ssh root@<pod> + tail logs | ✓ |
| Gateway-side (reconciler logs + audit_log + lifecycle row) | docker logs + DB query | ✓ |
| Network (nc/curl nos 4 endpoints) | Loop externo nc -zv 4 ports a cada 10s | ✓ |

### Sub-pergunta — budget iterações

| Option | Description | Selected |
|--------|-------------|----------|
| 2 iterações / ~$1.20 cap | Baseline + variante isolante | ✓ |
| 1 iteração / ~$0.60 cap | Só baseline, risco inconclusivo | |
| Aberto / hard cap $3.00 | Sem nº pré-definido | |

---

## Área 2 — Escopo diagnóstico cold-start

| Option | Description | Selected |
|--------|-------------|----------|
| Árvore aberta com priorização por evidência | Lista TODOS suspeitos, 1ª iter sem assumir, 2ª testa top-1 | ✓ |
| Hipótese única (whisper-missing) primeiro | Mais rápido, risco root cause errado | |
| Sequencial top-3 ranqueadas | 2 iter no budget → comprimido a top-2, não cabe | |

### Sub-pergunta — suspeitos a instrumentar 1ª iter (multiSelect)

| Option | Description | Selected |
|--------|-------------|----------|
| Onstart exit code + tempo | Onstart terminou? Errou? | ✓ |
| Weights presence (`/weights/{qwen,whisper,bge-m3}`) | df + ls + sha256 amostra | ✓ |
| Supervisord boot order + per-program status | supervisorctl status + startsecs/exitstatus | ✓ |
| Vast port mapping + sshd reachability | Phase 06.6 fix regrediu? | ✓ |

---

## Área 3 — Mapeamento env precedence

| Option | Description | Selected |
|--------|-------------|----------|
| Código + runtime real + docs | Tripla audit, produz tabela divergencias | ✓ |
| Só código + .env.example | Inferir intenção, não pega drift atual | |
| Só runtime vs MEMORY standing | Rápido mas perde gap código↔docs | |

### Sub-pergunta — escopo das vars

| Option | Description | Selected |
|--------|-------------|----------|
| Shape vars só (NUM_GPUS, GPU_NAME, PRICE_CAP, ALLOWLIST) | Carry-forward só menciona shape drift | ✓ |
| Todas PRIMARY_* (~30 vars) | Mais trabalho, pega drift inesperado | |
| Shape + cold-start vars (BUDGET_SECONDS, HEALTH_TIMEOUT) | Híbrido | |

---

## Área 4 — Entregável + sucesso

| Option | Description | Selected |
|--------|-------------|----------|
| RESEARCH.md só → fix vai pra 6.6.Y | Escopo apertado, ping-pong fase-fase aceitável | ✓ |
| RESEARCH + fix env-precedence inline | Env é quase zero-risco; cold-start fica RESEARCH | |
| Tudo inluso — root cause + fix + UAT | Phase única, risco scope creep | |

### Sub-pergunta — critério "root cause confirmado"

| Option | Description | Selected |
|--------|-------------|----------|
| Evidência direta dos 4 sinais (≥2 fontes) | Rigoroso, sem evidência → hipótese-restante | ✓ |
| Hipótese top-ranking por evidência parcial | Rápido encerrar, risco 6.6.Y atacar fix errado | |
| Repro do fix funcional (fix-valida-root-cause) | Estoura escopo (6.6.X vira fix-phase) | |

---

## Claude's Discretion

- Ordem de coleta dos 4 sinais na 1ª iter (paralelo OK).
- Formato exato da tabela de precedence (markdown vs csv anexo).
- Decisão de continuar pra 2ª iter vs encerrar após 1ª se evidência basta.
- Escolha de offer ID específico dentro allowlist `43803,55158` cap $0.60.

## Deferred Ideas

- Fix implementation phase 6.6.Y (criada via gsd-discuss-phase 6.6.Y pós-RESEARCH).
- Cold-start budget audit (`PrimaryProvisionColdStartBudgetSeconds`) — secondary finding se aparecer.
- Full PRIMARY_* audit (~30 vars) — só se shape audit surfacar padrão estrutural.
- Replay 11.2 cenários 1+5 UAT — em 6.6.Y pós-fix.
- OpenAI key rotation — operator runbook only.
- Boot-time resolved-shape log dump — fix candidate pra RESEARCH-ENV-PRECEDENCE options.
- MinIO weight key mock test — alt repro path, só se 1ª spike inconclusiva.
