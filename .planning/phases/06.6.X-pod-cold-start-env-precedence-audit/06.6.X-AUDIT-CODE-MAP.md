---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 01
artifact: code-side var map
scope: shape family only (NUM_GPUS, GPU_NAME, PRICE_CAP, ALLOWLIST/BLOCKLIST) per D-12
audit_date: 2026-06-09
audit_method: grep -rn -E 'PRIMARY_(VAST_)?(NUM_GPUS|GPU_NAME|PRICE|ALLOWLIST|VAST_OFFER)' gateway/ pod/ + manual config.go read (lines 195-199, 412-416)
---

# 06.6.X — Audit Code Map (shape vars)

**Purpose:** Code-side half of env precedence audit. Feeds plan 07 synthesis (divergence table: runtime vs prod-example vs runbook vs code).

**Scope (D-12):** Shape family only — `*NUM_GPUS*`, `*GPU_NAME*`, `*PRICE_CAP*`, `*ALLOWLIST*`. Other ~26 PRIMARY_* vars deferred.

**Method:** Direct grep + read at every cited file:line. Per CLAUDE.md: every claim cites file:line. No speculation. Where a referenced symbol is absent, marked "NONE FOUND via grep <pattern>".

---

## Critical Finding (read first)

The phase CONTEXT.md (D-10/D-11, `canonical_refs` block) cites symbols that **do not exist in the current code**:

| Symbol cited in CONTEXT.md | Status in code (HEAD = 1311a25) |
|---|---|
| `PRIMARY_VAST_NUM_GPUS_PRIMARY` | NONE FOUND via `grep -rn 'PRIMARY_VAST_NUM_GPUS_PRIMARY' gateway/ pod/` |
| `PRIMARY_VAST_NUM_GPUS_FALLBACK` | NONE FOUND via `grep -rn 'PRIMARY_VAST_NUM_GPUS_FALLBACK' gateway/ pod/` |
| `PRIMARY_VAST_OFFER_ALLOWLIST` | NONE FOUND (actual var is `PRIMARY_VAST_MACHINE_ALLOWLIST` — gateway/internal/config/config.go:197,414) |
| "legacy alias mapping block at `config.go:622-642`" | LINES EXIST but contain `csvOr()` helper (gateway/internal/config/config.go:622-642) — no alias mapping logic; `grep -n -iE '(legacy\|alias\|DEPRECATED)' gateway/internal/config/config.go` returns only comments at lines 23, 271, 272, 540 — none are a primary-shape alias mapping block |
| "`PRIMARY_NUM_GPUS` legacy var currently writes to Fallback slot" | NO SLOT EXISTS. `PRIMARY_NUM_GPUS` is read directly at `config.go:416` (`atoiOr(os.Getenv("PRIMARY_NUM_GPUS"), 1)`) into a single field `PrimaryNumGPUs`. There is no Primary/Fallback split, no alias mapping, no warn-on-set. |
| "`config.go:217-228` `PrimaryNumGPUs` field comment + `PrimaryVastNumGPUsPrimary/Fallback` defaults" | `PrimaryNumGPUs` declared at line 199 (not 217-228); `PrimaryVastNumGPUs{Primary,Fallback}` NONE FOUND |
| "`config.go:458-466` env var read with defaults" | Lines 458-466 contain `requiredOrder` slice + `required` map (auth/upstream DSN validation), NOT shape env reads. Shape reads are at lines 412-416. |

**Implication for plan 07 (env precedence synthesis):**
- The "divergence" between operator MEMORY (`PRIMARY_NUM_GPUS=2`) and Vast search outcome (1×3090) **cannot be explained by a Primary/Fallback alias misrouting** — that abstraction does not exist in code.
- The actual code path is direct: `PRIMARY_NUM_GPUS` env (or default `1`) → `cfg.PrimaryNumGPUs` → `vast.DefaultSearchFilter(..., r.cfg.PrimaryNumGPUs, ...)` at `reconciler.go:769` → `{"num_gpus":{"eq":N}}` in Vast search body (`vast/types.go:235`).
- If runtime captured `PRIMARY_NUM_GPUS=2` in vps-ifix-vm `.env` yet Vast received `num_gpus=1`, the root cause is upstream of code (env not exported into container, Portainer stack.env precedence over .env file, docker-compose env_file ordering, etc.) — to be confirmed by plan 02 runtime audit.

This finding itself is the deliverable expected by plan 07 (one of the divergence rows = "code reality vs phase-CONTEXT assumption").

---

## Shape Var Code Map

All shape vars declared in `gateway/internal/config/config.go` and read in the same file.

| var_name | declared_field (struct + Go type) | declaration file:line | default_value | read_at (file:line) | consumers (file:line) | legacy_alias_target | notes |
|---|---|---|---|---|---|---|---|
| `PRIMARY_VAST_PRICE_CAP_DPH` | `PrimaryVastPriceCapDPH float64` | gateway/internal/config/config.go:195 | `2.20` (USD/hour) | gateway/internal/config/config.go:412 | gateway/internal/primary/reconciler.go:769 (DefaultSearchFilter arg), reconciler.go:791,803 (FilterBelowCap), cmd/gateway/main.go:951 (boot log) | none | Default comment cites "RTX 5090 EU cap" — does NOT match standing MEMORY config `2×RTX 3090 cap $0.60`. Default and operator runtime value diverge by design; operator MUST set env. |
| `PRIMARY_VAST_MACHINE_BLOCKLIST` | `PrimaryVastMachineBlocklist []int64` | gateway/internal/config/config.go:196 | `nil` (empty) — parsed via `parseInt64CSV` | gateway/internal/config/config.go:413 | gateway/internal/primary/reconciler.go:769 (DefaultSearchFilter variadic tail) | none | Empty default = no machines excluded. |
| `PRIMARY_VAST_MACHINE_ALLOWLIST` | `PrimaryVastMachineAllowlist []int64` | gateway/internal/config/config.go:197 | `nil` (empty) — parsed via `parseInt64CSV` | gateway/internal/config/config.go:414 | gateway/internal/primary/reconciler.go:784 (length check), 785 (WithMachineAllowlist filter), 794 (log field) | none | Standing MEMORY config = `43803,55158`. Empty default = "no preference" (allowlist-first then broaden — see field comment line 197). |
| `PRIMARY_GPU_NAME` | `PrimaryGPUName string` | gateway/internal/config/config.go:198 | `"RTX 5090"` (via `envOr`) | gateway/internal/config/config.go:415 | gateway/internal/primary/reconciler.go:769 (DefaultSearchFilter arg) → vast/types.go:235 (`gpu_name` filter) | none | Default `"RTX 5090"` does NOT match standing MEMORY config `RTX 3090`. Operator MUST set env. |
| `PRIMARY_NUM_GPUS` | `PrimaryNumGPUs int` | gateway/internal/config/config.go:199 | `1` (via `atoiOr`) | gateway/internal/config/config.go:416 | gateway/internal/primary/reconciler.go:769 (DefaultSearchFilter arg) → vast/types.go:235 (`num_gpus: {eq: N}` filter) | none — **no Primary/Fallback alias exists**; read directly into single field | Field comment (config.go:199) explicitly documents `2` as the 2×RTX 3090 single-pod topology value. Standing MEMORY config requires `2`; default of `1` is the source of shape drift IF env not honored at runtime. |

**Consumer chain (single path):**

```
ENV PRIMARY_* → config.Load (gateway/internal/config/config.go:412-416)
              → cfg.Primary{VastPriceCapDPH, VastMachineBlocklist, VastMachineAllowlist, GPUName, NumGPUs}
              → reconciler.acquire / search (gateway/internal/primary/reconciler.go:769, 784-785)
              → vast.DefaultSearchFilter (gateway/internal/emerg/vast/types.go:235 emits {"num_gpus":{"eq":N}, "gpu_name":..., "dph_total":{"lte":cap}})
              → POST /api/v0/bundles (Vast search)
```

No alternate readers, no override layer, no slot routing. The CONTEXT.md "PRIMARY_NUM_GPUS → Fallback slot" claim has no code referent in HEAD.

---

## Legacy Alias Block — VERBATIM at cited line range

CONTEXT.md cites `config.go:622-642` as the "legacy alias mapping" block. Quoting verbatim:

```go
// csvOr parses a comma-separated string into []string, trimming whitespace
// around each element. Empty input or all-blank elements -> default. Used
// for UPSTREAM_LLM_OPENROUTER_PROVIDER_ORDER which the Director injects
// into the request body via {"provider":{"order":[...]}}, see CONTEXT.md
// D-C2.
func csvOr(s string, def []string) []string {
	if strings.TrimSpace(s) == "" {
		return def
	}
	parts := strings.Split(s, ",")
	out := make([]string, 0, len(parts))
	for _, p := range parts {
		if t := strings.TrimSpace(p); t != "" {
			out = append(out, t)
		}
	}
	if len(out) == 0 {
		return def
	}
	return out
}
```

**This is NOT an alias mapping block** — it is the `csvOr` helper. Used by `AlertEmailTo` (config.go:453) and (per its own comment) `UPSTREAM_LLM_OPENROUTER_PROVIDER_ORDER` — neither is a primary-shape var.

Exhaustive search for ANY legacy-alias logic targeting shape vars:

```bash
$ grep -n -iE '(legacy|alias|DEPRECATED|deprecated)' gateway/internal/config/config.go
23:	// DEPRECATED in Phase 3 — use WriteTimeoutChat/Embed/Audio per route.
271:// (multi-instance: gatewayctl model-alias set; per-instance: this env var).
272:// They are NOT deprecated. Phase 06.9 Plan 04 replaces an earlier
540:	// sensitive. We deliberately avoid the word "deprecated": per D-06
```

None of these four hits is a primary-shape alias mapping. Lines 23, 271-272, 540 reference unrelated subsystems (WriteTimeoutChat, model-alias multi-instance plugin, an unspecified Phase 06.9 D-06 comment).

**Conclusion:** There is **no legacy alias mapping for primary-pod shape vars in the current code** (HEAD = commit `1311a25`). The phase CONTEXT's D-10/D-11 premise — that a "legacy alias mapping" silently re-routes `PRIMARY_NUM_GPUS` to a `Fallback` slot — is unsupported by code evidence in HEAD.

---

## pod/ side (vast-ai.sh manual path, NOT live-tick)

Per phase CONTEXT.md D-03, manual `pod/scripts/vast-ai.sh` is **not** the path used by the gateway reconciler (live-tick). Captured here for completeness because plan 02 may use it as a control comparison.

| var/literal | file:line | value |
|---|---|---|
| `num_gpus:1` (HARDCODED literal in jq filter) | pod/scripts/vast-ai.sh:54 | `1` — NOT env-driven |
| `--gpu GPU_NAME` (CLI arg) | pod/scripts/vast-ai.sh:48,183 | operator-supplied; no env default |
| `--max-price N` (CLI arg, → `dph_total.lte`) | pod/scripts/vast-ai.sh:47,57 | operator-supplied |

No `PRIMARY_*` env consumption in `pod/scripts/vast-ai.sh`. The manual script's `num_gpus:1` hardcode would have to be edited for 2×3090 manual search; this is a separate code surface from the gateway live-tick path and is out of scope for the runtime drift discussed in carry-forward Item 3.

---

## Test coverage for shape vars (informational)

| var | test refs |
|---|---|
| `PRIMARY_VAST_PRICE_CAP_DPH` | gateway/internal/config/config_test.go:818 (required-env list test), 947 (`t.Setenv` to `"0.50"` for boot test) |
| `PRIMARY_NUM_GPUS` | gateway/internal/emerg/vast/types_test.go:94 (comment `TestDefaultSearchFilter_NumGPUs covers the num_gpus knob (PRIMARY_NUM_GPUS)`) |
| `PRIMARY_GPU_NAME` | NONE FOUND via `grep -rn 'PRIMARY_GPU_NAME' gateway/.*_test.go` |
| `PRIMARY_VAST_MACHINE_ALLOWLIST` | NONE FOUND via `grep -rn 'PRIMARY_VAST_MACHINE_ALLOWLIST' gateway/.*_test.go` |
| `PRIMARY_VAST_MACHINE_BLOCKLIST` | NONE FOUND via `grep -rn 'PRIMARY_VAST_MACHINE_BLOCKLIST' gateway/.*_test.go` |

---

## Hand-off to plan 07 (env precedence synthesis)

For the divergence-table merge, this doc provides the **code** column. Recommended row schema:

| var | code default | code reader | runtime value (plan 02) | prod-example value (plan 03) | runbook value (plan 03) | divergence? | impact |
|---|---|---|---|---|---|---|---|

Pre-filled rows from this audit (code columns only):

| var | code default | code reader |
|---|---|---|
| `PRIMARY_VAST_PRICE_CAP_DPH` | `2.20` | config.go:412 |
| `PRIMARY_VAST_MACHINE_BLOCKLIST` | empty `[]int64` | config.go:413 |
| `PRIMARY_VAST_MACHINE_ALLOWLIST` | empty `[]int64` | config.go:414 |
| `PRIMARY_GPU_NAME` | `"RTX 5090"` | config.go:415 |
| `PRIMARY_NUM_GPUS` | `1` | config.go:416 |

**Plus one synthesis row plan 07 must include** (CONTEXT vs code reality):

| claim | source | code evidence | status |
|---|---|---|---|
| "Legacy alias maps `PRIMARY_NUM_GPUS` → Fallback slot" | phase CONTEXT.md D-10/D-11; canonical_refs `config.go:622-642` | No alias logic; lines 622-642 are `csvOr` helper; no `*Fallback` field for shape | **claim is unsupported by HEAD code — drop or rewrite hypothesis before plan 07 synthesis** |

---

*Generated 2026-06-09 — plan 06.6.X-01.*
*Source commit: 1311a25 (HEAD on develop).*
*Diagnose-only (D-14): zero edits to gateway/ or pod/.*
