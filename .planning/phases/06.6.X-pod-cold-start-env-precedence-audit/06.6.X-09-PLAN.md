---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 09
type: execute
wave: 6
depends_on: [06.6.X-07, 06.6.X-08]
files_modified:
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-VERIFICATION.md
autonomous: true
requirements: [PC-COLD-START-FIX, PC-ENV-PRECEDENCE]

must_haves:
  truths:
    - "All live Vast.ai pods from this phase are confirmed destroyed (cost containment)"
    - "Both RESEARCH docs exist and are referenced from 06.6.X-VERIFICATION.md rollup"
    - "Total Vast spend across iter-1 + iter-2 is logged and ≤ $1.20"
    - "Phase 6.6.X verdict (passed / passed_partial) is set based on whether both PC-COLD-START-FIX and PC-ENV-PRECEDENCE deliverables meet D-13 + D-15 criteria"
  artifacts:
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-VERIFICATION.md"
      provides: "Phase rollup pointing to both RESEARCH docs + spend log + open items for 6.6.Y"
      contains: "RESEARCH-COLD-START|RESEARCH-ENV-PRECEDENCE|verdict"
  key_links:
    - from: "06.6.X-VERIFICATION.md"
      to: "STATE.md + Phase 6.6.Y discussion"
      via: "hand-off marker"
      pattern: "Phase 6.6.Y"
---

<objective>
Phase rollup: confirm pod destruction, sum spend, link both RESEARCH docs, set phase verdict.

Purpose: Standard phase closure artifact for STATE.md updates and 6.6.Y hand-off.
Output: 06.6.X-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify pod destruction + sum spend + write phase rollup</name>
  <read_first>
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-SIGNALS.md
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md
  </read_first>
  <files>.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-VERIFICATION.md</files>
  <action>
    Step 1 — Confirm cost containment. List all Vast instance_ids from iter-1 + iter-2 SIGNALS docs. For each, query:
    ```
    curl -sH "Authorization: Bearer $VAST_AI_API_KEY" "https://vast.ai/api/v0/instances/<id>" | jq '{actual_status,intended_status}'
    ```
    All MUST return either 404, actual_status=destroyed, or not present in account. If any still running, destroy it via DELETE and log.

    Also sanity check no other phase-related instances linger:
    ```
    curl -sH "Authorization: Bearer $VAST_AI_API_KEY" "https://vast.ai/api/v0/instances" | jq '.instances[] | {id,actual_status,label}'
    ```

    Step 2 — Sum spend from iter-1 + iter-2 spend blocks. Total MUST be ≤ $1.20.

    Step 3 — Write 06.6.X-VERIFICATION.md with sections:

    **1. Phase Verdict**: `passed` if both deliverables meet D-13 + D-15 criteria (both RESEARCH docs exist, env precedence has divergence table + canonical recommendation, cold-start has root cause OR labelled remaining hypotheses + ≥2 fix options). Otherwise `passed_partial` with explicit gap list.

    **2. Deliverables**:
    - PC-COLD-START-FIX: → 06.6.X-RESEARCH-COLD-START.md (1-line summary of verdict)
    - PC-ENV-PRECEDENCE: → 06.6.X-RESEARCH-ENV-PRECEDENCE.md (1-line summary of recommendation)

    **3. Spend Log**: iter-1 total / iter-2 total / combined total / budget cap $1.20 — pass/fail.

    **4. Cost Containment**: list of Vast instance_ids + status (destroyed/404).

    **5. Open Items for 6.6.Y**: bulleted carry-over from each RESEARCH doc's "Open Items" / "Hand-off" sections.

    **6. STATE.md Update Sketch**: 3-5 lines for the next STATE update (current phase complete, next = discuss 6.6.Y).

    Per CLAUDE.md: cite Vast API responses for destroyed status. No "should be destroyed" — only "GET returned 404" or "actual_status=destroyed".
  </action>
  <verify>
    <automated>test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-VERIFICATION.md && grep -cE 'passed|passed_partial' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-VERIFICATION.md && grep -cE 'RESEARCH-COLD-START|RESEARCH-ENV-PRECEDENCE' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-VERIFICATION.md | awk '{ if ($1 < 2) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all 6 sections
    - Verdict is exactly `passed` or `passed_partial` with rationale
    - All Vast instances from this phase confirmed destroyed (cited per-id evidence)
    - Combined spend ≤ $1.20 logged
    - Both RESEARCH docs linked
  </acceptance_criteria>
  <done>06.6.X-VERIFICATION.md committed; phase ready for STATE update</done>
</task>

</tasks>

<verification>
- All pods destroyed (per-id evidence)
- Spend ≤ $1.20
- Both RESEARCH docs present and linked
- Phase verdict set
</verification>

<success_criteria>
Operator can /gsd-discuss-phase 6.6.Y using the two RESEARCH docs as inputs; phase is closeable from STATE.md perspective.
</success_criteria>

<output>Create `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-09-SUMMARY.md` when done.</output>
