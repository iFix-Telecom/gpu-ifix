---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-DOCS-VALUES.md
autonomous: true
requirements: [PC-ENV-PRECEDENCE]

must_haves:
  truths:
    - "Every shape var has its documented value extracted from RUNBOOK-PRIMARY-POD.md, RUNBOOK-OPS.md, .env.example, .env.prod.example, and MEMORY primary-gpu-shape-06.8-final"
    - "Documentation gaps (var not mentioned in any doc) are explicitly flagged"
  artifacts:
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-DOCS-VALUES.md"
      provides: "Docs-side var capture for env precedence audit (intermediate)"
      contains: "RUNBOOK"
  key_links:
    - from: "06.6.X-AUDIT-DOCS-VALUES.md"
      to: "06.6.X-RESEARCH-ENV-PRECEDENCE.md (plan 09)"
      via: "divergence table input — docs columns"
      pattern: "doc"
---

<objective>
Capture documented values for shape vars across runbooks, .env examples, and MEMORY notes.

Purpose: Operator follows docs; if docs disagree with code default or runtime value, that's a divergence the audit must surface.
Output: 06.6.X-AUDIT-DOCS-VALUES.md — intermediate artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep shape vars across runbooks, .env examples, and MEMORY</name>
  <read_first>
    - gateway/docs/RUNBOOK-PRIMARY-POD.md (full read)
    - gateway/docs/RUNBOOK-OPS.md (sections mentioning PRIMARY_*)
    - gateway/.env.example
    - gateway/.env.prod.example
    - $HOME/.claude/projects/-home-pedro-projetos-pedro-gpu-ifix/memory/primary-gpu-shape-06.8-final.md
  </read_first>
  <files>.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-DOCS-VALUES.md</files>
  <action>
    For each of these 5 doc sources, run:
    ```
    grep -nE 'PRIMARY_.*(NUM_GPUS|GPU_NAME|PRICE|ALLOWLIST)' <doc-path>
    ```

    Build a table in 06.6.X-AUDIT-DOCS-VALUES.md:

    | var_name | RUNBOOK-PRIMARY-POD.md | RUNBOOK-OPS.md | .env.example | .env.prod.example | MEMORY primary-gpu-shape-06.8-final |

    Each cell either holds the documented value + source line OR the literal string `NOT MENTIONED`. Per CLAUDE.md: do not infer — if a doc does not mention the var, write NOT MENTIONED, never "implied default".

    Also include a short prose section at the end: "Documentation gaps (vars used in code but absent from operator-facing docs)" — list them. This feeds plan 09's recommendation block (e.g., "document var X in RUNBOOK").

    Scope per D-12: shape family only.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-DOCS-VALUES.md && grep -cE 'RUNBOOK|\.env\.example|MEMORY' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-DOCS-VALUES.md | awk '{ if ($1 < 3) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File exists with table covering ≥4 shape vars × 5 doc sources
    - Each cell either has a value+source-line or literal `NOT MENTIONED`
    - Documentation-gaps prose section present
  </acceptance_criteria>
  <done>06.6.X-AUDIT-DOCS-VALUES.md committed with 5-source table + gaps section</done>
</task>

</tasks>

<verification>Table covers all 5 doc sources × shape vars; NOT MENTIONED used instead of inference.</verification>

<success_criteria>Plan 09 can fill docs columns of divergence table directly from this doc.</success_criteria>

<output>Create `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-03-SUMMARY.md` when done.</output>
