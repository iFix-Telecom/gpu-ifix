---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 04
tasks_completed: 4/4
status: complete
spend_usd: 0.0399
budget_remaining_usd: 1.16
---

# Plan 04 Summary — Iter-1 cold-start signal capture

## Outcome

✅ All 4 signal families captured. ✅ Pod destroyed (lifecycle 101 `shutdown_reason=cancelled_in_flight`). ✅ Spend $0.040 / $0.60 cap.

## Artifacts

- `06.6.X-ITER1-SIGNALS.md` — rollup + spend + suspect summary + hand-off to Plan 05
- `iter1-vast-timeline.log` (poll + manual correction)
- `iter1-pod-snapshot.log` (SSH-unreachable observation + corrected reason)
- `iter1-gateway-logs.log` (full ai-gateway-dev logs + final DB row)
- `iter1-network-probe.log` (port reachability + corrected reason)

## Confirmed root causes (D-15 ≥2 signals)

1. **Cold-start root cause (PC-COLD-START-FIX):** pod runs but Vast host never binds public ports (`ssh_host=null`, `ssh_port=null`, `direct_port_count=250 unallocated`, `public_ipaddr=192.168.1.8 RFC1918`) → gateway reconciler waits forever. Confirmed by 4 independent signal families.
2. **Env-precedence drift (PC-ENV-PRECEDENCE):** operator-set `PRIMARY_VAST_NUM_GPUS_PRIMARY=2` and `PRIMARY_NUM_GPUS=2` ignored at runtime — gateway used `shape=0 gpu_shape="1xRTX 3090"` and provisioned `num_gpus=1`. Confirmed by Plan 02 runtime env capture + Plan 01 code map (`PRIMARY_VAST_NUM_GPUS_PRIMARY` has no reader in HEAD code) + Plan 04 live log + Vast API truth.

## Anomalies / data-quality

- Plan-prescribed `Authorization: Bearer` auth for Vast `/instances/<id>/` endpoint silently returns null fields. Correct auth: `?api_key=` query param. Three of four collectors initially captured nulls; rescued via manual mid-flight curl. **Plan 06 / future iters must use query-param auth.** Recommend updating Plan 06 inline before re-attempt.

## Compliance

- **D-14 (diagnose-only):** `git status gateway/ pod/` clean — zero source edits.
- **D-02 (budget cap $1.20):** spend $0.040, remaining $1.16.
- **D-03 (gateway live-tick path):** used `gatewayctl primary force-up` / `force-down`, NOT `vast-ai.sh`.

## Hand-off to Plan 05

Plan 05 (Wave 3, autonomous) can proceed with sufficient evidence. Suspect ranking + iter-2 go/no-go input documented in `06.6.X-ITER1-SIGNALS.md` final section. Iter-2 likely SKIPPABLE given 4-signal agreement (Plan 05 to decide formally).

## Deviations from plan

- Operator authorized phase to proceed without Task 1 explicit human checkpoint (delegated execution at session start).
- Vast API auth bug forced manual correction loop; recorded.
