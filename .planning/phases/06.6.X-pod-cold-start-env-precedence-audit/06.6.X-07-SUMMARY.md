---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 07
subsystem: env-precedence-audit
tags: [research, env-precedence, diagnose-only, D-14]
requires:
  - 06.6.X-AUDIT-CODE-MAP.md
  - 06.6.X-AUDIT-RUNTIME-ENV.md
  - 06.6.X-AUDIT-DOCS-VALUES.md
  - 06.6.X-ITER1-SIGNALS.md
  - 06.6.X-ITER1-ANALYSIS.md
provides:
  - 06.6.X-RESEARCH-ENV-PRECEDENCE.md (PC-ENV-PRECEDENCE deliverable)
affects:
  - Phase 6.6.Y (future fix-implementation phase)
tech-stack:
  added: []
  patterns: []
key-files:
  created:
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md
  modified: []
decisions:
  - "Recommended canonical: PRIMARY_VAST_{NUM_GPUS,GPU_NAME,PRICE_CAP}_{PRIMARY,FALLBACK} (already in code, already in compose; legacy aliases delete)"
  - "Recommended deprecation: HARD FAIL-FAST at boot (config.Load returns error if any legacy alias set); soft-warn rejected because it has been demonstrably inert for ~30 days"
  - "REVERSES Plan 05 ITER1-ANALYSIS REFUTED verdict — alias block at config.go:608-642 IS present and matches CONTEXT D-10 premise; Plan 01 grep had a false-negative (csvOr is at line 714 not 622-642)"
metrics:
  duration_minutes: 8
  tasks: 1
  files_created: 2
  files_modified: 0
  completed: 2026-06-09
---

# Phase 06.6.X Plan 07: RESEARCH Env Precedence Summary

Synthesized 3 audit inputs (code/runtime/docs) into the PC-ENV-PRECEDENCE deliverable — diagnose-only research doc with divergence table, canonical-var recommendation, hard fail-fast deprecation plan, and ready-to-paste config.go diff sketch for Phase 6.6.Y.

## What was built

- `06.6.X-RESEARCH-ENV-PRECEDENCE.md` — 8-section research doc covering:
  1. Executive summary (drift named, single-sentence root cause, canonical recommendation, deprecation severity)
  2. Divergence table — 4 shape var families × 6 columns (code-read / code-default / runtime / prod-example / runbook / divergence Y/N / impact)
  3. Legacy alias block verbatim quote (`config.go:608-642`) + analysis of why the alias is silently inert at runtime (compose-default poisons the copy-on-empty guard)
  4. Canonical var recommendation per domain (NUM_GPUS / GPU_NAME / PRICE_CAP / ALLOWLIST)
  5. Hard fail-fast deprecation plan (with tradeoffs table vs soft warn)
  6. Proposed `config.go:608-642` diff sketch + 7 companion changes (compose, env-example, runbook, MEMORY, vast-ai.sh) marked `FOR PHASE 6.6.Y IMPLEMENTATION, NOT THIS PHASE`
  7. 6 open questions for Phase 6.6.Y discuss-phase
  8. Hand-off paragraph

## Key technical decisions

1. **Reversed Plan 05's REFUTED verdict** on the CONTEXT D-10 alias-mapping premise. Plan 01 (`06.6.X-AUDIT-CODE-MAP.md`) hallucinated `config.go:622-642` as the `csvOr` helper; verification by direct grep (`grep -n 'func csvOr' gateway/internal/config/config.go` → line 714) and direct Read of lines 608-642 confirmed the alias mapping block IS present exactly as CONTEXT described. Plan 05 ITER1-ANALYSIS line 144 REFUTED verdict was based on the Plan 01 false-negative; this RESEARCH doc explicitly reverses it.
2. **Resolved the Plan 05 hand-off "unresolved sub-question"** (`06.6.X-ITER1-ANALYSIS.md:106,114`: "is there a second reader that wins over `PRIMARY_NUM_GPUS`?"). YES — `PrimaryVastNumGPUsPrimary` at `config.go:465` + reconciler shape-0 path at `reconciler.go:786-790`. The Plan 01 grep missed all 6 `PRIMARY_VAST_*_{PRIMARY,FALLBACK}` readers because the search pattern was too narrow.
3. **Recommended HARD FAIL-FAST over soft warn** because (a) the soft-warn has been live since Phase 11.1 D-A6 (~30 days) and produced zero observed deprecation logs in iter-1 capture, (b) the warn-guard `if os.Getenv(NEW) == ""` is poisoned by `docker-compose.yml` defaults that always inject a non-empty value, (c) MEMORY + `.env.prod.example` continue to teach the legacy names → ecosystem has not migrated. Boot-time crash creates a time-bounded actionable incident; silent drift creates Phase 11.2 Item 3 incidents repeatedly.

## Verification

- `test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md` → exists
- `grep -cE 'Divergence|Canonical|Deprecation|Alias Mapping' .../06.6.X-RESEARCH-ENV-PRECEDENCE.md` → 4 (acceptance ≥4 satisfied)
- `grep -c "FOR PHASE 6.6.Y IMPLEMENTATION" .../06.6.X-RESEARCH-ENV-PRECEDENCE.md` → 1 (marker present)
- `git status gateway/ pod/` → clean (D-14 compliance — zero source edits)
- All 4 shape families covered (NUM_GPUS, GPU_NAME, PRICE_CAP, ALLOWLIST) × 6 columns per row
- Canonical recommendation per divergent var (§4)
- Single deprecation option recommended with rationale (§5 = HARD FAIL-FAST)
- config.go change shown as ```diff fenced block, NOT committed in `gateway/`

## Deviations from Plan

### Rule 1 - Bug (analysis correction, not code edit)

**1. Plan 05 REFUTED verdict reversed**
- **Found during:** Task 1 (reading Plan 05 ITER1-ANALYSIS hand-off + re-grepping config.go to investigate the "shape=0 vs PRIMARY_NUM_GPUS=2" gap that Plan 05 flagged as unresolved)
- **Issue:** Plan 01 reported the alias mapping block at `config.go:622-642` was the `csvOr` helper and that the CONTEXT D-10 premise was unsupported. Plan 05 carried this forward and labelled the premise REFUTED at line 144. Re-grep showed `csvOr` is actually at line 714, and `config.go:608-642` contains exactly the alias mapping CONTEXT described.
- **Fix:** RESEARCH §1 + §2.5 + §3 explicitly document the correction. No code edits (D-14). Plan 01 audit-method gap flagged as open question #6 for Phase 6.6.Y.
- **Files modified:** None in source. Only the new RESEARCH doc.
- **Commit:** (current commit)

## Authentication Gates

None — no live infra access required for this synthesis plan.

## Known Stubs

None — every claim in the RESEARCH doc cites file:line.

## Threat Flags

None — no new network endpoints, auth paths, or schema changes. This is a documentation-only deliverable feeding a future fix phase.

## Self-Check: PASSED

- FOUND: `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md`
- FOUND: `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-07-SUMMARY.md` (this file)
- D-14 compliance: `git status gateway/ pod/` clean — verified before commit
