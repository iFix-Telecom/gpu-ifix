---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 04
type: execute
wave: 2
depends_on: [06.6.X-02]
files_modified:
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-vast-timeline.log
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-pod-snapshot.log
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-gateway-logs.log
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-network-probe.log
autonomous: false
requirements: [PC-COLD-START-FIX]

must_haves:
  truths:
    - "Iter-1 baseline pod is provisioned via gateway live-tick (NOT vast-ai.sh) per D-03"
    - "All 4 signal families (Vast / Pod / Gateway / Network) are captured concurrently"
    - "lifecycle_id is recorded so DB forensics remain accessible post-destroy"
    - "Total Vast spend for iter-1 is logged and ≤ $0.60 (~1h on 2×3090)"
  artifacts:
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md"
      provides: "Iter-1 signal rollup pointing to 4 raw log files"
      contains: "lifecycle_id"
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-vast-timeline.log"
      provides: "Vast-side timeline raw (D-04)"
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-pod-snapshot.log"
      provides: "Pod-side snapshot raw (D-05)"
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-gateway-logs.log"
      provides: "Gateway-side logs + DB row raw (D-06)"
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-network-probe.log"
      provides: "Network probe raw (D-07)"
  key_links:
    - from: "iter1 logs"
      to: "06.6.X-ITER1-ANALYSIS.md (plan 05)"
      via: "evidence input for suspect ranking"
      pattern: "lifecycle_id"
---

<objective>
Provision an iter-1 baseline pod via gateway live-tick path and capture all 4 signal families (D-04..D-07) concurrently.

Purpose: Repro cold-start flakiness with full instrumentation so plan 05 can rank suspects by evidence per D-08.
Output: 4 raw log files + one rollup md pointing to them.

**CRITICAL**: This plan spends Vast.ai $. autonomous=false. Operator MUST approve before Task 2 (provisioning) runs. Tasks 1 (env preflight) and 5 (destroy) are operator-safe but DO NOT skip operator approval at Task 2 boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-RUNTIME-ENV.md
@gateway/internal/primary/reconciler.go
@gateway/internal/primary/onstart.go
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: OPERATOR APPROVAL — provision iter-1 pod (≤$0.60 spend)</name>
  <what-built>
    Pre-flight check ready. Plan 02 captured runtime env (verify shape vars match MEMORY 2×3090 standing config: PRIMARY_VAST_NUM_GPUS_PRIMARY=2, allowlist=43803,55158, cap=0.60). About to provision a real Vast.ai pod via gateway live-tick.
  </what-built>
  <how-to-verify>
    1. Confirm 06.6.X-AUDIT-RUNTIME-ENV.md exists and shape vars are aligned with intended 2×3090 (or knowingly diverged — that itself is data for the audit).
    2. Confirm Vast.ai account balance ≥ $1.20 (cap for both iterations).
    3. Confirm vps-ifix-vm:/opt/ai-gateway-dev/ stack is up: `ssh vps-ifix-vm 'docker ps | grep ai-gateway-dev'`
    4. Approve iter-1 baseline run.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with provisioning, or describe blockers</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Trigger gateway live-tick provision; record lifecycle_id and Vast instance_id</name>
  <read_first>
    - gateway/internal/primary/reconciler.go lines 920-1020 (waitForReadyOrDestroy loop, 3-strike counters)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md (D-03 — gateway live-tick required, NOT vast-ai.sh)
  </read_first>
  <files>.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md</files>
  <action>
    SSH vps-ifix-vm and trigger the FSM to provision. The gateway control surface is gatewayctl (CONTEXT.md code-context section references it):
    ```
    ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary live-tick start 2>&1 | tee /tmp/06.6.X-iter1-tick.log'
    ```
    If that subcommand does not exist, fall back to: enable primary (via gatewayctl primary enable / SOAK gate clear) and trigger an evaluation tick. Read `gatewayctl --help` first to determine actual subcommand:
    ```
    ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl --help'
    ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary --help'
    ```

    Immediately after triggering (within 30s), capture lifecycle_id + Vast instance_id from the DB:
    ```
    ssh vps-ifix-vm 'docker exec ai-gateway-dev psql $DATABASE_URL -c "SELECT id, vast_instance_id, intended_status, actual_status, started_at FROM primary_pod_lifecycle ORDER BY id DESC LIMIT 1"'
    ```

    Write these IDs into the FIRST line of 06.6.X-ITER1-SIGNALS.md as a YAML block:
    ```
    iter: 1
    lifecycle_id: <id>
    vast_instance_id: <id>
    started_at: <ts>
    operator_approval_ts: <ts>
    ```

    DO NOT proceed to Task 3 until both IDs are recorded. If provisioning errors out before instance_id assigned, log the error and STOP — task 5 (destroy/cleanup) still runs.
  </action>
  <verify>
    <automated>grep -E '^lifecycle_id: [0-9]+' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md</automated>
  </verify>
  <acceptance_criteria>
    - 06.6.X-ITER1-SIGNALS.md has YAML header with non-zero lifecycle_id and vast_instance_id
    - Trigger path used gatewayctl (NOT vast-ai.sh) — record exact command used in doc
  </acceptance_criteria>
  <done>lifecycle_id + vast_instance_id captured</done>
</task>

<task type="auto">
  <name>Task 3: Parallel 4-signal capture for ~15 minutes (full cold-start window + 5min buffer)</name>
  <read_first>
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md (D-04..D-07 exact signals required)
    - ./CLAUDE.md "Vast.ai API Key" section (key in env VAST_AI_API_KEY)
  </read_first>
  <files>
    .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-vast-timeline.log,
    .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-pod-snapshot.log,
    .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-gateway-logs.log,
    .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-network-probe.log
  </files>
  <action>
    Launch 4 collectors in parallel (use background processes via Bash `run_in_background`). Each runs ~900s (15 min). Use vast_instance_id from Task 2.

    **Signal 1 — Vast-side (D-04):** Poll every 5s.
    ```
    while sleep 5; do echo "=== $(date -u +%FT%TZ) ==="; curl -sH "Authorization: Bearer $VAST_AI_API_KEY" "https://vast.ai/api/v0/instances/<vast_instance_id>" | jq '{actual_status,intended_status,status_msg,ports,ssh_host,ssh_port}'; done
    ```
    Redirect into iter1-vast-timeline.log. Stop after 900s OR when actual_status=running with port mapping, whichever first.

    **Signal 2 — Pod-side (D-05):** Once Vast exposes ssh_host:ssh_port, snapshot pod every 30s.
    ```
    POD_SSH="ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p <ssh_port> root@<ssh_host>"
    while sleep 30; do echo "=== $(date -u +%FT%TZ) ==="; $POD_SSH 'tail -50 /var/log/supervisord.log; echo ---onstart---; tail -50 /var/log/onstart.log; echo ---ps---; ps aux | head -30; echo ---weights---; ls -la /weights/qwen /weights/whisper /weights/bge-m3 2>&1; df -h /weights; echo ---dmesg---; dmesg | tail -20'; done
    ```
    Redirect into iter1-pod-snapshot.log. If pod ssh never becomes reachable, log "POD SSH UNREACHABLE for entire 900s window" and capture only what's available (Vast may still expose status_msg with extract errors).

    **Signal 3 — Gateway-side (D-06):**
    ```
    ssh vps-ifix-vm 'docker logs ai-gateway-dev --since=15m -f' > iter1-gateway-logs.log &
    # After 900s also append final DB row:
    ssh vps-ifix-vm 'docker exec ai-gateway-dev psql $DATABASE_URL -c "SELECT * FROM primary_pod_lifecycle WHERE id=<lifecycle_id>"' >> iter1-gateway-logs.log
    ```

    **Signal 4 — Network (D-07):** Once port mapping is visible in Vast response, probe externally.
    ```
    while sleep 10; do echo "=== $(date -u +%FT%TZ) ==="; for p in <mapped-8080> <mapped-5000> <mapped-8000> <mapped-9400>; do nc -zv <pod-ip> $p 2>&1; done; done
    ```
    If port mapping never appears, log "NO PORT MAPPING EVER ASSIGNED" with timestamp window — that IS the signal.

    All 4 collectors run in parallel. After 900s elapsed OR pod fully ready (4 ports reachable), stop all collectors. Update 06.6.X-ITER1-SIGNALS.md with a final section pointing to the 4 log files and a 1-paragraph rollup: "at T+<seconds>, actual_status=<x>, ports=<list>, pod-side reachable=Y/N, gateway closed lifecycle=Y/N (reason=<closure_reason>)".

    Per CLAUDE.md: no speculation in the rollup paragraph — only facts from the logs.
  </action>
  <verify>
    <automated>test -s .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-vast-timeline.log && test -s .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-gateway-logs.log && test -s .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter1-network-probe.log</automated>
  </verify>
  <acceptance_criteria>
    - 4 log files exist and are non-empty (pod-snapshot may be empty if pod ssh never came up — in that case the file must contain explicit "POD SSH UNREACHABLE" stub line)
    - 06.6.X-ITER1-SIGNALS.md rollup paragraph cites concrete timestamps + facts from logs (no "probably", no "likely")
    - lifecycle_id DB row captured in iter1-gateway-logs.log including closure_reason if closed
  </acceptance_criteria>
  <done>4 raw log files + signals rollup committed</done>
</task>

<task type="auto">
  <name>Task 4: Destroy iter-1 pod and log spend</name>
  <read_first>
    - ./CLAUDE.md "Vast.ai API Key" section
  </read_first>
  <files>.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md</files>
  <action>
    Destroy via gateway (preferred path, exercises same code as live ops):
    ```
    ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary destroy <lifecycle_id>'
    ```
    Or direct Vast API fallback:
    ```
    curl -X DELETE -H "Authorization: Bearer $VAST_AI_API_KEY" "https://vast.ai/api/v0/instances/<vast_instance_id>"
    ```
    Verify instance gone:
    ```
    curl -sH "Authorization: Bearer $VAST_AI_API_KEY" "https://vast.ai/api/v0/instances/<vast_instance_id>" | jq .
    ```

    Append to 06.6.X-ITER1-SIGNALS.md a `spend:` block with: hourly_rate (from Vast offer), elapsed_minutes, total_usd. Sum stays under $0.60 budget per iter.
  </action>
  <verify>
    <automated>grep -E '^spend:|^  total_usd:' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md</automated>
  </verify>
  <acceptance_criteria>
    - Vast instance confirmed destroyed (404 or actual_status=destroyed on GET)
    - spend block in signals doc with total_usd ≤ 0.60
  </acceptance_criteria>
  <done>Pod destroyed, spend logged</done>
</task>

</tasks>

<verification>
- 4 signal log files exist with timestamped entries
- lifecycle_id traceable from signals doc to DB row in iter1-gateway-logs.log
- Pod confirmed destroyed via Vast API GET
- spend ≤ $0.60
</verification>

<success_criteria>
Plan 05 has enough raw evidence across all 4 signal families to apply D-15 criterion (≥2 independent sources for any confirmed root cause).
</success_criteria>

<output>Create `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-04-SUMMARY.md` when done.</output>
