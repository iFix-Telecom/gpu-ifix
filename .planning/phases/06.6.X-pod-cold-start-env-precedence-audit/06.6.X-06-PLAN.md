---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 06
type: execute
wave: 4
depends_on: [06.6.X-05]
files_modified:
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-SIGNALS.md
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter2-vast-timeline.log
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter2-pod-snapshot.log
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter2-gateway-logs.log
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter2-network-probe.log
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-ANALYSIS.md
autonomous: false
requirements: [PC-COLD-START-FIX]

must_haves:
  truths:
    - "Iter-2 runs ONLY if plan 05 analysis recommended it (no confirmed root cause or top-1 hypothesis needs isolation test)"
    - "Iter-2 tests the specific variant defined in plan 05 (e.g., pre-baked whisper, or different image, or seeded weights)"
    - "Combined spend across iter-1+iter-2 ≤ $1.20 (D-02 budget cap)"
  artifacts:
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-SIGNALS.md"
      provides: "Iter-2 rollup (or skip-rationale if plan 05 said no iter-2)"
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-ANALYSIS.md"
      provides: "Iter-2 hypothesis verdict (confirmed/refuted/inconclusive)"
  key_links:
    - from: "06.6.X-ITER2-ANALYSIS.md"
      to: "06.6.X-RESEARCH-COLD-START.md (plan 08)"
      via: "either confirms root cause or downgrades top hypothesis"
      pattern: "hypothesis|root cause"
---

<objective>
Conditionally run iter-2 spike to test the top-1 hypothesis from plan 05.

Purpose: Per D-09, isolate the leading suspect via a targeted variant. Per D-02, hard stop at 2 iterations total.
Output: Either (a) 4 iter-2 logs + analysis, or (b) skip-rationale doc if plan 05 said no iter-2 needed.

**CRITICAL**: autonomous=false. Operator approval gate at Task 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: OPERATOR DECISION — iter-2 needed?</name>
  <what-built>
    Plan 05 ITER1-ANALYSIS produced verdicts. Either iter-1 evidence is sufficient (skip iter-2) or top-1 hypothesis needs isolation.
  </what-built>
  <how-to-verify>
    1. Read 06.6.X-ITER1-ANALYSIS.md "Iter-2 decision" section.
    2. If decision = NO ITER-2 NEEDED → respond "skip"; this plan writes a 1-paragraph skip-rationale into 06.6.X-ITER2-SIGNALS.md and 06.6.X-ITER2-ANALYSIS.md and exits.
    3. If decision = ITER-2 RECOMMENDED → confirm the proposed variant + estimated spend (≤ $0.60). Respond "approved <variant-summary>" or "skip" to halt diagnosis with iter-1 evidence only.
  </how-to-verify>
  <resume-signal>Type "approved" + variant name, or "skip" to write skip-rationale</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Either provision iter-2 with chosen variant OR write skip-rationale</name>
  <read_first>
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-ANALYSIS.md (iter-2 decision section)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-04-PLAN.md (signal collection mechanics — REUSE same 4-signal pattern)
  </read_first>
  <files>
    .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-SIGNALS.md,
    .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter2-vast-timeline.log,
    .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter2-pod-snapshot.log,
    .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter2-gateway-logs.log,
    .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/iter2-network-probe.log
  </files>
  <action>
    **If operator said "skip"**: Write 06.6.X-ITER2-SIGNALS.md and 06.6.X-ITER2-ANALYSIS.md with a single section "SKIPPED — rationale" quoting the iter-1 confirmed root cause + verdict source lines. No Vast spend. Done.

    **If operator approved a variant**: Apply the same 4-signal collection pattern as plan 04 Task 3 but with the variant config. Variant examples per D-09:
    - "pre-baked whisper" → use a one-off built image with /weights/whisper populated (build on vps-ifix-vm via `docker build` against an inline Dockerfile that COPYs whisper from MinIO, push as `:iter2-whisper-baked` tag, set PRIMARY_TEMPLATE_IMAGE to that tag temporarily, trigger gateway tick, REVERT image env after).
    - "weights mocked" → use mc cp to upload a stub weight to a test MinIO key, point onstart at it.
    - "fresh allowlist offer" → modify PRIMARY_VAST_OFFER_ALLOWLIST in container env temporarily to test specific offer ID.

    Whatever the variant, document the EXACT delta from iter-1 at the top of 06.6.X-ITER2-SIGNALS.md:
    ```
    iter: 2
    variant: <name>
    config_delta_from_iter1: <one-line diff>
    lifecycle_id: ...
    vast_instance_id: ...
    ```

    Then run the 4 parallel collectors (15-min window) identically to plan 04 Task 3. Destroy pod after collection. Append spend block.

    Then write 06.6.X-ITER2-ANALYSIS.md with sections:
    1. Hypothesis tested (verbatim from plan 05)
    2. Variant config
    3. Did the issue reproduce? (Yes/No with evidence cites)
    4. Verdict: HYPOTHESIS CONFIRMED | HYPOTHESIS REFUTED | INCONCLUSIVE (needs different variant — but per D-02 we STOP)

    Per CLAUDE.md: cite evidence, no speculation. Per D-02: hard stop. If iter-2 inconclusive, that fact goes into plan 08 RESEARCH as "remaining hypothesis".
  </action>
  <verify>
    <automated>test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-SIGNALS.md && test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-ANALYSIS.md && grep -E 'SKIPPED|HYPOTHESIS CONFIRMED|HYPOTHESIS REFUTED|INCONCLUSIVE' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER2-ANALYSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - Either skip-rationale OR full 4-signal capture + analysis
    - If provisioned: combined iter-1+iter-2 spend ≤ $1.20 (verify by summing spend blocks)
    - Verdict line present (SKIPPED | CONFIRMED | REFUTED | INCONCLUSIVE)
    - If provisioned: pod confirmed destroyed
  </acceptance_criteria>
  <done>Iter-2 path resolved (skipped or executed) with analysis verdict</done>
</task>

</tasks>

<verification>
- Decision honored (skip-rationale or 4 logs + analysis)
- Budget cap respected (combined spend ≤ $1.20)
- No third iteration attempted (D-02)
</verification>

<success_criteria>
Plan 08 RESEARCH-COLD-START has its final evidence set: either confirmed root cause or explicit remaining-hypothesis list.
</success_criteria>

<output>Create `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-06-SUMMARY.md` when done.</output>
