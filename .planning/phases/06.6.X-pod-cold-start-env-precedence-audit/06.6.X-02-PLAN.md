---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-RUNTIME-ENV.md
autonomous: true
requirements: [PC-ENV-PRECEDENCE]

must_haves:
  truths:
    - "Actual runtime values of all shape vars are captured from vps-ifix-vm:/opt/ai-gateway-dev/.env"
    - "Container view (docker exec env) is captured separately from .env file view"
    - "Any divergence between .env on disk and container env is flagged"
  artifacts:
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-RUNTIME-ENV.md"
      provides: "Runtime-side var capture for env precedence audit (intermediate)"
      contains: "PRIMARY_"
  key_links:
    - from: "06.6.X-AUDIT-RUNTIME-ENV.md"
      to: "06.6.X-RESEARCH-ENV-PRECEDENCE.md (plan 09)"
      via: "divergence table input — runtime column"
      pattern: "runtime"
---

<objective>
Capture the actual runtime env values for shape vars on vps-ifix-vm so plan 09 can compare runtime-vs-code-vs-docs.

Purpose: Operator-edited .env on dev VM is the ground truth for "what the gateway sees". Code defaults and docs may differ.
Output: 06.6.X-AUDIT-RUNTIME-ENV.md — intermediate artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSH vps-ifix-vm and capture runtime env from .env + docker exec env</name>
  <read_first>
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md (D-10 runtime audit section)
    - ./CLAUDE.md "Infra — Hetzner Dedicated" section (vps-ifix-vm SSH alias)
  </read_first>
  <files>.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-RUNTIME-ENV.md</files>
  <action>
    Run these commands; pipe outputs directly into the doc as fenced blocks (DO NOT summarize — capture verbatim for forensic value):

    ```
    ssh vps-ifix-vm 'grep -E "PRIMARY_.*(NUM_GPUS|GPU_NAME|PRICE|ALLOWLIST)" /opt/ai-gateway-dev/.env'
    ssh vps-ifix-vm 'docker exec ai-gateway-dev env | grep -E "PRIMARY_.*(NUM_GPUS|GPU_NAME|PRICE|ALLOWLIST)" | sort'
    ssh vps-ifix-vm 'ls -la /opt/ai-gateway-dev/.env /opt/ai-gateway-dev/docker-compose.yml; sha256sum /opt/ai-gateway-dev/.env'
    ssh vps-ifix-vm 'grep -E "PRIMARY_" /opt/ai-gateway-dev/docker-compose.yml | head -50'
    ```

    Also check Portainer-managed stack.env existence (CONTEXT.md D-10 mentions this path):
    ```
    ssh vps-ifix-vm 'ls /var/lib/docker/volumes/infra_portainer_data/_data/compose/ 2>/dev/null || echo "no portainer stacks"'
    ```

    In the doc, structure as 3 sections:
    1. **`.env` on disk** (raw output)
    2. **Container env** (docker exec env raw output)
    3. **docker-compose.yml passthrough** (grep result)
    4. **Divergence flags** — explicit YES/NO per var: does .env match container view? If NO → flag (this means compose.yml is filtering/overriding).

    Per CLAUDE.md communication rule: if a command returns nothing, write the exact command + "(no output)". Do NOT say "the var is not set" without showing the empty grep.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-RUNTIME-ENV.md && grep -c 'PRIMARY_' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-RUNTIME-ENV.md | awk '{ if ($1 < 4) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File 06.6.X-AUDIT-RUNTIME-ENV.md exists
    - Contains raw outputs (fenced blocks) of the 4 ssh commands above
    - Contains a divergence flag section comparing .env vs container env per shape var
    - Captures sha256 of .env (so any later edit by operator is detectable)
  </acceptance_criteria>
  <done>06.6.X-AUDIT-RUNTIME-ENV.md committed with verbatim ssh outputs and per-var divergence YES/NO column</done>
</task>

</tasks>

<verification>Doc exists, contains raw verbatim ssh outputs, divergence flags present.</verification>

<success_criteria>Runtime column of plan 09 divergence table can be populated directly from this doc.</success_criteria>

<output>Create `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-02-SUMMARY.md` when done.</output>
