---
phase: 06.6.X-pod-cold-start-env-precedence-audit
plan: 07
type: execute
wave: 5
depends_on: [06.6.X-01, 06.6.X-02, 06.6.X-03]
files_modified:
  - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md
autonomous: true
requirements: [PC-ENV-PRECEDENCE]

must_haves:
  truths:
    - "Divergence table covers all 4 shape var families (NUM_GPUS, GPU_NAME, PRICE_CAP, ALLOWLIST) with columns: runtime, prod-example, runbook-doc, code-read-location, divergence Y/N, impact"
    - "A canonical-var recommendation is given for each divergent var (which var name wins, why)"
    - "A deprecation plan exists for legacy aliases: hard fail-fast vs soft warn — with rationale"
    - "Proposed change to config.go:622-642 alias mapping is captured as a diff sketch (NOT applied as code edit per D-14)"
  artifacts:
    - path: ".planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md"
      provides: "Final RESEARCH doc for PC-ENV-PRECEDENCE; feeds Phase 6.6.Y fix planning"
      contains: "divergence|canonical|alias mapping"
  key_links:
    - from: "06.6.X-RESEARCH-ENV-PRECEDENCE.md"
      to: "Phase 6.6.Y (future)"
      via: "fix recommendation block"
      pattern: "canonical|deprecation"
---

<objective>
Synthesize the 3 audit inputs (code, runtime, docs) into the final RESEARCH-ENV-PRECEDENCE doc per D-11/D-13.

Purpose: This is one of the 2 phase deliverables. Plan 09 (cold-start RESEARCH) is the other.
Output: 06.6.X-RESEARCH-ENV-PRECEDENCE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-CODE-MAP.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-RUNTIME-ENV.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-DOCS-VALUES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build divergence table, canonical recommendation, deprecation plan</name>
  <read_first>
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-CONTEXT.md (D-10, D-11, D-13, D-14)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-CODE-MAP.md
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-RUNTIME-ENV.md
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-AUDIT-DOCS-VALUES.md
    - .planning/phases/11.2-readd-whisper-local-gemini-fallback/11.2-VERIFICATION.md (carry-forward evidence for the drift)
  </read_first>
  <files>.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md</files>
  <action>
    Sections:

    **1. Executive Summary** (~5 lines): named the drift (carry-forward item 3 from 11.2), what audit found in one sentence, recommended canonical var name, recommended deprecation severity (hard vs soft).

    **2. Divergence Table** (D-11 exact shape):

    | var | runtime-value (06.6.X-AUDIT-RUNTIME-ENV.md) | prod-example (.env.prod.example via 06.6.X-AUDIT-DOCS-VALUES.md) | runbook-doc (RUNBOOK-PRIMARY-POD.md via 06.6.X-AUDIT-DOCS-VALUES.md) | code-read-location (06.6.X-AUDIT-CODE-MAP.md) | divergence Y/N | impact |

    Build rows for all shape vars from AUDIT-CODE-MAP. Each cell cites the source doc + section. The `impact` column is concrete: "1×3090 provisioned instead of 2×3090 (11.2 evidence)".

    **3. Legacy Alias Analysis**: Quote the config.go:622-642 block. Per CONTEXT.md, `PRIMARY_NUM_GPUS` currently maps to Fallback slot. Discuss whether this is correct given operator intent (per MEMORY operator sets it expecting Primary). Recommend correct mapping target.

    **4. Canonical Var Recommendation** (per divergent var):
    - Which var name should be the single source of truth?
    - Strategy: keep + warn on legacy / hard fail-fast on legacy / rename canonical / introduce new precedence rule?

    **5. Deprecation Plan**: hard fail-fast vs soft warn. Cite tradeoffs (operator runbook break risk vs silent drift risk). Per D-13, recommend ONE option.

    **6. Proposed config.go Alias Mapping Change** (D-13): Diff sketch only — do NOT edit the file (D-14). Use ```diff fenced block showing the proposed change in `config.go:622-642`. Mark clearly: "FOR PHASE 6.6.Y IMPLEMENTATION, NOT THIS PHASE".

    **7. Open Questions for 6.6.Y**: Anything the audit could not resolve (e.g., "PRIMARY_VAST_OFFER_ALLOWLIST runtime value missing from .env — is this expected or another drift?").

    Per CLAUDE.md communication rule: every claim cites source artifact + section. Per D-15 (apply same evidence rigor to env audit): if a divergence's impact cannot be cited from runtime evidence (e.g., 11.2-VERIFICATION.md), mark impact as "INFERRED FROM CODE, NOT OBSERVED IN PROD".
  </action>
  <verify>
    <automated>test -f .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md && grep -cE 'Divergence|Canonical|Deprecation|Alias Mapping' .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md | awk '{ if ($1 < 4) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all 7 sections
    - Divergence table covers ≥4 shape vars × 6 columns
    - Canonical recommendation given per divergent var
    - Deprecation plan recommends ONE option (hard or soft) with rationale
    - config.go change shown as diff sketch ONLY (no code committed in gateway/)
    - Marker "FOR PHASE 6.6.Y IMPLEMENTATION" present
  </acceptance_criteria>
  <done>06.6.X-RESEARCH-ENV-PRECEDENCE.md committed; no source code edits</done>
</task>

</tasks>

<verification>
- Doc shape matches D-11/D-13
- No edits to gateway/ or pod/ (per D-14) — confirm via `git status gateway/ pod/`
</verification>

<success_criteria>
Phase 6.6.Y can be discussed and planned using this RESEARCH doc as the sole input for PC-ENV-PRECEDENCE.
</success_criteria>

<output>Create `.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-07-SUMMARY.md` when done.</output>
