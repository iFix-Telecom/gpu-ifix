---
phase: 06.6.X-pod-cold-start-env-precedence-audit
verified: 2026-06-09T18:30:00Z
status: passed
score: 7/7 truths verified
verdict: passed
spend_total_usd: 0.0399
spend_budget_cap_usd: 1.20
spend_under_budget: true
d14_compliance: true
pod_destruction_verified: true
inputs:
  - 06.6.X-MUST-HAVES.md (7 observable truths)
  - 06.6.X-RESEARCH-COLD-START.md (Plan 08)
  - 06.6.X-RESEARCH-ENV-PRECEDENCE.md (Plan 07)
  - 06.6.X-ITER1-SIGNALS.md (Plan 04 live capture; lifecycle 101; $0.0399)
  - 06.6.X-ITER2-ANALYSIS.md (Plan 06 SKIP; $0.00)
  - Vast API live query 2026-06-09T18:30Z (`?api_key=` corrected)
  - vps-ifix-vm gatewayctl primary state (live)
  - git log --since="3 days ago" -- gateway/ pod/
hand_off: Phase 6.6.Y discuss-phase (canonical inputs = both RESEARCH docs)
---

# Phase 06.6.X — VERIFICATION (Rollup)

**Phase goal (ROADMAP):** Diagnose 2 carry-forward items from Phase 11.2 (cold-start flakiness + env precedence drift) and deliver 2 RESEARCH docs. **NO code fixes** (D-14).

---

## 1. Phase Verdict

**`passed`**

Rationale: All 7 observable truths in `06.6.X-MUST-HAVES.md` hold; D-14 diagnose-only compliance proven (zero `gateway/` / `pod/` edits across all 9 plans); combined Vast spend $0.0399 ≪ phase budget cap $1.20.

The phase-verdict gate from MUST-HAVES.md §"Phase Verdict Rule" reads:

> `passed` IFF all 7 truths above hold AND D-14 compliance proven AND combined spend ≤ $1.20.

All three conditions met (see §2..§5 below).

---

## 2. Deliverables

| Deliverable | Artifact | One-line verdict |
|---|---|---|
| **PC-COLD-START-FIX** | `06.6.X-RESEARCH-COLD-START.md` | CONFIRMED root cause: Vast host `51096` (host_id `348505`, Brazil BR) reported `actual_status=running` + `status_msg=success` but **never bound public ports** (`ssh_host=null`, `ssh_port=null`, `public_ipaddr=192.168.1.8` RFC1918). 4-of-4 signal families converge. Recommended fix bundle = Option A (offer-side RFC1918 filter) + Option B (reconciler health-gate `PrimaryPublicPortBindBudgetSeconds=120s` fail-fast) for Phase 6.6.Y. |
| **PC-ENV-PRECEDENCE** | `06.6.X-RESEARCH-ENV-PRECEDENCE.md` | Drift confirmed: alias `PRIMARY_NUM_GPUS=2` is silently inert because compose default sets `PRIMARY_VAST_NUM_GPUS_FALLBACK=2`, killing the copy-on-empty guard at `config.go:608-642`. Recommended canonical: `PRIMARY_VAST_NUM_GPUS_{PRIMARY,FALLBACK}` / `PRIMARY_VAST_GPU_NAME_{PRIMARY,FALLBACK}` / `PRIMARY_VAST_PRICE_CAP_{PRIMARY,FALLBACK}`. Deprecation severity: **hard fail-fast at boot** (soft warn proven inert). Diff sketch provided for Phase 6.6.Y implementation. |

---

## 3. Observable Truths (per `06.6.X-MUST-HAVES.md`)

Independent verification — evidence cited per truth, not transcribed from plan SUMMARYs.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Operator can read `RESEARCH-COLD-START.md` and see CONFIRMED root cause backed by ≥2 signal families (D-15) OR ranked REMAINING HYPOTHESES | **PASS** | `06.6.X-RESEARCH-COLD-START.md` §4 "Root Cause Verdict" = `CONFIRMED ROOT CAUSE` backed by 4 signal families (Vast API, pod-side ssh, network probe, gateway silent-wait). §4 sub-table also lists 3 REMAINING HYPOTHESES (onstart timing, weights presence, supervisord boot order) with unresolved-because rationale. |
| 2 | Operator can read `RESEARCH-COLD-START.md` and find ≥2 fix options with tradeoff columns (image-size delta, cold-start delta, operator complexity) | **PASS** | `06.6.X-RESEARCH-COLD-START.md` §5 "Fix Options" table contains 5 options (A–E) — each row populates all 3 prescribed columns (image-size delta / cold-start delta / operator complexity) plus pros/cons. §6 ranks A+B bundled as recommended. |
| 3 | Operator can read `RESEARCH-ENV-PRECEDENCE.md` and see divergence table covering all 4 shape var families (NUM_GPUS, GPU_NAME, PRICE_CAP, ALLOWLIST) with code/runtime/docs columns | **PASS** | `06.6.X-RESEARCH-ENV-PRECEDENCE.md` §2 "Divergence Table" has 4 subsections §2.1–§2.4: NUM_GPUS, GPU_NAME, PRICE_CAP, ALLOWLIST. Each row contains the 8 prescribed columns (var / code-read / code-default / runtime / prod-example / runbook / divergence Y/N / impact) with file:line citations. |
| 4 | Operator can read `RESEARCH-ENV-PRECEDENCE.md` and see one recommended canonical-var + deprecation strategy with rationale | **PASS** | `06.6.X-RESEARCH-ENV-PRECEDENCE.md` §4 picks `PRIMARY_VAST_NUM_GPUS_{PRIMARY,FALLBACK}` family as canonical and §5 prescribes **HARD FAIL-FAST** at boot with explicit tradeoff matrix + rejection of soft-warn (proven inert by `iter1-gateway-logs.log` 18min capture showing zero deprecation slog.Warn lines). §6 includes Phase 6.6.Y diff sketch. |
| 5 | Operator can confirm no live Vast.ai pods from this phase remain billing (VERIFICATION lists instance_ids + destroyed evidence) | **PASS** | See §5 "Cost Containment" below. Vast API live query `2026-06-09T18:30Z` returned `{"instances": null}` for id `40271309` and `{"instances_found": 0, "instances": []}` for account-wide list. Gateway `primary state` returned `state=asleep`, `lifecycle_id=<empty>`. |
| 6 | Operator can confirm total Vast.ai spend ≤ $1.20 (D-02 budget cap) | **PASS** | See §4 "Spend Log" below. Combined spend = $0.0399 (iter-1 only; iter-2 SKIPPED). 96.7% of $1.20 budget unused. |
| 7 | Phase 6.6.Y discuss-phase has 2 RESEARCH docs as canonical inputs | **PASS** | Both `06.6.X-RESEARCH-COLD-START.md` and `06.6.X-RESEARCH-ENV-PRECEDENCE.md` exist, are committed, contain "Hand-off to Phase 6.6.Y" sections (§8 and §7 respectively) with single-paragraph specs ready to paste into `/gsd:discuss-phase 6.6.Y` prompt. |

**Score: 7/7 PASS.**

---

## 4. Spend Log

| Iter | Status | Spend (USD) | Source |
|---|---|---|---|
| iter-1 | live capture, lifecycle 101 destroyed | **$0.0399** | `06.6.X-ITER1-SIGNALS.md:56-65` (`0.1303 dph × 18.4min/60 = 0.0399`) |
| iter-2 | SKIPPED (top-1 hypothesis already at D-15 4-of-4) | **$0.0000** | `06.6.X-ITER2-ANALYSIS.md:9-22` |
| **TOTAL** | | **$0.0399** | |
| Phase budget cap | | $1.20 | `06.6.X-CONTEXT.md` D-02 |
| Budget remaining | | $1.1601 (96.7%) | |

**Verdict: PASS (under budget by $1.16).**

---

## 5. Cost Containment — Pod Destruction Evidence

All Vast instances spawned by this phase confirmed destroyed via three independent channels.

### Instances spawned during 06.6.X

| Instance ID | Lifecycle | Offer | Machine | Host | Started | Ended | Closure |
|---|---|---|---|---|---|---|---|
| `40271309` | `101` | `40097654` | `51096` | `348505` (Brazil BR) | 2026-06-09T16:28:36Z | 2026-06-09T16:47:00Z | `cancelled_in_flight` (operator force-down) |

### Per-instance destruction evidence (2026-06-09T18:30Z, ~1h45m post-destroy)

```
$ curl -sSL "https://console.vast.ai/api/v0/instances/40271309/?api_key=<key>"
{"instances": null}
```

`instances: null` = instance not present in account list (Vast API contract: destroyed instances disappear from `/instances/<id>/` after their cleanup window). ✅

### Account-wide sanity check

```
$ curl -sSL "https://console.vast.ai/api/v0/instances?api_key=<key>"
{"instances_found": 0, "instances": []}
```

Zero active instances on the account. ✅

### Gateway-side confirmation

```
$ ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary state'
KEY              VALUE
state            asleep
lifecycle_id
pod_url
pod_instance_id
entered_at       1781023620
```

Gateway FSM = `asleep`, no live `lifecycle_id`, no `pod_instance_id`. ✅

### Combined verdict

Three independent signals (per-instance API 404-equivalent, account-wide empty list, gateway FSM asleep) confirm no live pods from Phase 06.6.X remain billing. Cost containment verified.

---

## 6. D-14 Compliance — Diagnose-Only

`git status gateway/ pod/` on this worktree:

```
On branch develop
nothing to commit, working tree clean
```

`git log --since="3 days ago" -- gateway/ pod/` shows the most recent gateway/pod commits are from Phase 11.2 (commits `9e36ca4`, `f641dfb`, `58590fc`, etc.) — **zero** commits in the 06.6.X commit cluster (`12df74f` → `dd27bff`) touched `gateway/` or `pod/`.

The 06.6.X commit cluster (verified via `git log --all --oneline --grep="06.6.X"`):

```
dd27bff docs(06.6.X-08): complete plan 08 — RESEARCH-COLD-START synthesis
2de73a8 docs(06.6.X-08): synthesize iter-1 evidence into RESEARCH-COLD-START
27dd9ac docs(06.6.X-07): RESEARCH env precedence — divergence table + hard fail-fast plan
8343fbb docs(06.6.X-06): iter-2 SKIP rationale (top-1 already D-15 confirmed)
1eef0c7 chore(06.6.X): merge wave 3 plan 05 analysis
baf435b docs(06.6.X-05): iter-1 analysis — 1 confirmed RC, iter-2 SKIP
036c496 docs(06.6.X): mark plan 04 (wave 2) complete
1561081 docs(06.6.X-04): iter-1 cold-start + env-drift live signals (4 families)
779938f docs(06.6.X): mark wave 1 plans 01+02+03 complete
28418ee chore(06.6.X): merge wave 1 audits (01+02+03)
872bc13 docs(06.6.X-02): capture runtime env from vps-ifix-vm (audit)
0bf3b0a docs(06.6.X-03): docs-side shape var audit + 7 gaps
eb6f956 docs(06.6.X-01): code-side shape var audit + plan summary
3a768ae docs(06.6.X): commit phase plans + MUST-HAVES before execute
12df74f docs(06.6.X): capture phase context — diagnosis-first scope
```

Every subject line begins with `docs(...)` or `chore(...)`. No `feat(...)` / `fix(...)` commits in the cluster. D-14 verified.

---

## 7. Open Items for Phase 6.6.Y

Carry-over from both RESEARCH docs' Hand-off sections.

### From `06.6.X-RESEARCH-COLD-START.md` (§7 + §8)

1. **Host-specific vs universal port-binding failure** — iter-1 sampled exactly n=1 defective host (`host_id=348505`). Phase 6.6.Y discuss-phase MUST decide whether to run a cheap (~$0.30) iter against a different geo/host to disambiguate.
2. **`PrimaryPublicPortBindBudgetSeconds` default = 120s suggested but not validated** — Phase 6.6.Y should sample healthy cold-start `ssh_host` assignment times across 3-5 successful provisions to set a defensible default.
3. **Pre-bake vs onstart download for whisper** (Option C two flavors) — image size (+3 GB pulled every cold start) vs onstart wall-clock (+30-60s per cold start, masked by parallelism). Decision needed.
4. **Boot-time resolved-shape log dump** — fix-option-agnostic add-on; should land in same 6.6.Y wave so operator visibility lands alongside the fix.
5. **Vast API auth bug (`Bearer` vs `?api_key=`)** — hard requirement: all new iter scripts, monitoring probes, runbook commands MUST use `?api_key=` query-param against `/api/v0/instances/<id>/`. Carry as enforced rule in 6.6.Y planning.
6. **Suspects #1 (onstart timing) and #3 (supervisord boot order)** — defer to a follow-up UAT iter inside 6.6.Y once A+B land and a port-bound pod is reachable. UAT acceptance must capture `supervisorctl status` + `/var/log/onstart.log` + `/var/log/supervisord.log` snapshots to retire REMAINING HYPOTHESIS labels.
7. **`PrimaryProvisionColdStartBudgetSeconds` drift** (CONTEXT.md `<code_context>`) — not investigated in iter-1; capture in 6.6.Y if it surfaces.

### From `06.6.X-RESEARCH-ENV-PRECEDENCE.md` (§5 mitigation + §6 companion changes + §7)

1. **Operator-prep step required before deploying hard fail-fast** — SSH each host (vps-ifix-vm + any prod hosts), remove legacy `PRIMARY_NUM_GPUS` / `PRIMARY_GPU_NAME` / `PRIMARY_VAST_PRICE_CAP_DPH` lines from `stack.env`. Failing to do so = gateway crash-loop on first deploy.
2. **Same-PR companion changes in 6.6.Y** — `gateway/.env.prod.example` (drop 3 legacy rows, add 6 canonical), MEMORY `primary-gpu-shape-06.8-final.md` (rewrite), `gateway/docs/RUNBOOK-PRIMARY-POD.md` (replace `PRIMARY_VAST_PRICE_CAP_DPH=0.40` row at line 330 with 6 canonical-var rows; add "Shape pair model" subsection explaining shape 0 vs 1).
3. **`pod/scripts/vast-ai.sh:54`** has hardcoded `num_gpus:1` literal — out-of-scope for 6.6.Y main PR but should be parameterized in a separate 6.6.Y task.
4. **Test coverage** — `config_test.go` must assert `Load()` returns non-nil error when any of the 3 legacy vars is set + assert error message includes migration hint string.
5. **Allowlist contents must match the shape the reconciler tries FIRST** — once env-precedence is fixed (shape 0 demands 2×3090 instead of 1×3090), the existing allowlist `[43803, 55158]` will start matching. If still mismatched after fix, allowlist must be re-curated. Pair with cold-start Option D (hard-fail on allowlist empty for shape).

### Cross-doc coupling

The cold-start cascade (RESEARCH-COLD-START §4 cascading-finding) is **caused** by the env-precedence drift (RESEARCH-ENV-PRECEDENCE §2.4 allowlist row). Fixing env-precedence alone may eliminate the iter-1 reproducer (shape 0 would request 2×3090, allowlist `[43803, 55158]` would match, broaden-fallback would not trigger, defective host `51096` would never be picked). However, the cold-start fix (Option A+B) remains necessary as defence-in-depth because any future env drift or stale allowlist could re-trigger the broaden-fallback path.

---

## 8. STATE.md Update Sketch (for next STATE write — not done here)

```markdown
## 2026-06-09 — Phase 06.6.X CLOSED: passed

- Phase 06.6.X (pod-cold-start + env-precedence audit) verdict: **passed**
- Both PC-COLD-START-FIX and PC-ENV-PRECEDENCE delivered as RESEARCH docs (D-14 diagnose-only, zero gateway/pod edits)
- Iter-1 lifecycle 101 destroyed; iter-2 SKIPPED; total Vast spend $0.0399 / $1.20 cap
- Cold-start root cause CONFIRMED at D-15 4-of-4 family agreement: Vast host `51096` never bound public ports despite `actual_status=running`
- Env-precedence drift CONFIRMED: alias `PRIMARY_NUM_GPUS` silently inert due to compose default; hard fail-fast recommended for Phase 6.6.Y
- Next action: `/gsd:discuss-phase 6.6.Y` with both RESEARCH docs as canonical inputs (Option A+B cold-start fix + hard fail-fast env-precedence + companion docs/test changes)
```

---

## 9. Caveats / Notes

- **Plan 01 audit had a grep gap** (`06.6.X-AUDIT-CODE-MAP.md` reported `csvOr` at line 622-642; actual csvOr is at line 714 — verified via `grep -n 'func csvOr' gateway/internal/config/config.go`). Plan 05 ITER1-ANALYSIS labelled the CONTEXT D-10/D-11 alias-mapping premise REFUTED based on this hallucination. Plan 07 RESEARCH-ENV-PRECEDENCE §2.5 reverses the REFUTED verdict and re-confirms the alias mapping exists in HEAD code at `config.go:608-642`. This rollup carries the corrected verdict forward; Phase 6.6.Y planner should rely on the RESEARCH doc, not the Plan 01 audit, for the canonical code reference.
- **Vast API `Bearer` auth silently returns null fields** on `/api/v0/instances/<id>/` (does NOT 401). Three of four iter-1 collectors initially captured nulls before manual `?api_key=` mid-flight correction (`iter1-vast-timeline.log:316-318`). This VERIFICATION used `?api_key=` query-param throughout. Carry as 6.6.Y planning rule.
- **No edits to STATE.md or ROADMAP.md from this plan** (per Plan 09 objective). Operator/next workflow drives those updates.

---

*End — `06.6.X-VERIFICATION.md`*
