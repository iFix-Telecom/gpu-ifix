# 6.6.Y-06 — Deploy Evidence (fail-fast gateway image → ai-gateway-dev)

**Date:** 2026-06-11
**Stack:** `ai-gateway-dev`, Portainer `portainer3.ifixtelecom.com.br`, endpoint 3 (vps-ifix-vm / VM 102), stack id 34 (GitOps, repo `iFix-Telecom/gpu-ifix`)
**Operator:** Pedro (build/push on ops-claude + Portainer webhook) — orchestrator validated via SSH
**Precondition:** plan 6.6.Y-05 SAFE-TO-DEPLOY gate present (stack env migrated, legacy-free); compose with legacy rows REMOVED landed in plan 6.6.Y-04 (`393406b`) — GitOps re-pull delivers it on this deploy, resolving the empty-string legacy-row ressalva from 6.6.Y-05.
**Image deployed:** `ghcr.io/ifixtelecom/ifix-ai-gateway` (carries fail-fast from plan 02 + cold-start from plan 03 + Option C whisper 3-way onstart from Task 1 here, commits `f51e917` + gofmt `cb3bdec`).

---

## 1. Rollback target (recorded BEFORE deploy — finding #2 / T-6.6.Y-06-03)

- **Previous image:** `ghcr.io/ifixtelecom/ifix-ai-gateway:latest-dev` @ `sha256:e10b0e96e527bcd7342225ca27ca3fb529b366faa48e18445bddbb405f09d449`
- **Rollback procedure:** re-pin/retag the previous digest above + re-trigger the GitOps stack webhook (`POST /api/stacks/webhooks/<stack-webhook-id>` on portainer3) OR Portainer UI pull-and-redeploy with the pinned digest.
- **Rollback trigger criteria:** `ErrLegacyPrimaryEnv` in logs on boot **OR** boot-shape dump shows the wrong shape **OR** container fails health after recreate. On any trigger → re-pin previous digest, redeploy, return to plan 6.6.Y-05 to fix the env. Do NOT proceed to UAT on a dirty boot.

## 2. Build + push (GHCR GHA push BROKEN org-wide since 2026-06-09 — PAT-retag fallback used — finding #2 / T-6.6.Y-06-01)

- **GHA run 27365223797** (sha `72edfe0`): unit-test flake `TestHandleForceUpRequest_TransitionsAsleepToProvisioning` (failed first run, passed on rerun — pre-existing CI flake, also seen 2026-06-09 on sha `8d676b8c`), then the **gofmt gate** caught unformatted `gateway/internal/config/config.go` carried from plan 02.
- **gofmt fixed** + committed (`style(6.6.Y-06)`) + pushed as `cb3bdec`.
- **GHA run 27366113646** (sha `cb3bdec`): tests + integration GREEN; the **"Build & push gateway image"** step FAILED with `denied: permission_denied: The requested installation does not exist` — the known org-wide GHCR break (memory `ghcr-actions-install-broken`). A green test/integration run is NOT proof the image landed.
- **Fallback executed:** local `docker build` on ops-claude from `cb3bdec` (`gateway/Dockerfile`, `GATEWAY_VERSION=develop-cb3bdec`), tagged `develop` / `develop-cb3bdec` / `latest-dev`, pushed with the GitHub PAT (`~/.git-credentials` on the trusted host — PAT exposure disposition `accept`, T-6.6.Y-06-04).
- **Pushed manifest digest (all 3 tags):** `sha256:02bcb7b59d6eee03af11167a75419df98214f70b4d9187f8889f8ef61f53afa4`

## 3. Digest verification (3-way match — finding #2 / T-6.6.Y-06-01)

Verification commands: `docker inspect --format '{{index .RepoDigests 0}}' <local-image>` for the GHCR-landed digest, and `ssh vps-ifix-vm "docker inspect ai-gateway-dev --format '{{.Image}}'"` + `RepoDigests` for the running container.

| Source | Digest | Result |
|--------|--------|--------|
| Local built image ID | `sha256:ead1bc1618ad12f6c2f2a15dbd962e4f30eb4cbccb68323436bf0fd5ada02d7b` | — |
| GHCR landed manifest digest | `sha256:02bcb7b59d6eee03af11167a75419df98214f70b4d9187f8889f8ef61f53afa4` | reference |
| Running container `.Image` (vps-ifix-vm) | `sha256:ead1bc1618ad…` | matches local build ID |
| Running container RepoDigests | `ghcr.io/ifixtelecom/ifix-ai-gateway@sha256:02bcb7b59d6e…` | **MATCH** GHCR |

→ Built image == GHCR-landed digest == running-container repo digest. **No stale-image trap.**

## 4. Redeploy on the migrated stack (Portainer compose path 34)

- **Trigger:** Portainer stack webhook (stack `ai-gateway-dev` id=34, GitOps) → **HTTP 204**; re-pulled the repo (compose with legacy rows REMOVED, plan 04) + image `latest-dev`.
- **Container recreated:** `started=2026-06-11T19:37:22Z`, `health=healthy`.
- **Legacy env vars in container:** **0** (grep count 0 — the empty-string compose-row ressalva from plan 05 is RESOLVED by the plan-04 compose the GitOps re-pull delivered).

## 5. Clean boot confirmation (no ErrLegacyPrimaryEnv — finding #2 / T-6.6.Y-06-03)

- **`ErrLegacyPrimaryEnv` occurrences in logs:** **0** — CLEAN BOOT. The fail-fast image (plan 02, trips on *presence* via `os.LookupEnv`, even empty string) booted without tripping → confirms the deployed compose carries no legacy rows.

### Boot-shape log (verbatim, 2026-06-11T16:37:28.918-03:00 — plan 02 dump)

```
time=2026-06-11T16:37:28.918-03:00 level=INFO msg="Phase 6.6 primary reconciler started" module=GATEWAY replica_id=c2e568ab491c schedule_disabled=true next_transition=2026-06-11T22:00:00-03:00 next_kind=down tz=America/Sao_Paulo up_hour=8 down_hour=22 provision_lead_seconds=1800 grace_ramp_down_seconds=300 coldstart_budget_seconds=2400 failure_cooldown_seconds=300 shape0_num_gpus=1 shape0_gpu="RTX 3090" shape0_cap=0.3 shape1_num_gpus=2 shape1_gpu="RTX 3090" shape1_cap=0.6 allowlist="[43803 55158]" port_bind_budget_seconds=120 reject_private_ip=true monthly_budget_brl=2400 template_image=ghcr.io/ifixtelecom/converseai-primary-pod:develop
```

Canonical shape matrix confirmed against the dump:
- **shape0 (PRIMARY)** = `num_gpus=1`, `gpu="RTX 3090"`, `cap=0.3` ✓
- **shape1 (FALLBACK / standing 2×3090)** = `num_gpus=2`, `gpu="RTX 3090"`, `cap=0.6` ✓ — the standing 2×3090 config is the FALLBACK slot (matches plan 6.6.Y-01 single source of truth)
- `allowlist="[43803 55158]"` ✓
- `port_bind_budget_seconds=120` ✓

## 6. onstart Option C (whisper 3-way) — code reference

- Task 1 wired the third parallel weight download into `gateway/internal/primary/onstart.go` (commit `f51e917`). `grep -c 'WHISPER_PID' gateway/internal/primary/onstart.go` → **2** (PID assign + `wait`). bge-m3 KEPT (finding #3); whisper guards restored; whisper tarball extracted to `/weights/whisper`. Length-regression test (`TestPrimaryOnstartLengthBelowLimit`, <14KB) green. This is what makes the weights stage reachable for the live UAT (plan 6.6.Y-07).

## 7. Spend

- **Vast spend this plan:** **$0.00** — no GPU provisioned (provisioning happens in the live UAT, plan 6.6.Y-07).

---

## GATE: READY FOR LIVE UAT (plan 6.6.Y-07)

Fail-fast + cold-start + Option C image built, PAT-pushed to GHCR (GHA GHCR push broken), 3-way digest-verified (built == GHCR == running container), rollback path recorded, deployed on the migrated legacy-free stack, booting CLEAN (no `ErrLegacyPrimaryEnv`) with the resolved shape dump (shape1=2×3090 FALLBACK + shape0=1×3090 PRIMARY + allowlist + port_bind_budget=120). The weights stage is reachable. **READY FOR LIVE UAT (plan 6.6.Y-07).**
