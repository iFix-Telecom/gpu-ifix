---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 04
type: execute
wave: 2
depends_on: [6.6.Y-01]
files_modified:
  - gateway/docker-compose.yml
  - gateway/.env.prod.example
  - gateway/docs/RUNBOOK-PRIMARY-POD.md
  - /home/pedro/.claude/projects/-home-pedro-projetos-pedro-gpu-ifix/memory/primary-gpu-shape-06.8-final.md
  - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md
autonomous: true
requirements: [PC-ENV-PRECEDENCE, PC-COLD-START-FIX]

must_haves:
  truths:
    - "No legacy primary-shape var (PRIMARY_NUM_GPUS, PRIMARY_GPU_NAME, PRIMARY_VAST_PRICE_CAP_DPH) appears in docker-compose.yml, .env.prod.example, RUNBOOK-PRIMARY-POD.md, or the MEMORY file"
    - "The 6 canonical per-shape vars + the 2 new cold-start vars are documented in .env.prod.example and the runbook env table"
    - "The unprefixed emerg VAST_PRICE_CAP_DPH=0.40 in .env.prod.example is preserved (different subsystem)"
    - "Allowlist machines 43803/55158 have been verified against live Vast inventory and the result documented"
  artifacts:
    - path: "gateway/docker-compose.yml"
      provides: "Legacy alias rows removed; canonical rows + new cold-start vars present"
      contains: "PRIMARY_VAST_REJECT_PRIVATE_IP"
    - path: "gateway/.env.prod.example"
      provides: "Canonical + cold-start vars; legacy rows deleted; emerg VAST_PRICE_CAP_DPH preserved"
      contains: "PRIMARY_VAST_NUM_GPUS_PRIMARY"
    - path: ".planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md"
      provides: "Live Vast verification of allowlist 43803/55158 + correction-or-document outcome"
      min_lines: 15
  key_links:
    - from: "gateway/.env.prod.example"
      to: "operator stack.env migration (plan 6.6.Y-05)"
      via: "canonical var names are the migration target"
      pattern: "PRIMARY_VAST_NUM_GPUS_PRIMARY"
---

<objective>
Land all NON-CODE surface changes for the canonical migration (RESEARCH-ENV-PRECEDENCE §5 mitigation list + §6 companion changes 3-7) and the allowlist refresh (locked from RESEARCH). This plan owns docs/config surfaces only — zero overlap with the code plans (02/03) — so it runs parallel to 03 in W2.

1. **docker-compose.yml** — delete the 3 legacy-alias rows + the "Legacy deprecated-alias reads" comment; keep canonical rows + allowlist/blocklist; ADD the 2 new cold-start vars.
2. **.env.prod.example** — delete the 3 legacy rows; ADD the 6 canonical rows + 2 cold-start rows; PRESERVE the emerg `VAST_PRICE_CAP_DPH=0.40` (grep-confirmed config.go:427 — NOT a primary legacy alias).
3. **RUNBOOK-PRIMARY-POD.md** — replace the legacy cap row + narrative with the 6 canonical rows + 2 cold-start rows + a "Shape pair model" subsection + the public_port_bind_timeout closure_reason.
4. **MEMORY primary-gpu-shape-06.8-final.md** — rewrite to teach canonical names; drop the stale caveat.
5. **Allowlist refresh** — verify machines 43803/55158 still offer 2×3090 via live Vast API (`?api_key=` query-param HARD RULE); document outcome + recommend resolution of the shape-0/allowlist mismatch.

Purpose: make operator-facing surfaces teach ONLY canonical names so the hard fail-fast (plan 02) doesn't strand operators, and resolve the allowlist cascade behind the iter-1 root cause.
Output: 4 edited surfaces + ALLOWLIST-REFRESH.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md

<interfaces>
<!-- docker-compose.yml current state (extracted) -->
:108-113 canonical block (PRIMARY_VAST_GPU_NAME_{PRIMARY,FALLBACK}, _PRICE_CAP_{PRIMARY,FALLBACK}, _NUM_GPUS_{PRIMARY,FALLBACK} with :-defaults).
:114 comment "# Legacy deprecated-alias reads (warn-and-use until Phase 11.2 cleanup)".
:115 PRIMARY_GPU_NAME, :116 PRIMARY_NUM_GPUS, :117 PRIMARY_VAST_PRICE_CAP_DPH (all `${VAR:-}` empty defaults) — DELETE these 4 lines.
:118-119 PRIMARY_VAST_MACHINE_ALLOWLIST / BLOCKLIST — KEEP.
New cold-start rows to ADD: `PRIMARY_VAST_REJECT_PRIVATE_IP: ${PRIMARY_VAST_REJECT_PRIVATE_IP:-true}`, `PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS: ${PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS:-120}`.

<!-- .env.prod.example current state (extracted) -->
:119 VAST_PRICE_CAP_DPH=0.40   (emerg — KEEP)
:120 PRIMARY_VAST_PRICE_CAP_DPH=0.60   (legacy — DELETE)
:125 PRIMARY_NUM_GPUS=2   (legacy — DELETE)
:126 PRIMARY_GPU_NAME=RTX 3090   (legacy — DELETE)
:127 PRIMARY_VAST_MACHINE_ALLOWLIST=43803,55158   (KEEP)
:139 PRIMARY_VAST_MACHINE_BLOCKLIST=...   (KEEP)
Canonical rows to ADD: PRIMARY_VAST_NUM_GPUS_PRIMARY=1, PRIMARY_VAST_NUM_GPUS_FALLBACK=2, PRIMARY_VAST_GPU_NAME_PRIMARY=RTX 3090, PRIMARY_VAST_GPU_NAME_FALLBACK=RTX 3090, PRIMARY_VAST_PRICE_CAP_PRIMARY=0.30, PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60. Cold-start rows: PRIMARY_VAST_REJECT_PRIVATE_IP=true, PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS=120.

<!-- RUNBOOK-PRIMARY-POD.md (extracted) -->
:330 `| PRIMARY_VAST_PRICE_CAP_DPH | float | 0.40 | RTX 4090 price cap ...` — REPLACE.
:875,878 DPH cap=0.40 narrative — REPLACE with per-shape caps.

<!-- Vast API auth HARD RULE -->
?api_key=<key> query-param against /api/v0/bundles/ (offer search). Authorization: Bearer silently nulls fields.
Allowlist machines: 43803, 55158 (standing 2×3090 config per MEMORY primary-gpu-shape-06.8-final).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy vars + add canonical & cold-start vars across compose, env.prod.example, runbook, MEMORY</name>
  <files>gateway/docker-compose.yml, gateway/.env.prod.example, gateway/docs/RUNBOOK-PRIMARY-POD.md, /home/pedro/.claude/projects/-home-pedro-projetos-pedro-gpu-ifix/memory/primary-gpu-shape-06.8-final.md</files>
  <read_first>
    - gateway/docker-compose.yml (lines 108-119 region)
    - gateway/.env.prod.example (lines 115-140 region — note :119 emerg var to PRESERVE)
    - gateway/docs/RUNBOOK-PRIMARY-POD.md (lines 320-335 env table + 870-880 cap narrative)
    - /home/pedro/.claude/projects/-home-pedro-projetos-pedro-gpu-ifix/memory/primary-gpu-shape-06.8-final.md (full file)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md (§5 mitigation list, §6 companion changes 3-7)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md (docker-compose / .env.prod.example section)
  </read_first>
  <action>
    docker-compose.yml: delete the comment "# Legacy deprecated-alias reads ..." and the 3 rows PRIMARY_GPU_NAME / PRIMARY_NUM_GPUS / PRIMARY_VAST_PRICE_CAP_DPH. Keep the canonical block + allowlist/blocklist. Add after the canonical block: `PRIMARY_VAST_REJECT_PRIVATE_IP: ${PRIMARY_VAST_REJECT_PRIVATE_IP:-true}` and `PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS: ${PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS:-120}`. Per RESEARCH §7 Q4 keep the canonical `:-` defaults (do NOT drop them).
    .env.prod.example: delete the 3 legacy rows (PRIMARY_VAST_PRICE_CAP_DPH=0.60, PRIMARY_NUM_GPUS=2, PRIMARY_GPU_NAME=RTX 3090). PRESERVE the emerg `VAST_PRICE_CAP_DPH=0.40` line. Add the 6 canonical rows (NUM_GPUS_PRIMARY=1, NUM_GPUS_FALLBACK=2, GPU_NAME_PRIMARY=RTX 3090, GPU_NAME_FALLBACK=RTX 3090, PRICE_CAP_PRIMARY=0.30, PRICE_CAP_FALLBACK=0.60) + the 2 cold-start rows (PRIMARY_VAST_REJECT_PRIVATE_IP=true, PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS=120). Add a one-line comment that legacy PRIMARY_NUM_GPUS/PRIMARY_GPU_NAME/PRIMARY_VAST_PRICE_CAP_DPH now HARD-FAIL gateway boot (plan 6.6.Y-02) — do not re-add them.
    RUNBOOK-PRIMARY-POD.md: replace the PRIMARY_VAST_PRICE_CAP_DPH env-table row with 6 canonical-var rows (name|type|default|meaning|required) + 2 cold-start var rows; replace the :875/:878 0.40 narrative with the per-shape caps (0.30 primary / 0.60 fallback). Add a "## Shape pair model" subsection: shape 0 = primary (PRIMARY_VAST_*_PRIMARY, single GPU, searched first); shape 1 = fallback (PRIMARY_VAST_*_FALLBACK, dual GPU, searched when shape-0 returns zero qualified offers); document the public_port_bind_timeout closure_reason and that legacy vars now hard-fail boot.
    MEMORY primary-gpu-shape-06.8-final.md: rewrite env-var guidance to teach PRIMARY_VAST_NUM_GPUS_FALLBACK=2 / PRIMARY_VAST_GPU_NAME_FALLBACK=RTX 3090 / PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60 (2×3090 standing config = the FALLBACK shape) instead of legacy PRIMARY_NUM_GPUS; drop any "compose default 0.40 outdated" caveat; add a note that legacy vars now hard-fail gateway boot so the pre-primary-UAT env check must use canonical names.
  </action>
  <verify>
    <automated>! grep -Eq 'PRIMARY_NUM_GPUS|PRIMARY_GPU_NAME|PRIMARY_VAST_PRICE_CAP_DPH' gateway/docker-compose.yml && grep -q 'PRIMARY_VAST_REJECT_PRIVATE_IP' gateway/docker-compose.yml && grep -q '^VAST_PRICE_CAP_DPH=0.40' gateway/.env.prod.example && grep -q 'PRIMARY_VAST_NUM_GPUS_PRIMARY' gateway/.env.prod.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec 'PRIMARY_NUM_GPUS|PRIMARY_GPU_NAME|PRIMARY_VAST_PRICE_CAP_DPH' gateway/docker-compose.yml` returns 0.
    - `grep -c 'PRIMARY_VAST_REJECT_PRIVATE_IP' gateway/docker-compose.yml` returns ≥1.
    - `grep -c '^VAST_PRICE_CAP_DPH=0.40' gateway/.env.prod.example` returns 1 (emerg var preserved).
    - `grep -Ec '^PRIMARY_NUM_GPUS=|^PRIMARY_GPU_NAME=|^PRIMARY_VAST_PRICE_CAP_DPH=' gateway/.env.prod.example` returns 0 (legacy rows gone).
    - `grep -c 'PRIMARY_VAST_NUM_GPUS_PRIMARY' gateway/.env.prod.example` returns ≥1.
    - `grep -vE '^#' gateway/docs/RUNBOOK-PRIMARY-POD.md | grep -Ec 'PRIMARY_NUM_GPUS|PRIMARY_GPU_NAME|PRIMARY_VAST_PRICE_CAP_DPH'` returns 0 in non-comment lines (legacy not taught); and the runbook contains "PRIMARY_VAST_NUM_GPUS_FALLBACK" and "public_port_bind_timeout".
    - The MEMORY file contains "PRIMARY_VAST_NUM_GPUS_FALLBACK" and no longer instructs setting "PRIMARY_NUM_GPUS".
  </acceptance_criteria>
  <done>All 4 surfaces teach only canonical + cold-start vars; emerg VAST_PRICE_CAP_DPH preserved; legacy primary vars deleted everywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Allowlist refresh — verify 43803/55158 against live Vast + document shape-0 mismatch resolution</name>
  <read_first>
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md (any Option D / universal-failure directive)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md (§2.4 allowlist cascade impact + §7 Q1)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md (reconciler allowlist-refresh section)
  </read_first>
  <action>
    Query the live Vast offer inventory for machines 43803 and 55158 using the `?api_key=<key>` query-param HARD RULE (key from ~/.claude/CLAUDE.md or vps-ifix-vm stack.env): hit /api/v0/bundles/ with a filter for each machine_id and record whether each still lists a 2×RTX 3090 offer (and its dph, reliability, geo). Then author .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md with:
    - **Verification result:** per-machine — still offers 2×3090? dph/reliability/geo? retired?
    - **Shape-0 mismatch analysis:** shape 0 demands num_gpus=1 but allowlist hosts are 2×3090-only → shape-0 allowlist pass returns zero → broaden cascade (the iter-1 root-cause feeder). State the chosen resolution: (a) populate the allowlist with 1×3090-capable machine_ids for shape 0, OR (b) document that shape-0 broadening is expected and Option A+B (plan 6.6.Y-03) now make broadening safe, OR (c) apply the SPIKE-EVIDENCE Option D allowlist hard-fail directive if 6.6.Y-01 found universal failure.
    - **Allowlist correction (if any):** if a machine retired, propose the corrected PRIMARY_VAST_MACHINE_ALLOWLIST value for stack.env (plan 6.6.Y-05 applies it) and note it in .env.prod.example.
    - **Spend:** this verification is a read-only API query — $0 Vast spend (no provisioning). Record $0.00 against the phase cap.
    This task does NOT provision a pod; it is an API read only.
  </action>
  <verify>
    <automated>test -f .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md && grep -Eq '43803|55158' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md && grep -Eqi 'shape.?0|num_gpus' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md</automated>
  </verify>
  <acceptance_criteria>
    - ALLOWLIST-REFRESH.md exists and states, per machine 43803 and 55158, whether it still offers 2×3090 (with dph/geo or "retired").
    - The file names exactly one resolution (a/b/c) for the shape-0 allowlist mismatch.
    - The file records $0.00 Vast spend (read-only API query, no provisioning).
  </acceptance_criteria>
  <done>Allowlist verified against live Vast; shape-0 mismatch resolution chosen and documented; zero Vast spend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → Vast API | VAST_AI_API_KEY in `?api_key=` query-param for the allowlist verification |
| .env.prod.example → operator deploys | the example file is copied by operators; wrong content propagates the drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.6.Y-04-01 | Information disclosure | VAST_AI_API_KEY in `?api_key=` query-param during allowlist verification | accept | Read-only API query from trusted host; Vast HARD RULE mandates query-param (Bearer nulls fields); rotation available per CLAUDE.md |
| T-6.6.Y-04-02 | Tampering | .env.prod.example still teaching legacy vars → operator copies it → hard-fail crash-loop on deploy | mitigate | This plan deletes legacy rows from the example and adds a hard-fail note; verify grep asserts legacy rows absent |
| T-6.6.Y-04-03 | Information disclosure | secrets accidentally committed in the example file | mitigate | VAST_AI_API_KEY value stays `<from ~/.claude/CLAUDE.md>` placeholder in .env.prod.example; no literal secret added |
| T-6.6.Y-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; doc/config edits + read-only API query only |
</threat_model>

<verification>
- Legacy primary vars absent from compose, .env.prod.example (non-comment), runbook (non-comment), MEMORY.
- Canonical + 2 cold-start vars present in compose + .env.prod.example.
- Emerg VAST_PRICE_CAP_DPH=0.40 preserved.
- ALLOWLIST-REFRESH.md documents 43803/55158 live status + shape-0 resolution + $0 spend.
</verification>

<success_criteria>
All operator-facing surfaces teach only canonical + cold-start vars (legacy deleted, emerg preserved); allowlist verified live with a documented shape-0 mismatch resolution and zero Vast spend.
</success_criteria>

<output>
Create `.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-SUMMARY.md` when done.
</output>
