---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 04
type: execute
wave: 2
depends_on: [6.6.Y-01]
files_modified:
  - gateway/docker-compose.yml
  - gateway/.env.prod.example
  - gateway/docs/RUNBOOK-PRIMARY-POD.md
  - gateway/docs/MEMORY-primary-gpu-shape.md
  - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md
autonomous: false
requirements: [PC-ENV-PRECEDENCE, PC-COLD-START-FIX]
user_setup:
  - service: vast.ai
    why: "Allowlist refresh (Task 3) queries live Vast offer inventory — needs VAST_AI_API_KEY + network; not autonomous under workspace/secret constraints (review finding #5)"
    env_vars:
      - name: VAST_AI_API_KEY
        source: "~/.claude/CLAUDE.md or vps-ifix-vm stack.env"

must_haves:
  truths:
    - "No legacy primary-shape var (PRIMARY_NUM_GPUS, PRIMARY_GPU_NAME, PRIMARY_VAST_PRICE_CAP_DPH) appears in docker-compose.yml, .env.prod.example, RUNBOOK-PRIMARY-POD.md, or the repo-local MEMORY mirror"
    - "The 6 canonical per-shape vars + the 2 new cold-start vars are documented in .env.prod.example and the runbook env table, matching the canonical shape matrix (shape0 PRIMARY 1×3090@0.30, shape1 FALLBACK 2×3090@0.60)"
    - "The unprefixed emerg VAST_PRICE_CAP_DPH=0.40 in .env.prod.example is preserved (different subsystem)"
    - "Allowlist machines 43803/55158 have been verified against live Vast inventory and the result documented, with a PROACTIVE shape-0 resolution chosen (not merely 'broadening is safe')"
  artifacts:
    - path: "gateway/docker-compose.yml"
      provides: "Legacy alias rows removed; canonical rows + new cold-start vars present"
      contains: "PRIMARY_VAST_REJECT_PRIVATE_IP"
    - path: "gateway/.env.prod.example"
      provides: "Canonical + cold-start vars; legacy rows deleted; emerg VAST_PRICE_CAP_DPH preserved"
      contains: "PRIMARY_VAST_NUM_GPUS_PRIMARY"
    - path: ".planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md"
      provides: "Live Vast verification of allowlist 43803/55158 + PROACTIVE shape-0 resolution"
      min_lines: 15
  key_links:
    - from: "gateway/.env.prod.example"
      to: "operator stack.env migration (plan 6.6.Y-05)"
      via: "canonical var names are the migration target"
      pattern: "PRIMARY_VAST_NUM_GPUS_PRIMARY"
---

<objective>
Land all NON-CODE surface changes for the canonical migration (RESEARCH-ENV-PRECEDENCE §5 mitigation list + §6 companion changes 3-7) and the allowlist refresh (locked from RESEARCH). This plan owns docs/config surfaces only — zero overlap with the code plans (02/03) — so it runs parallel to 03 in W2.

**Autonomy (review finding #5):** This plan is now `autonomous: false`. Task 1 (repo doc/env edits) and Task 2 (repo-local MEMORY mirror) are autonomous repo edits, but Task 3 (live allowlist refresh) needs VAST_AI_API_KEY + network and is operator-gated. The original plan also edited the external MEMORY file at `/home/pedro/.claude/projects/.../memory/primary-gpu-shape-06.8-final.md` which is OUTSIDE the repo writable root — that edit is replaced by a repo-local mirror `gateway/docs/MEMORY-primary-gpu-shape.md` (autonomous, in-repo) plus an operator note to sync the external MEMORY file manually.

1. **docker-compose.yml** — delete the 3 legacy-alias rows + the "Legacy deprecated-alias reads" comment; keep canonical rows + allowlist/blocklist; ADD the 2 new cold-start vars.
2. **.env.prod.example** — delete the 3 legacy rows; ADD the 6 canonical rows + 2 cold-start rows matching the canonical shape matrix; PRESERVE the emerg `VAST_PRICE_CAP_DPH=0.40` (grep-confirmed config.go:427 — NOT a primary legacy alias).
3. **RUNBOOK-PRIMARY-POD.md** — replace the legacy cap row + narrative with the 6 canonical rows + 2 cold-start rows + a "Shape pair model" subsection (shape0 PRIMARY 1×3090@0.30, shape1 FALLBACK 2×3090@0.60) + the public_port_bind_timeout closure_reason.
4. **gateway/docs/MEMORY-primary-gpu-shape.md (repo-local mirror)** — author to teach canonical names; drop the stale caveat; plus an operator note to manually sync the external `~/.claude/.../memory/primary-gpu-shape-06.8-final.md`.
5. **Allowlist refresh** — verify machines 43803/55158 still offer 2×3090 via live Vast API (`?api_key=` query-param HARD RULE); document outcome + choose a PROACTIVE resolution of the shape-0/allowlist mismatch.

Purpose: make operator-facing surfaces teach ONLY canonical names so the hard fail-fast (plan 02) doesn't strand operators, and resolve the allowlist cascade behind the iter-1 root cause.
Output: 3 edited repo surfaces + 1 repo-local MEMORY mirror + ALLOWLIST-REFRESH.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md

<canonical_shape_matrix>
<!-- SINGLE SOURCE OF TRUTH — see plan 6.6.Y-01 <canonical_shape_matrix> for the full table. -->
Shape 0 PRIMARY  = 1 x RTX 3090 @ 0.30 -> PRIMARY_VAST_NUM_GPUS_PRIMARY=1,  PRIMARY_VAST_GPU_NAME_PRIMARY="RTX 3090",  PRIMARY_VAST_PRICE_CAP_PRIMARY=0.30  (searched first)
Shape 1 FALLBACK = 2 x RTX 3090 @ 0.60 -> PRIMARY_VAST_NUM_GPUS_FALLBACK=2, PRIMARY_VAST_GPU_NAME_FALLBACK="RTX 3090", PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60 (standing config; allowlist 43803/55158)
This is what .env.prod.example + runbook + MEMORY mirror must teach. The standing 2x3090 config is the FALLBACK shape, NOT the primary (legacy PRIMARY_NUM_GPUS=2 mapped to PrimaryVastNumGPUsFallback per config.go:217).

<interfaces>
<!-- docker-compose.yml current state (extracted) -->
:108-113 canonical block (PRIMARY_VAST_GPU_NAME_{PRIMARY,FALLBACK}, _PRICE_CAP_{PRIMARY,FALLBACK}, _NUM_GPUS_{PRIMARY,FALLBACK} with :-defaults).
:114 comment "# Legacy deprecated-alias reads (warn-and-use until Phase 11.2 cleanup)".
:115 PRIMARY_GPU_NAME, :116 PRIMARY_NUM_GPUS, :117 PRIMARY_VAST_PRICE_CAP_DPH (all `${VAR:-}` empty defaults) — DELETE these 4 lines.
:118-119 PRIMARY_VAST_MACHINE_ALLOWLIST / BLOCKLIST — KEEP.
New cold-start rows to ADD: `PRIMARY_VAST_REJECT_PRIVATE_IP: ${PRIMARY_VAST_REJECT_PRIVATE_IP:-true}`, `PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS: ${PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS:-120}`.

<!-- .env.prod.example current state (extracted) -->
:119 VAST_PRICE_CAP_DPH=0.40   (emerg — KEEP)
:120 PRIMARY_VAST_PRICE_CAP_DPH=0.60   (legacy — DELETE)
:125 PRIMARY_NUM_GPUS=2   (legacy — DELETE)
:126 PRIMARY_GPU_NAME=RTX 3090   (legacy — DELETE)
:127 PRIMARY_VAST_MACHINE_ALLOWLIST=43803,55158   (KEEP)
:139 PRIMARY_VAST_MACHINE_BLOCKLIST=...   (KEEP)
Canonical rows to ADD (per canonical_shape_matrix): PRIMARY_VAST_NUM_GPUS_PRIMARY=1, PRIMARY_VAST_NUM_GPUS_FALLBACK=2, PRIMARY_VAST_GPU_NAME_PRIMARY=RTX 3090, PRIMARY_VAST_GPU_NAME_FALLBACK=RTX 3090, PRIMARY_VAST_PRICE_CAP_PRIMARY=0.30, PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60. Cold-start rows: PRIMARY_VAST_REJECT_PRIVATE_IP=true, PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS=120.

<!-- RUNBOOK-PRIMARY-POD.md (extracted) -->
:330 `| PRIMARY_VAST_PRICE_CAP_DPH | float | 0.40 | RTX 4090 price cap ...` — REPLACE.
:875,878 DPH cap=0.40 narrative — REPLACE with per-shape caps.

<!-- Vast API auth HARD RULE -->
?api_key=<key> query-param against /api/v0/bundles/ (offer search). Authorization: Bearer silently nulls fields.
Allowlist machines: 43803, 55158 (standing 2x3090 FALLBACK config).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy vars + add canonical & cold-start vars across compose, env.prod.example, runbook (anchored grep, per canonical shape matrix)</name>
  <files>gateway/docker-compose.yml, gateway/.env.prod.example, gateway/docs/RUNBOOK-PRIMARY-POD.md</files>
  <read_first>
    - gateway/docker-compose.yml (lines 108-119 region)
    - gateway/.env.prod.example (lines 115-140 region — note :119 emerg var to PRESERVE)
    - gateway/docs/RUNBOOK-PRIMARY-POD.md (lines 320-335 env table + 870-880 cap narrative)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md (§5 mitigation list, §6 companion changes 3-7)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md (docker-compose / .env.prod.example section)
  </read_first>
  <action>
    docker-compose.yml: delete the comment "# Legacy deprecated-alias reads ..." and the 3 rows PRIMARY_GPU_NAME / PRIMARY_NUM_GPUS / PRIMARY_VAST_PRICE_CAP_DPH. Keep the canonical block + allowlist/blocklist. Add after the canonical block: `PRIMARY_VAST_REJECT_PRIVATE_IP: ${PRIMARY_VAST_REJECT_PRIVATE_IP:-true}` and `PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS: ${PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS:-120}`. Per RESEARCH §7 Q4 keep the canonical `:-` defaults (do NOT drop them).
    .env.prod.example: delete the 3 legacy rows (PRIMARY_VAST_PRICE_CAP_DPH=0.60, PRIMARY_NUM_GPUS=2, PRIMARY_GPU_NAME=RTX 3090). PRESERVE the emerg `VAST_PRICE_CAP_DPH=0.40` line. Add the 6 canonical rows per the canonical_shape_matrix (NUM_GPUS_PRIMARY=1, NUM_GPUS_FALLBACK=2, GPU_NAME_PRIMARY=RTX 3090, GPU_NAME_FALLBACK=RTX 3090, PRICE_CAP_PRIMARY=0.30, PRICE_CAP_FALLBACK=0.60) + the 2 cold-start rows (PRIMARY_VAST_REJECT_PRIVATE_IP=true, PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS=120). Add a one-line comment that legacy PRIMARY_NUM_GPUS/PRIMARY_GPU_NAME/PRIMARY_VAST_PRICE_CAP_DPH now HARD-FAIL gateway boot (plan 6.6.Y-02) — do not re-add them.
    RUNBOOK-PRIMARY-POD.md: replace the PRIMARY_VAST_PRICE_CAP_DPH env-table row with 6 canonical-var rows (name|type|default|meaning|required) + 2 cold-start var rows; replace the :875/:878 0.40 narrative with the per-shape caps (0.30 primary / 0.60 fallback). Add a "## Shape pair model" subsection: shape 0 = primary (PRIMARY_VAST_*_PRIMARY, single GPU 1x3090, searched first); shape 1 = fallback (PRIMARY_VAST_*_FALLBACK, dual GPU 2x3090, the standing config, searched when shape-0 returns zero qualified offers); document the public_port_bind_timeout closure_reason and that legacy vars now hard-fail boot.
  </action>
  <verify>
    <automated>! grep -Eq '(^|[^_])PRIMARY_NUM_GPUS([^_]|$)|(^|[^_])PRIMARY_GPU_NAME([^_]|$)|(^|[^_])PRIMARY_VAST_PRICE_CAP_DPH([^_]|$)' gateway/docker-compose.yml && grep -q 'PRIMARY_VAST_REJECT_PRIVATE_IP' gateway/docker-compose.yml && grep -q '^VAST_PRICE_CAP_DPH=0.40' gateway/.env.prod.example && grep -q '^PRIMARY_VAST_NUM_GPUS_PRIMARY=1' gateway/.env.prod.example && grep -q '^PRIMARY_VAST_NUM_GPUS_FALLBACK=2' gateway/.env.prod.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec '(^|[^_])PRIMARY_NUM_GPUS([^_]|$)|(^|[^_])PRIMARY_GPU_NAME([^_]|$)|(^|[^_])PRIMARY_VAST_PRICE_CAP_DPH([^_]|$)' gateway/docker-compose.yml` returns 0 (anchored — must NOT match PRIMARY_VAST_NUM_GPUS_PRIMARY / PRIMARY_VAST_GPU_NAME_PRIMARY / PRIMARY_VAST_PRICE_CAP_PRIMARY; finding #9).
    - `grep -c 'PRIMARY_VAST_REJECT_PRIVATE_IP' gateway/docker-compose.yml` returns >=1.
    - `grep -c '^VAST_PRICE_CAP_DPH=0.40' gateway/.env.prod.example` returns 1 (emerg var preserved).
    - `grep -Ec '^PRIMARY_NUM_GPUS=|^PRIMARY_GPU_NAME=|^PRIMARY_VAST_PRICE_CAP_DPH=' gateway/.env.prod.example` returns 0 (legacy rows gone — anchored to line start + literal name).
    - `grep -c '^PRIMARY_VAST_NUM_GPUS_PRIMARY=1' gateway/.env.prod.example` returns 1 AND `grep -c '^PRIMARY_VAST_NUM_GPUS_FALLBACK=2' gateway/.env.prod.example` returns 1 (shape matrix matches: primary=1, fallback=2).
    - `grep -c '^PRIMARY_VAST_PRICE_CAP_PRIMARY=0.30' gateway/.env.prod.example` returns 1 AND `grep -c '^PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60' gateway/.env.prod.example` returns 1.
    - `grep -vE '^\s*#' gateway/docs/RUNBOOK-PRIMARY-POD.md | grep -Ec '(^|[^_])PRIMARY_NUM_GPUS([^_]|$)|(^|[^_])PRIMARY_GPU_NAME([^_]|$)|(^|[^_])PRIMARY_VAST_PRICE_CAP_DPH([^_]|$)'` returns 0 in non-comment lines (legacy not taught; anchored so it does NOT match canonical PRIMARY_VAST_* names — finding #9); and the runbook contains "PRIMARY_VAST_NUM_GPUS_FALLBACK" and "public_port_bind_timeout".
  </acceptance_criteria>
  <done>compose + .env.prod.example + runbook teach only canonical + cold-start vars matching the shape matrix; emerg VAST_PRICE_CAP_DPH preserved; legacy primary vars deleted (anchored greps confirm no canonical false-positive).</done>
</task>

<task type="auto">
  <name>Task 2: Repo-local MEMORY mirror + operator-sync note (replaces external-file edit)</name>
  <files>gateway/docs/MEMORY-primary-gpu-shape.md</files>
  <read_first>
    - /home/pedro/.claude/projects/-home-pedro-projetos-pedro-gpu-ifix/memory/primary-gpu-shape-06.8-final.md (READ-ONLY source to mirror; do NOT edit it from this autonomous task — it is outside the repo writable root, finding #5)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md (MEMORY section)
  </read_first>
  <action>
    Author the repo-local mirror gateway/docs/MEMORY-primary-gpu-shape.md (in-repo, autonomous-safe) that teaches the canonical names per the canonical_shape_matrix: standing config = the FALLBACK shape (PRIMARY_VAST_NUM_GPUS_FALLBACK=2 / PRIMARY_VAST_GPU_NAME_FALLBACK=RTX 3090 / PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60, allowlist 43803/55158) instead of legacy PRIMARY_NUM_GPUS; drop any "compose default 0.40 outdated" caveat; add a note that legacy vars now HARD-FAIL gateway boot (plan 6.6.Y-02) so the pre-primary-UAT env check must use canonical names.
    At the top of the file add an OPERATOR-SYNC note: "This repo-local mirror is authoritative for the canonical migration. The external auto-memory file ~/.claude/projects/-home-pedro-projetos-pedro-gpu-ifix/memory/primary-gpu-shape-06.8-final.md must be manually synced by the operator (it is outside the repo writable root). Operator: copy the canonical guidance below into that file."
    Do NOT attempt to write to the external ~/.claude path from this task.
  </action>
  <verify>
    <automated>test -f gateway/docs/MEMORY-primary-gpu-shape.md && grep -q 'PRIMARY_VAST_NUM_GPUS_FALLBACK' gateway/docs/MEMORY-primary-gpu-shape.md && ! grep -Eq '(^|[^_])PRIMARY_NUM_GPUS([^_]|$)' gateway/docs/MEMORY-primary-gpu-shape.md && grep -qi 'manually sync\|operator-sync\|operator sync' gateway/docs/MEMORY-primary-gpu-shape.md</automated>
  </verify>
  <acceptance_criteria>
    - gateway/docs/MEMORY-primary-gpu-shape.md exists, teaches PRIMARY_VAST_NUM_GPUS_FALLBACK (standing 2x3090 = FALLBACK shape), and does NOT instruct setting legacy PRIMARY_NUM_GPUS (anchored grep, finding #9).
    - The file contains the operator-sync note pointing at the external ~/.claude memory file.
    - No write is attempted to the external ~/.claude path by this autonomous task (finding #5).
  </acceptance_criteria>
  <done>Repo-local canonical MEMORY mirror authored with operator-sync note; external file edit delegated to operator.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Allowlist refresh — verify 43803/55158 against live Vast (operator, $0 read-only) + PROACTIVE shape-0 resolution</name>
  <files>.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md</files>
  <read_first>
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md (any Option D / universal-failure directive)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md (§2.4 allowlist cascade impact + §7 Q1)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md (reconciler allowlist-refresh section)
  </read_first>
  <what-built>
    This is operator-gated (review finding #5): the live Vast offer query needs VAST_AI_API_KEY + network which autonomous mode cannot reliably reach. The operator runs the read-only API query and pastes results; the evidence file is then authored from the pasted results.
  </what-built>
  <action>
    Operator queries the live Vast offer inventory for machines 43803 and 55158 using the `?api_key=<key>` query-param HARD RULE (key from ~/.claude/CLAUDE.md or vps-ifix-vm stack.env): hit /api/v0/bundles/ with a filter for each machine_id and record whether each still lists a 2xRTX 3090 offer (and its dph, reliability, geo). SECRET HYGIENE (finding #10): use a shell var for the key and REDACT it in any pasted output (`?api_key=<REDACTED>`); the evidence file must contain NO literal key. Then author the evidence file with:
    - **Verification result:** per-machine — still offers 2x3090? dph/reliability/geo? retired?
    - **Shape-0 mismatch analysis + PROACTIVE resolution (finding #1):** shape 0 demands num_gpus=1 but the allowlist hosts are 2x3090-only -> shape-0 allowlist pass returns zero -> broaden cascade (the iter-1 root-cause feeder). Choose ONE PROACTIVE resolution and record it (do NOT settle for "broadening is safe"): (a) populate the allowlist with 1x3090-capable machine_ids for shape 0 (query Vast for qualified 1x3090 hosts and propose their machine_ids), OR (c) skip the shape-0 allowlist pass entirely when no 1x3090 allowlist entry exists (recommend the code change to plan 6.6.Y-03's allowlist path), OR — only if 6.6.Y-01 found UNIVERSAL failure — apply the SPIKE-EVIDENCE Option D allowlist hard-fail directive. Option (b) "just document broadening is safe" is NOT acceptable as the sole resolution per the review; if broadening is retained it must be paired with (a) or (c).
    - **Allowlist correction (if any):** if a machine retired, propose the corrected PRIMARY_VAST_MACHINE_ALLOWLIST value for stack.env (plan 6.6.Y-05 applies it) and note it in .env.prod.example.
    - **Spend:** read-only API query — $0.00 Vast spend (no provisioning). Record $0.00 against the phase cap.
    This task does NOT provision a pod; it is an API read only.
  </action>
  <how-to-verify>
    Operator pastes the per-machine offer query result (api_key redacted) and the chosen proactive shape-0 resolution.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md && grep -Eq '43803|55158' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md && grep -Eqi 'shape.?0|num_gpus' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md && ! grep -Eq 'api_key=[A-Za-z0-9]{8}' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md</automated>
  </verify>
  <acceptance_criteria>
    - ALLOWLIST-REFRESH.md exists and states, per machine 43803 and 55158, whether it still offers 2x3090 (with dph/geo or "retired").
    - The file names exactly one PROACTIVE resolution (a or c, or Option D if universal) for the shape-0 allowlist mismatch — NOT "broadening is safe" alone (finding #1).
    - The file records $0.00 Vast spend (read-only API query, no provisioning).
    - The file contains NO literal VAST_AI_API_KEY value (no `api_key=<8+ char value>` substring — finding #10).
  </acceptance_criteria>
  <done>Allowlist verified against live Vast; PROACTIVE shape-0 resolution chosen; zero Vast spend; secrets redacted.</done>
  <resume-signal>Type "allowlist refreshed" with the per-machine result (api_key redacted) + the chosen proactive shape-0 resolution</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → Vast API | VAST_AI_API_KEY in `?api_key=` query-param for the allowlist verification |
| .env.prod.example → operator deploys | the example file is copied by operators; wrong content propagates the drift |
| evidence file → git | captured query URLs could leak the API key into committed history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.6.Y-04-01 | Information disclosure | VAST_AI_API_KEY in `?api_key=` query-param during allowlist verification / committed evidence | mitigate | Operator uses a shell var for the key; REDACT api_key in pasted evidence; acceptance criterion greps for any literal key and fails if found; Vast HARD RULE mandates query-param; rotation available per CLAUDE.md |
| T-6.6.Y-04-02 | Tampering | .env.prod.example still teaching legacy vars → operator copies it → hard-fail crash-loop on deploy | mitigate | This plan deletes legacy rows (anchored grep) and adds a hard-fail note |
| T-6.6.Y-04-03 | Tampering | autonomous task attempting to write outside repo root (external MEMORY file) | mitigate | External-file edit replaced by a repo-local mirror + operator-sync note (finding #5); no out-of-root write attempted |
| T-6.6.Y-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; doc/config edits + read-only API query only |
</threat_model>

<verification>
- Legacy primary vars absent from compose, .env.prod.example (non-comment), runbook (non-comment), repo-local MEMORY mirror — all via ANCHORED greps that do not false-match canonical PRIMARY_VAST_* names.
- Canonical (shape0=1, shape1=2, caps 0.30/0.60) + 2 cold-start vars present in compose + .env.prod.example, matching the canonical shape matrix.
- Emerg VAST_PRICE_CAP_DPH=0.40 preserved.
- ALLOWLIST-REFRESH.md documents 43803/55158 live status + a PROACTIVE shape-0 resolution + $0 spend + redacted secrets.
</verification>

<success_criteria>
All operator-facing surfaces teach only canonical + cold-start vars matching the shape matrix (legacy deleted, emerg preserved); the external MEMORY edit is delegated to the operator via a repo-local mirror; allowlist verified live with a PROACTIVE shape-0 resolution, zero Vast spend, and redacted secrets.
</success_criteria>

<output>
Create `.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-SUMMARY.md` when done.
</output>
</content>
