---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 06
subsystem: infra
tags: [gpu-pod, onstart, whisper, ghcr, fail-fast, cold-start, vast, deploy]

requires:
  - phase: 6.6.Y-02
    provides: fail-fast on legacy PRIMARY_* env vars (os.LookupEnv trip on presence)
  - phase: 6.6.Y-03
    provides: cold-start budget + boot-shape log dump
  - phase: 6.6.Y-04
    provides: compose with legacy PRIMARY_* rows removed (393406b)
  - phase: 6.6.Y-05
    provides: SAFE-TO-DEPLOY gate — migrated legacy-free stack env (canonical shape matrix)
provides:
  - "Option C: whisper restored as the 3rd parallel onstart weight download (Qwen + bge-m3 KEPT + whisper)"
  - "fail-fast gateway image deployed to ai-gateway-dev, booting clean (no ErrLegacyPrimaryEnv) on the migrated stack"
  - "3-way digest verification (built == GHCR == running container) via PAT-retag fallback"
  - "recorded rollback path + READY-FOR-LIVE-UAT gate"
affects: [6.6.Y-07]

tech-stack:
  added: []
  patterns:
    - "GHCR PAT-retag fallback: GHA GHCR push broken org-wide since 2026-06-09 — build local, retag, push with PAT"
    - "3-way digest verification before declaring deploy done (built == GHCR == running)"
    - "rollback target recorded BEFORE recreate (previous digest + redeploy command + trigger)"

key-files:
  created:
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md
  modified:
    - gateway/internal/primary/onstart.go

key-decisions:
  - "Option C = Qwen + bge-m3 + whisper (3-way) — bge-m3 STAYS on-pod (finding #3); whisper restored as the 3rd download removed by Phase 11.1 D-A4"
  - "No lifecycle.go edit needed — PRIMARY_WHISPER_WEIGHTS_KEY/SHA256 forwarding already present (lifecycle.go:353-354); Option C is purely onstart shell wiring"
  - "Deploy via PAT-retag fallback (GHA GHCR push broken org-wide); 3-way digest match is the proof, not a green PR run"

patterns-established:
  - "Pattern: GHCR PAT-retag fallback for image deploy while GHA GHCR push is broken org-wide"
  - "Pattern: digest verification (built == GHCR landed == running container) as the deploy-done gate"

requirements-completed: [PC-COLD-START-FIX]

duration: ~25min (Task 3 continuation; Task 1 + deploy executed earlier)
completed: 2026-06-11
---

# Phase 6.6.Y Plan 06: Whisper 3-way onstart + fail-fast image deploy Summary

**Option C restores whisper as the 3rd parallel onstart weight download (Qwen + bge-m3 KEPT), and the fail-fast/cold-start gateway image is deployed to ai-gateway-dev via PAT-retag fallback — booting clean (no ErrLegacyPrimaryEnv) on the migrated stack with shape1=2×3090 FALLBACK + shape0=1×3090 PRIMARY, 3-way digest-verified.**

## Performance

- **Duration:** ~25 min (Task 3 continuation; Task 1 code + operator deploy completed earlier)
- **Completed:** 2026-06-11
- **Tasks:** 3 (Task 1 code, Task 2 operator-gated deploy, Task 3 evidence)
- **Files modified:** 1 code (onstart.go) + 1 evidence doc created

## Accomplishments

- **Option C (whisper 3-way onstart):** restored whisper as the third `download_with_verify ... & WHISPER_PID=$!` spawn in `gateway/internal/primary/onstart.go` — bge-m3 KEPT (finding #3), whisper env guards restored, whisper tarball extracted to `/weights/whisper`, `wait` extended to all 3 PIDs, echo strings bumped to "3". Length-regression test (`TestPrimaryOnstartLengthBelowLimit`, <14KB) stayed green. This makes the weights stage reachable for the live UAT.
- **Fail-fast image deployed:** built local (GHA GHCR push broken org-wide), pushed with GitHub PAT, 3-way digest-verified (built `ead1bc16…` == GHCR `02bcb7b5…` == running container repo digest). Recreated `ai-gateway-dev` on the migrated stack via Portainer webhook (HTTP 204), health=healthy.
- **Clean boot proven:** 0 `ErrLegacyPrimaryEnv` occurrences; boot-shape dump shows shape0=1×3090@$0.30 (PRIMARY) + shape1=2×3090@$0.60 (FALLBACK/standing) + allowlist [43803 55158] + port_bind_budget=120. Rollback target recorded (`e10b0e96…`). $0.00 Vast spend.
- **Evidence + gate:** `6.6.Y-06-DEPLOY-EVIDENCE.md` (72 lines) carries build method, 3-way digest table, rollback path, clean-boot log, and the READY-FOR-LIVE-UAT gate.

## Task Commits

1. **Task 1: Option C — whisper 3-way onstart download** - `f51e917` (feat) — merged into develop via worktree merge `72edfe0`
2. **Task 2: Build + deploy fail-fast image (operator-gated)** - no repo files; evidence captured in DEPLOY-EVIDENCE.md
3. **Task 3: Write 6.6.Y-06-DEPLOY-EVIDENCE.md** - `c7cc5f0` (docs)

**gofmt deviation commit:** `cb3bdec` (style) — see Deviations.

## Files Created/Modified

- `gateway/internal/primary/onstart.go` - 3rd parallel whisper download (WHISPER_PID), guards restored, /weights/whisper extraction (Task 1, `f51e917`)
- `.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md` - deploy evidence with 3-way digest verification, rollback path, clean-boot proof, live-UAT gate (Task 3, `c7cc5f0`)

## Decisions Made

- Option C = Qwen + bge-m3 + whisper (3-way). bge-m3 STAYS on-pod (finding #3 — the "BGE-M3 moved off-pod" note does NOT apply to the primary GPU pod's embedding service). Whisper is the restored 3rd download (removed by Phase 11.1 D-A4, plumbed back at config/lifecycle by 11.2 but never re-wired into the onstart shell).
- No `lifecycle.go` edit: PRIMARY_WHISPER_WEIGHTS_KEY/SHA256 forwarding already present (`:353-354`) + ErrMissingWhisperSHA validation (`:307`). Option C is purely onstart shell wiring — `files_modified` correctly lists only onstart.go.
- Deploy proof is the 3-way digest match (built == GHCR == running), not a green PR run — GHA's "Build & push gateway image" step reported failure even though tests were green.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] gofmt gate caught unformatted config.go from plan 02**
- **Found during:** Task 2 (deploy / CI run on sha `72edfe0`)
- **Issue:** The CI `gofmt` gate failed on `gateway/internal/config/config.go` (unformatted code carried in from plan 6.6.Y-02), blocking the image build pipeline.
- **Fix:** Ran gofmt on `config.go`, committed as `style(6.6.Y-06): gofmt config.go — unblock CI gofmt gate`.
- **Files modified:** `gateway/internal/config/config.go`
- **Verification:** GHA run `27366113646` (sha `cb3bdec`) tests + integration GREEN.
- **Committed in:** `cb3bdec`

---

**Total deviations:** 1 auto-fixed (1 blocking).
**Impact on plan:** gofmt fix was required to pass CI gates and reach the build step. No scope creep.

## Issues Encountered

- **GHA GHCR push broken org-wide (memory `ghcr-actions-install-broken`):** the "Build & push gateway image" step failed with `denied: permission_denied: The requested installation does not exist` even on the green test run (`cb3bdec`). Resolved via the planned PAT-retag fallback: local docker build on ops-claude + push with GitHub PAT. A green test/integration run is NOT proof the image landed — confirmed via 3-way digest verification.
- **CI flake:** `TestHandleForceUpRequest_TransitionsAsleepToProvisioning` failed on first run of sha `72edfe0`, passed on rerun (pre-existing flake also seen 2026-06-09 on `8d676b8c`). Not in scope for this plan; logged as known flake.
- **Empty-string legacy compose-row ressalva (from plan 05):** the pre-6.6.Y compose mapped 3 legacy PRIMARY_* rows as empty strings, which the presence-based fail-fast would trip on. Resolved by the GitOps re-pull delivering the plan-04 compose (legacy rows removed, `393406b`) on this deploy → 0 legacy vars in the recreated container, clean boot.

## User Setup Required

None - no external service configuration required (deploy executed by operator + orchestrator).

## Next Phase Readiness

- Weights stage reachable (Option C whisper 3-way wired) + fail-fast image booting clean on the migrated stack → **READY FOR LIVE UAT (plan 6.6.Y-07)**.
- Live UAT (plan 07) provisions the FALLBACK shape (2×3090). Rollback path recorded if the UAT exposes a dirty boot.
- Note: STATE.md / ROADMAP.md intentionally NOT updated by this continuation agent (orchestrator handles).

## Self-Check: PASSED

- FOUND: 6.6.Y-06-DEPLOY-EVIDENCE.md, 6.6.Y-06-SUMMARY.md
- FOUND commits: c7cc5f0 (evidence), f39f285 (summary), f51e917 (Task 1 code), cb3bdec (gofmt)
- `grep -c 'WHISPER_PID' gateway/internal/primary/onstart.go` → 2 (PID assign + wait; Option C 3rd download confirmed in onstart.go)

---
*Phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical*
*Completed: 2026-06-11*
