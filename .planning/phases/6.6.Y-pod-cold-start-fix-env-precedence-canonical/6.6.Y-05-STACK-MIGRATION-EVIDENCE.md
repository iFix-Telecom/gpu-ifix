# 6.6.Y-05 — Stack Migration Evidence (Portainer compose 34)

**Date:** 2026-06-11 (01:40Z redeploy)
**Stack:** `ai-gateway-dev`, Portainer `portainer3.ifixtelecom.com.br`, endpoint 3 (vps-ifix-vm / VM 102), stack id 34 (GitOps, repo `iFix-Telecom/gpu-ifix`)
**Operator:** Pedro (Portainer UI, env Advanced mode) — orquestrador validou via SSH

## Before → After (shape rows do stack env; secrets omitidos)

### Before (capturado 2026-06-10, pré-migração)
```
PRIMARY_GPU_NAME=RTX 3090            ← legacy (linha 42)
PRIMARY_NUM_GPUS=2                   ← legacy (linha 43)
PRIMARY_VAST_PRICE_CAP_DPH=0.60      ← legacy (linha 27)
PRIMARY_VAST_MACHINE_ALLOWLIST=43803,55158
PRIMARY_VAST_MACHINE_BLOCKLIST=55942,45778
VAST_PRICE_CAP_DPH=0.40              ← emerg (subsistema distinto)
(6 vars canônicas AUSENTES do stack env — só existiam como defaults do compose)
(2 vars cold-start AUSENTES)
```

### After (capturado 2026-06-11 pós-redeploy 01:40Z)
```
PRIMARY_HOST_ID=0
PRIMARY_POD_SCHEDULE_DISABLED=true
PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS=120
PRIMARY_TEMPLATE_IMAGE=ghcr.io/ifixtelecom/converseai-primary-pod:develop
PRIMARY_VAST_GPU_NAME_FALLBACK=RTX 3090
PRIMARY_VAST_GPU_NAME_PRIMARY=RTX 3090
PRIMARY_VAST_MACHINE_ALLOWLIST=43803,55158
PRIMARY_VAST_MACHINE_BLOCKLIST=55942,45778
PRIMARY_VAST_NUM_GPUS_FALLBACK=2
PRIMARY_VAST_NUM_GPUS_PRIMARY=1
PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60
PRIMARY_VAST_PRICE_CAP_PRIMARY=0.30
PRIMARY_VAST_REJECT_PRIVATE_IP=true
VAST_PRICE_CAP_DPH=0.40
```

- Legacy vars no stack env: **0** (`grep -cE "^PRIMARY_(GPU_NAME|NUM_GPUS|VAST_PRICE_CAP_DPH)=" stack.env` → 0)
- Matriz canônica confirmada: **PRIMARY = 1×RTX 3090 @ $0.30** (shape0), **FALLBACK = 2×RTX 3090 @ $0.60** (shape1) — o standing 2×3090 é o slot FALLBACK, não o primary ✓
- `VAST_PRICE_CAP_DPH=0.40` (emerg, unprefixed) preservado ✓

## Redeploy + health (container pré-fail-fast)

```
started=2026-06-11T01:40:24.668582102Z health=healthy
```

Container `ai-gateway-dev` recriado pelo Update the stack e **healthy** com o env migrado.

## Ressalva — empty-string legacy no container (resolvida no plan 06)

O compose **deployado** (repo pré-6.6.Y) ainda mapeia:
```
PRIMARY_GPU_NAME: ${PRIMARY_GPU_NAME:-}
PRIMARY_NUM_GPUS: ${PRIMARY_NUM_GPUS:-}
PRIMARY_VAST_PRICE_CAP_DPH: ${PRIMARY_VAST_PRICE_CAP_DPH:-}
```
→ container atual carrega as 3 legacy como **string vazia** (origem: compose row, não stack env). A imagem atual (pré-fail-fast) as ignora. O fail-fast do plan 6.6.Y-02 tripa em **presença** (os.LookupEnv), inclusive vazia — portanto o deploy do plan 6.6.Y-06 DEVE incluir o compose corrigido do repo (rows removidas no plan 6.6.Y-04, commit `393406b`), que o GitOps re-pull entrega junto. Sem isso: crash-loop.

## Incidentes da migração

1. Primeira tentativa de edição de env não persistiu no DB do Portainer (redeploy 00:31Z regenerou stack.env com env antigo). Causa: save de env sem apply/agregação na mesma ação.
2. Segunda tentativa: env reapareceu antigo no UI (edit perdido).
3. Resolução: paste completo via Advanced mode + Update the stack na mesma página → persistiu (01:40Z).
4. As 2 chaves `sk-proj-…` (STT/EMBED OpenAI) tinham quebra de linha no meio no env anterior — fornecido bloco com linhas íntegras no paste final.

## D-04 — decisão VAST_PRICE_CAP_DPH

`VAST_PRICE_CAP_DPH=0.40` permanece como cap do subsistema **emerg** (não prefixado). O cap primary agora vive exclusivamente em `PRIMARY_VAST_PRICE_CAP_{PRIMARY,FALLBACK}`. Nenhuma colisão de nome restante no stack env.

## Veredito

Migração **completa e confirmada ANTES do plan 6.6.Y-06** (must_have 4 ✓). Spend: $0.00 (sem GPU).
