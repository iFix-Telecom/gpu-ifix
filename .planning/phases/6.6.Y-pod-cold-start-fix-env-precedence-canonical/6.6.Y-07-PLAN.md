---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 07
type: execute
wave: 5
depends_on: [6.6.Y-06]
files_modified:
  - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-HUMAN-UAT.md
  - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-VERIFICATION.md
autonomous: false
requirements: [PC-COLD-START-FIX, PC-ENV-PRECEDENCE]

must_haves:
  truths:
    - "A pre-UAT consistency check confirms docker inspect env + boot-shape log + .env.prod.example + runbook ALL agree on the shape pair (PRIMARY 1×3090@0.30, FALLBACK 2×3090@0.60) and caps"
    - "A live force-up on the fail-fast image provisions a 2×3090 pod (FALLBACK shape) and reaches Ready (4 endpoints) within the cold-start budget — OR a defective host is failed-fast within ~120s with closure_reason public_port_bind_timeout and the run retried"
    - "When a port-bound pod is reached, supervisorctl status + /var/log/onstart.log + /var/log/supervisord.log snapshots are captured, retiring suspects #1/#3"
    - "The RFC1918 reject (Option A) and the 120s fail-fast (Option B) are each observed firing at least once OR documented as not-triggered-because-no-defective-host-sampled"
    - "Total phase Vast spend is logged and ≤ $2.00 (D-05 hard cap), with all evidence secrets redacted"
    - "6.6.Y-VERIFICATION.md records verdict (passed/passed_partial/failed) against PC-COLD-START-FIX + PC-ENV-PRECEDENCE"
  artifacts:
    - path: ".planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-HUMAN-UAT.md"
      provides: "Pre-UAT consistency check + UAT scenario sheet + operator sign-off + spend log + suspect-snapshot capture"
      min_lines: 30
    - path: ".planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-VERIFICATION.md"
      provides: "Phase verdict + per-requirement evidence + cumulative spend"
      min_lines: 25
  key_links:
    - from: "6.6.Y-HUMAN-UAT.md"
      to: "6.6.Y-VERIFICATION.md"
      via: "signed scenario results roll up into the phase verdict"
      pattern: "PASS|FAIL"
---

<objective>
Close Phase 6.6.Y with a live UAT on the deployed fail-fast image (2×3090 FALLBACK shape) that re-runs the iter-1 cold-start path and proves the A+B+C fix bundle + env-precedence migration work end-to-end, then write the phase VERIFICATION.

This is `autonomous: false` — the UAT requires real Vast GPU spend (operator-only) within the $2.00 phase cap (D-05).

UAT scenarios (operator runs on ai-gateway-dev via `docker exec ai-gateway-dev /gatewayctl primary force-up|force-down`):
- **S0 — Pre-UAT consistency check (review finding #11):** BEFORE any spend, confirm the four shape-bearing surfaces AGREE: (i) live `docker inspect ai-gateway-dev` env, (ii) the boot-shape log dump, (iii) gateway/.env.prod.example, (iv) gateway/docs/RUNBOOK-PRIMARY-POD.md — all must show PRIMARY=1×3090@0.30 and FALLBACK=2×3090@0.60. Any divergence STOPS the UAT (drift would reintroduce the exact class this phase fixes).
- **S1 — Clean boot + resolved shape:** gateway booted clean on migrated env (no ErrLegacyPrimaryEnv) and the boot-shape dump shows shape1=2×3090 (FALLBACK/standing) + shape0=1×3090 (PRIMARY) + allowlist + port_bind_budget=120 (carried from plan 06, re-asserted here).
- **S2 — Cold-start to Ready on 2×3090 (FALLBACK):** force-up provisions a 2×3090 pod and reaches FSM Ready (4 endpoints HTTP 200 — llm/stt/tts/dcgm) within the cold-start budget. Capture lifecycle_id, host, geo, dph, wall-clock.
- **S3 — Suspect #1/#3 retirement:** once port-bound, capture `supervisorctl status` + `/var/log/onstart.log` + `/var/log/supervisord.log` (D-03 acceptance, RESEARCH-COLD-START §8 Q6) confirming onstart ran the 3-way download (qwen + bge-m3 + whisper) + supervisord launched the services.
- **S4 — Option A/B observability:** if a defective (RFC1918 / never-binds) host is encountered, confirm Option A rejected it at pick OR Option B failed it fast within ~120s with closure_reason public_port_bind_timeout + per-strike logs. If no defective host is sampled, document that A/B did not trigger (acceptable) and rely on the unit-test coverage from plan 03.
- **S5 — Clean force-down:** force-down drains + destroys the pod; final Vast instance count 0.

Purpose: prove the fix on the real failure path and bound spend.
Output: 6.6.Y-HUMAN-UAT.md (signed) + 6.6.Y-VERIFICATION.md (verdict).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md

<canonical_shape_matrix>
<!-- SINGLE SOURCE OF TRUTH — see plan 6.6.Y-01. -->
Shape 0 PRIMARY  = 1 x RTX 3090 @ 0.30  (shape0_* in boot dump)
Shape 1 FALLBACK = 2 x RTX 3090 @ 0.60  (shape1_* in boot dump; STANDING config; allowlist 43803/55158)
The UAT provisions the FALLBACK shape (2x3090) because that is the standing config the allowlist serves. S0 asserts all four surfaces agree on this matrix.

<interfaces>
<!-- UAT trigger path (validated 06.6.X Plan 04) -->
Force-up: ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary force-up'
Force-down: ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary force-down --reason 6.6.Y_uat'
FSM state: ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary state'
Pod logs (once SSH reachable): operator SSHes the pod (POD_DEBUG_SSH_PUBLIC_KEY inline sshd, -p 22:22 mapping) and runs supervisorctl status + cats /var/log/onstart.log + /var/log/supervisord.log.
Vast API HARD RULE: ?api_key=<key> query-param against /api/v0/instances/<id>/ (Bearer nulls fields). SECRET HYGIENE (finding #10): use a shell var for the key; REDACT it in pasted evidence (`?api_key=<REDACTED>`).
S0 consistency-check commands: `ssh vps-ifix-vm "docker inspect ai-gateway-dev --format '{{range .Config.Env}}{{println .}}{{end}}' | grep -E 'PRIMARY_VAST_(NUM_GPUS|GPU_NAME|PRICE_CAP)'"`; `ssh vps-ifix-vm 'docker logs ai-gateway-dev 2>&1 | grep -E "shape0_|shape1_"'`; `grep -E '^PRIMARY_VAST_(NUM_GPUS|PRICE_CAP)_' gateway/.env.prod.example`; `grep -A2 'Shape pair model' gateway/docs/RUNBOOK-PRIMARY-POD.md`.
Spend tracking: phase running total includes plan 6.6.Y-01 spike spend; cap $2.00 (D-05).
new closure_reason: public_port_bind_timeout (plan 03 Option B).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 6.6.Y-HUMAN-UAT.md scenario sheet (S0 consistency check + S1-S5)</name>
  <files>.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md (D-03 UAT acceptance — supervisorctl + onstart/supervisord capture; D-05 spend cap)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md (§8 the 6 hand-off questions the UAT must close)
  </read_first>
  <action>
    Write the UAT sheet with S0 (pre-UAT consistency check) + the 5 scenarios above, each with: exact operator commands, expected result, a PASS/FAIL sign-off slot, and an evidence-paste slot.
    S0 (finding #11): include the four consistency-check commands from <interfaces> and an explicit assertion table — docker inspect env, boot-shape log (shape0_*/shape1_*), .env.prod.example, and runbook "Shape pair model" must ALL show PRIMARY=1×3090@0.30 and FALLBACK=2×3090@0.60. S0 PASS is a precondition for S2 spend; if any surface diverges, STOP and fix before provisioning.
    Include a spend-log table (per-attempt Vast cost + running phase total vs $2.00 cap) and an explicit instruction to force-down + verify 0 Vast instances at the end (no orphan). Include the Vast API HARD RULE + SECRET HYGIENE reminder (finding #10): redact ?api_key= in any pasted evidence; the UAT file must contain no literal key. Include a retry budget note: if S2's first host is defective, Option A/B should handle it; retry up to the spend cap.
  </action>
  <verify>
    <automated>test -f .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-HUMAN-UAT.md && grep -q 'public_port_bind_timeout' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-HUMAN-UAT.md && grep -q 'supervisorctl' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-HUMAN-UAT.md && grep -Eqi 'consistency check|shape1_' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - UAT sheet has S0 (consistency check across docker-inspect env + boot-shape log + .env.prod.example + runbook — finding #11) + S1-S5 each with commands + PASS/FAIL slot + evidence slot.
    - S0 asserts all four surfaces agree on PRIMARY=1×3090@0.30 and FALLBACK=2×3090@0.60 and is a precondition for S2 spend.
    - Sheet includes the supervisorctl/onstart/supervisord capture step (suspect #1/#3 retirement) confirming the 3-way download (qwen + bge-m3 + whisper).
    - Sheet includes a spend-log table referencing the $2.00 cap and a secret-redaction reminder (finding #10).
  </acceptance_criteria>
  <done>UAT scenario sheet (with S0 consistency gate) ready for operator execution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the live UAT (Vast spend within $2.00 cap)</name>
  <files>.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-HUMAN-UAT.md — operator fills sign-offs; no repo source modified</files>
  <what-built>
    The fail-fast + cold-start image is deployed (plan 06). The operator now runs the live force-up UAT to prove the fix on the real iter-1 path and signs each scenario.
  </what-built>
  <action>
    Operator-gated live UAT execution (full per-scenario recipe in how-to-verify). Run S0 (consistency gate) then S1-S5 from 6.6.Y-HUMAN-UAT.md on the live stack, capture evidence (secrets redacted), force-down to 0 instances, track spend ≤ $2.00, and sign each scenario PASS/FAIL.
  </action>
  <how-to-verify>
    Operator executes S0 then S1-S5 from 6.6.Y-HUMAN-UAT.md on the live ai-gateway-dev stack:
    0. S0 (finding #11): run the four consistency-check commands; confirm docker-inspect env + boot-shape log + .env.prod.example + runbook ALL show PRIMARY=1×3090@0.30 + FALLBACK=2×3090@0.60. If any diverges, STOP (do not spend) and fix the divergent surface first.
    1. S1: confirm boot-shape dump (shape1=2×3090 + budget) from docker logs.
    2. S2: force-up → watch FSM to Ready (4 endpoints 200) on a 2×3090 (FALLBACK) pod; record lifecycle/host/geo/dph/wall-clock. If the first host is defective, confirm Option A/B handled it (logged), retry.
    3. S3: SSH the port-bound pod, capture supervisorctl status + /var/log/onstart.log + /var/log/supervisord.log; confirm 3-way download ran (qwen + bge-m3 + whisper) + services launched.
    4. S4: record whether Option A reject and/or Option B 120s fail-fast fired (with log evidence) OR document not-triggered.
    5. S5: force-down → drain + destroy → confirm 0 Vast instances (?api_key= query-param check, key redacted in paste).
    6. Record per-attempt + total Vast spend; STOP if approaching $2.00. Redact all secrets in pasted evidence (finding #10).
    Sign each scenario PASS/FAIL in 6.6.Y-HUMAN-UAT.md and paste evidence.
  </how-to-verify>
  <verify>Operator signs S0-S5 PASS/FAIL in 6.6.Y-HUMAN-UAT.md with pasted evidence (secrets redacted) + total spend; final Vast instance count 0.</verify>
  <acceptance_criteria>
    - S0 consistency check signed PASS (all four surfaces agree on the shape matrix) before any S2 spend — finding #11.
    - S2 recorded a 2×3090 (FALLBACK) pod reaching Ready within budget OR a documented chain of A/B-handled defective hosts + a successful retry within the spend cap.
    - S3 supervisorctl/onstart/supervisord snapshots captured and pasted (or S2 never reached port-bound and that is explicitly recorded with the A/B fail-fast evidence).
    - S5 confirmed 0 Vast instances at end (no orphan).
    - Total phase Vast spend recorded and ≤ $2.00; the UAT file contains no literal VAST_AI_API_KEY value (finding #10).
  </acceptance_criteria>
  <done>Live UAT executed and signed (S0 consistency gate passed first); spend within cap; secrets redacted; no orphaned pod.</done>
  <resume-signal>Type "uat done" with S0-S5 PASS/FAIL + total spend $X.XX</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 6.6.Y-VERIFICATION.md + advance state</name>
  <files>.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-HUMAN-UAT.md (signed results)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md (the §8 hand-off questions to mark resolved)
    - @$HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Author 6.6.Y-VERIFICATION.md with: phase verdict (passed / passed_partial / failed); per-requirement evidence rows for PC-COLD-START-FIX (Option A reject + Option B fail-fast + Option C whisper download + cold-start-to-Ready result) and PC-ENV-PRECEDENCE (hard fail-fast live-confirmed + canonical migration + boot-shape dump + S0 four-surface consistency — finding #11); the resolution status of the RESEARCH-COLD-START §8 hand-off questions (host-specific vs universal from spike + plan 01; suspects #1/#3 from S3); the cumulative Vast spend table vs $2.00 cap; and any carry-forward tech debt (e.g. if a defective host was never sampled so A/B only have unit coverage, OR if the shape-0 allowlist proactive resolution recommended a follow-up code change). If any scenario FAILED, mark verdict passed_partial/failed and note that `/gsd:plan-phase 6.6.Y --gaps` is the next step. Then update STATE.md/ROADMAP per the summary template (mark Phase 6.6.Y plans complete + verdict).
  </action>
  <verify>
    <automated>test -f .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-VERIFICATION.md && grep -Eq 'PC-COLD-START-FIX|PC-ENV-PRECEDENCE' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-VERIFICATION.md && grep -Eqi 'verdict' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md records a verdict and per-requirement evidence for BOTH PC-COLD-START-FIX and PC-ENV-PRECEDENCE, including the S0 four-surface consistency result (finding #11).
    - Cumulative Vast spend table is present and ≤ $2.00.
    - The RESEARCH-COLD-START §8 hand-off questions are each marked resolved or carried-forward.
    - STATE.md/ROADMAP updated to reflect the phase outcome.
  </acceptance_criteria>
  <done>Phase verdict recorded with per-requirement evidence + spend log + consistency result; state advanced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Vast provisioning | live GPU spend; orphan risk if force-down skipped |
| Vast host → gateway | the host can still lie (the failure class under test) |
| evidence file → git | captured query URLs / pod SSH could leak secrets into committed evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.6.Y-07-01 | Denial of service / cost | Orphaned Vast pod after UAT burning budget past the cap | mitigate | S5 mandates force-down + a 0-instance check via ?api_key= query-param; spend-log table enforces the $2.00 stop |
| T-6.6.Y-07-02 | Spoofing | Defective host re-encountered during UAT | mitigate | This is the system under test — Option A/B are expected to handle it; if not, the UAT FAILS and triggers --gaps |
| T-6.6.Y-07-03 | Information disclosure | VAST_AI_API_KEY in ?api_key= during instance inspection + pod SSH key handling, leaking into committed evidence | mitigate | SECRET HYGIENE (finding #10): shell var for the key + REDACT ?api_key= in pasted evidence; acceptance criterion forbids a literal key in the UAT file; POD_DEBUG_SSH_PUBLIC_KEY is an operator-supplied debug key |
| T-6.6.Y-07-04 | Tampering | Shape drift between surfaces silently reintroducing the bug this phase fixes | mitigate | S0 pre-UAT consistency check (finding #11) blocks spend until docker-inspect env + boot log + .env.prod.example + runbook all agree on the shape matrix |
| T-6.6.Y-07-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; UAT is operator shell + doc authoring |
</threat_model>

<verification>
- S0 four-surface consistency check passes before spend (finding #11).
- S2 cold-start to Ready on 2×3090 (FALLBACK) within budget (or A/B-handled retries within cap).
- S3 suspect #1/#3 snapshots captured (3-way download confirmed: qwen + bge-m3 + whisper).
- Option A/B observed or documented-not-triggered.
- Total Vast spend ≤ $2.00; no literal secret in evidence.
- VERIFICATION.md verdict + per-requirement evidence; state advanced.
</verification>

<success_criteria>
Pre-UAT four-surface consistency confirmed (finding #11); live UAT proves the A+B+C bundle + env-precedence migration on the real cold-start path (pod-ON ≤ budget on 2×3090 FALLBACK, or fail-fast-bounded defective hosts within cap), suspects #1/#3 retired via S3 snapshots, spend ≤ $2.00 with redacted secrets, and the phase VERIFICATION records a verdict for both requirements.
</success_criteria>

<output>
Create `.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-07-SUMMARY.md` when done.
</output>
</content>
