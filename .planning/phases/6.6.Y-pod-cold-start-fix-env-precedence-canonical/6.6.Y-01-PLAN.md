---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md
autonomous: false
requirements: [PC-COLD-START-FIX]
user_setup:
  - service: vast.ai
    why: "Live force-up provisioning spike requires real Vast.ai GPU spend (~$0.30) — autonomous mode cannot satisfy paid GPU lifecycle"
    env_vars:
      - name: VAST_AI_API_KEY
        source: "~/.claude/CLAUDE.md (already present in vps-ifix-vm /data/docker/volumes/infra_portainer_data/_data/compose/34/ stack.env)"

must_haves:
  truths:
    - "A second (non-Brazil) Vast host has been provisioned via the gateway force-up path and its port-binding behaviour observed (binds vs never-binds)"
    - "The question 'is offer-side public_ipaddr available in SearchOffers listing' is answered YES or NO with captured evidence"
    - "The branch decision for Option A (search-filter map vs client-side post-SearchOffers reject) is frozen with a documented rationale"
  artifacts:
    - path: ".planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md"
      provides: "n=2 disambiguation evidence + offer-side public_ipaddr availability answer + Option A branch decision + spend log"
      min_lines: 30
  key_links:
    - from: "6.6.Y-01-SPIKE-EVIDENCE.md"
      to: "6.6.Y-03 Option A filter task"
      via: "offer-side public_ipaddr availability answer drives search-filter-map vs client-side-reject branch"
      pattern: "public_ipaddr"
---

<objective>
Run the D-01 disambiguation spike BEFORE the Option A filter heuristic is frozen. Provision ONE pod via the gateway `force-up` path against a different geo/host than iter-1's Brazil host 51096 (host_id 348505), capture the 4-signal evidence (reusing the 06.6.X method), and answer two questions that gate the code waves:

1. **Host-specific vs universal port-bind failure** (RESEARCH-COLD-START §8 Q1): does the 2nd host bind public ports, or also fail like 51096? n=2 sample.
2. **Offer-side `public_ipaddr` availability** (PATTERNS Option A caveat): does the Vast `SearchOffers` *offers* listing expose `public_ipaddr` the same way the *instance* detail does? This decides whether Option A's RFC1918 filter goes in the search-filter map (`vast/types.go`) or as a client-side post-`SearchOffers` reject (`reconciler.go`).

Purpose: prevent freezing a filter heuristic on n=1 evidence and prevent building the filter against a field that does not exist in the offers payload.
Output: `6.6.Y-01-SPIKE-EVIDENCE.md` with the two answers + Option A branch decision + spend log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md

<interfaces>
<!-- Vast instance/offer JSON fields the spike inspects (from gateway/internal/emerg/vast/types.go) -->
Instance struct (instance detail): `SshHost string json:"ssh_host"`, `PublicIPAddr string json:"public_ipaddr"`, `ActualStatus`, `StatusMsg`, `NumGpus int json:"num_gpus"`.
Offer struct (offers listing) — VERIFY in this spike whether `public_ipaddr` is present in the SearchOffers payload.
Vast API auth HARD RULE: use `?api_key=<key>` query-param against `/api/v0/instances/<id>/` and `/api/v0/bundles/`. `Authorization: Bearer` silently returns null fields.
Force-up trigger: `ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary force-up'`
Force-down trigger: `ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary force-down --reason <r>'`
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Live force-up disambiguation spike (operator, ~$0.30 Vast spend)</name>
  <read_first>
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md (§3 the 4-signal capture method; §8 Q1/Q5 the questions to answer)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-ITER1-SIGNALS.md (iter-1 lifecycle 101 baseline to compare against)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md (D-01 spike acceptance, D-05 budget cap $2.00)
  </read_first>
  <what-built>
    This is a live operator spike, not autonomous work. The operator triggers a single primary force-up on the live ai-gateway-dev stack (vps-ifix-vm), watches the provisioned host's port-binding behaviour, and captures evidence. To force a different host than iter-1's host_id 348505 (Brazil), the operator may temporarily add 348505/51096 to a transient blocklist OR rely on Vast picking a fresh offer; the goal is a non-Brazil host sample.
  </what-built>
  <action>
    Operator runs the spike on vps-ifix-vm following the 06.6.X 4-signal method:
    1. Trigger: ssh vps-ifix-vm 'docker exec ai-gateway-dev /gatewayctl primary force-up'. Confirm the offer-pick log line shows a host_id != 348505 and geo != Brazil; if it picks Brazil/348505 again, force-down and retry (or transiently blocklist 51096) until a distinct host is sampled.
    2. Capture per the 06.6.X families, using the Vast API auth HARD RULE (?api_key= query-param, NEVER Authorization: Bearer):
       - Vast-side: poll /api/v0/instances/<id>/?api_key=<key> — record ssh_host, ssh_port, public_ipaddr, actual_status, status_msg, num_gpus, disk_util, gpu_util over the wait window.
       - Gateway-side: docker logs ai-gateway-dev | grep subsys=primary — record offer-pick + any per-strike lines.
       - Network: external nc -zv probe of the published port mapping (if any) every 10s.
    3. ALSO capture the OFFERS payload to answer the public_ipaddr availability question: query the offers/bundles listing endpoint (/api/v0/bundles/?api_key=<key> with the same filter the reconciler uses) and record whether each offer object carries a public_ipaddr field (and ssh_host/direct_port_count) BEFORE provisioning. This is the decisive evidence for the Option A branch.
    4. If pod-ON cold-start reaches Ready (port-bound), capture supervisorctl status + /var/log/onstart.log + /var/log/supervisord.log snapshots (retires suspects #1/#3 — D-03 acceptance, RESEARCH-COLD-START §8 Q6) and force-down cleanly.
    5. If the 2nd host ALSO never binds ports within ~5min, force-down and record that finding (universal failure → Option D allowlist hard-fail must be hardened in W2 alongside Option A).
    Record total Vast spend against the $2.00 phase cap (D-05). Then write 6.6.Y-01-SPIKE-EVIDENCE.md (see Task 2).
  </action>
  <how-to-verify>
    1. Operator confirms a non-Brazil host was sampled (gateway offer-pick log shows host_id != 348505).
    2. Operator confirms the offers-listing payload was inspected for a public_ipaddr field.
    3. Operator records PASS (host bound ports) or FAIL (host never bound) + spend.
  </how-to-verify>
  <acceptance_criteria>
    - The gateway offer-pick log line for this spike names a host_id different from 348505 (iter-1 host).
    - The offers-listing JSON sample is captured and the presence/absence of `public_ipaddr` in each offer object is recorded verbatim.
    - Spend for this spike is recorded in USD and is ≤ $0.40.
  </acceptance_criteria>
  <resume-signal>Type "spike done" with: (a) 2nd host bound ports YES/NO, (b) offers carry public_ipaddr YES/NO, (c) spend $X.XX</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 6.6.Y-01-SPIKE-EVIDENCE.md with the two answers + Option A branch decision</name>
  <read_first>
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md (§5 Option A pros/cons, §8 Q1)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md (the `gateway/internal/emerg/vast/types.go` section — the "if offers don't carry public_ipaddr, the filter must move to a post-SearchOffers client-side reject in reconciler.go mirroring vastutil.FilterBelowCap" branch)
  </read_first>
  <action>
    Author .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md with these sections:
    - **Sample:** instance_id, host_id, geo, dph, shape provisioned.
    - **Answer Q1 (host-specific vs universal):** state whether the 2nd host bound public ports. If YES → host 348505 defect, Option A heuristic stands alone (W2 does NOT need to harden Option D). If NO → universal/Vast-class defect, W2 MUST harden Option D (allowlist hard-fail) alongside Option A — record this as a directive for plan 6.6.Y-03/6.6.Y-04.
    - **Answer Q2 (offer-side public_ipaddr availability):** state YES (offers carry public_ipaddr) → Option A filter goes in the `vast/types.go` DefaultSearchFilter search-filter map; OR NO (offers lack it) → Option A must be a client-side post-SearchOffers reject in reconciler.go mirroring vastutil.FilterBelowCap. Quote the offers-listing JSON sample as evidence.
    - **Frozen Option A branch decision:** one sentence naming which implementation site (types.go map vs reconciler.go client-side) plan 6.6.Y-03 must use.
    - **Suspect #1/#3 snapshot (if port-bound reached):** supervisorctl status + onstart/supervisord log excerpts, OR "not reached — pod never bound ports".
    - **Spend log:** this spike $X.XX; phase running total against $2.00 cap.
  </action>
  <verify>
    <automated>test -f .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md && grep -q 'public_ipaddr' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md && grep -Eqi 'types\.go|reconciler\.go' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists and contains a section answering offer-side `public_ipaddr` availability YES or NO.
    - File names exactly one Option A implementation site (`vast/types.go` search-filter map OR `reconciler.go` client-side reject) as the frozen decision.
    - File records this spike's Vast spend and the phase running total against $2.00.
  </acceptance_criteria>
  <done>SPIKE-EVIDENCE.md committed with both answers, frozen Option A branch decision, and spend log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → Vast API | VAST_AI_API_KEY travels in `?api_key=` query-param; query strings can appear in shell history and process listings |
| Vast host → gateway | untrusted GPU host advertises ssh_host/public_ipaddr; iter-1 proved a host can lie (actual_status=running while ports unbound) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.6.Y-01-01 | Information disclosure | VAST_AI_API_KEY in `?api_key=` query-param appearing in shell history / `ps` / Vast access logs | accept | Operator-only interactive spike on trusted vps-ifix-vm; key already lives in stack.env on that host; query-param is the Vast HARD RULE (Bearer silently nulls fields) so no alternative; key rotation available per CLAUDE.md if exposed |
| T-6.6.Y-01-02 | Spoofing | Vast host advertising false `actual_status=running` while ports never bind | mitigate | This spike's entire purpose is to characterise the lie; downstream W2 Option B fail-fast bounds the blast radius to 120s |
| T-6.6.Y-01-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (operator shell + doc write only) |
</threat_model>

<verification>
- 2nd non-Brazil host sampled (offer-pick log host_id != 348505).
- Offer-side public_ipaddr availability answered with captured JSON.
- Option A branch decision frozen (types.go vs reconciler.go).
- Spend recorded ≤ $0.40, phase total tracked against $2.00.
</verification>

<success_criteria>
SPIKE-EVIDENCE.md exists with: host-specific-vs-universal answer, offer-side public_ipaddr YES/NO + JSON evidence, frozen Option A implementation site, spend log.
</success_criteria>

<output>
Create `.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SUMMARY.md` when done.
</output>
