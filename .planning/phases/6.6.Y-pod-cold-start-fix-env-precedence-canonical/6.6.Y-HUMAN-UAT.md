# 6.6.Y — Human UAT (live force-up on the deployed fail-fast image)

**Operator:** Pedro
**Date:** 2026-06-11
**Stack under test:** `ai-gateway-dev` (vps-ifix-vm, operator-managed `docker compose`, `/opt/ai-gateway-dev`)
**Image:** fail-fast + cold-start bundle (digest `02bcb7b5` / `ead1bc16`), published 2026-06-11T20:00:28Z
**Verdict (operator sign-off):** **passed_partial** (decision "Fechar passed_partial", 2026-06-11)
**Phase Vast spend cap (D-05):** $2.00 — cumulative this UAT-inclusive total ≈ **$0.15 / $2.00** ✔

**Vast API HARD RULE:** instance inspection uses `?api_key=<REDACTED>` query-param only (a `Authorization: Bearer`
header silently nulls offer/instance fields). Key held in a shell var; **no literal `VAST_AI_API_KEY` appears in this file** (finding #10).
**Gateway log evidence below carries no secrets** (the gateway log lines contain no api_key; redaction is a no-op for them but the rule stands).

---

## S0 — Pre-UAT consistency check (review finding #11) — **PASS**

BEFORE any GPU spend, the four shape-bearing surfaces were checked for agreement on the shape pair + caps.
A divergence would re-introduce the exact drift class this phase fixes, so S0 is a hard precondition for S2 spend.

Commands run (2026-06-11):
- `ssh vps-ifix-vm "docker inspect ai-gateway-dev --format '{{range .Config.Env}}{{println .}}{{end}}' | grep -E 'PRIMARY_VAST_(NUM_GPUS|GPU_NAME|PRICE_CAP)'"`
- `ssh vps-ifix-vm 'docker logs ai-gateway-dev 2>&1 | grep -E "shape0_|shape1_"'`
- `grep -E '^PRIMARY_VAST_(NUM_GPUS|PRICE_CAP)_' gateway/.env.prod.example`
- `grep -A2 'Shape pair model' gateway/docs/RUNBOOK-PRIMARY-POD.md`

| Surface | shape0 PRIMARY | shape1 FALLBACK | allowlist | blocklist | extras |
|---------|----------------|-----------------|-----------|-----------|--------|
| `docker inspect` env | 1×RTX 3090 @ 0.30 | 2×RTX 3090 @ 0.60 | 43803,55158 | 55942,45778 | PORT_BIND_BUDGET=120; REJECT_PRIVATE_IP=true |
| boot-shape log (16:37:28-03) | shape0_num_gpus=1 shape0_cap=0.3 | shape1_num_gpus=2 shape1_cap=0.6 | `[43803 55158]` | — | port_bind_budget_seconds=120; reject_private_ip=true |
| `gateway/.env.prod.example` | 1×3090 @ 0.30 | 2×3090 @ 0.60 | 43803,55158 | — | same 8 vars, same values |
| `RUNBOOK-PRIMARY-POD.md` env table | 1×3090 @ 0.30 | 2×3090 @ 0.60 | 43803,55158 | — | same |

**S0 RESULT: PASS** — all four surfaces AGREE on PRIMARY=1×3090@0.30 and FALLBACK=2×3090@0.60. Spend authorized.

---

## S1 — Clean boot + resolved shape — **PASS**

The gateway booted clean on the migrated (canonical) env: **0 `ErrLegacyPrimaryEnv`**, **0 legacy primary vars** in the container.
The boot-shape dump (16:37:28-03) emitted shape0=1×3090, shape1=2×3090, allowlist `[43803 55158]`, `port_bind_budget_seconds=120`,
`reject_private_ip=true` — carried from plan 06 and re-asserted here.

**S1 RESULT: PASS** — clean boot on migrated env; resolved shape pair + budget visible in boot dump.

---

## S2 — Cold-start to Ready on FALLBACK — **PARTIAL (fail-fast-bounded; Ready not reached)**

Live force-up on the fail-fast image (`/gatewayctl primary force-up`), published 2026-06-11T20:00:28Z.

Gateway log sequence (verbatim, no secrets):
```
17:00:28-03 "primary force-up: provisioning by operator request"
17:00:29-03 "primary allowlist exhausted for shape; broadening to full qualified search" allowlist="[43803 55158]" shape=0
17:00:29-03 "primary offer reject: RFC1918 public_ipaddr" dropped=1 remaining=19 shape=0 pass=broaden   ← OPTION A FIRED LIVE
17:00:29-03 "primary offer picked" offer_id=24998116 machine_id=24953 host_id=102175 dph=0.1296 geo="Norway, NO" shape=0 gpu_shape="1xRTX 3090"
17:40:31-03 ERROR "primary provisionLifecycle returned error" lifecycle_id=103 err="primary: cold-start budget exhausted"
```

Outcome: Vast instance **40621316** (machine 24953, host 102175, Norway) **loaded the cached image** ("Successfully loaded")
but stayed `actual_status=created` — it **never transitioned to running**. The cold-start budget (2400s) closed the lifecycle:
`shutdown_reason=health_timeout`, `BestEffortDestroy` succeeded.

Lifecycle row 103: STARTED 2026-06-11T20:00:28Z · ENDED 20:40:31Z · TRIGGER `operator_force_up` · VAST_INST 40621316 · DPH 0.1296 · SHUTDOWN `health_timeout`.

**S2 RESULT: PARTIAL** — Ready-within-budget NOT demonstrated (no viable host: instance stuck in `created`).
The failure was **bounded and destroyed cleanly** (vs the previous silent 17-min lie). Root blocker is OPERATIONAL,
not code: the broadened shape-0 search lands on junk hosts; the allowlist machines (43803/55158) are not rentable for their shapes today.

---

## S3 — Suspect #1/#3 retirement (supervisorctl / onstart.log / supervisord.log) — **NOT CAPTURED (carried forward)**

S3 requires a **port-bound** pod to SSH into. No port-bound pod was reached this run (instance 40621316 stuck in `created`,
never running, so no inbound SSH/port mapping). Across the phase, n=3 distinct defective hosts were sampled and none became
port-bound + reachable:
- Brazil 348505 (iter-1): running-but-unreachable
- South Africa 317118: running-but-unreachable
- Norway 102175 (this run): stuck-`created`

**supervisorctl status + /var/log/onstart.log + /var/log/supervisord.log snapshots were NOT captured.**
Suspects **#1 (onstart never ran the 3-way download)** and **#3 (supervisord never launched the services)** are
**NOT retired — carried forward.** The onstart 3-way parallel download (qwen + bge-m3 + whisper, whisper restored) is
**code-proven** (plan 06 deploy evidence) but not yet **live-proven** on a port-bound host.

**S3 RESULT: NOT CAPTURED** — no port-bound host sampled; suspects #1/#3 carried forward.

---

## S4 — Option A / Option B observability — **A: FIRED LIVE · B: NOT-TRIGGERED (documented)**

- **Option A (`PRIMARY_VAST_REJECT_PRIVATE_IP=true`, offer-time RFC1918 drop): OBSERVED FIRING LIVE.**
  Log (item S2): `"primary offer reject: RFC1918 public_ipaddr" dropped=1 remaining=19 shape=0 pass=broaden`.
- **Option B (`PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS=120`, runtime gateway-reachability fail-fast): NOT TRIGGERED this run.**
  By design (plan 03), Option B's budget anchor is **contiguous RUNNING time** — the sampled Norway host never reached
  `running`, so the 120s port-bind window never armed. Documented as **not-triggered-because-no-running-but-unbound-host-sampled.**
  Option B's behavior is proven by the deterministic unit test `TestWaitForReady_PublicPortBindTimeout` (budget=0, plan 03),
  and its `closure_reason=public_port_bind_timeout` is wired.

**S4 RESULT: Option A live-fired; Option B not-triggered (documented + unit-proven).**

---

## S5 — Clean force-down (no orphan) — **PASS**

The cold-start budget closure invoked `BestEffortDestroy` which succeeded; the Vast instances list returned
**0 instances** after closure (`?api_key=<REDACTED>` query-param check) — no orphan. FSM returned to `asleep`.
(An explicit `force-down` was unnecessary because the budget closure already drained + destroyed.)

**S5 RESULT: PASS** — 0 Vast instances at end, no orphan.

---

## Spend log (phase cumulative vs D-05 $2.00 hard cap)

| Item | Instance | Geo | Duration | Rate | Cost |
|------|----------|-----|----------|------|------|
| 6.6.Y-01 spike | 40474442 | South Africa | ~25 min | $0.1406/h | ≈ $0.06 |
| 6.6.Y-04 allowlist refresh | — | — | read-only API | — | $0.00 |
| 6.6.Y-06 deploy | — | — | no GPU | — | $0.00 |
| 6.6.Y-07 UAT | 40621316 | Norway | ~40 min | $0.1296/h | ≈ $0.09 |
| **TOTAL** | | | | | **≈ $0.15 / $2.00 ✔** (well under cap) |

**Secret hygiene:** `VAST_AI_API_KEY` never appears literally in this file; all instance queries use `?api_key=<REDACTED>`.

---

## Operator sign-off

| Scenario | Result |
|----------|--------|
| S0 — four-surface consistency gate | **PASS** |
| S1 — clean boot + resolved shape | **PASS** |
| S2 — cold-start to Ready (FALLBACK) | **PARTIAL** (fail-fast-bounded; no viable host) |
| S3 — suspect #1/#3 snapshots | **NOT CAPTURED** (no port-bound host; carried forward) |
| S4 — Option A / Option B | **A FIRED LIVE · B not-triggered (documented)** |
| S5 — clean force-down (0 instances) | **PASS** |

**Operator decision (Pedro, 2026-06-11): "Fechar passed_partial".**
PC-ENV-PRECEDENCE is COMPLETE; PC-COLD-START-FIX is PARTIAL (Option A live-fired + Option B wired/unit-proven +
defective host bounded + destroyed cleanly; live Ready-within-budget not demonstrated for lack of a viable host).
Standing follow-up: the Option D no-broaden hard-fail (recommended in 6.6.Y-04-ALLOWLIST-REFRESH.md) +
allowlist rentability refresh + retire suspects #1/#3 on the next port-bound host.
