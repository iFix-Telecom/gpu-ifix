---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 05
subsystem: infra
tags: [portainer, stack-env, env-precedence, vast, primary-pod]

# Dependency graph
requires:
  - phase: 6.6.Y-02
    provides: fail-fast presence semantics (os.LookupEnv) that make this migration a hard pre-condition
  - phase: 6.6.Y-04
    provides: compose with legacy rows removed (committed, pending push) that plan 06's GitOps re-pull will deliver
provides:
  - "Portainer stack 34 env migrated: 3 legacy primary-shape vars removed, 6 canonical + 2 cold-start vars present"
  - "Running pre-fail-fast container recreated on migrated env and healthy"
  - "Documented empty-string legacy ressalva: deployed compose rows still inject empty legacy vars until plan 06 re-pull"
affects: [6.6.Y-06, 6.6.Y-07]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Portainer GitOps env edits MUST go through Advanced mode paste + Update the stack in one action — separate save/apply loses edits"

key-files:
  created:
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md
  modified: []

key-decisions:
  - "D-04: VAST_PRICE_CAP_DPH=0.40 stays as emerg-subsystem cap; primary caps live only in PRIMARY_VAST_PRICE_CAP_{PRIMARY,FALLBACK}"
  - "Cold-start vars (PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS=120, PRIMARY_VAST_REJECT_PRIVATE_IP=true) set explicitly in stack env rather than relying on code defaults"

patterns-established:
  - "Stack env is the single source for shape vars; compose defaults are fallback only"

requirements-completed: [PC-ENV-PRECEDENCE]

# Metrics
duration: 65min (operator round-trips included)
completed: 2026-06-11
---

# Phase 6.6.Y Plan 05: Portainer Stack Env Migration Summary

**Stack 34 env migrated to canonical shape vars (PRIMARY 1×3090@0.30, FALLBACK 2×3090@0.60) + cold-start vars; pre-fail-fast container recreated healthy; legacy-free stack env confirmed before plan 06 deploy.**

## Performance

- **Duration:** ~65 min (3 operator UI round-trips — first two env saves did not persist in Portainer DB)
- **Completed:** 2026-06-11T01:45Z
- **Tasks:** 3 (backup/before-capture → operator env migration → validation + evidence)

## Accomplishments
- 3 legacy vars (PRIMARY_GPU_NAME, PRIMARY_NUM_GPUS, PRIMARY_VAST_PRICE_CAP_DPH) removed from stack env (grep count 0)
- 6 canonical per-shape vars + 2 cold-start vars present, matching the canonical shape matrix
- Container `ai-gateway-dev` recreated 01:40Z on migrated env, health=healthy
- Emerg `VAST_PRICE_CAP_DPH=0.40` preserved (D-04 documented)
- Critical ressalva recorded: deployed compose still injects empty-string legacy vars; plan 06 MUST ship the corrected compose (plan 04) with the fail-fast image or the gateway crash-loops

## Task Commits

Evidence + summary committed together (docs-only plan, live-ops work happened in Portainer):
1. **Evidence + SUMMARY** - see `docs(6.6.Y-05)` commit

## Files Created/Modified
- `6.6.Y-05-STACK-MIGRATION-EVIDENCE.md` - before/after env diff, redeploy health, incidents, D-04

## Decisions Made
- See key-decisions frontmatter.

## Deviations from Plan

### Operator-loop deviation
Portainer env edits silently failed to persist twice (saved env reverted in UI; 00:31Z redeploy regenerated old stack.env). Resolved via Advanced-mode full paste + Update the stack in one action. Documented in evidence file as operational pattern.

## Issues Encountered
- OpenAI `sk-proj-` bearer values had embedded line breaks in the previous env; reassembled into single lines during the final paste.

## Self-Check: PASSED
- stack.env: 0 legacy rows; canonical + cold-start rows verified via SSH grep
- Container started 2026-06-11T01:40:24Z, health=healthy
- Evidence file ≥20 lines, secrets omitted

## Next Phase Readiness
- Plan 6.6.Y-06 unblocked: env migration confirmed complete BEFORE fail-fast image deploy (must_have 4)
- Plan 06 must push develop (incl. plan 04 compose) so GitOps re-pull removes the empty-string legacy rows

---
*Phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical*
*Completed: 2026-06-11*
