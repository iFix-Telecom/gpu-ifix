---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 01
subsystem: infra
tags: [vast.ai, gpu, cold-start, gateway, spike, public_ipaddr, force-up]

requires:
  - phase: 06.6.X-pod-cold-start-env-precedence-audit
    provides: 4-signal capture method, iter-1 Brazil host 348505 n=1 baseline
provides:
  - n=2 disambiguation evidence (Brazil + South Africa both fail external reachability → universal failure)
  - offer-side public_ipaddr FIELD-AVAILABILITY answer (YES, 55/55 offers, 0 RFC1918)
  - Option A effectiveness directive (effective for RFC1918 subclass only; Option B is primary catch)
  - Option D directive (universal failure → allowlist hard-fail must be hardened in W2)
  - confirmed silent empty-URLs poll branch live (plan 6.6.Y-03 log target)
affects: [6.6.Y-03, 6.6.Y-04]

tech-stack:
  added: []
  patterns:
    - "Vast `ports` map is NOT a reliable readiness signal — gate fail-fast on gateway-observable URL reachability, not the ports map"

key-files:
  created:
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md
  modified: []

key-decisions:
  - "Failure mode is universal (n=2 of 2 hosts unreachable), not Brazil-specific → W2 must harden Option D alongside Option A"
  - "Offers carry public_ipaddr → client-side RejectPrivateIPOffers is effective at offer time, but only for the RFC1918 subclass; Option B (runtime 120s fail-fast) is the primary catch for the observed timeout-on-public-IP mode"
  - "Vast ports map is a lie (populated + actual_status=running while externally unreachable) → fail-fast must gate on URL reachability"

patterns-established:
  - "Pattern: spike-driven directive — SPIKE-EVIDENCE.md answers FIELD-AVAILABILITY only, downstream plan owns implementation site (review finding #4)"

requirements-completed: [PC-COLD-START-FIX]

duration: ~15min (Task 2 doc authoring; Task 1 live spike run by operator)
completed: 2026-06-10
---

# Phase 6.6.Y Plan 01: Force-Up Disambiguation Spike Summary

**n=2 live force-up spike proving the pod cold-start failure is universal (Brazil + South Africa both bind Docker ports but stay externally unreachable), and confirming Vast offer objects carry `public_ipaddr` (55/55, 0 RFC1918) so the client-side Option A reject is effective at offer time for the RFC1918 subclass only.**

## Performance

- **Duration:** ~15 min (Task 2 doc authoring) — Task 1 live spike executed by operator with user authorization
- **Completed:** 2026-06-10
- **Tasks:** 2 (Task 1 operator live spike; Task 2 evidence authoring)
- **Files created:** 1

## Accomplishments
- Provisioned a 2nd non-Brazil host (South Africa, host_id 317118) via gateway `force-up`, distinct from iter-1 host 348505 — distinct-host requirement satisfied without re-roll.
- Disambiguated the failure: ports bind at the Docker/Vast-API layer (`actual_status=running`, populated `ports` map) but are externally unreachable (timeout, not refused) from TWO vantage points → universal, not Brazil-specific.
- Answered the gating FIELD-AVAILABILITY question: Vast offer objects DO carry `public_ipaddr` (55/55 offers non-empty, 0 RFC1918 in sample).
- Confirmed live the silent empty-URLs poll branch (zero reconciler lines for 40+ min) — the exact target for plan 6.6.Y-03's per-strike logging.
- Recorded Option A effectiveness + Option D directives for W2, and a $0.06 spend log against the $2.00 cap.

## Task Commits

1. **Task 1: Live force-up disambiguation spike** - executed by operator (orchestrator ran live with user authorization); evidence captured to transient artifacts
2. **Task 2: Write 6.6.Y-01-SPIKE-EVIDENCE.md** - `470b593` (docs)

## Files Created/Modified
- `.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-01-SPIKE-EVIDENCE.md` - n=2 disambiguation evidence, public_ipaddr FIELD-AVAILABILITY answer, Option A effectiveness directive, Option D universal-failure directive, redacted spend log (100 lines)

## Decisions Made
- **Universal failure classification:** with n=2 of 2 sampled hosts unreachable, the failure is treated as universal-until-proven-otherwise rather than host-specific — drives W2 to harden Option D (allowlist hard-fail) alongside Option A.
- **Option A is effective but narrow:** offers carry `public_ipaddr`, so the client-side reject can drop RFC1918 advertisers at offer time — but neither observed failure advertised an RFC1918 IP, so Option A is a guard for the RFC1918 subclass only; Option B (runtime fail-fast) is the primary catch.
- **Readiness signal:** the Vast `ports` map must NOT be used as a readiness signal; fail-fast gates on gateway-observable URL reachability.

## Deviations from Plan

None - plan executed exactly as written. SPIKE-EVIDENCE.md satisfies all must_haves: n=2 disambiguation, public_ipaddr FIELD-AVAILABILITY YES + redacted JSON evidence, Option A effectiveness directive (no implementation-site decision), Option D directive (universal failure), redacted spend log.

## Issues Encountered
- Both sampled hosts (Brazil iter-1, South Africa iter-2) reached `actual_status=running` with populated Docker port maps yet stayed externally unreachable (timeout, not refused) — this IS the characterized failure, captured as the spike's primary finding rather than a blocker.
- Suspect #1/#3 (onstart/supervisord log inspection) could not be retired because no path to the host opened (sshd mapping timed out from both vantage points) — recorded as "not reached" per plan allowance.

## User Setup Required
None - external Vast.ai spend was the operator's live spike (already completed); no further configuration.

## Next Phase Readiness
- 6.6.Y-03 has its two gating answers: implement client-side `RejectPrivateIPOffers` (effective at offer time, RFC1918 subclass), add the per-strike log on the now-confirmed silent empty-URLs poll branch, and gate Option B fail-fast on URL reachability (NOT the Vast ports map).
- 6.6.Y-04 / W2 must harden Option D (allowlist hard-fail) given the universal failure classification.
- STATE.md / ROADMAP.md intentionally NOT updated by this continuation agent (per orchestrator instruction).

## Self-Check: PASSED
- SPIKE-EVIDENCE.md exists (100 lines), `public_ipaddr` present, no literal API key (`api_key=<8+ char>` grep clean).
- Commit `470b593` present in git log.

---
*Phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical*
*Completed: 2026-06-10*
