---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 05
type: execute
wave: 3
depends_on: [6.6.Y-02, 6.6.Y-03, 6.6.Y-04]
files_modified:
  - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md
autonomous: false
requirements: [PC-ENV-PRECEDENCE]

must_haves:
  truths:
    - "The Portainer stack env at compose path 34 no longer contains any legacy primary-shape var (PRIMARY_NUM_GPUS, PRIMARY_GPU_NAME, PRIMARY_VAST_PRICE_CAP_DPH)"
    - "The 6 canonical per-shape vars are present in the stack env matching the canonical shape matrix (PRIMARY=1×3090@0.30, FALLBACK=2×3090@0.60) — the standing 2×3090 config is the FALLBACK slot, NOT the primary"
    - "The currently-running container (pre-fail-fast image) has picked up the migrated env via stack redeploy and is still healthy"
    - "This migration is confirmed complete BEFORE plan 6.6.Y-06 deploys the fail-fast image"
  artifacts:
    - path: ".planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md"
      provides: "Before/after stack env diff + redeploy confirmation + health check + D-04 VAST_PRICE_CAP_DPH decision update"
      min_lines: 20
  key_links:
    - from: "Portainer stack env (compose path 34)"
      to: "plan 6.6.Y-06 fail-fast image deploy"
      via: "env must be legacy-free before the fail-fast image boots, else crash-loop"
      pattern: "PRIMARY_VAST_NUM_GPUS"
---

<objective>
Execute the D-04 SAFETY-CRITICAL operator-gated stack.env migration. The hard fail-fast image (plan 6.6.Y-02) REFUSES to boot when legacy vars are present; the current Portainer stack HAS them set (06.6.X-AUDIT-RUNTIME-ENV). This plan migrates the live stack env to canonical names and confirms the running (pre-fail-fast) container picks them up — ALL BEFORE plan 6.6.Y-06 deploys the fail-fast image. Reversing this order would crash-loop the gateway.

Stack location: Portainer compose path `/data/docker/volumes/infra_portainer_data/_data/compose/34/` (NOT /opt/ai-gateway-dev — deprecated 2026-06-05). Operator edits via the Portainer stack UI env editor.

This is `autonomous: false` — only the operator can edit the Portainer stack env and trigger the redeploy.

**Canonical shape correction (review finding #1):** the prior revision of this plan set `PRIMARY_VAST_NUM_GPUS_PRIMARY=2` / `PRICE_CAP_PRIMARY=0.60`, which conflicted with plans 04/06/07 and the code defaults. The CORRECT migration sets the FULL per-shape pair matching the canonical shape matrix: PRIMARY (shape 0) = 1×3090 @ 0.30, FALLBACK (shape 1) = 2×3090 @ 0.60. The standing 2×3090 config is the FALLBACK slot (legacy PRIMARY_NUM_GPUS=2 mapped to PrimaryVastNumGPUsFallback per config.go:217). The 2×3090 boot-shape that plans 06/07 expect is therefore SHAPE 1, surfaced by the boot-shape dump's shape1_* fields.

**D-04 explicit decision update (review finding #8):** D-04's literal wording lists removing the unprefixed `VAST_PRICE_CAP_DPH`. Plans 6.6.Y-02/04 grep-confirmed that `VAST_PRICE_CAP_DPH` (config.go:427) is the EMERG subsystem cap, NOT a primary legacy alias. **DECISION UPDATE (recorded here per finding #8): VAST_PRICE_CAP_DPH=0.40 is PRESERVED in the stack env; D-04's "remove VAST_PRICE_CAP_DPH" wording is superseded because it is emerg-subsystem config, not primary drift.** The executor faces no conflict: KEEP VAST_PRICE_CAP_DPH=0.40, remove only the 3 prefixed primary legacy vars.

Purpose: zero-downtime migration that guarantees the next image deploy doesn't crash-loop.
Output: 6.6.Y-05-STACK-MIGRATION-EVIDENCE.md with before/after env + redeploy + health confirmation + the D-04 decision update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md

<canonical_shape_matrix>
<!-- SINGLE SOURCE OF TRUTH — see plan 6.6.Y-01 <canonical_shape_matrix>. -->
Shape 0 PRIMARY  = 1 x RTX 3090 @ 0.30 -> PRIMARY_VAST_NUM_GPUS_PRIMARY=1,  PRIMARY_VAST_GPU_NAME_PRIMARY="RTX 3090",  PRIMARY_VAST_PRICE_CAP_PRIMARY=0.30  (searched first)
Shape 1 FALLBACK = 2 x RTX 3090 @ 0.60 -> PRIMARY_VAST_NUM_GPUS_FALLBACK=2, PRIMARY_VAST_GPU_NAME_FALLBACK="RTX 3090", PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60 (STANDING config; allowlist 43803/55158)
The stack env must set ALL SIX. The 2x3090 standing config = the FALLBACK shape (shape1_* in the boot dump).

<interfaces>
<!-- D-04 migration recipe (corrected per findings #1 + #8) -->
REMOVE from stack env: PRIMARY_NUM_GPUS, PRIMARY_GPU_NAME, PRIMARY_VAST_PRICE_CAP_DPH (the 3 prefixed primary legacy vars).
KEEP: VAST_PRICE_CAP_DPH=0.40 (unprefixed EMERG subsystem var, config.go:427). D-04 DECISION UPDATE (finding #8): the literal D-04 "remove VAST_PRICE_CAP_DPH" wording is superseded — KEEP it; it is emerg config, not primary drift.
SET canonical per the shape matrix (finding #1): PRIMARY_VAST_NUM_GPUS_PRIMARY=1, PRIMARY_VAST_GPU_NAME_PRIMARY="RTX 3090", PRIMARY_VAST_PRICE_CAP_PRIMARY=0.30, PRIMARY_VAST_NUM_GPUS_FALLBACK=2, PRIMARY_VAST_GPU_NAME_FALLBACK="RTX 3090", PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60. Use the PRIMARY_VAST_MACHINE_ALLOWLIST value confirmed by 6.6.Y-04 ALLOWLIST-REFRESH.md. Also set PRIMARY_VAST_REJECT_PRIVATE_IP=true and PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS=120.
Verify container env after redeploy: ssh vps-ifix-vm 'docker inspect ai-gateway-dev --format "{{range .Config.Env}}{{println .}}{{end}}" | grep -E "PRIMARY_(NUM_GPUS|GPU_NAME|VAST_)|VAST_PRICE_CAP_DPH"'
Health after redeploy: ssh vps-ifix-vm 'docker exec ai-gateway-dev wget -qO- http://localhost:<port>/health' OR docker ps health status.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator migrates Portainer stack env to canonical (BEFORE fail-fast image deploy)</name>
  <files>Portainer stack env (compose path 34) — no repo files modified by this task</files>
  <read_first>
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md (D-04 full recipe + stack path)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-04-ALLOWLIST-REFRESH.md (confirmed allowlist value to set)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md (§5 mitigation step 1 — per-host migration)
  </read_first>
  <what-built>
    Plans 6.6.Y-02/03/04 produced the fail-fast code + canonical doc surfaces (merged on develop) but NOT yet deployed. This task is the operator-only env migration on the live stack that must precede the image deploy. Nothing is auto-built here.
  </what-built>
  <action>
    Operator-only Portainer stack env migration (full recipe in how-to-verify). Remove the 3 prefixed legacy primary-shape vars, KEEP the emerg VAST_PRICE_CAP_DPH=0.40 (D-04 decision update — finding #8), set the FULL canonical per-shape pair matching the shape matrix (PRIMARY=1/RTX 3090/0.30, FALLBACK=2/RTX 3090/0.60 — finding #1) + allowlist (from 6.6.Y-04) + the 2 cold-start vars, redeploy on the current pre-fail-fast image, and capture before/after env. Must complete BEFORE plan 6.6.Y-06 deploys the fail-fast image.
  </action>
  <how-to-verify>
    Operator, on the live ai-gateway-dev stack (Portainer compose path 34):
    1. Capture the BEFORE env: ssh vps-ifix-vm 'docker inspect ai-gateway-dev --format "{{range .Config.Env}}{{println .}}{{end}}" | grep -E PRIMARY_'.
    2. In the Portainer stack env editor: REMOVE PRIMARY_NUM_GPUS, PRIMARY_GPU_NAME, PRIMARY_VAST_PRICE_CAP_DPH. KEEP the emerg VAST_PRICE_CAP_DPH=0.40 (grep-confirmed emerg var — do NOT remove; D-04 decision update, finding #8). SET the full canonical pair (finding #1): PRIMARY_VAST_NUM_GPUS_PRIMARY=1, PRIMARY_VAST_GPU_NAME_PRIMARY="RTX 3090", PRIMARY_VAST_PRICE_CAP_PRIMARY=0.30, PRIMARY_VAST_NUM_GPUS_FALLBACK=2, PRIMARY_VAST_GPU_NAME_FALLBACK="RTX 3090", PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60, plus PRIMARY_VAST_MACHINE_ALLOWLIST per 6.6.Y-04, plus PRIMARY_VAST_REJECT_PRIVATE_IP=true and PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS=120.
    3. Redeploy the stack — STILL on the current pre-fail-fast image (validates env without fail-fast crash-loop risk).
    4. Capture the AFTER env (same docker inspect grep): zero legacy primary vars, the 6 canonical vars present (NUM_GPUS_PRIMARY=1, NUM_GPUS_FALLBACK=2), emerg VAST_PRICE_CAP_DPH still 0.40.
    5. Confirm the container is healthy after redeploy.
  </how-to-verify>
  <verify>Operator pastes the AFTER `docker inspect` env grep proving legacy primary vars absent, the 6 canonical vars present (PRIMARY=1, FALLBACK=2), emerg VAST_PRICE_CAP_DPH=0.40 preserved, container healthy.</verify>
  <acceptance_criteria>
    - AFTER `docker inspect` env shows NO PRIMARY_NUM_GPUS, PRIMARY_GPU_NAME, or PRIMARY_VAST_PRICE_CAP_DPH.
    - AFTER env shows PRIMARY_VAST_NUM_GPUS_PRIMARY=1 AND PRIMARY_VAST_NUM_GPUS_FALLBACK=2 (matches the canonical shape matrix — finding #1; the 2×3090 standing config is the FALLBACK slot).
    - AFTER env shows PRIMARY_VAST_PRICE_CAP_PRIMARY=0.30 AND PRIMARY_VAST_PRICE_CAP_FALLBACK=0.60.
    - AFTER env still shows VAST_PRICE_CAP_DPH=0.40 (emerg var preserved — D-04 decision update).
    - Container reports healthy after the redeploy on the current (pre-fail-fast) image.
  </acceptance_criteria>
  <done>Live stack env migrated to the full canonical per-shape pair (PRIMARY=1, FALLBACK=2) with emerg var preserved; current-image container healthy; ready to gate the fail-fast deploy.</done>
  <resume-signal>Type "stack migrated" with the AFTER env grep output pasted (proving legacy gone + canonical PRIMARY=1/FALLBACK=2 present + emerg preserved + container healthy)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 6.6.Y-05-STACK-MIGRATION-EVIDENCE.md</name>
  <files>.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md (D-04 ordering rationale)
  </read_first>
  <action>
    Author the evidence file with: BEFORE env grep, AFTER env grep, the exact var changes applied (the full 6-var canonical pair: PRIMARY=1/0.30, FALLBACK=2/0.60 per the shape matrix — finding #1), the explicit D-04 DECISION UPDATE note (D-04 literal wording said remove VAST_PRICE_CAP_DPH but plans 02/04 grep-confirmed it is the emerg var — it is PRESERVED; finding #8), redeploy confirmation, post-redeploy health status, and an explicit "SAFE TO DEPLOY FAIL-FAST IMAGE (plan 6.6.Y-06)" gate line. Record $0 Vast spend (no provisioning in this plan).
  </action>
  <verify>
    <automated>test -f .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md && grep -q 'SAFE TO DEPLOY' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md && grep -q 'VAST_PRICE_CAP_DPH' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md && grep -Eq 'PRIMARY_VAST_NUM_GPUS_PRIMARY=1|NUM_GPUS_PRIMARY.*1' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with BEFORE and AFTER env captures.
    - File records the full canonical pair applied (PRIMARY=1/0.30, FALLBACK=2/0.60 — finding #1).
    - File contains the explicit "SAFE TO DEPLOY FAIL-FAST IMAGE" gate line.
    - File documents the D-04 VAST_PRICE_CAP_DPH-preserved decision update (finding #8).
  </acceptance_criteria>
  <done>Migration evidence recorded with the full canonical pair, the D-04 decision update, and the explicit deploy-safety gate for plan 6.6.Y-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Portainer stack env | manual env edit on the live production-dev stack |
| stack env → gateway container | env values govern provisioning shape + boot fail-fast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.6.Y-05-01 | Denial of service | Deploying fail-fast image BEFORE migrating env → gateway crash-loop | mitigate | This plan (W3) is ordered BEFORE plan 6.6.Y-06 (W4) deploy; the SAFE-TO-DEPLOY gate line is the explicit handoff; redeploy on the current image validates env without fail-fast risk |
| T-6.6.Y-05-02 | Tampering | Operator accidentally removing the emerg VAST_PRICE_CAP_DPH (D-04 literal wording) → emerg pod cap regression | mitigate | Explicit KEEP instruction + D-04 decision update (finding #8) + AFTER-env acceptance criterion asserting VAST_PRICE_CAP_DPH=0.40 still present |
| T-6.6.Y-05-03 | Tampering | Wrong shape set (PRIMARY=2 instead of 1) → boot dump + UAT diverge from docs | mitigate | Full canonical pair pinned to the shape matrix (finding #1); acceptance criterion asserts PRIMARY=1 AND FALLBACK=2 |
| T-6.6.Y-05-04 | Tampering | Wrong allowlist set → shape-0 broaden cascade recurs | mitigate | Allowlist value sourced from plan 6.6.Y-04 ALLOWLIST-REFRESH.md (live-verified + proactive shape-0 resolution) |
| T-6.6.Y-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; Portainer env edit + doc write only |
</threat_model>

<verification>
- AFTER stack env: zero legacy primary vars; full canonical pair present (PRIMARY=1/0.30, FALLBACK=2/0.60); emerg VAST_PRICE_CAP_DPH=0.40 preserved.
- Container healthy post-redeploy on the current image.
- STACK-MIGRATION-EVIDENCE.md carries the SAFE-TO-DEPLOY gate line + the D-04 decision update.
</verification>

<success_criteria>
Live stack env migrated to the full canonical per-shape pair (PRIMARY 1×3090@0.30, FALLBACK 2×3090@0.60 — matching the shape matrix) with the emerg var preserved (D-04 decision update recorded), validated on the current image with zero downtime, and an explicit gate authorizing the plan 6.6.Y-06 fail-fast image deploy.
</success_criteria>

<output>
Create `.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-SUMMARY.md` when done.
</output>
</content>
