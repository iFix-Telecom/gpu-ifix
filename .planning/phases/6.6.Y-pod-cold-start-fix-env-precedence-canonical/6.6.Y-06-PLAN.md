---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 06
type: execute
wave: 4
depends_on: [6.6.Y-05]
files_modified:
  - gateway/internal/primary/onstart.go
  - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md
autonomous: false
requirements: [PC-COLD-START-FIX]

must_haves:
  truths:
    - "onstart spawns 3 parallel weight downloads (Qwen + bge-m3 + whisper) and waits on all three before launching services"
    - "Whisper weights env guards are restored (PRIMARY_WHISPER_WEIGHTS_KEY/SHA256 required) and whisper tarball is extracted to /weights/whisper"
    - "The onstart length-regression test stays green (<14KB)"
    - "The fail-fast gateway image is deployed to ai-gateway-dev and boots cleanly (no ErrLegacyPrimaryEnv crash) on the migrated stack env"
  artifacts:
    - path: "gateway/internal/primary/onstart.go"
      provides: "3-way parallel whisper download wired into onstart (Option C, D-03)"
      contains: "WHISPER_PID"
    - path: ".planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md"
      provides: "Image build/push (with GHCR PAT-retag fallback) + clean boot confirmation + boot-shape log capture"
      min_lines: 20
  key_links:
    - from: "gateway/internal/primary/onstart.go"
      to: "pod /weights/whisper"
      via: "download_with_verify whisper tarball + tar -xzf"
      pattern: "/weights/whisper"
    - from: "fail-fast image"
      to: "ai-gateway-dev container"
      via: "boots without ErrLegacyPrimaryEnv on migrated stack"
      pattern: "primary reconciler started|shape0_num_gpus"
---

<objective>
Land Option C (whisper 3-way onstart download, D-03 — ordered AFTER A+B so the next UAT actually reaches the weights stage) and deploy the fail-fast gateway image onto the migrated stack. Per D-03 this uses onstart download (NOT image pre-bake) reusing the existing 11.2 download-weights 3-way infra.

This is `autonomous: false` because the deploy step (image build/push + container recreate) is operator-gated, and GHCR push from GHA is BROKEN org-wide (memory ghcr-actions-install-broken) so the deploy may need a PAT-retag fallback.

Steps:
1. **onstart.go (code, autonomous):** restore whisper env guards, add the THIRD `download_with_verify ... & WHISPER_PID=$!` spawn, extend `wait` + add `tar -xzf /weights/whisper/...`, bump the "2 parallel"/"2 downloads" echo strings to "3", keep the onstart length test (<14KB) green.
2. **Deploy (operator-gated):** build + push the gateway image (fail-fast + cold-start code from plans 02/03 + this onstart change); if GHA GHCR push fails, use the PAT-retag fallback. Pull + recreate ai-gateway-dev on the MIGRATED stack (plan 6.6.Y-05 gate). Confirm it boots WITHOUT ErrLegacyPrimaryEnv and emits the boot-shape log dump (plan 02) showing the 2×3090 fallback shape + port-bind budget.

Purpose: get the weights stage reachable for the live UAT (plan 6.6.Y-07) and prove the fail-fast image boots clean on the migrated env.
Output: onstart.go change (build green, length test green) + 6.6.Y-06-DEPLOY-EVIDENCE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md

<interfaces>
<!-- onstart.go current state (extracted) -->
:71 comment already says "spawn 3 parallel weight downloads (Qwen GGUF + Whisper tarball + ...)" — code drifted to 2 after Phase 11.1 D-A4.
:114 "# Phase 11.1 D-A4: PRIMARY_WHISPER_WEIGHTS_* removed (STT shrunk to tier-1-only)." — restore the guards here.
:129-137 download_with_verify() helper — reuse verbatim (aria2c --max-tries=50 --retry-wait=15 + sha256sum -c).
:139 mkdir -p /weights/qwen /weights/bge-m3 /app/templates — extend with /weights/whisper.
:141 echo "...: spawning 2 parallel downloads" — bump to 3.
:142-145 QWEN_PID + BGE_PID spawns.
:153 wait "$QWEN_PID" "$BGE_PID" — extend to include "$WHISPER_PID".
:156 tar -xzf /weights/bge-m3/model.tar.gz -C /weights/bge-m3 — add a sibling whisper tar.
Config already reads PrimaryWhisperWeightsKey (envOr default whisper-large-v3/v1.0.0/model.tar.gz) + PrimaryWhisperWeightsSHA256 (FAIL-FAST, no default) at config.go:447-448 — the Go plumbing exists; this is purely the onstart shell wiring + forwarding into CreateRequest.Env (verify buildPrimaryCreateRequest already passes PRIMARY_WHISPER_WEIGHTS_* into the pod env).
Length-regression test: TestPrimaryOnstartLengthBelowLimit (<14KB) — keep green.

<!-- GHCR deploy fallback (memory ghcr-actions-install-broken) -->
GHA GITHUB_TOKEN push to GHCR fails org-wide since 2026-06-09. Fallback: build locally / on runner, retag, push with PAT (GitHub PAT in ~/.git-credentials / CLAUDE.md). A green PR run does NOT mean push works — verify the image digest actually landed in GHCR before recreate.
Deploy: pull + `docker compose up -d gateway` on the migrated stack (Portainer compose path 34) — confirm digest changed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Option C — add the third whisper parallel download to onstart.go</name>
  <files>gateway/internal/primary/onstart.go</files>
  <read_first>
    - gateway/internal/primary/onstart.go (the embedded shell: guards :114, download_with_verify :129-137, mkdir :139, echo :141, spawns :142-145, wait :153, tar :156)
    - gateway/internal/primary/onstart_test.go (or wherever TestPrimaryOnstartLengthBelowLimit lives — keep it green)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md (onstart.go section — copy the BGE spawn exactly, swap WHISPER vars)
  </read_first>
  <behavior>
    - The rendered onstart script contains exactly 3 `download_with_verify ... &` spawns (Qwen, bge-m3, whisper).
    - The `wait` line names all three PIDs ($QWEN_PID $BGE_PID $WHISPER_PID).
    - The script restores the whisper env guards (`: "${PRIMARY_WHISPER_WEIGHTS_KEY:?...}"` and `: "${PRIMARY_WHISPER_WEIGHTS_SHA256:?...}"`).
    - The script extracts whisper (`tar -xzf /weights/whisper/model.tar.gz -C /weights/whisper`).
    - The rendered script length stays < 14KB (TestPrimaryOnstartLengthBelowLimit green).
  </behavior>
  <action>
    Restore the whisper env guards at the :114 removal comment: add back `: "${PRIMARY_WHISPER_WEIGHTS_KEY:?required}"` and `: "${PRIMARY_WHISPER_WEIGHTS_SHA256:?required}"`. Extend the mkdir at :139 to include /weights/whisper. Add a third spawn copying the BGE_PID line exactly but with whisper vars: `download_with_verify "$PRIMARY_WHISPER_WEIGHTS_KEY" "/weights/whisper/model.tar.gz" "$PRIMARY_WHISPER_WEIGHTS_SHA256" & WHISPER_PID=$!`. Change the `wait "$QWEN_PID" "$BGE_PID"` to also wait on `"$WHISPER_PID"`. Add `tar -xzf /weights/whisper/model.tar.gz -C /weights/whisper` next to the bge-m3 tar. Bump the "2 parallel"/"2 downloads" echo string(s) to "3". Verify buildPrimaryCreateRequest already forwards PRIMARY_WHISPER_WEIGHTS_KEY/SHA256 into CreateRequest.Env (config.go:447-448 reads them); if not forwarded, add them to the env passthrough. Keep TestPrimaryOnstartLengthBelowLimit green.
  </action>
  <verify>
    <automated>cd gateway && go build ./... && go test ./internal/primary/ -run 'Onstart' -count=1 && grep -c 'download_with_verify' internal/primary/onstart.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WHISPER_PID' gateway/internal/primary/onstart.go` returns ≥1.
    - `grep -c 'download_with_verify "\$PRIMARY' gateway/internal/primary/onstart.go` returns 3 (Qwen, bge-m3, whisper spawns).
    - `grep -n 'wait "\$QWEN_PID" "\$BGE_PID" "\$WHISPER_PID"' gateway/internal/primary/onstart.go` matches the extended wait.
    - `grep -n 'tar -xzf /weights/whisper' gateway/internal/primary/onstart.go` matches.
    - `cd gateway && go test ./internal/primary/ -run Onstart -count=1` passes (length test < 14KB green).
  </acceptance_criteria>
  <done>onstart spawns 3 parallel downloads with whisper guards + extraction; length test green; whisper env forwarded to the pod.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Build + deploy the fail-fast image (GHCR PAT-retag fallback) + confirm clean boot</name>
  <files>Portainer stack (compose path 34) + GHCR image — no repo files modified by this task</files>
  <read_first>
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md (the SAFE-TO-DEPLOY gate must be present)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md (GHCR-broken constraint)
  </read_first>
  <what-built>
    Plans 02/03 + Task 1 here produced the fail-fast + cold-start + onstart code on develop. This task builds the image, gets it into GHCR (PAT-retag fallback if GHA push is still broken org-wide), and recreates ai-gateway-dev on the MIGRATED stack.
  </what-built>
  <action>
    Operator-gated build + deploy (full recipe in how-to-verify). Build + push the gateway image; on GHA GHCR push failure use the PAT-retag fallback and verify the digest actually landed in GHCR; recreate ai-gateway-dev on the migrated stack; confirm clean boot (no ErrLegacyPrimaryEnv) + the 2×3090 boot-shape dump. Precondition: plan 6.6.Y-05 SAFE-TO-DEPLOY gate present.
  </action>
  <how-to-verify>
    Operator:
    1. Confirm plan 6.6.Y-05 SAFE-TO-DEPLOY gate is present (stack env already migrated).
    2. Build + push the gateway image. If the GHA build's GHCR push fails (memory ghcr-actions-install-broken), retag locally + push with the GitHub PAT. VERIFY the new digest actually landed in GHCR (a green PR run is NOT proof) — e.g. `docker manifest inspect ghcr.io/ifixtelecom/converseai-gateway:develop` or compare digests.
    3. On the migrated stack (Portainer compose path 34): pull + recreate the gateway container. Confirm the running digest changed.
    4. Capture `docker logs ai-gateway-dev` and confirm: NO ErrLegacyPrimaryEnv AND the boot-shape log dump (plan 02) appears showing shape1 (fallback) = 2×3090, the allowlist, and port_bind_budget_seconds=120.
    5. If ErrLegacyPrimaryEnv DOES appear → the stack env still has a legacy var; do NOT proceed — go back to plan 6.6.Y-05, fix the env, redeploy.
  </how-to-verify>
  <verify>Operator pastes the GHCR digest + running container digest (matching) + the boot-shape log line proving clean boot (no ErrLegacyPrimaryEnv) + 2×3090 resolved shape.</verify>
  <acceptance_criteria>
    - The running ai-gateway-dev image digest matches the freshly-built fail-fast image (digest comparison recorded).
    - `docker logs ai-gateway-dev` contains the "primary reconciler started" / boot-shape dump line with shape fields, and does NOT contain ErrLegacyPrimaryEnv.
    - The boot-shape dump shows the 2×3090 fallback shape + allowlist + port_bind_budget_seconds=120.
  </acceptance_criteria>
  <done>Fail-fast image built, digest-verified into GHCR, deployed on the migrated stack, booting clean with the resolved 2×3090 shape dump.</done>
  <resume-signal>Type "deployed" with the digest + the boot-shape log line pasted (proving clean boot + 2×3090 resolved shape)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 6.6.Y-06-DEPLOY-EVIDENCE.md</name>
  <files>.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md (GHCR fallback + boot-log expectations)
  </read_first>
  <action>
    Author .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md with: the build/push method used (GHA vs PAT-retag fallback), the GHCR digest landed + the running container digest (must match), the boot-shape log line proving clean boot (no ErrLegacyPrimaryEnv) + resolved 2×3090 shape + port_bind_budget=120, and a "READY FOR LIVE UAT (plan 6.6.Y-07)" gate line. Record $0 Vast spend (no provisioning yet).
  </action>
  <verify>
    <automated>test -f .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md && grep -q 'READY FOR LIVE UAT' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with build method, matching GHCR + running digest, and the clean-boot log line.
    - File contains the "READY FOR LIVE UAT" gate line.
  </acceptance_criteria>
  <done>Deploy evidence recorded with the clean-boot proof and the live-UAT gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → GHCR | GHA GHCR push broken org-wide; PAT-retag fallback introduces a manual push path |
| fail-fast image → migrated stack | image refuses boot if any legacy var leaked back into the stack env |
| MinIO → pod onstart | whisper tarball downloaded + sha256-verified before extraction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.6.Y-06-01 | Tampering | Stale image deployed (GHA reported green but GHCR push silently failed — memory ghcr-actions-install-broken) | mitigate | Operator verifies the GHCR digest actually landed (manifest inspect) and the running container digest matches BEFORE declaring deploy done |
| T-6.6.Y-06-02 | Tampering | Whisper weights corrupted/substituted in transit from MinIO | mitigate | download_with_verify enforces sha256sum -c against PRIMARY_WHISPER_WEIGHTS_SHA256 (FAIL-FAST, no default) before tar extraction |
| T-6.6.Y-06-03 | Denial of service | Fail-fast image crash-loops if stack env still dirty | mitigate | Plan 6.6.Y-05 SAFE-TO-DEPLOY gate is a precondition; on ErrLegacyPrimaryEnv the operator reverts to 6.6.Y-05 (documented in how-to-verify step 5) |
| T-6.6.Y-06-04 | Information disclosure | GitHub PAT exposure during PAT-retag fallback | accept | PAT already in ~/.git-credentials on the trusted host; rotation documented in CLAUDE.md/MEMORY if exposed |
| T-6.6.Y-06-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan; image build uses the existing pinned Dockerfile + Go module set |
</threat_model>

<verification>
- onstart spawns 3 parallel downloads (whisper guards + extraction); length test green; build green.
- GHCR digest landed + running container digest match (no stale-image trap).
- ai-gateway-dev boots clean (no ErrLegacyPrimaryEnv) with the 2×3090 resolved-shape log + port_bind_budget=120.
- DEPLOY-EVIDENCE.md carries the READY-FOR-LIVE-UAT gate.
</verification>

<success_criteria>
Whisper 3-way onstart download wired (Option C, D-03, AFTER A+B); fail-fast image built, digest-verified into GHCR, deployed on the migrated stack, booting clean with the resolved 2×3090 shape dump — ready for the live UAT.
</success_criteria>

<output>
Create `.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-SUMMARY.md` when done.
</output>
