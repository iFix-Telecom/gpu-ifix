---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 06
type: execute
wave: 4
depends_on: [6.6.Y-05]
files_modified:
  - gateway/internal/primary/onstart.go
  - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md
autonomous: false
requirements: [PC-COLD-START-FIX]

must_haves:
  truths:
    - "onstart spawns 3 parallel weight downloads (Qwen + bge-m3 + whisper) and waits on all three before launching services"
    - "Whisper weights env guards are restored (PRIMARY_WHISPER_WEIGHTS_KEY/SHA256 required) and whisper tarball is extracted to /weights/whisper"
    - "The onstart length-regression test stays green (<14KB)"
    - "The fail-fast gateway image is deployed to ai-gateway-dev and boots cleanly (no ErrLegacyPrimaryEnv crash) on the migrated stack env, with the running digest verified against the freshly-built GHCR digest and a recorded rollback path"
  artifacts:
    - path: "gateway/internal/primary/onstart.go"
      provides: "3-way parallel whisper download wired into onstart (Option C, D-03)"
      contains: "WHISPER_PID"
    - path: ".planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md"
      provides: "Image build/push (with GHCR PAT-retag fallback) + digest verification + rollback path + clean boot confirmation + boot-shape log capture"
      min_lines: 20
  key_links:
    - from: "gateway/internal/primary/onstart.go"
      to: "pod /weights/whisper"
      via: "download_with_verify whisper tarball + tar -xzf"
      pattern: "/weights/whisper"
    - from: "fail-fast image"
      to: "ai-gateway-dev container"
      via: "boots without ErrLegacyPrimaryEnv on migrated stack"
      pattern: "primary reconciler started|shape1_num_gpus"
---

<objective>
Land Option C (whisper 3-way onstart download, D-03 — ordered AFTER A+B so the next UAT actually reaches the weights stage) and deploy the fail-fast gateway image onto the migrated stack. Per D-03 this uses onstart download (NOT image pre-bake) reusing the existing 11.2 download-weights 3-way infra.

**Option C scope (review finding #3 — BGE-M3 STAYS on-pod):** the reviewer asked whether bge-m3 should still download on-pod. VERIFIED against the codebase: BGE-M3 is STILL on-pod — onstart.go currently downloads Qwen + bge-m3 (2 downloads), and config.go (:451-453) + lifecycle.go (:355-356) still plumb PRIMARY_BGEM3_WEIGHTS_*. The "BGE-M3 moved off-pod to CPU Infinity" note in project context does NOT apply to the primary GPU pod's embedding service here. So Option C = **Qwen + bge-m3 + Whisper** (3-way): it RESTORES whisper as the THIRD download (it was removed by Phase 11.1 D-A4 and restored at config/lifecycle level by 11.2 D-B5′ but never re-wired into the onstart shell). BGE-M3 is NOT touched. The onstart header comment already describes 3 downloads; this aligns the code to the comment.

**Whisper env forwarding (review finding #3 — already wired, NO extra files):** VERIFIED — lifecycle.go:353-354 already forwards PRIMARY_WHISPER_WEIGHTS_KEY/SHA256 into CreateRequest.Env, and lifecycle.go:307 already validates ErrMissingWhisperSHA. The Go plumbing is COMPLETE; Option C is PURELY the onstart.go shell wiring. Therefore `files_modified` correctly lists only onstart.go — no additional file is needed. (If, on read, the executor finds the forwarding is somehow absent, it must add lifecycle.go to files_modified and forward the vars — but the grep above confirms it is present.)

This is `autonomous: false` because the deploy step (image build/push + container recreate) is operator-gated, and GHCR push from GHA is BROKEN org-wide (memory ghcr-actions-install-broken) so the deploy may need a PAT-retag fallback.

Steps:
1. **onstart.go (code, autonomous):** restore whisper env guards, add the THIRD `download_with_verify ... & WHISPER_PID=$!` spawn, extend `wait` + add `tar -xzf /weights/whisper/...`, bump the "2 parallel"/"2 downloads" echo strings to "3", keep the onstart length test (<14KB) green.
2. **Deploy (operator-gated):** build + push the gateway image (fail-fast + cold-start code from plans 02/03 + this onstart change); if GHA GHCR push fails, use the PAT-retag fallback. VERIFY the new digest landed in GHCR with an explicit `docker buildx imagetools inspect` / `docker manifest inspect` command. Pull + recreate ai-gateway-dev on the MIGRATED stack (plan 6.6.Y-05 gate). Confirm it boots WITHOUT ErrLegacyPrimaryEnv and emits the boot-shape log dump (plan 02) showing shape1 (FALLBACK) = 2×3090 (the standing config) + port-bind budget. RECORD a rollback path (previous digest + redeploy command + rollback trigger).

Purpose: get the weights stage reachable for the live UAT (plan 6.6.Y-07) and prove the fail-fast image boots clean on the migrated env.
Output: onstart.go change (build green, length test green) + 6.6.Y-06-DEPLOY-EVIDENCE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-COLD-START.md

<canonical_shape_matrix>
<!-- SINGLE SOURCE OF TRUTH — see plan 6.6.Y-01. The 2x3090 standing config = FALLBACK shape (shape1_*). -->
The boot-shape dump (plan 02) on the deployed image must show shape1_num_gpus=2, shape1_gpu="RTX 3090", shape1_cap=0.60 (the FALLBACK / standing 2x3090 config), AND shape0_num_gpus=1, shape0_cap=0.30 (the PRIMARY 1x3090). UAT (plan 07) provisions the FALLBACK shape (2x3090).

<interfaces>
<!-- onstart.go current state (extracted + VERIFIED 2026-06-10) -->
:71-76 header comment ALREADY says "spawn 3 parallel weight downloads (Qwen GGUF + Whisper tarball + BGE-M3 tarball)" + "extract whisper + bge-m3 tarballs" — code drifted to 2 (Qwen + bge-m3) after Phase 11.1 D-A4; Option C re-adds whisper as the 3rd.
:114 "# Phase 11.1 D-A4: PRIMARY_WHISPER_WEIGHTS_* removed (STT shrunk to tier-1-only)." — restore the guards here.
:115-116 PRIMARY_BGEM3_WEIGHTS_KEY/SHA256 guards — KEEP (bge-m3 stays on-pod, finding #3).
:129-137 download_with_verify() helper — reuse verbatim (aria2c --max-tries=50 --retry-wait=15 + sha256sum -c).
:139 mkdir -p /weights/qwen /weights/bge-m3 /app/templates — extend with /weights/whisper.
:141 echo "...: spawning 2 parallel downloads" — bump to 3.
:142-145 QWEN_PID + BGE_PID spawns (the two existing downloads — bge-m3 KEPT).
:153 wait "$QWEN_PID" "$BGE_PID" — extend to include "$WHISPER_PID".
:156 tar -xzf /weights/bge-m3/model.tar.gz -C /weights/bge-m3 — add a sibling whisper tar.
Config already reads PrimaryWhisperWeightsKey (config.go:447, default whisper-large-v3/v1.0.0/model.tar.gz) + PrimaryWhisperWeightsSHA256 (config.go:448, FAIL-FAST no default). lifecycle.go:307 validates ErrMissingWhisperSHA; lifecycle.go:353-354 ALREADY forwards PRIMARY_WHISPER_WEIGHTS_KEY/SHA256 into CreateRequest.Env — so the Go plumbing is complete; this is PURELY the onstart shell wiring (finding #3 — no extra file in files_modified).
Length-regression test: TestPrimaryOnstartLengthBelowLimit (<14KB) — keep green.

<!-- GHCR deploy + digest verification + rollback (memory ghcr-actions-install-broken; findings #2) -->
GHA GITHUB_TOKEN push to GHCR fails org-wide since 2026-06-09. Fallback: build locally / on runner, retag, push with PAT (GitHub PAT in ~/.git-credentials / CLAUDE.md). A green PR run does NOT mean push works.
Digest verification command (finding #2): `docker buildx imagetools inspect ghcr.io/ifixtelecom/converseai-gateway:develop --format '{{.Manifest.Digest}}'` (or `docker manifest inspect ghcr.io/ifixtelecom/converseai-gateway:develop` and read the config digest). Compare against the locally-built image digest (`docker inspect --format '{{index .RepoDigests 0}}' <local-image>`).
Running-container digest: `ssh vps-ifix-vm "docker inspect ai-gateway-dev --format '{{.Image}}'"` then resolve to the repo digest.
Rollback path (finding #2): BEFORE recreate, record the CURRENT running image digest (`ssh vps-ifix-vm "docker inspect ai-gateway-dev --format '{{index .Config.Image}} {{.Image}}'"`). Rollback = re-pin the stack to that previous digest + `docker compose up -d gateway`. Rollback TRIGGER = ErrLegacyPrimaryEnv on boot OR the boot-shape dump shows the wrong shape OR the container fails health after recreate.
Deploy: pull + `docker compose up -d gateway` on the migrated stack (Portainer compose path 34) — confirm digest changed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Option C — add the third whisper parallel download to onstart.go (bge-m3 KEPT)</name>
  <files>gateway/internal/primary/onstart.go</files>
  <read_first>
    - gateway/internal/primary/onstart.go (the embedded shell: header :71-76, guards :114-116, download_with_verify :129-137, mkdir :139, echo :141, spawns :142-145, wait :153, tar :156)
    - gateway/internal/primary/onstart_test.go (or wherever TestPrimaryOnstartLengthBelowLimit lives — keep it green)
    - gateway/internal/primary/lifecycle.go (:307 ErrMissingWhisperSHA + :353-354 whisper env forwarding — CONFIRM these exist; they do per the grep in <interfaces>)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md (onstart.go section — copy the BGE spawn exactly, swap WHISPER vars)
  </read_first>
  <behavior>
    - The rendered onstart script contains exactly 3 `download_with_verify ... &` spawns (Qwen, bge-m3, whisper) — bge-m3 is KEPT (finding #3).
    - The `wait` line names all three PIDs ($QWEN_PID $BGE_PID $WHISPER_PID).
    - The script restores the whisper env guards (`: "${PRIMARY_WHISPER_WEIGHTS_KEY:?...}"` and `: "${PRIMARY_WHISPER_WEIGHTS_SHA256:?...}"`).
    - The script extracts whisper (`tar -xzf /weights/whisper/model.tar.gz -C /weights/whisper`) and STILL extracts bge-m3 (no regression).
    - The rendered script length stays < 14KB (TestPrimaryOnstartLengthBelowLimit green).
  </behavior>
  <action>
    Restore the whisper env guards at the :114 removal comment: replace the "removed" comment with `: "${PRIMARY_WHISPER_WEIGHTS_KEY:?required}"` and `: "${PRIMARY_WHISPER_WEIGHTS_SHA256:?required}"` (keep the bge-m3 guards at :115-116). Extend the mkdir at :139 to include /weights/whisper. Add a third spawn copying the BGE_PID line exactly but with whisper vars: `download_with_verify "$PRIMARY_WHISPER_WEIGHTS_KEY" "/weights/whisper/model.tar.gz" "$PRIMARY_WHISPER_WEIGHTS_SHA256" & WHISPER_PID=$!`. Change the `wait "$QWEN_PID" "$BGE_PID"` to also wait on `"$WHISPER_PID"`. Add `tar -xzf /weights/whisper/model.tar.gz -C /weights/whisper` next to the existing bge-m3 tar (do NOT remove the bge-m3 tar — finding #3). Bump the "2 parallel"/"2 downloads" echo string(s) to "3". CONFIRM (read-only) that lifecycle.go:353-354 already forwards PRIMARY_WHISPER_WEIGHTS_KEY/SHA256 into CreateRequest.Env; per the grep it does, so no lifecycle.go edit is needed — only if it is somehow absent add lifecycle.go to files_modified and forward the vars. Keep TestPrimaryOnstartLengthBelowLimit green.
  </action>
  <verify>
    <automated>cd gateway && go build ./... && go test ./internal/primary/ -run 'Onstart' -count=1 && grep -c 'download_with_verify "\$PRIMARY' internal/primary/onstart.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WHISPER_PID' gateway/internal/primary/onstart.go` returns >=1.
    - `grep -c 'download_with_verify "\$PRIMARY' gateway/internal/primary/onstart.go` returns 3 (Qwen, bge-m3, whisper spawns — bge-m3 KEPT, finding #3).
    - `grep -c 'download_with_verify "\$PRIMARY_BGEM3' gateway/internal/primary/onstart.go` returns 1 (bge-m3 download NOT removed).
    - `grep -n 'wait "\$QWEN_PID" "\$BGE_PID" "\$WHISPER_PID"' gateway/internal/primary/onstart.go` matches the extended wait.
    - `grep -n 'tar -xzf /weights/whisper' gateway/internal/primary/onstart.go` matches AND `grep -n 'tar -xzf /weights/bge-m3' gateway/internal/primary/onstart.go` still matches.
    - `cd gateway && go test ./internal/primary/ -run Onstart -count=1` passes (length test < 14KB green).
  </acceptance_criteria>
  <done>onstart spawns 3 parallel downloads (Qwen + bge-m3 KEPT + whisper restored) with whisper guards + extraction; length test green; whisper env already forwarded (lifecycle.go verified).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Build + deploy the fail-fast image (GHCR PAT-retag fallback) + digest-verify + rollback path + confirm clean boot</name>
  <files>Portainer stack (compose path 34) + GHCR image — no repo files modified by this task</files>
  <read_first>
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-05-STACK-MIGRATION-EVIDENCE.md (the SAFE-TO-DEPLOY gate must be present)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md (GHCR-broken constraint)
  </read_first>
  <what-built>
    Plans 02/03 + Task 1 here produced the fail-fast + cold-start + onstart code on develop. This task builds the image, gets it into GHCR (PAT-retag fallback if GHA push is still broken org-wide), digest-verifies it, recreates ai-gateway-dev on the MIGRATED stack, and records a rollback path.
  </what-built>
  <action>
    Operator-gated build + deploy (full recipe in how-to-verify). Build + push the gateway image; on GHA GHCR push failure use the PAT-retag fallback; VERIFY the digest landed with `docker buildx imagetools inspect`; RECORD the previous running digest as the rollback target; recreate ai-gateway-dev on the migrated stack; confirm clean boot (no ErrLegacyPrimaryEnv) + the shape1=2×3090 boot-shape dump. Precondition: plan 6.6.Y-05 SAFE-TO-DEPLOY gate present.
  </action>
  <how-to-verify>
    Operator:
    1. Confirm plan 6.6.Y-05 SAFE-TO-DEPLOY gate is present (stack env already migrated).
    2. RECORD ROLLBACK TARGET (finding #2): `ssh vps-ifix-vm "docker inspect ai-gateway-dev --format '{{index .Config.Image}} {{.Image}}'"` — note the CURRENT image tag + digest. This is the rollback target.
    3. Build + push the gateway image. If the GHA build's GHCR push fails (memory ghcr-actions-install-broken), retag locally + push with the GitHub PAT.
    4. DIGEST VERIFY (finding #2): run `docker buildx imagetools inspect ghcr.io/ifixtelecom/converseai-gateway:develop --format '{{.Manifest.Digest}}'` (or `docker manifest inspect ghcr.io/ifixtelecom/converseai-gateway:develop`) and confirm the GHCR digest equals the freshly-built local image digest (`docker inspect --format '{{index .RepoDigests 0}}' <local-image>`). A green PR run is NOT proof.
    5. On the migrated stack (Portainer compose path 34): pull + recreate the gateway container. Confirm the running digest changed and equals the new GHCR digest (`ssh vps-ifix-vm "docker inspect ai-gateway-dev --format '{{.Image}}'"`).
    6. Capture `docker logs ai-gateway-dev` and confirm: NO ErrLegacyPrimaryEnv AND the boot-shape log dump (plan 02) appears showing shape1 (FALLBACK) = 2×3090, shape0 (PRIMARY) = 1×3090, the allowlist, and port_bind_budget_seconds=120.
    7. ROLLBACK TRIGGER (finding #2): if ErrLegacyPrimaryEnv appears OR the boot-shape shows the wrong shape OR the container fails health → re-pin the stack to the recorded previous digest (step 2) + `docker compose up -d gateway`, then go back to plan 6.6.Y-05 to fix the env, redeploy. Do NOT proceed to UAT on a dirty boot.
  </how-to-verify>
  <verify>Operator pastes: the recorded rollback-target digest, the GHCR digest + running container digest (matching the freshly-built image), and the boot-shape log line proving clean boot (no ErrLegacyPrimaryEnv) + shape1=2×3090.</verify>
  <acceptance_criteria>
    - The freshly-built image digest, the GHCR-landed digest, and the running ai-gateway-dev image digest are all recorded and MATCH (digest comparison via docker buildx imagetools inspect / manifest inspect — finding #2).
    - A rollback target (previous running digest) is recorded along with the redeploy command and the rollback trigger criteria (finding #2).
    - `docker logs ai-gateway-dev` contains the "primary reconciler started" / boot-shape dump line with shape fields, and does NOT contain ErrLegacyPrimaryEnv.
    - The boot-shape dump shows shape1=2×3090 (FALLBACK/standing) + shape0=1×3090 (PRIMARY) + allowlist + port_bind_budget_seconds=120.
  </acceptance_criteria>
  <done>Fail-fast image built, digest-verified into GHCR (matching running container), rollback path recorded, deployed on the migrated stack, booting clean with the resolved shape dump (shape1=2×3090).</done>
  <resume-signal>Type "deployed" with the matching GHCR+running digests, the rollback-target digest, and the boot-shape log line pasted</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 6.6.Y-06-DEPLOY-EVIDENCE.md</name>
  <files>.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md (GHCR fallback + boot-log expectations)
  </read_first>
  <action>
    Author the evidence file with: the build/push method used (GHA vs PAT-retag fallback), the explicit digest-verification command used + the GHCR digest landed + the running container digest (must all match — finding #2), the recorded ROLLBACK PATH (previous digest + redeploy command + rollback trigger — finding #2), the boot-shape log line proving clean boot (no ErrLegacyPrimaryEnv) + shape1=2×3090 (FALLBACK/standing) + shape0=1×3090 + port_bind_budget=120, and a "READY FOR LIVE UAT (plan 6.6.Y-07)" gate line. Record $0 Vast spend (no provisioning yet).
  </action>
  <verify>
    <automated>test -f .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md && grep -q 'READY FOR LIVE UAT' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md && grep -Eqi 'rollback' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md && grep -Eqi 'digest' .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-DEPLOY-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with build method, the digest-verification command + matching GHCR + running digest, and the clean-boot log line.
    - File records the rollback path (previous digest + redeploy command + trigger — finding #2).
    - File contains the "READY FOR LIVE UAT" gate line.
  </acceptance_criteria>
  <done>Deploy evidence recorded with digest verification, rollback path, clean-boot proof, and the live-UAT gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → GHCR | GHA GHCR push broken org-wide; PAT-retag fallback introduces a manual push path |
| fail-fast image → migrated stack | image refuses boot if any legacy var leaked back into the stack env |
| MinIO → pod onstart | whisper + bge-m3 tarballs downloaded + sha256-verified before extraction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.6.Y-06-01 | Tampering | Stale image deployed (GHA reported green but GHCR push silently failed — memory ghcr-actions-install-broken) | mitigate | Operator runs an explicit `docker buildx imagetools inspect` digest check and confirms GHCR digest == built digest == running digest BEFORE declaring deploy done (finding #2); rollback path recorded |
| T-6.6.Y-06-02 | Tampering | Whisper/bge-m3 weights corrupted/substituted in transit from MinIO | mitigate | download_with_verify enforces sha256sum -c against PRIMARY_WHISPER_WEIGHTS_SHA256 / PRIMARY_BGEM3_WEIGHTS_SHA256 (FAIL-FAST, no default) before tar extraction |
| T-6.6.Y-06-03 | Denial of service | Fail-fast image crash-loops if stack env still dirty | mitigate | Plan 6.6.Y-05 SAFE-TO-DEPLOY gate is a precondition; rollback path (finding #2) re-pins the previous digest on ErrLegacyPrimaryEnv |
| T-6.6.Y-06-04 | Information disclosure | GitHub PAT exposure during PAT-retag fallback | accept | PAT already in ~/.git-credentials on the trusted host; rotation documented in CLAUDE.md/MEMORY if exposed |
| T-6.6.Y-06-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan; image build uses the existing pinned Dockerfile + Go module set |
</threat_model>

<verification>
- onstart spawns 3 parallel downloads (Qwen + bge-m3 KEPT + whisper restored, guards + extraction); length test green; build green.
- GHCR digest landed (verified via imagetools inspect) == built digest == running container digest (no stale-image trap); rollback path recorded.
- ai-gateway-dev boots clean (no ErrLegacyPrimaryEnv) with shape1=2×3090 + shape0=1×3090 + port_bind_budget=120.
- DEPLOY-EVIDENCE.md carries the digest-verify command, rollback path, and the READY-FOR-LIVE-UAT gate.
</verification>

<success_criteria>
Whisper 3-way onstart download wired (Option C, D-03, bge-m3 KEPT, AFTER A+B); fail-fast image built, digest-verified into GHCR (matching running container), rollback path recorded, deployed on the migrated stack, booting clean with shape1=2×3090 — ready for the live UAT.
</success_criteria>

<output>
Create `.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-06-SUMMARY.md` when done.
</output>
</content>
