---
phase: 6.6.Y-pod-cold-start-fix-env-precedence-canonical
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/internal/config/config.go
  - gateway/internal/config/config_test.go
  - gateway/cmd/gateway/main.go
autonomous: true
requirements: [PC-ENV-PRECEDENCE, PC-COLD-START-FIX]

must_haves:
  truths:
    - "Gateway boot FAILS (non-nil error from config.Load) when any legacy primary-shape var (PRIMARY_NUM_GPUS, PRIMARY_GPU_NAME, PRIMARY_VAST_PRICE_CAP_DPH) is set"
    - "The boot error names the offending legacy var and the canonical replacement"
    - "The 3 legacy struct fields and their env reads are removed; csvOr and the emerg VAST_PRICE_CAP_DPH read are untouched"
    - "Two new cold-start env vars are read into Config: PrimaryPublicPortBindBudgetSeconds (default 120) and PrimaryVastRejectPrivateIP (default true)"
    - "Gateway emits a boot-time resolved-shape log dump with per-shape canonical fields + allowlist + port-bind budget"
  artifacts:
    - path: "gateway/internal/config/config.go"
      provides: "Hard fail-fast on legacy primary-shape vars + new cold-start config readers"
      contains: "ErrLegacyPrimaryEnv"
    - path: "gateway/internal/config/config_test.go"
      provides: "errors.Is assertions for ErrLegacyPrimaryEnv on each legacy var + positive all-canonical test"
      contains: "ErrLegacyPrimaryEnv"
    - path: "gateway/cmd/gateway/main.go"
      provides: "Boot-time resolved primary-shape log dump"
      contains: "PrimaryVastNumGPUsPrimary"
  key_links:
    - from: "gateway/internal/config/config.go"
      to: "config.Load callers"
      via: "ErrLegacyPrimaryEnv sentinel returned when legacy var present"
      pattern: "ErrLegacyPrimaryEnv"
    - from: "gateway/cmd/gateway/main.go"
      to: "operator stdout"
      via: "log.Info resolved-shape dump"
      pattern: "PrimaryVastNumGPUsPrimary|shape0_num_gpus"
---

<objective>
Land the PC-ENV-PRECEDENCE hard fail-fast (06.6.X-RESEARCH-ENV-PRECEDENCE §5/§6) and the cold-start config plumbing that plan 6.6.Y-03 consumes, plus the fix-option-agnostic boot-time resolved-shape log dump (locked from RESEARCH).

This plan is the SINGLE owner of config.go + config_test.go + main.go so no other W1 plan touches them. It:
1. Replaces the inert soft-warn legacy-alias block (config.go:608-642) with a HARD FAIL-FAST sentinel-error block.
2. Deletes the 3 legacy struct fields (config.go:213,216,217) and their env reads (config.go:454,457,458). KEEPS the emerg `VAST_PRICE_CAP_DPH` read at config.go:427 (different subsystem — grep-confirmed) and KEEPS `csvOr` (config.go:714) + allowlist/blocklist reads.
3. Adds two new cold-start config readers consumed by plan 6.6.Y-03: `PrimaryPublicPortBindBudgetSeconds` (env PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS, default 120 per D-02) and `PrimaryVastRejectPrivateIP` (env PRIMARY_VAST_REJECT_PRIVATE_IP, default true).
4. Adds config_test.go assertions.
5. Replaces the legacy `price_cap_dph` arg in the main.go boot log with the per-shape canonical dump.

Purpose: make env drift structurally impossible (the legacy reader is gone) and give the operator a visible boot-time shape confirmation.
Output: config.go + config_test.go + main.go changes, `go build ./...` + `go test ./internal/config/...` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-CONTEXT.md
@.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md
@.planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md

<interfaces>
<!-- Existing fail-fast idiom to copy (config.go) — extracted, executor uses directly -->
Sentinel decls (config.go:273-285): `var ErrMissingEnv = errors.New("config: required environment variable not set")`, `var ErrInvalidURLSuffix = errors.New("config: external upstream URL must not end with /v1")`.
Fail-fast block #1 (config.go:530-538): accumulate `missing []string` → `return Config{}, fmt.Errorf("%w: %s", ErrMissingEnv, strings.Join(missing, ", "))`.
Fail-fast block #2 (config.go:553-573): accumulate `invalidURLs []string` (each `envName+"="+url`) → `return Config{}, fmt.Errorf("%w: %s (hint)", ErrInvalidURLSuffix, strings.Join(...))`.
Legacy alias block to REPLACE: config.go:608-642 (the 3 `if legacy := os.Getenv(...); legacy != ""` + slog.Warn copy-on-empty guards).
Legacy struct fields to DELETE: config.go:213 PrimaryVastPriceCapDPH, :216 PrimaryGPUName, :217 PrimaryNumGPUs.
Legacy env reads to DELETE: config.go:454 PrimaryVastPriceCapDPH floatOr, :457 PrimaryGPUName envOr, :458 PrimaryNumGPUs atoiOr.
DO NOT TOUCH: config.go:427 `VastPriceCapDPH: floatOr(os.Getenv("VAST_PRICE_CAP_DPH"), 0.40)` (emerg subsystem, grep-confirmed NOT a primary alias). csvOr (config.go:714). PrimaryVastMachineAllowlist/Blocklist reads.
Canonical fields ALREADY present (config.go:223-228 / read config.go:461-466): PrimaryVastGPUNamePrimary, PrimaryVastGPUNameFallback, PrimaryVastPriceCapPrimary, PrimaryVastPriceCapFallback, PrimaryVastNumGPUsPrimary, PrimaryVastNumGPUsFallback.
Imports already present: errors, fmt, log/slog, os, strconv, strings, time.
Helpers available: envOr, atoiOr, floatOr, csvOr, parseInt64CSV.

<!-- config_test.go idiom (extracted) -->
clearAll(t) (config_test.go:37) sets all to ""; setAllRequired(t) (:47); TestLoad_MissingRequired (:54) asserts errors.Is(err, config.ErrMissingEnv) + strings.Contains per name; TestLoad_AllRequiredPresent (:83).

<!-- main.go boot log (extracted) — main.go:984-998 -->
log.Info("Phase 6.6 primary reconciler started", ... "coldstart_budget_seconds", cfg.PrimaryProvisionColdStartBudgetSeconds, "price_cap_dph", cfg.PrimaryVastPriceCapDPH /* legacy — REMOVE */, "template_image", cfg.PrimaryTemplateImage)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hard fail-fast on legacy primary-shape vars + delete legacy fields/reads + add cold-start config readers</name>
  <files>gateway/internal/config/config.go, gateway/internal/config/config_test.go</files>
  <read_first>
    - gateway/internal/config/config.go (current state: sentinels :273-285, fail-fast blocks :530-538 and :553-573, alias block :608-642, struct fields :213-228, reads :427/:454-466)
    - gateway/internal/config/config_test.go (clearAll/setAllRequired/TestLoad_* idioms)
    - .planning/phases/06.6.X-pod-cold-start-env-precedence-audit/06.6.X-RESEARCH-ENV-PRECEDENCE.md (§6 paste-ready accumulator body + companion deletions list)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md (config.go sentinel-error idiom section)
  </read_first>
  <behavior>
    - config.Load() returns non-nil err with errors.Is(err, config.ErrLegacyPrimaryEnv)==true when PRIMARY_NUM_GPUS is set (all required present).
    - Same for PRIMARY_GPU_NAME set alone, and PRIMARY_VAST_PRICE_CAP_DPH set alone.
    - The error message for PRIMARY_NUM_GPUS contains the string "PRIMARY_VAST_NUM_GPUS_FALLBACK" (migration hint).
    - All 6 canonical vars set + zero legacy vars → config.Load() returns err==nil.
    - Setting the emerg var VAST_PRICE_CAP_DPH (unprefixed) does NOT trigger ErrLegacyPrimaryEnv (it is a different subsystem var).
  </behavior>
  <action>
    Declare a new sentinel alongside the two existing ones (config.go:273-285): `var ErrLegacyPrimaryEnv = errors.New("config: legacy primary-shape env var(s) detected; use per-shape canonical names")`.
    Replace the alias block at config.go:608-642 with a `legacyErrs []string` accumulator following the ErrInvalidURLSuffix idiom (offending-name slice + actionable hint). Check os.Getenv for each of PRIMARY_GPU_NAME, PRIMARY_VAST_PRICE_CAP_DPH, PRIMARY_NUM_GPUS; for each non-empty, append a hint string per RESEARCH §6 (e.g. "PRIMARY_NUM_GPUS is removed; set PRIMARY_VAST_NUM_GPUS_FALLBACK=<v> (and _PRIMARY=1 if you want shape-0 search to find 1×3090 hosts first)"). If len(legacyErrs)>0, `return Config{}, fmt.Errorf("%w: %s", ErrLegacyPrimaryEnv, strings.Join(legacyErrs, "; "))`. Use the sentinel idiom (NOT RESEARCH §6's inline fmt.Errorf) so config_test.go can assert via errors.Is — per CONTEXT phase constraint.
    DELETE legacy struct fields config.go:213 (PrimaryVastPriceCapDPH), :216 (PrimaryGPUName), :217 (PrimaryNumGPUs) and their reads :454, :457, :458. KEEP config.go:427 VAST_PRICE_CAP_DPH (emerg), csvOr, and allowlist/blocklist reads. After deletion, grep the file to ensure no remaining reference to cfg.PrimaryVastPriceCapDPH / cfg.PrimaryGPUName / cfg.PrimaryNumGPUs (main.go reference handled in Task 2).
    ADD two new readers in the Config literal next to the canonical shape fields: `PrimaryPublicPortBindBudgetSeconds: atoiOr(os.Getenv("PRIMARY_PUBLIC_PORT_BIND_BUDGET_SECONDS"), 120)` and `PrimaryVastRejectPrivateIP: os.Getenv("PRIMARY_VAST_REJECT_PRIVATE_IP") != "false"` (default true; only literal "false" disables — matches the project opt-out pattern). Add matching struct field decls (int and bool) with doc comments naming the env var and default.
    Add config_test.go tests per the <behavior> block: one errors.Is assertion per legacy var (copy TestLoad_MissingSingleVarNamedInError idiom — setAllRequired then t.Setenv the one legacy var), the migration-hint strings.Contains assertion, the positive all-canonical-no-legacy test, and the negative VAST_PRICE_CAP_DPH-does-not-trigger test.
  </action>
  <verify>
    <automated>cd gateway && go build ./... && go test ./internal/config/ -run 'TestLoad' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go vet ./internal/config/` passes.
    - `grep -n 'ErrLegacyPrimaryEnv' gateway/internal/config/config.go` returns the sentinel decl + the fmt.Errorf return.
    - `grep -nc 'cfg.PrimaryNumGPUs\b\|cfg.PrimaryGPUName\b\|cfg.PrimaryVastPriceCapDPH\b' gateway/internal/config/config.go` returns 0 (legacy reads gone).
    - `grep -n 'VAST_PRICE_CAP_DPH' gateway/internal/config/config.go` still shows the emerg read at the `VastPriceCapDPH:` line (untouched).
    - `grep -n 'PrimaryPublicPortBindBudgetSeconds\|PrimaryVastRejectPrivateIP' gateway/internal/config/config.go` shows both the struct field and the reader.
    - `go test ./internal/config/ -run TestLoad -count=1` passes including the new ErrLegacyPrimaryEnv subtests.
  </acceptance_criteria>
  <done>config.Load fails fast on any legacy primary-shape var with a named hint; legacy fields/reads removed; two cold-start readers added; config tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Boot-time resolved primary-shape log dump in main.go</name>
  <files>gateway/cmd/gateway/main.go</files>
  <read_first>
    - gateway/cmd/gateway/main.go (the `log.Info("Phase 6.6 primary reconciler started", ...)` block at main.go:984-998, which currently passes the now-deleted `"price_cap_dph", cfg.PrimaryVastPriceCapDPH`)
    - .planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-PATTERNS.md (main.go boot-log section)
  </read_first>
  <action>
    In the `log.Info("Phase 6.6 primary reconciler started", ...)` block, REMOVE the now-invalid `"price_cap_dph", cfg.PrimaryVastPriceCapDPH` arg (the struct field was deleted in Task 1, so the build currently breaks here). REPLACE it with the per-shape canonical fields so the operator sees resolved env precedence at boot: `"shape0_num_gpus", cfg.PrimaryVastNumGPUsPrimary`, `"shape0_gpu", cfg.PrimaryVastGPUNamePrimary`, `"shape0_cap", cfg.PrimaryVastPriceCapPrimary`, `"shape1_num_gpus", cfg.PrimaryVastNumGPUsFallback`, `"shape1_gpu", cfg.PrimaryVastGPUNameFallback`, `"shape1_cap", cfg.PrimaryVastPriceCapFallback`, `"allowlist", cfg.PrimaryVastMachineAllowlist`, `"port_bind_budget_seconds", cfg.PrimaryPublicPortBindBudgetSeconds`, `"reject_private_ip", cfg.PrimaryVastRejectPrivateIP`. Keep the existing keys (coldstart_budget_seconds, template_image, etc.). This is fix-option-agnostic per CONTEXT "Locked from RESEARCH — Boot-time resolved-shape log dump".
  </action>
  <verify>
    <automated>cd gateway && go build ./... && grep -q 'shape0_num_gpus' cmd/gateway/main.go && ! grep -q 'cfg.PrimaryVastPriceCapDPH' cmd/gateway/main.go</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go build ./...` succeeds (proves the deleted-field reference is gone).
    - `grep -n 'shape0_num_gpus' gateway/cmd/gateway/main.go` shows the new arg in the boot log block.
    - `grep -c 'cfg.PrimaryVastPriceCapDPH' gateway/cmd/gateway/main.go` returns 0.
    - `grep -n 'port_bind_budget_seconds' gateway/cmd/gateway/main.go` shows the new cold-start budget surfaced at boot.
  </acceptance_criteria>
  <done>Gateway emits per-shape canonical config + port-bind budget at boot; no reference to deleted legacy field; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment → config.Load | untrusted env vars cross into typed config at boot |
| config → operator logs | resolved-shape dump emits config values (no secrets) to stdout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.6.Y-02-01 | Tampering / Elevation | Silent env drift mis-provisioning a 1×3090 pod when operator intended 2×3090 | mitigate | Hard fail-fast: config.Load returns ErrLegacyPrimaryEnv when legacy var present; gateway refuses to boot until env is clean (RESEARCH §5) |
| T-6.6.Y-02-02 | Information disclosure | Boot-shape log dump leaking config values | accept | Dumped fields are GPU counts/names/price caps/allowlist machine_ids — non-secret operational config; VAST_AI_API_KEY and other secrets are NOT in the dump |
| T-6.6.Y-02-03 | Denial of service | Fail-fast crash-loops the gateway on first deploy if stack.env still has legacy vars | mitigate | Plan 6.6.Y-05 (D-04) migrates stack.env BEFORE this image deploys — operator-gated ordering prevents the crash-loop |
| T-6.6.Y-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; Go module set unchanged |
</threat_model>

<verification>
- `cd gateway && go build ./...` green.
- `cd gateway && go test ./internal/config/ -run TestLoad -count=1` green including ErrLegacyPrimaryEnv subtests.
- Legacy fields/reads removed; emerg VAST_PRICE_CAP_DPH + csvOr untouched.
- Boot log dumps per-shape canonical fields + port-bind budget.
</verification>

<success_criteria>
config.Load hard-fails on any of the 3 legacy primary-shape vars with a named migration hint; two cold-start config readers added for plan 6.6.Y-03; boot-shape log dump live; all config tests pass.
</success_criteria>

<output>
Create `.planning/phases/6.6.Y-pod-cold-start-fix-env-precedence-canonical/6.6.Y-02-SUMMARY.md` when done.
</output>
