---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 03
subsystem: gateway-tier0-routing
tags: [tts, tier0, override, probe, quota, routing, chatterbox-agnostic]
requires:
  - 06.7-02 (Wave 0 RED/skip stubs)
  - 06.7-WAVE0-GATES.md (engine = Chatterbox Multilingual; TTS on :8003 /v1/audio/speech)
provides:
  - tts as a first-class dynamic tier-0 override role (D-11)
  - Loader.Tier0OverrideURL(role) getter for Plan 08 re-assert (D-13)
  - tts synthetic probe (POST /v1/audio/speech)
  - quota.RouteClassTTS + classifyRoute(/v1/audio/speech)
affects:
  - 06.7-07 (TTS proxy/voices — routes role=tts traffic)
  - 06.7-08 (primary reconciler Pitfall #11 re-assert — reads Tier0OverrideURL, overrides tts not embed)
tech-stack:
  added: []
  patterns: [role-agnostic tier-0 override map, role-keyed probe dispatch switch, RouteClass wire-contract const]
key-files:
  created:
    - gateway/internal/quota/classify_tts_internal_test.go
  modified:
    - gateway/internal/upstreams/loader.go
    - gateway/internal/upstreams/types.go
    - gateway/internal/upstreams/loader_test.go
    - gateway/internal/upstreams/probe.go
    - gateway/internal/upstreams/probe_test.go
    - gateway/internal/quota/bucket.go
    - gateway/internal/quota/enforcer.go
    - gateway/internal/quota/enforcer_test.go
decisions:
  - "Dynamic tier-0 override roster {llm,stt,embed} -> {llm,stt,tts}: tts added (D-11), embed removed (D-03 — embed is now a static tier-0 upstream)"
  - "Tier0OverrideURL mirrors Resolve's lockless atomic.Pointer read; returns (\"\",false) for roles with no slot (e.g. embed)"
  - "tts probe reuses the role-agnostic 5xx->breaker.HTTPError success/failure path; bounded ping body + response_format=pcm (T-06.7-04)"
  - "classifyRoute mapping asserted in an internal-package test (classifyRoute is unexported); external test locks the RouteClassTTS=\"tts\" Redis wire value"
metrics:
  duration: ~25m
  completed: 2026-05-20
---

# Phase 06.7 Plan 03: Gateway Tier-0 Routing Core (TTS) Summary

Engine-agnostic tier-0 routing primitives for TTS: made `tts` a first-class dynamic-override role, removed `embed` from the override map (now static per D-03), added the `Tier0OverrideURL` getter (Plan 08 dependency), the `tts` synthetic `/v1/audio/speech` probe, and the `RouteClassTTS` quota class. All four owned Plan 02 stubs flipped SKIP → PASS.

## What Was Built

Two atomic commits, surgical edits to existing role-keyed machinery (no new subsystem). Drives the role slot regardless of which TTS server runs (Chatterbox primary on :8003 or Piper tier-1 adapter).

### Task 1 — tier-0 map swap + Tier0OverrideURL getter (commit db4e579)
- `loader.go`: `newTier0OverrideMap()` roster `{llm,stt,embed}` → `{llm,stt,tts}` (D-03/D-11). Added `Loader.Tier0OverrideURL(role) (string, bool)` mirroring Resolve's lockless atomic read — returns `("",false)` for roles with no slot (embed) or empty slots, `(url,true)` when an override is active (D-13).
- `types.go`: `Role` field comment updated to `"llm" | "stt" | "embed" | "tts"`.
- `loader_test.go`: unskipped + asserted `TestTier0_TTSKeyPresentEmbedAbsent` (map shape + OverrideTier0("tts") routes / OverrideTier0("embed") no-ops) and `TestTier0OverrideURL_Getter` (active→(url,true), absent/unknown→("",false)).

### Task 2 — tts probe + RouteClassTTS quota classify (commit c6e4354)
- `probe.go`: added `case "tts":` to `dispatch()` — POSTs `<URL>/v1/audio/speech` with `{"model":"tts-1","input":"ping","voice":"random","response_format":"pcm"}` (Content-Type application/json), reusing the shared 200=success / 5xx→`*breaker.HTTPError` classification.
- `bucket.go`: added `RouteClassTTS RouteClass = "tts"` (Redis wire-contract const).
- `enforcer.go`: added `case "/v1/audio/speech": return RouteClassTTS` to `classifyRoute`.
- `probe_test.go`: unskipped + asserted `TestProbe_TTS_PostsAudioSpeech` (httptest server verifying POST path/CT/body + 200=success, 502→`*breaker.HTTPError`).
- `enforcer_test.go` + new `classify_tts_internal_test.go`: unskipped `TestClassifyRoute_TTS` (locks `RouteClassTTS=="tts"`); internal-package test asserts `classifyRoute("/v1/audio/speech")==RouteClassTTS` + no regression on STT/default.

## Verification

- `cd gateway && go build ./...` → exit 0 (FULL_BUILD_OK)
- `go test ./internal/upstreams/... ./internal/quota/...` → all `ok`
- `go test ./internal/primary/` → `ok` (reconciler unaffected; embed override is now a benign no-op, Resolve("embed",0) serves the static row)
- Owned stubs PASS, zero SKIP among owned:
  - `TestTier0_TTSKeyPresentEmbedAbsent` → PASS
  - `TestTier0OverrideURL_Getter` → PASS
  - `TestProbe_TTS_PostsAudioSpeech` (200_success + 5xx_failure) → PASS
  - `TestClassifyRoute_TTS` + `TestClassifyRoute_TTS_Internal` → PASS
- Acceptance greps confirmed: `case "tts"` + `/v1/audio/speech` in probe.go; `RouteClassTTS` in bucket.go; `"/v1/audio/speech"` in enforcer.go; `func (l *Loader) Tier0OverrideURL(role string) (string, bool)` in loader.go; `newTier0OverrideMap` contains `"tts"`, lacks `"embed"`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Realigned pre-existing Phase 6.6 tier-0 map tests to the {llm,stt,tts} roster**
- **Found during:** Task 1
- **Issue:** Removing `embed` from `newTier0OverrideMap()` directly broke two Phase 6.6 tests that hard-coded the old `{llm,stt,embed}` roster: `TestNewTier0OverrideMap_Has3Roles` (asserted `embed` present) and `TestOverrideTier0_RestoreTier0_AllRoles/embed` (overrode embed expecting dynamic routing).
- **Fix:** Updated both to assert the new `{llm,stt,tts}` roster (tts replaces embed as the 3rd dynamic role; `Has3Roles` also now asserts embed is absent). These supersede the old assertions in lockstep with the new owned stub `TestTier0_TTSKeyPresentEmbedAbsent`.
- **Files modified:** gateway/internal/upstreams/loader_test.go
- **Commit:** db4e579

**2. [Rule 3 - Blocking] classifyRoute is unexported — split the TTS classify assertion**
- **Found during:** Task 2
- **Issue:** The Plan 02 stub `TestClassifyRoute_TTS` lives in package `quota_test` (external) but the plan requires asserting `classifyRoute("/v1/audio/speech")` — an unexported function unreachable from an external test package.
- **Fix:** Created `classify_tts_internal_test.go` (package `quota`) to assert the path→class mapping directly; the external stub now locks the exported `RouteClassTTS="tts"` Redis wire-contract value. Both behaviors are covered, both PASS.
- **Files modified:** gateway/internal/quota/enforcer_test.go; gateway/internal/quota/classify_tts_internal_test.go (new)
- **Commit:** c6e4354

## Out-of-Scope Note (NOT fixed — Plan 08 owns it)

`gateway/internal/primary/reconciler.go` still calls `OverrideTier0("embed", ...)` / `RestoreTier0("embed")` (lines 507/554/620/1015). After this plan these are benign no-ops (embed has no dynamic slot; `Resolve("embed",0)` serves the static row unchanged — exactly D-03 intent). The reconciler is Plan 08's scope (Pitfall #11 re-assert must cover llm/stt/tts, never embed — owned stubs `TestEvaluateReady_ReassertsTier0WhenCleared` / `TestMarkReady_OverridesTTSNotEmbed`). Not modified here per this plan's `files_modified` boundary. The primary package test suite remains green.

## Authentication Gates

None.

## Known Stubs

None introduced. All four owned Plan 02 stubs were unskipped and now assert real behavior.

## Self-Check: PASSED

- gateway/internal/quota/classify_tts_internal_test.go — FOUND
- Commit db4e579 — FOUND in git log
- Commit c6e4354 — FOUND in git log
