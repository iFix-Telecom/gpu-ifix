---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 08
type: execute
wave: 2
depends_on: ["06.7-03", "06.7-05"]
files_modified:
  - gateway/internal/primary/reconciler.go
  - gateway/internal/primary/lifecycle.go
autonomous: true
requirements: [D-03, D-11, D-13]
must_haves:
  truths:
    - "When the primary pod reaches Ready, the reconciler overrides tier-0 for llm/stt/tts (NOT embed) with the pod URLs"
    - "On every Ready-tick, if the tier-0 slot for llm/stt/tts is empty (cleared by emerg cutback), the reconciler re-asserts the override — closing Pitfall #11 / tech debt #9"
    - "embed is no longer touched by OverrideTier0/RestoreTier0 anywhere in the primary reconciler (D-03 — embed is static)"
    - "The pod lifecycle exposes/forwards TTS on port 8003 (was embed 8002); health-check covers llm/stt/tts/dcgm"
    - "The Plan 02 reconciler stubs owned by this plan are unskipped and assert real behavior"
  artifacts:
    - path: "gateway/internal/primary/reconciler.go"
      provides: "markReady tts override + Ready-tick re-assert"
      contains: "Tier0OverrideURL"
    - path: "gateway/internal/primary/lifecycle.go"
      provides: "podTTSURL on 8003 + port map"
      contains: "8003"
  key_links:
    - from: "evaluateReady re-assert loop"
      to: "loader.Tier0OverrideURL + OverrideTier0"
      via: "1Hz tick re-writes empty slots for llm/stt/tts"
      pattern: "Tier0OverrideURL"
    - from: "markReady OverrideTier0(\"tts\",...)"
      to: "dispatcher Resolve(tts,0)"
      via: "dynamic pod URL written to tts slot"
      pattern: "OverrideTier0(\"tts\""
---

<objective>
Make the primary reconciler drive the TTS tier-0 slot instead of embed, and fix Pitfall #11 (tech debt #9) so the primary re-asserts its tier-0 overrides on each Ready-tick when an emergency cutback has cleared them. Also flip the pod lifecycle port wiring from embed:8002 to tts:8003.

This is the routing brain of the phase. It is engine-agnostic — it drives the `tts` slot regardless of which TTS server (now Chatterbox per Wave 0 GATE 1) runs on pod port 8003. It depends on Plan 03 (the `tts` map slot + `Tier0OverrideURL` getter) and Plan 05 (the pod actually serving tts on 8003). No file overlap with Plan 07 (proxy/main.go), so they run the same wave.

OWNED Plan 02 stubs (must become non-skipped + asserting before this plan completes — REVIEWS action #3): `TestEvaluateReady_ReassertsTier0WhenCleared`, `TestMarkReady_OverridesTTSNotEmbed` (reconciler_test.go).

Purpose: Dynamic-IP TTS routing + close the emerg-clears-primary-slot bug across llm/stt/tts.
Output: reconciler markReady/evaluateReady edits + lifecycle port/URL edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-RESEARCH.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-PATTERNS.md

<interfaces>
From Plan 03 — Loader now has tier0Override keys {llm,stt,tts} and method Tier0OverrideURL(role)(string,bool).
From gateway/internal/primary/reconciler.go:
- markReady (:547-559) currently OverrideTier0 for llm/stt/embed; recoverOpenLifecycle (:1013-1015) same; startDrain (:505-507) + closeLifecycle (:618-620) RestoreTier0 for llm/stt/embed.
- evaluateReady (:394-405) runs at 1Hz; drain-window checks live here.
- health-check 4-endpoint (:895-902) includes urls.Embed.
- r.activePodURLs atomic holds primaryPodURLs.
From gateway/internal/primary/lifecycle.go:
- primaryPodURLs struct (:93-98) field Embed; podEmbedURL (:415-419) uses podPortURL(inst,"8002","/health"); buildPodURLs (:917-924); port forward map (:313-316) has -p 8002:8002; env map (:331-336) forwards PRIMARY_BGEM3_WEIGHTS_*.
From emerg/reconciler.go:511 — RestoreTier0("llm") on cutback is what clears the primary's slot (the bug).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reconciler — swap embed->tts overrides + Pitfall #11 Ready-tick re-assert</name>
  <read_first>
    - gateway/internal/primary/reconciler.go (markReady :547-559, recoverOpenLifecycle :1013-1015, startDrain :505-507, closeLifecycle :618-620, evaluateReady :394-405, health-check :895-902)
    - gateway/internal/primary/reconciler_test.go (Plan 02: TestEvaluateReady_ReassertsTier0WhenCleared, TestMarkReady_OverridesTTSNotEmbed)
    - gateway/internal/upstreams/loader.go (Tier0OverrideURL from Plan 03)
    - gateway/internal/emerg/reconciler.go:511 (RestoreTier0 cutback — the clearing event)
    - 06.7-RESEARCH.md §Pitfall 3 (exact evaluateReady fix) + 06.7-PATTERNS.md reconciler.go section
  </read_first>
  <behavior>
    - markReady (and recoverOpenLifecycle) call OverrideTier0 for "llm","stt","tts" (NOT "embed")
    - startDrain + closeLifecycle RestoreTier0 for "llm","stt","tts" (NOT "embed")
    - evaluateReady: when FSM=Ready and activePodURLs!=nil, for role in {llm,stt,tts} if Tier0OverrideURL(role) returns set=false -> OverrideTier0(role, podURL) + Warn log; embed excluded
    - health-check covers llm/stt/tts/dcgm (no embed)
    - test TestEvaluateReady_ReassertsTier0WhenCleared: clear the tts slot, tick, assert it is re-asserted; embed slot never written
  </behavior>
  <action>
    In `markReady` (:547-559) and `recoverOpenLifecycle` (:1013-1015), replace the `OverrideTier0("embed", ...urls.Embed)` line with `OverrideTier0("tts", stripPrimaryReadinessSuffix(urls.TTS))`. In `startDrain` (:505-507) and `closeLifecycle` (:618-620), replace `RestoreTier0("embed")` with `RestoreTier0("tts")`. Add the Pitfall #11 fix to `evaluateReady` (:394-405) exactly per RESEARCH §Pitfall 3: after the existing drain checks, when `r.activePodURLs.Load()` is non-nil and Loader present, loop `role := range []string{"llm","stt","tts"}` and if `_, set := r.deps.Loader.Tier0OverrideURL(role); !set` then `OverrideTier0(role, stripPrimaryReadinessSuffix(roleURL(*urls, role)))` + `log.Warn("primary re-asserted tier-0 override (emerg cleared it)", "role", role)`. embed is EXCLUDED from this loop (D-03). Add a `roleURL(urls primaryPodURLs, role string) string` helper (switch over LLM/STT/TTS). Update the health-check (:895-902) to probe `urls.TTS` instead of `urls.Embed`. UNSKIP the reconciler_test stubs and make them assert real behavior.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/primary/ -run 'Ready|Reassert|MarkReady' -count=1 -v 2>&1 | grep -q -- '--- PASS' && ! (go test ./internal/primary/ -run 'Ready|Reassert|MarkReady' -count=1 -v 2>&1 | grep -q -- '--- SKIP') && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'OverrideTier0("tts"' gateway/internal/primary/reconciler.go` and no `OverrideTier0("embed"` remains
    - `grep -q 'Tier0OverrideURL' gateway/internal/primary/reconciler.go` inside evaluateReady re-assert loop
    - re-assert loop iterates exactly llm/stt/tts (grep shows the slice literal without "embed")
    - HARD (stub enforcement, REVIEWS #3): `TestEvaluateReady_ReassertsTier0WhenCleared` and `TestMarkReady_OverridesTTSNotEmbed` show `--- PASS`, none `--- SKIP`
  </acceptance_criteria>
  <done>Reconciler overrides llm/stt/tts (not embed) and re-asserts cleared slots at 1Hz; Pitfall #11 stubs unskipped and PASS.</done>
</task>

<task type="auto">
  <name>Task 2: lifecycle — port 8002->8003, podTTSURL, struct + env map</name>
  <read_first>
    - gateway/internal/primary/lifecycle.go (primaryPodURLs :93-98, podEmbedURL :415-419, buildPodURLs :917-924, port map :313-316, env map :331-336)
    - 06.7-RESEARCH.md §Runtime State Inventory (port 8002->8003) + 06.7-PATTERNS.md lifecycle.go section
    - 06.7-WAVE0-GATES.md GATE 1/2 (engine = Chatterbox; weights/HF model id key/sha if applicable)
    - Plan 05 <config_surface> (CHATTERBOX_* env the pod expects; lifecycle forwards the weights/model key/sha + MinIO creds for lazy voice-WAV fetch)
  </read_first>
  <action>
    In `lifecycle.go`: rename the `primaryPodURLs.Embed` field to `TTS` (:93-98). Replace `podEmbedURL` (:415-419) with `podTTSURL` using `r.podPortURL(inst, "8003", "/health")` and update `buildPodURLs` (:917-924) to set `TTS: r.podTTSURL(inst)`. In the port forward map (:313-316) remove `-p 8002:8002` (embed) and add `-p 8003:8003` (tts). In the env map (:331-336) remove the embed weights forward (`PRIMARY_BGEM3_WEIGHTS_KEY`/`_SHA256`) since embed left the pod, and add the Chatterbox weights/model key/sha forward in the identical format (names per WAVE0-GATES / Plan 05 config surface). Ensure the MinIO creds (MINIO_*) remain forwarded so the pod server's lazy voice-WAV fetch works (Plan 05 zero-shot contract). Keep the `stripPrimaryReadinessSuffix` usage consistent with reconciler Task 1.
  </action>
  <verify>
    <automated>cd gateway && grep -q '8003' internal/primary/lifecycle.go && grep -q 'podTTSURL' internal/primary/lifecycle.go && ! grep -q 'podEmbedURL' internal/primary/lifecycle.go && go build ./... && go test ./internal/primary/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'podTTSURL' gateway/internal/primary/lifecycle.go` and `podEmbedURL` removed
    - port map forwards `8003` and no longer forwards `8002`
    - primaryPodURLs has `TTS` field, no `Embed` field
    - MINIO_* creds still forwarded to the pod (lazy voice-WAV fetch dependency)
    - `cd gateway && go test ./internal/primary/... -count=1` green; `go build ./...` exits 0
  </acceptance_criteria>
  <done>Pod lifecycle forwards/discovers TTS on 8003; embed weights forward removed; Chatterbox weights/model + MinIO creds forwarded; primary package builds + tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciler -> loader override map | reconciler writes pod URLs into the shared tier-0 slots read by the dispatcher |
| emerg reconciler -> same map | emerg cutback clears slots (the Pitfall #11 vector) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-17 | Tampering | stale tier-0 slot after emerg cutback | mitigate | evaluateReady re-asserts empty llm/stt/tts slots at 1Hz (window <=1s) — closes tech debt #9 (D-13) |
| T-06.7-18 | Denial of Service | embed slot accidentally cleared off-peak | mitigate | embed removed from reconciler override/restore entirely (D-03); embed stays a static row immune to pod sleep |
| T-06.7-19 | Spoofing | wrong pod URL written to tts slot | mitigate | URL derived from authenticated Vast instance + stripPrimaryReadinessSuffix; health-check on 8003 gates Ready |
</threat_model>

<verification>
- `cd gateway && go test ./internal/primary/... -count=1` green; owned stubs unskipped and PASS
- `cd gateway && go build ./...` exits 0
- no `OverrideTier0("embed"` / `RestoreTier0("embed"` / `podEmbedURL` remain in primary package
</verification>

<success_criteria>
Primary reconciler routes tts (not embed) dynamically, re-asserts cleared slots within 1s, and the pod lifecycle uses port 8003. embed is fully static.
</success_criteria>

<output>
Create `.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-08-SUMMARY.md` when done.
</output>
