---
phase: 11.1
slug: shrink-pod-remove-whisper
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-04
---

# Phase 11.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go stdlib `testing` + `testify/require` (gateway/pod); `pytest` + `httpx` (smoke scripts) |
| **Config file** | `go.mod`, `scripts/integration-smoke/requirements.txt` |
| **Quick run command** | `go test ./gateway/internal/primary/... ./gateway/internal/config/... ./gateway/internal/emerg/vast/... -count=1` |
| **Full suite command** | `go test ./gateway/... -count=1 && python -m pytest scripts/integration-smoke/ -k whisper` |
| **Estimated runtime** | quick ~30s; full ~3min + manual Vast cold-start UAT ~15min |

---

## Sampling Rate

- **After every task commit:** Run quick command above (~30s)
- **After every plan wave:** Run full suite (~3min)
- **Before `/gsd:verify-work`:** Full suite green + Vast 1×3090 cold-start UAT + `smoke-chat-ifix.py` STT path green
- **Max feedback latency:** 30 seconds (per task), 3 min (per wave)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Decision Ref | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 11.1-XX-01 | reconciler-refactor | 1 | D-A4 (preempt migration) | T-11.1-01 (stale URL phantom) | `OverrideTier0("stt",...)` call removed; `markReady` skips role=stt | unit | `go test ./gateway/internal/primary/ -run TestMarkReady -count=1` | ✅ exists (`lifecycle_test.go:45,481`); update fixtures | ⬜ pending |
| 11.1-XX-02 | reconciler-refactor | 1 | D-A4 (drain gate) | — | Reconciler drain gate sums only local-llm + local-embed | unit | `go test ./gateway/internal/primary/ -run TestReconcilerDrain -count=1` | ✅ exists; update assertions | ⬜ pending |
| 11.1-XX-03 | migration-0028 | 2 | D-A4 / D-A5 | T-11.1-03 (out-of-order migration) | Up DELETEs `upstreams.local-stt` + `model_aliases (whisper, local-stt)`; Down restores both | unit | `go test ./gateway/internal/integration_test/ -run TestMigrate0028 -count=1` | ❌ Wave 0 — create `migration_0028_test.go` from `migration_0026_test.go` template | ⬜ pending |
| 11.1-XX-04 | migration-0028 | 2 | D-A5 (alias preserve) | — | `(whisper, openai-whisper) → whisper-1` preserved | unit | `go test ./gateway/internal/integration_test/ -run TestModelAlias -count=1` | ✅ exists; update fixtures | ⬜ pending |
| 11.1-XX-05 | tier-1-stt-audit | 2 | tier-1 fallthrough | — | `POST /v1/audio/transcriptions` resolves openai-whisper directly without breaker-OPEN intermediate when local-stt row absent | integration | `go test ./gateway/internal/integration_test/ -run TestOpenAIWhisperRewrite -count=1` | ✅ exists (`openai_whisper_model_rewrite_test.go`); simplify harness — remove `local-stt` seed + `driveBreaker` | ⬜ pending |
| 11.1-XX-06 | vast-search-filter | 1 | D-A6 (1×3090 primary + 4090 fallback) | — | `DefaultSearchFilters(...)` returns `[primary, fallback]`; primary num_gpus=1 gpu_name=RTX_3090 cap=$0.30; fallback num_gpus=1 gpu_name=RTX_4090 cap=$0.40 | unit | `go test ./gateway/internal/emerg/vast/ -run TestDefaultSearchFilters -count=1` | ❌ Wave 0 — extend `types_test.go` | ⬜ pending |
| 11.1-XX-07 | vast-search-filter | 2 | D-A6 (dispatch) | T-11.1-04 (4090 scarcity) | `evaluateAsleep` iterates filter list; retries fallback when primary returns zero offers | unit | `go test ./gateway/internal/primary/ -run TestReconcilerVastFallback -count=1` | ❌ Wave 0 | ⬜ pending |
| 11.1-XX-08 | config-rename | 1 | D-A6 (env vars) | — | New `PRIMARY_VAST_GPU_NAME_PRIMARY` + `_FALLBACK` + `_PRICE_CAP_PRIMARY` + `_FALLBACK` + `_NUM_GPUS` parse; old `PRIMARY_SPEACHES_IMAGE` dropped from required-env list | unit | `go test ./gateway/internal/config/ -count=1` | ✅ exists; extend (`config_test.go:806,1149-1150`) | ⬜ pending |
| 11.1-XX-09 | pod-dockerfile | 3 | D-A2 (Infinity bundle) + SEED Mudança 1 | — | `docker build pod/primary/` succeeds; image lacks `/opt/speaches-venv` + `/opt/infinity-venv` + `silero_*.onnx`; size ≤8GB | smoke | `docker buildx build --target final pod/primary/ && docker image inspect ghcr.io/ifixtelecom/converseai-primary-pod:test --format '{{.Size}}'` | manual (CI workflow) | ⬜ pending |
| 11.1-XX-10 | pod-supervisord | 3 | SEED Mudanças 2 + bundle Infinity | — | supervisord.conf lacks `[program:speaches]` + `[program:infinity]`; only chatterbox+llama+dcgm+health-bridge remain | source | `grep -c '\\[program:speaches\\|\\[program:infinity' pod/primary/supervisord.conf` returns 0 | manual | ⬜ pending |
| 11.1-XX-11 | pod-onstart | 3 | SEED Mudança 3 | — | `pod/onstart.sh` lacks whisper tarball curl + `mkdir /weights/whisper` | source | `grep -c 'whisper' pod/onstart.sh` returns 0 | manual | ⬜ pending |
| 11.1-XX-12 | pod-health-bridge | 3 | SEED Mudança 7 | — | `health-bridge/main.go` probes list lacks `:8001` entry; `probes.go` lacks `probeSTT` function | unit | `go test ./pod/health-bridge/ -count=1` | ✅ exists; extend assertions | ⬜ pending |
| 11.1-XX-13 | pod-env-docs | 4 | SEED Mudanças 4,5,6,13 | — | `pod/.env.example` + `pod/docker-compose.yml` + `pod/README.md` + `pod/weights/README.md` lack whisper/speaches refs; prod ai-gateway `.env` operator task documented | source | `grep -ci 'whisper\\|speaches' pod/.env.example pod/docker-compose.yml` returns 0 | manual | ⬜ pending |
| 11.1-XX-14 | smoke-tests | 4 | SEED Mudança 15 | — | `scripts/integration-smoke/` STT tier-0 specific tests removed; tier-1 OpenAI whisper smoke continues green | smoke | `python -m pytest scripts/integration-smoke/ -k whisper` | partial — drop tier-0 cases | ⬜ pending |
| 11.1-XX-15 | uat-cold-start | 5 | gain table validation | — | Vast 1×3090 pod cold-start ≤15min; LLM+TTS+DCGM endpoints up; no `:8001` listener; cost ≤$0.30/h actual | manual UAT | `pod/scripts/vast-ai.sh create --gpu RTX_3090 --price 0.30`; observe lifecycle FSM → Ready; `curl :9100/health` | manual | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `gateway/internal/db/migrations/0028_remove_local_stt_upstream.sql` — Up DELETEs both rows; Down restores via INSERT (model on `0026_*.sql` restorative pattern)
- [ ] `gateway/internal/integration_test/migration_0028_test.go` — model on `migration_0026_test.go`; asserts Up DELETEs both rows + Down restores both
- [ ] `gateway/internal/emerg/vast/types_test.go` — extend with `TestDefaultSearchFilters` covering primary+fallback composition + empty-cap handling
- [ ] `gateway/internal/primary/reconciler_test.go` — extend with `TestReconcilerVastFallback` (fakeVast returns empty primary, non-empty fallback, asserts pod created on 4090)
- [ ] Update `openai_whisper_model_rewrite_test.go` harness — remove `local-stt` seed row + `driveBreaker` for `local-stt`
- [ ] Update `lifecycle_test.go:45,481` — remove `PrimarySpeachesImage` field reference; update env-key list
- [ ] Update `config_test.go:806,1149-1150` — drop `PRIMARY_SPEACHES_IMAGE` from required-env list + drop sha256 assertion
- [ ] Update `pod/health-bridge/probes_test.go` (if exists) — assert no probeSTT registration

---

## Manual-Only Verifications

| Behavior | Decision Ref | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Vast 1×3090 cold-start ≤15min on real host | D-A6 + gain table | Requires Vast spend + real GPU + WAN download | `pod/scripts/vast-ai.sh search --gpu RTX_3090 --price 0.30 --inet-down 200`; verify ≥3 offers; create pod; observe FSM Ready ≤15min; teardown |
| 4090 fallback fleet availability ≥3 offers cap $0.40 | D-A6 + assumption #2 from RESEARCH | Vast inventory snapshot needed pre-commit; market dynamic | `pod/scripts/vast-ai.sh search --gpu RTX_4090 --price 0.40 --inet-down 200`; record offer count + host IDs; if <3, escalate cap or geofilter relax |
| Operator removes `UPSTREAM_STT_URL` from `/opt/ai-gateway-prod/.env` | D-A4 deploy hygiene | n8n-ia-vm operator action post-deploy | RUNBOOK-DEPLOY.md task added; grep audit `ssh n8n-ia-vm grep UPSTREAM_STT_URL /opt/ai-gateway-prod/.env` returns empty |
| Image size ≤8GB post-build | gain table | CI build artifact inspection | `docker image inspect <tag> --format '{{.Size}}'`; compare against pre-shrink baseline ~10-12GB |
| `/v1/audio/transcriptions` request via live prod gateway returns OpenAI tier-1 200 | tier-1 fallthrough | Live prod traffic; tenant chat-ifix or test tenant | `curl -X POST https://ai-gateway.converse-ai.app/v1/audio/transcriptions -H "Authorization: Bearer <chat-ifix-key>" -F file=@sample.wav -F model=whisper` returns 200 + JSON transcript |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (per task), < 3min (per wave)
- [ ] `nyquist_compliant: true` set in frontmatter after Wave 0 lands

**Approval:** pending
