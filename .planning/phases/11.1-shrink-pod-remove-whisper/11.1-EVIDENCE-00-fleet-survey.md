---
phase: 11.1-shrink-pod-remove-whisper
plan: 00
type: evidence
gate: T-11.1-04 (4090 EU fleet scarcity)
date: 2026-06-04
operator: pedro@ops-claude
api_endpoint: https://vast.ai/api/v0/bundles/
---

# 11.1 Wave 0 — Vast Fleet Survey (Pre-Code-Change Gate)

## Method

Direct Vast API `GET /bundles/?q=...` (script `pod/scripts/vast-ai.sh search`
broken — curl rejects nested braces in URL; bypassed via direct curl with
`--data-urlencode`. Also: Vast API schema now requires dict form
(`{eq: ...}`) for `gpu_name` / `num_gpus` / `rentable` — outer script still
sends primitives, fails `bad_request`. Tracking separately).

Common filters: `num_gpus=1`, `disk_space≥50GB`, `reliability≥0.98`,
`inet_down≥200 Mbps`, `rentable=true`, `verified=true`. EU geo filter applied
post-query (regex match on country code in `.geolocation`).

## Primary shape — 1×RTX 3090 @ ≤$0.30/h

| Metric | Value | Gate (≥3 EU) |
|--------|-------|--------------|
| Total offers global | **21** | ✓ PASS |
| Offers in EU | **10** | ✓ PASS |

Top 3 cheapest EU host_ids:

| id | host_id | geo | $/h | inet Mbps |
|----|---------|-----|-----|-----------|
| 18990238 | 135723 | Czechia, CZ | 0.218 | 431.8 |
| 39522139 | 51345 | Poland, PL | 0.242 | 1779.2 |
| 37083760 | 38488 | Hungary, HU | 0.244 | 427.7 |

## Fallback shape — 1×RTX 4090 @ ≤$0.40/h

| Metric | Value | Gate (≥3 EU) |
|--------|-------|--------------|
| Total offers global | **0** | ✗ **FAIL** |
| Offers in EU | **0** | ✗ **FAIL** |

T-11.1-04 (4090 EU fleet scarcity) **confirmed**. Probed mitigations:

| Variant | Total | EU |
|---------|-------|-----|
| 4090 @ $0.40 inet≥200 | 0 | 0 |
| 4090 @ $0.40 inet≥100 | 0 | 0 |
| 4090 @ $0.45 inet≥200 | 0 | 0 |
| 4090 @ $0.50 inet≥200 | 1 | 0 |
| 4090 @ $0.55 inet≥200 | 2 | 1 |
| **4090 @ $0.60 inet≥200** | **16** | **11** |

Neither RESEARCH Pitfall #6 mitigation (drop inet to 100 Mbps OR bump cap to $0.50)
is sufficient. Only `$0.60/h` cap clears the ≥3 EU gate.

Top 3 cheapest EU host_ids at $0.60 cap:

| id | host_id | geo | $/h | inet Mbps |
|----|---------|-----|-----|-----------|
| 35654911 | 537424 | France, FR | 0.533 | 877.5 |
| 33103568 | 411489 | Romania, RO | 0.576 | 719.6 |
| 39291598 | 133935 | Bulgaria, BG | 0.602 | 4423.2 |

## Expanded survey — fallback-shape comparison

| Shape | Cap | Total | EU | Cheapest EU $/h |
|-------|-----|-------|-----|------------------|
| 2×RTX 3090 | $0.45 | 3 | 0 | — |
| 2×RTX 3090 | $0.50 | 9 | 1 | $0.483 |
| **2×RTX 3090** | **$0.60** | **16** | **7** | **$0.424** |
| 1×RTX 4090 | $0.60 | 16 | 11 | $0.533 |
| 1×RTX 5090 | $0.80 | 5 | 1 | $0.522 |
| 1×RTX 5090 | $1.00 | 19 | 11 | $0.522 (outlier; typical ~$0.90+) |

## Operator decision (2026-06-04)

**Adopt shape: 1×RTX 3090 primary + 2×RTX 3090 fallback.**

- Primary: 1×3090 @ ≤$0.30/h — 10 EU offers ✓
- Fallback: 2×3090 @ ≤$0.60/h — 7 EU offers ✓, cheapest $0.42/h
- Both shapes same GPU model — single driver/CUDA matrix, simpler ops
- 2×3090 fallback is cheaper than 1×4090 ($0.42 vs $0.53) AND clears gate at same cap

**Scope-shift for Plan 01:** Original D-A6 said `1×3090 + 1×4090`. Updated:
`1×3090 (num_gpus=1) + 2×3090 (num_gpus=2)`. Plan 01 SearchFilter pair must
swap fallback `gpu_name="RTX 4090"` → `"RTX 3090"`, `num_gpus 1 → 2`,
`dph_total cap 0.40 → 0.60`. All other Plan 01 tasks (drop role=stt,
SearchFilter pair introduction, reconciler/lifecycle wiring) unaffected by
this swap.

## Anomalies recorded

1. **`pod/scripts/vast-ai.sh search` broken** — curl rejects nested braces in
   query URL (`curl: (3) nested brace in URL position 84`). Bypassed via
   direct API call with `--data-urlencode`. Fix-up backlog item.
2. **Vast API schema changed** — `gpu_name`, `num_gpus`, `rentable` now
   require dict form (`{eq: ...}`). Script sends primitives → `bad_request`.
   Same fix scope as #1.

Both anomalies should be addressed in a follow-up plan (not blocking 11.1
since survey ran via direct API).
