---
phase: 11.1-shrink-pod-remove-whisper
plan: 02
type: execute
wave: 2
depends_on: ["11.1-01"]
files_modified:
  - gateway/db/migrations/0028_remove_local_stt_upstream.sql
  - gateway/internal/integration_test/migration_0028_test.go
  - gateway/internal/integration_test/openai_whisper_model_rewrite_test.go
  - gateway/internal/integration_test/model_alias_test.go
  - gateway/internal/integration_test/upstreams_loader_test.go
  - gateway/internal/integration_test/upstreams_listen_test.go
autonomous: true
requirements:
  - "D-A4 (DELETE migration for upstreams.local-stt — not soft-disable)"
  - "D-A5 (DELETE model_aliases (whisper, local-stt); PRESERVE (whisper, openai-whisper) → whisper-1)"
  - "SEED Mudança 9 (SQL migration 0028 DELETE rows + cascade test)"
  - "tier-1 STT fallthrough (Mudança 12 audit /v1/audio/transcriptions through openai-whisper)"
threat_refs:
  - T-11.1-03 (HIGH: must NOT land before Plan 01 ships; depends_on enforces)

must_haves:
  truths:
    - "ai_gateway.upstreams row 'local-stt' DELETEd by Up"
    - "ai_gateway.model_aliases (whisper, local-stt) row DELETEd by Up"
    - "ai_gateway.model_aliases (whisper, openai-whisper) → whisper-1 PRESERVED"
    - "ai_gateway.upstreams row 'openai-whisper' PRESERVED"
    - "Down restores both deleted rows via ON CONFLICT DO NOTHING INSERTs"
    - "/v1/audio/transcriptions request resolves to openai-whisper directly without breaker-OPEN intermediate"
  artifacts:
    - path: "gateway/db/migrations/0028_remove_local_stt_upstream.sql"
      provides: "+goose Up DELETEs both rows; +goose Down restores Phase 06.9 baseline shape"
      contains: "DELETE FROM ai_gateway.upstreams WHERE name = 'local-stt'"
    - path: "gateway/internal/integration_test/migration_0028_test.go"
      provides: "TestMigrate0028 Up→assert deletions; Down→assert restoration; goose up/down/up roundtrip identical"
  key_links:
    - from: "POST /v1/audio/transcriptions {model=whisper}"
      to: "openai whisper-1 director"
      via: "resolver matches (whisper, openai-whisper) → whisper-1; dispatcher picks role=stt with only one upstream"
      pattern: "openai_whisper_director\\.go"
---

<objective>
Wave 2 DB migration: DELETE the `local-stt` upstream row + `(whisper, local-stt)`
model alias from `ai_gateway.*`. Bundle with integration test fixture updates so
the gateway's STT request path verifies tier-1 OpenAI fallthrough end-to-end with
zero breaker drives.

This plan MUST NOT merge before Plan 01 (Wave 1 code refactor). Wave-ordering
enforced via `depends_on: ["11.1-01"]`. T-11.1-03 mitigation depends on this.

Purpose: Remove dead schema rows. Confirm tier-1 OpenAI whisper-1 absorbs all
STT traffic per D-A5 preservation contract.

Output:
- New goose migration `0028_remove_local_stt_upstream.sql` with restorative Down
- New `migration_0028_test.go` (Wave 0 gap) modeled on `migration_0026_test.go`
- Updated `openai_whisper_model_rewrite_test.go` harness (no local-stt seed, no driveBreaker)
- Updated `model_alias_test.go` + `upstreams_loader_test.go` + `upstreams_listen_test.go` fixtures dropping local-stt seed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-CONTEXT.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-VALIDATION.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-01-SUMMARY.md
@gateway/db/migrations/0026_evolve_model_aliases_per_upstream.sql
@gateway/db/migrations/0027_openrouter_target_deepseek_v4_flash.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author migration 0028 + matching migration_0028_test.go</name>
  <files>
    gateway/db/migrations/0028_remove_local_stt_upstream.sql,
    gateway/internal/integration_test/migration_0028_test.go
  </files>
  <read_first>
    .planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md (Code Examples: Pattern Migration Up/Down + Pitfall #3),
    gateway/db/migrations/0026_evolve_model_aliases_per_upstream.sql (full file — restorative Down precedent),
    gateway/db/migrations/0008_seed_upstreams.sql (lines 5-18 — original local-stt seed shape),
    gateway/db/migrations/0017_evolve_upstreams_shed_thresholds.sql (lines 35-67 — current upstreams column set),
    gateway/internal/integration_test/migration_0026_test.go (full file — test template)
  </read_first>
  <behavior>
    - +goose Up DELETEs `model_aliases (whisper, local-stt)` first, then `upstreams.local-stt` (order arbitrary but specific-to-broad recommended per Pitfall #3)
    - +goose Down INSERTs both with ON CONFLICT DO NOTHING using Phase 06.9 baseline shape (alias='whisper', upstream='stt', target='Systran/faster-whisper-large-v3', upstream_name='local-stt')
    - migration_0028_test.go: TestMigrate0028Up — apply 0028, query for both rows by name/PK, assert COUNT(*)=0 each
    - TestMigrate0028Down — Up then Down, assert COUNT(*)=1 for upstreams.local-stt + COUNT(*)=1 for model_aliases (whisper, local-stt)
    - TestMigrate0028Roundtrip — goose up → down → up on clean DB; schema dump comparison identical pre/post
    - audit_log historical `upstream_name='local-stt'` strings untouched (text-only, no FK)
  </behavior>
  <action>
    1. Create `gateway/db/migrations/0028_remove_local_stt_upstream.sql` using the exact pattern from RESEARCH Code Examples (Migration Up/Down):
       - +goose Up StatementBegin/StatementEnd block: SET search_path = ai_gateway, public; DELETE FROM model_aliases WHERE alias='whisper' AND upstream_name='local-stt'; DELETE FROM upstreams WHERE name='local-stt';
       - +goose Down StatementBegin/StatementEnd block: SET search_path = ai_gateway, public; INSERT INTO upstreams (name, role, tier, url_env, auth_bearer_env) VALUES ('local-stt', 'stt', 0, 'UPSTREAM_STT_URL', NULL) ON CONFLICT (name) DO NOTHING; INSERT INTO model_aliases (alias, upstream, target, upstream_name) VALUES ('whisper', 'stt', 'Systran/faster-whisper-large-v3', 'local-stt') ON CONFLICT (alias, upstream_name) DO NOTHING;
       - Add header comment citing Phase 11.1 D-A4/D-A5 and SEED-010.
       - Verify column set matches `0017_evolve_upstreams_shed_thresholds.sql` current shape (read that file first; if extra columns added since 0008 they must appear in Down INSERT or default-not-null will fire).
    2. Create `gateway/internal/integration_test/migration_0028_test.go` modeled on `migration_0026_test.go`:
       - TestMigrate0028Up: seed Phase 06.9 baseline via prior migrations; apply 0028 Up; assert `SELECT COUNT(*) FROM upstreams WHERE name='local-stt'` returns 0 AND `SELECT COUNT(*) FROM model_aliases WHERE alias='whisper' AND upstream_name='local-stt'` returns 0.
       - TestMigrate0028Up_PreservesOpenAIWhisper: after Up, assert `(whisper, openai-whisper) → whisper-1` row still present AND `upstreams.openai-whisper` row still present (per D-A5).
       - TestMigrate0028Down: apply Up then Down; assert both rows re-present with exact shape (name/role/tier/url_env/auth_bearer_env for upstream; alias/upstream/target/upstream_name for alias).
       - TestMigrate0028Roundtrip: up→down→up on clean DB; assert final state matches Up-only state row-for-row.
       - Reuse the integration_test/migration helper (`newTestDB`, `applyMigrationsTo` or similar found in `migration_0026_test.go`).
  </action>
  <verify>
    <automated>
      cd /home/pedro/projetos/pedro/gpu-ifix && go test ./gateway/internal/integration_test/ -run TestMigrate0028 -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - File `gateway/db/migrations/0028_remove_local_stt_upstream.sql` exists
    - `grep -c '+goose Up\\|+goose Down' gateway/db/migrations/0028_remove_local_stt_upstream.sql` returns 2
    - `grep -c "DELETE FROM ai_gateway.upstreams" gateway/db/migrations/0028_remove_local_stt_upstream.sql` returns ≥1
    - `grep -c "INSERT INTO ai_gateway.upstreams" gateway/db/migrations/0028_remove_local_stt_upstream.sql` returns ≥1
    - `go test ./gateway/internal/integration_test/ -run "TestMigrate0028Up|TestMigrate0028Down|TestMigrate0028Roundtrip|TestMigrate0028Up_PreservesOpenAIWhisper" -count=1` exits 0
  </acceptance_criteria>
  <done>Migration 0028 applied + reverted cleanly in test DB; openai-whisper preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simplify integration test harnesses — drop local-stt seed + driveBreaker</name>
  <files>
    gateway/internal/integration_test/openai_whisper_model_rewrite_test.go,
    gateway/internal/integration_test/model_alias_test.go,
    gateway/internal/integration_test/upstreams_loader_test.go,
    gateway/internal/integration_test/upstreams_listen_test.go
  </files>
  <read_first>
    .planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md (Pitfall #4 + Component Responsibilities table row for openai_whisper_model_rewrite_test.go),
    gateway/internal/integration_test/openai_whisper_model_rewrite_test.go (full file especially lines 25, 140-219, 245-315),
    gateway/internal/integration_test/model_alias_test.go (full file — locate local-stt seed rows),
    gateway/internal/integration_test/upstreams_loader_test.go (full file — locate local-stt seed),
    gateway/internal/integration_test/upstreams_listen_test.go (full file — locate local-stt seed)
  </read_first>
  <behavior>
    - openai_whisper_model_rewrite_test.go: tier-1 fallthrough test no longer seeds local-stt + no longer calls driveBreaker for local-stt; resolver should hit tier-1 directly (only one upstream registered for role=stt)
    - model_alias_test.go: fixtures reflect 5-upstream shape (local-llm, openrouter-chat, openai-whisper, local-embed, openai-embed) — local-stt seed row gone
    - upstreams_loader_test.go: Loader test fixtures drop local-stt seed row from any in-test DB insert
    - upstreams_listen_test.go: LISTEN/NOTIFY tests fixture drop local-stt seed
    - All tests continue green
  </behavior>
  <action>
    Per Pitfall #4 + RESEARCH Component Responsibilities:
    1. `openai_whisper_model_rewrite_test.go:198-219` harness: locate the `seed(...)` / fixture setup that inserts the `local-stt` upstream row + the `(whisper, local-stt)` alias. DELETE those seed lines. Locate the `driveBreaker(t, bs, "local-stt", 500, 3)` call and DELETE it — with only one upstream registered for role=stt, dispatcher routes to openai-whisper directly without needing breaker drive.
    2. Update the test's expectation comment (~line 25) to reflect "Phase 11.1: only (whisper, openai-whisper)→whisper-1 alias exists; resolver returns whisper-1 directly".
    3. Verify `Test_PassThrough...` (the no-row pass-through case) still has its zero-row scenario — does not need local-stt; just confirm it still asserts the original model name passes through.
    4. `model_alias_test.go`: grep for `local-stt` and delete seed rows; if any test asserts a count of upstreams or aliases that includes local-stt, update from 6→5.
    5. `upstreams_loader_test.go`: drop any seed inserting `local-stt` row; if Loader-presence assertion exists, drop that assertion.
    6. `upstreams_listen_test.go`: drop any seed inserting `local-stt` row from LISTEN test fixtures.
    7. Run `grep -rn '"local-stt"' gateway/internal/integration_test/` — should return ONLY hits in `migration_0028_test.go` (which legitimately references it in Up/Down assertions).
  </action>
  <verify>
    <automated>
      cd /home/pedro/projetos/pedro/gpu-ifix && go test ./gateway/internal/integration_test/ -run "TestOpenAIWhisperRewrite|TestModelAlias|TestUpstreamsLoader|TestUpstreamsListen" -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'driveBreaker.*local-stt' gateway/internal/integration_test/openai_whisper_model_rewrite_test.go` returns 0
    - `grep -rn '"local-stt"' gateway/internal/integration_test/` returns matches ONLY in `migration_0028_test.go`
    - `go test ./gateway/internal/integration_test/ -run "TestOpenAIWhisperRewrite" -count=1` exits 0
    - `go test ./gateway/internal/integration_test/ -run "TestModelAlias|TestUpstreamsLoader|TestUpstreamsListen" -count=1` exits 0
    - Full suite: `go test ./gateway/... -count=1` exits 0
    - Mudança 11 probe-loop verification: `go test ./gateway/internal/upstreams/ -run "TestProbeRegistration|TestLoaderReload" -count=1` confirms zero `local-stt` probe registrations after Loader.Reload() post-migration. If no such test exists, add a regression test under `gateway/internal/upstreams/loader_test.go` that seeds the post-0028 DB shape and asserts the probe set returned by Reload() contains no entry with upstream_name=='local-stt'.
  </acceptance_criteria>
  <done>Integration tests reflect post-shrink schema; tier-1 STT path verified end-to-end with single upstream registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| migration tool → live DB | goose applies SQL; rollback path exercised by Down |
| dispatcher → resolver (post-migration) | resolver returns whisper-1 with only one upstream registered for role=stt |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-03 | Denial of Service | Out-of-order migration vs gateway binary | mitigate | `depends_on: ["11.1-01"]` enforces Wave 1 code change merges first; Plan 06 UAT additionally verifies live gateway post-deploy |
| T-11.1-Migration-FK | Tampering (data integrity) | model_aliases.upstream_name FK | accept | RESEARCH Pitfall #3 + Assumption A2 verified: column is plain TEXT with COMMENT, no FK. Order of DELETE arbitrary; specific-to-broad chosen for clarity |
</threat_model>

<verification>
- Tasks 1 + 2 verify commands green
- Full integration suite: `go test ./gateway/internal/integration_test/ -count=1` (~2min)
- Manual: `psql` on local dev DB → run `goose up`/`goose down`/`goose up` → `pg_dump --schema-only` diff returns empty
</verification>

<success_criteria>
- Migration 0028 committed; CI green; openai-whisper preserved
- Integration tests no longer reference local-stt outside migration_0028_test.go
</success_criteria>

<output>
Create `.planning/phases/11.1-shrink-pod-remove-whisper/11.1-02-SUMMARY.md` when done
</output>
