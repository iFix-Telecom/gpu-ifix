---
phase: 11.1-shrink-pod-remove-whisper
plan: 06
subsystem: infra
tags: [vast-ai, pod-uat, deferred, cold-start, tier-1-stt]
status: deferred

requires:
  - phase: 11.1
    provides: Waves 1-4 merged (gateway code + DB migration + pod image + docs)
provides:
  - Plan 06 marked DEFERRED — UAT requires CI image publish + Vast pod $ + prod SSH + WAV sample
  - Plan 06 PLAN.md frontmatter D-A6 reference updated for fallback shape pivot (2×3090 @ $0.60)
affects: [follow-up live UAT, primary-gpu-shape-11.1-final memory note creation]

tech-stack:
  added: []

key-files:
  created: []
  modified:
    - .planning/phases/11.1-shrink-pod-remove-whisper/11.1-06-PLAN.md (D-A6 reference patched)

key-decisions:
  - "Defer live UAT: phase 11.1 closes passed_partial (Phase 11 pattern). UAT resumes when CI publishes phase-11.1-<sha> pod image + operator schedules Vast pod"

requirements-completed: []
requirements-deferred:
  - "D-A6 live cold-start verification (Task 1)"
  - "gain table validation: image size ≤8GB / VRAM ≤22GB / cold-start ≤15min (Task 1)"
  - "tier-1 STT fallthrough live curl via prod gateway (Task 2)"
  - "memory note primary-gpu-shape-11.1-final.md (Task 2)"
  - "T-11.1-02 prod .env UPSTREAM_STT_URL removal audit (Task 2)"
  - "T-11.1-04 post-deploy re-verification (Task 1) — pre-code-change Wave 0 gate (EVIDENCE-00) already mitigated via shape pivot"

duration: deferred
completed: deferred
---

# Phase 11.1 Plan 06 Summary — DEFERRED

**Wave 5 live UAT deferred. Phase 11.1 closes `passed_partial` following Phase 11 pattern.**

## Why deferred

Plan 06 = 2 human-verify checkpoints requiring infrastructure not available autonomously:

1. **Task 1 cold-start UAT** needs a CI-published `ghcr.io/ifixtelecom/converseai-primary-pod:phase-11.1-<sha>` image. Phase 11.1 commits are local on branch `gsd/phase-06.9-close`, not yet pushed to `develop`. CI hasn't run. No pod image to deploy.
2. **Task 2 tier-1 STT live curl** needs (a) prod gateway already serving the post-migration schema, (b) operator-applied `/opt/ai-gateway-prod/.env` cleanup (`UPSTREAM_STT_URL` removal), (c) WAV sample, (d) Vast pod $ ≈$0.30/h.
3. Wave 0 gate (EVIDENCE-00) already pre-mitigated T-11.1-04 by pivoting fallback shape 1×4090 → 2×3090. Post-deploy re-verification is wanted but not blocking — the pivot is conservative (same GPU model both shapes).

## Plan 06 shape reference patch

`PLAN.md` frontmatter `requirements:` line for D-A6 still cited the pre-Wave-0 shape `1×4090 fallback cap $0.40`. Patched in this commit to reference the operator-approved `2×3090 fallback cap $0.60` per EVIDENCE-00 + Plan 01 amendment. Plan body text (Task 1 step 1 `--gpu "RTX 3090" --num-gpus 1`) was already shape-agnostic — only checks primary path. Task 2 memory-note checklist (step 4) still lists `PRIMARY_VAST_GPU_NAME_FALLBACK=RTX 4090` + `PRICE_CAP_FALLBACK=0.40` + `PRIMARY_VAST_NUM_GPUS=1` — that checklist needs update at UAT-resume time using the post-Wave-1 env var names (`PRIMARY_VAST_GPU_NAME_FALLBACK=RTX 3090`, `PRICE_CAP_FALLBACK=0.60`, `PRIMARY_VAST_NUM_GPUS_PRIMARY=1`, `_FALLBACK=2`).

## Resume signal

When Wave 5 UAT can run:

```
/gsd-execute-phase 11.1 --wave 5
```

Pre-conditions:
- `gsd/phase-06.9-close` (or successor branch) merged to `develop`
- GHA `build-primary-pod.yml` published image with new `phase-11.1-<sha>` tag
- Operator confirmed `UPSTREAM_STT_URL` removed from `/opt/ai-gateway-prod/.env`
- WAV sample available on ops-claude

## Self-Check: PASSED (defer disposition recorded)
