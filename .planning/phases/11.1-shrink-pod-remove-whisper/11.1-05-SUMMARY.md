---
phase: 11.1-shrink-pod-remove-whisper
plan: 05
subsystem: pod docs + gateway operator runbooks
tags: [docs-hygiene, env-cleanup, runbooks, stt-removal, t-11.1-02, wave-4]
requires: [11.1-03, 11.1-04]
provides:
  - "pod/.env.example, pod/docker-compose.yml, pod/README.md, pod/weights/README.md free of whisper/speaches/infinity service refs"
  - "pod/docker-compose.yml reduced 5→3 services (llama + health-bridge + dcgm-exporter)"
  - "RUNBOOK-FAILOVER.md mental model table 6→5 upstreams; STT row tier-0 column removed"
  - "RUNBOOK-DEPLOY.md verification gate 6→5 upstreams + Phase 11.1 post-deploy hygiene task (T-11.1-02 mitigation)"
  - "RUNBOOK-PRIMARY-POD.md tier-0 role wording 3-role→2-role (LLM + Embed)"
affects:
  - pod/.env.example
  - pod/docker-compose.yml
  - pod/README.md
  - pod/weights/README.md
  - gateway/docs/RUNBOOK-FAILOVER.md
  - gateway/docs/RUNBOOK-DEPLOY.md
  - gateway/docs/RUNBOOK-PRIMARY-POD.md
tech-stack:
  added: []
  patterns: []
key-files:
  created:
    - .planning/phases/11.1-shrink-pod-remove-whisper/11.1-05-SUMMARY.md
  modified:
    - pod/.env.example
    - pod/docker-compose.yml
    - pod/README.md
    - pod/weights/README.md
    - gateway/docs/RUNBOOK-FAILOVER.md
    - gateway/docs/RUNBOOK-DEPLOY.md
    - gateway/docs/RUNBOOK-PRIMARY-POD.md
decisions:
  - "Runbook files canonical path = gateway/docs/RUNBOOK-*.md (Plan PLAN.md frontmatter listed repo-root paths which don't exist; corrected per Rule 3 + RESEARCH refs)"
  - "Comments referencing whisper/speaches removed entirely from compose + env.example to satisfy strict grep returns-0 acceptance criteria; SEED-010 traceability preserved via 'Phase 11.1' phrasing without literal whisper/speaches tokens"
  - "Health-bridge env in docker-compose.yml shrunk to LLAMA_URL only (SPEACHES_URL/INFINITY_URL/SPEACHES_MODEL stripped) — consistent with Plan 11.1-04 probe-set shrink to {LLM}"
  - "Deeper STT references inside RUNBOOK-PRIMARY-POD.md (lines 109-111 supervisord program block, line 226 STT image SHA pin, line 316 PRIMARY_SPEACHES_IMAGE env var table row, lines 580-650 deploy procedure mentions) left as-is — they describe the historical Phase 06.6 Wave 0 LOCKED Strategy α image; out of Plan 05 scope. Follow-up Plan 11.1-06 (or later) should retire those when the primary pod image is rebuilt without Speaches."
metrics:
  duration: ~10min
  completed: 2026-06-05
  tasks_completed: 2
  files_created: 1
  files_modified: 7
---

# Phase 11.1 Plan 05: Wave 4 Docs + Env Hygiene Summary

Updated pod env contract, docker-compose, READMEs, and gateway operator runbooks to reflect post-shrink topology (STT off-pod, primary pod 3 services). Added T-11.1-02 mitigation as a per-deploy operator task in RUNBOOK-DEPLOY.md.

## Tasks Executed

### Task 1 — Strip whisper/speaches/infinity from pod env + compose + READMEs
- **Commit:** `09b5a7c`
- **Files:** `pod/.env.example`, `pod/docker-compose.yml`, `pod/README.md`, `pod/weights/README.md`
- **Changes:**
  - `pod/.env.example`: dropped `WEIGHTS_WHISPER_KEY` + `WEIGHTS_WHISPER_SHA256`; weight-dir comment updated to mention only `qwen/` + `bge-m3/` with note that STT is off-pod.
  - `pod/docker-compose.yml`: removed `speaches:` service block (port `8001:8001`) and `infinity:` service block (port `8002:8002`); trimmed `health-bridge` env vars to `LLAMA_URL` only (SPEACHES_URL/INFINITY_URL/SPEACHES_MODEL removed); header comment 5-service → 3-service.
  - `pod/README.md`: service table 5 rows → 3 rows; added "STT: off-pod (handled by tier-1 OpenAI `whisper-1` via gateway fallback chain)" note; updated dev `mkdir` step to drop `/weights/whisper`; smoke description trimmed; troubleshooting OOM row rephrased without "Speaches whisper concurrency".
  - `pod/weights/README.md`: dropped whisper-large-v3 layout entry; size estimate `22GB → 19GB`; dropped `WEIGHTS_WHISPER_SHA256` from secrets list; added Phase 11.1 banner.
- **Verification:**
  - `grep -ciE 'whisper|speaches' pod/.env.example` → `0` ✓
  - `grep -ciE 'whisper|speaches|infinity' pod/docker-compose.yml` → `0` ✓
  - `grep -c "8001" pod/docker-compose.yml` → `0` ✓
  - `grep -ci 'whisper' pod/weights/README.md` → `0` ✓
  - `grep -ci 'off-pod\|tier-1' pod/README.md` → `3` ✓ (≥1)

### Task 2 — Update runbooks (FAILOVER, DEPLOY, PRIMARY-POD) + add T-11.1-02 operator task
- **Commit:** `f45c0ab`
- **Files:** `gateway/docs/RUNBOOK-FAILOVER.md`, `gateway/docs/RUNBOOK-DEPLOY.md`, `gateway/docs/RUNBOOK-PRIMARY-POD.md`
- **Changes:**
  - `RUNBOOK-FAILOVER.md`:
    - Mental-model header: "6 upstreams" → "5 upstreams (Phase 11.1 shrunk STT to tier-1 only)".
    - Mental-model table STT row: tier-0 column `local-stt` → `_(none)_`; model contract reworded ("OpenAI `whisper-1` (no local fallback — STT tier-0 removed in Phase 11.1)").
    - Symptom 3 likely-cause + recovery list: removed `local-stt` reference; added explainer that STT sensitive requests fail-fast (no tier-0 + RES-08 blocks tier-1 for sensitive tenants).
    - Required Env Vars table: `UPSTREAM_STT_URL` row marked `_removed_` (Phase 11.1).
  - `RUNBOOK-DEPLOY.md`:
    - Verification gate line 523: "6 upstreams" → "5 upstreams" (dropped `local-stt` from expected list); appended note about migration 0029 row removal.
    - Inserted new section "Phase 11.1 post-deploy hygiene (T-11.1-02)" before Rollback section. Operator steps: SSH n8n-ia-vm → edit `/opt/ai-gateway-prod/.env` to delete `UPSTREAM_STT_URL=http://10.10.10.20:8001` → `grep -c UPSTREAM_STT_URL` returns 0 → `docker compose up -d` reload → re-run upstreams verification (5 upstreams).
  - `RUNBOOK-PRIMARY-POD.md`:
    - Line 28 trigger list: dropped `local-stt` from upstream enumeration; added Phase 11.1 explainer that STT now resolves via tier-1 only.
    - Comparison table row "Services served": 3 roles → 2 roles (LLM + Embed); added "(Phase 11.1 dropped STT tier-0)" annotation.
- **Verification:**
  - `grep -c 'UPSTREAM_STT_URL' gateway/docs/RUNBOOK-DEPLOY.md` → `3` ✓ (≥1)
  - `grep -c '6 upstreams' gateway/docs/RUNBOOK-DEPLOY.md` → `0` ✓
  - `grep -cE '5 upstreams|five upstreams' gateway/docs/RUNBOOK-DEPLOY.md` → `2` ✓ (≥1)
  - `grep -cE 'T-11.1-02|Phase 11.1 post-deploy' gateway/docs/RUNBOOK-DEPLOY.md` → `1` ✓ (≥1)
  - `grep -ciE 'tier-0.*stt|3-role.*stt|local-stt' gateway/docs/RUNBOOK-FAILOVER.md` → `0` ✓
  - `grep -ciE 'tier-0.*stt|3-role.*stt|local-stt' gateway/docs/RUNBOOK-PRIMARY-POD.md` → `0` ✓

## Deviations from Plan

### [Rule 3 – Blocking] Plan frontmatter listed runbook paths at repo root; canonical paths are `gateway/docs/`
- **Found during:** Task 2 readahead
- **Issue:** Plan 11.1-05 PLAN.md frontmatter declares `files_modified` as `RUNBOOK-FAILOVER.md`, `RUNBOOK-DEPLOY.md`, `RUNBOOK-PRIMARY-POD.md` at repo root, but `find` returned 0 hits there. The actual runbooks live at `gateway/docs/RUNBOOK-*.md` (the RESEARCH refs cited in the plan's `<read_first>` already use those paths, e.g. "RUNBOOK-FAILOVER.md:24" matches the line-24 STT row in `gateway/docs/RUNBOOK-FAILOVER.md`).
- **Fix:** Applied edits to `gateway/docs/RUNBOOK-FAILOVER.md`, `gateway/docs/RUNBOOK-DEPLOY.md`, `gateway/docs/RUNBOOK-PRIMARY-POD.md` per the canonical paths from RESEARCH state-of-the-art deprecation refs.
- **Commit:** `f45c0ab`

### [Rule 2 – Critical] Health-bridge env trimmed beyond strict plan scope
- **Found during:** Task 1 (compose edit)
- **Issue:** Plan said "Preserve chatterbox / llama / dcgm / health-bridge config" — but `health-bridge` had `SPEACHES_URL`, `INFINITY_URL`, `SPEACHES_MODEL` env vars that became dead-code after Plan 11.1-04 (probe set shrunk to `{LLM}`). Leaving them would (a) confuse next operator and (b) reference services that no longer exist in the same compose file.
- **Fix:** Trimmed health-bridge env to `LLAMA_URL` only (kept `HEALTH_BRIDGE_PORT`, `PROBE_INTERVAL_SECONDS`, `LOG_LEVEL`, `ENV`). Consistent with the Plan 11.1-04 health-bridge code that no longer reads these env vars.
- **Commit:** `09b5a7c`

### Out of scope (deferred to follow-up plan)
- `RUNBOOK-PRIMARY-POD.md` lines 109-111 (supervisord `[program:speaches]` block in architecture diagram), line 226 (STT image SHA pin `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3@sha256:...`), line 316 (`PRIMARY_SPEACHES_IMAGE` env var row), lines 580-650 (deploy procedure mentions of `PRIMARY_WHISPER_WEIGHTS_*`). These describe the historical Phase 06.6 Wave 0 LOCKED primary-pod image which still contains the speaches stage at the build level. **Retire when:** `pod/primary/Dockerfile` + `pod/primary/supervisord.conf` are edited to remove speaches stage in a follow-up plan (likely a separate Phase 11.1 plan or future bundle).
- Tracked in this SUMMARY's `decisions` array for handoff to next plan owner.

## Threat Mitigations Implemented

| Threat ID | Mitigation | Status |
|-----------|------------|--------|
| T-11.1-02 (MEDIUM: operator forgets to remove `UPSTREAM_STT_URL` from prod `.env`) | RUNBOOK-DEPLOY.md "Phase 11.1 post-deploy hygiene" section added with `grep -c UPSTREAM_STT_URL` audit returns 0 + `docker compose up -d` reload sequence; cross-linked in verification gate | ✅ committed in `f45c0ab` |

## Deferred Issues

None.

## Self-Check: PASSED

Files verified:
- `pod/.env.example` → exists ✓
- `pod/docker-compose.yml` → exists ✓
- `pod/README.md` → exists ✓
- `pod/weights/README.md` → exists ✓
- `gateway/docs/RUNBOOK-FAILOVER.md` → exists ✓
- `gateway/docs/RUNBOOK-DEPLOY.md` → exists ✓
- `gateway/docs/RUNBOOK-PRIMARY-POD.md` → exists ✓

Commits verified:
- `09b5a7c` → present in `git log --oneline` ✓
- `f45c0ab` → present in `git log --oneline` ✓
