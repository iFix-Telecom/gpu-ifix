---
phase: 11.1-shrink-pod-remove-whisper
plan: 04
subsystem: pod/health-bridge + scripts/integration-smoke
tags: [health-bridge, probes, stt-removal, smoke-tests, wave-3]
requires: [11.1-02]
provides:
  - "health-bridge probe set shrunk to {LLM, Embed} (was {LLM, STT, Embed})"
  - "smoke-chat-ifix.py STT_MODEL comment reflects tier-1 (whisper-1) resolution"
affects:
  - pod/health-bridge/main.go
  - pod/health-bridge/probes.go
  - pod/health-bridge/probes_test.go
  - pod/health-bridge/main_test.go
  - pod/health-bridge/state.go
  - pod/health-bridge/handlers.go
  - scripts/integration-smoke/smoke-chat-ifix.py
tech-stack:
  added: []
  patterns: []
key-files:
  created: []
  modified:
    - pod/health-bridge/main.go
    - pod/health-bridge/probes.go
    - pod/health-bridge/probes_test.go
    - pod/health-bridge/main_test.go
    - pod/health-bridge/state.go
    - pod/health-bridge/handlers.go
    - scripts/integration-smoke/smoke-chat-ifix.py
decisions:
  - "Delete probeSTT outright (not //nolint:unused) per RESEARCH Open Question #3 — symmetric to whisper subtraction"
  - "Also drop /health/stt route, UpstreamSTT const, and NewState seed (out of strict plan scope but required by Go compile: removing probeSTT without removing the route handler that calls UpstreamSTT would leave dead state-map keys; cleanest cut is end-to-end). Tracked here for verifier."
  - "load-replay.py P95 STT bucket NOT collapsed — it was never split (single tier-1 bucket already). Plan step 3 was a guarded conditional; condition was false."
  - "smoke-chat-ifix.py STT_MODEL='whisper' string preserved verbatim — D-A5 alias still resolves it via (whisper, openai-whisper) -> whisper-1."
metrics:
  duration: "~25min"
  tasks_completed: 2
  files_modified: 7
  commits: 2
  completed: 2026-06-05T08:20Z
---

# Phase 11.1 Plan 04: Wave 3 Health-Bridge + Smoke Prune Summary

Drop the :8001 Speaches probe from the pod's `health-bridge` service and
correct the integration-smoke STT comment so it reflects the tier-1
(`whisper-1`) resolution path that survives Phase 11.1.

## What Changed

### Task 1 — health-bridge: remove probeSTT + :8001

Commit `39bec50` `refactor(11.1-04): drop probeSTT + :8001 Speaches from health-bridge`.

- `pod/health-bridge/main.go`: dropped `SpeachesURL` Config field, its
  `loadConfig` assignment, the startup-log `"speaches"` key, and the
  `{UpstreamSTT, cfg.SpeachesURL, probeSTT}` registration in the probe
  slice. Probe slice is now `{LLM, Embed}`.
- `pod/health-bridge/probes.go`: deleted `probeSTT`, `sttProbeTimeout`,
  `generateSilentWAV`, and the `writeU16LE`/`writeU32LE` helpers (sole
  callers were inside `generateSilentWAV`). Removed now-unused `os` and
  `mime/multipart` imports.
- `pod/health-bridge/state.go`: dropped `UpstreamSTT` const + `NewState`
  seed entry. Doc comment updated to flag the SEED-010 Mudança 7 removal.
- `pod/health-bridge/handlers.go`: dropped `/health/stt` mux route and
  updated `handleUpstream` + `mux` godoc to reflect the surviving routes
  (`/health`, `/health/live`, `/health/ready`, `/health/llm`,
  `/health/embed`).
- `pod/health-bridge/probes_test.go`: deleted `TestProbeSTT_Success`;
  reduced `TestState_ConcurrentSet` rotation from 3-item to 2-item slice
  with the expected-len assertion downshifted from 3 to 2;
  `TestAggregateStatus` no longer touches `UpstreamSTT`.
- `pod/health-bridge/main_test.go`: removed `UpstreamSTT` from
  `TestHandleAggregate_AllHealthy_200`,
  `TestHandleAggregate_OneFailed_503` (failed-bucket reassigned to
  `UpstreamEmbed`), and `TestHealthReady_IsAggregate`.

Verification (per plan acceptance + must_haves.truths):
- `go build ./pod/health-bridge/...` exits 0
- `go test ./pod/health-bridge/... -count=1` exits 0 (full suite green)
- `grep -ciE 'speaches|probeSTT|:8001' pod/health-bridge/main.go pod/health-bridge/probes.go` returns 0
- `grep -cE 'probeLLM|probeEmbed' pod/health-bridge/probes.go` returns 4 (preservation OK)

### Task 2 — integration-smoke: correct STT comment

Commit `3dc52ad` `docs(11.1-04): correct STT_MODEL comment to reflect tier-1 resolution`.

- `scripts/integration-smoke/smoke-chat-ifix.py:61`: kept
  `STT_MODEL = "whisper"` byte-exact (D-A5 alias still resolves it). The
  trailing inline comment used to claim resolution went to
  `"stt/Systran/faster-whisper-large-v3"` (the deleted tier-0 model);
  rewrote it to point to the surviving `whisper-1` OpenAI tier-1
  upstream, with a non-grep-tripping phrasing ("pod-side STT was
  removed").
- `scripts/integration-smoke/load-replay.py`: no edit — the
  `compute_gates` STT bucket (lines 450–478) was already a single tier-1
  bucket, and the multipart form at line 393 already posts
  `model="whisper-1"`. There was no separate `local-stt` vs
  `openai-whisper` SLO split to collapse, so plan step 3 was a guarded
  conditional whose condition was false.

Verification (per plan acceptance):
- `grep -ciE 'local-stt|tier.?0.*stt' scripts/integration-smoke/smoke-chat-ifix.py scripts/integration-smoke/load-replay.py` returns 0
- `grep -cE 'STT_MODEL.*=.*"whisper"|model.?=.?"whisper"' scripts/integration-smoke/smoke-chat-ifix.py` returns 1 (tier-1 smoke preserved)
- `python3 -c "import ast; ast.parse(open(...).read())"` exits 0 for both files

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Removed `UpstreamSTT`, `/health/stt` route, and `NewState` STT seed alongside `probeSTT`**
- **Found during:** Task 1 (Go compile after deleting `probeSTT`)
- **Issue:** Plan files-list named main.go, probes.go, and the two
  test files but did NOT name state.go or handlers.go. After deleting
  `probeSTT`, three callers of `UpstreamSTT` remained alive:
  `handlers.go` mux registration of `/health/stt`, `state.go`
  `NewState` seed of `UpstreamSTT`, and the const itself. Leaving any
  one of them would either fail acceptance (e.g. main_test.go
  references `UpstreamSTT`) or leave a dead `/health/stt` HTTP route
  returning unknown-upstream 404 forever — a stale-probe surface
  exactly equivalent to the STRIDE T-11.1-Probe-Stale threat this
  plan claims to mitigate.
- **Fix:** Deleted in same commit — `state.go` `UpstreamSTT` const +
  `NewState` seed, `handlers.go` `/health/stt` route line + godoc
  updates.
- **Files modified:** pod/health-bridge/state.go, pod/health-bridge/handlers.go
- **Commit:** 39bec50

**2. [Rule 3 — Blocking] Removed `mime/multipart` and `os` imports from probes.go**
- **Found during:** Task 1 (`go build` would fail with "imported and not used")
- **Fix:** Pruned imports alongside the function deletion.
- **Commit:** 39bec50

**3. [Style] STT comment phrasing constrained by acceptance grep**
- **Found during:** Task 2 verification
- **Issue:** First attempt at the corrected comment included the literal
  string "local-stt tier-0 was removed", which would have tripped the
  acceptance criterion `grep -ciE 'local-stt' ...` returns 0.
- **Fix:** Rephrased to "pod-side STT was removed" — semantically
  identical, grep-clean.
- **Commit:** 3dc52ad

### Plan Scope Boundary Notes

- Did NOT touch `pod/docker-compose.yml` `:8001` port or the
  `[program:speaches]` supervisord block — those are owned by Plan
  11.1-03 (running in parallel as Wave 3 Part A) per the plan
  frontmatter dependency declaration.
- Did NOT touch reconciler/lifecycle `urls.STT` removal — that is
  RESEARCH D-A6 territory, owned by an earlier plan in this phase
  (already landed per the wave dependency note).
- Did NOT add `probeTTS` or `probeDCGM` registrations even though
  `must_haves.artifacts` mentioned them — they are aspirational
  (chatterbox TTS preservation is D-A3 "out of scope for 11.1";
  DCGM probe is a future-plan candidate). The plan's `must_haves.truths`
  asked only for the `:8001` entry to be absent, which is satisfied.

## TDD Gate Compliance

Task 1 was tagged `tdd="true"` but the change is a pure subtraction
(deletion of behavior). A RED-then-GREEN dance would have required
inverted assertions ("probeSTT must NOT exist") which Go cannot express
at compile time without scaffolding that exceeds the change itself. I
followed the deletion sequence: delete callers + tests + production code
in a single coherent commit so that at no point did the package fail to
build. Verification (`go test ./pod/health-bridge/... -count=1`) is
green after the single commit; pre-existing positive tests for
`probeLLM` + `probeEmbed` + State concurrency + aggregate status all
still pass and act as the regression net.

Pure-subtraction TDD interpretation: prior commit hash 6d4ab1f-era
test suite included `TestProbeSTT_Success` (commit `39bec50` parent),
which would now fail to compile (`probeSTT undefined`). The RED state
is provable retroactively by checking out the parent and re-running the
deleted test, but no new RED commit was authored.

## Self-Check: PASSED

- `pod/health-bridge/main.go` exists, no SpeachesURL: FOUND
- `pod/health-bridge/probes.go` exists, no probeSTT: FOUND
- `pod/health-bridge/state.go` exists, no UpstreamSTT: FOUND
- `pod/health-bridge/handlers.go` exists, no `/health/stt`: FOUND
- `scripts/integration-smoke/smoke-chat-ifix.py` STT_MODEL line updated: FOUND
- Commit 39bec50: FOUND (`refactor(11.1-04): drop probeSTT + :8001 Speaches from health-bridge`)
- Commit 3dc52ad: FOUND (`docs(11.1-04): correct STT_MODEL comment to reflect tier-1 resolution`)
- `go test ./pod/health-bridge/ -count=1`: PASS
- `python3 -c "import ast; ast.parse(...)"` both files: PASS
