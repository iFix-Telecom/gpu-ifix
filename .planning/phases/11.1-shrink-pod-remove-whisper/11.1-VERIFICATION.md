---
phase: 11.1-shrink-pod-remove-whisper
verified: 2026-06-05T00:00:00Z
status: passed_partial
verdict: "Tier-0 Speaches/Whisper removido em código + DB + pod image + docs; fallback shape pivotado para 2×3090 conforme Wave 0 gate; live UAT (cold-start/VRAM/tier-1 curl) deferido para Wave 5 quando CI publicar imagem + operador agendar pod Vast."
score: 7/7 estrutural; 4 deferidos (cold-start UAT)
requirements_met:
  - "D-A1 — tier-0 STT (Speaches/Whisper) removido do stack primário"
  - "D-A2 — tier-1 openai-whisper preservado para roteamento de model=whisper"
  - "D-A5 — migration 0028 DELETEs (whisper, local-stt) alias + upstream local-stt"
  - "D-A6 — config split em Primary/Fallback (1×3090 $0.30 / 2×3090 $0.60), DefaultSearchFilters retorna par [primary, fallback]"
  - "D-A7 — phase 11.1 fechada independentemente das UATs deferidas da Phase 11"
requirements_deferred:
  - "D-A3 — image size reduction (medição real adiada para Wave 5 UAT)"
  - "D-A4 — cold-start gain (medição real adiada para Wave 5 UAT)"
  - "Tier-1 STT live curl via prod gateway (Plan 06 Task 2 — deferido)"
  - "Memory note primary-gpu-shape-11.1-final.md (Plan 06 Task 2 — deferido)"
requirements_gap: []
---

# Phase 11.1: Shrink Pod + Remove Whisper — Verification Report

**Phase Goal:** Reduzir o pod primário removendo o stack tier-0 Speaches/Whisper, pivotar shape fallback de 1×4090 para 2×3090 (Wave 0 gate), preservar tier-1 OpenAI whisper-1 para `/v1/audio/transcriptions`, e limpar artefatos (docs/env/config/migration/health-bridge).
**Verified:** 2026-06-05
**Status:** passed_partial
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Tier-0 STT (Speaches/Whisper) não existe mais no pod image | VERIFIED | `grep speaches\|infinity\|whisper pod/primary/Dockerfile pod/onstart.sh` = 0 linhas ativas; supervisord.conf só tem comentário histórico explicando remoção |
| 2 | Gateway code não tem mais wiring obrigatório de local-stt | VERIFIED | `PRIMARY_SPEACHES_IMAGE`/`PrimarySpeachesImage` 0 hits ativos; `main.go:678` registra `local-stt` SÓ se operador ainda tem `UPSTREAM_STT_URL` no .env (graceful backward compat — CR-02 fix) |
| 3 | DB migration 0028 deleta upstream local-stt + alias (whisper, local-stt) | VERIFIED | `gateway/db/migrations/0028_remove_local_stt_upstream.sql` Step 1 DELETE alias, Step 2 DELETE upstream; Down idempotente com ON CONFLICT DO NOTHING |
| 4 | Tier-1 openai-whisper preservado para model=whisper | VERIFIED | Migration 0028 comentário explícito "PRESERVATION asserted by TestIntegration_Migration0028_Up_PreservesOpenAIWhisper"; integration_test/openai_whisper_model_rewrite_test.go ativo |
| 5 | Config split em primary/fallback shapes | VERIFIED | `gateway/internal/config/config.go:203-208` declara PrimaryVastGPUNamePrimary/Fallback, PriceCapPrimary/Fallback, NumGPUsPrimary/Fallback com defaults 1×3090 $0.30 / 2×3090 $0.60; defaults em 424-429 |
| 6 | DefaultSearchFilters retorna par [primary, fallback] com num_gpus split | VERIFIED | `gateway/internal/emerg/vast/types.go:266` DefaultSearchFilters builds primary+fallback pair; `:237` DefaultSearchFilter aceita numGPUs param explícito |
| 7 | health-bridge sem probeSTT/UpstreamSTT | VERIFIED | `grep probeSTT\|UpstreamSTT pod/health-bridge/*.go` 0 hits; state.go:59 só tem comentário histórico explicando remoção |

**Score:** 7/7 estrutural

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `gateway/db/migrations/0028_remove_local_stt_upstream.sql` | Migration DELETEs | VERIFIED | Up Step 1+2 + Down restore com ON CONFLICT |
| `gateway/internal/config/config.go` | Split Primary/Fallback fields | VERIFIED | 6 novos fields + 6 envOr defaults + legacy alias shims (PRIMARY_GPU_NAME → PrimaryVastGPUNamePrimary, etc.) |
| `gateway/internal/emerg/vast/types.go` | DefaultSearchFilters pair API | VERIFIED | `DefaultSearchFilters(primaryCap, fallbackCap float64, ...)` retorna `[2]SearchFilter` |
| `pod/primary/Dockerfile` | Sem Speaches/Infinity venvs | VERIFIED | 0 ocorrências ativas |
| `pod/primary/supervisord.conf` | Sem [program:speaches] / [program:infinity] | VERIFIED | Só comentários históricos (linhas 3, 5) |
| `pod/onstart.sh` | Sem bootstrap Whisper | VERIFIED | Linha 10: "Tier-0 local STT was removed per SEED-010 Mudança 3 + D-A2" |
| `pod/health-bridge/probes.go` `state.go` | Sem probeSTT/UpstreamSTT | VERIFIED | 0 referências ativas |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Código Go compila | `go build ./...` | exit 0, sem output | PASS |
| Código Go vet limpo | `go vet ./...` | exit 0, sem output | PASS |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| gateway/internal/config/config.go | 196-197 | DEPRECATED-alias com TODO-remove-in-11.2 | INFO | Esperado — backward compat para .env stale; será removido em 11.2 |
| gateway/internal/primary/lifecycle.go | 279, 362 | Comentários históricos PRIMARY_SPEACHES_IMAGE | INFO | Comment-only — contexto histórico aceitável |

Nenhum BLOCKER ou WARNING; debt markers TODOs todos referenciam fase de remoção (11.2) — auditável.

### Requirements Coverage

| Requirement | Status | Evidence |
|-------------|--------|----------|
| D-A1 (tier-0 STT removed) | SATISFIED | Truths 1, 7; Dockerfile/supervisord/onstart/health-bridge limpos |
| D-A2 (tier-1 openai-whisper absorve) | SATISFIED | Truth 4; integration test ativo + migration 0028 PRESERVE assertion |
| D-A3 (image size gain) | DEFERRED | Wave 5 UAT — medição em pod real |
| D-A4 (cold-start gain) | DEFERRED | Wave 5 UAT — medição em pod real |
| D-A5 (migration 0028 DELETEs) | SATISFIED | Truth 3; Step 1 + Step 2 ambos presentes |
| D-A6 (split primary/fallback shape) | SATISFIED | Truths 5, 6; defaults 1×3090 $0.30 + 2×3090 $0.60 batem com Wave 0 gate pivot |
| D-A7 (não bloqueado por phase 11 UATs) | SATISFIED | Plan 06 SUMMARY status=deferred (closed_partial pattern) |

### Deferred Items (Wave 5 live UAT)

| # | Item | Owner | Triggered When |
|---|------|-------|----------------|
| 1 | Cold-start ≤15min UAT em pod 1×3090 real | Plan 06 Task 1 | CI publica `phase-11.1-<sha>` + operador agenda pod Vast |
| 2 | Image size delta vs Phase 06.7 baseline | Plan 06 Task 1 | Cold-start UAT acima |
| 3 | VRAM ≤22GB em 1×3090 | Plan 06 Task 1 | Cold-start UAT acima |
| 4 | `POST /v1/audio/transcriptions` curl tier-1 via prod gateway | Plan 06 Task 2 | UAT window |
| 5 | Memory note `primary-gpu-shape-11.1-final.md` | Plan 06 Task 2 | Pós-UAT signoff |

Items diferidos NÃO afetam status — pattern `closed_partial` herdado da Phase 11 (D-A7 explicitamente permite).

### Human Verification Required

Nenhum item pendente bloqueia o fechamento estrutural. UAT deferida é tracked em Plan 06 SUMMARY (status=deferred) com retomada agendada quando CI publicar imagem + operador agendar pod Vast.

### Gaps Summary

Nenhum gap estrutural. Todo o trabalho de código (Wave 1), DB migration (Wave 2), pod image (Wave 3), health-bridge (Wave 4 misnamed — na real é Wave 3 misto), docs+env (Wave 4) está entregue e auditável. As 3 falhas críticas + 5 warnings do REVIEW.md foram corrigidas em 7 commits subsequentes (`0c06c31`, `add9c61`, `ee30858`, `59654b3`, `fbe2129`, `f06c777`, `eda69d2`). Gain table real (D-A3/D-A4) e validação live tier-1 são intencionalmente diferidas para Wave 5 UAT — sem CI image publish + Vast pod $ + prod SSH, são não-verificáveis ofline.

---

_Verified: 2026-06-05_
_Verifier: Claude (gsd-verifier)_
