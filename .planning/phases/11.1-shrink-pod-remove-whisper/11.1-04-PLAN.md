---
phase: 11.1-shrink-pod-remove-whisper
plan: 04
type: execute
wave: 3
depends_on: ["11.1-02"]
files_modified:
  - pod/health-bridge/main.go
  - pod/health-bridge/probes.go
  - pod/health-bridge/probes_test.go
  - pod/health-bridge/main_test.go
  - scripts/integration-smoke/smoke-chat-ifix.py
  - scripts/integration-smoke/load-replay.py
autonomous: true
requirements:
  - "SEED Mudança 7 (health-bridge drop :8001 probe)"
  - "SEED Mudança 15 (smoke tests drop STT tier-0 path; keep tier-1 OpenAI smoke)"
  - "RESEARCH Open Question #3 (delete probeSTT, not //nolint:unused)"
threat_refs: []

must_haves:
  truths:
    - "health-bridge main.go probe registration list has no :8001 entry"
    - "health-bridge probes.go has no probeSTT function"
    - "scripts/integration-smoke/smoke-chat-ifix.py STT block uses model='whisper' resolving via tier-1 OpenAI"
    - "scripts/integration-smoke/load-replay.py STT P95 SLO assertion targets tier-1 path only"
  artifacts:
    - path: "pod/health-bridge/main.go"
      provides: "Probe registration of llama (:8080) + chatterbox + dcgm only — no :8001"
    - path: "pod/health-bridge/probes.go"
      provides: "probeLLM + probeTTS + probeDCGM — no probeSTT"
  key_links: []
---

<objective>
Wave 3 pod image refactor (Part B): drop the `:8001` Speaches probe from
health-bridge AND prune integration-smoke scripts of any STT tier-0 specific
test cases (tier-1 OpenAI smoke continues green per Mudança 15).

Output: health-bridge probes only the 3 surviving services; smoke inventory
clean of dead tier-0 references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/seeds/SEED-010-shrink-pod-remove-whisper-stt.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md
@pod/health-bridge/main.go
@scripts/integration-smoke/smoke-chat-ifix.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop probeSTT + :8001 from health-bridge</name>
  <files>
    pod/health-bridge/main.go,
    pod/health-bridge/probes.go,
    pod/health-bridge/probes_test.go,
    pod/health-bridge/main_test.go
  </files>
  <read_first>
    .planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md (Component Responsibilities row: pod/health-bridge/main.go lines 23, 34, 100, 130 + Open Question #3),
    pod/health-bridge/main.go (full file — locate SpeachesURL const ~line 23, probe registration ~line 100/130),
    pod/health-bridge/probes.go (full file — locate probeSTT function)
  </read_first>
  <behavior>
    - main.go has no `SpeachesURL` const + no `probeSTT` registration in the probe slice
    - probes.go has no `probeSTT` function (delete entirely per Open Question #3 recommendation)
    - main_test.go probe list assertion reflects 3-probe set (LLM + TTS + DCGM) or current surviving set
    - probes_test.go has no TestProbeSTT case
  </behavior>
  <action>
    Per SEED Mudança 7 + Open Question #3:
    1. `pod/health-bridge/main.go` line ~23: DELETE `SpeachesURL` const declaration.
    2. Line ~34 (if there's a probe-URL map): DELETE the speaches entry.
    3. Line ~100 / 130 (probe registration slice — RESEARCH names these): DELETE the probeSTT registration entry.
    4. `pod/health-bridge/probes.go`: DELETE the entire `probeSTT` function (per Open Question #3 — symmetric to whisper subtraction, not //nolint:unused).
    5. `pod/health-bridge/probes_test.go`: DELETE TestProbeSTT and any STT-specific table-test entries.
    6. `pod/health-bridge/main_test.go`: locate the registered-probes assertion list; remove the `"stt"` / `"speaches"` entry; update expected probe count if hardcoded.
    7. Verify build + test green.
  </action>
  <verify>
    <automated>
      cd /home/pedro/projetos/pedro/gpu-ifix && go build ./pod/health-bridge/... && go test ./pod/health-bridge/... -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE 'speaches|probeSTT|:8001' pod/health-bridge/main.go pod/health-bridge/probes.go` returns 0
    - `go build ./pod/health-bridge/...` exits 0
    - `go test ./pod/health-bridge/... -count=1` exits 0
    - `grep -c 'probeLLM\\|probeTTS' pod/health-bridge/probes.go` returns ≥1 (preservation)
  </acceptance_criteria>
  <done>health-bridge no longer probes :8001 / Speaches; probes.go shrunk by one function.</done>
</task>

<task type="auto">
  <name>Task 2: Prune integration-smoke for tier-0 STT references</name>
  <files>
    scripts/integration-smoke/smoke-chat-ifix.py,
    scripts/integration-smoke/load-replay.py
  </files>
  <read_first>
    .planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md (Component Responsibilities rows: scripts/integration-smoke/smoke-chat-ifix.py:61 STT_MODEL + scripts/integration-smoke/load-replay.py:393, 452-475),
    scripts/integration-smoke/smoke-chat-ifix.py (full file — line 61 + STT block),
    scripts/integration-smoke/load-replay.py (full file — lines 393 + 452-475 STT P95 SLO block)
  </read_first>
  <action>
    Per SEED Mudança 15 + RESEARCH (smoke STT_MODEL='whisper' preserved through tier-1 alias):
    1. `scripts/integration-smoke/smoke-chat-ifix.py:61` — confirm `STT_MODEL = "whisper"` line UNCHANGED (resolves via preserved (whisper, openai-whisper)→whisper-1 alias per D-A5). If any local-stt URL override exists in this file, DELETE it.
    2. Locate any block in smoke-chat-ifix.py that asserts "tier-0 path" specifically (audit_log upstream_name=='local-stt' or response header signaling tier-0). DELETE those assertion blocks; keep tier-1 assertions (upstream_name=='openai-whisper').
    3. `scripts/integration-smoke/load-replay.py:393` — confirm STT request still uses model='whisper'. Update line 452-475 P95 SLO block: if it has separate `local-stt` vs `openai-whisper` SLO buckets, collapse to single `openai-whisper`/tier-1 bucket (keep D-04 Phase 11 P95 ≤10s SLO).
    4. Run `grep -in 'local-stt\\|tier.?0.*stt' scripts/integration-smoke/*.py` — output MUST be empty.
    5. Run a dry-import / syntax check on both files: `python3 -c "import ast; ast.parse(open('scripts/integration-smoke/smoke-chat-ifix.py').read())"` etc.
  </action>
  <verify>
    <automated>
      cd /home/pedro/projetos/pedro/gpu-ifix && python3 -c "import ast; ast.parse(open('scripts/integration-smoke/smoke-chat-ifix.py').read()); ast.parse(open('scripts/integration-smoke/load-replay.py').read())" && grep -c 'local-stt' scripts/integration-smoke/smoke-chat-ifix.py scripts/integration-smoke/load-replay.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE 'local-stt' scripts/integration-smoke/smoke-chat-ifix.py scripts/integration-smoke/load-replay.py` returns 0
    - `grep -c 'STT_MODEL.*=.*"whisper"\\|model.?=.?"whisper"' scripts/integration-smoke/smoke-chat-ifix.py` returns ≥1 (tier-1 smoke preserved)
    - `python3 -c "import ast; ast.parse(open('scripts/integration-smoke/smoke-chat-ifix.py').read())"` exits 0
    - `python3 -c "import ast; ast.parse(open('scripts/integration-smoke/load-replay.py').read())"` exits 0
  </acceptance_criteria>
  <done>Smoke inventory mentions no tier-0 STT; tier-1 OpenAI whisper smoke path intact for Plan 06 UAT use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| health-bridge → pod processes | probe paths must not query removed services |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-Probe-Stale | Availability (false-positive) | health-bridge probe registration | mitigate | Delete SpeachesURL + probeSTT entirely so the probe loop cannot tick on a removed listener and emit false "down" alerts |
</threat_model>

<verification>
- Tasks 1 + 2 verify commands green
- `go test ./pod/health-bridge/... -count=1` exits 0
</verification>

<success_criteria>
- health-bridge has 3 (or current count - 1) probes registered
- smoke inventory free of tier-0 STT references
</success_criteria>

<output>
Create `.planning/phases/11.1-shrink-pod-remove-whisper/11.1-04-SUMMARY.md` when done
</output>
