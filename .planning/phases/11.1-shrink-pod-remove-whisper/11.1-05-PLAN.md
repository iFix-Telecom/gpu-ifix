---
phase: 11.1-shrink-pod-remove-whisper
plan: 05
type: execute
wave: 4
depends_on: ["11.1-03", "11.1-04"]
files_modified:
  - pod/.env.example
  - pod/docker-compose.yml
  - pod/README.md
  - pod/weights/README.md
  - RUNBOOK-FAILOVER.md
  - RUNBOOK-DEPLOY.md
  - RUNBOOK-PRIMARY-POD.md
autonomous: true
requirements:
  - "SEED Mudança 4 (.env.example drop WHISPER_* + whisper HF_HUB_CACHE)"
  - "SEED Mudança 5 (docker-compose.yml drop :8001 port + whisper env + infinity service)"
  - "SEED Mudança 6 (README + weights/README docs update STT off-pod)"
  - "SEED Mudança 13 (prod ai-gateway .env operator task to remove UPSTREAM_STT_URL)"
  - "RESEARCH State of the Art deprecations (RUNBOOK-FAILOVER.md:24, RUNBOOK-DEPLOY.md:523, RUNBOOK-PRIMARY-POD.md:28)"
threat_refs:
  - T-11.1-02 (MEDIUM: operator forgets to remove UPSTREAM_STT_URL from prod .env)

must_haves:
  truths:
    - "pod/.env.example has no WHISPER_* variables"
    - "pod/docker-compose.yml has no :8001 port mapping + no infinity service block"
    - "pod/README.md + pod/weights/README.md note STT off-pod"
    - "RUNBOOK-DEPLOY.md upstream count updated 6→5"
    - "RUNBOOK-FAILOVER.md STT row updated (tier-0 column removed)"
    - "RUNBOOK-PRIMARY-POD.md tier-0 role wording updated 3-role→2-role"
    - "RUNBOOK-DEPLOY.md contains operator task to remove UPSTREAM_STT_URL from /opt/ai-gateway-prod/.env"
  artifacts:
    - path: "pod/.env.example"
      provides: "Env contract reference without whisper vars"
    - path: "RUNBOOK-DEPLOY.md"
      provides: "Post-deploy operator task list including UPSTREAM_STT_URL removal step"
  key_links: []
---

<objective>
Wave 4 docs + env hygiene: update `.env.example`, `docker-compose.yml`, READMEs,
and runbooks to reflect post-shrink topology. Add operator task to
RUNBOOK-DEPLOY.md for removing `UPSTREAM_STT_URL` from prod
`/opt/ai-gateway-prod/.env` per D-A4 deploy hygiene (T-11.1-02 mitigation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/seeds/SEED-010-shrink-pod-remove-whisper-stt.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-CONTEXT.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md
@pod/.env.example
@pod/docker-compose.yml
@pod/README.md
@pod/weights/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip whisper/speaches from pod env + compose + READMEs</name>
  <files>
    pod/.env.example,
    pod/docker-compose.yml,
    pod/README.md,
    pod/weights/README.md
  </files>
  <read_first>
    .planning/seeds/SEED-010-shrink-pod-remove-whisper-stt.md (Mudanças 4, 5, 6),
    pod/.env.example (full file — line 13 weight list + whisper vars),
    pod/docker-compose.yml (full file — lines 90-145 infinity service + :8001 port),
    pod/README.md, pod/weights/README.md (full files)
  </read_first>
  <action>
    Per SEED Mudanças 4-6:
    1. `pod/.env.example`:
       - DELETE all `WHISPER_*` variables (e.g., WHISPER_MODEL, WHISPER_LANGUAGE, HF_HUB_CACHE pointing at whisper).
       - Update line 13 weight list / inline doc to remove "whisper-large-v3" entry.
       - Preserve `UPSTREAM_STT_OPENAI_AUTH_BEARER` (tier-1 — RESEARCH Runtime State Inventory).
    2. `pod/docker-compose.yml`:
       - DELETE `"8001:8001"` from the primary service ports list.
       - DELETE the entire `infinity:` service block (lines 90-145 region — D-A2 bundle).
       - DELETE any whisper env vars set on the primary service.
       - Preserve chatterbox / llama / dcgm / health-bridge config.
    3. `pod/README.md`:
       - Locate any section listing pod services / open ports / STT capability.
       - Update to "STT: off-pod (handled by tier-1 OpenAI whisper-1 via gateway fallback)".
       - Update port list: remove :8001.
       - Update process list: remove speaches + infinity.
    4. `pod/weights/README.md`:
       - DELETE any `/weights/whisper` documentation / size estimate.
       - Update total weights size estimate downward by ~5GB.
    5. Verify:
       - `grep -ciE 'whisper|speaches' pod/.env.example pod/docker-compose.yml` returns 0
       - `grep -c "8001" pod/docker-compose.yml` returns 0
  </action>
  <verify>
    <automated>
      cd /home/pedro/projetos/pedro/gpu-ifix && grep -ciE 'whisper|speaches|infinity' pod/.env.example pod/docker-compose.yml pod/README.md pod/weights/README.md && grep -c "8001" pod/docker-compose.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE 'whisper|speaches' pod/.env.example` returns 0
    - `grep -ciE 'whisper|speaches|infinity' pod/docker-compose.yml` returns 0
    - `grep -c "8001" pod/docker-compose.yml` returns 0
    - `grep -ci 'whisper' pod/weights/README.md` returns 0
    - `grep -ci 'off-pod\\|tier-1' pod/README.md` returns ≥1 (STT off-pod note present)
    - `docker compose -f pod/docker-compose.yml config -q` exits 0 (compose still parses)
  </acceptance_criteria>
  <done>Env contract + compose + READMEs reflect post-shrink topology.</done>
</task>

<task type="auto">
  <name>Task 2: Update runbooks (FAILOVER, DEPLOY, PRIMARY-POD) + add operator task</name>
  <files>
    RUNBOOK-FAILOVER.md,
    RUNBOOK-DEPLOY.md,
    RUNBOOK-PRIMARY-POD.md
  </files>
  <read_first>
    .planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md (State of the Art deprecations: RUNBOOK-FAILOVER.md:24, RUNBOOK-DEPLOY.md:523 "6 upstreams", RUNBOOK-PRIMARY-POD.md:28),
    .planning/phases/11.1-shrink-pod-remove-whisper/11.1-CONTEXT.md (Touchpoint 13 prod .env cleanup),
    RUNBOOK-FAILOVER.md (line 24 STT row),
    RUNBOOK-DEPLOY.md (line 523 upstream count),
    RUNBOOK-PRIMARY-POD.md (line 28 tier-0 wording)
  </read_first>
  <action>
    Per T-11.1-02 mitigation + RESEARCH State of the Art:
    1. `RUNBOOK-FAILOVER.md:24` STT row: remove tier-0 column / change "local-stt → openai-whisper" to "openai-whisper (no local fallback)". Add note "STT tier-0 removed in Phase 11.1".
    2. `RUNBOOK-DEPLOY.md:523` "6 upstreams" expectation: change to **5 upstreams** (local-llm, openrouter-chat, openai-whisper, local-embed, openai-embed). Update any grep gate / smoke check in the runbook accordingly.
    3. `RUNBOOK-DEPLOY.md` post-deploy section: ADD new operator task block:
       ```
       ### Phase 11.1 post-deploy hygiene (T-11.1-02)

       After ai-gateway-prod restart:
       1. `ssh n8n-ia-vm`
       2. Edit `/opt/ai-gateway-prod/.env`; delete the line `UPSTREAM_STT_URL=http://10.10.10.20:8001`
       3. Audit: `grep -c UPSTREAM_STT_URL /opt/ai-gateway-prod/.env` returns 0
       4. Reload: `cd /opt/ai-gateway-prod && docker compose up -d` (no-op if env unchanged; rebinds otherwise)
       ```
    4. `RUNBOOK-PRIMARY-POD.md:28` 3-role tier-0 wording: change to 2-role (`local-llm`, `local-embed`). Update any architecture diagram / wording reference to `local-stt` (delete row).
  </action>
  <verify>
    <automated>
      cd /home/pedro/projetos/pedro/gpu-ifix && grep -c 'UPSTREAM_STT_URL' RUNBOOK-DEPLOY.md && grep -c '5 upstreams\|five upstreams' RUNBOOK-DEPLOY.md && grep -ci 'local-stt' RUNBOOK-FAILOVER.md RUNBOOK-PRIMARY-POD.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'UPSTREAM_STT_URL' RUNBOOK-DEPLOY.md` returns ≥1 (operator removal task present)
    - `grep -c '6 upstreams' RUNBOOK-DEPLOY.md` returns 0
    - `grep -c '5 upstreams\\|five upstreams' RUNBOOK-DEPLOY.md` returns ≥1
    - `grep -ci 'tier-0.*stt\\|3-role.*stt\\|local-stt' RUNBOOK-FAILOVER.md` returns 0
    - `grep -ci 'tier-0.*stt\\|3-role.*stt\\|local-stt' RUNBOOK-PRIMARY-POD.md` returns 0
    - `grep -c 'T-11.1-02\\|Phase 11.1 post-deploy' RUNBOOK-DEPLOY.md` returns ≥1
  </acceptance_criteria>
  <done>Runbooks reflect post-shrink topology; T-11.1-02 mitigation operator task committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → prod `.env` | manual edit; documented runbook procedure |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-02 | Information disclosure (low) | /opt/ai-gateway-prod/.env UPSTREAM_STT_URL=http://10.10.10.20:8001 | mitigate | RUNBOOK-DEPLOY.md operator task + post-deploy grep audit `ssh n8n-ia-vm grep UPSTREAM_STT_URL /opt/ai-gateway-prod/.env` returns 0. Blast radius is internal NAT URL only (no secret). |
</threat_model>

<verification>
- Tasks 1 + 2 verify commands match acceptance criteria
- `docker compose -f pod/docker-compose.yml config -q` exits 0
</verification>

<success_criteria>
- pod env + compose + docs free of whisper/speaches/infinity references
- runbooks reflect 5-upstream post-shrink topology + carry operator hygiene task
</success_criteria>

<output>
Create `.planning/phases/11.1-shrink-pod-remove-whisper/11.1-05-SUMMARY.md` when done
</output>
