---
phase: 11.1-shrink-pod-remove-whisper
plan: 06
type: execute
wave: 5
depends_on: ["11.1-01", "11.1-02", "11.1-03", "11.1-04", "11.1-05"]
files_modified:
  - .planning/phases/11.1-shrink-pod-remove-whisper/11.1-EVIDENCE.md
autonomous: false
requirements:
  - "D-A6 (PRIMARY_GPU_SHAPE validation in production — 1×3090 primary cap $0.30 + 4090 fallback cap $0.40, num_gpus=1)"
  - "gain table validation (image size ≤8GB, VRAM ≤22GB, GPU=1×3090, cost ≤$0.30/h, cold-start ≤15min)"
  - "tier-1 STT fallthrough live verification via prod gateway curl"
  - "T-11.1-04 mitigation (re-verify post-deploy at cold-start UAT; Plan 00 ran pre-code-change survey)"
  - "Memory note `primary-gpu-shape-11.1-final` superseding `06.8-final`"
threat_refs:
  - T-11.1-04 (MEDIUM: 4090 fleet scarcity — pre-code gate at Plan 00; this plan re-verifies post-deploy via Task 1 cold-start UAT)

must_haves:
  truths:
    - "Cold-start UAT on real 1×3090 pod reaches FSM Ready ≤15min"
    - "curl :9100/health on running pod returns no STT/:8001 entries"
    - "POST /v1/audio/transcriptions via prod gateway returns 200 with JSON transcript via openai-whisper tier-1"
    - "Image size delta vs Phase 06.7 baseline recorded in EVIDENCE.md"
    - "VRAM running on 1×3090 ≤22GB recorded in EVIDENCE.md"
    - "Memory note `primary-gpu-shape-11.1-final.md` created in $HOME/.claude/projects/-home-pedro-projetos-pedro-gpu-ifix/memory/"
  artifacts:
    - path: ".planning/phases/11.1-shrink-pod-remove-whisper/11.1-EVIDENCE.md"
      provides: "Cold-start time + image size + VRAM + actual Vast price + tier-1 STT curl response captured"
    - path: "$HOME/.claude/projects/-home-pedro-projetos-pedro-gpu-ifix/memory/primary-gpu-shape-11.1-final.md"
      provides: "Standing config superseding 06.8-final: 1×3090 primary + 4090 fallback"
  key_links: []
---

<objective>
Wave 5 UAT: validate gain table assumptions on a real Vast pod cold-start.
Capture image-size / VRAM / cost / cold-start deltas in `11.1-EVIDENCE.md`.
Supersede memory note `primary-gpu-shape-06.8-final` with `primary-gpu-shape-11.1-final`.

Pre-code-change Vast fleet survey (T-11.1-04 pre-mitigation) is owned by Plan 00
(Wave 0); this plan post-verifies at cold-start UAT against a real Vast 1×3090
pod, then confirms tier-1 STT fallthrough through the prod gateway.

This plan is NOT autonomous — both tasks are human-verify checkpoints against
real Vast + live prod gateway.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-CONTEXT.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-VALIDATION.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-01-SUMMARY.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-02-SUMMARY.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-03-SUMMARY.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-04-SUMMARY.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Cold-start UAT on real 1×3090 Vast pod + image inspection</name>
  <what-built>
    Plans 01-04 produced a shrunk pod image + reconciler that targets 1×3090 cap
    $0.30/h. This UAT validates the gain table targets: image size ≤8GB, cold-start
    ≤15min, VRAM running ≤22GB, no :8001 listener, llama+chatterbox+dcgm endpoints
    healthy.
  </what-built>
  <how-to-verify>
    Pre-req: Plans 01-04 merged + CI pushed image `ghcr.io/ifixtelecom/converseai-primary-pod:phase-11.1-<sha>`.

    1. From ops-claude: spin a Vast pod against the cheapest 1×3090 offer from Plan 00's EVIDENCE-00 allowlist:
       ```
       export VAST_AI_API_KEY=<from CLAUDE.md>
       pod/scripts/vast-ai.sh create \
         --gpu "RTX 3090" --num-gpus 1 --price 0.30 \
         --image ghcr.io/ifixtelecom/converseai-primary-pod:phase-11.1-<sha> \
         --inet-down 200 --geo EU,DE,PL
       ```
    2. Record image pull start timestamp + onstart.sh start.
    3. Monitor via `pod/scripts/vast-ai.sh logs <instance_id> | grep -E 'llama|chatterbox|dcgm|health-bridge|ready'` until lifecycle FSM publishes Ready.
    4. Record total cold-start (image pull + onstart + supervisord boot + llama warmup) wall time. Target ≤15min per gain table.
    5. From the pod (or via Vast-routed port forward), check health:
       ```
       curl -s http://<pod-ip>:9100/health
       ```
       Expected: response includes llama + chatterbox + dcgm entries; NO `stt` / `:8001` / `speaches` entries.
    6. Check VRAM: `nvidia-smi --query-gpu=memory.used --format=csv,noheader` via Vast exec. Record value. Target ≤22GB.
    7. `docker image inspect ghcr.io/ifixtelecom/converseai-primary-pod:phase-11.1-<sha> --format '{{.Size}}'` (locally on ops-claude after a pull). Record bytes. Target ≤8589934592 (8GB).
    8. Tear down pod: `pod/scripts/vast-ai.sh destroy <instance_id>`.
    9. Append: image_size_bytes, cold_start_seconds, vram_running_mb, pod_health_response to `.planning/phases/11.1-shrink-pod-remove-whisper/11.1-EVIDENCE.md`.
    10. If cold-start >15min: per Pitfall #5, log as KNOWN deferred-not-blocking (SC-2 P90 ≤6min remains unmet pending image pre-bake — Phase 12+).
  </how-to-verify>
  <resume-signal>
    Type "approved: size=<bytes> coldstart=<sec> vram=<mb>" OR describe deviation from target with disposition (accept / re-plan / escalate).
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Tier-1 STT live curl against prod gateway + memory note supersede</name>
  <what-built>
    Plans 01-05 merged. Confirms tier-1 OpenAI whisper-1 still absorbs STT
    through the prod gateway with the local-stt row removed; emits the new
    standing-config memory note.
  </what-built>
  <how-to-verify>
    1. From ops-claude, with a small WAV sample (e.g. `~/sample.wav` ≤1MB), POST to prod gateway:
       ```
       curl -X POST https://ai-gateway.converse-ai.app/v1/audio/transcriptions \
         -H "Authorization: Bearer ifix_sk_npday2ekg4jmoonb7xd3lsrl5muplo4w" \
         -F file=@~/sample.wav \
         -F model=whisper
       ```
       (chat-ifix tenant key per CLAUDE.md "API Keys" section)
       Expected: HTTP 200; JSON body with `text` field; latency observable (Phase 11 D-04 SLO P95 ≤10s — single sample OK).
    2. Check audit_log on n8n-ia-vm:
       ```
       ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl audit tail --limit 5 --filter route=transcriptions'
       ```
       Expected: latest entry has `upstream_name=openai-whisper`, NOT `local-stt`.
    3. Confirm operator removed `UPSTREAM_STT_URL` from prod .env (T-11.1-02):
       ```
       ssh n8n-ia-vm 'grep -c UPSTREAM_STT_URL /opt/ai-gateway-prod/.env'
       ```
       Expected: 0
    4. Create new memory note at `$HOME/.claude/projects/-home-pedro-projetos-pedro-gpu-ifix/memory/primary-gpu-shape-11.1-final.md` containing:
       - PRIMARY_VAST_GPU_NAME_PRIMARY=RTX 3090; PRICE_CAP_PRIMARY=0.30
       - PRIMARY_VAST_GPU_NAME_FALLBACK=RTX 4090; PRICE_CAP_FALLBACK=0.40
       - PRIMARY_VAST_NUM_GPUS=1
       - Allowlist host_ids (from Plan 00 EVIDENCE-00 evidence + Task 1 cold-start measurement)
       - Supersede note: "Replaces `primary-gpu-shape-06.8-final` after Phase 11.1 deploy"
       - Cite EVIDENCE.md cold-start + VRAM + image size measurements
    5. Append index entry to `$HOME/.claude/projects/-home-pedro-projetos-pedro-gpu-ifix/memory/MEMORY.md`:
       ```
       - [Primary GPU shape 11.1 final](primary-gpu-shape-11.1-final.md) — supersedes 06.8-final; 1×3090 primary cap $0.30 + 1×4090 fallback cap $0.40
       ```
    6. Finalize `.planning/phases/11.1-shrink-pod-remove-whisper/11.1-EVIDENCE.md` with retro summary (cost savings observed; actual vs gain-table deltas).
  </how-to-verify>
  <resume-signal>
    Type "approved: tier-1 200 + audit upstream=openai-whisper + UPSTREAM_STT_URL removed + memory note created" OR describe any failure (e.g. breaker OPEN on first call) with remediation path.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Vast.ai market dynamics | offer inventory may shift between survey + actual deploy |
| Prod gateway live traffic | live curl exercises the post-shrink path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-04 | Availability | Vast 4090 fallback fleet scarcity (RESEARCH Assumption A1) | mitigate | Plan 00 pre-code Vast survey (Wave 0) established fleet baseline + operator-approved decision. This plan's Task 1 cold-start UAT re-verifies post-deploy against a real 1×3090 pod; deviation triggers SC-2 deferred-not-blocking log or fallback to 4090 shape. |
| T-11.1-02 | Information disclosure (low) | Prod `UPSTREAM_STT_URL=http://10.10.10.20:8001` lingering after deploy | mitigate | Task 2 post-deploy grep audit verifies env line removed; documented in RUNBOOK-DEPLOY.md by Plan 05 |
| T-11.1-Cost-Regression | (financial) | actual Vast price drift vs cap | accept | EVIDENCE.md captures actual $/h observed; if >$0.30, post-deploy retro decides cap adjustment |
</threat_model>

<verification>
- Task 1 (was Task 2): cold-start ≤15min on real 1×3090; image ≤8GB; no :8001 listener
- Task 2 (was Task 3): prod tier-1 STT curl returns 200; UPSTREAM_STT_URL removed from prod .env; memory note created
- Pre-code Vast fleet survey: see Plan 00 EVIDENCE-00 (gating pre-Plan 01)
</verification>

<success_criteria>
- 11.1-EVIDENCE.md committed with cold-start + image size + VRAM + price (fleet inventory snapshot lives in Plan 00 EVIDENCE-00)
- memory note `primary-gpu-shape-11.1-final.md` created + indexed in MEMORY.md
- T-11.1-04 disposition resolved (verified or escalated)
- T-11.1-02 disposition resolved (operator removed env line + audit confirms)
</success_criteria>

<output>
Create `.planning/phases/11.1-shrink-pod-remove-whisper/11.1-06-SUMMARY.md` when done; reference EVIDENCE.md + new memory note path.
</output>
