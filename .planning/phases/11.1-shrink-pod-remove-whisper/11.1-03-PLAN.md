---
phase: 11.1-shrink-pod-remove-whisper
plan: 03
type: execute
wave: 3
depends_on: ["11.1-02"]
files_modified:
  - pod/primary/Dockerfile
  - pod/primary/supervisord.conf
  - pod/onstart.sh
autonomous: true
requirements:
  - "SEED Mudança 1 (Dockerfile drop Stage 1b speaches-assets + venv + silero_*.onnx + realtime-console stub)"
  - "SEED Mudança 2 (supervisord remove [program:speaches])"
  - "SEED Mudança 3 (onstart.sh remove whisper tarball download + mkdir /weights/whisper)"
  - "D-A2 (bundle Infinity venv removal — drop Stage 1d + [program:infinity])"
  - "SEED Mudança 8 (CI workflow audit — .github/workflows/build-primary-pod.yml free of speaches/whisper/infinity/silero tokens)"
threat_refs: []

must_haves:
  truths:
    - "docker build pod/primary/ succeeds"
    - "Resulting image lacks /opt/speaches-venv directory"
    - "Resulting image lacks /opt/infinity-venv directory (D-A2 bundle)"
    - "Resulting image lacks silero_*.onnx files"
    - "Image size ≤8GB"
    - "supervisord.conf has no [program:speaches] block"
    - "supervisord.conf has no [program:infinity] block (D-A2 bundle)"
    - "onstart.sh has no whisper tarball curl + no mkdir /weights/whisper"
  artifacts:
    - path: "pod/primary/Dockerfile"
      provides: "Multi-stage build without speaches venv / Infinity venv / silero / realtime-console stub"
    - path: "pod/primary/supervisord.conf"
      provides: "Process supervision for llama + chatterbox + dcgm-scraper + health-bridge only"
    - path: "pod/onstart.sh"
      provides: "Cold-start bootstrap without whisper-large-v3 download"
  key_links: []
---

<objective>
Wave 3 pod image refactor (Part A): drop speaches venv + Infinity venv from
Dockerfile, remove `[program:speaches]` + `[program:infinity]` from supervisord,
remove whisper tarball download from onstart.sh.

D-A2 bundles Infinity venv removal symmetric to speaches cleanup.

Output: shrunk pod image ~6-7GB (target -3-4GB delta per gain table).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/seeds/SEED-010-shrink-pod-remove-whisper-stt.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-CONTEXT.md
@.planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md
@pod/primary/Dockerfile
@pod/primary/supervisord.conf
@pod/onstart.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip speaches + Infinity venv stages from Dockerfile</name>
  <files>pod/primary/Dockerfile</files>
  <read_first>
    .planning/phases/11.1-shrink-pod-remove-whisper/11.1-RESEARCH.md (Component Responsibilities table row: pod/primary/Dockerfile),
    .planning/seeds/SEED-010-shrink-pod-remove-whisper-stt.md (Mudança 1 enumerated),
    pod/primary/Dockerfile (full file — locate Stage 1b lines 35-97, Stage 1d infinity lines 144-152, Stage 1e realtime-console stub, silero_*.onnx COPY)
  </read_first>
  <action>
    Per SEED Mudança 1 + D-A2:
    1. In `pod/primary/Dockerfile`, DELETE the entire Stage 1b `speaches-assets` block (locate via `FROM ... AS speaches-assets` header — RESEARCH points to lines 35-97).
    2. DELETE the Stage 1c `speaches-venv` block (`FROM ... AS speaches-venv` / `pip install ...speaches`).
    3. DELETE any final-stage COPY that pulls from `speaches-venv` / `speaches-assets` (typically `COPY --from=speaches-venv /opt/speaches-venv /opt/speaches-venv` + `COPY --from=speaches-assets /opt/speaches-data ...`).
    4. DELETE any `COPY ... silero_*.onnx ...` lines from final stage.
    5. DELETE the `/opt/speaches-data/realtime-console/dist` stub creation/COPY.
    6. Per D-A2, DELETE the Stage 1d `infinity-venv` block (`FROM ... AS infinity-venv` / `pip install infinity-emb`; lines 144-152 per RESEARCH) AND any final-stage `COPY --from=infinity-venv /opt/infinity-venv /opt/infinity-venv`.
    7. Verify chatterbox stages remain untouched (D-A3 preservation).
    8. After edits, validate build. ops-claude lacks the build context; run on vps-ifix-vm (has buildx + GHA runner workspace) OR defer to CI: `ssh vps-ifix-vm 'docker buildx build --target final -f pod/primary/Dockerfile pod/primary/ -t pod-primary:11.1-shrink-test --load'` OR push a branch and confirm `.github/workflows/build-primary-pod.yml` runs green.
    9. Run `docker run --rm pod-primary:11.1-shrink-test ls -la /opt/` — output MUST NOT include `speaches-venv` or `infinity-venv` directories.
    10. Run `docker image inspect pod-primary:11.1-shrink-test --format '{{.Size}}'` — record value in commit message; MUST be ≤8589934592 (8GB).
  </action>
  <verify>
    <automated>
      cd /home/pedro/projetos/pedro/gpu-ifix && grep -c 'speaches-venv\|infinity-venv\|silero_.*\.onnx\|speaches-assets' pod/primary/Dockerfile
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'speaches' pod/primary/Dockerfile` returns 0
    - `grep -c 'infinity-venv\\|infinity-emb' pod/primary/Dockerfile` returns 0
    - `grep -c 'silero_.*\\.onnx' pod/primary/Dockerfile` returns 0
    - `grep -c 'chatterbox' pod/primary/Dockerfile` returns ≥1 (preservation check per D-A3)
    - `docker buildx build --target final -f pod/primary/Dockerfile pod/primary/ -t pod-primary:11.1-shrink-test --load` exits 0
    - `docker image inspect pod-primary:11.1-shrink-test --format '{{.Size}}'` returns value ≤ 8589934592
    - `docker run --rm pod-primary:11.1-shrink-test sh -c 'ls /opt/ | grep -E "speaches|infinity"'` returns exit code 1 (no matches)
    - SEED Mudança 8 (CI workflow audit): if `.github/workflows/build-primary-pod.yml` exists, `grep -ci 'speaches\|whisper\|infinity\|silero' .github/workflows/build-primary-pod.yml` returns 0. If the file does not exist, document the absence in the SUMMARY (no CI workflow to audit — build happens elsewhere).
  </acceptance_criteria>
  <done>Image builds without speaches/Infinity venv; size under 8GB; chatterbox stages intact.</done>
</task>

<task type="auto">
  <name>Task 2: Remove [program:speaches] + [program:infinity] from supervisord.conf</name>
  <files>pod/primary/supervisord.conf</files>
  <read_first>
    .planning/seeds/SEED-010-shrink-pod-remove-whisper-stt.md (Mudança 2),
    pod/primary/supervisord.conf (full file — locate [program:speaches] block ~lines 35-66 and [program:infinity] block if present)
  </read_first>
  <action>
    Per SEED Mudança 2 + D-A2:
    1. DELETE the entire `[program:speaches]` block from `pod/primary/supervisord.conf` (header line + all directives: command/autostart/autorestart/stdout_logfile/stderr_logfile/environment/etc., terminating at the next `[program:` header or EOF).
    2. DELETE the entire `[program:infinity]` block if present (D-A2 bundle).
    3. Confirm remaining program blocks: `[program:llama]`, `[program:chatterbox]`, `[program:dcgm-scraper]`, `[program:health-bridge]` (exact set depends on current file — preserve everything except speaches + infinity).
    4. Update file header comment (if any) to reflect 4-process supervision (was 5-6 pre-shrink).
  </action>
  <verify>
    <automated>
      cd /home/pedro/projetos/pedro/gpu-ifix && grep -cE '^\[program:(speaches|infinity)' pod/primary/supervisord.conf
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\\[program:(speaches|infinity)' pod/primary/supervisord.conf` returns 0
    - `grep -cE '^\\[program:(llama|chatterbox|dcgm-scraper|health-bridge)' pod/primary/supervisord.conf` returns ≥3 (chatterbox + llama + at least one of dcgm/health-bridge per D-A3 preservation)
    - File parses as valid INI: `python3 -c "import configparser; c=configparser.ConfigParser(); c.read('pod/primary/supervisord.conf'); print(c.sections())"` exits 0
  </acceptance_criteria>
  <done>supervisord no longer launches speaches or infinity processes; chatterbox / llama / scrapers / bridge intact.</done>
</task>

<task type="auto">
  <name>Task 3: Strip whisper download from onstart.sh</name>
  <files>pod/onstart.sh</files>
  <read_first>
    .planning/seeds/SEED-010-shrink-pod-remove-whisper-stt.md (Mudança 3),
    pod/onstart.sh (full file — locate whisper tarball curl section + mkdir /weights/whisper + line 129-130 embed log line)
  </read_first>
  <action>
    Per SEED Mudança 3:
    1. DELETE the whisper section from `pod/onstart.sh`: any `mkdir -p /weights/whisper` + any `curl ... whisper ... tar.gz` + any `tar -xzf ... -C /weights/whisper` + matching log lines.
    2. DELETE the embed-related log line at ~line 129-130 (D-A2 bundle — Phase 06.7 D-03 dead-code).
    3. Preserve all chatterbox / llama / TTS / embed (if-active) weight bootstrap blocks.
    4. Update header comment to reflect "Phase 11.1: STT off-pod; chatterbox TTS + llama LLM only".
  </action>
  <verify>
    <automated>
      cd /home/pedro/projetos/pedro/gpu-ifix && grep -ciE 'whisper|/weights/whisper' pod/onstart.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE 'whisper' pod/onstart.sh` returns 0
    - `grep -c '/weights/whisper' pod/onstart.sh` returns 0
    - `bash -n pod/onstart.sh` exits 0 (syntax check)
    - `grep -c 'chatterbox\\|llama' pod/onstart.sh` returns ≥1 (preservation)
  </acceptance_criteria>
  <done>Cold-start script no longer downloads whisper-large-v3; ~5GB WAN delta on cold-start expected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build → runtime | image content frozen at build; runtime must not reference deleted layers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-Attack-Surface | (positive) Info disclosure reduction | pod image | mitigate | Removing `:8001` Speaches HTTP listener + Infinity HTTP listener reduces attack surface (RESEARCH Security Domain V12 row). Plan 04 closes the health-bridge probe path. |
</threat_model>

<verification>
- All three task verify commands return 0 hits
- `docker buildx build` succeeds
- Image size ≤8GB
</verification>

<success_criteria>
- pod image rebuilt without speaches+Infinity layers; under 8GB
- supervisord launches only the 4 preserved processes
- onstart skips whisper download
</success_criteria>

<output>
Create `.planning/phases/11.1-shrink-pod-remove-whisper/11.1-03-SUMMARY.md` when done; record image size delta vs Phase 06.7 baseline.
</output>
