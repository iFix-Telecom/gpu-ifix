---
phase: 13-dashboard-user-management-gest-o-de-operadores-owner-only-se
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - dashboard/src/lib/auth.ts
  - dashboard/src/lib/email.ts
  - dashboard/src/lib/schema.ts
  - dashboard/src/lib/schema-custom.ts
  - dashboard/src/lib/db.ts
  - dashboard/drizzle.config.ts
  - dashboard/src/lib/auth.test.ts
  - scripts/dashboard/seed-owner.ts
autonomous: false
requirements: [UM-02, UM-03, UM-08, UM-09]
user_setup:
  - service: brevo-smtp
    why: "Invite/reset email delivery via Brevo SMTP relay (D-04)"
    env_vars:
      - name: BREVO_SMTP_USER
        source: "Brevo account 797fad001 (CLAUDE.md §Brevo) — add to dashboard stack env on n8n-ia-vm"
      - name: BREVO_SMTP_PASS
        source: "Brevo account 797fad001 SMTP key — dashboard stack env on n8n-ia-vm"
      - name: BETTER_AUTH_URL
        source: "Confirm = https://ai-dashboard.converse-ai.app in prod container (drives reset-link origin, A4/Pitfall 6)"

must_haves:
  truths:
    - "auth.ts registers admin({ adminRoles:['owner'], defaultRole:'operator' }) and boots without invalid-roles error"
    - "auth.ts emailAndPassword.sendResetPassword wired to nodemailer Brevo transport"
    - "schema.ts (regenerated) carries role/banned/banReason/banExpires on user + impersonatedBy on session"
    - "admin_audit_log table exists in bd_ai_dashboard_prod public schema (via schema-custom + drizzle-kit push)"
    - "After schema push, exactly one user row has role='owner'; all other NULL roles normalized to 'operator'"
    - "CR-01 hook (auth.ts:127-147) and D-13 allowlist hook (auth.ts:156-168) preserved verbatim"
  artifacts:
    - path: "dashboard/src/lib/auth.ts"
      provides: "admin plugin + sendResetPassword"
      contains: "admin({ adminRoles"
    - path: "dashboard/src/lib/email.ts"
      provides: "nodemailer Brevo SMTP transport (smtp-relay.brevo.com:587)"
      contains: "smtp-relay.brevo.com"
    - path: "dashboard/src/lib/schema-custom.ts"
      provides: "admin_audit_log pgTable (D-08 columns)"
      contains: "admin_audit_log"
    - path: "scripts/dashboard/seed-owner.ts"
      provides: "one-shot idempotent owner backfill"
      contains: "ORDER BY created_at ASC"
  key_links:
    - from: "dashboard/src/lib/db.ts"
      to: "dashboard/src/lib/schema-custom.ts"
      via: "spread { ...authSchema, ...customSchema } into drizzle client"
      pattern: "schema-custom"
    - from: "dashboard/drizzle.config.ts"
      to: "dashboard/src/lib/schema*.ts"
      via: "schema glob so push migrates admin_audit_log"
      pattern: "schema\\*"
    - from: "dashboard/src/lib/auth.ts"
      to: "dashboard/src/lib/email.ts"
      via: "import mailer for sendResetPassword"
      pattern: "from \"@/lib/email\""
---

<objective>
Wire the admin plugin, the Brevo SMTP transport, the regenerated CLI-canonical
schema, the isolated custom audit table, and the coupled schema-push → owner-seed
migration. This is the security-and-data foundation every server action in Plan 03
depends on.

Purpose: D-01 reverses the prior no-admin-plugin choice; D-02 makes owner an
explicit persisted role; D-08 adds the audit table; D-04 wires reset/invite email.
Output: auth.ts + email.ts + schema(regen) + schema-custom.ts + db.ts/drizzle.config
wiring + seed-owner.ts, with prod schema migrated and owner seeded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-RESEARCH.md
@.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-PATTERNS.md
@.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-CONTEXT.md

<interfaces>
<!-- Verified from codebase 2026-06-15 -->
From dashboard/src/lib/auth.ts:
  L44 import { twoFactor } from "better-auth/plugins";  (add admin alongside)
  L64 emailAndPassword: { enabled: true, autoSignIn: false }  (add sendResetPassword)
  L127-147 CR-01 before-hook (TWO_FACTOR_ALREADY_ENABLED at L143) — PRESERVE VERBATIM
  L151 plugins: [twoFactor({ issuer: "Ifix AI Gateway" })]  (append admin)
  L156-168 databaseHooks D-13 allowlist — PRESERVE VERBATIM
From dashboard/src/lib/db.ts:
  L22 import * as schema from "./schema";  →  spread authSchema+customSchema
  L43 drizzle(pool, { schema });  L63-65 getDb();  L76 export { schema };
From dashboard/src/lib/schema.ts:
  L24 pgTable("user",...);  L35 twoFactorEnabled boolean("two_factor_enabled");
  L98 pgTable("two_factor",...)
From dashboard/drizzle.config.ts:
  L51 schema: "./src/lib/schema.ts"  →  "./src/lib/schema*.ts"
admin plugin endpoints (better-auth 1.4.22): /admin/create-user, /admin/remove-user,
  /admin/set-user-password, /admin/set-role, /admin/revoke-user-sessions.
  Schema cols added: user.{role,banned,banReason,banExpires}, session.impersonatedBy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire admin plugin + Brevo email transport + sendResetPassword</name>
  <files>dashboard/src/lib/auth.ts, dashboard/src/lib/email.ts</files>
  <read_first>
    - dashboard/src/lib/auth.ts (L1-40 header comment documenting why admin plugin was omitted; L44 import; L64 emailAndPassword; L127-147 CR-01; L151 plugins; L156-168 allowlist)
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-RESEARCH.md §Pattern 1 (admin plugin), §Pattern 4 (sendResetPassword via nodemailer), §Pitfall 4 (adminRoles must include "owner"), §Pitfall 6 (BETTER_AUTH_URL origin)
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-PATTERNS.md §auth.ts (verbatim plugin + emailAndPassword edits), §email.ts
    - CLAUDE.md global §Brevo (smtp-relay.brevo.com:587, FROM noreply@ifixtelecom.com.br, account 797fad001)
  </read_first>
  <action>Create dashboard/src/lib/email.ts exporting `mailer = nodemailer.createTransport({ host:"smtp-relay.brevo.com", port:587, secure:false, auth:{ user:process.env.BREVO_SMTP_USER, pass:process.env.BREVO_SMTP_PASS } })`. Edit auth.ts: add `admin` to the `better-auth/plugins` import (alongside twoFactor at L44); append `admin({ adminRoles: ["owner"], defaultRole: "operator" })` to the plugins array (keep twoFactor first, L151). Extend the emailAndPassword block (L64) with a `sendResetPassword: async ({ user, url }) => ...` callback that calls `mailer.sendMail({ from: "'iFix AI Gateway' <noreply@ifixtelecom.com.br>", to: user.email, subject: "Defina sua senha — iFix AI Gateway", text: ... url ... })`; keep `enabled:true` + `autoSignIn:false`. Update the L1-40 header comment to record the Phase 13 reversal (D-01). DO NOT touch the CR-01 hook (L127-147) or D-13 allowlist hook (L156-168). Per RESEARCH A2/Pitfall 4: if `admin({ adminRoles:["owner"] })` warns about an invalid custom role at boot, add a `createAccessControl` roles map including `owner` and `operator` (small, contained) — this is verified in Task 2 staging boot-test before any prod push.</action>
  <verify>
    <automated>cd dashboard && grep -q 'admin({ adminRoles: \["owner"\]' src/lib/auth.ts && grep -q 'smtp-relay.brevo.com' src/lib/email.ts && grep -q 'sendResetPassword' src/lib/auth.ts && grep -n 'TWO_FACTOR_ALREADY_ENABLED' src/lib/auth.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - auth.ts contains `admin({ adminRoles: ["owner"], defaultRole: "operator" })`
    - auth.ts emailAndPassword contains sendResetPassword calling mailer.sendMail
    - email.ts createTransport host = smtp-relay.brevo.com, port 587, secure false
    - CR-01 hook still present (grep TWO_FACTOR_ALREADY_ENABLED matches) and unchanged
    - D-13 allowlist hook still present; `bunx tsc --noEmit` clean
  </acceptance_criteria>
  <done>admin plugin + sendResetPassword wired; CR-01/allowlist verbatim; types clean.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate schema (CLI-canonical) + custom audit table + db/drizzle wiring + owner-seed script + staging boot-test (A2)</name>
  <files>dashboard/src/lib/schema.ts, dashboard/src/lib/schema-custom.ts, dashboard/src/lib/db.ts, dashboard/drizzle.config.ts, dashboard/src/lib/auth.test.ts, scripts/dashboard/seed-owner.ts</files>
  <read_first>
    - dashboard/src/lib/schema.ts (header L1-20 CLI-canonical workflow; L21-36 user pgTable imports idiom; L98-112 two_factor)
    - dashboard/src/lib/db.ts (L22 import * as schema; L43 drizzle(pool,{schema}); L76 export)
    - dashboard/drizzle.config.ts (L51 schema field; TLS/CA logic to keep)
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-RESEARCH.md §Pitfall 1 (regen drops custom table → schema-custom.ts), §Pitfall 2 (staging-first push), §Pattern 7 (owner seed), §Open Questions A2
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-PATTERNS.md §schema-custom.ts, §audit.ts (column idiom), §seed-owner.ts
    - dashboard/src/lib/auth.test.ts (extend for UM-02 admin-plugin boot + UM-07 CR-01 invariant)
  </read_first>
  <action>Run `cd dashboard && bunx @better-auth/cli@latest generate --output src/lib/schema.ts --yes` to regenerate schema.ts WITH the admin columns (role/banned/banReason/banExpires on user; impersonatedBy on session) — never hand-edit. Create schema-custom.ts with `adminAuditLog = pgTable("admin_audit_log", { id text pk, actorId text("actor_id") notNull, actorEmail text("actor_email") notNull, targetId text("target_id"), targetEmail text("target_email"), action text notNull, metadata jsonb, createdAt timestamp("created_at").defaultNow().notNull() }, t => [index("admin_audit_log_actor_idx").on(t.actorId)])` (D-08 columns). Wire db.ts: `import * as authSchema from "./schema"; import * as customSchema from "./schema-custom"; const schema = { ...authSchema, ...customSchema };` keep `drizzle(pool,{schema})` + `export { schema }`. Change drizzle.config.ts L51 to `schema: "./src/lib/schema*.ts"` (keep strict:true + TLS/CA). Create scripts/dashboard/seed-owner.ts using getDb() + db.execute(sql) with the idempotent two-statement backfill (earliest user→owner only if no owner exists; NULL→operator); fail-fast on missing DASHBOARD_DATABASE_URL; print no secrets. Extend auth.test.ts: assert betterAuth boots with admin({adminRoles:["owner"]}) and the user table carries `role` (UM-02); and a reset-2FA CR-01 invariant test — after clearing two_factor + twoFactorEnabled=false, /two-factor/enable is permitted (UM-07). A2 staging boot-test: run the extended auth.test.ts which boots the real plugin config over memoryAdapter — if it throws/warns invalid-roles, add the createAccessControl roles map from Task 1 and re-run until green.</action>
  <verify>
    <automated>cd dashboard && grep -q 'admin_audit_log' src/lib/schema-custom.ts && grep -q 'schema-custom' src/lib/db.ts && grep -q 'schema\*' drizzle.config.ts && grep -Eq 'text\("role"\)' src/lib/schema.ts && bunx tsc --noEmit && bun run test src/lib/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - schema.ts (regenerated) contains a `role` column on user (admin plugin)
    - schema-custom.ts declares admin_audit_log with all D-08 columns + actor index
    - db.ts spreads authSchema + customSchema; drizzle.config schema = ./src/lib/schema*.ts
    - scripts/dashboard/seed-owner.ts is idempotent (re-run no-op when owner exists), fail-fast on missing DSN
    - auth.test.ts boots admin plugin (UM-02 green) + CR-01 reset-2FA invariant (UM-07 green)
  </acceptance_criteria>
  <done>schema regenerated with admin cols; admin_audit_log isolated in schema-custom; db/config wired; seed-owner idempotent; UM-02/UM-07 auth.test green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [BLOCKING] Staging-rehearsed drizzle-kit push → IMMEDIATE owner-seed (A4/A5, irreversible prod)</name>
  <read_first>
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-RESEARCH.md §Pitfall 2 (staging-first, additive-only), §Runtime State Inventory (push MUST be immediately followed by owner-seed or no owner exists → admin gate locks everyone), §Open Questions A4/A5
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-VALIDATION.md §Manual-Only Verifications + §Staging-first checks
    - memory ai-dashboard-access-and-2fa-recovery.md (DSN sslmode=no-verify + search_path=public; bd_ai_dashboard_prod / bd_ai_dashboard_staging)
  </read_first>
  <what-built>schema-custom + regenerated schema declare additive columns (role/banned/banReason/banExpires, impersonatedBy) + the admin_audit_log table. This task applies them to the live DB. RESEARCH §Runtime State: the prod `user` table has role IS NULL for every existing row until the seed runs — schema push and owner-seed are COUPLED with no gap, or the admin gate denies everyone and the dashboard becomes unmanageable.</what-built>
  <how-to-verify>
    1. A5 staging dry-run: against `bd_ai_dashboard_staging`, run `cd dashboard && NODE_ENV=development bunx drizzle-kit push` and REVIEW the proposed SQL. CONFIRM it is purely ADD COLUMN + CREATE TABLE admin_audit_log. ABORT if any DROP / ALTER COLUMN ... TYPE / two_factor recreation appears.
    2. A4: confirm `BETTER_AUTH_URL=https://ai-dashboard.converse-ai.app` in the prod dashboard container env on n8n-ia-vm (drives reset-link origin). Add BREVO_SMTP_USER + BREVO_SMTP_PASS to the dashboard stack env (account 797fad001).
    3. Apply to prod: `bunx drizzle-kit push` against bd_ai_dashboard_prod. NEVER auto-approve.
    4. IMMEDIATELY (same session, no gap) run `cd dashboard && bun run scripts/dashboard/seed-owner.ts` against bd_ai_dashboard_prod.
    5. Assert exactly one owner: `SELECT count(*) FROM "user" WHERE role='owner'` returns 1; `SELECT count(*) FROM "user" WHERE role IS NULL` returns 0.
  </how-to-verify>
  <acceptance_criteria>
    - Staging diff reviewed = additive-only (no DROP/ALTER TYPE/two_factor recreate)
    - BETTER_AUTH_URL confirmed public origin; BREVO_SMTP_USER/PASS set in prod stack
    - admin_audit_log + admin columns present in bd_ai_dashboard_prod public schema
    - Exactly one user row role='owner'; zero rows role IS NULL (seed ran immediately after push)
  </acceptance_criteria>
  <files>dashboard/src/lib/schema.ts, dashboard/src/lib/schema-custom.ts, scripts/dashboard/seed-owner.ts</files>
  <action>Rehearse on bd_ai_dashboard_staging then apply to bd_ai_dashboard_prod per how-to-verify: staging `NODE_ENV=development bunx drizzle-kit push` + review additive-only diff (abort on DROP/ALTER TYPE/two_factor recreate); confirm BETTER_AUTH_URL=public origin + set BREVO_SMTP_USER/BREVO_SMTP_PASS in the n8n-ia-vm dashboard stack; `bunx drizzle-kit push` on prod (never auto-approve); IMMEDIATELY `bun run scripts/dashboard/seed-owner.ts` with no gap. This is a blocking-human, staging-first, irreversible prod migration coupled to the owner-seed.</action>
  <verify>
    <automated>HUMAN: SELECT count(*) FROM "user" WHERE role='owner' returns 1 AND SELECT count(*) FROM "user" WHERE role IS NULL returns 0 against bd_ai_dashboard_prod</automated>
  </verify>
  <done>Additive-only push applied to prod + owner seeded immediately (exactly 1 owner, 0 NULL roles); BREVO_SMTP creds + BETTER_AUTH_URL confirmed.</done>
  <resume-signal>Type "approved" after additive-only diff review + prod push + immediate owner-seed verified (1 owner, 0 NULL), or describe the blocking diff.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration tool → prod DB | drizzle-kit push mutates bd_ai_dashboard_prod (irreversible) |
| dashboard → Brevo SMTP relay | invite/reset links leave the container to an external relay |
| dashboard DB ↔ ai_gateway | strict isolation required (07-RESEARCH Pitfall 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-mig | Tampering | drizzle-kit push on prod | mitigate | staging-first additive-only diff review (A5); abort on DROP/ALTER TYPE; never auto-approve (Task 3) |
| T-13-lockout | Denial of Service | role IS NULL after push, no owner | mitigate | owner-seed runs IMMEDIATELY after push, same session, no gap (coupled Task 3); assert exactly 1 owner |
| T-13-authz | Elevation of Privilege | adminRoles default ["admin"] vs role 'owner' | mitigate | adminRoles:["owner"] set (Task 1); staging boot-test confirms custom role accepted (A2, Task 2) |
| T-13-crossdb | Tampering | writing to ai_gateway | mitigate | admin_audit_log in schema-custom → bd_ai_dashboard_prod public only; db.ts isolated DSN unchanged |
| T-13-smtp | Information Disclosure | Brevo creds / origin | mitigate | SMTP creds in container env only (not API, IP-locked); BETTER_AUTH_URL=public origin confirmed (A4) |
| T-13-2fa | Tampering | CR-01 credential-rotation vector | mitigate | CR-01 hook preserved verbatim (Task 1); reset-2FA never calls enable (verified Task 2 invariant test) |
</threat_model>

<verification>
- auth.ts admin plugin + sendResetPassword; CR-01 + allowlist verbatim; tsc clean.
- schema.ts regenerated with admin cols; admin_audit_log isolated in schema-custom.ts; db/config wired.
- UM-02 + UM-07 green in auth.test.ts.
- Prod: additive-only push applied + owner seeded (1 owner, 0 NULL roles).
</verification>

<success_criteria>
- Admin plugin gate uses role='owner'; created users default 'operator'.
- Brevo SMTP transport wired; reset-link origin = public dashboard URL.
- Audit table live + isolated; owner deterministically seeded.
</success_criteria>

<output>
Create `.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
