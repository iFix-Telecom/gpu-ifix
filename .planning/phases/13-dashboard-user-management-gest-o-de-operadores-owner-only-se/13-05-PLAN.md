---
phase: 13-dashboard-user-management-gest-o-de-operadores-owner-only-se
plan: 05
type: execute
wave: 4
depends_on: ["13-01", "13-03"]
files_modified:
  - dashboard/src/app/settings/operadores/page.tsx
autonomous: true
requirements: [UM-10]

must_haves:
  truths:
    - "The Função column reads the REAL role column (owner=warning badge, operator=neutral), NOT i===0"
    - "+ Provisionar operador opens the provision dialog (name+email) → inviteOperator server action"
    - "The per-row ··· (MoreHorizontal) opens a dropdown-menu with Resetar senha / Resetar 2FA / Remover operador, each behind an alert-dialog confirm"
    - "When the viewer role !== 'owner', + Provisionar and every ··· trigger are hidden (server actions still re-check, D-03)"
    - "Footer note no longer cites seed-admins.sh (superseded by UI)"
  artifacts:
    - path: "dashboard/src/app/settings/operadores/page.tsx"
      provides: "functional owner-only operator management UI wired to server actions"
      contains: "MoreHorizontal"
  key_links:
    - from: "dashboard/src/app/settings/operadores/page.tsx"
      to: "dashboard/src/app/settings/operadores/actions.ts"
      via: "dialog/menu forms call inviteOperator/removeOperator/resetOperatorPassword/resetOperator2FA"
      pattern: "inviteOperator|removeOperator|resetOperator"
    - from: "dashboard/src/app/settings/operadores/page.tsx"
      to: "dashboard/src/lib/auth.ts (getSession)"
      via: "RSC reads viewer role for owner-gate"
      pattern: "getSession"
---

<objective>
Activate the placeholder operator-management UI: read the real role column,
owner-gate the controls, and wire `+ Provisionar` + the `···` row menu to the
Plan 03 server actions via dialog/dropdown-menu/alert-dialog. Turns the UM-10
RED stub green.

Purpose: D-02 (real role, not i===0), D-03 (owner-gate cosmetic + server re-check),
D-04/D-06/D-07 ops surfaced through dialog/menu/confirm. State of the art:
seed-admins.sh superseded by this UI.
Output: edited operadores/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-PATTERNS.md
@.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-UI-SPEC.md
@.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-CONTEXT.md

<interfaces>
<!-- Verified from codebase 2026-06-15 + upstream plan outputs -->
From dashboard/src/app/settings/operadores/page.tsx (RSC):
  L39-123 loadOperators(); L60 roster SQL (add role: COALESCE(role,'operator'));
  L57 Operator type two_factor_enabled (add role); L330-339 i===0 badge derivation (REPLACE);
  L260 "+ Provisionar operador" button (activate + owner-gate);
  L371 aria-label={`Ações para ${o.name}`} ··· trigger (replace literal ··· w/ MoreHorizontal);
  L386 footer "scripts/dashboard/seed-admins.sh" (replace).
From dashboard/src/app/settings/operadores/actions.ts (Plan 03): inviteOperator(name,email),
  removeOperator(userId), resetOperatorPassword(email,userId), resetOperator2FA(userId).
From dashboard/src/components/ui/ (Plan 01): dialog, dropdown-menu, alert-dialog.
From dashboard/src/lib/auth.ts: auth.api.getSession({ headers: await headers() }) → viewer role.
Lucide: MoreHorizontal, UserPlus?, KeyRound, ShieldOff, Trash2.
Badge tones: badge-warning (owner), badge-neutral (operator) — preserve padding:2px 8px.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real role + owner-gate in the RSC (D-02, D-03 read side)</name>
  <files>dashboard/src/app/settings/operadores/page.tsx</files>
  <read_first>
    - dashboard/src/app/settings/operadores/page.tsx (Operator type L57; loadOperators L39-123; roster SQL L60; i===0 badge L330-339; footer L386)
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-PATTERNS.md §operadores/page.tsx (real-role SELECT + badge replacement + owner-gate)
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-UI-SPEC.md §Owner-only visibility gate (state 8), §Color (badge-warning owner / badge-neutral operator), §Implementation Notes (preserve 2px 8px badge padding)
    - dashboard/src/app/settings/operadores/page.test.tsx (Plan 01 RED UM-10 assertions)
  </read_first>
  <action>Edit the roster query (L60) to add role: `SELECT id, name, email, COALESCE(role, 'operator') AS role, COALESCE(two_factor_enabled, false) AS two_factor_enabled FROM "user" ORDER BY created_at ASC` and add `role: string` to the Operator type (L57) + the row map (L71). Replace the i===0 badge derivation (L330-339) with `o.role === 'owner'` → badge-warning tone, else badge-neutral — preserve the color-mix badge styling + padding:2px 8px (Implementation Notes; do NOT shift to grid). In the RSC, read the viewer role via `auth.api.getSession({ headers: await headers() })`; compute `const isOwner = (session?.user as {role?:string})?.role === 'owner'`. Replace the footer note (L386) citing seed-admins.sh with "operadores gerenciados pelo painel". Pass isOwner down to gate the controls (Task 2). Server actions re-check regardless (D-03 — UI hiding is cosmetic).</action>
  <verify>
    <automated>cd dashboard && grep -q "COALESCE(role, 'operator')" src/app/settings/operadores/page.tsx && grep -Lq 'i === 0' src/app/settings/operadores/page.tsx && grep -Lq 'seed-admins.sh' src/app/settings/operadores/page.tsx; bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - roster SQL selects COALESCE(role,'operator'); Operator type has role
    - badge derives from o.role (owner=warning, operator=neutral); 2px 8px padding preserved
    - no remaining `i === 0` role derivation; footer no longer cites seed-admins.sh
    - viewer isOwner computed from getSession; tsc clean
  </acceptance_criteria>
  <done>Real role badge + owner flag + updated footer; i===0 gone.</done>
</task>

<task type="auto">
  <name>Task 2: Wire + Provisionar dialog + ··· dropdown-menu + destructive alert-dialogs to server actions</name>
  <files>dashboard/src/app/settings/operadores/page.tsx</files>
  <read_first>
    - dashboard/src/app/settings/operadores/page.tsx ("+ Provisionar" L260; ··· trigger L371; aria-label idiom)
    - dashboard/src/app/login/page.tsx (client form + inline error + pending spinner for the dialog form)
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-UI-SPEC.md §Provision dialog (state 5), §Row-action menu (state 6, item order + icons), §Destructive confirm (state 7, default-focus Cancelar, --destructive confirm), §Copywriting (titles/descriptions/CTAs/toasts)
    - dashboard/src/components/ui/{dialog,dropdown-menu,alert-dialog}.tsx (Plan 01)
    - dashboard/src/app/settings/operadores/actions.ts (Plan 03 server action signatures)
  </read_first>
  <action>Activate the controls (a client subcomponent island is fine — keep the page an RSC, extract the interactive pieces into a "use client" child receiving operators + isOwner). Render `+ Provisionar operador` + every `···` trigger ONLY when isOwner. Provision: `+ Provisionar` opens a `dialog` (max-w 384px, 24px padding, 16px gap) with Nome + E-mail fields → primary "Enviar convite" (pending "Enviando…" + 14×14 spinner) calling `inviteOperator(name,email)`; on success toast "Convite enviado para {email}."; allowlist/duplicate errors → inline copy (UI-SPEC). Row `···`: replace literal `···` text with `<MoreHorizontal/>` (keep `aria-label={`Ações para ${o.name}`}`), open a `dropdown-menu` with items in order: Resetar senha (neutral, KeyRound), Resetar 2FA (neutral, ShieldOff), separator, Remover operador (text-destructive, Trash2). Each item opens its `alert-dialog` confirm (max-w 384px, default-focus Cancelar, --destructive confirm button) with the UI-SPEC title/description (target email + consequence: sessions revoked; reset-2FA body notes CR-01-safe re-enroll). Confirms call resetOperatorPassword(email,userId) / resetOperator2FA(userId) / removeOperator(userId) and toast on success. Do NOT surface banUser/impersonate (D-01). Privacy: never render TOTP/backup/hash/temp-password/IP/raw UUID — only email + relative time.</action>
  <verify>
    <automated>cd dashboard && grep -q 'MoreHorizontal' src/app/settings/operadores/page.tsx && grep -Eq 'inviteOperator|removeOperator|resetOperator' src/app/settings/operadores/page.tsx && grep -Lq 'banUser\|impersonate' src/app/settings/operadores/page.tsx; bun run test src/app/settings/operadores/page.test.tsx && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - + Provisionar + ··· render only when isOwner; ··· uses MoreHorizontal with aria-label preserved
    - provision dialog → inviteOperator; menu items wire reset-pw/reset-2fa/remove via alert-dialog confirms
    - destructive confirms default-focus Cancelar, --destructive confirm button
    - no banUser/impersonate; no TOTP/backup/hash/temp-pwd rendered
    - operadores/page.test.tsx green (UM-10); tsc clean
  </acceptance_criteria>
  <done>Provision + row ops functional behind owner-gate + confirms; UM-10 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → server actions | UI affordances are owner-gated cosmetically; server actions re-enforce (D-03) |
| RSC role read → control visibility | viewer role from getSession decides which controls render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-authz | Elevation of Privilege | hidden-but-callable controls | mitigate | UI hides controls when !isOwner BUT Plan 03 requireOwner re-checks every action server-side (D-03); never trust UI gating |
| T-13-disclosure | Information Disclosure | roster / dialog content | mitigate | privacy rule — only email + relative time; never TOTP/backup/hash/temp-pwd/IP/raw UUID (UI-SPEC §Privacy) |
| T-13-session | Session Management | destructive confirm copy | accept | confirm dialogs state sessions are revoked; cookieCache ≤30min staleness documented (RUNBOOK class 4) |
| T-13-escalation | Elevation of Privilege | banUser/impersonate exposure | mitigate | those endpoints never wired into UI (D-01); grep gate confirms absence |
</threat_model>

<verification>
- Real role badge; owner-gate hides controls for non-owners; server actions re-check.
- Provision dialog + ··· menu + destructive confirms wired to Plan 03 actions.
- No banUser/impersonate; no credential rendered.
- operadores/page.test.tsx green (UM-10); tsc clean.
</verification>

<success_criteria>
- Functional owner-only operator management via UI.
- seed-admins.sh superseded; placeholder buttons live.
</success_criteria>

<output>
Create `.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-05-SUMMARY.md` when done.
</output>
