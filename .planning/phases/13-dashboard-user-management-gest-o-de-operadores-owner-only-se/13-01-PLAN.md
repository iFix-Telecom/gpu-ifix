---
phase: 13-dashboard-user-management-gest-o-de-operadores-owner-only-se
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/package.json
  - dashboard/src/components/ui/dialog.tsx
  - dashboard/src/components/ui/dropdown-menu.tsx
  - dashboard/src/components/ui/alert-dialog.tsx
  - dashboard/src/lib/admin-actions.test.ts
  - dashboard/src/lib/seed-owner.test.ts
  - dashboard/src/app/settings/page.test.tsx
  - dashboard/src/app/settings/operadores/page.test.tsx
autonomous: false
requirements: [UM-01, UM-02, UM-03, UM-04, UM-05, UM-06, UM-07, UM-08, UM-09, UM-10]
user_setup: []

must_haves:
  truths:
    - "Vitest harness boots and runs the new RED test stubs (they fail because implementation is absent — expected)"
    - "nodemailer install is gated behind an explicit human-verify checkpoint (package is [ASSUMED] — slopcheck unavailable)"
    - "shadcn dialog, dropdown-menu, alert-dialog blocks exist in components/ui"
  artifacts:
    - path: "dashboard/src/lib/admin-actions.test.ts"
      provides: "RED stubs for UM-04/05/06/07 owner-gate + audit-write + D-09 self-pw-not-logged"
    - path: "dashboard/src/lib/seed-owner.test.ts"
      provides: "RED stub for UM-03 owner-seed idempotency"
    - path: "dashboard/src/app/settings/page.test.tsx"
      provides: "RED stub for UM-01 self-service change-password"
    - path: "dashboard/src/app/settings/operadores/page.test.tsx"
      provides: "RED stub for UM-10 real-role badge + owner-gate"
    - path: "dashboard/src/components/ui/dialog.tsx"
      provides: "provision modal primitive"
    - path: "dashboard/src/components/ui/dropdown-menu.tsx"
      provides: "row-action menu primitive"
    - path: "dashboard/src/components/ui/alert-dialog.tsx"
      provides: "destructive-confirm primitive"
  key_links:
    - from: "dashboard/src/lib/admin-actions.test.ts"
      to: "dashboard/src/lib/auth.test.ts"
      via: "memoryAdapter buildTestAuth harness reuse"
      pattern: "memoryAdapter"
---

<objective>
Wave 0 scaffolding for Phase 13. Establish the failing-test surface for all
ten UM-* requirements (RED state), install the three net-new shadcn primitives
the UI waves consume, and gate the single net-new npm dependency (nodemailer,
tagged [ASSUMED]) behind a blocking human-verify checkpoint before install.

Purpose: per Nyquist, every downstream task verifies against a test that exists.
This plan creates those tests as RED stubs reusing the established
`auth.test.ts` memoryAdapter harness, so Waves 1-3 turn them GREEN.
Output: 4 test files (RED), 3 shadcn UI blocks, nodemailer added to package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-RESEARCH.md
@.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-PATTERNS.md
@.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-VALIDATION.md
@.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-UI-SPEC.md

<interfaces>
<!-- Test harness the new stubs reuse. Extracted from codebase 2026-06-15. -->
From dashboard/src/lib/auth.test.ts:
  buildTestAuth() boots a real betterAuth({...}) over memoryAdapter mirroring auth.ts.
  Helpers: parseTotpSecret, createOTP (L164-169). CR-01 test block at L430-494.
  Calling convention: auth.api.<op>({ body, headers }) — headers MANDATORY.
From dashboard/src/lib/db.ts:
  getDb(): DrizzleClient (L63-65); export { schema } (L76); drizzle(pool,{schema}) (L43).
From dashboard/src/app/login/page.test.tsx:
  Component-test pattern mocking authClient (mirror for settings/page.test.tsx).
Vitest: include "src/**/*.test.{ts,tsx}"; setup src/test-setup.ts; jsdom; globals:true.
Run command: `cd dashboard && bun run test` (script "test": "vitest run").
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Gate + install nodemailer ([ASSUMED] package legitimacy)</name>
  <files>dashboard/package.json</files>
  <read_first>
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-RESEARCH.md §Package Legitimacy Audit (nodemailer [ASSUMED]; slopcheck unavailable; no postinstall verified; v9.0.0 named in CONTEXT.md D-04)
    - dashboard/package.json (confirm nodemailer absent before install)
  </read_first>
  <what-built>nodemailer is the only net-new external npm package this phase (Brevo SMTP transport for D-04 invite/reset links). RESEARCH tagged it [ASSUMED] because slopcheck was unavailable at research time. It is the de-facto Node email library named explicitly in CLAUDE.md §Brevo and CONTEXT.md D-04, with no postinstall script (verified). T-13-SC in the threat model requires a blocking human verification before install.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/nodemailer — confirm publisher (nodemailer org), ~15yr age, very-high downloads, repo github.com/nodemailer/nodemailer.
    2. Confirm latest is 9.0.0 and there is no install/postinstall script.
    3. Approve, then run: `cd dashboard && bun add nodemailer`.
    4. Check whether nodemailer 9 ships bundled types: `ls node_modules/nodemailer/lib/*.d.ts 2>/dev/null`. Only if ABSENT run `cd dashboard && bun add -d @types/nodemailer`.
  </how-to-verify>
  <action>Pause for blocking-human verification of the [ASSUMED] nodemailer package per the how-to-verify steps, then `cd dashboard && bun add nodemailer` (+ `bun add -d @types/nodemailer` only if nodemailer 9 lacks bundled types). [ASSUMED] packages are never auto-approved; record the legitimacy decision in plan evidence.</action>
  <verify>
    <automated>cd dashboard && grep -q '"nodemailer"' package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/package.json dependencies contains "nodemailer" at ^9
    - No [SLOP]/[SUS] verdict recorded; install decision noted in plan evidence
    - @types/nodemailer present ONLY if nodemailer lacks bundled .d.ts
  </acceptance_criteria>
  <done>nodemailer present in dashboard/package.json after human-approved install; legitimacy decision recorded.</done>
  <resume-signal>Type "approved" after npmjs verification + bun add completes, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install shadcn dialog + dropdown-menu + alert-dialog blocks</name>
  <files>dashboard/src/components/ui/dialog.tsx, dashboard/src/components/ui/dropdown-menu.tsx, dashboard/src/components/ui/alert-dialog.tsx</files>
  <read_first>
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-UI-SPEC.md §Net-new shadcn blocks + §Registry Safety (official registry only; components.json registries:{})
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-PATTERNS.md §components/ui (npx shadcn add)
    - dashboard/components.json (confirm style radix-nova, registries {})
  </read_first>
  <action>From the official shadcn registry only, run `cd dashboard && npx shadcn add dialog dropdown-menu alert-dialog`. These map to UI-SPEC states 5 (provision modal = dialog), 6 (row menu = dropdown-menu), 7 (destructive confirm = alert-dialog). Do NOT install `form` or `label` (UI-SPEC: plain `<form>`+`<label>` precedent from 2fa/enroll). Record the slopcheck/install decision in plan evidence per UI-SPEC §Registry Safety. These are copied source, not npm installs.</action>
  <verify>
    <automated>test -f dashboard/src/components/ui/dialog.tsx && test -f dashboard/src/components/ui/dropdown-menu.tsx && test -f dashboard/src/components/ui/alert-dialog.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist under dashboard/src/components/ui/
    - No form.tsx or label.tsx added
    - components.json registries remains {} (no third-party registry added)
  </acceptance_criteria>
  <done>dialog.tsx, dropdown-menu.tsx, alert-dialog.tsx present; form/label NOT installed.</done>
</task>

<task type="auto">
  <name>Task 3: Write RED test stubs for UM-01..UM-10 (reuse memoryAdapter harness)</name>
  <files>dashboard/src/lib/admin-actions.test.ts, dashboard/src/lib/seed-owner.test.ts, dashboard/src/app/settings/page.test.tsx, dashboard/src/app/settings/operadores/page.test.tsx</files>
  <read_first>
    - dashboard/src/lib/auth.test.ts (buildTestAuth + memoryAdapter harness L46-145; parseTotpSecret/createOTP L164-169; CR-01 test L430-494; auth.api.* headers convention L240-258, L352-356)
    - dashboard/src/app/login/page.test.tsx (authClient mock pattern for component test)
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-VALIDATION.md §Per-Task Verification Map (UM-01..UM-10 commands)
    - .planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-RESEARCH.md §Validation Architecture (test→req map)
  </read_first>
  <action>Create four RED test files describing expected behavior before implementation exists. (a) admin-actions.test.ts: reuse buildTestAuth/memoryAdapter; assert non-owner session → requireOwner throws "Ação restrita ao owner do dashboard." (owner-gate); inviteOperator non-@ifixtelecom rejected + sendResetPassword fires on a MOCKED mailer transport (vi.mock the lib/email mailer) (UM-04); removeOperator revokes+removes (UM-05); resetOperatorPassword + revokeUserSessions (UM-06); resetOperator2FA clears two_factor + sets twoFactorEnabled=false + CR-01 enable allowed after (UM-07); audit assertion: each admin op writes one admin_audit_log row AND self-service change-password writes ZERO (D-09/UM-08). (b) seed-owner.test.ts: earliest user → role='owner', rest → 'operator', idempotent re-run no-op when an owner exists (UM-03). (c) settings/page.test.tsx: mirror login/page.test.tsx mocking authClient.changePassword — wrong currentPassword surfaces inline error code; success clears fields (UM-01). (d) settings/operadores/page.test.tsx: roster renders role badge from real role column (owner=warning, operator=neutral), non-owner viewer sees no + Provisionar / no ··· (UM-10). All four are EXPECTED TO FAIL (RED) until Waves 1-3. Mark unimplemented assertions with vitest so the suite reports red, not error — but do NOT use `.skip`/`.todo` that hide them from the run.</action>
  <verify>
    <automated>cd dashboard && bun run test src/lib/admin-actions.test.ts src/lib/seed-owner.test.ts src/app/settings/page.test.tsx src/app/settings/operadores/page.test.tsx 2>&1 | grep -Eq 'fail|FAIL|✗|×' && echo "RED-CONFIRMED"</automated>
  </verify>
  <acceptance_criteria>
    - Four test files exist and are collected by vitest (no `.skip`/`.todo` masking)
    - Suite runs RED (assertions fail because implementation absent) — NOT a collection/import error
    - admin-actions.test.ts mocks the lib/email mailer transport (no real SMTP)
    - Each UM-01..UM-10 has at least one assertion across the four files
  </acceptance_criteria>
  <done>4 RED test files map UM-01..UM-10; `bun run test` collects them and reports failures (not import errors).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dashboard build | nodemailer install introduces external code into the auth surface |
| shadcn registry → components/ui | copied UI source enters the tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | nodemailer npm install | mitigate | blocking-human checkpoint (Task 1) verifies npmjs.com/package/nodemailer before `bun add`; [ASSUMED] never auto-approved; no postinstall (verified) |
| T-13-REG | Tampering | shadcn add (dialog/dropdown/alert-dialog) | accept | official shadcn registry only; components.json registries:{} (no third-party); copied source reviewed in diff |
</threat_model>

<verification>
- `cd dashboard && bun run test` collects 4 new files and reports them RED (failing assertions, not import errors).
- Three shadcn blocks present; form/label NOT installed.
- nodemailer in package.json at ^9; @types/nodemailer only if not bundled.
</verification>

<success_criteria>
- RED test surface exists for all 10 UM requirements.
- shadcn primitives ready for UI waves.
- nodemailer install gated + completed via human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/13-dashboard-user-management-gest-o-de-operadores-owner-only-se/13-01-SUMMARY.md` when done.
</output>
