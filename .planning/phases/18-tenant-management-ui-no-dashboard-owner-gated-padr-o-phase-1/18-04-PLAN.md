---
phase: 18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1
plan: 04
type: execute
wave: 4
depends_on: [18-03]
files_modified:
  - dashboard/src/app/(dashboard)/tenants/gerenciar/page.tsx
  - dashboard/src/app/(dashboard)/tenants/gerenciar/tenant-controls.tsx
  - dashboard/src/components/app-sidebar.tsx
autonomous: false
requirements: [TEN-UI-10, TEN-UI-11]

must_haves:
  truths:
    - "Owner vê a página /tenants/gerenciar: lista tenants, cria tenant (slug+name), gera key mostrando o raw UMA vez, revoga key"
    - "O seletor data-class (normal|sensitive) pertence ao fluxo de GERAR KEY, não ao criar tenant"
    - "Revogar key ativa exige confirm de 1 clique com string de IMPACTO (o que quebra), sem type-to-confirm"
    - "Operator vê os mesmos dados read-only: nenhum controle de mutação renderizado"
    - "Link 'Tenants (gestão)' na sidebar aponta p/ /tenants/gerenciar (rota nova, não sobrescreve /tenants de métricas)"
  artifacts:
    - path: dashboard/src/app/(dashboard)/tenants/gerenciar/page.tsx
      provides: "RSC: getViewerRole + fetchTenantsServer; passa isOwner + tenants ao island"
      min_lines: 40
    - path: dashboard/src/app/(dashboard)/tenants/gerenciar/tenant-controls.tsx
      provides: "client island: criar tenant, gerar key (raw 1×), revogar (confirm impacto); gate cosmético isOwner"
      min_lines: 120
  key_links:
    - from: tenant-controls.tsx
      to: createTenant/createTenantKey/revokeKey
      via: "@/lib/admin-actions server actions"
      pattern: "createTenant|createTenantKey|revokeKey"
    - from: app-sidebar.tsx
      to: /tenants/gerenciar
      via: "novo NavItem"
      pattern: "/tenants/gerenciar"
---

<objective>
A página de gestão de tenants no dashboard — o produto visível da fase. RSC owner-aware
(`getViewerRole` server-side + `fetchTenantsServer`) que passa `isOwner` + a lista de tenants
para um client island; o island lista tenants, cria tenant (slug+name), gera API key exibindo
o raw UMA vez (copiar-e-esquecer), e revoga key com confirm de impacto. O seletor data-class
(normal|sensitive) vive no fluxo de GERAR KEY (a data_class é por-key, RES-08), NÃO no criar
tenant. Owner edita; operator vê read-only (gate cosmético — as actions re-checam requireOwner
server-side).

Rota NOVA `/tenants/gerenciar` — a `/tenants` existente é a página de MÉTRICAS (não sobrescrever).

Purpose: eliminar a dor "criar tenant hoje é só CLI" (pedido 2026-07-01). Output: página +
island + nav. autonomous:false — a validação E2E owner/operator do fluxo é human-verify (o
gate visual e o fluxo autenticado só são observáveis num dashboard rodando, padrão Phase 17).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1/18-RESEARCH.md

<interfaces>
<!-- Do Plan 18-03 (server actions) + 18-02 (reads) -->
import { createTenant, createTenantKey, revokeKey } from "@/lib/admin-actions"
  // createTenant({slug,name}): Promise<void>
  // createTenantKey({slug,dataClass}): Promise<{id?,key_prefix?,data_class?,key?}>   // key = raw, mostrar 1×
  // revokeKey({keyId}): Promise<void>
import { fetchTenantsServer } from "@/lib/gateway-server"   // RSC read: TenantRow[]
import { fetchTenants, fetchTenantKeys, type TenantRow, type TenantKeyRow } from "@/lib/gateway"  // client-side reads via proxy
import { getViewerRole } from "@/lib/viewer"                // server-side role

<!-- Padrões-âncora existentes -->
// page RSC owner-aware: src/app/(dashboard)/operacao/config/page.tsx (getViewerRole + fetchXServer + isOwner → island)
// client island + AlertDialog + toast + action call: src/app/(dashboard)/settings/operadores/operator-controls.tsx
// sidebar NavItem list: src/components/app-sidebar.tsx:43-49 ({href,label,icon}); nav match = href mais específico
// UI primitives disponíveis: alert-dialog, dialog, table, select, input, button, badge, card, dropdown-menu, skeleton, sonner(toast)
</interfaces>

Arquivos-âncora (LER):
@dashboard/src/app/(dashboard)/operacao/config/page.tsx
@dashboard/src/app/(dashboard)/settings/operadores/operator-controls.tsx
@dashboard/src/components/app-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RSC /tenants/gerenciar/page.tsx + nav na sidebar</name>
  <files>dashboard/src/app/(dashboard)/tenants/gerenciar/page.tsx, dashboard/src/components/app-sidebar.tsx</files>
  <read_first>
    - dashboard/src/app/(dashboard)/operacao/config/page.tsx (padrão RSC: `export const dynamic="force-dynamic"`, getViewerRole, fetchXServer, isOwner → island)
    - dashboard/src/components/app-sidebar.tsx:43-82 (NAV_ITEMS + lógica de match do href mais específico)
  </read_first>
  <action>
    Criar `tenants/gerenciar/page.tsx` como RSC (`export const dynamic = "force-dynamic"`): `const role = await getViewerRole(); const isOwner = role === "owner"; const tenants = await fetchTenantsServer();` (try/catch → estado de erro pt-BR com card, igual as outras páginas). Renderizar um `<Card>` de título + o island `<TenantControls isOwner={isOwner} initialTenants={tenants} />`. NÃO buscar keys aqui (o island busca sob demanda ao expandir um tenant).
    Em `app-sidebar.tsx`: adicionar UM NavItem `{ href: "/tenants/gerenciar", label: "Tenants (gestão)", icon: <um ícone lucide já importado ou importar, ex. UserCog/KeyRound> }` DEPOIS do item `/tenants`. A lógica de match existente (href mais específico) já acende "Tenants (gestão)" em /tenants/gerenciar sem tocar no item /tenants. Não remover nem alterar o item /tenants (métricas).
  </action>
  <acceptance_criteria>
    - Arquivo `tenants/gerenciar/page.tsx` existe, é RSC (sem "use client"), chama getViewerRole + fetchTenantsServer, passa isOwner + tenants ao island.
    - `grep -q '/tenants/gerenciar' dashboard/src/components/app-sidebar.tsx` e o item `/tenants` (métricas) permanece intacto (`grep -c '"/tenants"' ...` continua ≥1).
    - `cd dashboard && bunx tsc --noEmit` (ou o build) não acusa erro nos arquivos novos.
  </acceptance_criteria>
  <verify><automated>cd dashboard && bunx tsc --noEmit 2>&1 | grep -E 'tenants/gerenciar|app-sidebar' | head -5; echo "tsc-done"</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Client island tenant-controls.tsx (criar / gerar key raw-1× / revogar confirm-impacto)</name>
  <files>dashboard/src/app/(dashboard)/tenants/gerenciar/tenant-controls.tsx</files>
  <read_first>
    - dashboard/src/app/(dashboard)/settings/operadores/operator-controls.tsx (client island completo: useState, toast, action call, AlertDialog confirm, gate cosmético)
    - dashboard/src/lib/gateway.ts (fetchTenants, fetchTenantKeys, TenantRow, TenantKeyRow)
  </read_first>
  <action>
    Criar `tenant-controls.tsx` (`"use client"`) recebendo `{ isOwner: boolean; initialTenants: TenantRow[] }`.
    Superfícies:
    (1) TABELA de tenants (`table`): slug, name, created_at; cada linha expansível → ao expandir, `fetchTenantKeys(slug)` (useState + loading skeleton) lista as keys ({key_prefix, status, data_class badge, created_at, last_used_at}).
    (2) CRIAR TENANT (só se isOwner): botão "Novo tenant" → `dialog` com inputs Slug + Nome → chama `createTenant({slug,name})`; on-success toast + refetch da lista (`fetchTenants()`); erro → toast com a mensagem do throw. Validação client leve (slug url-safe) espelhando a server (a server é a autoritativa).
    (3) GERAR KEY (só se isOwner; por linha de tenant): botão "Gerar API key" → `dialog` com um `select` de DATA-CLASS (normal|sensitive; default normal) → chama `createTenantKey({slug,dataClass})`; on-success, exibir o `key` (raw) UMA vez num bloco copiável (`Input readOnly` + botão copiar) com aviso "Copie agora — não será exibida de novo"; NÃO guardar o raw em estado persistente além do dialog; ao fechar o dialog, o raw some. Refetch das keys do tenant.
    (4) REVOGAR KEY (só se isOwner; por key ativa): botão/ação → `alert-dialog` com STRING DE IMPACTO específica (ex. "Revogar a key {key_prefix}… do tenant {slug}? Qualquer app usando essa key passa a receber 401 imediatamente e para de funcionar."), confirm de 1 clique (SEM type-to-confirm; default-focus Cancelar, confirm --destructive) → `revokeKey({keyId})`; on-success toast + refetch.
    Gate cosmético: quando `!isOwner`, renderizar a tabela + keys read-only, SEM os botões (2)(3)(4) — as actions server re-checam requireOwner de qualquer forma.
    Copy em pt-BR. Reusar AlertDialog/Dialog/Table/Select/Input/Button/Badge/toast já disponíveis.
  </action>
  <acceptance_criteria>
    - `tenant-controls.tsx` tem `"use client"`, importa createTenant/createTenantKey/revokeKey de "@/lib/admin-actions".
    - O select data-class aparece SOMENTE no dialog de gerar key (grep: o `select`/opções normal|sensitive está no bloco de key, não no de criar tenant).
    - O raw key é exibido a partir do retorno de `createTenantKey` e NÃO é re-buscado/persistido (não há fetch que retorne raw; fetchTenantKeys nunca traz `key`).
    - Revoke usa `alert-dialog` com texto de impacto e SEM input de digitação (`! grep -qi 'digite\|type-to-confirm\|confirmação por texto' ...`).
    - Quando `!isOwner`, os botões Novo tenant / Gerar key / Revogar não são renderizados (condicional em isOwner).
    - `bunx tsc --noEmit` sem erro no arquivo.
  </acceptance_criteria>
  <verify><automated>cd dashboard && bunx tsc --noEmit 2>&1 | grep -E 'tenant-controls' | head -5; echo "tsc-done"</automated></verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify E2E owner/operator + audit sem raw</name>
  <action>Pausar para verificação humana bloqueante do fluxo E2E no dashboard rodando (owner cria/gera-raw-1×/revoga-com-impacto/data-class + operator read-only + audit sem raw), conforme os passos de how-to-verify. Não há automação de UI autenticada — o gate visual owner/operator e o fluxo de raw-1× só são observáveis num dashboard live (mesmo gate manual da Phase 17).</action>
  <what-built>
    Página /tenants/gerenciar owner-gated: lista tenants, criar tenant (slug+name), gerar API key
    (raw exibido 1×), revogar key (confirm de impacto), seletor data-class no fluxo de gerar key,
    e link "Tenants (gestão)" na sidebar. Todas as mutações passam pelas server actions
    owner-gated + auditadas (Plan 18-03) contra o gateway consolidado (worker-vm).
  </what-built>
  <how-to-verify>
    No dashboard rodando (worker-vm stack 40, ou dashboard-dev):
    1. Login como OWNER → abrir "Tenants (gestão)" na sidebar → ver a lista de tenants.
    2. Criar um tenant de teste (ex. slug `uat-ten18`, nome "UAT Phase 18") → aparece na lista; tentar criar de novo com o mesmo slug → erro "slug já existe" (409).
    3. Nesse tenant, "Gerar API key" com data-class `normal` → a key raw (`ifix_sk_…`) é exibida UMA vez num bloco copiável; fechar o dialog → a raw some; a key aparece na lista do tenant (só prefixo, status active).
    4. Revogar essa key → confirm mostra a string de impacto (o que quebra) → confirmar → a key some da lista de ativas.
    5. Gerar uma key data-class `sensitive` e confirmar que a lista mostra o badge `sensitive`.
    6. Logout → login como OPERATOR → abrir "Tenants (gestão)" → ver os MESMOS dados read-only, SEM os botões Novo tenant / Gerar key / Revogar.
    7. Confirmar no gateway que houve audit: `docker exec $(ssh worker-vm 'docker ps -q -f name=ai-gateway-prod_gateway|head -1')` … OU checar a página de audit do dashboard: 1 row por tenant.create/key.create/key.revoke, metadata SEM o raw.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que falhou (ex.: raw persistiu, operator viu botões, 409 não disparou)</resume-signal>
  <verify><human-check>Fluxo E2E owner/operator validado no dashboard live conforme how-to-verify</human-check></verify>
  <done>Owner cria tenant + gera key (raw 1×) + revoga (confirm impacto) + data-class por-key; operator vê read-only; audit tem 1 row/ação sem raw — tudo confirmado no dashboard rodando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (owner/operator) → server actions | gate cosmético no island; requireOwner server-side é a autoridade |
| UI → gateway | sempre via server action / proxy; admin key nunca no browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Elevation of Privilege | operator vê/usa controles de mutação | mitigate | Botões renderizados só se isOwner (cosmético); as actions re-checam requireOwner (Plan 18-03) — controle escondido-mas-chamável ainda é barrado server-side. Checkpoint passo 6 valida operator read-only. |
| T-18-04 | Info Disclosure | raw key persistido/re-exibido | mitigate | Raw vem só do retorno de createTenantKey, exibido 1× no dialog, não persistido nem re-buscável (fetchTenantKeys nunca traz `key`). Checkpoint passo 3 valida "some ao fechar". |
| T-18-09 | Tampering | revoke acidental de key ativa | mitigate | alert-dialog com string de impacto explícita (o que quebra), confirm 1-clique sem type-to-confirm (padrão POD-CFG-12); default-focus Cancelar. |
| T-18-03 | Info Disclosure | admin key no bundle client | mitigate | O island só chama server actions + fetch relativo do proxy; nenhuma referência a GATEWAY_ADMIN_KEY (leak-guard do Plan 18-02 continua verde). |
| T-18-SC | Tampering | npm/pip installs | accept | Zero pacote novo — só componentes shadcn/lucide já instalados. |
</threat_model>

<verification>
- `bunx tsc --noEmit` sem erro nos arquivos novos.
- Rota nova /tenants/gerenciar não toca a /tenants de métricas; nav adicionada.
- Checkpoint human-verify: fluxo owner (criar/gerar-raw-1×/revogar-impacto/data-class) + operator read-only + audit sem raw.
</verification>

<success_criteria>
Owner cria tenant e gera/revoga API key pelo dashboard (fim do "só CLI"), com o raw exibido uma
única vez, confirm de impacto na revogação, data-class por-key, e operator estritamente
read-only — validado E2E no checkpoint.
</success_criteria>

<output>
Create `.planning/phases/18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1/18-04-SUMMARY.md` when done
</output>
