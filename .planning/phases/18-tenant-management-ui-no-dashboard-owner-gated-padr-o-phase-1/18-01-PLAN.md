---
phase: 18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/internal/admin/tenants_admin_http.go
  - gateway/internal/admin/tenants_admin_http_test.go
  - gateway/internal/admin/keys_admin_http.go
  - gateway/internal/admin/keys_admin_http_test.go
  - gateway/cmd/gateway/main.go
autonomous: true
requirements: [TEN-UI-01, TEN-UI-02, TEN-UI-03, TEN-UI-04, TEN-UI-05]

must_haves:
  truths:
    - "GET /admin/tenants (X-Admin-Key) retorna todos os tenants, inclusive sem tráfego"
    - "POST /admin/tenants cria tenant (slug+name); slug duplicado → 409"
    - "GET /admin/tenants/{slug}/keys lista keys ativas SEM key_hash e SEM raw"
    - "POST /admin/tenants/{slug}/keys gera key, retorna o raw UMA vez no JSON, nunca key_hash; data_class ∈ {normal,sensitive}"
    - "POST /admin/keys/{id}/revoke é idempotente (2ª chamada = 200 no-op)"
    - "Todas as 5 rotas montadas dentro do adminRouter (X-Admin-Key exigido; sem key → 401)"
  artifacts:
    - path: gateway/internal/admin/tenants_admin_http.go
      provides: "handlers GET/POST /admin/tenants (list + create com 409 em dup)"
      min_lines: 90
    - path: gateway/internal/admin/keys_admin_http.go
      provides: "handlers GET/POST /admin/tenants/{slug}/keys + POST /admin/keys/{id}/revoke"
      min_lines: 130
    - path: gateway/internal/admin/tenants_admin_http_test.go
      provides: "testes fake-queries: list, create, dup→409"
      min_lines: 60
    - path: gateway/internal/admin/keys_admin_http_test.go
      provides: "testes: create sem key_hash no JSON + raw presente, data_class inválida→400, revoke idempotente, list sem hash"
      min_lines: 90
  key_links:
    - from: gateway/cmd/gateway/main.go
      to: adminRouter
      via: "adminRouter.Method(GET/POST, /tenants ...) + /tenants/{slug}/keys + /keys/{id}/revoke"
      pattern: "adminRouter\\.Method\\(http\\.Method(Get|Post), \"/tenants"
    - from: keys_admin_http.go create handler
      to: auth.GenerateAPIKey + q.InsertAPIKey
      via: "raw key gerado + inserido; response omite key_hash"
      pattern: "GenerateAPIKey|InsertAPIKey"
---

<objective>
Construir os handlers HTTP admin de tenant/key no gateway Go — o achado que dimensiona a
Phase 18: a admin API `/admin/*` HOJE não expõe nenhum CRUD de tenant/key (só o gatewayctl
fala direto no Postgres). Sem estes handlers, o padrão herdado da Phase 17 (dashboard →
proxy server-only → admin API) não tem o que consumir.

Os handlers são FINOS sobre queries sqlc que JÁ EXISTEM (CreateTenant, ListTenants,
GetTenantBySlug, InsertAPIKey, RevokeAPIKey, ListActiveKeys*WithMeta) + `auth.GenerateAPIKey`.
Nenhuma migration nova. Espelham 1:1 o template `config_write.go`/`config_read.go` da Phase
17-04 (interface de queries isolada, dual constructor prod/teste, envelope OpenAI + métrica
`obs.GatewayAdminRequests`).

Purpose: dar ao dashboard a superfície HTTP owner-gated de gestão de tenant/key sem shell-out
ao gatewayctl (invariante Phase 17: o dashboard fala HTTP com a admin API, nunca docker socket).
Output: 5 endpoints admin montados no adminRouter + testes com fake-queries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1/18-RESEARCH.md

<interfaces>
<!-- Queries sqlc JÁ EXISTENTES (gateway/internal/db/gen) — usar verbatim, NÃO criar migration -->

// gateway/db/queries/admin.sql (gerado em gen)
q.CreateTenant(ctx, gen.CreateTenantParams{Slug, Name}) (row{ID uuid.UUID, Slug, Name string, CreatedAt, UpdatedAt}, error)  // 23505 = slug dup
q.GetTenantBySlug(ctx, slug string) (gen.GetTenantBySlugRow{ID uuid.UUID, Slug, Name, ...}, error)  // pgx.ErrNoRows se não existe
q.ListTenants(ctx) ([]gen.ListTenantsRow{ID, Slug, Name, CreatedAt, UpdatedAt}, error)  // ORDER BY created_at DESC
q.InsertAPIKey(ctx, gen.InsertAPIKeyParams{TenantID uuid.UUID, KeyHash, KeyLookupHash, KeyPrefix, DataClass string}) (row, error)
  // row RETORNA key_hash + key_lookup_hash — NUNCA serializar esses dois no JSON de resposta
q.RevokeAPIKey(ctx, id uuid.UUID) error  // :exec — UPDATE ... WHERE id=$1 AND status='active' (idempotente: 0 rows na 2ª vez, sem erro)

// gateway/db/queries/auth.sql — projeção operator-safe SEM hash
q.ListActiveKeysAllWithMeta(ctx) ([]gen.ListActiveKeysAllWithMetaRow{ID, TenantID, TenantSlug, KeyPrefix, Status, DataClass, CreatedAt, LastUsedAt}, error)
q.ListActiveKeysByTenantWithMeta(ctx, tenantID uuid.UUID) ([]gen.ListActiveKeysByTenantWithMetaRow{...mesmas colunas...}, error)

// gateway/internal/auth/argon2.go:46
auth.GenerateAPIKey() (raw, hash, lookupHash, prefix string, err error)  // raw aparece 1× — copiar agora

// gateway/internal/httpx/envelope.go:16
httpx.WriteOpenAIError(w, status int, errType, code, msg string)

// gateway/internal/obs — métrica por rota+classe
obs.GatewayAdminRequests.WithLabelValues(route, "2xx"|"4xx"|"5xx").Inc()

// chi v5 (já importado em main.go): chi.URLParam(r, "slug") / chi.URLParam(r, "id")
</interfaces>

Padrões-âncora a espelhar (LER):
@gateway/internal/admin/config_write.go   # interface de queries isolada + dual constructor + envelope/métrica
@gateway/internal/admin/config_read.go    # struct de response typed + query-interface + dual constructor
@gateway/internal/admin/config_write_test.go  # fakeQueries que conta chamadas (httptest, sem DB)
@gateway/cmd/gatewayctl/key.go            # a lógica EXATA a portar (create raw-once, list sem hash, revoke)
@gateway/cmd/gateway/main.go              # bloco adminRouter (~1568-1620) + proxy struct fields (~119-126) + constructor wiring (~1326-1363)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handlers admin de tenant (GET+POST /admin/tenants)</name>
  <files>gateway/internal/admin/tenants_admin_http.go, gateway/internal/admin/tenants_admin_http_test.go</files>
  <read_first>
    - gateway/internal/admin/config_read.go (struct response typed + interface de queries + dual constructor NewX/newXWithQueries)
    - gateway/internal/admin/config_write.go:53-141,414-442 (decode body + envelope bad()/finish() + obs.GatewayAdminRequests)
    - gateway/cmd/gatewayctl/tenant.go:55-90 (runTenantCreate: CreateTenant + trata pgconn.PgError.Code=="23505" → dup)
  </read_first>
  <action>
    Criar `tenants_admin_http.go` (package admin) com DOIS handlers espelhando os templates da Phase 17-04.
    (1) `TenantListHandler` sobre interface `tenantAdminQueries{ ListTenants(ctx) ([]gen.ListTenantsRow,error); CreateTenant(ctx, gen.CreateTenantParams) (gen.CreateTenantRow,error) }` — dual constructor `NewTenantAdminHandler(*gen.Queries, *slog.Logger)` (prod) + `newTenantAdminHandlerWithQueries(fake, log)` (teste). GET serializa `[]{id, slug, name, created_at, updated_at}` typed (NÃO devolver structs gen cru — declarar struct de response local com json tags snake_case).
    (2) POST: decode body `{slug, name}`; validar server-side `strings.TrimSpace(slug)!="" && name!=""` senão `httpx.WriteOpenAIError(400, "invalid_request_error", "invalid_body", ...)`; chamar CreateTenant; se `errors.As(err,&pgconn.PgError{})` e `Code=="23505"` → `WriteOpenAIError(409, "invalid_request_error", "tenant_exists", "slug já existe")`; sucesso → 201 `{id, slug, name}`.
    Toda saída incrementa `obs.GatewayAdminRequests.WithLabelValues("/admin/tenants", class).Inc()` (2xx/4xx/5xx), igual config_write finish/bad.
    Um único handler struct pode servir os dois métodos (dois ServeHTTP-like ou dois http.Handler methods) — seguir a forma que config_read/config_write usam. SEM SQL dinâmico. Comentários NÃO devem conter o token literal "PATCH" nem "restart" (grep-clean, Pitfall 7 da RESEARCH).
    Teste `tenants_admin_http_test.go`: fake que implementa `tenantAdminQueries` e conta chamadas (padrão config_write_test.go fakeWriteQueries). Cobrir: GET lista N tenants no JSON; POST slug+name válido → 201 com id; POST slug vazio → 400 sem chamar CreateTenant; POST com fake retornando `&pgconn.PgError{Code:"23505"}` → 409.
  </action>
  <acceptance_criteria>
    - `go test ./internal/admin/ -run 'TestTenant'` passa.
    - GET handler responde JSON array com campos snake_case {id, slug, name, created_at, updated_at}; nunca structs gen cru.
    - POST slug vazio → HTTP 400, e o fake registra ZERO chamadas a CreateTenant (validação antes da query).
    - POST com fake sinalizando 23505 → HTTP 409 com code "tenant_exists".
    - `grep -n 'PATCH\|restart' gateway/internal/admin/tenants_admin_http.go` retorna vazio (comentários grep-clean).
  </acceptance_criteria>
  <verify><automated>cd gateway && go test ./internal/admin/ -run 'TestTenant' -count=1</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Handlers admin de key (list/create raw-once/revoke)</name>
  <files>gateway/internal/admin/keys_admin_http.go, gateway/internal/admin/keys_admin_http_test.go</files>
  <read_first>
    - gateway/cmd/gatewayctl/key.go:79-108 (create: GenerateAPIKey → InsertAPIKey → imprime raw 1×, NUNCA loga raw, NUNCA imprime hash)
    - gateway/cmd/gatewayctl/key.go:171-230 (list: projeção operator-safe, NUNCA key_hash; enumString para ENUM sqlc)
    - gateway/cmd/gatewayctl/key.go:110-164 (revoke idempotente)
    - gateway/internal/admin/config_write.go:110-141 (decode body writeRequest + switch)
  </read_first>
  <action>
    Criar `keys_admin_http.go` (package admin) com interface isolada `keysAdminQueries{ GetTenantBySlug(ctx,string)(gen.GetTenantBySlugRow,error); ListActiveKeysByTenantWithMeta(ctx,uuid.UUID)([]gen.ListActiveKeysByTenantWithMetaRow,error); InsertAPIKey(ctx,gen.InsertAPIKeyParams)(gen.InsertAPIKeyRow,error); RevokeAPIKey(ctx,uuid.UUID)error }` + dual constructor `NewKeysAdminHandler(*gen.Queries,*slog.Logger)` / `newKeysAdminHandlerWithQueries(fake,log)`.
    (a) LIST `GET /admin/tenants/{slug}/keys`: `slug := chi.URLParam(r,"slug")`; GetTenantBySlug → se pgx.ErrNoRows → 404 `tenant_not_found`; ListActiveKeysByTenantWithMeta(tenant.ID); serializar `[]{id, tenant_slug, key_prefix, status, data_class, created_at, last_used_at}` — declarar struct de response local; NUNCA incluir key_hash nem key_lookup_hash. Coerção de ENUM (status/data_class vêm como interface{}) via helper string igual `enumString` do key.go.
    (b) CREATE `POST /admin/tenants/{slug}/keys`: decode body `{data_class}` (default "normal" se ausente); validar `data_class ∈ {"normal","sensitive"}` senão 400 `invalid_data_class`; GetTenantBySlug (404 se não existe); `raw,hash,lookupHash,prefix,_ := auth.GenerateAPIKey()`; InsertAPIKey{TenantID, KeyHash:hash, KeyLookupHash:lookupHash, KeyPrefix:prefix, DataClass:data_class}; resposta 201 struct EXPLÍCITO `{id, key_prefix, tenant, data_class, key: raw}` — o campo `key` (raw) aparece SÓ AQUI, UMA vez. NUNCA serializar `key_hash`/`key_lookup_hash`. NUNCA logar o raw (log.Info só com id/prefix/tenant/data_class, igual key.go:99-105).
    (c) REVOKE `POST /admin/keys/{id}/revoke`: `id := chi.URLParam(r,"id")`; `uuid.Parse(id)` → inválido → 400 `invalid_id`; RevokeAPIKey(uuid); 200 `{status:"revoked", id}`. Idempotente por construção (2ª chamada não erra).
    Todas as saídas incrementam `obs.GatewayAdminRequests.WithLabelValues(route, class).Inc()` com route por endpoint. Comentários grep-clean (sem "PATCH"/"restart").
    Teste `keys_admin_http_test.go` com fake contando chamadas: (1) CREATE → resposta 201 contém `"key":` (raw) e o corpo NÃO contém a substring `key_hash` nem `key_lookup_hash`; (2) CREATE data_class="invalido" → 400 e ZERO chamadas InsertAPIKey; (3) CREATE data_class ausente → usa "normal"; (4) REVOKE id inválido → 400; REVOKE id válido → 200; chamar 2× o mesmo id → ambas 200 (fake RevokeAPIKey retorna nil sempre); (5) LIST → corpo NÃO contém `key_hash`.
  </action>
  <acceptance_criteria>
    - `go test ./internal/admin/ -run 'TestKey'` passa.
    - No teste CREATE, o corpo JSON da resposta contém `"key"` (raw) e uma asserção `!strings.Contains(body,"key_hash") && !strings.Contains(body,"key_lookup_hash")` PASSA.
    - `grep -n 'key_hash\|key_lookup_hash' gateway/internal/admin/keys_admin_http.go` NÃO aparece dentro de nenhuma struct de response com json tag (só pode aparecer como campo do InsertAPIKeyParams/row de entrada, nunca no output serializado).
    - CREATE com data_class fora de {normal,sensitive} → 400 sem chamar InsertAPIKey.
    - REVOKE com UUID inválido → 400; UUID válido chamado 2× → 200 nas duas.
  </acceptance_criteria>
  <verify><automated>cd gateway && go test ./internal/admin/ -run 'TestKey' -count=1</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Montar as 5 rotas no adminRouter (main.go) + build/test</name>
  <files>gateway/cmd/gateway/main.go</files>
  <read_first>
    - gateway/cmd/gateway/main.go:115-126 (campos http.Handler no proxy struct — adminEconomyHandler, adminPrimaryConfigReadHandler)
    - gateway/cmd/gateway/main.go:1326-1363 (construção dos handlers + atribuição no struct literal)
    - gateway/cmd/gateway/main.go:1568-1620 (bloco `if px.adminVerifier != nil { adminRouter ... }` com os `adminRouter.Method(...)`)
  </read_first>
  <action>
    Adicionar ao proxy struct 2 campos `http.Handler` (ex. `adminTenantHandler`, `adminKeysHandler`) — ou mais se os handlers de tenant list/create forem structs separados; reusar o mesmo handler struct para os métodos que ele serve. Construir com `admin.NewTenantAdminHandler(gen.New(pool), log)` e `admin.NewKeysAdminHandler(gen.New(pool), log)` na mesma seção onde `adminEconomyHandler`/`adminPrimaryConfigReadHandler` são construídos (~1326-1363), e atribuir no struct literal.
    No bloco adminRouter (dentro do `if px.adminVerifier != nil`, junto dos mounts Phase 17), montar, com guarda `if px.adminTenantHandler != nil`:
      - `adminRouter.Method(http.MethodGet,  "/tenants", <tenantList>)`
      - `adminRouter.Method(http.MethodPost, "/tenants", <tenantCreate>)`
      - `adminRouter.Method(http.MethodGet,  "/tenants/{slug}/keys", <keysList>)`
      - `adminRouter.Method(http.MethodPost, "/tenants/{slug}/keys", <keysCreate>)`
      - `adminRouter.Method(http.MethodPost, "/keys/{id}/revoke", <keyRevoke>)`
    (chi já suporta path params `{slug}`/`{id}` — os handlers usam chi.URLParam.) Sem tocar em nenhum mount existente. Comentário do bloco grep-clean (sem "PATCH"/"restart").
    Rodar `go build ./...` e `go test ./internal/admin/... -count=1` e `gofmt -l` nos arquivos da fase — devem estar limpos.
  </action>
  <acceptance_criteria>
    - `cd gateway && go build ./...` exit 0.
    - `cd gateway && go test ./internal/admin/... -count=1` passa (todos os testes das tasks 1 e 2 + os pré-existentes).
    - `gofmt -l gateway/internal/admin/tenants_admin_http.go gateway/internal/admin/keys_admin_http.go gateway/cmd/gateway/main.go` retorna vazio.
    - `grep -c 'adminRouter.Method.*\"/tenants\"' gateway/cmd/gateway/main.go` ≥ 1 e as 5 rotas (`/tenants`, `/tenants/{slug}/keys` ×2, `/keys/{id}/revoke`) estão presentes.
    - Rotas montadas DENTRO do `if px.adminVerifier != nil` (portanto sob `admin.Middleware` X-Admin-Key).
  </acceptance_criteria>
  <verify><automated>cd gateway && go build ./... && go test ./internal/admin/... -count=1 && gofmt -l internal/admin/tenants_admin_http.go internal/admin/keys_admin_http.go cmd/gateway/main.go</automated></verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard/cliente → gateway `/admin/*` | X-Admin-Key atravessa aqui; entrada não-confiável (slug, name, data_class, id) |
| handler → Postgres | queries sqlc parametrizadas; ENUM validado por pgx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Spoofing | endpoint admin sem auth | mitigate | Rotas montadas DENTRO do adminRouter (`admin.Middleware` X-Admin-Key bcrypt); sem key → 401. Task 3 exige mount dentro do `if px.adminVerifier != nil`. |
| T-18-04 | Info Disclosure | raw API key / hash | mitigate | CREATE retorna `key` (raw) só no JSON dessa 1 resposta; struct de response NUNCA tem `key_hash`/`key_lookup_hash`; raw jamais logado. Teste asserta `!Contains(body,"key_hash")`. |
| T-18-05 | Tampering | SQL injection via slug/data_class | mitigate | Queries sqlc parametrizadas; sem SQL string-built; data_class validado ∈ {normal,sensitive} antes da query (whitelist); slug/id via chi.URLParam passados como parâmetros tipados. |
| T-18-07 | Info Disclosure | data_class errada não protege RES-08 | mitigate | data_class é por-KEY (api_keys.data_class, autoritativa p/ RES-08), setada no create-key; validada contra whitelist enum. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — handlers finos sobre queries + auth.GenerateAPIKey existentes; `go.mod` inalterado. |
</threat_model>

<verification>
- `cd gateway && go build ./... && go test ./internal/admin/... -count=1` verde.
- `gofmt -l` limpo nos 3 arquivos Go.
- As 5 rotas presentes no adminRouter (grep) e sob X-Admin-Key.
- Resposta de create-key nunca serializa hash (teste), raw só 1×.
</verification>

<success_criteria>
5 endpoints admin de tenant/key existem, montados sob X-Admin-Key, com testes fake-queries
verdes; nenhuma migration nova; response de create-key expõe o raw exatamente uma vez e nunca
o hash. Base pronta para o dashboard consumir via proxy (Plan 18-02).
</success_criteria>

<output>
Create `.planning/phases/18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1/18-01-SUMMARY.md` when done
</output>
