---
phase: 18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - dashboard/src/lib/gateway-admin.ts
  - dashboard/src/lib/gateway.ts
  - dashboard/src/lib/gateway-server.ts
  - dashboard/src/lib/gateway.test.ts
autonomous: true
requirements: [TEN-UI-06, TEN-UI-07]

must_haves:
  truths:
    - "gateway-admin.ts expõe um write helper server-only que aceita POST (além do PATCH), lendo X-Admin-Key só de process.env"
    - "fetchTenants() e fetchTenantKeys(slug) leem via proxy GET-only /api/gateway/*, SEM admin key no browser"
    - "fetchTenantsServer() lê tenants de um contexto RSC (URL absoluta derivada dos headers, mesma rota proxy)"
    - "leak-guard: GATEWAY_ADMIN_KEY aparece em EXATAMENTE {route.ts, gateway-admin.ts} (test verde)"
    - "stack 40 (dashboard prod) GATEWAY_BASE_URL aponta pro gateway consolidado worker-vm"
  artifacts:
    - path: dashboard/src/lib/gateway-admin.ts
      provides: "write helper server-only generalizado (método POST + PATCH); X-Admin-Key server-side"
      min_lines: 60
    - path: dashboard/src/lib/gateway.ts
      provides: "TenantRow/TenantKeyRow types + fetchTenants + fetchTenantKeys via proxyGet"
      min_lines: 30
    - path: dashboard/src/lib/gateway-server.ts
      provides: "fetchTenantsServer() RSC read via proxy absoluto"
      min_lines: 20
  key_links:
    - from: dashboard/src/lib/gateway.ts fetchTenants
      to: /api/gateway/tenants
      via: proxyGet
      pattern: "proxyGet<.*>\\(\"tenants\""
    - from: dashboard/src/lib/gateway-admin.ts
      to: process.env.GATEWAY_ADMIN_KEY
      via: "X-Admin-Key header, method param"
      pattern: "GATEWAY_ADMIN_KEY"
---

<objective>
Preparar a camada server-side do dashboard que consome os endpoints do Plan 18-01: (1)
generalizar o write helper `gateway-admin.ts` — hoje só faz PATCH — para aceitar o método
(POST para create/revoke), mantendo `import "server-only"` e o X-Admin-Key confinado a
process.env; (2) read wrappers GET-only `fetchTenants`/`fetchTenantKeys` via o proxy genérico
existente (nenhuma mudança no route.ts — ele já encaminha qualquer path GET); (3)
`fetchTenantsServer()` para a página RSC ler tenants server-side sem vazar a key; (4) manter o
leak-guard verde (a key só nos 2 arquivos server-only).

Purpose: dar às server actions (Plan 18-03) o helper de mutação + os reads, sem que a admin
key toque o browser (invariante Phase 17 T-07-24). Output: helper generalizado + wrappers +
tipos + testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1/18-RESEARCH.md
@.planning/phases/17-dashboard-pod-config-control-owner-controla-todas-as-configs/17-05-SUMMARY.md

<interfaces>
<!-- Padrões existentes a reusar/estender (dashboard/src/lib) -->

// gateway-admin.ts (HOJE só PATCH — generalizar):
export async function gatewayAdminPatch(path: string, body: unknown): Promise<void>
  // lê process.env.GATEWAY_BASE_URL + GATEWAY_ADMIN_KEY; fetch `${base}/admin/${path}` header X-Admin-Key; throw {error.message} em non-2xx

// gateway.ts — proxy GET-only helper existente:
const GATEWAY_PROXY_BASE = "/api/gateway";
async function proxyGet<T>(path: string, query?: Record<string,string>): Promise<T>  // fetch relativo /api/gateway/<path>, no-store
// tipos existentes exemplo: export interface TenantMetricRow {...}  (NÃO confundir — é métricas)

// gateway-server.ts — padrão RSC existente:
export async function fetchPodConfigServer(): Promise<PodConfigResponse>
  // reconstrói URL absoluta de headers() (x-forwarded-host/proto), reencaminha cookie de sessão, GET /api/gateway/primary/config

// gateway.test.ts — leak-guard existente: scan recursivo de src/ assert GATEWAY_ADMIN_KEY ∈ {route.ts, gateway-admin.ts}

// Endpoints do gateway (Plan 18-01) que os wrappers consomem (via proxy → /admin/*):
//   GET  /admin/tenants                    -> [{id, slug, name, created_at, updated_at}]
//   GET  /admin/tenants/{slug}/keys        -> [{id, tenant_slug, key_prefix, status, data_class, created_at, last_used_at}]
//   POST /admin/tenants                    (body {slug,name})  -> 201 {id, slug, name}
//   POST /admin/tenants/{slug}/keys        (body {data_class}) -> 201 {id, key_prefix, tenant, data_class, key}
//   POST /admin/keys/{id}/revoke           -> 200 {status, id}
</interfaces>

Arquivos-âncora (LER antes de editar):
@dashboard/src/lib/gateway-admin.ts
@dashboard/src/lib/gateway.ts
@dashboard/src/lib/gateway-server.ts
@dashboard/src/lib/gateway.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirmar stack 40 aponta pro gateway consolidado + generalizar gateway-admin.ts p/ POST</name>
  <files>dashboard/src/lib/gateway-admin.ts</files>
  <read_first>
    - dashboard/src/lib/gateway-admin.ts (todo — o helper PATCH atual)
    - .planning/STATE.md (19-05: cutover worker-vm, ai-gateway.converse-ai.app → 10.10.10.50)
  </read_first>
  <action>
    GATE OPERACIONAL (locked default #6, revisável): ANTES de codar, confirmar que o env do
    dashboard prod (stack 40, Portainer endpoint 6) tem `GATEWAY_BASE_URL` apontando pro gateway
    consolidado worker-vm (público `https://ai-gateway.converse-ai.app` — que pós-19-05 resolve
    p/ 10.10.10.50 — ou o Host interno 10.10.10.50:80), NÃO um gateway descomissionado
    (n8n-ia-vm/vps-ifix-vm). Consultar via Portainer API (token em CLAUDE.md `ptr_jBtR69...`,
    `GET /api/stacks/40` ou o env do stack) OU `ssh worker-vm 'docker service inspect ...'`.
    Registrar o valor observado no SUMMARY. Se apontar p/ gateway morto → PARAR e sinalizar.

    Depois: generalizar `gateway-admin.ts`. Renomear/estender para um helper que aceita o método:
    manter `import "server-only"` na linha 1; extrair a lógica atual num `gatewayAdminMutate(method: "POST"|"PATCH", path: string, body: unknown): Promise<void>` (mesma leitura de
    GATEWAY_BASE_URL+GATEWAY_ADMIN_KEY, mesmo header X-Admin-Key, mesmo tratamento de erro
    {error.message}); adicionar `export async function gatewayAdminPost(path, body)` que delega
    com "POST"; MANTER `gatewayAdminPatch(path, body)` (delega "PATCH") para não quebrar Plan 17-05.
    Comentário do arquivo NÃO deve mudar a invariante do leak-guard (a key só aqui + route.ts).
  </action>
  <acceptance_criteria>
    - `gatewayAdminPost` e `gatewayAdminPatch` ambos exportados; ambos passam por um caminho comum que injeta X-Admin-Key server-side.
    - Linha 1 de gateway-admin.ts continua `import "server-only";`.
    - Valor de GATEWAY_BASE_URL do stack 40 registrado no SUMMARY e confirmado = gateway consolidado worker-vm (não n8n-ia-vm/vps-ifix-vm).
    - `grep -rln GATEWAY_ADMIN_KEY dashboard/src --include='*.ts' | grep -v '.test.'` retorna EXATAMENTE gateway-admin.ts + route.ts.
  </acceptance_criteria>
  <verify><automated>cd dashboard && bunx vitest run src/lib/gateway.test.ts -t 'leak' 2>&1 | tail -5</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Read wrappers fetchTenants/fetchTenantKeys + tipos + RSC server read + leak-guard</name>
  <files>dashboard/src/lib/gateway.ts, dashboard/src/lib/gateway-server.ts, dashboard/src/lib/gateway.test.ts</files>
  <read_first>
    - dashboard/src/lib/gateway.ts (proxyGet + padrão dos tipos/fetch existentes)
    - dashboard/src/lib/gateway-server.ts (fetchPodConfigServer — padrão RSC de URL absoluta + cookie)
    - dashboard/src/lib/gateway.test.ts (o leak-guard test + estilo dos testes de wrapper)
  </read_first>
  <action>
    Em `gateway.ts`: adicionar tipos `TenantRow { id: string; slug: string; name: string; created_at: string; updated_at: string }` e
    `TenantKeyRow { id: string; tenant_slug: string; key_prefix: string; status: string; data_class: string; created_at: string; last_used_at: string | null }`
    (espelham field-for-field o JSON dos handlers Go do Plan 18-01). Adicionar `export async function fetchTenants(): Promise<TenantRow[]>` = `proxyGet<TenantRow[]>("tenants")` e
    `export async function fetchTenantKeys(slug: string): Promise<TenantKeyRow[]>` = `proxyGet<TenantKeyRow[]>(\`tenants/${encodeURIComponent(slug)}/keys\`)`. NÃO adicionar wrapper de mutação aqui (mutação vai por gateway-admin.ts nas actions do Plan 18-03).
    Em `gateway-server.ts`: adicionar `export async function fetchTenantsServer(): Promise<TenantRow[]>` espelhando `fetchPodConfigServer` (URL absoluta de headers(), reencaminha cookie, GET /api/gateway/tenants, GatewayError em non-2xx). Import de TenantRow de "@/lib/gateway".
    Em `gateway.test.ts`: adicionar teste de fetchTenants/fetchTenantKeys mockando o fetch relativo (padrão dos testes de wrapper existentes) — assert que batem em `/api/gateway/tenants` e `/api/gateway/tenants/<slug>/keys`; e CONFIRMAR que o leak-guard continua passando (nenhum novo arquivo referencia GATEWAY_ADMIN_KEY — os wrappers usam só o proxy relativo).
  </action>
  <acceptance_criteria>
    - `cd dashboard && bunx vitest run src/lib/gateway.test.ts` passa (incluindo o leak-guard e os novos testes de wrapper).
    - `grep -q 'proxyGet<TenantRow\[\]>("tenants")' dashboard/src/lib/gateway.ts` (fetchTenants usa o proxy GET-only).
    - `! grep -q GATEWAY_ADMIN_KEY dashboard/src/lib/gateway.ts` e `! grep -q GATEWAY_ADMIN_KEY dashboard/src/lib/gateway-server.ts` (wrappers de leitura não leem a key).
    - fetchTenantKeys faz `encodeURIComponent(slug)` no path.
  </acceptance_criteria>
  <verify><automated>cd dashboard && bunx vitest run src/lib/gateway.test.ts -count=1 2>&1 | tail -8</automated></verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → dashboard (Next server) | só paths relativos /api/gateway/*; a admin key nunca cruza |
| dashboard server → gateway /admin/* | X-Admin-Key injetada server-side (route.ts GET, gateway-admin.ts mutação) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Info Disclosure | X-Admin-Key vaza p/ browser | mitigate | gateway-admin.ts mantém `import "server-only"` (import em client bundle = erro de build); leak-guard test scan de src/ exige key ∈ {route.ts, gateway-admin.ts}. Wrappers de leitura usam só o proxy relativo. |
| T-18-10 | Spoofing | dashboard aponta p/ gateway morto | mitigate | Task 1 gate: confirmar GATEWAY_BASE_URL do stack 40 = gateway consolidado worker-vm antes de prosseguir. |
| T-18-SC | Tampering | npm/pip installs | accept | Zero pacote novo — só edição de .ts existentes (fetch nativo + tipos). package.json inalterado. |
</threat_model>

<verification>
- `bunx vitest run src/lib/gateway.test.ts` verde (wrappers + leak-guard).
- gatewayAdminPost + gatewayAdminPatch exportados; server-only intacto.
- fetchTenants/fetchTenantKeys via proxy relativo; nenhuma key nos wrappers de leitura.
- GATEWAY_BASE_URL do stack 40 confirmado apontar pro worker-vm.
</verification>

<success_criteria>
Camada server-side pronta: helper de mutação POST/PATCH server-only + reads GET-only + tipos
espelhando o JSON Go, com a admin key confinada aos 2 arquivos server-only (leak-guard verde) e
o dashboard prod confirmado apontando pro gateway consolidado.
</success_criteria>

<output>
Create `.planning/phases/18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1/18-02-SUMMARY.md` when done
</output>
