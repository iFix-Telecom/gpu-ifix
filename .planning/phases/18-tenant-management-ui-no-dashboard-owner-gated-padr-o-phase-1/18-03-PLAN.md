---
phase: 18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - dashboard/src/lib/admin-actions-core.ts
  - dashboard/src/lib/admin-actions.ts
  - dashboard/src/lib/admin-actions.test.ts
autonomous: true
requirements: [TEN-UI-08, TEN-UI-09]

must_haves:
  truths:
    - "createTenant / createTenantKey / revokeKey rodam requireOwner ANTES de qualquer chamada ao gateway"
    - "operator é rejeitado: ZERO chamada ao gateway e ZERO row de audit"
    - "cada mutação grava EXATAMENTE 1 admin_audit_log {action, actor, target, metadata}"
    - "o raw key NUNCA aparece em metadata de audit nem em log"
    - "validação server-side: slug url-safe não-vazio, name não-vazio, data_class ∈ {normal,sensitive} — ANTES do gateway"
  artifacts:
    - path: dashboard/src/lib/admin-actions-core.ts
      provides: "createTenantCore/createTenantKeyCore/revokeKeyCore server-only (requireOwner→validar→gatewayAdminPost→writeAuditLog→revalidate)"
      min_lines: 90
    - path: dashboard/src/lib/admin-actions.ts
      provides: "wrappers use-server thin: createTenant/createTenantKey/revokeKey (requireOwner() sem args)"
      min_lines: 30
    - path: dashboard/src/lib/admin-actions.test.ts
      provides: "testes owner-gate + audit + no-secret p/ as 3 actions"
      min_lines: 80
  key_links:
    - from: admin-actions-core.ts createTenantCore
      to: gatewayAdminPost
      via: "POST tenants após requireOwner+validação"
      pattern: "gatewayAdminPost\\(\"tenants\""
    - from: admin-actions-core.ts *Core
      to: writeAuditLog
      via: "1 row por ação; metadata sem raw"
      pattern: "writeAuditLog\\("
---

<objective>
As server actions owner-gated de gestão de tenant/key, espelhando 1:1 o `updatePodConfigCore`
da Phase 17-05 e o `inviteOperatorCore` da Phase 13: `requireOwner` PRIMEIRO (identidade só da
sessão — sem seam de actor forjável, CR-01), validação server-side ANTES de qualquer chamada
gateway (defense-in-depth), mutação via `gatewayAdminPost` (server-only, X-Admin-Key), e
EXATAMENTE 1 row `admin_audit_log` por ação com metadata sem segredo.

Purpose: a única superfície de mutação do dashboard; o gate owner é server-side e não-burlável
(o UI-hide da Plan 18-04 é cosmético). Output: 3 cores + 3 wrappers + testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1/18-RESEARCH.md
@.planning/phases/17-dashboard-pod-config-control-owner-controla-todas-as-configs/17-05-SUMMARY.md

<interfaces>
<!-- Padrões a espelhar (admin-actions-core.ts) — LER, é o template exato -->

// requireOwner: identidade SÓ da sessão quando actor omitido; role != owner → throw
export async function requireOwner(actor?, authInstance?): Promise<{ actor: Actor; h: Headers }>

// writeAuditLog: 1 row admin_audit_log; metadata sem segredo
export async function writeAuditLog(args: { db?, actor: {id?,email?}, target?: {id?,email?}, action: string, metadata?: Record<string,unknown>|null }): Promise<void>

// updatePodConfigCore (o molde exato): requireOwner FIRST → refetch → validar → patch → writeAuditLog{field,old,new} → safeRevalidatePodConfig
export async function updatePodConfigCore(args: { actor?, auth?, db?, field, value, deps? }): Promise<void>

// Do Plan 18-02:
import { gatewayAdminPost } from "@/lib/gateway-admin"   // server-only mutação; retorna o corpo JSON parseado (Plan 18-02) — este plan só CONSOME, não edita gateway-admin.ts
import { fetchTenants } from "@/lib/gateway"             // se precisar refetch p/ audit-old (revoke não precisa)

// Wrapper use-server existente (admin-actions.ts) — molde:
export async function updatePodConfig(args) { "use server"; const {actor}=await requireOwner(); await updatePodConfigCore({actor, field, value}); }

// Endpoints alvo (via gatewayAdminPost, path relativo ao /admin/):
//   gatewayAdminPost("tenants", {slug, name})
//   gatewayAdminPost(`tenants/${slug}/keys`, {data_class})  -> resposta traz {key} raw — retornar ao caller, NUNCA auditar
//   gatewayAdminPost(`keys/${id}/revoke`, {})
</interfaces>

Arquivos-âncora:
@dashboard/src/lib/admin-actions-core.ts
@dashboard/src/lib/admin-actions.ts
@dashboard/src/lib/admin-actions.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createTenantCore + createTenantKeyCore + revokeKeyCore (server-only)</name>
  <files>dashboard/src/lib/admin-actions-core.ts</files>
  <read_first>
    - dashboard/src/lib/admin-actions-core.ts:133-176 (requireOwner + writeAuditLog)
    - dashboard/src/lib/admin-actions-core.ts:511-592 (updatePodConfigCore — o molde: requireOwner→validar→gateway→audit→revalidate)
    - dashboard/src/lib/admin-actions-core.ts:215-260 (inviteOperatorCore — molde de create com audit {target})
  </read_first>
  <behavior>
    - createTenantCore: operator → throw ANTES de gatewayAdminPost (0 calls, 0 audit); slug vazio/não-url-safe → throw antes do gateway; owner+válido → gatewayAdminPost("tenants",{slug,name}) + 1 audit action="tenant.create" metadata={slug,name}.
    - createTenantKeyCore: operator → throw; data_class ∉ {normal,sensitive} → throw antes do gateway; owner+válido → gatewayAdminPost(`tenants/${slug}/keys`,{data_class}) RETORNA a resposta {id,key_prefix,data_class,key} ao caller; 1 audit action="key.create" metadata={tenant:slug, data_class, key_prefix} — SEM o raw `key`.
    - revokeKeyCore: operator → throw; id ausente → throw; owner → gatewayAdminPost(`keys/${id}/revoke`,{}) + 1 audit action="key.revoke" metadata={key_id:id}.
    - Nenhuma metadata de audit contém o raw key.
  </behavior>
  <action>
    Adicionar em `admin-actions-core.ts` (server-only, SEM "use server") três funções espelhando
    `updatePodConfigCore`:
    - `createTenantCore(args:{actor?,auth?,db?,slug:string,name:string,deps?})`: `const {actor}=await requireOwner(args.actor, args.auth ?? realAuth)`; validar `slug` com regex url-safe (ex. `/^[a-z0-9][a-z0-9-]*$/`) não-vazio e `name.trim()!==""` → senão throw pt-BR; `await post("tenants",{slug,name})` (post = args.deps?.post ?? gatewayAdminPost); `await writeAuditLog({db:args.db, actor:{id:actor.id,email:actor.email}, action:"tenant.create", metadata:{slug, name}})`; `safeRevalidateTenants()`.
    - `createTenantKeyCore(args:{actor?,auth?,db?,slug:string,dataClass:string,deps?}): Promise<{id?:string,key_prefix?:string,data_class?:string,key?:string}>`: requireOwner FIRST; validar `dataClass ∈ {"normal","sensitive"}` senão throw; `const created = await post<{id?:string,key_prefix?:string,data_class?:string,key?:string}>(\`tenants/${encodeURIComponent(slug)}/keys\`,{data_class:dataClass})` — o `gatewayAdminPost` do Plan 18-02 JÁ retorna o corpo JSON parseado (create-key `{id,key_prefix,data_class,key}`); apenas CONSUMIR (NÃO editar `gateway-admin.ts` aqui — é um dos 2 arquivos do leak-guard, dono no 18-02). `post = args.deps?.post ?? gatewayAdminPost`. audit action="key.create" metadata={tenant:slug, data_class:dataClass, key_prefix: created.key_prefix} — NUNCA `created.key`. `return created`; `safeRevalidateTenants()`.
    - `revokeKeyCore(args:{actor?,auth?,db?,keyId:string,deps?})`: requireOwner FIRST; `keyId` não-vazio; `await post(\`keys/${encodeURIComponent(keyId)}/revoke\`,{})`; audit action="key.revoke" metadata={key_id:keyId}; safeRevalidateTenants().
    Adicionar `function safeRevalidateTenants()` espelhando `safeRevalidatePodConfig` (revalida a rota da página de gestão do Plan 18-04, ex. "/tenants/gerenciar"; swallow fora de request scope).
    NOTA: este plan NÃO edita `gateway-admin.ts` — o `gatewayAdminPost` do Plan 18-02 já retorna o corpo JSON parseado (invariante do leak-guard: só o 18-02 toca esse arquivo). Se o helper importado não retornar o corpo, PARAR e sinalizar (dependência 18-02 incompleta) em vez de editar `gateway-admin.ts` aqui.
  </action>
  <acceptance_criteria>
    - As 3 funções exportadas de admin-actions-core.ts; arquivo mantém `import "server-only"` na linha 1 e NÃO tem "use server".
    - `requireOwner` é a PRIMEIRA await em cada core (antes de qualquer post/fetch).
    - createTenantKeyCore retorna o objeto com `key` (raw) ao caller, mas a chamada writeAuditLog NÃO inclui `key` em metadata (grep do metadata).
    - Validação (slug regex, data_class enum) roda antes do post.
  </acceptance_criteria>
  <verify><automated>cd dashboard && bunx vitest run src/lib/admin-actions.test.ts -t 'TEN-UI' -count=1 2>&1 | tail -8</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Wrappers use-server + testes owner-gate/audit/no-secret</name>
  <files>dashboard/src/lib/admin-actions.ts, dashboard/src/lib/admin-actions.test.ts</files>
  <read_first>
    - dashboard/src/lib/admin-actions.ts:47-140 (wrappers thin: requireOwner() sem args → *Core; NÃO exportar requireOwner/writeAuditLog daqui)
    - dashboard/src/lib/admin-actions.test.ts:198-560 (padrão dos testes: operator-rejected/no-gateway/no-audit, owner-success com mocks hoisted gatewayAdminPatchMock, audit assert)
  </read_first>
  <action>
    Em `admin-actions.ts` adicionar 3 wrappers "use server" thin espelhando `updatePodConfig`:
    - `createTenant({slug,name})`: `const {actor}=await requireOwner(); await createTenantCore({actor, slug, name})`.
    - `createTenantKey({slug,dataClass})`: `const {actor}=await requireOwner(); return await createTenantKeyCore({actor, slug, dataClass})` — RETORNA o {key,...} p/ a UI mostrar o raw 1×.
    - `revokeKey({keyId})`: `const {actor}=await requireOwner(); await revokeKeyCore({actor, keyId})`.
    NÃO exportar requireOwner/writeAuditLog daqui (invariante CR-02). Importar os cores de admin-actions-core.
    Em `admin-actions.test.ts` adicionar um `describe("admin-actions — TEN-UI-08/09 tenant management", ...)` com mocks hoisted (padrão existente `gatewayAdminPatchMock` → adicionar `gatewayAdminPostMock`):
    (1) createTenantCore operator → rejeitado, gatewayAdminPost NÃO chamado, writeAuditLog 0×.
    (2) createTenantCore owner válido → gatewayAdminPost("tenants",{slug,name}) chamado 1× + 1 audit action "tenant.create".
    (3) createTenantKeyCore owner → retorna {key:...}; assert a metadata do audit NÃO contém a chave `key` nem o valor raw (só {tenant,data_class,key_prefix}).
    (4) createTenantKeyCore data_class inválida → throw antes do gateway (0 calls).
    (5) revokeKeyCore owner → gatewayAdminPost(`keys/<id>/revoke`) 1× + audit "key.revoke".
  </action>
  <acceptance_criteria>
    - `cd dashboard && bunx vitest run src/lib/admin-actions.test.ts` passa (novos + pré-existentes).
    - Teste (1) e (4) provam ZERO chamada gateway quando rejeitado/inválido.
    - Teste (3) prova que o raw key nunca entra no metadata de audit.
    - `grep -c 'export async function' dashboard/src/lib/admin-actions.ts` inclui createTenant, createTenantKey, revokeKey; `! grep -q 'export.*requireOwner' dashboard/src/lib/admin-actions.ts`.
  </acceptance_criteria>
  <verify><automated>cd dashboard && bunx vitest run src/lib/admin-actions.test.ts -count=1 2>&1 | tail -10</automated></verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente/UI → server action | actor/role é derivado da SESSÃO, nunca do argumento (CR-01) |
| server action → gateway | X-Admin-Key via gatewayAdminPost server-only |
| server action → admin_audit_log | metadata sem segredo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Elevation of Privilege | operator cria/revoga tenant/key | mitigate | requireOwner() sem args (identidade só da sessão) é a 1ª await em cada core; teste operator-rejected assevera 0 gateway + 0 audit. Wrappers use-server não expõem seam de actor (CR-01). |
| T-18-04 | Info Disclosure | raw key em audit/log | mitigate | metadata de key.create = {tenant,data_class,key_prefix} apenas; o raw só é retornado ao caller p/ exibição efêmera; teste assevera ausência do raw no metadata. |
| T-18-05 | Tampering | slug/data_class malicioso | mitigate | validação server-side (slug regex url-safe, data_class enum whitelist) ANTES do gateway — defense-in-depth com o handler Go (Plan 18-01). |
| T-18-08 | Repudiation | mutação sem trilha | mitigate | writeAuditLog exatamente 1 row por ação (tenant.create/key.create/key.revoke); não-forjável/suprimível (writeAuditLog não-RPC no core server-only, CR-02). |
| T-18-SC | Tampering | npm/pip installs | accept | Zero pacote novo — só edição de .ts existentes. |
</threat_model>

<verification>
- `bunx vitest run src/lib/admin-actions.test.ts` verde.
- requireOwner 1ª await em cada core; operator → 0 gateway + 0 audit.
- 1 audit row por ação; raw key nunca no metadata (teste).
- validação server-side antes do gateway.
</verification>

<success_criteria>
3 server actions owner-gated auditadas, com requireOwner server-side não-burlável, validação
defense-in-depth e audit sem segredo — prontas p/ a UI (Plan 18-04) consumir.
</success_criteria>

<output>
Create `.planning/phases/18-tenant-management-ui-no-dashboard-owner-gated-padr-o-phase-1/18-03-SUMMARY.md` when done
</output>
