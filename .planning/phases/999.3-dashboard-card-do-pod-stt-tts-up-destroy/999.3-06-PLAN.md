---
phase: 999.3-dashboard-card-do-pod-stt-tts-up-destroy
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/incidents-history-date-filter-search.md
  - .planning/todos/pending/stt-embed-metering-fix.md
  - .planning/todos/pending/phase8-9-client-uat-signoff.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "O todo do filtro de data e busca do /incidents esta fechado com a evidencia de onde cada checkbox foi entregue"
    - "O todo do metering de STT e embed esta fechado ou reaberto com base em uma consulta real ao banco de producao"
    - "O status atual do sign-off UAT dos 6 apps cliente esta registrado por escrito"
  artifacts:
    - path: ".planning/todos/done/incidents-history-date-filter-search.md"
      provides: "Todo arquivado com evidencia arquivo-e-linha por checkbox"
      contains: "Phase 15"
    - path: ".planning/todos/done/stt-embed-metering-fix.md"
      provides: "Todo arquivado com o resultado da consulta SQL em producao"
      contains: "billing_events"
  key_links:
    - from: ".planning/todos/done/stt-embed-metering-fix.md"
      to: "bd_ai_gateway billing_events"
      via: "consulta SQL de verificacao com resultado colado"
      pattern: "audio_seconds"
---

<objective>
Fechar (ou reabrir com dado na mao) os 3 todos dobrados nesta fase, sem escrever codigo de
producao.

Purpose: o CONTEXT dobrou 3 todos nesta fase. A pesquisa ja provou que dois deles estao
resolvidos e que o terceiro e processo, nao codigo — o que falta e a verificacao formal e o
arquivamento, para o backlog parar de carregar item morto.
Output: 3 arquivos de todo movidos para `.planning/todos/done/` com a evidencia colada, ou
mantidos em pending com o motivo explicito.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-CONTEXT.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md

<interfaces>
Evidencia ja levantada pela pesquisa (RESEARCH secao "Todos dobrados - status verificado"),
a ser CONFIRMADA arquivo a arquivo antes de arquivar — nao copiar sem checar:

Todo 1 - incidents-history-date-filter-search (OBS-10):
  handler aceita from/to        gateway/internal/admin/audit.go:19-25 + parsing no ServeHTTP
  handler retorna total (COUNT) gateway/internal/admin/audit.go:56-63 (AuditResponse.Total int64)
                                 + auditQueries.CountAuditStateChanges (:83-86)
  busca por texto               gateway/internal/admin/audit.go:26-28 (ILIKE parametrizado)
  UI date picker + search box   dashboard/src/app/(dashboard)/incidents/page.tsx:104-136
                                 (Popover + Calendar mode="range" + Input) e pager por total (:166)
  REQUIREMENTS.md:269-270 marca OBS-10 como Complete.

Todo 2 - stt-embed-metering-fix (TEN-04 / OBS-09):
  STATE.md:33-35 registra RESOLVIDO na Phase 16, live-proven em PROD (commit 3374999).
  Consulta de confirmacao contra o DB live bd_ai_gateway (DSN root-600 em
  /opt/ai-gateway-prod/.env no worker-vm). ATENCAO: o schema qualificado (ai_gateway. vs
  public.) NAO foi verificado na pesquisa — confirmar em gateway/db/ antes de rodar.
  Consulta: agregar por route em billing_events dos ultimos 7 dias, filtrando
  route in ('stt','embed'), somando audio_seconds e embeds_count e pegando max(created_at).
  PASS = linha stt com soma de audio_seconds maior que zero E linha embed com embeds maior
  que zero. Ausencia de trafego embed em 7 dias NAO e regressao — ampliar a janela antes de
  concluir qualquer coisa.

Todo 3 - phase8-9-client-uat-signoff (INT-01..06):
  REQUIREMENTS.md:271-276 lista os 6 em "Code-complete; live UAT pending".
  .planning/phases/08-*/08-HUMAN-UAT.md e 09-*/09-HUMAN-UAT.md com final_status: pending.
  Processo, nao codigo — nao bloqueia esta fase (decisao do CONTEXT).

Acesso a producao: `ssh worker-vm` (alias configurado). O DSN esta em arquivo root-600 —
usar sudo na leitura e NUNCA imprimir a senha no output nem no arquivo de todo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirmar e arquivar o todo do filtro do /incidents</name>
  <files>.planning/todos/pending/incidents-history-date-filter-search.md</files>
  <read_first>
    - .planning/todos/pending/incidents-history-date-filter-search.md (os 4 checkboxes originais)
    - gateway/internal/admin/audit.go (arquivo inteiro) — parametros from/to/search e o campo Total
    - dashboard/src/app/(dashboard)/incidents/page.tsx linhas 40-180 — date range picker, search box e pager
    - .planning/REQUIREMENTS.md linhas 260-280 — status de OBS-10
  </read_first>
  <action>
    Ler o todo e verificar cada um dos 4 checkboxes contra o codigo ATUAL, com grep/leitura,
    anotando arquivo e linha reais (nao os da pesquisa — reconfirmar, o codigo pode ter
    mudado). Se os 4 estiverem entregues: marcar os checkboxes, acrescentar ao final do
    arquivo uma secao "Resolucao" datada dizendo que o item foi entregue pela Phase 15
    (OBS-10), com a tabela checkbox-para-evidencia (arquivo dois-pontos linha), e mover o
    arquivo para `.planning/todos/done/` com `git mv` (criar o diretorio se nao existir).
    Se algum checkbox NAO estiver entregue: NAO arquivar — acrescentar a secao "Resolucao
    parcial" listando exatamente o que falta e deixar o arquivo em pending. Registrar a
    decisao tomada no SUMMARY do plano.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && grep -c "from\|to\|search" gateway/internal/admin/audit.go && grep -c "Calendar\|mode=\"range\"" "dashboard/src/app/(dashboard)/incidents/page.tsx" && ls .planning/todos/done/incidents-history-date-filter-search.md</automated>
  </verify>
  <acceptance_criteria>
    - `gateway/internal/admin/audit.go` contem os parametros `from`, `to` e `search` e o campo `Total` (grep confirma os 4)
    - `dashboard/src/app/(dashboard)/incidents/page.tsx` contem `mode="range"` e um campo de busca
    - O arquivo esta em `.planning/todos/done/incidents-history-date-filter-search.md` OU permanece em `pending/` com uma secao "Resolucao parcial" listando o que falta
    - Se arquivado, o arquivo contem a string `Phase 15` e uma referencia de arquivo e linha para cada um dos 4 checkboxes
    - Nenhum arquivo de codigo de producao foi modificado: `git status --porcelain gateway/ dashboard/ ops/` sem saida
  </acceptance_criteria>
  <done>Backlog nao carrega mais um item ja entregue, e a evidencia de onde foi entregue esta escrita.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar o metering de STT/embed em producao por SQL</name>
  <files>.planning/todos/pending/stt-embed-metering-fix.md</files>
  <read_first>
    - .planning/todos/pending/stt-embed-metering-fix.md
    - .planning/STATE.md linhas 25-45 — registro da resolucao na Phase 16
    - gateway/db/ (migrations e schema) — confirmar o schema qualificado e os nomes exatos das colunas `audio_seconds` e `embeds_count` na tabela `billing_events` ANTES de montar a consulta
  </read_first>
  <action>
    Confirmar em `gateway/db/` o nome do schema (`ai_gateway.` ou `public.`) e das colunas.
    Obter o DSN de `bd_ai_gateway` lendo `/opt/ai-gateway-prod/.env` no `worker-vm` via ssh
    com sudo, sem imprimir a senha no transcript nem em arquivo. Rodar a consulta de
    agregacao por `route` sobre `billing_events` dos ultimos 7 dias, filtrando
    `route in ('stt','embed')`, somando `audio_seconds` e `embeds_count` e pegando
    `max(created_at)`. Se nao houver linha `embed` na janela de 7 dias, ampliar para 30 dias
    antes de concluir qualquer coisa — ausencia de trafego nao e regressao de metering.
    Se o resultado for PASS (stt com `audio_seconds` maior que zero e embed com `embeds_count`
    maior que zero): acrescentar ao todo uma secao "Resolucao" datada com a consulta usada e
    o resultado agregado colado (sem DSN, sem senha) e mover para `.planning/todos/done/`.
    Se for FAIL: NAO arquivar — registrar o resultado, marcar o todo como reaberto e
    descrever no SUMMARY o que foi observado, sem tentar corrigir nesta fase (seria escopo
    novo).
    Aplicar a regra de ouro do projeto: o que nao for observado no resultado da consulta vai
    escrito como HIPOTESE, nunca como fato.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && grep -rn "audio_seconds" gateway/db/ | head -5 && ls .planning/todos/done/stt-embed-metering-fix.md .planning/todos/pending/stt-embed-metering-fix.md 2>&1 | head -2</automated>
  </verify>
  <acceptance_criteria>
    - As colunas `audio_seconds` e `embeds_count` foram localizadas em `gateway/db/` (grep retorna resultado) e o schema qualificado esta anotado no todo
    - O arquivo do todo contem uma secao datada com a consulta SQL executada e o resultado agregado por `route` colado
    - Nenhuma credencial no arquivo: `grep -cEi "password|postgres://|postgresql://" .planning/todos/*/stt-embed-metering-fix.md` retorna 0
    - O arquivo esta em `.planning/todos/done/` (PASS) OU permanece em `pending/` marcado como reaberto com o resultado observado (FAIL)
    - Nenhum arquivo de codigo de producao foi modificado: `git status --porcelain gateway/ dashboard/ ops/` sem saida
  </acceptance_criteria>
  <done>O status do metering em producao esta decidido por dado real, nao por memoria de STATE.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar o status do sign-off UAT dos 6 apps cliente</name>
  <files>.planning/todos/pending/phase8-9-client-uat-signoff.md</files>
  <read_first>
    - .planning/todos/pending/phase8-9-client-uat-signoff.md
    - .planning/REQUIREMENTS.md linhas 265-280 — status de INT-01..06
    - .planning/phases/08-*/08-HUMAN-UAT.md e .planning/phases/09-*/09-HUMAN-UAT.md — campo `final_status` no frontmatter
  </read_first>
  <action>
    Ler os dois arquivos HUMAN-UAT e a secao INT do REQUIREMENTS e acrescentar ao todo uma
    secao "Status em 2026-07-31" com uma tabela de INT-01 a INT-06 contendo, por linha, o
    app, o status atual textual do REQUIREMENTS e o `final_status` do HUMAN-UAT
    correspondente. NAO arquivar o todo — e processo pendente de sign-off humano, nao um
    item entregue (decisao do CONTEXT: item de checklist, nao bloqueia codigo). Deixar
    explicito no arquivo o que exatamente falta para fechar (quem assina, qual evidencia) e
    que esta fase nao alterou nada disso.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && grep -c "INT-0" .planning/todos/pending/phase8-9-client-uat-signoff.md && grep -rn "final_status" .planning/phases/08-*/08-HUMAN-UAT.md .planning/phases/09-*/09-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/pending/phase8-9-client-uat-signoff.md` continua em `pending/` (nao arquivado)
    - O arquivo contem uma secao datada com uma tabela citando INT-01 ate INT-06 (grep de `INT-0` retorna 6 ou mais)
    - O `final_status` lido de cada HUMAN-UAT esta citado no arquivo
    - Nenhum arquivo de codigo de producao foi modificado: `git status --porcelain gateway/ dashboard/ ops/` sem saida
  </acceptance_criteria>
  <done>O estado do sign-off esta escrito, com o que falta nomeado, sem fingir que foi entregue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador -> DSN de producao root-600 | Leitura de credencial de banco de PROD para rodar a consulta de verificacao |
| resultado de PROD -> artefato de planejamento versionado | Dado de producao colado em arquivo do repositorio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-14 | Information Disclosure | DSN de `bd_ai_gateway` no todo arquivado | mitigate | Colar apenas o resultado agregado (route, contagem, somas, max created_at); criterio de aceite roda grep case-insensitive por `password`, `postgres://` e `postgresql://` exigindo zero ocorrencias |
| T-999.3-15 | Tampering | consulta em PROD | mitigate | Somente `SELECT` com agregacao; nenhuma escrita, nenhum DDL. A consulta e read-only por construcao |
| T-999.3-16 | Repudiation | arquivamento de todo sem prova | mitigate | Arquivar exige evidencia arquivo-e-linha (todo 1) ou resultado de consulta colado (todo 2); FAIL mantem o item em `pending` |
| T-999.3-SC | Tampering | package installs | accept | Plano sem codigo e sem instalacao |
</threat_model>

<verification>
- `git status --porcelain gateway/ dashboard/ ops/ pod/` sem saida (plano nao toca codigo)
- `ls .planning/todos/pending/` mostra no maximo 2 arquivos dobrados restantes
- `grep -rEi "password|postgres://|postgresql://" .planning/todos/` sem saida
- Todo arquivo movido para `.planning/todos/done/` tem uma secao de resolucao datada com evidencia
</verification>

<success_criteria>
Os 3 todos dobrados tem status decidido com evidencia na mao: os entregues arquivados com
prova, o de processo mantido em pending com o que falta escrito, e nenhuma afirmacao sem
dado.
</success_criteria>

<output>
Create `.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-06-SUMMARY.md` when done
</output>
