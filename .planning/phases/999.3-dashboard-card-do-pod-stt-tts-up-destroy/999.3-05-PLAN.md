---
phase: 999.3-dashboard-card-do-pod-stt-tts-up-destroy
plan: 05
type: execute
wave: 2
depends_on: ["999.3-04"]
files_modified:
  - dashboard/src/components/operacao-sttpod-panel.tsx
  - dashboard/src/components/operacao-sttpod-panel.test.tsx
  - dashboard/src/components/operacao-panels.tsx
  - dashboard/src/app/(dashboard)/operacao/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "A pagina /operacao mostra um quinto painel STT/TTS pod com maquina, geo, dolar-hora, ip porta, uptime e custo dia/mes em BRL com USD entre parenteses"
    - "O painel lista o machine_avoid e o historico de eventos numa tabela com data, maquina, dolar-hora, resultado e duracao"
    - "O painel mostra badge stale quando published_at tem mais de 20 minutos, e texto distinto quando nunca houve publicacao"
    - "Owner ve os botoes provisionar e destruir com dialogo de confirmacao; operator ve o mesmo painel sem afordancia de acao"
    - "Os outros 4 paineis continuam renderizando exatamente como antes"
  artifacts:
    - path: "dashboard/src/components/operacao-sttpod-panel.tsx"
      provides: "Quinto painel da pagina Operacao"
      min_lines: 150
    - path: "dashboard/src/components/operacao-panels.tsx"
      provides: "Client island com o useQuery de 10s e os 5 paineis"
      contains: "use client"
    - path: "dashboard/src/app/(dashboard)/operacao/page.tsx"
      provides: "RSC shell que resolve isOwner"
      contains: "getViewerRole"
  key_links:
    - from: "dashboard/src/app/(dashboard)/operacao/page.tsx"
      to: "dashboard/src/components/operacao-panels.tsx"
      via: "prop isOwner"
      pattern: "OperacaoPanels isOwner"
    - from: "dashboard/src/components/operacao-sttpod-panel.tsx"
      to: "dashboard/src/lib/admin-actions.ts"
      via: "sttPodCommand"
      pattern: "sttPodCommand"
---

<objective>
Renderizar o card do pod STT/TTS como quinto painel da `/operacao`, com os botoes manuais
owner-gated (D-03/D-11/D-13/D-14/D-15).

Purpose: o card existe para responder "por que a oferta barata foi pulada?" e "quanto esse
pod custou hoje?" num lugar so — por isso repete propositalmente os breakers `local-stt` e
`kokoro-tts` (D-14) e mostra o `machine_avoid` correlacionavel com o historico de falhas.
Output: painel novo, client island extraido da page, RSC shell resolvendo `isOwner`, teste
de componente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-CONTEXT.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-PATTERNS.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-04-SUMMARY.md
@dashboard/src/components/operacao-lifecycle-timeline.tsx
@dashboard/src/components/operacao-cost-panel.tsx

<interfaces>
Tipos criados no plano 999.3-04 (`dashboard/src/lib/gateway.ts`):
  OperationsSttPod { state: OperationsSttPodState | null;
                     pending_command: { command: string; requested_at: string } | null;
                     usd_to_brl: number }
  OperationsSttPodState { published_at; window {start_h,end_h}; instance_id; machine_id;
                          geolocation; dph; ip; port; provisioned_at; machine_avoid[];
                          alerted_no_offer; cost {day,day_usd,month,month_usd};
                          events[]; last_command_result }
  OperationsSttPodEvent { ts; kind; result; machine_id; dph; geolocation; duration_s; detail }
  OperationsResponse.sttpod?: OperationsSttPod
  OperationsBreaker { upstream: string; state: string }

Server action criada no plano 999.3-04 (`dashboard/src/lib/admin-actions.ts`):
  export async function sttPodCommand(args: { command: string }): Promise<void>

Helpers ja existentes a REUSAR (Don't Hand-Roll):
  formatBrl              de "@/lib/format"        formatacao BRL
  getViewerRole          de "@/lib/viewer"        RSC only (usa next/headers)
  KpiCard                de "@/components/kpi-card"
  Card/CardHeader/CardTitle/CardContent          de "@/components/ui/card"
  Table/TableHeader/TableBody/TableRow/TableHead/TableCell  de "@/components/ui/table"
  AlertDialog e derivados de "@/components/ui/alert-dialog"
  toast                  de "sonner"
  Formatacao de data: d.toLocaleString("pt-BR") — NUNCA toISOString().slice()

Corpo ATUAL de dashboard/src/app/(dashboard)/operacao/page.tsx (78 linhas) que sera MOVIDO
para o island sem reescrita: "use client", useQuery com queryKey ["operations"],
queryFn fetchOperations, refetchInterval 10000, helper errorMessage usando GatewayError,
h1 "Operacao" + StaleIndicator, ramos isLoading (Skeletons) / isError (mensagem + botao
"Tentar novamente") / data (os 4 paineis).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Componente OperacaoSttPodPanel</name>
  <files>dashboard/src/components/operacao-sttpod-panel.tsx</files>
  <read_first>
    - dashboard/src/components/operacao-cost-panel.tsx (arquivo inteiro) — doc-comment, Card shell, KpiCard row, uso de `formatBrl`
    - dashboard/src/components/operacao-lifecycle-timeline.tsx (arquivo inteiro) — helper `formatDateTime` com fallback "—", tabela com empty-state e `tabular-nums`
    - dashboard/src/components/operacao-breaker-badges.tsx (linhas 1-60) — switch de classes por estado e switch de label pt-BR
    - dashboard/src/components/pod-config-controls.tsx (linhas 1-60, 720-800, 930-977) — `"use client"`, funcao `commit` com pending/erro/toast, AlertDialog de confirmacao sem type-to-confirm, gate cosmetico `{isOwner ? controle : leitura}`
    - dashboard/src/lib/gateway.ts (tipos criados no plano 999.3-04)
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-CONTEXT.md decisoes D-03, D-08, D-11, D-14, D-15
  </read_first>
  <action>
    Criar `dashboard/src/components/operacao-sttpod-panel.tsx` com `"use client"` na linha 1
    e doc-comment explicando que e o quinto painel da Operacao, que os dados vem do
    `state.json` publicado por `ops/vast-3060/vast3060.py` e que a redundancia dos breakers
    `local-stt` e `kokoro-tts` e proposital (D-14).
    Props: `{ sttpod?: OperationsSttPod; breakers: OperationsBreaker[]; isOwner: boolean }`.
    Estrutura, de cima para baixo, dentro de um `<Card>` com `<CardTitle>` "STT/TTS pod":

    1. Cabecalho de frescor (D-03): tres estados textuais DISTINTOS —
       (a) `sttpod` undefined ou `sttpod.state` null: "Sem publicacao ainda - aguardando o
       proximo ciclo do script (ate 2 min)"; (b) `published_at` com mais de 20 minutos:
       badge amber "stale ha Xmin" no molde de `breakerBadgeClass`; (c) fresco: badge
       discreto com o horario de `published_at` em `toLocaleString("pt-BR")`.
       Calcular a idade com `Date.now() - new Date(published_at).getTime()`; entrada
       invalida cai no estado (a).
    2. Linha de KPIs com `KpiCard`: Maquina (`machine_id` + `geolocation`, "—" quando null),
       Custo/hora (`dph` com 4 casas + sufixo "/h"), Endereco (`ip` mais dois-pontos mais
       `port`), Uptime (derivado de `provisioned_at` ate agora, formatado em h e min; "pod
       destruido" quando `instance_id` e null).
    3. Custo (D-08): custo do dia e custo do mes em BRL via
       `formatBrl(cost.day_usd * sttpod.usd_to_brl)` com o valor em USD entre parenteses.
       `usd_to_brl` vem do gateway — NAO hardcodar rate.
    4. Agenda (D-14): janela `window.start_h`-`window.end_h` e a proxima transicao calculada
       da hora local atual ("proximo destroy as 22:00" dentro da janela, "proximo provision
       as 07:00" fora dela).
    5. Upstreams (D-14): badges de `local-stt` e `kokoro-tts` filtrados de `breakers` por
       `upstream`, reusando o mapeamento estado-para-classe e o label pt-BR do
       `operacao-breaker-badges.tsx`. Upstream ausente da lista renderiza "—".
    6. Maquinas evitadas (D-14): os ids de `machine_avoid` como chips, com um texto curto
       explicando que sao maquinas que falharam provision e sao puladas na busca de oferta.
    7. Comando (D-12): quando `pending_command` nao e null, linha "comando pendente:
       <command> (desde HH:MM)"; senao, quando `last_command_result` existe, "<command>
       <status> as HH:MM" com `status` traduzido (running -> "executando", ok -> "executado",
       fail -> "falhou").
    8. Historico (D-15): `<Table>` no molde de `operacao-lifecycle-timeline.tsx` com colunas
       Data, Tipo, Maquina, Custo/hora, Resultado e Duracao, alimentada por `state.events`
       em ordem cronologica inversa (mais recente primeiro); celulas numericas com
       `className="tabular-nums"`; empty-state "Sem eventos registrados." com `colSpan`
       correto.
    9. Acoes (D-11): renderizar os botoes "Provisionar agora" e "Destruir pod" APENAS quando
       `isOwner` e true (gate cosmetico; o gate real e o `requireOwner` do server action).
       Cada botao abre um `AlertDialog` com titulo e corpo especificos em pt-BR descrevendo
       o impacto (destruir derruba STT e TTS ate o proximo provision; provisionar cria
       instancia nova e passa a cobrar), sem type-to-confirm (decisao D-04 da Phase 17).
       Confirmar chama `sttPodCommand({ command })` dentro de um `try/catch/finally` com
       estado `pending` desabilitando os botoes, `toast.success` com "Comando enfileirado -
       o script executa em ate 2 min" e `toast.error` com a mensagem do erro. Nao fazer
       refetch manual: o `useQuery` de 10s do island ja atualiza.
    Todo texto visivel em pt-BR; nenhum emoji.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/dashboard && npx tsc --noEmit && npx eslint src/components/operacao-sttpod-panel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com codigo 0
    - Primeira linha de `dashboard/src/components/operacao-sttpod-panel.tsx` e `"use client";`
    - `grep -c "formatBrl" dashboard/src/components/operacao-sttpod-panel.tsx` retorna 1 ou mais
    - `grep -c "toISOString" dashboard/src/components/operacao-sttpod-panel.tsx` retorna 0
    - `grep -c "toLocaleString(\"pt-BR\")" dashboard/src/components/operacao-sttpod-panel.tsx` retorna 1 ou mais
    - `grep -c "sttPodCommand" dashboard/src/components/operacao-sttpod-panel.tsx` retorna 1 ou mais
    - `grep -c "AlertDialog" dashboard/src/components/operacao-sttpod-panel.tsx` retorna 4 ou mais
    - `grep -c "window.confirm" dashboard/src/components/operacao-sttpod-panel.tsx` retorna 0
    - `grep -c "isOwner" dashboard/src/components/operacao-sttpod-panel.tsx` retorna 2 ou mais
    - `grep -c "machine_avoid" dashboard/src/components/operacao-sttpod-panel.tsx` retorna 1 ou mais
    - `grep -c "local-stt" dashboard/src/components/operacao-sttpod-panel.tsx` retorna 1 ou mais e `grep -c "kokoro-tts"` retorna 1 ou mais
    - Nenhum rate USD hardcodado: `grep -cE "5\.[0-9]|usdToBrl = [0-9]" dashboard/src/components/operacao-sttpod-panel.tsx` retorna 0
  </acceptance_criteria>
  <done>Painel completo, tipado, com os 9 blocos de conteudo do D-14/D-15 e os botoes gated.</done>
</task>

<task type="auto">
  <name>Task 2: Client island operacao-panels.tsx + RSC shell da page</name>
  <files>dashboard/src/components/operacao-panels.tsx, dashboard/src/app/(dashboard)/operacao/page.tsx</files>
  <read_first>
    - dashboard/src/app/(dashboard)/operacao/page.tsx (arquivo inteiro, 78 linhas) — este e o corpo a MOVER, nao a reescrever
    - dashboard/src/app/(dashboard)/operacao/config/page.tsx linhas 20, 32-33, 101-115, 137-141 — molde de RSC shell: `export const dynamic = "force-dynamic"`, `getViewerRole()`, passagem da prop `isOwner`
    - dashboard/src/components/operacao-sttpod-panel.tsx (criado na Task 1)
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md Pattern 5 e Pitfall 2
  </read_first>
  <action>
    Criar `dashboard/src/components/operacao-panels.tsx` MOVENDO o corpo atual de
    `operacao/page.tsx` sem alterar a logica: mesma diretiva `"use client"`, mesmo helper
    `errorMessage` com `GatewayError`, mesmo `useQuery` com `queryKey: ["operations"]`,
    `queryFn: fetchOperations` e `refetchInterval: 10000` (D-13: um poll so, nao criar query
    nova para o painel), mesmos ramos `isLoading` / `isError` / `data`, mesmo h1 "Operacao" e
    mesmo `StaleIndicator` (dependem de `query.dataUpdatedAt`, por isso ficam no island e nao
    na shell). Diferencas permitidas: exportar como
    `export function OperacaoPanels({ isOwner }: { isOwner: boolean })` (named export, nao
    default) e adicionar o quinto painel apos `OperacaoLifecycleTimeline`, passando
    `sttpod={query.data.sttpod}`, `breakers={query.data.breakers}` e `isOwner={isOwner}`.
    Acrescentar mais um `Skeleton` no ramo de loading para o painel novo.

    Reescrever `dashboard/src/app/(dashboard)/operacao/page.tsx` como RSC shell com cerca de
    10 linhas: doc-comment curto explicando o split (a page le a sessao a cada request, o
    island faz o poll), `export const dynamic = "force-dynamic"`, e
    `export default async function OperacaoPage()` que faz
    `const isOwner = (await getViewerRole()) === "owner"` (fail-closed: qualquer valor que
    nao seja "owner", inclusive null, resulta em false) e retorna
    `<OperacaoPanels isOwner={isOwner} />`. A page NAO pode ter `"use client"` nem importar
    `useQuery` — `getViewerRole` usa `next/headers` e quebra o build no bundle do cliente
    (Pitfall 2).
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/dashboard && npx tsc --noEmit && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` no diretorio dashboard sai com codigo 0
    - `grep -c "use client" "dashboard/src/app/(dashboard)/operacao/page.tsx"` retorna 0
    - `grep -c "getViewerRole" "dashboard/src/app/(dashboard)/operacao/page.tsx"` retorna 2 (import + uso)
    - `grep -c "force-dynamic" "dashboard/src/app/(dashboard)/operacao/page.tsx"` retorna 1
    - `wc -l < "dashboard/src/app/(dashboard)/operacao/page.tsx"` retorna 25 ou menos
    - `grep -c "refetchInterval: 10000" dashboard/src/components/operacao-panels.tsx` retorna 1
    - `grep -c "useQuery" dashboard/src/components/operacao-panels.tsx` retorna 2 (import + uso) e o mesmo grep na page retorna 0
    - `grep -c "OperacaoFsmPanel\|OperacaoCostPanel\|OperacaoBreakerBadges\|OperacaoLifecycleTimeline\|OperacaoSttPodPanel" dashboard/src/components/operacao-panels.tsx` retorna 10 (5 imports + 5 usos)
  </acceptance_criteria>
  <done>Pagina divide RSC shell e island; os 4 paineis antigos intactos e o quinto no ar com a prop isOwner.</done>
</task>

<task type="auto">
  <name>Task 3: Teste de componente do painel</name>
  <files>dashboard/src/components/operacao-sttpod-panel.test.tsx</files>
  <read_first>
    - dashboard/src/components/fsm-panel.test.tsx (arquivo inteiro) — molde de teste de componente com @testing-library/react no repo
    - dashboard/src/components/operacao-sttpod-panel.tsx (criado na Task 1)
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-VALIDATION.md linha D-03 do mapa por tarefa
  </read_first>
  <action>
    Criar `dashboard/src/components/operacao-sttpod-panel.test.tsx` no molde do
    `fsm-panel.test.tsx`, montando fixtures em memoria (nenhuma chamada de rede; mockar o
    modulo `@/lib/admin-actions` com `vi.mock` para que `sttPodCommand` seja um spy).
    Casos:
    - "sem publicacao": prop `sttpod` undefined -> renderiza o texto de "Sem publicacao
      ainda" e NAO renderiza a tabela de historico com linhas.
    - "stale": `published_at` de 45 minutos atras -> aparece o badge com "stale"; o mesmo
      fixture com `published_at` de 1 minuto atras NAO mostra "stale". Estes dois asserts
      juntos sao o teste do D-03.
    - "historico renderiza": `events` com 2 entradas de kinds diferentes -> a tabela mostra
      2 linhas e o `machine_id` de cada uma aparece no documento.
    - "gate cosmetico": com `isOwner` false, os botoes "Provisionar agora" e "Destruir pod"
      NAO estao no documento; com `isOwner` true, ambos estao.
    Usar `queryBy*` para as asercoes de ausencia e `getBy*` para as de presenca.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/dashboard && npx vitest run src/components/operacao-sttpod-panel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/operacao-sttpod-panel.test.tsx` sai com codigo 0 e reporta 4 testes ou mais passando
    - `grep -c "stale" dashboard/src/components/operacao-sttpod-panel.test.tsx` retorna 2 ou mais (presenca e ausencia)
    - `grep -c "isOwner" dashboard/src/components/operacao-sttpod-panel.test.tsx` retorna 2 ou mais
    - `grep -c "queryBy" dashboard/src/components/operacao-sttpod-panel.test.tsx` retorna 1 ou mais
    - `npx vitest run` (suite inteira) sai com codigo 0
  </acceptance_criteria>
  <done>Badge de frescor, historico e gate de UI cobertos por teste automatizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> browser (blob `sttpod.state`) | Conteudo produzido pelo script e renderizado na UI do operador |
| browser (clique) -> server action | Origem do comando manual; a autorizacao real e server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-01 | Elevation of Privilege | botoes do `OperacaoSttPodPanel` | mitigate | Esconder botao e COSMETICO por construcao; a autorizacao e o `requireOwner` de `sttPodCommandCore` (plano 999.3-04). O painel nunca chama o gateway direto — so a server action |
| T-999.3-12 | Tampering | render do blob `state` | accept | React escapa texto por padrao; o painel nao usa `dangerouslySetInnerHTML` (verificavel por grep) e o produtor do blob e um script interno root-only |
| T-999.3-04 | Information Disclosure | bundle do cliente | mitigate | Nenhum segredo importado no painel; os dados chegam via `fetchOperations` (proxy GET server-side que injeta a admin key). `npx vitest run src/lib/gateway.test.ts` guarda a invariante |
| T-999.3-13 | Denial of Service | reclique do botao | mitigate | Estado `pending` desabilita os botoes durante a chamada; no backend o comando e idempotente (sobrescreve a mesma chave Redis) |
| T-999.3-SC | Tampering | npm installs | accept | Nenhuma dependencia nova; radix alert-dialog, sonner e testing-library ja instalados |
</threat_model>

<verification>
- `cd dashboard && npx tsc --noEmit` verde
- `cd dashboard && npx vitest run` verde (suite inteira)
- `cd dashboard && npm run build` verde (mesmo comando do CI `build-dashboard.yml`)
- `grep -rn "dangerouslySetInnerHTML" dashboard/src/components/operacao-sttpod-panel.tsx` sem saida
</verification>

<success_criteria>
A `/operacao` renderiza 5 paineis; o quinto mostra maquina, custo, agenda, upstreams,
machine_avoid, historico e estado do comando, com badge de frescor correto e botoes visiveis
apenas para owner. Build e suite do dashboard verdes.
</success_criteria>

<output>
Create `.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-05-SUMMARY.md` when done
</output>
