# Phase 999.3: Dashboard — card do pod STT/TTS (up→destroy) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-25
**Phase:** 999.3-dashboard-card-do-pod-stt-tts-up-destroy
**Areas discussed:** Todos dobrados, Canal de publicação, Histórico + custo/dia, Botões manuais, Card no dashboard

---

## Todos dobrados

| Option | Description | Selected |
|--------|-------------|----------|
| Só incidents-filter | Dobrar só o todo de dashboard; stt-embed fechar stale; uat fica no backlog | |
| Nenhum, só o card | Fase só com o card do pod | |
| Dobrar os 3 mesmo assim | Mantém os 3; pesquisador/planner revalidam (stt-embed vira verificação, uat vira checklist) | ✓ |

**User's choice:** Dobrar os 3 mesmo assim
**Notes:** Claude sinalizou que stt-embed-metering já consta resolvido (Phase 16) e phase8-9-uat é processo; usuário manteve os 3 mesmo assim.

---

## Canal de publicação

| Option | Description | Selected |
|--------|-------------|----------|
| POST admin | POST /admin/sttpod/state com X-Admin-Key; gateway grava no Redis | ✓ |
| Redis direto | Script escreve na chave via rede interna (exige expor Redis) | |
| Gateway puxa (pull) | Gateway lê state.json do ops-claude | |

**User's choice:** POST admin

| Option | Description | Selected |
|--------|-------------|----------|
| Ensure + eventos | Heartbeat 15min + publish imediato em provision/migrate/destroy/falha | ✓ |
| Só eventos | Publica só em mudança de estado | |
| Só ensure 15min | Só heartbeat periódico | |

**User's choice:** Ensure + eventos

| Option | Description | Selected |
|--------|-------------|----------|
| Timestamp + badge | published_at no payload, chave sem TTL, badge stale >20min | ✓ |
| TTL na chave | Chave expira ~20min, card fica "sem dados" | |
| TTL + snapshot | Chave viva + chave persistente | |

**User's choice:** Timestamp + badge

| Option | Description | Selected |
|--------|-------------|----------|
| Admin key existente | ifix_admin_****613f em /etc/onboard/secrets/vast-3060.env | ✓ |
| Admin key dedicada | gatewayctl admin-key create --label sttpod-publisher | |
| Endpoint sem auth + IP | Rota interna restrita por IP | |

**User's choice:** Admin key existente

---

## Histórico + custo/dia

| Option | Description | Selected |
|--------|-------------|----------|
| Blob no payload | Lista capped no state.json, gateway guarda JSON no Redis, zero migration | ✓ |
| Tabela Postgres | Tabela nova tipo primary_lifecycles (migration + sqlc) | |
| Redis list separada | LPUSH+LTRIM em gw:sttpod:events | |

**User's choice:** Blob no payload

| Option | Description | Selected |
|--------|-------------|----------|
| Últimos 50 eventos | Cap por contagem | ✓ |
| 30 dias | Cap por idade | |
| Tudo | Sem cap | |

**User's choice:** Últimos 50 eventos

| Option | Description | Selected |
|--------|-------------|----------|
| Script acumula | Custo dia/mês acumulado no state.json, payload pronto | ✓ |
| Dashboard deriva | Front calcula dos eventos | |
| Só $/h + uptime | Sem custo/dia | |

**User's choice:** Script acumula

| Option | Description | Selected |
|--------|-------------|----------|
| BRL + USD entre parênteses | Consistente com cost panel do primário | ✓ |
| Só USD | Rate nativo Vast | |
| Só BRL | Esconde $/h de comparação | |

**User's choice:** BRL + USD entre parênteses

---

## Botões manuais

| Option | Description | Selected |
|--------|-------------|----------|
| Sim, comando pendente | POST /admin/sttpod/command → Redis → script consome | ✓ |
| Deferir | Fase só visibilidade | |
| Só destroy | Só botão de emergência | |

**User's choice:** Sim, comando pendente

| Option | Description | Selected |
|--------|-------------|----------|
| Poll dedicado 1-2min | Timer novo só pra comando pendente | ✓ |
| No ensure 15min | Zero timer novo, até 15min de atraso | |
| Você decide | Claude escolhe no planejamento | |

**User's choice:** Poll dedicado 1-2min

| Option | Description | Selected |
|--------|-------------|----------|
| Owner-only + confirm | Padrão Phase 18 + admin_audit_log | ✓ |
| Qualquer admin logado | Sem gate de role | |

**User's choice:** Owner-only + confirm

| Option | Description | Selected |
|--------|-------------|----------|
| Estado no payload | Script ecoa pending_command/last_command_result | ✓ |
| Só toast otimista | Toast "enviado", cego a falha | |
| Polling do comando | Endpoint extra de status | |

**User's choice:** Estado no payload

---

## Card no dashboard

| Option | Description | Selected |
|--------|-------------|----------|
| Estender /admin/operations | Campo sttpod na resposta agregada; 5º painel na /operacao | ✓ |
| Endpoint + seção próprios | GET /admin/sttpod/state + useQuery próprio | |
| Página /sttpod dedicada | Página nova na sidebar | |

**User's choice:** Estender /admin/operations

| Option | Description | Selected |
|--------|-------------|----------|
| machine_avoid list | Blocklist do script visível | ✓ |
| Histórico provisions/falhas | Tabela dos últimos eventos | ✓ |
| Status upstreams STT/TTS | Breakers repetidos no card (redundância proposital) | ✓ |
| Agenda 07-22 + próx. transição | Janela diária + countdown | ✓ |

**User's choice:** Todos os 4 (multiSelect)

| Option | Description | Selected |
|--------|-------------|----------|
| Tabela estilo timeline | Reusa padrão operacao-lifecycle-timeline | ✓ |
| Lista compacta | Últimos 5-10 eventos, "ver mais" | |
| Você decide | Claude escolhe pelo espaço | |

**User's choice:** Tabela estilo timeline

---

## Claude's Discretion

- Formato exato do payload JSON (nomes de campos).
- Detalhe visual/espaçamento do 5º painel.

## Deferred Ideas

None.
