---
phase: 999.3-dashboard-card-do-pod-stt-tts-up-destroy
plan: 07
type: execute
wave: 3
depends_on: ["999.3-02", "999.3-03", "999.3-05"]
files_modified: []
autonomous: false
requirements: []
user_setup:
  - service: ifix-ai-gateway (admin key existente)
    why: "O script precisa autenticar no POST /admin/sttpod/state (D-04)"
    env_vars:
      - name: GW_ADMIN_KEY
        source: "Admin key ifix_admin_****613f ja existente — adicionar a linha em /etc/onboard/secrets/vast-3060.env (root-600) no ops-claude, via sudo"
    dashboard_config:
      - task: "Atualizar a imagem do stack 38 (ai-gateway-prod) preservando as 76 env vars"
        location: "Portainer portainer3.ifixtelecom.com.br, endpoint 6 (worker-vm)"
      - task: "Atualizar a imagem do stack 40 (ai-gateway-dashboard)"
        location: "Portainer portainer3.ifixtelecom.com.br, endpoint 6 (worker-vm)"

must_haves:
  truths:
    - "O card do pod aparece populado na pagina /operacao em producao com dados reais do script"
    - "O botao destruir, clicado por um owner, derruba o pod no Vast em ate 2 minutos"
    - "O timer de poll de 2 minutos esta ativo no ops-claude dentro da janela 07-22"
  artifacts: []
  key_links:
    - from: "/opt/vast-3060/vast3060.py"
      to: "gateway POST /admin/sttpod/state"
      via: "execucao manual de vast3060.py poll retornando HTTP 200 no log"
      pattern: "publish -> HTTP 200"
---

<objective>
Colocar as tres frentes em producao e provar o ciclo completo ao vivo: script publica, card
popula, botao executa.

Purpose: as tres frentes so valem juntas. O script roda de `/opt/vast-3060/vast3060.py`
(nao do repo) via systemd, o gateway roda do stack 38 digest-pinado e o dashboard do stack
40 — nenhum dos tres pega o codigo novo por merge. Sem este plano o card fica "sem
publicacao" para sempre (RESEARCH Pitfall 7).
Output: pre-flight registrado, deploy das 3 frentes e UAT E2E aprovado pelo operador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-CONTEXT.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-02-SUMMARY.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-03-SUMMARY.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-05-SUMMARY.md

<interfaces>
Alvos de deploy (RESEARCH secao Runtime State Inventory):

  gateway    Portainer stack 38 `ai-gateway-prod`, endpoint 6 (worker-vm), imagem
             digest-pinada. O PUT do stack precisa PRESERVAR as 76 env vars — alterar
             apenas a linha da imagem (memoria gateway-prod-deploy-mechanism).
  dashboard  Portainer stack 40 `ai-gateway-dashboard`, mesmo endpoint.
  script     `cp ops/vast-3060/vast3060.py /opt/vast-3060/vast3060.py` no ops-claude.
             As units systemd apontam para /opt, NAO para o repo.
  units      copiar `ops/vast-3060/vast-3060-poll.{service,timer}` para
             `/etc/systemd/system/`, depois `systemctl daemon-reload` e
             `systemctl enable --now vast-3060-poll.timer`.
  secret     adicionar `GW_ADMIN_KEY=<admin key ifix_admin_****613f>` em
             `/etc/onboard/secrets/vast-3060.env` (root-600, ja tem 7 chaves).
  limpeza    `systemctl reset-failed vast-3060-start.service` — unit fantasma do modelo
             stop/start antigo, ja removida do disco mas com estado failed persistente.

Questao aberta a resolver no pre-flight (RESEARCH Assumption A2 / Open Question 1):
  `/admin/*` e alcancavel pelo edge publico `https://ai-gateway.converse-ai.app`?
  O CLAUDE.md documenta `curl -H "X-Admin-Key: ..." https://ai-gateway.converse-ai.app/admin/metrics`,
  mas isso nao foi testado nesta fase. Se falhar, o `EDGE` do publish precisa apontar para
  `http://10.10.10.50:80` com header Host — o script esta na mesma rede NAT.

Estado atual do `/var/lib/vast-3060/state.json` (verificado 2026-07-31): 7 campos, sem
nenhum dos campos novos. Todas as leituras novas usam default, entao nao ha migracao
manual — o arquivo se completa sozinho na primeira publicacao.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight nao-mutante do deploy</name>
  <files>nenhum — task de leitura e verificacao, sem escrita fora do SUMMARY</files>
  <read_first>
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md secoes Runtime State Inventory, Assumptions Log (A1 e A2) e Open Questions 1 e 4
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-03-SUMMARY.md — confirmar o nome do comando (`poll`) e das units geradas
  </read_first>
  <action>
    Executar apenas checagens de leitura, sem alterar nada em producao, e registrar cada
    resultado no SUMMARY do plano (a Regra de Ouro do projeto se aplica: o que nao for
    observado vai marcado como HIPOTESE):
    (1) Resolver a Open Question 1: `curl` de `GET /admin/metrics` em
        `https://ai-gateway.converse-ai.app` com o header `X-Admin-Key` da admin key
        existente, imprimindo SOMENTE o codigo HTTP. Registrar se o edge publico serve
        `/admin/*`. Se nao servir, registrar no SUMMARY que o `EDGE` do publish precisa
        virar `http://10.10.10.50:80` com header Host e sinalizar isso como bloqueio para a
        Task 2.
    (2) `diff` entre `ops/vast-3060/vast3060.py` do repo e `/opt/vast-3060/vast3060.py` —
        confirmar que ha delta (o codigo novo ainda nao esta deployado) e que o arquivo de
        destino existe.
    (3) `sudo grep -c '^GW_ADMIN_KEY=' /etc/onboard/secrets/vast-3060.env` — apenas a
        contagem, NUNCA o valor. Zero significa que o passo manual de secret e obrigatorio
        antes do primeiro publish.
    (4) `systemctl list-units 'vast-3060*' --all` e `systemctl list-timers 'vast-3060*'` —
        registrar as units existentes, confirmar que `vast-3060-poll.timer` ainda nao existe
        e confirmar a unit fantasma `vast-3060-start.service` em estado failed.
    (5) Confirmar o estado das suites: `go build ./...`, `go test ./gateway/... -count=1`,
        `cd dashboard && npx vitest run && npm run build`, `python3 -m pytest ops/vast-3060/test_vast3060.py -q`.
        Nenhuma das 4 pode estar vermelha antes do deploy.
    NAO copiar arquivo, NAO reiniciar servico, NAO fazer PUT em stack nesta task.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && go build ./... && go test ./gateway/internal/admin/ -count=1 && python3 -m pytest ops/vast-3060/test_vast3060.py -q && cd dashboard && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` sai com codigo 0
    - `go test ./gateway/internal/admin/ -count=1` sai com codigo 0
    - `python3 -m pytest ops/vast-3060/test_vast3060.py -q` sai com codigo 0
    - `npx vitest run` e `npm run build` no diretorio dashboard saem com codigo 0
    - O SUMMARY registra o codigo HTTP obtido em `/admin/metrics` pelo edge publico e a decisao de EDGE resultante
    - O SUMMARY registra se `GW_ADMIN_KEY` ja existe no arquivo de secrets (contagem, nunca o valor)
    - O SUMMARY registra a lista de units `vast-3060*` observadas antes do deploy
    - `git status --porcelain` nao mostra alteracao em nenhum arquivo fora de `.planning/` (task nao-mutante)
  </acceptance_criteria>
  <done>Todas as suites verdes e as 4 incognitas de ambiente (edge, delta do /opt, secret, units) resolvidas por observacao.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy das 3 frentes e UAT E2E do card e do botao</name>
  <action>Executar o roteiro de deploy e verificacao descrito em how-to-verify, na ordem dada (secret, gateway stack 38, dashboard stack 40, script e units no ops-claude, smoke do publish, card, botao, operator), parando e reportando em qualquer passo que falhe. Registrar no SUMMARY: codigo HTTP do smoke de publish, o que o card exibiu, o resultado do clique em destruir e em provisionar, a linha de admin_audit_log observada, e a confirmacao de que a conta operator nao ve os botoes.</action>
  <what-built>
    Tres frentes prontas e verdes no repositorio:
    - Gateway: `POST /admin/sttpod/state` (grava o blob no Redis e devolve o comando pendente
      via GETDEL) e `POST /admin/sttpod/command` (whitelist provision/destroy), mais o campo
      `sttpod` fail-soft no `GET /admin/operations`.
    - Script `ops/vast-3060/vast3060.py`: publish best-effort em 6 pontos de evento e no
      heartbeat, comando `poll`, acumulador de custo com guarda do gap noturno, historico
      capped em 50, e as units `vast-3060-poll.{service,timer}` versionadas no repo.
    - Dashboard: quinto painel "STT/TTS pod" na `/operacao` com custo, agenda, upstreams,
      machine_avoid, historico e botoes provisionar/destruir owner-gated com dialogo de
      confirmacao e auditoria.
    Nada disso esta em producao ainda: o gateway roda do stack 38 digest-pinado, o dashboard
    do stack 40, e o script executa de `/opt/vast-3060/vast3060.py` — nenhum pega o codigo
    novo por merge.
  </what-built>
  <how-to-verify>
    Ordem sugerida: secret, script, gateway, dashboard, E2E. Parar e reportar em qualquer
    passo que falhe.

    1. Secret (uma vez): como root no ops-claude, adicionar a linha `GW_ADMIN_KEY=` com a
       admin key `ifix_admin_****613f` em `/etc/onboard/secrets/vast-3060.env`. Manter o
       arquivo root-600 e nao commitar nada.
    2. Gateway: build da imagem a partir do `develop` e PUT no stack 38 do Portainer
       (endpoint 6) alterando APENAS a linha da imagem — as 76 env vars precisam ser
       preservadas. Depois confirmar `GET /health` respondendo 200.
    3. Dashboard: mesma mecanica no stack 40.
    4. Script: `cp ops/vast-3060/vast3060.py /opt/vast-3060/vast3060.py`; copiar
       `ops/vast-3060/vast-3060-poll.service` e `vast-3060-poll.timer` para
       `/etc/systemd/system/`; `systemctl daemon-reload`;
       `systemctl enable --now vast-3060-poll.timer`;
       `systemctl reset-failed vast-3060-start.service` (limpa a unit fantasma).
    5. Smoke do publish: rodar `python3 /opt/vast-3060/vast3060.py poll` a mao e conferir a
       linha `publish -> HTTP 200` em `/var/log/vast-3060-sched.log`. Qualquer outro codigo
       (401, 404, 000) e bloqueio: 401 significa admin key errada, 404 significa que o edge
       nao serve `/admin/*` e o `EDGE` do publish precisa virar `http://10.10.10.50:80`.
    6. Card: abrir `https://ai-dashboard.converse-ai.app/operacao` logado como owner e
       conferir o quinto painel. Esperado: maquina e geo do pod atual, custo/hora, ip e
       porta, uptime, custo do dia e do mes em BRL com USD entre parenteses, janela 07-22
       com a proxima transicao, badges de `local-stt` e `kokoro-tts`, lista de maquinas
       evitadas, e o historico com os eventos ja registrados. O badge de frescor deve
       mostrar horario recente, nao "stale".
    7. Botao (executa de verdade, gasta dinheiro): clicar "Destruir pod", confirmar no
       dialogo. Esperado, dentro de ate 2 minutos: o card passa de "comando pendente" para
       "destroy executado as HH:MM", o pod some no console da Vast, e uma linha nova aparece
       em `admin_audit_log` com `action = sttpod.destroy`. Em seguida clicar "Provisionar
       agora" para restaurar o pod e confirmar que ele volta (o `ensure` das 15min tambem
       restauraria, mas o teste do botao de provision e o objetivo aqui).
    8. Operator: entrar com uma conta de role operator e confirmar que o painel aparece
       IGUAL mas SEM os dois botoes.
    9. Rollback se algo quebrar: reverter o stack 38 e o 40 para o digest anterior no
       Portainer; `systemctl disable --now vast-3060-poll.timer`; restaurar a copia anterior
       de `/opt/vast-3060/vast3060.py`. O `state.json` sobrevive: campos novos sao ignorados
       pelo codigo velho.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que falhou (passo, codigo HTTP, o que o card mostrou)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador -> `/etc/onboard/secrets/vast-3060.env` | Admin key de PROD adicionada a arquivo root-600 |
| operador -> Portainer stacks 38 e 40 | PUT que pode apagar env vars de producao se feito errado |
| dashboard -> pod real da Vast | O botao destroy gasta dinheiro e derruba STT e TTS de verdade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-17 | Denial of Service | PUT no stack 38 | mitigate | Instrucao explicita de alterar APENAS a linha da imagem preservando as 76 env vars; passo 2 exige `GET /health` 200 antes de seguir; passo 9 define o rollback por digest anterior |
| T-999.3-18 | Information Disclosure | `GW_ADMIN_KEY` | mitigate | Linha adicionada em arquivo root-600 pelo operador; a Task 1 so conta ocorrencias e nunca imprime o valor; nada da key entra em commit |
| T-999.3-01 | Elevation of Privilege | botao destroy em PROD | mitigate | Passo 8 do checkpoint exige verificar com conta operator que os botoes NAO aparecem; o gate real (`requireOwner`) ja e provado por teste no plano 999.3-04 |
| T-999.3-19 | Denial of Service | destroy do pod no UAT | accept | O destroy derruba STT e TTS ate o proximo provision; risco aceito e limitado pelo passo 7, que manda reprovisionar em seguida — mesmo efeito do ciclo diario das 22h |
| T-999.3-SC | Tampering | imagens de container | mitigate | Stacks 38 e 40 sao digest-pinados; o deploy troca digest conhecido, sem tag movel |
</threat_model>

<verification>
- Pre-flight: 4 suites verdes (go build, go test, vitest+build, pytest)
- `/var/log/vast-3060-sched.log` contem `publish -> HTTP 200` apos o smoke manual
- `systemctl list-timers 'vast-3060*'` lista `vast-3060-poll.timer` ativo com proximo disparo dentro de 2 minutos na janela
- `systemctl --failed` nao lista mais `vast-3060-start.service`
- Card visivel e populado em `https://ai-dashboard.converse-ai.app/operacao`
- Linha em `admin_audit_log` com `action = sttpod.destroy` apos o clique
- Aprovacao explicita do operador no checkpoint
</verification>

<success_criteria>
O ciclo completo funciona em producao: o script publica a cada 2 minutos, o card mostra o
estado real do pod com custo e historico, e o botao destroy executado por um owner derruba o
pod em ate 2 minutos deixando rastro em auditoria. Operator ve o card sem botoes.
</success_criteria>

<output>
Create `.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-07-SUMMARY.md` when done
</output>
