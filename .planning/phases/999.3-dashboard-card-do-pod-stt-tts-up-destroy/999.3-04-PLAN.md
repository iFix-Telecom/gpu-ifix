---
phase: 999.3-dashboard-card-do-pod-stt-tts-up-destroy
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/lib/gateway.ts
  - dashboard/src/lib/admin-actions-core.ts
  - dashboard/src/lib/admin-actions.ts
  - dashboard/src/lib/admin-actions.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Um operator que tenta disparar provision ou destroy e rejeitado sem nenhuma chamada ao gateway e sem nenhuma linha de auditoria"
    - "Um owner que dispara o comando gera exatamente 1 POST no gateway e exatamente 1 linha em admin_audit_log"
    - "Um comando fora da whitelist provision/destroy e rejeitado antes de qualquer chamada ao gateway"
    - "A admin key do gateway continua confinada a exatamente 2 arquivos do dashboard"
  artifacts:
    - path: "dashboard/src/lib/gateway.ts"
      provides: "Tipos OperationsSttPod espelhando a struct Go"
      contains: "OperationsSttPod"
    - path: "dashboard/src/lib/admin-actions-core.ts"
      provides: "sttPodCommandCore owner-gated com audit"
      exports: ["sttPodCommandCore"]
    - path: "dashboard/src/lib/admin-actions.ts"
      provides: "Server action publica sttPodCommand"
      exports: ["sttPodCommand"]
  key_links:
    - from: "dashboard/src/lib/admin-actions-core.ts"
      to: "gateway POST /admin/sttpod/command"
      via: "gatewayAdminPost(\"sttpod/command\", { command })"
      pattern: "sttpod/command"
    - from: "dashboard/src/lib/admin-actions-core.ts"
      to: "admin_audit_log"
      via: "writeAuditLog action sttpod.provision / sttpod.destroy"
      pattern: "sttpod\\.(provision|destroy)"
---

<objective>
Dar ao dashboard o tipo do campo `sttpod` e a acao owner-gated que enfileira o comando
manual no gateway (D-08/D-11/D-13).

Purpose: D-11 exige o padrao da Phase 18 sem desvio — `requireOwner` como PRIMEIRO await,
validacao server-side ANTES da chamada ao gateway, exatamente 1 linha de auditoria. O gate
de UI e cosmetico por construcao; o gate real vive aqui.
Output: interfaces `OperationsSttPod*` em `gateway.ts`, `sttPodCommandCore` em
`admin-actions-core.ts`, wrapper `sttPodCommand` em `admin-actions.ts`, 3 testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-CONTEXT.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-PATTERNS.md
@dashboard/src/lib/admin-actions-core.ts
@dashboard/src/lib/gateway.ts

<interfaces>
Shape que o gateway serve em `GET /admin/operations` (fonte de verdade =
`admin.SttPodSection` em `gateway/internal/admin/operations.go`, plano 999.3-02):

  sttpod?: {
    state: <blob publicado pelo script> | null
    pending_command: { command: "provision" | "destroy", requested_at: string } | null
    usd_to_brl: number
  }

Shape do blob `state` (fonte de verdade = `build_payload` em `ops/vast-3060/vast3060.py`,
plano 999.3-03) — os mesmos campos, na mesma ordem:

  published_at: string                         RFC3339 com offset
  window: { start_h: number; end_h: number }
  instance_id: number | null
  machine_id: number | null
  geolocation: string | null
  dph: number | null
  ip: string | null
  port: number | null
  provisioned_at: string | null
  machine_avoid: number[]
  alerted_no_offer: boolean
  cost: { day: string; day_usd: number; month: string; month_usd: number }
  events: Array<{ ts: string; kind: string; result: string; machine_id: number | null;
                  dph: number | null; geolocation: string | null;
                  duration_s: number | null; detail: string }>
  last_command_result: { command: string; status: string; received_at: string;
                         finished_at: string | null; detail: string } | null

Assinaturas ja existentes que este plano CONSOME (nao editar os arquivos de origem):

  dashboard/src/lib/gateway-admin.ts:163
    export async function gatewayAdminPost<T = void>(path: string, body: unknown): Promise<T>
    Path SEM barra inicial e SEM o prefixo /admin. Ex: "tenants", "sttpod/command".
    NUNCA editar gateway-admin.ts — o leak-guard de gateway.test.ts exige a admin key em
    EXATAMENTE 2 arquivos (route.ts + gateway-admin.ts).

  dashboard/src/lib/admin-actions-core.ts:136
    export async function requireOwner(actor?: Actor, authInstance?: AuthLike): Promise<{ actor: Actor; h: Headers }>
  dashboard/src/lib/admin-actions-core.ts:179
    export async function writeAuditLog(args: { db?, actor: {id?, email?}, target?, action: string, metadata?: Record<string, unknown> | null }): Promise<void>
  dashboard/src/lib/admin-actions-core.ts:667
    export interface TenantMutationDeps { post?: typeof realGatewayAdminPost }

Molde 1:1 a clonar: `createTenantCore` (admin-actions-core.ts:685-723) + o wrapper
`createTenant` (admin-actions.ts:152-159) + os 3 testes TEN-UI-08 (admin-actions.test.ts:650-730).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos OperationsSttPod em gateway.ts</name>
  <files>dashboard/src/lib/gateway.ts</files>
  <read_first>
    - dashboard/src/lib/gateway.ts linhas 190-280 (bloco Operations com o cabecalho que declara o Go como fonte de verdade, interfaces por secao, e o `OperationsResponse` agregado) e 424-528 (`proxyGet` e `fetchOperations`)
    - gateway/internal/admin/operations.go (struct `SttPodSection` criada no plano 999.3-02)
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-PATTERNS.md secao dashboard/src/lib/gateway.ts
  </read_first>
  <action>
    Estender o bloco `/admin/operations` de `dashboard/src/lib/gateway.ts` com 4 interfaces
    exportadas, cada uma com doc-comment de uma linha apontando a fonte de verdade
    (o repo ja usa esse padrao: "Mirrors `admin.BreakerRow`"):
    - `OperationsSttPodEvent` — um evento do historico, campos exatamente como o bloco
      interfaces (`ts`, `kind`, `result`, `machine_id`, `dph`, `geolocation`, `duration_s`,
      `detail`), nullables como `T | null`.
    - `OperationsSttPodCommandResult` — `command`, `status`, `received_at`, `finished_at`,
      `detail`.
    - `OperationsSttPodState` — o blob publicado, com doc dizendo que a fonte de verdade e
      `build_payload` em `ops/vast-3060/vast3060.py` (NAO o Go: o gateway trata como opaco).
    - `OperationsSttPod` — `state: OperationsSttPodState | null`,
      `pending_command: { command: string; requested_at: string } | null`,
      `usd_to_brl: number`, com doc dizendo que espelha `admin.SttPodSection`.
    Adicionar `sttpod?: OperationsSttPod;` ao final da interface `OperationsResponse`, com
    comentario de que o campo e opcional porque o handler Go usa `omitempty` — ausente
    significa "sem publicacao ainda", estado distinto de `state: null`.
    NAO criar funcao de fetch nova: o campo chega no `fetchOperations()` existente (D-13,
    uma fetch e um poll).
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/dashboard && npx tsc --noEmit && npx vitest run src/lib/gateway.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` no diretorio dashboard sai com codigo 0
    - `grep -c "OperationsSttPod" dashboard/src/lib/gateway.ts` retorna 5 ou mais
    - `grep -c "sttpod?: OperationsSttPod" dashboard/src/lib/gateway.ts` retorna 1
    - `grep -c "export function fetchSttPod\|fetchSttpod" dashboard/src/lib/gateway.ts` retorna 0 (nenhuma fetch nova)
    - `npx vitest run src/lib/gateway.test.ts` passa (leak-guard da admin key intacto)
  </acceptance_criteria>
  <done>Contrato TS espelha a struct Go e o payload do script; o card consome o poll existente.</done>
</task>

<task type="auto">
  <name>Task 2: sttPodCommandCore owner-gated + wrapper server action</name>
  <files>dashboard/src/lib/admin-actions-core.ts, dashboard/src/lib/admin-actions.ts</files>
  <read_first>
    - dashboard/src/lib/admin-actions-core.ts linhas 126-206 (`requireOwner` com a docstring que explica por que NAO e server action, e `writeAuditLog`), 655-775 (cabecalho do bloco TEN-UI que fixa a ordem de operacoes, `TenantMutationDeps`, whitelist `KEY_DATA_CLASSES`, `createTenantCore` verbatim) e 823-841 (`safeRevalidateTenants` com try/catch)
    - dashboard/src/lib/admin-actions.ts linhas 1-40 (invariantes de seguranca do arquivo) e 120-177 (wrappers TEN-UI)
    - dashboard/src/lib/gateway-admin.ts linhas 140-168 (assinatura de `gatewayAdminPost`) — LER, NAO EDITAR
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md Pattern 4
  </read_first>
  <action>
    Em `dashboard/src/lib/admin-actions-core.ts`, adicionar um bloco novo com cabecalho no
    molde do bloco TEN-UI (comentario de caixa nomeando a fase 999.3, a decisao D-11 e a
    ordem nao-negociavel: requireOwner primeiro, validacao server-side antes do gateway,
    exatamente 1 audit, revalidate por ultimo):
    - `const STTPOD_COMMANDS = new Set(["provision", "destroy"]);` — whitelist fechada,
      paridade com a whitelist do handler Go do plano 999.3-01.
    - `export async function sttPodCommandCore(args: { actor?: Actor; auth?: AuthLike; db?: unknown; command: string; machineId?: number | null; deps?: TenantMutationDeps }): Promise<void>`
      clonando `createTenantCore` 1:1: resolve `authInstance`, faz
      `const { actor } = await requireOwner(args.actor, authInstance)` como PRIMEIRO await;
      normaliza `const command = (args.command ?? "").trim()`; se
      `!STTPOD_COMMANDS.has(command)` lanca
      `new Error('Comando invalido - use "provision" ou "destroy".')` ANTES de qualquer
      chamada de rede; obtem `const post = args.deps?.post ?? realGatewayAdminPost`; chama
      `await post("sttpod/command", { command })`; chama `writeAuditLog` UMA unica vez com
      `action: "sttpod." + command` e `metadata: { command, machine_id: args.machineId ?? null }`
      (sem nenhum segredo; o `machine_id` identifica o pod alvo no incident log — sugestao
      Gemini REVIEWS.md; e informativo/best-effort, vindo do state exibido no painel);
      termina chamando `safeRevalidateOperacao()`.
    - `function safeRevalidateOperacao(): void` no molde de `safeRevalidateTenants`
      (linhas 831-841): `revalidatePath("/operacao")` dentro de try/catch vazio com o mesmo
      comentario explicando que fora do escopo de request do Next (teste unitario) nao ha o
      que revalidar.
    Reusar `TenantMutationDeps` — nao criar interface de deps nova para um unico seam.

    Em `dashboard/src/lib/admin-actions.ts`, adicionar o wrapper thin no molde dos wrappers
    TEN-UI: `export async function sttPodCommand(args: { command: string; machineId?: number | null }): Promise<void>`
    que faz `const { actor } = await requireOwner();` SEM argumento (forca o caminho da
    sessao, CR-01) e delega para
    `sttPodCommandCore({ actor, command: args.command, machineId: args.machineId })`.
    A assinatura publica NAO pode ter `actor`, `auth`, `db` nem `deps` — invariante do
    arquivo (linhas 8-28). NAO exportar `requireOwner`, `writeAuditLog` nem
    `sttPodCommandCore` deste modulo (CR-02).
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/dashboard && npx tsc --noEmit && npx vitest run src/lib/gateway.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com codigo 0
    - `grep -c "STTPOD_COMMANDS" dashboard/src/lib/admin-actions-core.ts` retorna 2
    - `grep -c "sttpod/command" dashboard/src/lib/admin-actions-core.ts` retorna 1
    - `grep -c "writeAuditLog" dashboard/src/lib/admin-actions-core.ts` aumenta em exatamente 1 vs o valor pre-mudanca
    - `grep -c "safeRevalidateOperacao" dashboard/src/lib/admin-actions-core.ts` retorna 2
    - `grep -c "export async function sttPodCommand" dashboard/src/lib/admin-actions.ts` retorna 1
    - `sed -n '/export async function sttPodCommand/,/^}/p' dashboard/src/lib/admin-actions.ts | grep -c "requireOwner()"` retorna 1 (chamada sem argumento)
    - `git diff --name-only dashboard/src/lib/gateway-admin.ts` nao lista o arquivo (nao editado)
    - `npx vitest run src/lib/gateway.test.ts` passa (leak-guard verde: admin key em 2 arquivos)
  </acceptance_criteria>
  <done>Comando manual so sai do dashboard com sessao de owner, validado server-side e auditado.</done>
</task>

<task type="auto">
  <name>Task 3: Testes de autorizacao, auditoria e whitelist</name>
  <files>dashboard/src/lib/admin-actions.test.ts</files>
  <read_first>
    - dashboard/src/lib/admin-actions.test.ts linhas 640-735 (os 3 testes TEN-UI-08 e os helpers `importAdminActions`, `freshDb`, `gatewayAdminPostMock`, e o `beforeEach` que faz `gatewayAdminPostMock.mockReset()`)
    - dashboard/src/lib/admin-actions-core.ts (`sttPodCommandCore` criado na Task 2)
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-VALIDATION.md linha D-11 do mapa por tarefa
  </read_first>
  <action>
    Adicionar ao `dashboard/src/lib/admin-actions.test.ts` um `describe` novo para a fase
    999.3, reusando os helpers ja existentes no arquivo (NAO criar mocks novos):
    - Teste "operator rejeitado": chamar `sttPodCommandCore` com
      `{ actor: { role: "operator" }, db, command: "destroy" }`; asserir que a promessa
      rejeita, que `gatewayAdminPostMock` NAO foi chamado, e que `db.adminAuditLog.length`
      e 0 — as 3 asercoes juntas sao o contrato (requireOwner lanca PRIMEIRO).
    - Teste "owner: 1 POST + 1 audit": actor owner com `id` e `email`, `command: "destroy"`,
      `machineId: 143726`; asserir `gatewayAdminPostMock` chamado exatamente 1 vez, que o
      primeiro argumento e a string `"sttpod/command"`, que o segundo casa com
      `{ command: "destroy" }` (machine_id NAO vai pro gateway — so pro audit), que
      `db.adminAuditLog.length` e 1, que `row.action` e `"sttpod.destroy"` e que
      `row.metadata` e igual a `{ command: "destroy", machine_id: 143726 }`.
    - Teste "comando fora da whitelist": actor owner com `command: "rm -rf /"`; asserir
      rejeicao, `gatewayAdminPostMock` nao chamado e `db.adminAuditLog.length` igual a 0 —
      a validacao roda ANTES da chamada de rede (T-999.3-02).
    Opcionalmente cobrir `command: "provision"` no teste de owner via `it.each` ou um quarto
    caso curto, para provar que `action` vira `"sttpod.provision"`.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/dashboard && npx vitest run src/lib/admin-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/admin-actions.test.ts` sai com codigo 0 e o numero total de testes do arquivo aumenta em 3 ou mais
    - `grep -c "sttPodCommandCore" dashboard/src/lib/admin-actions.test.ts` retorna 3 ou mais
    - `grep -c "sttpod.destroy" dashboard/src/lib/admin-actions.test.ts` retorna 1 ou mais
    - `grep -c "not.toHaveBeenCalled" dashboard/src/lib/admin-actions.test.ts` aumenta em 2 ou mais (operator + whitelist)
    - `npx vitest run` (suite inteira) sai com codigo 0
  </acceptance_criteria>
  <done>Os 3 comportamentos de seguranca da acao (operator bloqueado, owner auditado 1x, whitelist server-side) provados automaticamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> server action | Clique de UI atravessa a fronteira; a identidade NUNCA vem do cliente, so da sessao |
| server action -> gateway `/admin/*` | A admin key existe apenas no servidor; o browser jamais a ve |
| server action -> `admin_audit_log` | Trilha de quem disparou destroy do pod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-01 | Elevation of Privilege | `sttPodCommandCore` | mitigate | `requireOwner` como PRIMEIRO await; o wrapper publico `sttPodCommand` chama `requireOwner()` SEM argumento, impedindo o caller de afirmar o proprio role (CR-01). Provado pelo teste "operator rejeitado" (0 POST, 0 audit) |
| T-999.3-02 | Tampering | whitelist `STTPOD_COMMANDS` | mitigate | Set fechado validado ANTES de qualquer chamada de rede, em paridade com a whitelist do handler Go (defense-in-depth). Provado pelo teste de comando invalido |
| T-999.3-04 | Information Disclosure | `GATEWAY_ADMIN_KEY` | mitigate | Consumo exclusivo via `gatewayAdminPost`; `gateway-admin.ts` NAO e editado (criterio de aceite verifica o `git diff --name-only`). Leak-guard `gateway.test.ts` roda em toda task |
| T-999.3-06 | Repudiation | `writeAuditLog` | mitigate | Exatamente 1 linha por acao com `action` no namespace pontuado `sttpod.provision` / `sttpod.destroy` e metadata sem segredo; `writeAuditLog` nao e exportado como server action (CR-02) |
| T-999.3-SC | Tampering | npm installs | accept | Nenhuma dependencia nova; `package.json` intacto |
</threat_model>

<verification>
- `cd dashboard && npx tsc --noEmit` verde
- `cd dashboard && npx vitest run` verde (suite inteira, zero regressao)
- `cd dashboard && npm run build` verde
- `git diff --name-only dashboard/src/lib/` lista somente os 4 arquivos deste plano
</verification>

<success_criteria>
O tipo `sttpod` existe e casa com o Go; a acao manual e owner-only, validada server-side e
auditada exatamente uma vez; a admin key continua confinada aos 2 arquivos do leak-guard.
</success_criteria>

<output>
Create `.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-04-SUMMARY.md` when done
</output>
