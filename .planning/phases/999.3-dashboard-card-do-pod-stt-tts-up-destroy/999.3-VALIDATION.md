---
phase: 999.3
slug: dashboard-card-do-pod-stt-tts-up-destroy
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-31
---

# Phase 999.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from 999.3-RESEARCH.md §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (gateway)** | Go stdlib `testing` + miniredis/v2 v2.37.0 |
| **Config file** | none (go test default) |
| **Quick run command** | `go test ./gateway/internal/admin/... -count=1` |
| **Full suite command** | `go test ./gateway/... ./pkg/openai/... ./pod/... -count=1 -race -timeout=5m` |
| **Framework (dashboard)** | vitest 3 + @testing-library/react |
| **Quick run command** | `cd dashboard && npx vitest run src/lib/admin-actions.test.ts` |
| **Full suite command** | `cd dashboard && npx vitest run && npm run build` |
| **Framework (script)** | pytest 9.0.2 (fora do CI, manual) |
| **Quick run command** | `pytest ops/vast-3060/test_vast3060.py` |
| **Estimated runtime** | ~60-120s (Go quick <10s; vitest <30s; pytest <5s) |

---

## Sampling Rate

- **After every task commit:** quick command da frente tocada (Go admin / vitest arquivo / pytest ops)
- **After every plan wave:** `gofmt -l` limpo + `go build ./...` + suíte Go completa + `npx vitest run` + `npm run build`
- **Before `/gsd:verify-work`:** CI build-gateway + build-dashboard verdes; UAT manual do card + botão
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | D-01/D-05 | T-999.3-03 | POST state grava blob verbatim no Redis; body não-JSON → 400, zero writes; MaxBytesReader 256KiB | unit (Go) | `go test ./gateway/internal/admin/ -run TestSttPod` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-09/D-10 | T-999.3-02 | POST command grava; POST state faz GETDEL (2º POST → null); whitelist {provision,destroy} | unit (Go) | `go test ./gateway/internal/admin/ -run TestSttPod` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-13 | — | /admin/operations devolve sttpod verbatim; chave ausente ⇒ campo ausente; erro Redis ⇒ 200 fail-soft | unit (Go) | `go test ./gateway/internal/admin/ -run TestOperations` | ✅ estende operations_test.go | ⬜ pending |
| TBD | TBD | TBD | D-11 | T-999.3-01 | operator → 0 POST + 0 audit; owner → 1 POST + 1 audit; comando fora da whitelist → 0 POST | unit (vitest) | `npx vitest run src/lib/admin-actions.test.ts` | ✅ estende admin-actions.test.ts | ⬜ pending |
| TBD | TBD | TBD | D-03 | — | badge stale renderiza com published_at > 20min | unit (vitest RTL) | `npx vitest run src/components/operacao-sttpod-panel.test.tsx` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-06 | T-999.3-04 | events[] nunca passa de 50 | unit (pytest) | `pytest ops/vast-3060/test_vast3060.py -k events` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-07 | — | acumulador não cobra gap noturno 22→07; rollover dia/mês zera bucket certo | unit (pytest) | `pytest ops/vast-3060/test_vast3060.py -k cost` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | leak-guard | T-999.3-05 | GATEWAY_ADMIN_KEY só em route.ts + gateway-admin.ts | unit (vitest) | `npx vitest run src/lib/gateway.test.ts` | ✅ regressão existente | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `gateway/internal/admin/sttpod_test.go` — POST state/command + GETDEL
- [ ] `ops/vast-3060/test_vast3060.py` — acumulador de custo + cap de eventos (D-06/D-07)
- [ ] `dashboard/src/components/operacao-sttpod-panel.test.tsx` — badge stale + estados vazios (cortável se escopo apertar — painel é formatação)
- [ ] Nenhuma instalação de framework — Go testing, vitest e pytest já disponíveis

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| E2E: script publica → card popula → botão destroy → pod destruído | D-01..D-15 | Envolve pod Vast real + deploy prod (stacks 38/40) + cp pro /opt | Rodar `vast3060.py` com publish, abrir /operacao, verificar card, clicar destroy como owner, confirmar destroy no Vast |
| Todo #2 (stt-embed-metering stale) | folded | SQL contra bd_ai_gateway prod | `SELECT count(*) FROM billing_events WHERE route='stt' AND audio_seconds>0 AND created_at > now()-interval '7 days'` (confirmar schema ai_gateway. vs public. antes) |
| Todo #3 (phase8-9 UAT) | folded | Processo/sign-off | Registrar status atual do sign-off INT-01..06 na verificação |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
