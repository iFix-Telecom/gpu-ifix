---
phase: 999.3-dashboard-card-do-pod-stt-tts-up-destroy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ops/vast-3060/vast3060.py
  - ops/vast-3060/test_vast3060.py
  - ops/vast-3060/vast-3060-poll.service
  - ops/vast-3060/vast-3060-poll.timer
autonomous: true
requirements: []

must_haves:
  truths:
    - "O script publica o estado no gateway a cada ensure e imediatamente em provision, migrate, destroy e falha"
    - "O script consome o comando pendente devolvido pelo publish e executa provision ou destroy"
    - "O custo acumulado nunca cobre o gap noturno 22:00-07:00 e zera no rollover de dia e de mes"
    - "O historico de eventos nunca passa de 50 entradas"
    - "Uma falha de publicacao nunca derruba o provision nem o destroy"
  artifacts:
    - path: "ops/vast-3060/vast3060.py"
      provides: "publish, poll, accrue_cost, record_event, build_payload"
      contains: "def publish"
    - path: "ops/vast-3060/test_vast3060.py"
      provides: "pytest das funcoes puras (custo + cap de eventos)"
      contains: "def test_"
    - path: "ops/vast-3060/vast-3060-poll.timer"
      provides: "Timer systemd de 2min na janela 07-21:59"
      contains: "OnCalendar"
  key_links:
    - from: "ops/vast-3060/vast3060.py"
      to: "gateway POST /admin/sttpod/state"
      via: "http_json com header X-Admin-Key"
      pattern: "admin/sttpod/state"
    - from: "ops/vast-3060/vast3060.py"
      to: "/var/lib/vast-3060/state.json"
      via: "save_state com campos novos"
      pattern: "last_command_result"
---

<objective>
Fazer o `vast3060.py` publicar o estado do pod no gateway e consumir comandos manuais,
acumulando custo e historico no proprio `state.json` (D-02/D-05/D-06/D-07/D-10/D-12).

Purpose: o script e o unico processo que conhece o pod 3060 (D-01 proibe o gateway de
fazer pull). Toda a semantica — custo acumulado, historico, agenda, uptime — mora aqui;
o gateway e pipe e o dashboard so formata.
Output: `publish()` best-effort em 6 pontos de evento, comando `poll`, acumulador de custo
com guarda de gap noturno, `events[]` capped em 50, pytest das funcoes puras, e as duas
units systemd do timer de 2min.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-CONTEXT.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-PATTERNS.md
@.planning/quick/260724-ktg-3060-auto-migrate/SUMMARY.md
@ops/vast-3060/vast3060.py

<interfaces>
CONTRATO DO PAYLOAD — fixado aqui, espelhado em TS pelo plano 999.3-04.

POST para `{EDGE}/admin/sttpod/state`, header `X-Admin-Key` = `GW_ADMIN_KEY` do
`/etc/onboard/secrets/vast-3060.env`. Body montado campo a campo por `build_payload`
(NUNCA o dict `st` inteiro, NUNCA `env`):

  published_at      RFC3339 COM offset, ex "2026-07-31T11:20:00-03:00"
  window            objeto {start_h: 7, end_h: 22}
  instance_id       int ou null
  machine_id        int ou null
  geolocation       string ou null
  dph               float ou null
  ip                string ou null
  port              int ou null
  provisioned_at    RFC3339 ou null
  machine_avoid     array de int
  alerted_no_offer  bool
  cost              objeto {day, day_usd, month, month_usd}
  events            array de objetos {ts, kind, result, machine_id, dph, geolocation, duration_s, detail}
                    kind em: up, destroy, bail, create_fail, edge_fail, no_offer, command
                    result em: ok, fail
  last_command_result  objeto {command, status, received_at, finished_at, detail} ou null
                    status em: running, ok, fail

Resposta HTTP 200 do gateway:
  {"pending_command": {"command": "provision"|"destroy", "requested_at": RFC3339} }
  ou {"pending_command": null}

Helpers JA EXISTENTES no `vast3060.py` — usar, nao reescrever:
  http_json(method, url, headers=None, payload=None, timeout=30) -> (code, dict)   (:105) nunca levanta
  save_state(st)                                                                    (:123) write-tmp + replace
  load_state(), load_env(), log(msg)
  notify(env, text)                                                                 (:161) contrato best-effort a copiar
  provision(env, st, motivo), cmd_ensure, cmd_stop, cmd_status
  constantes EDGE, WINDOW_START_H = 7, WINDOW_END_H = 22, STATE_PATH

Pontos de evento existentes onde o publish entra (todos ja chamam save_state/notify):
  :293-303  no_offer     nenhuma oferta elegivel (dedup por alerted_no_offer)
  :312-315  create_fail  create HTTP != 200
  :319-329  bail         destroi a nova + machine_avoid.append(bad) — fonte do machine_id correlacionavel (D-14)
  :371-379  edge_fail    flip feito mas edge nao validou
  :390-396  up           sucesso, st.update(instance_id, machine_id, ip, port, ...)
  :420-430  destroy      cmd_stop
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Acumulador de custo, historico capped e build_payload/publish</name>
  <files>ops/vast-3060/vast3060.py</files>
  <behavior>
    - accrue_cost com instance_id None NAO soma nada, mas atualiza last_ts (guarda do gap noturno)
    - accrue_cost com instance_id setado e dph 0.03 por 3600s soma 0.03 ao day_usd e ao month_usd
    - accrue_cost com day diferente do bucket zera day_usd e mantem month_usd
    - accrue_cost com mes diferente zera month_usd
    - record_event mantem no maximo 50 entradas, descartando as mais antigas
    - build_payload nao le nada de env (nenhum segredo pode entrar no payload)
  </behavior>
  <read_first>
    - ops/vast-3060/vast3060.py (arquivo inteiro, 459 linhas) — docstring do modulo (:21-24), constantes (:46-56), http_json (:105-116), save_state (:123-127), notify (:161-170 contrato best-effort literal), e os 6 pontos de evento listados no bloco interfaces
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md secoes Pattern 6 (acumulador), Pitfall 4 (gap noturno), Pitfall 6 (publish sem try/except derruba o provision), Pitfall 8 (cap)
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-PATTERNS.md secao ops/vast-3060/vast3060.py
  </read_first>
  <action>
    Editar `ops/vast-3060/vast3060.py`. Python stdlib puro — NAO adicionar `requests`,
    `httpx` nem qualquer dependencia.

    Constantes novas ao lado de `EDGE`: `EVENTS_CAP = 50` (D-06) e `PUBLISH_TIMEOUT = 20`.

    Funcao `now_rfc3339()` retornando `datetime.now().astimezone().isoformat(timespec="seconds")` —
    RFC3339 COM offset. O ops-claude roda em America/Sao_Paulo; timestamp sem offset e a
    classe de bug de shift de dia documentada no dashboard.

    Funcao `accrue_cost(st, now_ts)` conforme RESEARCH Pattern 6: bucket `st["cost"]` criado
    por `setdefault` com as chaves `day`, `day_usd`, `month`, `month_usd`, `last_ts`;
    rollover comparando `%Y-%m-%d` e `%Y-%m` zera o bucket correspondente; soma
    `dph * (now_ts - last_ts) / 3600` SOMENTE quando `last_ts` e `st.get("dph")` existem E
    `st.get("instance_id")` nao e None (guarda do gap noturno, Pitfall 4); `last_ts` e
    atualizado em TODA chamada, inclusive nas que nao acumulam — as duas condicoes juntas sao
    o que impede creditar 9 horas inexistentes no primeiro tick das 07:00. So le e escreve o
    dict recebido, para ser testavel sem I/O.

    Funcao `record_event(st, kind, result, detail="", **fields)` que faz append em
    `st.setdefault("events", [])` de um dict com `ts=now_rfc3339()`, `kind`, `result`,
    `detail` e os opcionais `machine_id`, `dph`, `geolocation`, `duration_s`; trunca com
    `st["events"] = st["events"][-EVENTS_CAP:]`.

    Funcao `build_payload(st)` montando o dicionario do bloco interfaces com literais
    explicitos, campo a campo. NUNCA `dict(st)`, spread do state inteiro ou qualquer leitura
    de `env` — o payload e lido por qualquer operator no dashboard (T-999.3-05).
    `published_at` vem de `now_rfc3339()`; `window` de `WINDOW_START_H`/`WINDOW_END_H`; os
    demais de `st.get(...)` com default None, lista vazia ou False.

    Funcao `publish(env, st)` copiando LITERALMENTE o contrato best-effort do `notify`
    (:161-170): corpo inteiro dentro de `try/except Exception as e` com
    `log("publish FALHOU: ...")` e `return None`. Dentro do try, nesta ordem:
    `accrue_cost(st, time.time())`, `save_state(st)`, `http_json` POST para
    `f"{EDGE}/admin/sttpod/state"` com header `X-Admin-Key` = `env["GW_ADMIN_KEY"]` e
    `timeout=PUBLISH_TIMEOUT`, `log` do codigo HTTP, e retorno de
    `resp.get("pending_command")` quando o codigo for 200 (senao None). O `except` amplo e
    obrigatorio: cobre `KeyError` de `GW_ADMIN_KEY` ausente no host — sem ele um secret
    faltando aborta o provision no meio da migracao (Pitfall 6).

    Instrumentar os 6 pontos de evento com `record_event(...)` seguido de `publish(env, st)`,
    usando os `kind` da tabela do bloco interfaces: `no_offer` result fail; `create_fail`
    result fail; `bail` result fail passando o `machine_id` da maquina que entrou no
    `machine_avoid` (e o dado que responde "por que a oferta barata foi pulada?", D-14);
    `edge_fail` result fail; `up` result ok com `machine_id`, `dph` de `offer["dph_total"]` e
    `geolocation` de `offer["geolocation"]`; `destroy` result ok com `duration_s` derivado de
    `provisioned_at`.

    No ponto `up` (:390-391), estender o `st.update(...)` com `dph` de `offer.get("dph_total")`,
    `geolocation` de `offer.get("geolocation")` e `provisioned_at=now_rfc3339()`.

    No branch `edge_fail` (:371-379), estender o `st.update(...)` com os MESMOS campos
    (`dph`, `geolocation`, `provisioned_at`) — a instancia fica rodando para rollback manual
    e continua cobrando na Vast; sem `dph` o `accrue_cost` acumularia zero e o card mostraria
    $/h vazio para um pod que esta faturando (D-07).

    No `cmd_stop` (:420-430), ORDEM OBRIGATORIA: (1) capturar `provisioned_at` do state numa
    variavel local, (2) computar `duration_s` a partir dela e chamar
    `record_event(st, "destroy", "ok", duration_s=...)`, (3) SO ENTAO limpar `dph`,
    `geolocation` e `provisioned_at` junto do `instance_id=None`. Limpar antes de derivar
    produziria `duration_s=None` silencioso. O clear e necessario: o guard do acumulador
    depende do `instance_id`, e deixar `dph` velho no payload confundiria o card.

    Ao final do `cmd_ensure`, chamar `publish(env, st)` como heartbeat (D-02) INCLUSIVE no
    caminho no-op de pod healthy: e o publish que mantem a chave viva no Redis, que roda sem
    persistencia.

    Atualizar a docstring do modulo (:21-24) listando os campos novos do `state.json`
    (`dph`, `geolocation`, `provisioned_at`, `cost`, `events`, `last_command_result`) e
    reservando a linha do comando `poll` que chega na Task 2.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && python3 -m py_compile ops/vast-3060/vast3060.py && grep -c "def accrue_cost\|def record_event\|def build_payload\|def publish\|def now_rfc3339" ops/vast-3060/vast3060.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile ops/vast-3060/vast3060.py` sai com codigo 0
    - O grep do comando de verify retorna 5 (as 5 funcoes novas existem)
    - `grep -c "record_event(" ops/vast-3060/vast3060.py` retorna 6 ou mais
    - `grep -c "publish(env, st)" ops/vast-3060/vast3060.py` retorna 6 ou mais
    - `grep -c "EVENTS_CAP" ops/vast-3060/vast3060.py` retorna 2 ou mais
    - `grep -c "provisioned_at" ops/vast-3060/vast3060.py` retorna 3 ou mais
    - `grep -c "import requests" ops/vast-3060/vast3060.py` retorna 0
    - Nenhuma leitura de env dentro de build_payload: `sed -n '/def build_payload/,/^def /p' ops/vast-3060/vast3060.py | grep -c "env"` retorna 0
  </acceptance_criteria>
  <done>Estado publicado com custo acumulado e historico capped; nenhuma falha de publicacao escapa para o fluxo de provision.</done>
</task>

<task type="auto">
  <name>Task 2: Comando poll, execucao do comando pendente e last_command_result</name>
  <files>ops/vast-3060/vast3060.py</files>
  <read_first>
    - ops/vast-3060/vast3060.py (apos a Task 1) — main() (:440-455), cmd_ensure (:401-415) incluindo o guard de janela (:406-408), cmd_stop (:418-430), provision(env, st, motivo)
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md Pitfall 3 (corrida entre GETDEL e execucao) e Open Question 2 (janela do poll)
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-CONTEXT.md decisoes D-09, D-10 e D-12
  </read_first>
  <action>
    Ainda em `vast3060.py`, implementando D-09/D-10/D-12.

    Funcao `cmd_poll(env, st)`: aplica o MESMO guard de janela do `cmd_ensure`
    (`h = datetime.now().hour`, retorna cedo se `h < WINDOW_START_H or h >= WINDOW_END_H`) —
    o poll fica alinhado a janela 07-22, decisao registrada em RESEARCH Open Question 2.
    Depois chama `pending = publish(env, st)`; se `pending` for None, retorna; senao chama
    `run_command(env, st, pending)`.

    Funcao `run_command(env, st, pending)`: le `cmd = str(pending.get("command", ""))` e
    despacha por if-chain LITERAL — `provision` chama `provision(env, st, "manual via dashboard")`,
    `destroy` chama `cmd_stop(env, st)`, qualquer outro valor grava resultado `fail` com
    detalhe "comando desconhecido". NUNCA usar `eval`, `exec`, `getattr` dinamico ou
    `subprocess` com o valor recebido do gateway (T-999.3-02).
    Antes de executar, gravar em `st["last_command_result"]` o dict
    `{command, status: "running", received_at: now_rfc3339(), finished_at: None, detail: ""}`
    e chamar `save_state(st)` — assim o card mostra "executando..." e o comando nunca some
    sem rastro se o processo morrer no meio (Pitfall 3, at-most-once).
    Apos a execucao, atualizar `status` para `ok` ou `fail`, preencher `finished_at`, chamar
    `record_event(st, "command", <ok ou fail>, detail=cmd)` e `publish(env, st)` para o card
    refletir "executado as HH:MM" (D-12). Envolver a execucao num `try/except Exception`
    que registra `fail` com a mensagem — uma excecao do provision nao pode deixar o
    `last_command_result` preso em `running`.
    Marcar a simplificacao com um comentario iniciado por `# ponytail:` explicitando o teto
    (at-most-once, GETDEL consome antes da execucao, recovery e o operador reclicar) e o
    upgrade (ACK explicito no gateway).

    Em `main()`: adicionar `"poll"` a tupla de comandos aceitos, a string de uso
    (`uso: vast3060.py ensure|stop|provision|poll|status`) e ao if-chain, despachando para
    `cmd_poll(env, st)`.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && python3 -m py_compile ops/vast-3060/vast3060.py && grep -c "def cmd_poll\|def run_command" ops/vast-3060/vast3060.py && python3 ops/vast-3060/vast3060.py 2>&1 | grep -c poll</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile ops/vast-3060/vast3060.py` sai com codigo 0
    - O grep de funcoes do verify retorna 2 (`cmd_poll` e `run_command` existem)
    - Rodar o script sem argumento imprime a string de uso contendo `poll` (grep do verify retorna 1)
    - `grep -cE "\beval\(|\bexec\(|subprocess" ops/vast-3060/vast3060.py` retorna 0
    - `grep -c "last_command_result" ops/vast-3060/vast3060.py` retorna 3 ou mais
    - `grep -c "ponytail:" ops/vast-3060/vast3060.py` retorna 1 ou mais
    - `grep -c "WINDOW_START_H" ops/vast-3060/vast3060.py` aumenta em pelo menos 1 vs o valor pre-mudanca (guard de janela replicado no poll)
  </acceptance_criteria>
  <done>Botao do dashboard tem latencia de ate 2min; comando executado deixa rastro no state mesmo se o processo morrer.</done>
</task>

<task type="auto">
  <name>Task 3: pytest das funcoes puras + units systemd do timer de 2min</name>
  <files>ops/vast-3060/test_vast3060.py, ops/vast-3060/vast-3060-poll.service, ops/vast-3060/vast-3060-poll.timer</files>
  <read_first>
    - ops/vast-3060/vast3060.py (apos as Tasks 1 e 2) — assinaturas de `accrue_cost` e `record_event`
    - pod/primary/test_chatterbox_shim.py (linhas 1-60) — molde de docstring com autoridade e Run target explicito, `from __future__ import annotations`, fakes locais em vez de mocks de framework
    - scripts/price-sync/gateway-price-sync.service e scripts/price-sync/gateway-price-sync.timer — precedente de unit systemd versionada no repo
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md secao Unidades systemd novas (D-10)
  </read_first>
  <action>
    Criar `ops/vast-3060/test_vast3060.py` com docstring no molde do
    `pod/primary/test_chatterbox_shim.py`: citar a autoridade (Phase 999.3 Plan 03, decisoes
    D-06 e D-07) e o Run target literal `pytest ops/vast-3060/test_vast3060.py`. O modulo
    importa `vast3060` por path (o arquivo tem shebang e nao esta em pacote — usar
    `importlib.util.spec_from_file_location` apontando para o `vast3060.py` ao lado do teste)
    para nao depender de estar instalado em `/opt`. Como o import executa apenas definicoes,
    nao ha I/O no import — nao mockar nada alem disso.
    Testes obrigatorios, todos sobre dicts em memoria (zero I/O, zero rede):
    - `test_cost_no_accrual_without_instance`: state com `instance_id` None, `dph` 0.03 e
      `last_ts` de 9 horas atras; apos `accrue_cost`, `day_usd` continua 0.0 e `last_ts` foi
      atualizado para o `now_ts` passado. Este e o teste do gap noturno 22:00-07:00.
    - `test_cost_accrues_one_hour`: `instance_id` setado, `dph` 0.03, `last_ts` = now - 3600;
      apos `accrue_cost`, `day_usd` e `month_usd` valem aproximadamente 0.03 (`pytest.approx`).
    - `test_cost_day_rollover_resets_day_only`: bucket com `day` de ontem e `month_usd`
      acumulado; apos `accrue_cost` num `now_ts` de hoje, `day_usd` reinicia do zero e
      `month_usd` preserva o acumulado do mes.
    - `test_cost_month_rollover_resets_month`: bucket com `month` do mes anterior; apos
      `accrue_cost`, `month_usd` reinicia.
    - `test_events_capped_at_50`: 60 chamadas a `record_event`; `len(st["events"])` e 50 e a
      entrada mais antiga preservada corresponde a 11a inserida (FIFO por descarte do inicio).
    Nomear os testes de forma que `-k cost` selecione os 4 de custo e `-k events` selecione o
    de cap, conforme o mapa de validacao da fase.

    Criar `ops/vast-3060/vast-3060-poll.service` no molde do
    `/etc/systemd/system/vast-3060-ensure.service`: secao `[Unit]` com Description
    "Pod STT/TTS 3060 - poll de comando pendente + heartbeat (2min)"; secao `[Service]` com
    `Type=oneshot` e `ExecStart=/usr/bin/python3 /opt/vast-3060/vast3060.py poll`. Sem secao
    `[Install]` — o service e acionado pelo timer. NAO setar `StandardOutput`/`StandardError`:
    o default do systemd ja manda stdout/stderr pro journald, que fica como backup do
    LOG_PATH manual (sugestao Gemini REVIEWS.md) — verificavel com
    `journalctl -u vast-3060-poll.service`.

    Criar `ops/vast-3060/vast-3060-poll.timer` no molde do `vast-3060-ensure.timer`:
    `[Unit]` Description "Poll de comando do pod 3060 a cada 2min (07-21h59)"; `[Timer]` com
    `OnCalendar=*-*-* 07..21:00/2:00` e `Persistent=true`; `[Install]` com
    `WantedBy=timers.target`.
    Adicionar em ambos os arquivos um comentario de cabecalho dizendo que o `ExecStart`
    aponta para `/opt/vast-3060/vast3060.py` (copia deployada), NAO para o repo, e que o
    deploy exige `cp` do script + `systemctl daemon-reload`.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && python3 -m pytest ops/vast-3060/test_vast3060.py -q && grep -c "OnCalendar=\*-\*-\* 07..21:00/2:00" ops/vast-3060/vast-3060-poll.timer</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest ops/vast-3060/test_vast3060.py -q` sai com codigo 0 e reporta 5 testes passando
    - `python3 -m pytest ops/vast-3060/test_vast3060.py -k cost -q` seleciona e passa 4 testes
    - `python3 -m pytest ops/vast-3060/test_vast3060.py -k events -q` seleciona e passa 1 teste
    - `ops/vast-3060/vast-3060-poll.service` contem a linha exata `ExecStart=/usr/bin/python3 /opt/vast-3060/vast3060.py poll`
    - `ops/vast-3060/vast-3060-poll.timer` contem `OnCalendar=*-*-* 07..21:00/2:00` e `Persistent=true` (grep do verify retorna 1)
    - Os testes nao fazem rede nem escrita em disco: `grep -cE "urlopen|http_json|save_state|open\(" ops/vast-3060/test_vast3060.py` retorna 0
  </acceptance_criteria>
  <done>As duas logicas com ramificacao real (rollover/guarda de custo e cap de eventos) estao provadas; units do timer versionadas no repo, prontas para o deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> script (`pending_command`) | Valor vindo da rede decide qual acao o script executa como root no ops-claude |
| script -> gateway (`build_payload`) | O payload publicado e legivel por qualquer operator no dashboard |
| script -> `/etc/onboard/secrets/vast-3060.env` | Segredos root-600 no mesmo processo que monta o payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-02 | Tampering | `run_command` em `vast3060.py` | mitigate | If-chain literal comparando com as strings `provision` e `destroy`; proibicao verificada por grep de `eval(`, `exec(` e `subprocess` retornando 0 |
| T-999.3-05 | Information Disclosure | `build_payload` | mitigate | Payload montado campo a campo com literais; criterio de aceite exige zero referencias a `env` dentro da funcao — nem `VAST_API_KEY` nem `PORTAINER_API_KEY` podem vazar para o dashboard |
| T-999.3-04 | Denial of Service | `st["events"]` | mitigate | `EVENTS_CAP = 50` aplicado em toda insercao (`record_event`), provado por `test_events_capped_at_50`; primeira linha de defesa antes do `MaxBytesReader` do gateway |
| T-999.3-10 | Denial of Service | `publish` dentro de `provision` | mitigate | `try/except Exception` amplo no molde do `notify` — falha de rede, 401 ou `KeyError` de secret ausente nunca aborta o flip de stack nem o destroy da instancia antiga (Pitfall 6) |
| T-999.3-11 | Repudiation | `last_command_result` | mitigate | Status `running` gravado ANTES da execucao e atualizado depois; comando executado deixa rastro mesmo se o processo morrer no meio (Pitfall 3) |
| T-999.3-SC | Tampering | pip installs | accept | Nenhuma dependencia nova; script permanece stdlib puro (criterio de aceite grep `import requests` = 0) |
</threat_model>

<verification>
- `python3 -m py_compile ops/vast-3060/vast3060.py` verde
- `python3 -m pytest ops/vast-3060/test_vast3060.py -q` verde (5 testes)
- `python3 ops/vast-3060/vast3060.py` sem argumento imprime uso com `poll`
- `git diff --stat ops/vast-3060/` mostra apenas os 4 arquivos deste plano
- Nenhuma mudanca na semantica de `machine_avoid` (fora de escopo, RESEARCH Pitfall 5): `git diff ops/vast-3060/vast3060.py | grep -c "machine_avoid"` limitado a leitura no payload e ao evento `bail`
</verification>

<success_criteria>
O script publica estado a cada evento e a cada ciclo, consome comando pendente com latencia
de ate 2 minutos, acumula custo sem cobrar o gap noturno, e nenhuma falha de publicacao ou
de comando derruba o fluxo de provision. Units do timer versionadas no repo.
</success_criteria>

<output>
Create `.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-03-SUMMARY.md` when done
</output>
