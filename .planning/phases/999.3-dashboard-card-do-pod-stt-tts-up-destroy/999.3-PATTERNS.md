# Phase 999.3: Dashboard — card do pod STT/TTS (up→destroy) - Pattern Map

**Mapped:** 2026-07-31
**Files analyzed:** 17 (7 create, 8 modify, 2 host units)
**Analogs found:** 17 / 17 (todos têm molde no repo — zero pattern inventado)

Built on 999.3-RESEARCH.md §Component Responsibilities. A pesquisa já nomeou os
analogs; este documento extrai o **código concreto** de cada um com path + linha
para o planner copiar direto na ação do plano.

## File Classification

| New/Modified File | Ação | Role | Data Flow | Closest Analog | Match |
|---|---|---|---|---|---|
| `gateway/internal/admin/sttpod.go` | CREATE | controller (admin handler) | request-response (write-through Redis) | `gateway/internal/admin/tenants_admin_http.go` | exact |
| `gateway/internal/admin/sttpod_test.go` | CREATE | test | request-response | `gateway/internal/admin/operations_test.go:60-135` (miniredis + doRequest) | exact |
| `gateway/internal/redisx/sttpod.go` (ou consts em arquivo existente) | CREATE (mínimo) | utility (key namespace) | — | `gateway/internal/redisx/shed.go:34-86` | role-match |
| `gateway/internal/admin/operations.go` | MODIFY | controller (aggregator) | request-response | ele mesmo (`operations.go:281-354` seções fail-soft) | exact |
| `gateway/internal/admin/operations_test.go` | MODIFY | test | request-response | ele mesmo | exact |
| `gateway/cmd/gateway/main.go` | MODIFY | config/wiring | — | `main.go:1360-1361,1387-1388,1636-1639` (Phase 18 wiring) | exact |
| `ops/vast-3060/vast3060.py` | MODIFY | service (ops agent) | event-driven + batch | ele mesmo (`notify()` :161-170 = contrato best-effort) | exact |
| `ops/vast-3060/test_vast3060.py` | CREATE | test | transform (funções puras) | `pod/primary/test_chatterbox_shim.py:1-60` | role-match |
| `/etc/systemd/system/vast-3060-poll.{service,timer}` | CREATE (host) | config | batch/cron | `/etc/systemd/system/vast-3060-ensure.{service,timer}` | exact |
| `dashboard/src/lib/gateway.ts` | MODIFY | service (typed client) | request-response | ele mesmo (`:195-264` bloco Operations) | exact |
| `dashboard/src/lib/admin-actions-core.ts` | MODIFY | service (server action core) | CRUD/command | `createTenantCore` (`:685-723`) | exact |
| `dashboard/src/lib/admin-actions.ts` | MODIFY | controller (RPC surface) | request-response | `createTenant` wrapper (`:152-159`) | exact |
| `dashboard/src/lib/admin-actions.test.ts` | MODIFY | test | CRUD | `:650-730` (3 testes TEN-UI-08) | exact |
| `dashboard/src/components/operacao-sttpod-panel.tsx` | CREATE | component (panel) | request-response (props) | `operacao-lifecycle-timeline.tsx` + `operacao-cost-panel.tsx` + `pod-config-controls.tsx` (dialog) | exact (composto) |
| `dashboard/src/components/operacao-sttpod-panel.test.tsx` | CREATE | test | — | `dashboard/src/components/fsm-panel.test.tsx` | exact |
| `dashboard/src/components/operacao-panels.tsx` | CREATE | component (client island) | request-response | corpo atual de `operacao/page.tsx:34-78` (move, não reescreve) | exact |
| `dashboard/src/app/(dashboard)/operacao/page.tsx` | MODIFY | route (RSC shell) | request-response | `operacao/config/page.tsx:33,101-104` | exact |

---

## Pattern Assignments

### `gateway/internal/admin/sttpod.go` (controller, request-response)

**Analog:** `gateway/internal/admin/tenants_admin_http.go` (143 linhas, lido integral)

**Package doc + imports** (`tenants_admin_http.go:1-29`) — todo handler admin
abre com um doc-comment que diz QUAIS rotas serve, QUEM consome e QUAL molde
seguiu:
```go
// Package admin (tenants_admin_http.go): the admin HTTP surface for tenant
// list + create (Plan 18-01). Two thin handlers over the existing sqlc
// ListTenants / CreateTenant queries — no new migration, no dynamic SQL.
//
// Mirrors the config_read/config_write template: an isolated query interface
// (so tests inject a call-counting fake), a dual constructor (prod *gen.Queries
// + test fake), typed response structs (never the raw gen row), the OpenAI
// error envelope, and the obs.GatewayAdminRequests metric per outcome.
package admin

import (
	"context"
	"encoding/json"
	"errors"
	"log/slog"
	"net/http"

	"github.com/ifixtelecom/gpu-ifix/gateway/internal/httpx"
	"github.com/ifixtelecom/gpu-ifix/gateway/internal/obs"
)
```
Para o sttpod trocar as deps sqlc por `redis.UniversalClient` (import
`"github.com/redis/go-redis/v9"` + `"github.com/ifixtelecom/gpu-ifix/gateway/internal/redisx"`).

**Constante de rota + struct + construtor** (`:31,56-78`):
```go
const tenantsRoute = "/admin/tenants"

// TenantAdminHandler serves GET + POST /admin/tenants via its List and Create
// methods (one struct, two routes — mounted separately in main.go).
type TenantAdminHandler struct {
	q   tenantAdminQueries
	log *slog.Logger
}

func NewTenantAdminHandler(q *gen.Queries, log *slog.Logger) *TenantAdminHandler {
	if log == nil {
		log = slog.Default()
	}
	return &TenantAdminHandler{q: q, log: log.With("module", "ADMIN_TENANTS")}
}
```
> **Nota:** o construtor duplo (prod + `newXWithQueries` de teste) existe porque
> a dep é `*gen.Queries` concreto. O sttpod recebe `redis.UniversalClient`, que
> **já é interface** — miniredis satisfaz direto. **Um construtor só**
> (`NewSttPodHandler(rdb redis.UniversalClient, log *slog.Logger)`). Copiar o
> segundo construtor aqui seria cerimônia sem ganho.

**Core POST: decode → validação server-side → ação → envelope** (`:107-138`):
```go
func (h *TenantAdminHandler) Create(w http.ResponseWriter, r *http.Request) {
	var body createTenantRequest
	if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
		h.badRequest(w, "invalid_body", "request body is not valid JSON")
		return
	}
	slug := strings.TrimSpace(body.Slug)
	name := strings.TrimSpace(body.Name)
	if slug == "" || name == "" {
		h.badRequest(w, "invalid_body", "slug and name are required")
		return
	}
	// ... ação ...
	h.log.Info("tenant created", "tenant_id", t.ID.String(), "slug", t.Slug)
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(http.StatusCreated)
	_ = json.NewEncoder(w).Encode(tenantResponse{...})
	obs.GatewayAdminRequests.WithLabelValues(tenantsRoute, "2xx").Inc()
}
```

**Error helper — obrigatório, um por handler** (`:140-143`):
```go
func (h *TenantAdminHandler) badRequest(w http.ResponseWriter, code, msg string) {
	httpx.WriteOpenAIError(w, http.StatusBadRequest, "invalid_request_error", code, msg)
	obs.GatewayAdminRequests.WithLabelValues(tenantsRoute, "4xx").Inc()
}
```

**Erro 5xx** (`:128-131`) — log estruturado + envelope SEM detalhe interno:
```go
	h.log.Error("CreateTenant failed", "err", err)
	httpx.WriteOpenAIError(w, http.StatusInternalServerError, "api_error", "tenant_create_failed", "")
	obs.GatewayAdminRequests.WithLabelValues(tenantsRoute, "5xx").Inc()
```

**Deltas do sttpod vs este analog** (RESEARCH Pattern 1 / Pitfall 8):
- `PostState` lê o body com `io.ReadAll(http.MaxBytesReader(w, r.Body, 256<<10))`
  em vez de `json.NewDecoder` — o blob é opaco (`json.Valid(body)`), não decodado.
- Whitelist do `PostCommand` no servidor: `{"provision","destroy"}` (defense-in-depth
  com o server action; molde de whitelist = `KEY_DATA_CLASSES` em
  `admin-actions-core.ts:683`).
- `GetDel` no fim do `PostState`, resposta `{"pending_command": <raw>|null}`.

---

### `gateway/internal/redisx/sttpod.go` (utility, key namespace)

**Analog:** `gateway/internal/redisx/shed.go:34-86`

```go
const (
	// shedStateKeyPrefix + {upstream} = Hash holding the per-upstream
	// current state mirror.
	shedStateKeyPrefix = "gw:shed:"
	// redisOpTimeout caps individual Redis calls. Matches breaker.go.
	redisOpTimeout = 2 * time.Second
)

// ShedStateKey returns "gw:shed:{upstream}".
func ShedStateKey(upstream string) string { return shedStateKeyPrefix + upstream }
```

**Escopo (ponytail, RESEARCH Pattern 2):** só as 2 constantes + 2 getters. NÃO
clonar os wrappers `WriteShedState`/`ReadShedState` — o handler chama
`rdb.Set/Get/GetDel` direto com o `ctx` do request. `redisOpTimeout` já existe
no pacote (`shed.go:50`): **reusar, não redeclarar** (redeclarar não compila).

---

### `gateway/internal/admin/operations.go` (controller, request-response) — MODIFY

**Analog:** ele mesmo — as seções já são fail-soft por construção.

**Struct de resposta + campo novo** (`:51-57`) — adicionar `SttPod` com
`omitempty` para o campo sumir do JSON quando não há publicação:
```go
type OperationsResponse struct {
	FSM        FSMSection      `json:"fsm"`
	Schedule   ScheduleSection `json:"schedule"`
	Lifecycles []LifecycleRow  `json:"lifecycles"`
	Breakers   []BreakerRow    `json:"breakers"`
	VastCost   VastCostSection `json:"vast_cost"`
}
```

**Deps + construtor** (`:118-147`) — o padrão de dep nil-safe já está
documentado no comentário; `rdb` entra na mesma lista:
```go
type OperationsHandler struct {
	q        operationsQueries
	breakers *breaker.Set
	rec      *primary.Reconciler // nil-safe: Vast off
	emergFSM *emerg.FSM          // nil-safe
	podCfg   *podconfig.Loader   // nil-safe: Phase 17 live budget; falls back to boot cfg
	cfg      config.Config
	log      *slog.Logger
}

// NewOperationsHandler wires the production dependencies. Accepts the
// concrete *gen.Queries; rec, emergFSM and podCfg may be nil when
// Vast/Phase-6/Phase-17 is disabled — the handler reports "unknown"
// (or falls back to the boot budget) rather than panicking.
func NewOperationsHandler(q *gen.Queries, b *breaker.Set, rec *primary.Reconciler,
	emergFSM *emerg.FSM, podCfg *podconfig.Loader, cfg config.Config, log *slog.Logger) *OperationsHandler {
```
> A assinatura tem 7 params e ganha um 8º (`rdb`). Ambos os construtores
> (`NewOperationsHandler` :133 e `newOperationsHandlerWithQueries` :151) mudam,
> **e os 4+ call-sites do teste** (`operations_test.go:183` e irmãos) também.

**Montagem fail-soft da resposta** (`:200-205`) — o campo novo entra aqui:
```go
	resp := OperationsResponse{
		FSM:        h.fsmSection(),
		Schedule:   h.scheduleSection(now),
		Lifecycles: make([]LifecycleRow, 0),
		Breakers:   h.breakerRows(),
	}
```

**Molde exato de seção degradável** (`:296-308`) — erro NÃO vira 500, vira log
+ seção mínima. É este contrato que o `sttPodSection` copia:
```go
func (h *OperationsHandler) scheduleSection(now time.Time) ScheduleSection {
	rule, err := primary.ParseScheduleEnv(h.cfg)
	if err != nil {
		h.log.Warn("ParseScheduleEnv failed; reporting schedule disabled", "err", err)
		return ScheduleSection{ Timezone: h.cfg.PrimaryPodScheduleTimezone, Disabled: true, Days: []string{} }
	}
```
e o guard de dep nil (`:339-343`):
```go
func (h *OperationsHandler) breakerRows() []BreakerRow {
	out := make([]BreakerRow, 0)
	if h.breakers == nil {
		return out
	}
```

**Nullable → JSON null** (`:373-385`) — o repo distingue "não computado" de
"zero" com ponteiro. Para o sttpod isso vira `*SttPodSection` + `json.RawMessage`
(RESEARCH Pattern 3):
```go
// numericPtr converts a nullable Postgres numeric into a *float64 so the
// JSON encoder renders an unset column as null rather than 0.
```

---

### `gateway/internal/admin/sttpod_test.go` + `operations_test.go` (test)

**Analog:** `gateway/internal/admin/operations_test.go`

**miniredis wiring + cleanup** (`:60-78`) — copiar removendo o `breaker.NewSet`:
```go
func newOpBreakerSet(t *testing.T, names []string) *breaker.Set {
	t.Helper()
	mr, err := miniredis.Run()
	if err != nil {
		t.Fatalf("miniredis.Run: %v", err)
	}
	rdb := redis.NewClient(&redis.Options{Addr: mr.Addr()})
	t.Cleanup(func() {
		_ = rdb.Close()
		mr.Close()
	})
	return breaker.NewSet(rdb, discardLog(), breaker.Options{...}, names)
}
```

**Helper de request + decode** (`:123-135`):
```go
func doOperationsRequest(t *testing.T, h *OperationsHandler) (*httptest.ResponseRecorder, opResponse) {
	t.Helper()
	rec := httptest.NewRecorder()
	req := httptest.NewRequest("GET", "/admin/operations", nil)
	h.ServeHTTP(rec, req)
	var body opResponse
	if rec.Code == http.StatusOK {
		if err := json.Unmarshal(rec.Body.Bytes(), &body); err != nil {
			t.Fatalf("decode response: %v (body=%s)", err, rec.Body.String())
		}
	}
	return rec, body
}
```
Para POST: `httptest.NewRequest("POST", "/admin/sttpod/state", bytes.NewReader(body))`.

**Struct de decode local ao teste** (`:95-121`) — o teste NÃO reusa a struct de
produção; declara `opResponse` própria com só os campos que assere. Para o
sttpod: `SttPod *json.RawMessage \`json:"sttpod"\`` (ponteiro prova a ausência).

**Assert padrão** (`:187-192`):
```go
	if rec.Code != http.StatusOK {
		t.Fatalf("status = %d, want 200 (body=%s)", rec.Code, rec.Body.String())
	}
	if ct := rec.Header().Get("Content-Type"); ct != "application/json" {
		t.Errorf("content-type = %q, want application/json", ct)
	}
```

**Casos mínimos (RESEARCH §Validation):** POST body não-JSON → 400 + zero writes ·
POST ok → 200 e chave gravada **verbatim** (comparar bytes) · segundo POST →
`pending_command` null (GETDEL consumiu) · comando fora da whitelist → 400 + zero
writes · `/admin/operations` sem chave → campo `sttpod` ausente · com blob →
devolve verbatim.

---

### `gateway/cmd/gateway/main.go` (wiring) — MODIFY

**Analog:** o próprio wiring da Phase 18, três pontos.

**(1) Construção do handler** (`:1355-1361`) — comentário nomeia a fase/req:
```go
	// Phase 18 (TEN-UI-01..05) — tenant/key management endpoints the dashboard
	// consumes via its owner server action. GET/POST /tenants (list + create,
	// slug dup → 409) ... Thin over existing sqlc queries — no new migration.
	adminTenantHandler := admin.NewTenantAdminHandler(gen.New(pool), log)
	adminKeysHandler := admin.NewKeysAdminHandler(gen.New(pool), log)
```
`rdb` já existe no escopo desta função (`main.go:222` `rdb, err := redisx.NewClient(ctx, cfg)`),
então `admin.NewSttPodHandler(rdb, log)` não precisa de fiação nova. O
`adminOperationsHandler` (`:1336`) ganha `rdb` como argumento.

**(2) Campo no struct `proxies`** (`:1387-1391`) — `rdb` **já é campo**
(`main.go:101 rdb redis.UniversalClient`, passado em `:1391`):
```go
		adminTenantHandler: adminTenantHandler,
		adminKeysHandler:   adminKeysHandler,

		adminVerifier:     adminVerifier,
		rdb:               rdb,
```

**(3) Rotas no adminRouter** (`:1632-1639`) — guard `!= nil` por handler:
```go
		// Phase 18 (TEN-UI-01..05) — tenant/key management, same X-Admin-Key
		// gate. One guard per handler field (each handler serves several
		// method routes).
		if px.adminTenantHandler != nil {
			adminRouter.Method(http.MethodGet, "/tenants", http.HandlerFunc(px.adminTenantHandler.List))
			adminRouter.Method(http.MethodPost, "/tenants", http.HandlerFunc(px.adminTenantHandler.Create))
		}
```
→ `POST /sttpod/state` + `POST /sttpod/command`. A auth vem de graça:
`adminRouter.Use(admin.Middleware(px.adminVerifier, log))` (`:1595`) cobre todo
o subrouter (D-04, zero código de auth novo).

---

### `ops/vast-3060/vast3060.py` (service, event-driven) — MODIFY

**Analog:** ele mesmo — o script já tem todos os contratos necessários.

**Contrato best-effort — copiar LITERALMENTE para `publish()`** (`:161-170`),
é o antídoto do Pitfall 6:
```python
def notify(env: dict, text: str) -> None:
    """Best-effort — nunca derruba o fluxo por falha de notificação."""
    try:
        code, _ = http_json(
            "POST", f"{env['DINASTIA_BASE_URL']}/chat/send/text",
            {"token": env["DINASTIA_TOKEN"]},
            {"Phone": env["NOTIFY_PHONE"], "Body": text}, timeout=20)
        log(f"notify -> HTTP {code}")
    except Exception as e:
        log(f"notify FALHOU: {e}")
```
O `try/except Exception` amplo cobre inclusive `KeyError` de `env['GW_ADMIN_KEY']`
ausente no host — exatamente o modo de falha do Pitfall 6.

**HTTP client já pronto** (`:105-116`) — devolve `(code, dict)` e nunca levanta:
```python
def http_json(method: str, url: str, headers: dict | None = None, payload=None,
              timeout: int = 30):
    h = dict(headers or {})
    body = None
    if payload is not None:
        h["Content-Type"] = "application/json"
        body = json.dumps(payload).encode()
    code, raw = http(method, url, h, body, timeout)
    try:
        return code, json.loads(raw)
    except Exception:
        return code, {"_raw": raw[:300].decode(errors="replace")}
```

**Escrita atômica do state** (`:123-127`) — já é write-tmp+replace; nenhuma
mudança necessária, só campos novos no dict:
```python
def save_state(st: dict) -> None:
    STATE_PATH.parent.mkdir(parents=True, exist_ok=True)
    tmp = STATE_PATH.with_suffix(".tmp")
    tmp.write_text(json.dumps(st, indent=2))
    tmp.replace(STATE_PATH)
```

**Pontos de evento existentes onde o `publish(event=…)` entra** (todos já
chamam `save_state` + `notify` — o publish acompanha):

| Linha | Evento | Contexto |
|---|---|---|
| `:293-303` | `no_offer` | `PROVISION abortado: nenhuma oferta elegível` + dedup `alerted_no_offer` |
| `:312-315` | `create_fail` | create HTTP != 200 |
| `:319-329` | `bail` | destrói a nova + `machine_avoid.append(bad)` — **é aqui que sai o `machine_id` correlacionável do D-14** |
| `:371-379` | `edge_fail` | flip feito mas edge não validou (mantém a velha) |
| `:390-396` | `up` | sucesso: `st.update(instance_id, machine_id, ip, port, …)` + notify com dph/geolocation |
| `:420-430` | `destroy` | `cmd_stop`, `st.update(instance_id=None, ip=None, port=None)` |

**Campos hoje escritos no state** (`:390-391`) — a linha que precisa ganhar
`dph`, `geolocation`, `provisioned_at` (o `offer` dict tem `dph_total` e
`geolocation`, já usados no notify `:393-395`):
```python
    st.update(instance_id=new_id, machine_id=offer.get("machine_id"),
              ip=ip, port=port, fail_count=0, alerted_no_offer=False)
    save_state(st)
```

**Guarda de janela — molde para o comando `poll`** (`:406-408`):
```python
    h = datetime.now().hour
    if h < WINDOW_START_H or h >= WINDOW_END_H:
        return  # fora da janela — quem manda é o destroy das 22h
```

**Dispatch de comando novo** (`:440-455`) — o `poll` entra na tupla de validação
e no if-chain; whitelist literal, **nunca** `eval`/`subprocess` com o valor
recebido do gateway (threat §Command injection):
```python
def main() -> int:
    cmd = sys.argv[1] if len(sys.argv) > 1 else ""
    if cmd not in ("ensure", "stop", "provision", "migrate", "status"):
        print("uso: vast3060.py ensure|stop|provision|status")
        return 1
    env = load_env()
    st = load_state()
    if cmd == "ensure":
        cmd_ensure(env, st)
    elif cmd == "stop":
        cmd_stop(env, st)
    elif cmd in ("provision", "migrate"):  # migrate = alias legado
        provision(env, st, "manual")
```

**Constante de endpoint** (`:46-56`) — o bloco de constantes onde entra o
timeout/cap do publish; `EDGE` já aponta pro gateway:
```python
VAST = "https://console.vast.ai/api/v0"
PORTAINER = "https://portainer3.ifixtelecom.com.br/api"
STACK_ID = 38
EDGE = "https://ai-gateway.converse-ai.app"
```

**Docstring do módulo** (`:21-24`) — documenta o contrato de estado; atualizar
com os campos novos:
```python
State:   /var/lib/vast-3060/state.json  {instance_id, machine_id, ip, port,
         machine_avoid[], alerted_no_offer}
Secrets: /etc/onboard/secrets/vast-3060.env (root-600)
```

---

### `ops/vast-3060/test_vast3060.py` (test, transform)

**Analog:** `pod/primary/test_chatterbox_shim.py:1-26`

```python
"""pod/primary/test_chatterbox_shim.py — Phase 06.7 Plan 05 (Wave 1, GREEN).

ENGINE (authority: 06.7-WAVE0-GATES.md GATE 1/2, 06.7-CONTEXT.md D-05/D-06/D-08):
...
Run target: `pytest pod/primary/test_chatterbox_shim.py`
"""

from __future__ import annotations

import pytest
```
Padrões a copiar: docstring com a **autoridade** (decisão/plano) + o `Run target`
explícito; `from __future__ import annotations`; fakes locais em vez de mocks
de framework. **NÃO roda em CI** (RESEARCH §Test Framework) — o `Run target` na
docstring é o que torna o teste descobrível.

**Alvo:** só as funções puras — `accrue_cost` (rollover dia/mês + guarda de gap
noturno, Pitfall 4) e o cap de `events[]` em 50 (D-06). O `publish` faz I/O e
não é alvo.

---

### `/etc/systemd/system/vast-3060-poll.{service,timer}` (config, host)

**Analog:** `/etc/systemd/system/vast-3060-ensure.{service,timer}` (lidos ao vivo)

```ini
# vast-3060-ensure.service
[Unit]
Description=Pod STT/TTS 3060 — ensure running (janela 7-22, retry 15min)
[Service]
Type=oneshot
ExecStart=/usr/bin/python3 /opt/vast-3060/vast3060.py ensure
```
```ini
# vast-3060-ensure.timer
[Unit]
Description=Ensure pod 3060 a cada 15min (07-21h59)
[Timer]
OnCalendar=*-*-* 07..21:00/15:00
Persistent=true
[Install]
WantedBy=timers.target
```
Trocar `ensure`→`poll` e `/15:00`→`/2:00`. Sem `[Install]` no `.service` (é
oneshot acionado pelo timer). `ExecStart` aponta pro **`/opt`**, não pro repo —
daí o passo de `cp` obrigatório (Pitfall 7).

---

### `dashboard/src/lib/gateway.ts` (service, typed client) — MODIFY

**Analog:** o bloco Operations do próprio arquivo (`:195-264`).

**Cabeçalho de bloco — declara o Go como fonte de verdade** (`:195-202`):
```ts
// --- /admin/operations (quick-260625-v04, Tier-2 "Operação") --------------
//
// These interfaces mirror the Go handler `admin.OperationsResponse` and its
// sections in gateway/internal/admin/operations.go FIELD-FOR-FIELD. The Go
// handler is the source of truth. Nullable Postgres columns are rendered as
// JSON null by the handler → typed as `T | null` here. `phantom_month_brl`
// is omitted by the handler this version (economy deferred) → optional.
```

**Interface por seção + doc de uma linha apontando a struct Go** (`:239-252`):
```ts
/** One upstream's effective breaker state. Mirrors `admin.BreakerRow`. */
export interface OperationsBreaker {
  upstream: string;
  state: string; // closed|half-open|open|forced-open
}

/** Vast spend + budget. Mirrors `admin.VastCostSection`. */
export interface OperationsVastCost {
  today_brl: number;
  month_brl: number;
  budget_brl: number;
  budget_pct_used: number;
  phantom_month_brl?: number; // omitted this version (economy deferred)
}
```

**Campo agregado** (`:258-264`) — `sttpod?:` opcional espelha o `omitempty` Go:
```ts
export interface OperationsResponse {
  fsm: OperationsFSM;
  schedule: OperationsSchedule;
  lifecycles: OperationsLifecycle[];
  breakers: OperationsBreaker[];
  vast_cost: OperationsVastCost;
}
```

**Fetch — já existe, NÃO precisa de função nova** (`:521-524`), o campo entra na
mesma chamada (D-13):
```ts
/** GET /admin/operations — Tier-2 "Operação" aggregate (FSM/schedule/cost). */
export function fetchOperations(): Promise<OperationsResponse> {
  return proxyGet<OperationsResponse>("operations");
}
```
`proxyGet` (`:452-476`) é catch-all sobre `/api/gateway/*` — zero mudança.

---

### `dashboard/src/lib/admin-actions-core.ts` (service, command) — MODIFY

**Analog:** `createTenantCore` (`:685-723`) — ordem de operações **não-negociável**.

**Cabeçalho de bloco que fixa a ordem** (`:655-665`):
```ts
// ──────────────────────────────────────────────────────────────────────────
// TEN-UI-08/09 — owner-gated tenant / key mutations (Plan 18-03).
//
// Mirror updatePodConfigCore EXACTLY: requireOwner FIRST (D-03, server-side,
// identity only from the session via the wrappers) → server-side validation
// (defense-in-depth with the Go handler, Plan 18-01) BEFORE any gateway call →
// mutate via `gatewayAdminPost` (server-only, X-Admin-Key) → EXACTLY one audit
// row (never a secret in metadata) → revalidate.
// ──────────────────────────────────────────────────────────────────────────
```

**Seam injetável + whitelist** (`:667-683`):
```ts
/** Injectable gateway-POST seam for unit tests (mock the mutation). */
export interface TenantMutationDeps {
  post?: typeof realGatewayAdminPost;
}

/** The only accepted data classes (whitelist — Plan 18-01 parity). */
const KEY_DATA_CLASSES = new Set(["normal", "sensitive"]);
```
→ `const STTPOD_COMMANDS = new Set(["provision", "destroy"]);`

**Corpo verbatim a clonar** (`:690-723`):
```ts
export async function createTenantCore(args: {
  actor?: Actor;
  auth?: AuthLike;
  db?: unknown;
  slug: string;
  name: string;
  deps?: TenantMutationDeps;
}): Promise<void> {
  const authInstance = args.auth ?? (realAuth as unknown as AuthLike);
  const { actor } = await requireOwner(args.actor, authInstance);   // 1º await

  const slug = (args.slug ?? "").trim();
  if (!TENANT_SLUG_RE.test(slug)) {
    throw new Error("Slug inválido — use apenas letras minúsculas, números e hífens.");
  }

  const post = args.deps?.post ?? realGatewayAdminPost;
  await post("tenants", { slug, name });

  await writeAuditLog({
    db: args.db,
    actor: { id: actor.id, email: actor.email },
    action: "tenant.create",
    metadata: { slug, name },
  });

  safeRevalidateTenants();
}
```

**Revalidate seguro** (`:831-841`) — cada página tem o seu, sempre try/catch:
```ts
/**
 * Revalidate the tenant-management page after a tenant/key mutation (Plan
 * 18-03/18-04). Swallowed outside a Next.js request scope (unit test).
 */
function safeRevalidateTenants(): void {
  try {
    revalidatePath("/tenants/gerenciar");
  } catch {
    // Not inside a Next.js request scope (unit test) — nothing to revalidate.
  }
}
```
→ `safeRevalidateOperacao()` com `revalidatePath("/operacao")`.

**`gatewayAdminPost` — assinatura a consumir** (`gateway-admin.ts:157-168`):
```ts
export async function gatewayAdminPost<T = void>(
  path: string,
  body: unknown,
): Promise<T> {
  return gatewayAdminMutate<T>("POST", path, body);
}
```
Path SEM barra inicial e SEM `/admin` (`"tenants"`, `"sttpod/command"`). **NUNCA
editar `gateway-admin.ts`** — o leak-guard de `gateway.test.ts` exige a admin key
em exatamente 2 arquivos.

---

### `dashboard/src/lib/admin-actions.ts` (RPC surface) — MODIFY

**Analog:** os wrappers TEN-UI (`:150-177`).

```ts
// ──────────────────────────────────────────────────────────────────────────
// TEN-UI-08/09 — owner-gated tenant / key mutations. Identity from session.
// ──────────────────────────────────────────────────────────────────────────

export async function createTenant(args: {
  slug: string;
  name: string;
}): Promise<void> {
  const { actor } = await requireOwner();
  await createTenantCore({ actor, slug: args.slug, name: args.name });
}

export async function revokeKey(args: { keyId: string }): Promise<void> {
  const { actor } = await requireOwner();
  await revokeKeyCore({ actor, keyId: args.keyId });
}
```

**Invariantes de segurança do arquivo** (`:8-28`), aplicáveis ao wrapper novo:
```
 *   1. Identity comes from the SESSION ONLY. Each wrapper calls `requireOwner()`
 *      with NO `actor` argument, forcing the cookie/session path.
 *   2. NO injectable `actor` / `auth` / `db` / `deps` in any public signature.
```
Assinatura pública: `sttPodCommand(args: { command: string }): Promise<void>` —
nada mais.

---

### `dashboard/src/lib/admin-actions.test.ts` (test) — MODIFY

**Analog:** os 3 testes TEN-UI-08 (`:655-730`).

**Rejeição de operator** (`:655-678`) — as 3 asserções são o contrato:
```ts
  it("(TEN-UI-08) createTenant: operator rejected — NO gateway call, NO audit row", async () => {
    const mod = await importAdminActions();
    const createTenant = mod?.createTenantCore as ((args: unknown) => Promise<unknown>) | undefined;
    expect(typeof createTenant).toBe("function");

    const db = freshDb();
    let rejected = false;
    try {
      await createTenant?.({ actor: { role: "operator" }, db, slug: "acme", name: "Acme" });
    } catch { rejected = true; }
    // requireOwner throws FIRST — the gateway is never called, nothing audited.
    expect(rejected).toBe(true);
    expect(gatewayAdminPostMock).not.toHaveBeenCalled();
    expect(db.adminAuditLog.length).toBe(0);
  });
```

**Sucesso owner — assere path, body, e EXATAMENTE 1 audit row** (`:680-706`):
```ts
    gatewayAdminPostMock.mockResolvedValue({ slug: "acme", name: "Acme" });
    await createTenant?.({ actor: { id: "ow-1", email: "ow@ifixtelecom.com.br", role: "owner" }, db, slug: "acme", name: "Acme" });

    expect(gatewayAdminPostMock).toHaveBeenCalledTimes(1);
    const [path, body] = gatewayAdminPostMock.mock.calls[0];
    expect(path).toBe("tenants");
    expect(body).toMatchObject({ slug: "acme", name: "Acme" });

    expect(db.adminAuditLog.length).toBe(1);
    const row = db.adminAuditLog[0];
    expect(row.action).toBe("tenant.create");
    expect(row.metadata).toEqual({ slug: "acme", name: "Acme" });
```

**Validação antes do gateway** (`:708-730`) — mesma forma, input fora da
whitelist, assere `not.toHaveBeenCalled()`. Helpers `importAdminActions`,
`freshDb`, `gatewayAdminPostMock` já existem no arquivo (`:651-653` faz
`gatewayAdminPostMock.mockReset()` no `beforeEach` do describe).

---

### `dashboard/src/components/operacao-sttpod-panel.tsx` (component) — CREATE

**Analogs (composto):** `operacao-cost-panel.tsx` (KPI+doc), `operacao-lifecycle-timeline.tsx`
(tabela do histórico, D-15), `operacao-breaker-badges.tsx` (badges de estado),
`pod-config-controls.tsx` (dialog + toast + gate isOwner).

**Doc-comment + Card shell + KPI row** (`operacao-cost-panel.tsx:1-48`):
```tsx
/**
 * Operação — Vast cost + budget panel.
 *
 * KPI row: custo hoje / custo mês / budget, plus a month/budget progress bar
 * (budget_pct_used). The bar turns amber ≥ 75% and red ≥ 90% (UI-SPEC
 * §Semantic status palette).
 */

import { KpiCard } from "@/components/kpi-card";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { formatBrl } from "@/lib/format";
import type { OperationsVastCost } from "@/lib/gateway";

export function OperacaoCostPanel({ vastCost }: OperacaoCostPanelProps) {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-[20px] font-semibold">Custo Vast</CardTitle>
      </CardHeader>
      <CardContent className="flex flex-col gap-6">
        <div className="grid grid-cols-1 gap-4 sm:grid-cols-3">
          <KpiCard caption="Custo hoje" value={formatBrl(vastCost.today_brl)} />
          <KpiCard caption="Custo mês" value={formatBrl(vastCost.month_brl)} />
```
→ Custo dia/mês em BRL com USD entre parênteses (D-08): `formatBrl(usd * rate)`
+ sufixo. **`formatBrl` de `@/lib/format` é obrigatório** (Don't Hand-Roll).

**Tabela do histórico com empty-state + `tabular-nums`** (`operacao-lifecycle-timeline.tsx:29-98`):
```tsx
/** Format an RFC3339 timestamp in pt-BR, or "—" when empty/invalid. */
function formatDateTime(iso: string | null): string {
  if (!iso) return "—";
  const d = new Date(iso);
  if (Number.isNaN(d.getTime())) return "—";
  return d.toLocaleString("pt-BR");
}
...
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead>Início</TableHead>
              <TableHead>Trigger</TableHead>
              <TableHead>Instância Vast</TableHead>
              <TableHead className="text-right">Custo</TableHead>
              <TableHead>Estado</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {lifecycles.length === 0 ? (
              <TableRow>
                <TableCell colSpan={5} className="py-8 text-center text-[14px] text-muted-foreground">
                  Sem lifecycles no período.
                </TableCell>
              </TableRow>
            ) : (
              lifecycles.map((lc) => (
                <TableRow key={lc.id}>
                  <TableCell className="tabular-nums">{formatDateTime(lc.started_at)}</TableCell>
```
`toLocaleString("pt-BR")` — **nunca** `toISOString().slice()` (Don't Hand-Roll:
bug de shift de dia documentado em `incidents/page.tsx:44-54`).

**Badge de estado — switch class + switch label** (`operacao-breaker-badges.tsx:21-48`):
```tsx
/** Breaker state → badge classes. */
function breakerBadgeClass(state: string): string {
  switch (state) {
    case "closed":  return "bg-primary/15 text-primary";
    case "half-open": return "bg-status-warning/15 text-status-warning";
    case "open":
    case "forced-open": return "bg-destructive/15 text-destructive";
    default: return "bg-muted text-muted-foreground";
  }
}
/** Breaker state → pt-BR label. */
function breakerLabel(state: string): string {
  switch (state) { case "closed": return "fechado"; /* ... */ default: return state; }
}
```
Molde direto do badge **stale** (D-03: `published_at` > 20min → amber) e dos
badges `local-stt`/`kokoro-tts` cruzados de `data.breakers` (D-14, sem fetch extra).

**Botões owner-gated: dialog + pending + toast** (`pod-config-controls.tsx:734-796, 940-976`):
```tsx
  /** Server write → on success update local snapshot + toast; else inline error. */
  async function commit(value: unknown) {
    setPending(true);
    setError(null);
    try {
      await updatePodConfig({ field: f.field, value });
      onSaved(f, value);
      setMode("view");
      setConfirm(null);
    } catch (err) {
      setError(mapSaveError(err));
      setConfirm(null);
    } finally {
      setPending(false);
    }
  }
```
```tsx
    <AlertDialog open={confirm !== null} onOpenChange={(o) => { if (!o && !pending) onCancel(); }}>
      <AlertDialogContent style={{ maxWidth: 384, padding: 24 }}>
        <AlertDialogHeader>
          <AlertDialogTitle>{confirm?.danger.title}</AlertDialogTitle>
          <AlertDialogDescription>{confirm?.danger.body}</AlertDialogDescription>
        </AlertDialogHeader>
        <AlertDialogFooter className="gap-2">
          <AlertDialogCancel disabled={pending}>Cancelar</AlertDialogCancel>
          <AlertDialogAction variant="destructive" disabled={pending}
            onClick={(e) => { e.preventDefault(); onConfirm(); }}>
            {pending ? <span className="inline-flex items-center gap-2"><Spinner />{confirm?.danger.confirmLabel}</span>
                     : confirm?.danger.confirmLabel}
          </AlertDialogAction>
        </AlertDialogFooter>
      </AlertDialogContent>
    </AlertDialog>
```
**Gate cosmético** (`:779-796`): `{isOwner ? <control/> : <readonly-span/>}` —
o operator vê os mesmos valores sem afordância de ação. Sem type-to-confirm
(decisão D-04 da Phase 17).

**Imports do island** (`pod-config-controls.tsx:1,23-57`): `"use client"` na
linha 1, `toast` de `sonner`, `AlertDialog*` de `@/components/ui/alert-dialog`,
actions de `@/lib/admin-actions`.

---

### `dashboard/src/components/operacao-panels.tsx` (client island) — CREATE

**Analog:** o corpo atual de `operacao/page.tsx:1-78` — **mover, não reescrever**
(RESEARCH Pattern 5). O arquivo inteiro vira o island, ganhando só `isOwner` na
prop e o 5º painel na lista:
```tsx
"use client";
import { useQuery } from "@tanstack/react-query";
import { OperacaoBreakerBadges } from "@/components/operacao-breaker-badges";
/* ... */
import { fetchOperations, GatewayError } from "@/lib/gateway";

/** WR-06: the specific proxy/gateway cause, or the generic fallback. */
function errorMessage(error: unknown): string {
  return error instanceof GatewayError
    ? error.message
    : "Não foi possível carregar o estado operacional do gateway.";
}

export default function OperacaoPage() {
  const query = useQuery({
    queryKey: ["operations"],
    queryFn: fetchOperations,
    // Live panel — the gateway data sources poll ~5–10s.
    refetchInterval: 10000,
  });

  return (
    <div className="flex flex-col gap-8">
      <div className="flex items-center justify-between gap-4">
        <h1 className="text-[28px] font-semibold leading-[1.2]">Operação</h1>
        <StaleIndicator updatedAt={query.dataUpdatedAt} />
      </div>

      {query.isLoading ? (
        <div className="flex flex-col gap-8">
          <Skeleton className="h-48 w-full" />
          {/* ... */}
        </div>
      ) : query.isError ? (
        <div className="flex flex-col items-center gap-4 py-8 text-center">
          <p className="text-[14px] text-muted-foreground">
            {errorMessage(query.error)} Verifique se o gateway está no ar e se a
            admin-key está válida, depois use Tentar novamente.
          </p>
          <Button size="sm" variant="outline" onClick={() => query.refetch()}>
            Tentar novamente
          </Button>
        </div>
      ) : query.data ? (
        <>
          <OperacaoFsmPanel fsm={query.data.fsm} schedule={query.data.schedule} />
          <OperacaoCostPanel vastCost={query.data.vast_cost} />
          <OperacaoBreakerBadges breakers={query.data.breakers} />
          <OperacaoLifecycleTimeline lifecycles={query.data.lifecycles} />
        </>
      ) : null}
    </div>
  );
}
```
Mudanças: `export function OperacaoPanels({ isOwner }: { isOwner: boolean })`,
`<OperacaoSttPodPanel sttpod={query.data.sttpod} breakers={query.data.breakers} isOwner={isOwner} />`.
O `queryKey: ["operations"]` e o `refetchInterval: 10000` ficam **iguais** (D-13:
um poll só).

---

### `dashboard/src/app/(dashboard)/operacao/page.tsx` (RSC shell) — MODIFY

**Analog:** `operacao/config/page.tsx:32-33, 101-104`

```tsx
// The page reads the live session + the gateway on every request.
export const dynamic = "force-dynamic";

export default async function ConfigDoPodPage() {
  // Owner-gate (D-07): COSMETIC — the server actions re-check requireOwner.
  const viewerRole = await getViewerRole();
  const isOwner = viewerRole === "owner";
```
com o import (`:20`) `import { getViewerRole } from "@/lib/viewer";` e a passagem
da prop (`:137-141`):
```tsx
        <PodConfigControls isOwner={isOwner} config={data.config} bounds={data.bounds} />
```
A `/operacao` NÃO precisa de `fetchPodConfigServer` — os dados vêm do `useQuery`
do island. A page fica com ~10 linhas: `dynamic`, `getViewerRole`, `<OperacaoPanels isOwner/>`.
> O `<h1>Operação</h1>` + `StaleIndicator` moram no island (dependem de
> `query.dataUpdatedAt`), não na shell.

---

## Shared Patterns

### Autenticação (gateway)
**Source:** `gateway/cmd/gateway/main.go:1593-1595`
**Apply to:** ambos os handlers novos — **zero código de auth** no handler.
```go
	if px.adminVerifier != nil {
		adminRouter := chi.NewRouter()
		adminRouter.Use(admin.Middleware(px.adminVerifier, log))
```

### Autorização (dashboard)
**Source:** `dashboard/src/lib/admin-actions-core.ts:136-159`
**Apply to:** `sttPodCommandCore` — primeiro await, sempre.
```ts
export async function requireOwner(
  actor?: Actor,
  authInstance: AuthLike = realAuth as unknown as AuthLike,
): Promise<{ actor: Actor; h: Headers }> {
  /* ... */
  if (resolved.role !== "owner") {
    throw new Error("Ação restrita ao owner do dashboard.");
  }
  return { actor: resolved, h };
}
```
Docstring (`:131-134`) fixa a razão de NÃO ser server action:
```
 * NOT a Server Action — lives in this non-`"use server"` module so it is never
 * independently invocable over the network (CR-02). The public Server Actions
 * in `@/lib/admin-actions` call this with NO `actor` argument, forcing the
 * session path so the caller can never assert their own role (CR-01).
```

### Audit (dashboard)
**Source:** `dashboard/src/lib/admin-actions-core.ts:179-206`
**Apply to:** `sttPodCommandCore` — **exatamente 1** chamada, sem segredo em metadata.
```ts
export async function writeAuditLog(args: {
  db?: unknown;
  actor: { id?: string; email?: string };
  target?: { id?: string; email?: string };
  action: string;
  metadata?: Record<string, unknown> | null;
}): Promise<void> {
```
`action`: `"sttpod.provision"` / `"sttpod.destroy"` (namespace pontuado, como
`tenant.create` / `key.revoke` / `operator.invite`).

### Error envelope (gateway)
**Source:** `gateway/internal/admin/tenants_admin_http.go:140-143` + `:128-131`
**Apply to:** todo branch de saída dos handlers novos.
```go
httpx.WriteOpenAIError(w, http.StatusBadRequest, "invalid_request_error", code, msg)
obs.GatewayAdminRequests.WithLabelValues(<route>, "4xx").Inc()
```
O `proxyGet` do dashboard parseia exatamente esse envelope (`gateway.ts:460-472`):
```ts
      const body = (await res.json()) as ErrorEnvelope;
      if (body.error?.message) message = body.error.message;
      if (body.error?.type) type = body.error.type;
```

### Best-effort no script
**Source:** `ops/vast-3060/vast3060.py:161-170` (`notify`)
**Apply to:** `publish()` e a execução do comando — `try/except Exception` amplo
+ `log(...)`, **nunca** deixar exceção escapar para o `provision()`.

### Formatação pt-BR
**Source:** `operacao-lifecycle-timeline.tsx:29-35`, `operacao-cost-panel.tsx:53-58`
**Apply to:** todo timestamp e todo número do painel novo.
```tsx
return d.toLocaleString("pt-BR");
```
```tsx
{pct.toLocaleString("pt-BR", { minimumFractionDigits: 1, maximumFractionDigits: 1 })}
```
+ `className="tabular-nums"` em toda célula numérica (evita jitter no refetch de 10s).

---

## No Analog Found

Nenhum. Todos os 17 arquivos têm molde direto no repo.

Dois pontos **sem precedente exato**, resolvidos por composição (o planner NÃO
deve procurar um analog melhor — não existe):

| Item | Por quê não há analog | Decisão |
|---|---|---|
| `json.RawMessage` opaco numa resposta admin | Todas as seções de `/admin/operations` hoje são structs tipadas | RESEARCH §Alternatives: RawMessage é a escolha (D-05, gateway não lê nenhum campo). Molde de "nullable → ponteiro" vem de `numericPtr` (`operations.go:373-385`) |
| `GetDel` no gateway | Nenhum call-site de `GetDel` existe no repo hoje | `go-redis@v9.18.0/string_commands.go:176` + miniredis suportam. Tratar `redis.Nil` como "sem comando", igual `errors.Is(err, redis.Nil)` já usado no pacote redisx |

---

## Metadata

**Analog search scope:** `gateway/internal/admin/`, `gateway/internal/redisx/`,
`gateway/cmd/gateway/`, `dashboard/src/lib/`, `dashboard/src/components/`,
`dashboard/src/app/(dashboard)/`, `ops/vast-3060/`, `pod/primary/`,
`/etc/systemd/system/vast-3060-*`

**Files read in full:** `tenants_admin_http.go`, `operations.go`, `vast3060.py`,
`operacao-cost-panel.tsx`, `operacao-lifecycle-timeline.tsx`,
`operacao/page.tsx`, `operacao/config/page.tsx`, `stale-indicator.tsx`,
`vast-3060-ensure.{service,timer}`

**Files read in targeted ranges:** `operations_test.go:1-195`, `shed.go:1-100`,
`main.go:1330-1399,1590-1659`, `gateway.ts:190-280,424-528`,
`admin-actions-core.ts:126-206,655-775,823-841`, `admin-actions.ts:1-40,120-177`,
`admin-actions.test.ts:640-735`, `pod-config-controls.tsx:1-60,720-800,930-977`,
`operacao-breaker-badges.tsx:1-60`, `operacao-fsm-panel.tsx:1-75`,
`gateway-admin.ts:140-168`, `fsm-panel.test.tsx:1-45`,
`test_chatterbox_shim.py:1-55`

**Pattern extraction date:** 2026-07-31
