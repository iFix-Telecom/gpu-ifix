---
phase: 999.3-dashboard-card-do-pod-stt-tts-up-destroy
plan: 02
type: execute
wave: 2
depends_on: ["999.3-01"]
files_modified:
  - gateway/internal/admin/operations.go
  - gateway/internal/admin/operations_test.go
  - gateway/cmd/gateway/main.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "GET /admin/operations devolve o campo sttpod com o blob publicado, verbatim"
    - "Sem publicação no Redis, o campo sttpod some da resposta e os outros 4 painéis continuam intactos"
    - "Erro de Redis não vira 500 — /admin/operations responde 200 degradando só a seção sttpod"
    - "As rotas POST /admin/sttpod/state e /admin/sttpod/command estão montadas no adminRouter e herdam o X-Admin-Key"
  artifacts:
    - path: "gateway/internal/admin/operations.go"
      provides: "SttPodSection + seção fail-soft no agregado"
      contains: "SttPodSection"
    - path: "gateway/cmd/gateway/main.go"
      provides: "Wiring do SttPodHandler e das 2 rotas admin"
      contains: "NewSttPodHandler"
  key_links:
    - from: "gateway/cmd/gateway/main.go"
      to: "gateway/internal/admin/sttpod.go"
      via: "admin.NewSttPodHandler(rdb, log) + adminRouter.Method(POST, ...)"
      pattern: "adminRouter\\.Method\\(http\\.MethodPost, \"/sttpod/(state|command)\""
    - from: "gateway/internal/admin/operations.go"
      to: "gateway/internal/redisx/sttpod.go"
      via: "leitura de gw:sttpod:state e gw:sttpod:command"
      pattern: "redisx\\.SttPod(State|Command)Key\\(\\)"
---

<objective>
Expor o estado do pod 3060 no agregado que o dashboard já poll a cada 10s (D-13) e montar
as rotas novas no `adminRouter`.

Purpose: D-13 exige UMA fetch e UM poll — nenhuma rota nova no dashboard. O card lê o
campo `sttpod` do mesmo `GET /admin/operations` que já alimenta os outros 4 painéis, e
por isso a seção precisa ser fail-soft: Redis fora do ar não pode derrubar o painel de
FSM, custo, breakers e lifecycles.
Output: campo `sttpod` no `OperationsResponse`, dep `rdb` no handler, 2 rotas POST no router.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-CONTEXT.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-PATTERNS.md
@.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-01-SUMMARY.md
@gateway/internal/admin/operations.go

<interfaces>
<!-- Contratos fixados. O campo `sttpod` é o contrato que o plano 999.3-04 espelha em TS. -->

Shape final da seção (fonte de verdade para o TS do dashboard):
```go
// SttPodSection is the STT/TTS 3060 pod mirror (Phase 999.3, D-05/D-13).
type SttPodSection struct {
	State          json.RawMessage `json:"state"`            // blob verbatim do script
	PendingCommand json.RawMessage `json:"pending_command"`  // null quando não há
	UsdToBrl       float64         `json:"usd_to_brl"`       // cfg.USDToBRLRate (D-08)
}
```
Campo no agregado: `SttPod *SttPodSection \`json:"sttpod,omitempty"\`` — ponteiro +
omitempty: o campo SOME do JSON quando não há publicação nem comando.

Assinatura ATUAL dos construtores (ambos ganham `rdb redis.UniversalClient` como
penúltimo parâmetro, antes de `cfg`):
```go
func NewOperationsHandler(q *gen.Queries, b *breaker.Set, rec *primary.Reconciler,
	emergFSM *emerg.FSM, podCfg *podconfig.Loader, cfg config.Config, log *slog.Logger) *OperationsHandler
func newOperationsHandlerWithQueries(q operationsQueries, b *breaker.Set, rec *primary.Reconciler,
	emergFSM *emerg.FSM, podCfg *podconfig.Loader, cfg config.Config, log *slog.Logger) *OperationsHandler
```

Call-sites que QUEBRAM com a mudança de assinatura (todos precisam de update):
- `gateway/cmd/gateway/main.go:1336`
- `gateway/internal/admin/operations_test.go:183`
- `gateway/internal/admin/operations_test.go:285`
- `gateway/internal/admin/operations_test.go:306`

Já disponível no escopo de `main.go` (não precisa de fiação nova):
- `rdb` (linha ~222, `redisx.NewClient(ctx, cfg)`) e campo `rdb redis.UniversalClient`
  já existente no struct `proxies` (linha ~101, atribuído em ~1391)
- `cfg.USDToBRLRate`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SttPodSection fail-soft no OperationsHandler</name>
  <files>gateway/internal/admin/operations.go</files>
  <read_first>
    - gateway/internal/admin/operations.go (arquivo inteiro) — struct `OperationsResponse` (~:51-57), struct `OperationsHandler` + os DOIS construtores (~:118-165), montagem da resposta em `ServeHTTP` (~:200-205), molde de seção degradável `scheduleSection` (~:296-308), guard de dep nil `breakerRows` (~:339-343)
    - gateway/internal/redisx/sttpod.go (criado no plano 999.3-01)
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-RESEARCH.md §Pattern 3 e §Code Examples (bloco `sttPodSection`)
  </read_first>
  <action>
    Adicionar em `operations.go` (implementa D-05/D-08/D-13):
    - `type SttPodSection struct` exatamente com os 3 campos do bloco `<interfaces>`
      (`State`/`PendingCommand` como `json.RawMessage`, `UsdToBrl float64`), com doc-comment
      dizendo que o blob é opaco por decisão D-05 e que o gateway não interpreta campo nenhum.
    - Campo `SttPod *SttPodSection \`json:"sttpod,omitempty"\`` no fim do `OperationsResponse`,
      com comentário apontando que o dashboard espelha essa struct field-for-field.
    - Campo `rdb redis.UniversalClient` no struct `OperationsHandler`, comentado como nil-safe
      (mesmo estilo dos comentários de `rec`/`emergFSM`/`podCfg`).
    - Parâmetro `rdb redis.UniversalClient` em `NewOperationsHandler` E em
      `newOperationsHandlerWithQueries`, posicionado ANTES de `cfg config.Config`, propagado
      nos dois literais de struct. Atualizar o doc-comment de `NewOperationsHandler`
      mencionando que `rdb` nil => seção sttpod ausente.
    - Método `func (h *OperationsHandler) sttPodSection(ctx context.Context) *SttPodSection`:
      retorna nil imediatamente se `h.rdb == nil`; `context.WithTimeout(ctx, 2*time.Second)`
      com `defer cancel()`; `h.rdb.Get(ctx, redisx.SttPodStateKey()).Bytes()` e
      `h.rdb.Get(ctx, redisx.SttPodCommandKey()).Bytes()`; erro que NÃO seja
      `errors.Is(err, redis.Nil)` vira `h.log.Warn(...)` e o valor fica vazio — NUNCA
      propagar erro para o status HTTP (Pitfall: derrubaria os outros 4 painéis); se
      `len(state) == 0 && len(cmd) == 0` retorna nil (omitempty faz o campo sumir);
      caso contrário devolve `&SttPodSection{State: state, PendingCommand: cmd, UsdToBrl: h.cfg.USDToBRLRate}`.
      Passar `json.RawMessage(nil)` quando o respectivo valor estiver vazio, para o encoder
      renderizar `null` e não `""`.
    - Chamar `SttPod: h.sttPodSection(r.Context())` na montagem do `resp` em `ServeHTTP`,
      junto das outras seções.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && go build ./gateway/... 2>&1 | head -20; gofmt -l gateway/internal/admin</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SttPodSection' gateway/internal/admin/operations.go` retorna >= 4
    - `grep -c 'sttpod,omitempty' gateway/internal/admin/operations.go` retorna 1
    - `grep -c 'redis.Nil' gateway/internal/admin/operations.go` retorna >= 1
    - `grep -c 'USDToBRLRate' gateway/internal/admin/operations.go` retorna >= 1
    - `sttPodSection` NÃO contém `http.Error`, `WriteOpenAIError` nem `return err` — degradação só por log (`grep -c 'WriteOpenAIError' gateway/internal/admin/operations.go` permanece igual ao valor pré-mudança)
    - `gofmt -l gateway/internal/admin` não imprime nada
    - `go build ./gateway/...` só falha nos call-sites de teste ainda não atualizados (resolvidos na Task 2)
  </acceptance_criteria>
  <done>Agregado carrega o campo sttpod, fail-soft, com o rate USD→BRL da config única.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar call-sites e cobrir a seção sttpod em operations_test.go</name>
  <files>gateway/internal/admin/operations_test.go</files>
  <read_first>
    - gateway/internal/admin/operations_test.go (linhas 1-320) — helper miniredis (~:60-79), struct de decode LOCAL `opResponse` (~:95-121), helper `doOperationsRequest` (~:123-135), asserts padrão (~:187-192), e os 3 call-sites de `newOperationsHandlerWithQueries` (:183, :285, :306)
    - gateway/internal/admin/operations.go (modificado na Task 1)
  </read_first>
  <action>
    Atualizar os 3 call-sites de `newOperationsHandlerWithQueries` (linhas ~183, ~285, ~306)
    passando `nil` na nova posição de `rdb` — esses testes existentes NÃO exercitam sttpod e
    devem continuar provando que `rdb == nil` não quebra nada.
    Adicionar `SttPod *json.RawMessage \`json:"sttpod"\`` na struct de decode local
    `opResponse` (ponteiro para conseguir provar ausência).
    Adicionar 3 testes novos:
    - `TestOperations_SttPodAbsent_WhenNoRedis`: handler com `rdb` nil => 200 e
      `body.SttPod == nil` (campo ausente do JSON); asserir também que os outros campos
      (`fsm`, `breakers`) continuam presentes.
    - `TestOperations_SttPodAbsent_WhenNoKey`: handler com miniredis VAZIO => 200 e
      `body.SttPod == nil`.
    - `TestOperations_SttPodVerbatim`: pré-gravar em `gw:sttpod:state` um blob com campo
      aninhado conhecido, gravar `gw:sttpod:command` com um objeto, e asserir que o JSON de
      resposta traz `sttpod.state` byte-a-byte igual ao gravado (unmarshal parcial +
      `bytes.Equal` do RawMessage), `sttpod.pending_command` não-nulo, e `sttpod.usd_to_brl`
      igual ao `USDToBRLRate` da cfg de teste. Confirmar que o GET **não** apaga a chave de
      comando (o consumo é só no POST do script — GETDEL vive em `sttpod.go`).
    Reusar o helper de miniredis já existente no arquivo em vez de criar outro; se ele
    devolver apenas o `*breaker.Set`, extrair uma função que devolva também o `*redis.Client`
    sem alterar a semântica dos testes atuais.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && go test ./gateway/internal/admin/ -run 'TestOperations|TestSttPod' -count=1 -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./gateway/internal/admin/ -count=1` sai com código 0 (pacote inteiro, zero regressão)
    - A saída lista `TestOperations_SttPodAbsent_WhenNoRedis`, `TestOperations_SttPodAbsent_WhenNoKey` e `TestOperations_SttPodVerbatim` todos com `--- PASS`
    - `grep -c 'newOperationsHandlerWithQueries' gateway/internal/admin/operations_test.go` retorna >= 6 e nenhum call-site com a aridade antiga (o pacote compila)
    - `gofmt -l gateway/internal/admin` não imprime nada
  </acceptance_criteria>
  <done>Fail-soft e verbatim provados; nenhum teste existente do painel Operação regrediu.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring do SttPodHandler e das rotas no adminRouter</name>
  <files>gateway/cmd/gateway/main.go</files>
  <read_first>
    - gateway/cmd/gateway/main.go linhas 1330-1400 (construção dos handlers admin + campos do struct `proxies`) e 1589-1660 (montagem do `adminRouter` com `admin.Middleware` e as rotas Phase 18)
    - gateway/internal/admin/sttpod.go (criado no plano 999.3-01)
    - .planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-PATTERNS.md §`gateway/cmd/gateway/main.go`
  </read_first>
  <action>
    Três edits pontuais em `main.go`, todos no molde do wiring da Phase 18:
    (1) Ao lado de `adminOperationsHandler` (~linha 1336): passar `rdb` no novo parâmetro de
        `admin.NewOperationsHandler` e criar
        `adminSttPodHandler := admin.NewSttPodHandler(rdb, log)` com comentário de bloco
        nomeando a fase e as decisões: "Phase 999.3 (D-01/D-09) — espelho do pod STT/TTS
        3060 publicado por ops/vast-3060/vast3060.py; pipe burro pro Redis, GETDEL entrega o
        comando pendente no mesmo POST".
    (2) Adicionar o campo `adminSttPodHandler *admin.SttPodHandler` no struct `proxies` e
        atribuí-lo no literal de construção (~linha 1387-1391), junto de
        `adminTenantHandler`/`adminKeysHandler`. O campo `rdb` já existe — não duplicar.
    (3) No bloco do `adminRouter` (~linha 1632), adicionar, com o guard `if px.adminSttPodHandler != nil`:
        `adminRouter.Method(http.MethodPost, "/sttpod/state", http.HandlerFunc(px.adminSttPodHandler.PostState))`
        e `adminRouter.Method(http.MethodPost, "/sttpod/command", http.HandlerFunc(px.adminSttPodHandler.PostCommand))`.
        NÃO adicionar nenhum middleware de auth — `adminRouter.Use(admin.Middleware(px.adminVerifier, log))`
        (~linha 1595) já cobre todo o subrouter (D-04, zero código de auth novo).
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && go build ./... && go vet ./gateway/... && gofmt -l gateway/cmd/gateway</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'NewSttPodHandler' gateway/cmd/gateway/main.go` retorna 1
    - `grep -c '"/sttpod/state"' gateway/cmd/gateway/main.go` retorna 1 e `grep -c '"/sttpod/command"' gateway/cmd/gateway/main.go` retorna 1
    - Ambas as rotas estão registradas com `adminRouter.Method(http.MethodPost, ...)` — `grep -c 'router.Method(http.MethodPost, "/sttpod' gateway/cmd/gateway/main.go` retorna 2
    - `grep -c 'adminSttPodHandler' gateway/cmd/gateway/main.go` retorna >= 4 (declaração, campo do struct, atribuição, guard+rotas)
    - Nenhuma chamada a `admin.Middleware` nova: `grep -c 'admin.Middleware' gateway/cmd/gateway/main.go` mantém o valor pré-mudança
    - `go build ./...` sai com código 0
  </acceptance_criteria>
  <done>Rotas montadas sob o gate X-Admin-Key existente; binário compila com o handler novo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → `/admin/*` | Toda rota admin é atingível pelo edge público; o gate é o X-Admin-Key (bcrypt) do middleware |
| Redis → `GET /admin/operations` | Blob de terceiro (script) é re-servido ao dashboard sem sanitização — é dado de operador, não HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-08 | Spoofing | rotas `/admin/sttpod/*` em `main.go` | mitigate | Rotas registradas DENTRO do `adminRouter` que já tem `adminRouter.Use(admin.Middleware(px.adminVerifier, log))`; guard `if px.adminSttPodHandler != nil` evita rota órfã sem handler |
| T-999.3-09 | Denial of Service | `OperationsHandler.sttPodSection` | mitigate | `context.WithTimeout` de 2s por leitura + degradação por log: Redis lento/fora não bloqueia nem derruba os outros 4 painéis (teste `TestOperations_SttPodAbsent_WhenNoRedis`) |
| T-999.3-05 | Information Disclosure | campo `sttpod.state` re-servido | mitigate | O gateway devolve o blob verbatim; a garantia de que segredos NUNCA entram no payload é do produtor (`build_payload` campo-a-campo no plano 999.3-03). Consumidor do `/admin/operations` já é autenticado por X-Admin-Key e proxied server-side |
| T-999.3-SC | Tampering | go/npm/pip installs | accept | Nenhum pacote novo (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go build ./...` verde
- `go test ./gateway/internal/admin/ -count=1` verde
- `go test ./gateway/... -count=1 -race -timeout=5m` verde (mesma suíte do CI `build-gateway.yml`)
- `gofmt -l gateway/` sem saída
</verification>

<success_criteria>
`GET /admin/operations` carrega `sttpod` quando há publicação, omite o campo quando não há,
e responde 200 mesmo com Redis indisponível. As duas rotas POST existem no router sob o
X-Admin-Key herdado.
</success_criteria>

<output>
Create `.planning/phases/999.3-dashboard-card-do-pod-stt-tts-up-destroy/999.3-02-SUMMARY.md` when done
</output>
