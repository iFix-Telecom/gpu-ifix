---
phase: 11
plan: 11-01
slug: load-test-scaffolding
wave: 1
depends_on: []
requirements: [PRD-01]
files_modified:
  - path: scripts/integration-smoke/audit-log-export.py
    kind: NEW
  - path: scripts/integration-smoke/load-replay.py
    kind: NEW
  - path: scripts/integration-smoke/load-replay-report-schema.json
    kind: NEW
  - path: scripts/integration-smoke/fixtures/whatsapp-sample.ogg
    kind: NEW
  - path: .planning/load-test-fixtures/.gitignore
    kind: NEW
  - path: .gitignore
    kind: MOD
autonomous: true
estimated_duration: 120
---

<objective>
Scaffold the PRD-01 load-test infrastructure with four invariants enforced at the file level (each tagged to the cross-AI review finding it closes):

1. **[reviews HIGH #1] Body source-of-truth lives in `ai_gateway.audit_log_content.prompt` (JSONB).** The exporter SELECT MUST `LEFT JOIN ai_gateway.audit_log_content ON (request_id, ts)` to pull the request body. `ai_gateway.audit_log` alone does NOT carry the body — schema verified against `gateway/db/migrations/0003_create_audit_log_partitioned.sql` (envelope) + `0004_create_audit_log_content_partitioned.sql` (content; columns: `request_id UUID`, `ts TIMESTAMPTZ`, `prompt JSONB`, `response JSONB`).
2. **[reviews MEDIUM #2] Zero secret-at-rest in JSONL fixtures.** Fixtures store ONLY `tenant_slug`. The replay engine resolves the per-tenant API key at request time from env var `IFIX_KEY_${TENANT_SLUG^^}` (uppercase, hyphens→underscores). No `_replay_api_key` field, no `Authorization` header, no raw secret material — ever.
3. **[reviews MEDIUM #3] Multipart STT replay uses a deterministic fixture audio file (NOT re-constructed from DB).** Per migration `0003` D-B6 invariant, audit_log persists only Whisper *envelope metadata* (`audio_filename`, `audio_mime`, `audio_size_bytes`, `audio_duration_s`, `audio_language`) — the raw audio bytes are NEVER stored. Replay therefore attaches `scripts/integration-smoke/fixtures/whatsapp-sample.ogg` as the file part for every `/v1/audio/transcriptions` request. JSON Content-Type does NOT apply to STT routes.
4. **[reviews LOW #4] `zero_5xx_panic` is an externally-observed gate, not a body-content heuristic.** The replay engine writes `zero_5xx_panic: null` initially. The operator MUST set it to true/false post-run based on a Sentry query (`level=fatal AND service=ifix-ai-gateway` count over run window) AND a gateway-log query (count of lines matching `panic:` over run window). `all_passed` stays null until the operator flips the gate. This removes the unreliable response-body heuristic — recoverers should NEVER leak panic strings to clients.

Plus [reviews LOW #5] an optional `--speedup` multiplier on the replay engine (Gemini suggestion) so operators can compress timing if the chosen audit_log window is too quiet to hit saturation.

Zero live Vast spend in this plan — UAT execution lives in 11-06.
</objective>

<must_haves>
  truths:
    - "Phase 11 introduces ZERO new server-side runtime dependencies — psycopg, httpx, structlog, jsonschema are already in scripts/integration-smoke/requirements.txt. (Dashboard 2FA UI adds up to 4 packages scoped to plan 11-02 only, gated by inventory + slopcheck.)"
    - "Sanitized audit_log JSONL excludes sensitive-class tenants from baseline replay (PRD-01 + Pitfall 1)"
    - "load-replay.py output validates against load-replay-report-schema.json (WR-05 hard-fail)"
    - "All gate names map verbatim to D-04 SLO v1.0: P95 chat ≤5s + embed ≤1s + STT ≤10s + error <1% non-503 + zero 5xx panic"
    - "Secret-once CLI discipline applies (Pattern A) — no committed defaults for tokens"
    - "Cross-cutting hard rule: NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in committed files (plans, evidence, schemas, JSONL fixtures, load reports). Tenant API keys resolve at REPLAY time from env vars (IFIX_KEY_${TENANT_SLUG^^}) — never stored in JSONL fixtures."
    - "[reviews HIGH #1] Body column source-of-truth: the request body lives in ai_gateway.audit_log_content.prompt (JSONB). The exporter MUST LEFT JOIN audit_log → audit_log_content ON (request_id, ts). audit_log alone does NOT carry the body."
    - "[reviews LOW #4] Panic gate: zero_5xx_panic is an OBSERVATION field populated externally (operator queries Sentry post-run AND counts gateway log lines matching 'panic:'). The exporter does NOT infer panic from response body strings — recoverers should never leak panic text."
    - "[reviews MEDIUM #3] STT multipart: /v1/audio/transcriptions records replay as multipart/form-data with a stub audio file (scripts/integration-smoke/fixtures/whatsapp-sample.ogg); JSON Content-Type does NOT apply to STT routes. Audio bytes are NEVER in audit_log_content (D-B6) — fixture is the only legitimate source."
    - "[reviews MEDIUM #2] Tenant API keys: fixtures contain `tenant_slug` ONLY. Authorization header is computed at request time from env `IFIX_KEY_${TENANT_SLUG^^}` (uppercase, hyphens→underscores). Missing env var = hard exit BEFORE traffic flows."
  must_haves:
    - scripts/integration-smoke/audit-log-export.py exists and is executable
    - audit-log-export.py SELECT joins audit_log with audit_log_content (LEFT JOIN ON request_id + ts) to pull `prompt` JSONB as the body source
    - JSONL records DO NOT contain any field named `_replay_api_key`, `api_key`, `Authorization`, or any raw secret-like value
    - JSONL records DO contain `tenant_slug` (resolved from tenants table) so load-replay.py can compute env-var name at runtime
    - scripts/integration-smoke/load-replay.py exists and is executable
    - load-replay.py resolves the per-request Authorization header at runtime from env `IFIX_KEY_${TENANT_SLUG^^}`; raises a clear error if the env var is missing for any tenant present in the fixture
    - load-replay.py treats `/v1/audio/transcriptions` as multipart/form-data with a stub audio file part; other routes remain application/json
    - scripts/integration-smoke/fixtures/whatsapp-sample.ogg exists (small deterministic OGG/Opus sample, ≤16 KB) — used as the canonical STT replay file part because audit_log_content does NOT store audio bytes (D-B6)
    - scripts/integration-smoke/load-replay-report-schema.json exists and is valid JSON Schema Draft 2020-12
    - load-replay-report-schema.json `gates.zero_5xx_panic` is typed `[boolean, null]`; description explains the dual-source external observation (Sentry query + log grep) and forbids body-content heuristics
    - load-replay-report-schema.json `gates.all_passed` is typed `[boolean, null]` — null whenever any required gate is still null
    - load-replay-report-schema.json includes a `gates_external_inputs` sibling object documenting Sentry URL + log query commands + `verified_at` timestamp for operator audit trail
    - load-replay-report-schema.json forbids fields likely to carry secrets — `additionalProperties:false` enforced AT EVERY nested object; description in the schema explicitly forbids `headers`, `authorization`, `cookie`, `api_key`, `bearer`, `prompt_text`, `response_text`, `body`, `dsn`, `password`, `secret` keys
    - .planning/load-test-fixtures/.gitignore commits an empty allow-rule directory
    - Root .gitignore has explanatory comment + `.planning/load-test-fixtures/` and `.planning/legal/*.pdf` patterns appended
  key_links:
    - from: scripts/integration-smoke/load-replay.py
      to: scripts/integration-smoke/load-replay-report-schema.json
      via: Path(__file__).parent + jsonschema.Draft202012Validator.validate
      pattern: "load-replay-report-schema.json"
    - from: scripts/integration-smoke/audit-log-export.py
      to: ai_gateway.audit_log + ai_gateway.audit_log_content
      via: psycopg.connect(pg_dsn) + SELECT … LEFT JOIN audit_log_content ON (request_id, ts)
      pattern: "FROM ai_gateway.audit_log .* JOIN ai_gateway.audit_log_content|audit_log_content"
    - from: scripts/integration-smoke/load-replay.py
      to: env IFIX_KEY_<TENANT_SLUG_UPPER>
      via: os.environ[f"IFIX_KEY_{slug.upper().replace('-','_')}"]
      pattern: "IFIX_KEY_"
    - from: scripts/integration-smoke/load-replay.py
      to: scripts/integration-smoke/fixtures/whatsapp-sample.ogg
      via: httpx files={'file': (open(audio_stub, 'rb'), 'audio/ogg')}
      pattern: "whatsapp-sample\\.ogg|audio_stub"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude → DO Postgres | psycopg query against `ai_gateway.audit_log` + `audit_log_content`; DSN is secret |
| ops-claude → ai-gateway prod HTTPS | replay request bodies cross this boundary |
| audit_log_content.prompt JSONB → JSONL fixture | sanitization step strips PII before write |
| env IFIX_KEY_* → load-replay.py | tenant API keys read from env vars ONLY, never from fixture file |
| fixtures/whatsapp-sample.ogg → STT replay file part | deterministic stub; audio bytes are NEVER reconstructed from DB (D-B6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-LOAD-01 | Information Disclosure | audit-log-export.py | mitigate | Per Pitfall 1: replace `tool_calls[].function.arguments` with `{"_replay_placeholder": true}` in the JOINed `audit_log_content.prompt` JSONB; skip all `data_class=sensitive` rows in baseline (PRD-01); for `/v1/audio/transcriptions` rows the sanitizer emits a marker (`_replay_audio_part: "stub"`) so replay knows to attach the fixture file at runtime. No api_key/headers/cookie fields written to JSONL. |
| T-11-LOAD-02 | Information Disclosure | psycopg error logging | mitigate | Never include DSN in `log.error(...)` payload; pattern from smoke-sensitive-failover.py:472-498 strips str(e) to first 300 chars |
| T-11-LOAD-03 | Tampering | load-replay-report-schema.json | mitigate | `additionalProperties: false` at every nested level + `schema_version` const `"1.0.0"` + WR-05 hard-fail on validation error + explicit "forbidden key names" list in schema description (api_key, headers, authorization, cookie, prompt_text, response_text, bearer, body, dsn, password, secret) |
| T-11-LOAD-04 | Information Disclosure | .planning/load-test-fixtures/*.jsonl on disk | mitigate | Gitignored at directory level; fixture is local-only; never contains api_key/Authorization |
| T-11-LOAD-05 | Tampering | load-replay.py env-var key resolution | mitigate | KeyError on missing IFIX_KEY_<TENANT_SLUG_UPPER> at startup is a hard exit (operator notices BEFORE traffic flows); never logged with key value |
| T-11-LOAD-06 | Information Disclosure | report file may grow to contain leaked PII | mitigate | Schema forbids `prompt_text`, `response_text`, `headers`, `authorization`, `cookie`, `api_key`, `bearer`, `body`, `dsn`, `password`, `secret`; report writer emits ONLY pre-vetted columns from the metric record (request_id, tenant_slug, route, status_code, elapsed_ms, upstream, error_class) |
| T-11-LOAD-07 | Repudiation | `zero_5xx_panic` gate verification | mitigate | Gate is `[boolean, null]` in schema; `gates_external_inputs.verified_at` ISO-8601 + `gates_external_inputs.sentry_query_url` + `gates_external_inputs.log_grep_command` create an audit trail; `all_passed` stays null until operator flips the gate (prevents accidental green builds with unverified panic counts) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Phase 11 introduces zero new dependencies; slopcheck verified 2026-05-27 in 11-RESEARCH.md §Package Legitimacy Audit |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@scripts/integration-smoke/smoke-sensitive-failover.py
@scripts/integration-smoke/sensitive-failover-report-schema.json
@scripts/integration-smoke/requirements.txt
@gateway/db/migrations/0003_create_audit_log_partitioned.sql
@gateway/db/migrations/0004_create_audit_log_content_partitioned.sql
@.gitignore
</context>

<tasks>

<task type="auto">
  <id>11-01-01</id>
  <title>Task 1 [reviews HIGH #1, MEDIUM #2]: Author audit-log-export.py — JOIN audit_log_content.prompt + tenant_slug-only fixtures</title>
  <read_first>
    - scripts/integration-smoke/smoke-sensitive-failover.py (psycopg shape lines 442-505 — query_audit; structlog bind module label; secret-once discipline at 151-228)
    - .planning/phases/11-prod-hardening/11-PATTERNS.md §audit-log-export.py (Pattern B 8-section spine + Pattern A secret-once)
    - .planning/phases/11-prod-hardening/11-RESEARCH.md §Pitfall 1 (sanitization invariant — tool_calls placeholder + Whisper file stub + sensitive tenant skip)
    - gateway/db/migrations/0003_create_audit_log_partitioned.sql (audit_log columns: request_id UUID, ts TIMESTAMPTZ, tenant_id UUID, api_key_id UUID, data_class enum, route TEXT, method TEXT, upstream TEXT, status_code SMALLINT, latency_ms, tokens_in/out, cost_brl, error_code, audio_filename/mime/size_bytes/duration_s/language. PRIMARY KEY (request_id, ts). Note D-B6 comment: audio bytes are NEVER stored — only envelope metadata.)
    - gateway/db/migrations/0004_create_audit_log_content_partitioned.sql (audit_log_content columns: request_id UUID, ts TIMESTAMPTZ, prompt JSONB, response JSONB. PRIMARY KEY (request_id, ts). NO FK to audit_log — JOIN purely on composite PK match. This is the BODY SOURCE referenced by reviews HIGH #1.)
    - scripts/integration-smoke/sensitive-failover-report-schema.json (top-of-file shape for sibling Pattern C tail)
  </read_first>
  <action>
    Create `scripts/integration-smoke/audit-log-export.py` per D-01 (PRD-01 fixture generation). Module docstring states: read sanitized audit_log + audit_log_content dev rows into a JSONL fixture for load-replay.py consumption. The body column lives in `audit_log_content.prompt` (JSONB) — verified against migration 0004; `audit_log` alone has only envelope metadata. This addresses reviews HIGH #1 (Codex) which flagged that the prior SELECT referenced `sanitize_body(body, data_class)` without fetching any body column.

    argparse args (5): `--dsn` (env DASHBOARD_DATABASE_URL or AI_GATEWAY_DB_URL fallback, secret-once Pattern A — no committed default), `--window-start` ISO-8601, `--window-end` ISO-8601, `--out` JSONL output path, `--include-sensitive` flag (default False — Pitfall 1 + Open Question #1 baseline excludes sensitive tenants).

    Use psycopg double-`with` context-manager pattern with `connect_timeout=10`. SELECT body source MUST JOIN audit_log_content. Use this canonical SQL shape (column names verified against migrations 0003/0004 — no speculation):

    - SELECT alias `a` = `ai_gateway.audit_log`
    - SELECT alias `c` = `ai_gateway.audit_log_content`
    - SELECT alias `t` = `ai_gateway.tenants` (verify table name via `\dt ai_gateway.*` at startup; substitute the canonical tenants table if the schema differs — executor must NOT speculate; if not found, fall back to selecting `a.tenant_id` and accept that downstream env-var resolution requires a separate slug lookup)
    - Columns SELECTed: `a.request_id`, `a.ts`, `a.tenant_id`, `t.slug AS tenant_slug`, `a.route`, `a.upstream`, `a.status_code`, `a.data_class`, `a.audio_filename`, `a.audio_mime`, `c.prompt AS body`
    - FROM `ai_gateway.audit_log a`
    - `LEFT JOIN ai_gateway.audit_log_content c ON c.request_id = a.request_id AND c.ts = a.ts` (composite PK match — there is NO FK per migration 0004 docstring)
    - `LEFT JOIN ai_gateway.tenants t ON t.id = a.tenant_id`
    - WHERE `a.ts >= %s AND a.ts < %s AND (%s OR a.data_class != 'sensitive')`
    - ORDER BY `a.ts ASC`

    For each row: compute `_replay_delay_s` = ts - prev_ts (seconds; first row = 0.0). Call `sanitize_body(prompt_jsonb, data_class, route, audio_filename)` which:
    - Returns `None` when `data_class == 'sensitive'` (caller skips row entirely — PRD-01 baseline);
    - Replaces `tool_calls[].function.arguments` (recursively under `messages[].tool_calls[]` AND top-level `tool_calls[]`) with `{"_replay_placeholder": true}` when present;
    - Drops keys `audio.filename`, `file.url`, `image.url` if present in the JSONB;
    - For route `/v1/audio/transcriptions`: returns the sanitized prompt JSONB MINUS any base64 audio bytes (key names: `audio`, `file`, `data`, `bytes`), AND adds marker `{"_replay_audio_part": "stub", "_replay_audio_mime": audio_mime or "audio/ogg"}` so the replay engine knows to attach the fixture OGG file at runtime. **The audio bytes are NEVER reconstructed from DB** — migration 0003 line 23-24 comment (D-B6) explicitly states "audio stays out of content table". Reviewer-flagged limitation: STT replay uses fixed stub audio (not the original utterance) — documented in `<pitfalls>` section of the script docstring.

    Tenant API key resolution: this exporter DOES NOT touch tenant API keys (closes reviews MEDIUM #2). It writes only `tenant_slug` to the JSONL. load-replay.py resolves the actual key at runtime from env `IFIX_KEY_${TENANT_SLUG^^}`. There is NO `_replay_api_key` field in the JSONL line.

    Emit JSONL line per non-skipped row with exactly these 9 fields (no more, no less):
    `request_id` (str UUID), `ts` (ISO-8601 str), `tenant_slug` (str — REQUIRED, error if NULL), `route` (str), `upstream` (str or null), `status_code` (int), `data_class` (str — `normal`/`sensitive`), `_replay_delay_s` (float seconds), `_sanitized_body` (object — the post-sanitization JSONB; may include `_replay_placeholder` markers and/or `_replay_audio_part` marker for STT).

    Forbidden JSONL fields (negative grep enforced in <verify>): `tenant_id` (UUID; tenant_slug suffices), `_replay_api_key`, `api_key`, `Authorization`, `headers`, `cookie`, raw `body` field name (use `_sanitized_body`), `prompt_text`, `response_text`.

    Apply Pattern A secret-once: NEVER log DSN; NEVER log raw keys; on psycopg error log only `str(e)[:300]`. Apply 8-section spine per Pattern B (Module docstring → Constants → Config+CLI → Helpers → Audit-DB → Gates+exit → Orchestration → main). Exit codes: 0=ok, 1=runtime error, 2=usage error. Per D-01 + Pitfall 1: write to `.planning/load-test-fixtures/audit-export-<window_start_iso>.jsonl` by default.
  </action>
  <acceptance_criteria>
    - File `scripts/integration-smoke/audit-log-export.py` exists and is executable (`chmod +x`).
    - `python scripts/integration-smoke/audit-log-export.py --help` exits 0 and prints exactly 5 argument groups: `--dsn`, `--window-start`, `--window-end`, `--out`, `--include-sensitive`.
    - Source assertion: `grep -E "LEFT JOIN ai_gateway\.audit_log_content|JOIN .* audit_log_content" scripts/integration-smoke/audit-log-export.py` returns ≥1 match (proves JOIN to body source — closes reviews HIGH #1).
    - Source assertion: `grep -E "c\.prompt AS body|c\.prompt" scripts/integration-smoke/audit-log-export.py` returns ≥1 match (proves prompt column selected).
    - Source assertion: `grep -E "_replay_placeholder" scripts/integration-smoke/audit-log-export.py` returns ≥1 match (proves tool_calls sanitization).
    - Source assertion: `grep -E "_replay_audio_part" scripts/integration-smoke/audit-log-export.py` returns ≥1 match (proves STT marker emission).
    - Negative grep (closes reviews MEDIUM #2): `grep -E '_replay_api_key|"api_key"\s*:|"Authorization"\s*:' scripts/integration-smoke/audit-log-export.py` returns 0 matches (no API-key field in exporter).
    - Negative grep: `grep -E 'log\.(info|warn|error).*\b(api_key|dsn|password|secret)\b' scripts/integration-smoke/audit-log-export.py` returns 0 matches (no raw secret in log statements).
    - Pattern B 8-section spine: `grep -cE '^#\s*(Module docstring|Constants|Config\+CLI|Helpers|Audit-DB|Gates\+exit|Orchestration|main)' scripts/integration-smoke/audit-log-export.py` returns ≥8 (or equivalent section comments).
    - `data_class != 'sensitive'` clause present in SQL: `grep -E "data_class\s*(!=|<>)\s*'sensitive'" scripts/integration-smoke/audit-log-export.py` returns ≥1 match.
  </acceptance_criteria>
  <verify>
    <automated>chmod +x scripts/integration-smoke/audit-log-export.py && python scripts/integration-smoke/audit-log-export.py --help 2>&1 | grep -E "dsn|window-start|window-end|out|include-sensitive" | wc -l | grep -E "^\s*[5-9]" && grep -E "LEFT JOIN ai_gateway\.audit_log_content|JOIN .* audit_log_content" scripts/integration-smoke/audit-log-export.py && grep -E "_replay_audio_part" scripts/integration-smoke/audit-log-export.py && ! grep -E '_replay_api_key|"api_key"\s*:|"Authorization"\s*:' scripts/integration-smoke/audit-log-export.py</automated>
  </verify>
  <done>All 9 acceptance criteria pass. Closes reviews HIGH #1 (JOIN added with body column) and MEDIUM #2 (no `_replay_api_key` field).</done>
</task>

<task type="auto">
  <id>11-01-02</id>
  <title>Task 2 [reviews MEDIUM #2, #3, LOW #4, #5]: Author load-replay.py + load-replay-report-schema.json + fixture audio file</title>
  <read_first>
    - scripts/integration-smoke/smoke-sensitive-failover.py (full 8-section spine, schema-validate tail at 756-770)
    - scripts/integration-smoke/sensitive-failover-report-schema.json (top-of-file shape — $schema, $id, additionalProperties:false, schema_version const)
    - .planning/phases/11-prod-hardening/11-PATTERNS.md §load-replay.py + §load-replay-report-schema.json (target shape with gate names locked to D-04)
    - .planning/phases/11-prod-hardening/11-RESEARCH.md §Pattern 3 (httpx skeleton lines 326-394) + §Architecture Patterns System Architecture Diagram (request path through edge Traefik to n8n-ia-vm:8080)
    - .planning/phases/11-prod-hardening/11-CONTEXT.md D-04 (P95 chat ≤5s + embed ≤1s + STT ≤10s + error <1% non-503 + zero 5xx panic)
    - gateway/db/migrations/0003_create_audit_log_partitioned.sql lines 22-29 (D-B6 invariant — audio bytes NOT in DB; fixture is the only legitimate source for STT replay)
  </read_first>
  <action>
    Create THREE artifacts in this single task — they form one contract surface that must be authored together:

    **(a) `scripts/integration-smoke/load-replay.py`** per D-05/D-06.

    argparse args (7):
    - `--gateway-url` (env SMOKE_GATEWAY_URL or default `https://ai-gateway.converse-ai.app`)
    - `--fixture` JSONL path required
    - `--duration` seconds (D-03 default 1800)
    - `--max-concurrency` int default 50
    - `--out` report.json output path required
    - `--speedup` float default 1.0 — **[reviews LOW #5]** Gemini-suggested optional flag. Replay delays are divided by this multiplier (e.g. `--speedup 2.0` compresses traffic 2x to densify RPS when the chosen audit_log window is too quiet to hit saturation). Reject values `<= 0` with exit 2.
    - `--audio-stub` path (env SMOKE_AUDIO_STUB or default `scripts/integration-smoke/fixtures/whatsapp-sample.ogg`) — see (c) below.

    **Startup pre-flight (closes reviews MEDIUM #2):** scan the fixture, collect the set of unique `tenant_slug` values. For each, compute env-var name `f"IFIX_KEY_{slug.upper().replace('-', '_')}"`. If ANY required env var is missing OR empty, raise a clear error listing the missing var names (NOT the values) and exit code 2 BEFORE any HTTP traffic flows. Cache the resolved keys in a module-local dict — NEVER log the key value, only the var name + a 4-char prefix for diagnostics (e.g. `IFIX_KEY_CHAT_IFIX=ifix...`).

    HTTP client: `httpx.AsyncClient(http2=True, timeout=httpx.Timeout(60.0, connect=10.0))` + `asyncio.Semaphore(max_concurrency)`. Preserve original timing — between successive records do `await asyncio.sleep(rec.get("_replay_delay_s", 0.0) / args.speedup)`.

    **Per-route request shape (closes reviews MEDIUM #3):**
    - For `route == "/v1/audio/transcriptions"`: build multipart/form-data using httpx `files={"file": (Path(args.audio_stub).name, open(args.audio_stub, "rb"), "audio/ogg")}` + `data={"model": rec["_sanitized_body"].get("model", "whisper-1"), "language": rec["_sanitized_body"].get("language", "pt")}` + other form fields recovered from `_sanitized_body` minus `_replay_audio_part` marker. Do NOT set `Content-Type` manually — httpx + `files=` computes the multipart boundary. Document in script docstring `<pitfalls>` block: "STT replay uses fixed stub audio (whatsapp-sample.ogg) because audit_log_content does NOT persist audio bytes per migration 0003 D-B6. Replay therefore measures STT pipeline latency on a representative file, not on the literal original utterance — this is a deliberate limitation."
    - For all other routes: set `Content-Type: application/json` + `content=json.dumps(rec["_sanitized_body"]).encode()` (use `content=` not `json=` to preserve byte-exact framing).

    **Authorization header per request:** resolve key from the startup-cached dict — `headers["Authorization"] = f"Bearer {keys[rec['tenant_slug']]}"`. Add `headers["X-Idempotency-Key"] = rec["request_id"]`.

    Capture per-request metric record fields (and ONLY these — schema forbids extras):
    `request_id` (str), `tenant_slug` (str), `route` (str), `status_code` (int — use -1 for transport errors), `elapsed_ms` (int — `(time.monotonic()-t0)*1000`), `upstream` (str or null — from `X-Upstream` response header), `error_class` (str — short label only: `"timeout"`, `"connection_refused"`, `"connection_reset"`, `"schema_invalid"`, `"upstream_5xx"`, or `null`; NEVER raw bodies/headers).

    Stop when `time.monotonic() - t_start >= duration`. Build summary with per-route p50/p95/p99 (sort + integer-index — no numpy dep).

    **Compute gates per D-04 (closes reviews LOW #4):**
    - `p95_chat_ms_le_5000`: bool — route `/v1/chat/completions` p95 ≤ 5000.
    - `p95_embed_ms_le_1000`: bool — route `/v1/embeddings` p95 ≤ 1000.
    - `p95_stt_ms_le_10000`: bool — route `/v1/audio/transcriptions` p95 ≤ 10000.
    - `error_rate_lt_1pct`: bool — `count(status_code >= 500 OR status_code < 0) / total_requests < 0.01`. Explicitly EXCLUDES 503 responses where `error_class == "upstream_5xx"` AND `upstream` indicates a sensitive-tenant block (per D-04 "<1% non-503" wording — sensitive 503 is expected per RES-08).
    - `zero_5xx_panic`: **MUST be set to Python `None` (JSON `null`) at write time** — the replay engine has no authority to observe panics from response bodies because recoverers should never expose panic strings (closes reviews LOW #4). The operator MUST flip this to `true`/`false` post-run based on the dual external observation described in `gates_external_inputs` (see schema below).
    - `all_passed`: AND of the 5 booleans. **MUST be `None` (JSON `null`) whenever any of the 5 gates is still `None`.** Never compute `all_passed=true` when `zero_5xx_panic` is null.

    Also populate top-level `gates_external_inputs` block: `{sentry_query_url: <Sentry URL with run-window-scoped filter>, log_grep_command: <docker logs grep example>, verified_at: null}`. Operator sets `verified_at` ISO-8601 when flipping `zero_5xx_panic`.

    Schema-validate tail per Pattern C — load schema sibling file, write report always (even on validation failure for forensics), exit 1 if schema invalid. Apply Pattern A secret-once (no committed API key default — keys come ONLY from env vars at startup).

    **(b) `scripts/integration-smoke/load-replay-report-schema.json`** per 11-PATTERNS.md §load-replay-report-schema.json target shape, extended with reviews findings:

    Top-of-file: `$schema: https://json-schema.org/draft/2020-12/schema`, `$id: https://ifixtelecom.com.br/schemas/integration-smoke/load-replay-report/1.0.0`, title "Load Replay Smoke Report", version "1.0.0".

    Top-level `description` includes the cross-cutting hard rule verbatim: "Reports MUST NOT contain raw API keys, Authorization/Cookie headers, request or response bodies, DSN strings, full upstream URLs with embedded credentials, or PII. Forbidden property names anywhere in the document: api_key, authorization, cookie, bearer, prompt_text, response_text, headers, body, dsn, password, secret. Validators MAY enforce this via additional pattern checks."

    `additionalProperties: false` at the top level AND every nested object.

    Required top-level fields:
    - `schema_version` (const `"1.0.0"`)
    - `started_at` (date-time)
    - `finished_at` (date-time)
    - `target` (object: `gateway_url` uri + `tenant` string + `additionalProperties: false`)
    - `git_sha` (pattern `^[0-9a-f]{7,40}$`)
    - `summary` (object: `routes` map → record with `n`/`p50_ms`/`p95_ms`/`p99_ms` only (additionalProperties:false), `total_requests`, `error_count`)
    - `errors` (array of objects with `error_class` string + `count` int only — NO raw bodies; additionalProperties:false)
    - `gates` (object with EXACTLY 6 properties — `additionalProperties: false`):
      - `p95_chat_ms_le_5000`: `{ "type": "boolean" }`
      - `p95_embed_ms_le_1000`: `{ "type": "boolean" }`
      - `p95_stt_ms_le_10000`: `{ "type": "boolean" }`
      - `error_rate_lt_1pct`: `{ "type": "boolean" }`
      - `zero_5xx_panic`: `{ "type": ["boolean", "null"], "description": "EXTERNAL OBSERVATION ONLY. Set by operator post-run based on (1) Sentry query: level=fatal AND service=ifix-ai-gateway over [started_at, finished_at]; AND (2) gateway log grep: 'docker logs ifix-ai-gateway --since <started_at> --until <finished_at> | grep -c \"^panic:\"'. Reports written by load-replay.py MUST set this to null; recoverers do NOT leak panic strings so response-body inference is forbidden. closes reviews LOW #4." }`
      - `all_passed`: `{ "type": ["boolean", "null"], "description": "AND of the 5 gates above. MUST be null when any gate is null — never compute true with an unverified zero_5xx_panic." }`
    - `gates_external_inputs` (object — `additionalProperties: false`, REQUIRED):
      - `sentry_query_url`: `{ "type": "string", "format": "uri" }` — Sentry URL pre-filled with run-window scope
      - `log_grep_command`: `{ "type": "string" }` — exact docker-logs grep command for `panic:` count
      - `verified_at`: `{ "type": ["string", "null"], "format": "date-time" }` — ISO-8601 set when operator flips `zero_5xx_panic`; null until then

    **(c) `scripts/integration-smoke/fixtures/whatsapp-sample.ogg`** — small deterministic OGG/Opus audio sample, ≤16 KB. Executor obtains a CC0/public-domain or self-generated sample (e.g., `ffmpeg -f lavfi -i "sine=frequency=440:duration=1" -c:a libopus -b:a 16k whatsapp-sample.ogg`). The file is committed to the repo because it is non-secret and deterministic — it IS the canonical STT replay payload. Document provenance in a sibling `scripts/integration-smoke/fixtures/README.md` (small note: source + license + why it exists).
  </action>
  <acceptance_criteria>
    - File `scripts/integration-smoke/load-replay.py` exists and is executable.
    - File `scripts/integration-smoke/load-replay-report-schema.json` exists and is valid JSON Schema Draft 2020-12.
    - File `scripts/integration-smoke/fixtures/whatsapp-sample.ogg` exists, is binary, and `file scripts/integration-smoke/fixtures/whatsapp-sample.ogg` reports `Ogg data` (or similar OGG signature).
    - `python scripts/integration-smoke/load-replay.py --help` exits 0 and prints all 7 argument names: `gateway-url`, `fixture`, `duration`, `max-concurrency`, `out`, `speedup`, `audio-stub`.
    - Schema validates with jsonschema: `python -c "import json, jsonschema; s=json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); jsonschema.Draft202012Validator.check_schema(s)"` exits 0.
    - Schema assertion: `python -c "import json; s=json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); assert s['additionalProperties']==False; assert set(s['properties']['gates']['properties'].keys()) == {'p95_chat_ms_le_5000','p95_embed_ms_le_1000','p95_stt_ms_le_10000','error_rate_lt_1pct','zero_5xx_panic','all_passed'}; assert s['properties']['gates']['properties']['zero_5xx_panic']['type']==['boolean','null']; assert s['properties']['gates']['properties']['all_passed']['type']==['boolean','null']; assert 'gates_external_inputs' in s['properties']; print('OK')"` exits 0.
    - Source assertion (closes reviews MEDIUM #2): `grep -E "IFIX_KEY_|os\\.environ\\[" scripts/integration-smoke/load-replay.py` returns ≥1 match (env-var key resolution).
    - Source assertion (closes reviews MEDIUM #3): `grep -E "audio/transcriptions" scripts/integration-smoke/load-replay.py | grep -v '^#'` returns ≥1 match AND `grep -E "files\s*=|multipart" scripts/integration-smoke/load-replay.py` returns ≥1 match (multipart STT handling).
    - Source assertion (closes reviews LOW #5): `grep -E "--speedup|args\.speedup" scripts/integration-smoke/load-replay.py` returns ≥2 matches (argparse + usage in delay computation).
    - Source assertion (closes reviews LOW #4): `grep -E "zero_5xx_panic.*=.*None|zero_5xx_panic.*null" scripts/integration-smoke/load-replay.py` returns ≥1 match (engine writes null, not bool).
    - Schema description assertion: `grep -E "external observation|EXTERNAL OBSERVATION" scripts/integration-smoke/load-replay-report-schema.json` returns ≥1 match.
    - Negative grep: `grep -v '^#' scripts/integration-smoke/load-replay.py | grep -cE '\bapi_key\s*=\s*"[a-zA-Z0-9_]{8,}"'` returns 0 (no inline key default).
  </acceptance_criteria>
  <verify>
    <automated>chmod +x scripts/integration-smoke/load-replay.py && python scripts/integration-smoke/load-replay.py --help 2>&1 | grep -E "gateway-url|fixture|duration|max-concurrency|out|speedup|audio-stub" | wc -l | grep -E "^\s*[7-9]" && python -c "import json, jsonschema; s=json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); jsonschema.Draft202012Validator.check_schema(s); assert s['properties']['gates']['properties']['zero_5xx_panic']['type']==['boolean','null']; assert 'gates_external_inputs' in s['properties']; print('OK')" && grep -E "IFIX_KEY_" scripts/integration-smoke/load-replay.py && grep -E "files\s*=" scripts/integration-smoke/load-replay.py && grep -E "--speedup" scripts/integration-smoke/load-replay.py && file scripts/integration-smoke/fixtures/whatsapp-sample.ogg | grep -iE "ogg|opus"</automated>
  </verify>
  <done>All 11 acceptance criteria pass. Closes reviews MEDIUM #2 (env-var keys, no inline default), MEDIUM #3 (multipart STT with stub file), LOW #4 (engine writes null for zero_5xx_panic; schema typed `[boolean,null]`; gates_external_inputs documents Sentry+log dual observation), LOW #5 (--speedup flag wired into delay computation).</done>
</task>

<task type="auto">
  <id>11-01-03</id>
  <title>Task 3: gitignore fixtures + legal directories</title>
  <read_first>
    - .gitignore (current ~50 lines — read allow-rule pattern at lines 24-29 + .claude/ block at end)
    - .planning/phases/11-prod-hardening/11-PATTERNS.md §.gitignore (insertion convention with explanatory comments above each new pattern)
    - .planning/phases/11-prod-hardening/11-CONTEXT.md D-16 (LGPD evidence file convention path `.planning/legal/lgpd-signoff-{YYYY-MM-DD}-{tenant}.pdf` gitignored)
  </read_first>
  <action>
    Create `.planning/load-test-fixtures/.gitignore` with a single line `*.jsonl` so the directory tracks but JSONL fixtures stay local. Append to root `.gitignore` (preserving existing `.claude/` block as the final block — insert ABOVE it) two blocks:

    Block 1 (load-test fixtures) — comment line `# Phase 11: load-test fixtures (sanitized audit_log JSONL — local-only, gitignored)` followed by pattern `.planning/load-test-fixtures/*.jsonl`.

    Block 2 (LGPD signed letters) — comment line `# Phase 11: LGPD signed letters (binary PDFs — gitignored; templates in same dir ARE committed)` followed by pattern `.planning/legal/*.pdf`.

    The directory-level `.gitignore` in `.planning/load-test-fixtures/` is the canonical track-this-empty-directory marker (project convention). The root `.gitignore` patterns are the actual exclude rules.

    Note: `scripts/integration-smoke/fixtures/whatsapp-sample.ogg` from task 11-01-02 is intentionally NOT gitignored — it is a non-secret deterministic fixture and is part of the contract surface for STT replay.
  </action>
  <acceptance_criteria>
    - `.planning/load-test-fixtures/.gitignore` exists with content `*.jsonl` (single line).
    - Root `.gitignore` contains both Phase 11 comment lines AND both pattern lines.
    - `.claude/` block remains the final block in root `.gitignore` (executor verifies via `tail -5 .gitignore` shows `.claude/`).
    - `git check-ignore .planning/load-test-fixtures/audit-export-2026-05-27T14-00-00Z.jsonl` returns the path (proves the rule matches).
    - `git check-ignore .planning/legal/lgpd-signoff-2026-06-01-chat-ifix.pdf` returns the path.
    - `git check-ignore scripts/integration-smoke/fixtures/whatsapp-sample.ogg` exits non-zero (fixture file is NOT gitignored).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/load-test-fixtures/.gitignore && grep -E "load-test-fixtures/\*\.jsonl" .gitignore && grep -E "legal/\*\.pdf" .gitignore && grep -E "Phase 11: load-test" .gitignore && grep -E "Phase 11: LGPD" .gitignore && git check-ignore .planning/load-test-fixtures/test.jsonl && git check-ignore .planning/legal/test.pdf && ! git check-ignore scripts/integration-smoke/fixtures/whatsapp-sample.ogg</automated>
  </verify>
  <done>All 6 acceptance criteria pass. Directory marker + root patterns + fixture audio file remains tracked.</done>
</task>

</tasks>

<verification>
- `python scripts/integration-smoke/audit-log-export.py --help` exits 0 and shows all 5 args.
- `python scripts/integration-smoke/load-replay.py --help` exits 0 and shows all 7 args (including --speedup and --audio-stub).
- jsonschema validation: `python -c "import jsonschema, json; jsonschema.Draft202012Validator.check_schema(json.load(open('scripts/integration-smoke/load-replay-report-schema.json')))"` exits 0.
- Schema gate types: `zero_5xx_panic` and `all_passed` are both `[boolean, null]` (closes reviews LOW #4).
- `gates_external_inputs` block present with `sentry_query_url`, `log_grep_command`, `verified_at` keys (closes reviews LOW #4).
- `git check-ignore .planning/load-test-fixtures/test.jsonl` returns the path.
- `git check-ignore scripts/integration-smoke/fixtures/whatsapp-sample.ogg` exits non-zero (fixture is tracked).
- Negative grep: `! grep -E '_replay_api_key' scripts/integration-smoke/audit-log-export.py scripts/integration-smoke/load-replay.py` (closes reviews MEDIUM #2).
- Positive grep: `grep -E "audit_log_content" scripts/integration-smoke/audit-log-export.py` (closes reviews HIGH #1).
- Positive grep: `grep -E "files\s*=" scripts/integration-smoke/load-replay.py` (closes reviews MEDIUM #3).
- Positive grep: `grep -E -- "--speedup" scripts/integration-smoke/load-replay.py` (closes reviews LOW #5).
</verification>

<success_criteria>
- audit-log-export.py JOINs `ai_gateway.audit_log_content` and pulls `c.prompt AS body` (reviews HIGH #1).
- load-replay.py resolves tenant API keys at runtime from env `IFIX_KEY_${TENANT_SLUG^^}`; no `_replay_api_key` anywhere (reviews MEDIUM #2).
- load-replay.py handles `/v1/audio/transcriptions` as multipart with `scripts/integration-smoke/fixtures/whatsapp-sample.ogg`; STT audio bytes are NEVER reconstructed from DB (reviews MEDIUM #3, D-B6 invariant).
- `zero_5xx_panic` schema type is `[boolean, null]`; engine writes null; `gates_external_inputs` documents Sentry + log grep dual observation; `all_passed` is null until operator verifies (reviews LOW #4).
- `--speedup` flag wired into delay computation (reviews LOW #5).
- gitignore rules exclude fixtures + signed PDFs but keep the OGG sample tracked.
- D-04 SLO gate names locked verbatim in the schema (acts as load-test contract for 11-06).
- Zero live spend incurred.
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies. Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only, gated by inventory + slopcheck.
- Truth 2: No speculative language in script docstrings.
- Truth 3: Live UAT execution deferred to 11-06.
- Truth 4: PII sanitization is a hard invariant — sensitive-class tenants skipped in baseline.
- Truth 8 (cross-cutting): NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in committed files. Tenant API keys resolve at replay time from env vars. Schema enforces forbidden-key rule for downstream report writers.
- Truth 9 (D-B6 invariant): audio bytes are NEVER stored in audit_log_content; STT replay uses the committed fixture OGG file as the canonical and only legitimate source. Replay measures pipeline latency on a representative file, not on the literal original utterance.
</cross_cutting_truths>

<output>
Create `.planning/phases/11-prod-hardening/11-01-SUMMARY.md` when done with files modified, byte-counts, a one-line snippet from `load-replay.py --help` output, and explicit `closes reviews` tags (HIGH #1, MEDIUM #2, MEDIUM #3, LOW #4, LOW #5) for traceability into the next review cycle.
</output>
</content>
</invoke>