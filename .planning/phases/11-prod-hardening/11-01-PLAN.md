---
phase: 11
plan: 11-01
slug: load-test-scaffolding
wave: 1
depends_on: []
requirements: [PRD-01]
files_modified:
  - path: scripts/integration-smoke/audit-log-export.py
    kind: NEW
  - path: scripts/integration-smoke/load-replay.py
    kind: NEW
  - path: scripts/integration-smoke/load-replay-report-schema.json
    kind: NEW
  - path: .planning/load-test-fixtures/.gitignore
    kind: NEW
  - path: .gitignore
    kind: MOD
autonomous: true
estimated_duration: 90
---

<objective>
Scaffold the PRD-01 load test infrastructure: a psycopg-based audit-log exporter that produces a sanitized JSONL fixture, an asyncio+httpx replay engine that drives the fixture against the prod gateway URL, a JSON Schema 2020-12 report contract, and gitignore entries that keep both the exported fixture and any future signed legal PDFs out of git. Zero live Vast spend in this plan — UAT execution lives in 11-06.
</objective>

<must_haves>
  truths:
    - "Phase 11 introduces ZERO new server-side runtime dependencies — psycopg, httpx, structlog, jsonschema are already in scripts/integration-smoke/requirements.txt. (Dashboard 2FA UI adds 4 packages scoped to plan 11-02 only.)"
    - "Sanitized audit_log JSONL excludes sensitive-class tenants from baseline replay (PRD-01 + Pitfall 1)"
    - "load-replay.py output validates against load-replay-report-schema.json (WR-05 hard-fail)"
    - "All gate names map verbatim to D-04 SLO v1.0: P95 chat ≤5s + embed ≤1s + STT ≤10s + error <1% non-503 + zero 5xx panic"
    - "Secret-once CLI discipline applies (Pattern A) — no committed defaults for tokens"
  must_haves:
    - scripts/integration-smoke/audit-log-export.py exists and is executable
    - scripts/integration-smoke/load-replay.py exists and is executable
    - scripts/integration-smoke/load-replay-report-schema.json exists and is valid JSON Schema Draft 2020-12
    - .planning/load-test-fixtures/.gitignore commits an empty allow-rule directory
    - Root .gitignore has explanatory comment + `.planning/load-test-fixtures/` and `.planning/legal/*.pdf` patterns appended
  key_links:
    - from: scripts/integration-smoke/load-replay.py
      to: scripts/integration-smoke/load-replay-report-schema.json
      via: Path(__file__).parent + jsonschema.Draft202012Validator.validate
      pattern: "load-replay-report-schema.json"
    - from: scripts/integration-smoke/audit-log-export.py
      to: ai_gateway.audit_log
      via: psycopg.connect(pg_dsn) + SELECT
      pattern: "FROM ai_gateway.audit_log"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude → DO Postgres | psycopg query against `ai_gateway.audit_log`; DSN is secret |
| ops-claude → ai-gateway prod HTTPS | replay request bodies cross this boundary |
| audit_log row → JSONL fixture | sanitization step strips PII before write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-LOAD-01 | Information Disclosure | audit-log-export.py | mitigate | Per Pitfall 1: replace `tool_calls[].function.arguments` with `{"_replay_placeholder": true}`; skip all `data_class=sensitive` rows in baseline (PRD-01); Whisper file part replaced with `scripts/integration-smoke/fixtures/whatsapp-sample.ogg` stub |
| T-11-LOAD-02 | Information Disclosure | psycopg error logging | mitigate | Never include DSN in `log.error(...)` payload; pattern from smoke-sensitive-failover.py:472-498 strips str(e) to first 300 chars |
| T-11-LOAD-03 | Tampering | load-replay-report-schema.json | mitigate | `additionalProperties: false` + `schema_version` const `"1.0.0"` + WR-05 hard-fail on validation error |
| T-11-LOAD-04 | Information Disclosure | .planning/load-test-fixtures/*.jsonl on disk | mitigate | Gitignored at directory level; fixture is local-only |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Phase 11 introduces zero new dependencies; slopcheck verified 2026-05-27 in 11-RESEARCH.md §Package Legitimacy Audit |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@scripts/integration-smoke/smoke-sensitive-failover.py
@scripts/integration-smoke/sensitive-failover-report-schema.json
@scripts/integration-smoke/requirements.txt
@.gitignore
</context>

<tasks>

<task type="auto">
  <id>11-01-01</id>
  <title>Task 1: Author audit-log-export.py sanitizing exporter</title>
  <read_first>
    - scripts/integration-smoke/smoke-sensitive-failover.py (psycopg shape lines 442-505 — query_audit; structlog bind module label; secret-once discipline at 151-228)
    - 11-PATTERNS.md §audit-log-export.py (Pattern B 8-section spine + Pattern A secret-once)
    - 11-RESEARCH.md §Pitfall 1 (sanitization invariant — tool_calls placeholder + Whisper file stub + sensitive tenant skip)
    - gateway/db/migrations (audit_log schema — columns: request_id, ts, tenant_id, route, upstream, status_code, data_class enum)
  </read_first>
  <action>
    Create scripts/integration-smoke/audit-log-export.py per D-01 (PRD-01 fixture generation). Module docstring states: read sanitized audit_log dev rows into a JSONL fixture for load-replay.py consumption. argparse args: `--dsn` (env DASHBOARD_DATABASE_URL or AI_GATEWAY_DB_URL fallback), `--window-start` ISO-8601, `--window-end` ISO-8601, `--out` JSONL output path, `--include-sensitive` flag (default False — Pitfall 1 + Open Question #1 baseline excludes sensitive tenants). Use psycopg double-`with` context-manager pattern with `connect_timeout=10`. SELECT columns request_id, ts, tenant_id, route, upstream, status_code, data_class FROM ai_gateway.audit_log WHERE ts >= window_start AND ts < window_end ORDER BY ts ASC. For each row: compute `_replay_delay_s` = ts - prev_ts (seconds), call `sanitize_body(body, data_class)` which returns None when data_class=sensitive (skip), else replaces `tool_calls[].function.arguments` with `{"_replay_placeholder": true}` and drops `audio.filename`. Resolve `_replay_api_key` per tenant_id via env mapping `SMOKE_TENANT_KEYS_JSON` (per-tenant dict from operator). Emit JSONL line per non-skipped row with fields: request_id, ts (ISO-8601), tenant_id, tenant_slug, route, upstream, status_code, data_class, _replay_delay_s, _replay_api_key, _sanitized_body. Apply Pattern A secret-once: NEVER log DSN; NEVER log `_replay_api_key`. Apply 8-section spine per Pattern B (Module docstring → Constants → Config+CLI → Helpers → Audit-DB → Gates+exit → Orchestration → main). Exit codes 0/1/2 (0=ok, 1=runtime, 2=usage). Per D-01 + Pitfall 1: write to `.planning/load-test-fixtures/audit-export-<window_start_iso>.jsonl` by default.
  </action>
  <verify>
    <automated>python scripts/integration-smoke/audit-log-export.py --help 2>&1 | grep -E "dsn|window-start|window-end|out|include-sensitive"</automated>
  </verify>
  <done>File exists, executable, --help shows all 5 args, module has the 8 section markers in Pattern B order, grep "data_class" returns at least one occurrence, grep "_replay_placeholder" returns at least one occurrence, NO occurrence of `print(api_key)` or `log.*api_key.*"` raw key emission.</done>
</task>

<task type="auto">
  <id>11-01-02</id>
  <title>Task 2: Author load-replay.py + load-replay-report-schema.json</title>
  <read_first>
    - scripts/integration-smoke/smoke-sensitive-failover.py (full 8-section spine, schema-validate tail at 756-770)
    - scripts/integration-smoke/sensitive-failover-report-schema.json (top-of-file shape — $schema, $id, additionalProperties:false, schema_version const)
    - 11-PATTERNS.md §load-replay.py + §load-replay-report-schema.json (target shape with gates names locked to D-04)
    - 11-RESEARCH.md §Pattern 3 (httpx skeleton lines 326-394) + §Architecture Patterns System Architecture Diagram (request path through edge Traefik to n8n-ia-vm:8080)
    - 11-CONTEXT.md D-04 (P95 chat ≤5s + embed ≤1s + STT ≤10s + error <1% non-503 + zero 5xx panic)
  </read_first>
  <action>
    Create scripts/integration-smoke/load-replay.py per D-05/D-06. argparse args: `--gateway-url` (env SMOKE_GATEWAY_URL or default `https://ai-gateway.converse-ai.app`), `--fixture` JSONL path required, `--duration` seconds (D-03 default 1800), `--max-concurrency` int default 50, `--out` report.json output path required. Use `httpx.AsyncClient(http2=True)` + `asyncio.Semaphore(max_concurrency)`. Per Open Question #2 recommendation: preserve original timing — between successive records do `await asyncio.sleep(rec.get("_replay_delay_s", 0))` (NOT constant RPS). For each JSONL record build POST to `{gateway_url}{rec.route}` with headers `Authorization: Bearer {_replay_api_key}`, `Content-Type: application/json`, `X-Idempotency-Key: {request_id}`. Capture per-request metric record: request_id, tenant_slug, route, status_code, elapsed_ms (time.monotonic delta in ms), upstream (X-Upstream response header). On exception: status_code=-1, error first 300 chars. Stop when `time.monotonic() - t_start >= duration`. Build summary with per-route p50/p95/p99 (sort + index). Compute gates per D-04: `p95_chat_ms_le_5000` (route `/v1/chat/completions` p95 ≤ 5000), `p95_embed_ms_le_1000` (route `/v1/embeddings` p95 ≤ 1000), `p95_stt_ms_le_10000` (route `/v1/audio/transcriptions` p95 ≤ 10000), `error_rate_lt_1pct` (count(status_code>=500 OR status_code<0)/total < 0.01), `zero_5xx_panic` (count(status_code=500 with body containing "panic")==0; if response body capture not available, evaluate via summary placeholder + cross-reference with Sentry post-run in HUMAN-UAT), `all_passed` = AND of 5 gates. Schema-validate tail per Pattern C — load schema sibling file, write report always, exit 1 if schema invalid. Apply Pattern A secret-once (no committed API key default).

    Then create scripts/integration-smoke/load-replay-report-schema.json per 11-PATTERNS.md §load-replay-report-schema.json target shape. Top-of-file: `$schema: https://json-schema.org/draft/2020-12/schema`, `$id: https://ifixtelecom.com.br/schemas/integration-smoke/load-replay-report/1.0.0`, title "Load Replay Smoke Report", version "1.0.0", `additionalProperties: false`. Required top-level fields: schema_version (const "1.0.0"), started_at (date-time), finished_at (date-time), target (object: gateway_url uri + tenant), git_sha (pattern ^[0-9a-f]{7,40}$), summary (object: routes additionalProperties=record with n/p50_ms/p95_ms/p99_ms, total_requests, error_count), errors (array), gates (object with 6 booleans matching D-04 names — p95_chat_ms_le_5000, p95_embed_ms_le_1000, p95_stt_ms_le_10000, error_rate_lt_1pct, zero_5xx_panic, all_passed). All sub-objects MUST have `additionalProperties: false`.
  </action>
  <verify>
    <automated>python scripts/integration-smoke/load-replay.py --help 2>&1 | grep -E "gateway-url|fixture|duration|max-concurrency|out" && python -c "import json; s=json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); assert s['\$schema']=='https://json-schema.org/draft/2020-12/schema'; assert s['additionalProperties']==False; assert 'p95_chat_ms_le_5000' in s['properties']['gates']['properties']; print('schema OK')"</automated>
  </verify>
  <done>load-replay.py --help lists all 5 args; load-replay-report-schema.json validates (jsonschema.Draft202012Validator can be instantiated with it); gates section contains exactly 6 boolean property names matching D-04 SLO; grep "p95_chat_ms_le_5000\|p95_embed_ms_le_1000\|p95_stt_ms_le_10000\|error_rate_lt_1pct\|zero_5xx_panic\|all_passed" returns 6 lines in load-replay-report-schema.json.</done>
</task>

<task type="auto">
  <id>11-01-03</id>
  <title>Task 3: gitignore fixtures + legal directories</title>
  <read_first>
    - .gitignore (current ~50 lines — read allow-rule pattern at lines 24-29 + .claude/ block at end)
    - 11-PATTERNS.md §.gitignore (insertion convention with explanatory comments above each new pattern)
    - 11-CONTEXT.md D-16 (LGPD evidence file convention path `.planning/legal/lgpd-signoff-{YYYY-MM-DD}-{tenant}.pdf` gitignored)
  </read_first>
  <action>
    Create .planning/load-test-fixtures/.gitignore with a single line `*.jsonl` so the directory tracks but JSONL fixtures stay local. Append to root .gitignore (preserving existing `.claude/` block as the final block — insert ABOVE it):

    ```
    # Phase 11: load-test fixtures (sanitized audit_log JSONL — local-only, gitignored)
    .planning/load-test-fixtures/*.jsonl

    # Phase 11: LGPD signed letters (binary PDFs — gitignored; templates in same dir ARE committed)
    .planning/legal/*.pdf
    ```

    The directory-level `.gitignore` in `.planning/load-test-fixtures/` is the canonical track-this-empty-directory marker (project convention). The root `.gitignore` patterns are the actual exclude rules.
  </action>
  <verify>
    <automated>grep -c "phase11-load-test-fixtures\|load-test-fixtures\|legal/\*.pdf\|Phase 11: load-test\|Phase 11: LGPD" .gitignore && test -f .planning/load-test-fixtures/.gitignore</automated>
  </verify>
  <done>.planning/load-test-fixtures/.gitignore exists with `*.jsonl` content; root .gitignore contains both phase11 explanatory comments AND patterns; `.claude/` block remains the final block; `git check-ignore .planning/load-test-fixtures/audit-export-2026-05-27.jsonl` returns the path (proving the rule matches).</done>
</task>

</tasks>

<verification>
- `python scripts/integration-smoke/audit-log-export.py --help` exits 0 and shows args.
- `python scripts/integration-smoke/load-replay.py --help` exits 0 and shows args.
- `jsonschema -i /dev/null scripts/integration-smoke/load-replay-report-schema.json` style validation: `python -c "import jsonschema, json; jsonschema.Draft202012Validator(json.load(open('scripts/integration-smoke/load-replay-report-schema.json'))).check_schema(json.load(open('scripts/integration-smoke/load-replay-report-schema.json')))"` exits 0.
- `git check-ignore .planning/load-test-fixtures/test.jsonl` returns the path.
- No raw API key emission grep: `! grep -E 'log\.(info|warn|error).*api_key.*=.*[a-z]{8}' scripts/integration-smoke/audit-log-export.py scripts/integration-smoke/load-replay.py`.
</verification>

<success_criteria>
- audit-log-export.py + load-replay.py + load-replay-report-schema.json committed.
- gitignore rules exclude fixtures + signed PDFs.
- Zero live spend incurred.
- D-04 SLO gate names locked verbatim in the schema (acts as load-test contract for 11-06).
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies (gateway Go binary, scripts Python smokes). Dashboard 2FA UI adds 4 packages scoped to 11-02 only (input-otp + @radix-ui/react-dialog + qrcode + @types/qrcode), all slopcheck-audited.
- Truth 2: No speculative language in script docstrings.
- Truth 3: Live UAT execution deferred to 11-06.
- Truth 4: PII sanitization is a hard invariant — sensitive-class tenants skipped in baseline.
</cross_cutting_truths>

<output>
Create `.planning/phases/11-prod-hardening/11-01-SUMMARY.md` when done with files modified, byte-counts, and a one-line snippet from `load-replay.py --help` output.
</output>
