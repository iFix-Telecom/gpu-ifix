---
phase: 11
plan: 11-05
slug: per-env-keys-and-deploy-runbook
wave: 1
depends_on: []
requirements: [PRD-04]
secondary_decisions: [D-18.4, D-19]
files_modified:
  - path: gateway/docs/RUNBOOK-DEPLOY.md
    kind: MOD
  - path: scripts/dashboard/seed-admins.sh
    kind: NEW
  - path: scripts/deploy/preflight.sh
    kind: MOD
autonomous: true
estimated_duration: 90
---

<objective>
Document the D-19 per-env upstream key separation procedure (operator-managed key rotation for OpenRouter + OpenAI + Vast on n8n-ia-vm /opt/ai-gateway-prod/.env) and the D-18.4 GHA workflow_dispatch retrigger procedure (recovery for the Phase 10 tag-SHA dedup that left :v1.0.0 image unbuilt). Ship the dashboard admin provisioning helper (scripts/dashboard/seed-admins.sh) so D-13 manual provisioning of the 4 Ifix admins is repeatable WITHOUT printing generated passwords to stdout (reviews MEDIUM). Extend preflight.sh with a dashboard /api/auth/health probe so SSO hardening cannot silently break operator-facing surfaces (reviews suggestion). All deliverables target RUNBOOK-DEPLOY.md (cross-cutting operator surface); execution itself is operator work tracked in 11-10 HUMAN-UAT.
</objective>

<must_haves>
  truths:
    - "D-19: prod OR + OpenAI keys distinct from dev keys; .env file /opt/ai-gateway-prod/.env mode 600; Vast.ai fallback to shared key if per-key labels unsupported (Open Question #4)"
    - "D-18.4: GHA workflow_dispatch with inputs.tag wired in .github/workflows/build-gateway.yml lines 18-23 (verified RESEARCH Sources)"
    - "D-13: 4 admins manual provisioning via Better Auth admin/invite API OR password-reset flow; idempotent SELECT-then-INSERT pattern but NEVER prints generated passwords to stdout — writes to a `0600` credentials file under /tmp with explicit deletion instructions; race-free via probe-then-create idempotency check (reviews MEDIUM)"
    - "RUNBOOK-DEPLOY.md voice preserved (operational, step-numbered, Pitfall N callouts when relevant)"
    - "Cross-cutting hard rule: NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in committed plans, scripts, or runbook examples. Per-env diff outputs sanitized to first-4-char prefixes only (reviews LOW)."
    - "2FA recovery / device-loss path (reviews/Gemini suggestion #6): seed-admins.sh OR RUNBOOK-2FA-RECOVERY.md (delivered in 11-09) provides a documented audit-logged path to reset an admin's 2FA when device + backup codes are lost."
    - "preflight.sh adds dashboard health probe (reviews Gemini suggestion): curl -fs https://ai-dashboard.converse-ai.app/api/auth/health (or /health, whichever the Better Auth instance exposes) — non-200 fails the preflight."
  must_haves:
    - gateway/docs/RUNBOOK-DEPLOY.md has new section "GHA retrigger procedure (D-18.4)" with gh workflow run example
    - gateway/docs/RUNBOOK-DEPLOY.md has new section "Per-env key rotation (D-19)" with 3-step operator procedure AND sanitized diff verification recipe (only var names + first-4 prefixes, no raw values)
    - scripts/dashboard/seed-admins.sh exists, executable, idempotent SELECT-then-INSERT pattern
    - seed-admins.sh validates DASHBOARD_DATABASE_URL env (fail-fast with specific pointer per Pattern E)
    - seed-admins.sh never echoes the DSN nor any raw password to stdout/stderr/log; writes generated passwords to `/tmp/admin-creds-$(date +%Y%m%d-%H%M%S).txt` mode 600 with explicit `chmod 600` and prints ONLY the file path to operator
    - seed-admins.sh prefers Better Auth admin invite/reset flow over sign-up-API + generated password where the installed version supports it; documents the choice
    - seed-admins.sh handles probe-then-create race: uses a single transaction OR uses Better Auth admin API exclusively (which is idempotent by email) — never both probing + sign-up API in the same run (reviews MEDIUM)
    - scripts/deploy/preflight.sh extended with `curl -fs $DASHBOARD_BASE/api/auth/health` check (Gemini suggestion); preflight fails fast if non-200
  key_links:
    - from: gateway/docs/RUNBOOK-DEPLOY.md GHA section
      to: .github/workflows/build-gateway.yml workflow_dispatch inputs.tag
      via: gh workflow run command
      pattern: "build-gateway.yml.*tag=v"
    - from: gateway/docs/RUNBOOK-DEPLOY.md per-env section
      to: /opt/ai-gateway-prod/.env on n8n-ia-vm
      via: ssh n8n-ia-vm sudoedit
      pattern: "/opt/ai-gateway-prod/.env"
    - from: scripts/dashboard/seed-admins.sh
      to: dashboard_auth.user table (DO Postgres bd_ai_dashboard_prod) OR Better Auth admin API endpoint
      via: psql query + Better Auth admin API call (preferred) OR direct INSERT with bcrypt hash (fallback)
      pattern: "dashboard_auth.user|/api/auth/admin"
    - from: scripts/deploy/preflight.sh
      to: https://ai-dashboard.converse-ai.app/api/auth/health
      via: curl -fs
      pattern: "ai-dashboard.*auth.*health"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> /opt/ai-gateway-prod/.env on n8n-ia-vm | secrets at rest; mode 600 |
| seed-admins.sh -> bd_ai_dashboard_prod | INSERTs admin rows; password generation must not leak to stdout/log |
| operator -> GitHub workflow_dispatch | requires repo write permission; PAT in ~/.git-credentials mode 600 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-OPS-02 | Information Disclosure | .env key leak via shell history | mitigate | sudoedit (no piping); never paste keys in CLI args; D-19 procedure uses dashboard-issued raw values via clipboard transfer; ~/.claude/CLAUDE.md tracks current values local-only |
| T-11-OPS-05 | Tampering | seed-admins.sh inserts wrong-domain admin | mitigate | Hard-coded allowlist check before INSERT: if email not @ifixtelecom.com.br exit 1; defense-in-depth with auth.ts databaseHooks (D-13) |
| T-11-OPS-06 | Repudiation | per-env key rotation lost | mitigate | RUNBOOK-DEPLOY.md mandates label "env=prod" on dashboard issuance + sanitized diff verification command (first-4 prefix only) |
| T-11-OPS-07 | Spoofing | GHA workflow_dispatch unauthorized retrigger | mitigate | GitHub already gates by repo write permission; documented in RUNBOOK-DEPLOY (no auth scope creep) |
| T-11-OPS-09 | Information Disclosure | seed-admins.sh leaks generated passwords to stdout/log | mitigate | Reviews MEDIUM — passwords written to `/tmp/admin-creds-{ts}.txt` mode 600; only file path is printed; explicit deletion instruction in runbook + script docstring |
| T-11-OPS-10 | Tampering | probe-then-create race between psql probe and Better Auth sign-up | mitigate | Reviews MEDIUM — pick ONE method: (a) Better Auth admin API exclusively (idempotent by email, atomic), (b) single SQL transaction with `INSERT … ON CONFLICT DO NOTHING`. Never both. |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@gateway/docs/RUNBOOK-DEPLOY.md
@scripts/deploy/bootstrap-postgres.sh
@scripts/deploy/migrate-dashboard.sh
@scripts/deploy/preflight.sh
@.github/workflows/build-gateway.yml
@.github/workflows/build-dashboard.yml
</context>

<tasks>

<task type="auto">
  <id>11-05-01</id>
  <title>Task 1: Extend RUNBOOK-DEPLOY.md with GHA retrigger and per-env key sections (sanitized diff)</title>
  <files>gateway/docs/RUNBOOK-DEPLOY.md</files>
  <read_first>
    - gateway/docs/RUNBOOK-DEPLOY.md (current state — preserve voice and existing step ordering; see end of "Cut-release procedure" for insertion point)
    - 11-PATTERNS.md RUNBOOK-DEPLOY.md MOD (2 insertion sites: GHA retrigger + per-env keys)
    - 11-RESEARCH.md Pitfall 6 (GHA workflow_dispatch with inputs.tag; .github/workflows/build-gateway.yml lines 18-23 verified)
    - 11-CONTEXT.md D-18.4 + D-19 + Phase 10 deferred line 70
    - 11-RESEARCH.md Open Question #4 (Vast.ai may not support per-key labels — fallback documented)
    - .github/workflows/build-gateway.yml lines 1-30 (verify workflow_dispatch.inputs.tag shape)
  </read_first>
  <action>
    Edit gateway/docs/RUNBOOK-DEPLOY.md adding two new top-level sections.

    Section A: insert after "Cut-release procedure" (locate end-of-section). H2 title "GHA retrigger procedure (D-18.4)".
    Content:
    - Symptom from Phase 10: tag push for v1.0.0 did not dispatch build-gateway.yml because GitHub deduped on same-SHA (CONTEXT.md D-18 line 70).
    - Fix command: gh workflow run build-gateway.yml --ref v1.0.0 -f tag=v1.0.0 (RESEARCH Pitfall 6; workflow_dispatch.inputs.tag already wired per .github/workflows/build-gateway.yml lines 18-23).
    - Same recipe for build-dashboard.yml: gh workflow run build-dashboard.yml --ref v1.0.0 -f tag=v1.0.0.
    - Verification: gh run list --limit 5 --workflow build-gateway.yml shows new run; docker pull ghcr.io/ifixtelecom/ifix-ai-gateway:v1.0.0 succeeds.
    - Fallback if dedup still suspected: git tag -d v1.0.0 then git push origin :refs/tags/v1.0.0 then git tag -a v1.0.0 SHA -m "..." then git push origin v1.0.0 — generates a NEW tag push event (Pitfall 6 last bullet).

    Section B: insert as new top-level section "Per-env key rotation (D-19)" after the GHA section. H2 title.
    Content:
    - Rationale: Phase 10 D-08 trade-off (shared dev keys); Phase 11 D-19 fixes by creating prod-labeled keys.
    - Procedure (operator-only, ~30 min):
      1. Create new OpenRouter API key in dashboard (https://openrouter.ai/keys) with label env=prod; copy raw value to clipboard (single transfer; never paste in CLI args).
      2. Create new OpenAI API key in https://platform.openai.com/api-keys with label env=prod; copy raw value.
      3. Create new Vast.ai key in https://cloud.vast.ai/account/ if Vast API supports labels; otherwise fall back to shared key per Open Question #4 and CONTEXT.md D-19 fallback clause (document "shared Vast key acceptable").
      4. ssh n8n-ia-vm; sudoedit /opt/ai-gateway-prod/.env (mode 600); swap UPSTREAM_LLM_OPENROUTER_AUTH_BEARER, UPSTREAM_STT_OPENAI_AUTH_BEARER, UPSTREAM_EMBED_OPENAI_AUTH_BEARER values with the new prod keys.
      5. docker compose -f /opt/ai-gateway-prod/docker-compose.yml restart ai-gateway. Wait 10s; verify health: curl https://ai-gateway.converse-ai.app/health returns 200. Also probe dashboard: curl -fs https://ai-dashboard.converse-ai.app/api/auth/health (SSO hardening sanity).
      6. SANITIZED diff verification (reviews LOW — never expose raw key values, only first-4-char prefixes):
         ```
         diff \
           <(ssh n8n-ia-vm 'sudo grep -E "^UPSTREAM_.*_AUTH_BEARER" /opt/ai-gateway-prod/.env' | awk -F= '{key=$1; val=$2; printf "%s=%s...\n", key, substr(val,1,4)}') \
           <(ssh vps-ifix-vm 'sudo grep -E "^UPSTREAM_.*_AUTH_BEARER" /opt/ai-gateway-dev/.env' | awk -F= '{key=$1; val=$2; printf "%s=%s...\n", key, substr(val,1,4)}')
         ```
         The awk projection truncates to first-4-char prefix on both sides — non-empty diff confirms separation without exposing raw values to terminal or shell history.
      7. Revoke dev-shared keys in OpenRouter + OpenAI dashboards (NOT before step 5 verifies new keys work).
    - Per-env key matrix (Markdown table):
      | Var | Dev value source | Prod value source | Rotation cadence |
      | UPSTREAM_LLM_OPENROUTER_AUTH_BEARER | shared in ~/.claude/CLAUDE.md | env=prod label in OpenRouter dashboard | annual or on-incident |
      | UPSTREAM_STT_OPENAI_AUTH_BEARER | shared (same) | env=prod label in OpenAI dashboard | annual |
      | UPSTREAM_EMBED_OPENAI_AUTH_BEARER | shared (same) | env=prod label in OpenAI dashboard | annual |
      | VAST_AI_API_KEY | ~/.claude/CLAUDE.md | per-env if supported, else shared | annual |
    - Final note: documents Pitfall 5 / D-15 session cleanup advisory (DELETE FROM dashboard_auth.session WHERE expires_at > NOW() + INTERVAL '30 minutes' after dashboard image roll — optional, coordinated via WhatsApp).
  </action>
  <verify>
    <automated>grep -c "GHA retrigger procedure\|Per-env key rotation\|workflow_dispatch\|UPSTREAM_LLM_OPENROUTER_AUTH_BEARER" gateway/docs/RUNBOOK-DEPLOY.md | awk '{if ($1 >= 4) exit 0; else exit 1}' && grep "gh workflow run build-gateway.yml" gateway/docs/RUNBOOK-DEPLOY.md && grep -E "substr\(val,1,4\)|first-4-char prefix" gateway/docs/RUNBOOK-DEPLOY.md</automated>
  </verify>
  <done>RUNBOOK-DEPLOY.md has both new sections; grep returns ≥4 lines for headings; gh workflow run command line for build-gateway.yml present; per-env matrix table with at least 4 rows; sanitized diff (first-4-char prefix awk recipe) explicit; existing sections (Cut-release procedure etc.) preserved verbatim.</done>
</task>

<task type="auto">
  <id>11-05-02</id>
  <title>Task 2: Dashboard admin seeding script (idempotent, no stdout password leak)</title>
  <files>scripts/dashboard/seed-admins.sh</files>
  <read_first>
    - scripts/deploy/bootstrap-postgres.sh (Pattern E + env validation lines 62-79 + idempotent SELECT-then-INSERT lines 127-163 + summary tail)
    - 11-PATTERNS.md seed-admins.sh (env-validation + idempotency + never-echo-DSN invariant + Pattern E bash skeleton)
    - 11-CONTEXT.md D-13 (manual provisioning of 4 admins via Better Auth admin API or DB INSERT)
    - 11-RESEARCH.md Pitfall 5 (session cleanup advisory; sessions don't auto-expire on policy change)
    - node_modules/better-auth/dist/* — confirm whether installed version exposes an admin invite/reset API endpoint
  </read_first>
  <action>
    Create scripts/dashboard/seed-admins.sh per Pattern E. Bash, set -euo pipefail, ISO-8601 log() to stderr.

    Required structure:
    1. Shebang + set -euo pipefail.
    2. log() function printing to stderr with ISO-8601 timestamp tagged [seed-admins].
    3. Env validation block: if DASHBOARD_DATABASE_URL unset, log FATAL with specific pointer ("must be the prod DSN pointing at bd_ai_dashboard_prod") exit 1. Same for DASHBOARD_BASE_URL.
    4. Admin roster: read from env DASHBOARD_ADMIN_EMAILS (comma-separated). Validate each email ends with @ifixtelecom.com.br (defense-in-depth with auth.ts hook); exit 1 if any fails.
    5. CREDS_FILE constant: `CREDS_FILE="/tmp/admin-creds-$(date +%Y%m%d-%H%M%S).txt"`. Immediately `touch "$CREDS_FILE" && chmod 600 "$CREDS_FILE"` before any password is generated. Log "credentials file: $CREDS_FILE (mode 600 — DELETE after transfer to password manager)".

    6. ensure_admin() function (RACE-FREE — pick ONE method per reviews MEDIUM):

       PREFERRED — Better Auth admin invite/reset API path (if installed version supports it):
       - POST to $DASHBOARD_BASE_URL/api/auth/admin/invite with body `{"email": "$EMAIL"}` (or equivalent — verify endpoint per Better Auth docs for the installed version). Better Auth's invite/reset flow is idempotent by email and does NOT require generating a server-side password.
       - The invited admin clicks the email link to set their own password. Script logs "invited $EMAIL — link sent" and writes the invite URL (NOT the password) to $CREDS_FILE.

       FALLBACK — sign-up API with random password (only if invite/reset endpoint absent):
       - Idempotency: a single SQL `INSERT INTO dashboard_auth.user (email, ...) VALUES (...) ON CONFLICT (email) DO NOTHING RETURNING id` (atomic; race-free). If RETURNING is empty, skip password generation (admin already exists).
       - If RETURNING returns a new row: generate random password (`openssl rand -base64 24`), call Better Auth /api/auth/sign-up/email to set the password, append `EMAIL=<value> PASSWORD=<value>` to $CREDS_FILE. Print ONLY the file path to operator stdout — NEVER print the password.
       - Document the fallback path is operationally fragile; recommend upgrading Better Auth to a version with invite/reset.

       Hard rule (reviews MEDIUM): the script MUST NOT log/echo any generated password to stdout, stderr, or structlog. The ONLY output channel for passwords is $CREDS_FILE (mode 600). Operator transfers to password manager and deletes the file.

    7. main() loop over DASHBOARD_ADMIN_EMAILS calling ensure_admin per email.

    8. Final summary block to stdout (per Pattern E — stdout=parseable):
       ```
       seeded_count: N
       skipped_count: M
       credentials_file: /tmp/admin-creds-{ts}.txt (mode 600 — DELETE after transfer)
       next_steps: operators must enroll TOTP via /2fa/enroll on first login (D-12); see RUNBOOK-2FA-RECOVERY.md for device-loss recovery
       ```

    Invariants:
    - NEVER echo DSN in log lines (verify with grep $DASHBOARD_DATABASE_URL).
    - NEVER echo password to stdout/stderr/log.
    - $CREDS_FILE is the ONLY password sink, mode 600, with explicit deletion instruction.
    - Idempotent re-run safe.
    - Single race-free method per run (NEVER probe + sign-up-API mix).

    Make executable: file ends with main "$@" call.
  </action>
  <verify>
    <automated>test -x scripts/dashboard/seed-admins.sh && bash -n scripts/dashboard/seed-admins.sh && DASHBOARD_DATABASE_URL="" bash scripts/dashboard/seed-admins.sh 2>&1 | grep -E "FATAL.*DASHBOARD_DATABASE_URL" && ! grep -E "echo .*PASSWORD=" scripts/dashboard/seed-admins.sh && grep -E "chmod 600.*CREDS_FILE|CREDS_FILE.*mode 600" scripts/dashboard/seed-admins.sh</automated>
  </verify>
  <done>seed-admins.sh executable; bash -n clean; FATAL on missing env; grep returns no `echo .*PASSWORD=` (no password leak to stdout); CREDS_FILE with chmod 600 invariant grep-able; grep "set -euo pipefail" returns 1; grep "ifixtelecom.com.br" returns at least 1; grep "ensure_admin\|seed-admins" returns at least 2; grep "DASHBOARD_DATABASE_URL" returns at least 2.</done>
</task>

<task type="auto">
  <id>11-05-03</id>
  <title>Task 3: Extend preflight.sh with dashboard /api/auth/health probe</title>
  <files>scripts/deploy/preflight.sh</files>
  <read_first>
    - scripts/deploy/preflight.sh (current shape — locate existing health-check block for the gateway)
    - 11-RESEARCH.md (Gemini suggestion section — dashboard /health critical post-PRD-06)
    - dashboard/src/lib/auth.ts (Better Auth instance — verify whether `/api/auth/health` is exposed; if not, document the actual endpoint Better Auth surfaces)
  </read_first>
  <action>
    Edit scripts/deploy/preflight.sh adding a dashboard health probe section (reviews Gemini suggestion). Insertion point: immediately after the existing gateway /health curl block.

    Add:
    ```
    # Phase 11 PRD-06: dashboard health probe — SSO hardening must not silently break operator-facing surface.
    DASHBOARD_HEALTH_URL="${DASHBOARD_HEALTH_URL:-https://ai-dashboard.converse-ai.app/api/auth/health}"
    log "probing dashboard health at $DASHBOARD_HEALTH_URL"
    if ! curl -fs -o /dev/null -w "%{http_code}" "$DASHBOARD_HEALTH_URL" > /tmp/dashboard-health-status; then
      log "FATAL: dashboard health probe failed at $DASHBOARD_HEALTH_URL"
      exit 1
    fi
    STATUS=$(cat /tmp/dashboard-health-status)
    if [ "$STATUS" != "200" ]; then
      log "FATAL: dashboard health returned HTTP $STATUS (expected 200)"
      exit 1
    fi
    log "dashboard health OK ($STATUS)"
    ```

    If Better Auth does not expose `/api/auth/health` on the installed version, executor checks the actual endpoint via `curl -sv https://ai-dashboard.converse-ai.app/api/auth/` and documents the chosen URL in a comment above the block. Candidates: `/api/auth/ok-routes`, `/health`, root `/` (last resort).

    Make sure preflight.sh stays POSIX-bash compatible; if not already POSIX, keep `#!/usr/bin/env bash` and `set -euo pipefail`.
  </action>
  <verify>
    <automated>test -x scripts/deploy/preflight.sh && bash -n scripts/deploy/preflight.sh && grep -E "DASHBOARD_HEALTH_URL|ai-dashboard.*auth.*health|dashboard health probe" scripts/deploy/preflight.sh | wc -l | awk '{if ($1 >= 2) exit 0; else exit 1}'</automated>
  </verify>
  <done>preflight.sh extended with dashboard probe; bash -n clean; fail-fast on non-200; URL env-overridable via DASHBOARD_HEALTH_URL.</done>
</task>

</tasks>

<verification>
- Markdown lint clean (no broken links in RUNBOOK-DEPLOY.md additions).
- bash -n scripts/dashboard/seed-admins.sh exits 0.
- bash -n scripts/deploy/preflight.sh exits 0.
- grep "GHA retrigger\|Per-env key rotation" gateway/docs/RUNBOOK-DEPLOY.md returns at least 2 lines.
- grep "substr(val,1,4)\|first-4-char prefix" gateway/docs/RUNBOOK-DEPLOY.md returns at least 1 (sanitized diff recipe).
- grep "DASHBOARD_HEALTH_URL" scripts/deploy/preflight.sh returns at least 1.
- ! grep "echo .*PASSWORD=" scripts/dashboard/seed-admins.sh (no password stdout leak).
- Manual diff verification (deferred to 11-10): sanitized first-4 diff shows non-empty between prod and dev.
</verification>

<success_criteria>
- RUNBOOK-DEPLOY.md extended with D-18.4 + D-19 sections + sanitized diff recipe.
- seed-admins.sh ships executable, idempotent, fail-fast on missing env, never echoes DSN/password to stdout/log; passwords sink to CREDS_FILE mode 600.
- preflight.sh adds dashboard health probe.
- D-13 manual provisioning ready for 11-10 operator execution.
- D-19 key rotation procedure step-numbered with sanitized verification.
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies — bash + psql + curl all already present. (Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.)
- Truth 2: No speculative language; pt-BR operational voice per RUNBOOK convention.
- Truth 4: Vast.ai key never literal in shell history (env var + sudoedit pattern).
- Truth 6: gateway/dashboard images stay at :latest-dev until D-18.4 retrigger produces real :v1.0.0.
- Truth 8 (cross-cutting): NO raw API keys, passwords, DSNs, or PII in committed scripts, runbook examples, or stdout/logs. Sanitized diff uses first-4-char prefix only. seed-admins.sh routes passwords to mode-600 file with deletion instructions.
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-05-SUMMARY.md when done with: diff stats for RUNBOOK-DEPLOY.md (lines added), seed-admins.sh byte count, bash -n result, preflight.sh diff stats, grep results for the 4 D-19 vars + GHA retrigger heading + sanitized-diff awk recipe + dashboard health probe.
</output>
