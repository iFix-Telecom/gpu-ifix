---
phase: 11
plan: 11-03
slug: lgpd-signoff-docs
wave: 1
depends_on: []
requirements: [PRD-05]
files_modified:
  - path: gateway/docs/LGPD-SIGNOFF-PROCESS.md
    kind: NEW
  - path: gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md
    kind: NEW
  - path: gateway/docs/LGPD-SUBPROCESSORS.md
    kind: MOD
  - path: .planning/legal/.gitignore
    kind: NEW
autonomous: true
estimated_duration: 75
---

<objective>
Ship PRD-05 LGPD doc-only deliverables: signoff process document, letter template for jurídico to review and sign, evidence-file convention, and MinIO addition to the canonical sub-processor list. Per D-16/D-17 the actual signature is an external gate (jurídico Ifix); Phase 11 delivers ONLY the material jurídico needs.

Reviews-noted nuances (Codex §11-03):
- **[reviews MEDIUM #1] MinIO classification:** MinIO is internally self-hosted (`s3.ifixtelecom.com.br`, internal infra — not an external SaaS vendor). The letter template MUST hedge the classification as "componente de infraestrutura (sub-processador se aplicável — confirmar com jurídico)" so legal performs the final classification call. Same hedge applies to the canonical SUBPROCESSORS.md MinIO row.
- **[reviews LOW #2] Model/provider slug drift:** The letter MUST NOT inline specific model slugs (Qwen 3.5 27B, Whisper, gpt-4, Novita routing, etc.) since models rotate between phases. The letter delegates current model/routing details to the canonical `gateway/docs/LGPD-SUBPROCESSORS.md` via a dated cross-ref ("revisão {YYYY-MM-DD}, commit `{SHA}`"). Sub-processor *company names* (Vast.ai, OpenAI, OpenRouter, MinIO) MUST remain in the letter — they are the PRD-05 grep gate per RESEARCH line 654.
</objective>

<must_haves>
  truths:
    - "D-16: doc-only deliverable our side; PRD-05 sign-off itself is external gate (D-17)"
    - "LGPD-SIGNOFF-LETTER-TEMPLATE.md references all 4 sub-processor companies: Vast.ai, OpenAI, OpenRouter, MinIO (RESEARCH §Validation Architecture line 654 grep gate)"
    - "LGPD-SUBPROCESSORS.md (canonical) extended to include MinIO as 4th sub-processor — single source of truth alignment with letter template"
    - "[reviews MEDIUM #1] Letter template MinIO row uses hedge wording 'componente de infraestrutura (sub-processador se aplicável — confirmar com jurídico)' since MinIO is self-hosted internally; the canonical SUBPROCESSORS.md MinIO row carries the same hedge"
    - "[reviews LOW #2] Letter template contains ZERO model/routing slugs (Qwen, gpt-4, gpt-5, Whisper, BGE, Novita); current model/provider details delegated via dated cross-ref to gateway/docs/LGPD-SUBPROCESSORS.md (revisão YYYY-MM-DD, commit SHA placeholder)"
    - "Evidence file convention `.planning/legal/lgpd-signoff-{YYYY-MM-DD}-{tenant}.pdf` is gitignored; templates in same dir ARE committed"
    - "pt-BR doc voice inherited verbatim from gateway/docs/LGPD-SUBPROCESSORS.md (Pattern H)"
  must_haves:
    - gateway/docs/LGPD-SIGNOFF-PROCESS.md exists with 'Quem assina', 'O que é entregue', 'Onde arquivar', 'Cadência' sections
    - gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md exists with placeholder fields ({{TENANT_SLUG}}, {{DATA_CLASS}}, {{DATE}}, {{SIGNATORY_*}}) and references all 4 sub-processor companies
    - "[reviews MEDIUM #1] LGPD-SIGNOFF-LETTER-TEMPLATE.md MinIO bullet contains EXACT phrase 'componente de infraestrutura (sub-processador se aplicável — confirmar com jurídico)' (or equivalent pt-BR hedge with all three semantic elements: infraestrutura / sub-processador conditional / confirmar jurídico)"
    - "[reviews MEDIUM #1] LGPD-SUBPROCESSORS.md MinIO row contains the same hedge wording so letter and canonical doc agree"
    - "[reviews LOW #2] LGPD-SIGNOFF-LETTER-TEMPLATE.md model/routing slugs absent: `grep -Ei 'Qwen|Novita|gpt-[345]|Whisper|BGE-M3|Chatterbox|llama'` returns ZERO matches (sub-processor company names Vast.ai/OpenAI/OpenRouter/MinIO are NOT slugs and remain)"
    - "[reviews LOW #2] LGPD-SIGNOFF-LETTER-TEMPLATE.md contains the canonical-doc cross-ref sentence: 'Os modelos e provedores upstream atuais estão documentados em `gateway/docs/LGPD-SUBPROCESSORS.md` (revisão {{SUBPROCESSORS_REVIEW_DATE}}, commit `{{SUBPROCESSORS_REVIEW_SHA}}`)'"
    - gateway/docs/LGPD-SUBPROCESSORS.md (canonical) contains MinIO row + data-path justification ("Last updated" bumped to 2026-05-27)
    - .planning/legal/.gitignore exists; root .gitignore already covers `.planning/legal/*.pdf` from plan 11-01
    - grep "Vast.ai\|OpenAI\|OpenRouter\|MinIO" gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md returns ≥4 lines (PRD-05 RESEARCH gate)
    - grep "MinIO" gateway/docs/LGPD-SUBPROCESSORS.md returns ≥1 line
  key_links:
    - from: gateway/docs/LGPD-SIGNOFF-PROCESS.md
      to: gateway/docs/LGPD-SUBPROCESSORS.md
      via: cross-ref "Lista atualizada de sub-processadores"
      pattern: "LGPD-SUBPROCESSORS"
    - from: gateway/docs/LGPD-SIGNOFF-PROCESS.md
      to: gateway/docs/LGPD-REVIEW-CHECKLIST.md
      via: cross-ref "Controles técnicos"
      pattern: "LGPD-REVIEW-CHECKLIST"
    - from: gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md
      to: gateway/docs/LGPD-SUBPROCESSORS.md
      via: dated cross-ref "(revisão {{SUBPROCESSORS_REVIEW_DATE}}, commit `{{SUBPROCESSORS_REVIEW_SHA}}`)" — model slugs live only in canonical doc
      pattern: "SUBPROCESSORS_REVIEW_DATE"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed doc → external legal review | template + process leave git; signed PDFs return as binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-LGPD-01 | Information Disclosure | signed PDFs accidentally committed to git | mitigate | `.planning/legal/*.pdf` gitignored at root + `.planning/legal/.gitignore` reinforces directory boundary; template-only checked in |
| T-11-LGPD-02 | Repudiation | sign-off provenance lost | mitigate | LGPD-SIGNOFF-PROCESS.md mandates evidence path `.planning/legal/lgpd-signoff-{YYYY-MM-DD}-{tenant}.pdf` naming convention; cadência anual review documented |
| T-11-LGPD-03 | Tampering | letter template lists sub-processor missing from canonical SUBPROCESSORS.md (drift) | mitigate | Task 11-03-03 aligns canonical doc by adding MinIO row; grep gate enforces consistency; letter cross-refs canonical (dated revisão + commit SHA) for model slugs so drift surfaces at next review (Codex LOW #2) |
| T-11-LGPD-04 | Repudiation | jurídico signs letter pre-classified as "sub-processor" when MinIO might be internal infra | mitigate | Letter + canonical doc both hedge MinIO row as "componente de infraestrutura (sub-processador se aplicável — confirmar com jurídico)" so legal retains classification authority (Codex MEDIUM #1) |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@.planning/phases/11-prod-hardening/11-REVIEWS.md
@gateway/docs/LGPD-SUBPROCESSORS.md
@gateway/docs/LGPD-REVIEW-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <id>11-03-01</id>
  <title>Task 1: LGPD-SIGNOFF-PROCESS.md authoring</title>
  <files>gateway/docs/LGPD-SIGNOFF-PROCESS.md, .planning/legal/.gitignore</files>
  <read_first>
    - gateway/docs/LGPD-SUBPROCESSORS.md (pt-BR doc voice, header convention, sub-processor terminology — Pattern H)
    - gateway/docs/LGPD-REVIEW-CHECKLIST.md (related Phase 09 doc, cross-ref target)
    - 11-CONTEXT.md D-16, D-17 + 11-PATTERNS.md §LGPD-SIGNOFF-PROCESS.md skeleton
  </read_first>
  <acceptance_criteria>
    - File `gateway/docs/LGPD-SIGNOFF-PROCESS.md` exists.
    - File `.planning/legal/.gitignore` exists with `*.pdf` line.
    - `grep -cE '^## (Quem assina|O que é entregue|Onde arquivar|Cadência)' gateway/docs/LGPD-SIGNOFF-PROCESS.md` ≥ 4.
    - `grep -c 'LGPD-SUBPROCESSORS\|LGPD-REVIEW-CHECKLIST' gateway/docs/LGPD-SIGNOFF-PROCESS.md` ≥ 2.
    - `grep -q '{YYYY-MM-DD}-{tenant}' gateway/docs/LGPD-SIGNOFF-PROCESS.md` exit 0.
    - `git check-ignore .planning/legal/lgpd-signoff-2026-05-27-telefonia.pdf` returns the path.
  </acceptance_criteria>
  <action>
    Create gateway/docs/LGPD-SIGNOFF-PROCESS.md per CONTEXT.md D-16 skeleton + PATTERNS Pattern H pt-BR voice:

    H1 title "LGPD — Processo de Sign-off"
    Header line `**Last updated: 2026-05-27.**` (current planning date)
    Section `## Purpose` — pt-BR paragraph stating the process precedes ativação de tenants `data_class: sensitive` em produção.
    Section `## Quem assina` — bulleted: Encarregado de Dados (DPO) da Ifix; Diretor jurídico; Owner técnico do gateway (Plataforma).
    Section `## O que é entregue ao jurídico` — bulleted cross-refs: gateway/docs/LGPD-SUBPROCESSORS.md; gateway/docs/LGPD-REVIEW-CHECKLIST.md; gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md.
    Section `## Onde arquivar a carta assinada` — single line: `.planning/legal/lgpd-signoff-{YYYY-MM-DD}-{tenant}.pdf` — gitignored binário (CONTEXT.md D-16 line 62).
    Section `## Cadência` — pt-BR paragraph: revisão anual + revisão event-driven em mudanças de sub-processador.
    Section `## Referências cruzadas` — list of: LGPD-SUBPROCESSORS.md, LGPD-REVIEW-CHECKLIST.md, RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md.

    Then create .planning/legal/.gitignore with content `*.pdf` (directory marker; the actual exclude rule is already in root .gitignore from 11-01).
  </action>
  <verify>
    <automated>test -f gateway/docs/LGPD-SIGNOFF-PROCESS.md && test -f .planning/legal/.gitignore && [ "$(grep -cE '^## (Quem assina|O que é entregue|Onde arquivar|Cadência)' gateway/docs/LGPD-SIGNOFF-PROCESS.md)" -ge 4 ]</automated>
  </verify>
  <done>LGPD-SIGNOFF-PROCESS.md has all 4 required H2 sections; cross-refs LGPD-SUBPROCESSORS + LGPD-REVIEW-CHECKLIST (≥2 backlinks); evidence path with `{YYYY-MM-DD}-{tenant}` placeholders matches CONTEXT.md D-16 verbatim; .planning/legal/.gitignore exists.</done>
</task>

<task type="auto">
  <id>11-03-02</id>
  <title>Task 2: LGPD-SIGNOFF-LETTER-TEMPLATE.md authoring [reviews MEDIUM #1 MinIO hedge + reviews LOW #2 model-slug delegation]</title>
  <files>gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md</files>
  <read_first>
    - gateway/docs/LGPD-SUBPROCESSORS.md (sub-processor table + pt-BR voice — Pattern H; canonical model slugs live here ONLY)
    - 11-PATTERNS.md §LGPD-SIGNOFF-LETTER-TEMPLATE.md (4-sub-processor invariant for PRD-05 grep gate)
    - 11-CONTEXT.md D-16 + 11-RESEARCH.md §Validation Architecture line 654 (grep gate)
    - 11-REVIEWS.md §"#### 11-03 LGPD Docs" (Codex MEDIUM = MinIO hedge; Codex LOW = no inline model slugs)
  </read_first>
  <acceptance_criteria>
    - File `gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` exists.
    - **[reviews PRD-05 gate]** `grep -c 'Vast.ai\|OpenAI\|OpenRouter\|MinIO' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` ≥ 4 — these are sub-processor *company names*, NOT model slugs, and MUST remain.
    - **[reviews LOW #2]** `grep -Ei 'Qwen|Novita|gpt-[345]|Whisper|BGE-M3|Chatterbox|llama' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` returns ZERO matches OR only inside an HTML comment block `<!-- example -->` clearly marked as illustrative. The legal letter MUST NOT inline current model slugs since they drift between phases.
    - **[reviews LOW #2]** `grep -q 'gateway/docs/LGPD-SUBPROCESSORS.md' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` exit 0 — cross-ref present.
    - **[reviews LOW #2]** `grep -q 'revisão.*{{SUBPROCESSORS_REVIEW_DATE}}.*commit.*{{SUBPROCESSORS_REVIEW_SHA}}' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` exit 0 — the dated cross-ref placeholder is the mechanism that forces jurídico to anchor each signature to a specific canonical-doc revision.
    - **[reviews MEDIUM #1]** `grep -qE 'componente de infraestrutura.*sub-processador se aplicável.*confirmar com jurídico' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` exit 0 — MinIO row hedge present with all three semantic elements (infraestrutura / conditional sub-processor / confirmar jurídico).
    - `grep -cE '\{\{TENANT_SLUG\}\}|\{\{DATA_CLASS\}\}|\{\{SIGNATORY_DPO\}\}|\{\{SIGNOFF_DATE\}\}' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` ≥ 4 placeholder fields.
    - `grep -q 'never-external\|NUNCA roteiam\|503.*upstream_unavailable_for_sensitive_tenant' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` exit 0 — sensitive-tenant invariant callout present.
  </acceptance_criteria>
  <action>
    Create gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md per Pattern H + CONTEXT.md D-16. Structure as a legal-ready letter template in pt-BR with replaceable placeholders. Required content:

    H1 "Carta de Sign-off LGPD — ifix-ai-gateway"
    Header line `**Last updated: 2026-05-27.**`

    Section `## Identificação` — placeholders: `{{TENANT_SLUG}}`, `{{DATA_CLASS}}` (normal|sensitive), `{{SIGNOFF_DATE}}` (YYYY-MM-DD), `{{SIGNATORY_DPO}}`, `{{SIGNATORY_LEGAL}}`, `{{SIGNATORY_PLATFORM}}`.

    Section `## Escopo` — pt-BR paragraph stating o gateway ifix-ai processa requisições do tenant `{{TENANT_SLUG}}` (classe `{{DATA_CLASS}}`) e roteia dados de prompt/áudio/embedding para upstreams locais (pod GPU primário) e externos quando aplicável. Use the generic phrase "pod GPU primário" — DO NOT name Vast.ai shape, GPU model, or any model slug here (reviews LOW #2).

    Section `## Sub-processadores declarados` — list referencing ALL 4 sub-processor *companies* (PRD-05 grep gate per RESEARCH line 654). Use generic role descriptors; the letter explicitly delegates current model slugs and routing providers to the canonical doc to avoid drift (reviews LOW #2):
      - **Vast.ai** — provedor de GPU primária; processa payloads tier-0 dentro do pod efêmero; sem persistência além do ciclo de vida do pod. Detalhes técnicos atuais (shape de GPU, allowlist, hardware) em `gateway/docs/LGPD-SUBPROCESSORS.md`.
      - **OpenAI** — fallback tier-1 para STT e embeddings; apenas tenants `data_class: normal` por D-19 / RES-08. Modelos e SKUs atuais em `gateway/docs/LGPD-SUBPROCESSORS.md`.
      - **OpenRouter** — fallback tier-1 para LLM; apenas tenants `data_class: normal`; bloqueio explícito para `sensitive` por RES-08. Provedor de roteamento e modelo atual em `gateway/docs/LGPD-SUBPROCESSORS.md`.
      - **MinIO — componente de infraestrutura (sub-processador se aplicável — confirmar com jurídico, reviews MEDIUM #1)** — armazenamento de pesos de modelos open-source consumidos pelo pod; nenhum dado de cliente persistido. Detalhes técnicos (bucket, endpoint, artefatos armazenados) em `gateway/docs/LGPD-SUBPROCESSORS.md`. **Nota classificatória**: MinIO é self-hosted internamente em `s3.ifixtelecom.com.br` sem fornecedor externo; a classificação final (sub-processador formal vs componente de infraestrutura interno) é decisão do jurídico Ifix. A inclusão aqui é divulgação completa (full-disclosure) do plano de dados.

    Section `## Modelos e provedores upstream` — single pt-BR sentence (verbatim form for the grep gate): "Os modelos e provedores upstream atuais estão documentados em `gateway/docs/LGPD-SUBPROCESSORS.md` (revisão `{{SUBPROCESSORS_REVIEW_DATE}}`, commit `{{SUBPROCESSORS_REVIEW_SHA}}`)." Then one explanatory line: "Esta carta intencionalmente não inline slugs de modelo para evitar drift entre revisões; cada assinatura âncora-se na revisão canônica acima."

    Section `## Garantia "never-external" para tenants sensíveis` — bold callout: tenants `data_class: sensitive` (telefonia, cobrancas) NUNCA roteiam para sub-processadores externos tier-1; quando tier-0 indisponível retornam HTTP 503 `upstream_unavailable_for_sensitive_tenant` (referência: gateway/internal/proxy/dispatcher.go + RES-08 invariant).

    Section `## Base legal e retenção` — placeholder for jurídico to fill: `{{LEGAL_BASIS}}`, `{{RETENTION_PERIOD}}`.

    Section `## Assinaturas` — 3 named signature lines: DPO da Ifix / Diretor jurídico / Owner técnico do gateway, with `{{SIGNOFF_DATE}}` next to each.

    Closing `## Referências` — bullet: `gateway/docs/LGPD-SUBPROCESSORS.md` (canonical, model slugs + technical detail authority), `gateway/docs/LGPD-REVIEW-CHECKLIST.md`, `gateway/docs/LGPD-SIGNOFF-PROCESS.md`.

    **HARD CONSTRAINT (reviews LOW #2):** Do NOT write any of these tokens anywhere outside an `<!-- example -->` HTML comment: Qwen, Novita, gpt-3, gpt-4, gpt-5, Whisper, BGE-M3, Chatterbox, llama. The four company names Vast.ai/OpenAI/OpenRouter/MinIO are required (PRD-05 gate) and are NOT slugs.
  </action>
  <verify>
    <automated>test -f gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md && [ "$(grep -c 'Vast.ai\|OpenAI\|OpenRouter\|MinIO' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md)" -ge 4 ] && [ "$(grep -cEi 'Qwen|Novita|gpt-[345]|Whisper|BGE-M3|Chatterbox|llama' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md | head -1)" -eq 0 ] && grep -qE 'componente de infraestrutura.*sub-processador se aplicável.*confirmar com jurídico' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md && grep -q '{{SUBPROCESSORS_REVIEW_DATE}}' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md && grep -q '{{SUBPROCESSORS_REVIEW_SHA}}' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md && grep -qE '\{\{TENANT_SLUG\}\}' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md && grep -qE '\{\{SIGNATORY_DPO\}\}' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md</automated>
  </verify>
  <done>Template exists; the 4 sub-processor companies enumerated (PRD-05 grep gate green); placeholder fields {{TENANT_SLUG}}/{{DATA_CLASS}}/{{SIGNOFF_DATE}}/{{SIGNATORY_*}}/{{SUBPROCESSORS_REVIEW_DATE}}/{{SUBPROCESSORS_REVIEW_SHA}} present; pt-BR "never-external" callout for sensitive tenants present; **[reviews MEDIUM #1]** MinIO row hedge "componente de infraestrutura (sub-processador se aplicável — confirmar com jurídico)" present verbatim; **[reviews LOW #2]** zero model/routing slugs (Qwen/Novita/gpt-X/Whisper/BGE-M3/Chatterbox/llama) anywhere in the file + dated canonical-doc cross-ref present.</done>
</task>

<task type="auto">
  <id>11-03-03</id>
  <title>Task 3: Add MinIO to canonical LGPD-SUBPROCESSORS.md [reviews MEDIUM #1 hedge mirrored]</title>
  <files>gateway/docs/LGPD-SUBPROCESSORS.md</files>
  <read_first>
    - gateway/docs/LGPD-SUBPROCESSORS.md (current LOC — 3 sub-processors: Vast.ai, OpenAI, OpenRouter; pt-BR voice; Markdown table shape)
    - gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md (task 11-03-02 output — 4-sub-processor list including MinIO; the hedge wording here MUST match what we put in the canonical doc row)
    - 11-CONTEXT.md D-16 + 11-RESEARCH.md §Validation Architecture line 654 (4-sub-processor grep gate must be consistent across both letter template AND canonical doc)
    - 11-REVIEWS.md §"#### 11-03 LGPD Docs" (Codex MEDIUM = same hedge mirrored in canonical doc)
    - ~/.claude/CLAUDE.md §MinIO S3 (endpoint, bucket `ai-gateway`, weights consumption — pod/scripts context)
  </read_first>
  <acceptance_criteria>
    - `grep -q 'MinIO' gateway/docs/LGPD-SUBPROCESSORS.md` exit 0.
    - `grep -c '^| \*\*' gateway/docs/LGPD-SUBPROCESSORS.md` ≥ 4 (table rows jumped from 3 to 4).
    - `grep -q 'Last updated: 2026-05-27' gateway/docs/LGPD-SUBPROCESSORS.md` exit 0.
    - **[reviews MEDIUM #1]** `grep -qE 'componente de infraestrutura.*sub-processador se aplicável.*confirmar com jurídico' gateway/docs/LGPD-SUBPROCESSORS.md` exit 0 — same hedge as the letter template MinIO row so legal sees one consistent classification posture.
    - Existing 3-row content (Vast.ai, OpenAI, OpenRouter) preserved — `grep -c 'Vast.ai\|OpenAI\|OpenRouter' gateway/docs/LGPD-SUBPROCESSORS.md` ≥ 3 (was ≥3 before, MUST still be ≥3 after).
  </acceptance_criteria>
  <action>
    Edit gateway/docs/LGPD-SUBPROCESSORS.md to add MinIO as 4th sub-processor. Preserve existing pt-BR voice (Pattern H) and Markdown table shape.

    1. Bump header: change `**Last updated: 2026-05-14.**` (or whatever current date is) to `**Last updated: 2026-05-27.**`.

    2. Append a 4th row to the `## Sub-processadores` table (between OpenRouter row and the "Garantia never-external" callout block). Hedge wording MUST match the letter template MinIO row (reviews MEDIUM #1) — semantic elements infraestrutura / conditional sub-processor / confirmar jurídico:

       Column 1 (Sub-processador): `**MinIO — componente de infraestrutura (sub-processador se aplicável — confirmar com jurídico)**`
       Column 2 (Função / dados processados): `Armazenamento S3-compatible de pesos de modelos open-source (s3.ifixtelecom.com.br, bucket ai-gateway); self-hosted internamente — classificação final (sub-processador formal vs componente interno) é decisão do jurídico Ifix`
       Column 3 (Dados de cliente recebidos): `**Nenhum dado de cliente** — apenas artefatos de modelo open-source pré-treinados consumidos pelo pod no boot`
       Column 4 (Quando): `**Sempre** no boot do pod GPU primário — pod baixa os pesos do bucket via mc cp/curl antes de servir tráfego.`

       (Note: column 2 may name MinIO's role and bucket name — these are infra identifiers, not model slugs, so they belong in the canonical doc. The letter template delegates to here.)

    3. Add a one-sentence rationale paragraph immediately after the table OR as a footnote under the Mechanism section explaining MinIO's data-path and the classification hedge:
       "**MinIO bucket `ai-gateway` em `s3.ifixtelecom.com.br` armazena pesos GGUF/safetensors do primary pod — nenhum dado de cliente persistido, apenas artefatos de modelo open-source pré-treinados.** A inclusão aqui é divulgação completa (full-disclosure) do plano de dados, mesmo que o sub-processador (se assim classificado pelo jurídico) não receba PII por design. A classificação final (sub-processador formal vs componente de infraestrutura interno) é decisão do jurídico Ifix dado o caráter self-hosted — esta hedge é intencional e espelhada na carta de sign-off."

    4. Preserve EVERY other line of the file unchanged (CR invariant — Mechanism section + Mapeamento tenant table + Related Docs section + Garantia never-external callout).

    Acceptance: `grep -q "MinIO" gateway/docs/LGPD-SUBPROCESSORS.md` exits 0; row count under `## Sub-processadores` table jumps from 3 to 4; hedge wording present.
  </action>
  <verify>
    <automated>grep -q 'MinIO' gateway/docs/LGPD-SUBPROCESSORS.md && [ "$(grep -c '^| \*\*' gateway/docs/LGPD-SUBPROCESSORS.md)" -ge 4 ] && grep -q 'Last updated: 2026-05-27' gateway/docs/LGPD-SUBPROCESSORS.md && grep -qE 'componente de infraestrutura.*sub-processador se aplicável.*confirmar com jurídico' gateway/docs/LGPD-SUBPROCESSORS.md</automated>
  </verify>
  <done>MinIO row present in `## Sub-processadores` table with **[reviews MEDIUM #1]** hedge "componente de infraestrutura (sub-processador se aplicável — confirmar com jurídico)" mirroring letter template; data-path justification sentence committed (full-disclosure + classification-pending note); "Last updated" header bumped to 2026-05-27; existing 3-row content preserved; sensitive-tenant "never-external" callout intact; both letter template AND canonical doc now list the same 4 sub-processor companies with consistent MinIO classification hedge (drift eliminated).</done>
</task>

</tasks>

<verification>
- `grep -q 'Vast.ai\|OpenAI\|OpenRouter\|MinIO' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` returns 0 exit (PRD-05 RESEARCH gate — sub-processor company names).
- `grep -q 'MinIO' gateway/docs/LGPD-SUBPROCESSORS.md` returns 0 exit (canonical doc alignment).
- **[reviews MEDIUM #1]** `grep -qE 'componente de infraestrutura.*sub-processador se aplicável.*confirmar com jurídico' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md gateway/docs/LGPD-SUBPROCESSORS.md` returns 0 exit (hedge present in BOTH files).
- **[reviews LOW #2]** `grep -cEi 'Qwen|Novita|gpt-[345]|Whisper|BGE-M3|Chatterbox|llama' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` returns 0 (no model/routing slugs in legal letter).
- **[reviews LOW #2]** `grep -q '{{SUBPROCESSORS_REVIEW_DATE}}' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md && grep -q '{{SUBPROCESSORS_REVIEW_SHA}}' gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` (dated cross-ref placeholders present so each signed letter anchors to a specific canonical revision).
- `grep -c '## ' gateway/docs/LGPD-SIGNOFF-PROCESS.md` returns ≥4 H2 sections.
- `git check-ignore .planning/legal/lgpd-signoff-2026-05-27-telefonia.pdf` returns the path (gitignore active).
</verification>

<success_criteria>
- Both LGPD docs exist with pt-BR doc voice from Pattern H.
- 4 sub-processor *companies* enumerated in letter template (PRD-05 grep gate green).
- **[reviews MEDIUM #1]** Letter template AND canonical SUBPROCESSORS.md both hedge MinIO classification as "componente de infraestrutura (sub-processador se aplicável — confirmar com jurídico)" — consistent posture, jurídico retains classification authority.
- **[reviews LOW #2]** Letter template contains ZERO inline model/routing slugs (Qwen, Novita, gpt-X, Whisper, BGE-M3, Chatterbox, llama); current model/provider details delegated via dated cross-ref `gateway/docs/LGPD-SUBPROCESSORS.md (revisão {{SUBPROCESSORS_REVIEW_DATE}}, commit {{SUBPROCESSORS_REVIEW_SHA}})` — eliminates drift between letter and canonical when models rotate.
- Canonical LGPD-SUBPROCESSORS.md extended with MinIO row (drift between letter template and canonical doc eliminated).
- Evidence path convention documented per D-16 verbatim.
- D-17 external sign-off gate explicitly stated as out-of-scope for code work.
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies — doc-only this plan. (Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.)
- Truth 2: No speculative language; pt-BR voice consistent with LGPD-SUBPROCESSORS.md.
- Truth 8 (cross-cutting): NO raw API keys, tenant identifiers, headers, or PII in committed LGPD docs; templates use placeholders only.
- Templates committed; signed PDFs stay gitignored.
- **[reviews LOW #2 invariant]** Legal letter NEVER inlines model slugs — they live in canonical SUBPROCESSORS.md only, accessed via dated revisão+SHA cross-ref. This is the drift-prevention contract.
- **[reviews MEDIUM #1 invariant]** MinIO classification hedge ("componente de infraestrutura (sub-processador se aplicável — confirmar com jurídico)") is mirrored verbatim across letter template AND canonical doc.
</cross_cutting_truths>

<output>
Create `.planning/phases/11-prod-hardening/11-03-SUMMARY.md` with file paths, byte counts, and grep results for: (a) the 4-sub-processor PRD-05 gate, (b) the canonical doc MinIO addition, (c) **[reviews MEDIUM #1]** the "componente de infraestrutura ... confirmar com jurídico" hedge in BOTH files, and (d) **[reviews LOW #2]** zero-match for `Qwen|Novita|gpt-[345]|Whisper|BGE-M3|Chatterbox|llama` in the letter template + presence of `{{SUBPROCESSORS_REVIEW_DATE}}` / `{{SUBPROCESSORS_REVIEW_SHA}}` placeholders.
</output>
