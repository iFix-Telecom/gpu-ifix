---
phase: 11
plan: 11-03
slug: lgpd-signoff-docs
wave: 1
depends_on: []
requirements: [PRD-05]
files_modified:
  - path: gateway/docs/LGPD-SIGNOFF-PROCESS.md
    kind: NEW
  - path: gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md
    kind: NEW
  - path: gateway/docs/LGPD-SUBPROCESSORS.md
    kind: MOD
  - path: .planning/legal/.gitignore
    kind: NEW
autonomous: true
estimated_duration: 75
---

<objective>
Ship PRD-05 LGPD doc-only deliverables: signoff process document, letter template for jurídico to review and sign, evidence-file convention, and MinIO addition to the canonical sub-processor list. Per D-16/D-17 the actual signature is an external gate (jurídico Ifix); Phase 11 delivers ONLY the material jurídico needs. Reviews-noted nuances: (a) MinIO is internally self-hosted — letter template uses "sub-processador/componente de infraestrutura (a confirmar com jurídico)" wording to let legal classify; (b) provider/model detail strings live in the canonical LGPD-SUBPROCESSORS.md, and the letter template REFERENCES that doc rather than duplicating model slugs (so drift is impossible).
</objective>

<must_haves>
  truths:
    - "D-16: doc-only deliverable our side; PRD-05 sign-off itself is external gate (D-17)"
    - "LGPD-SIGNOFF-LETTER-TEMPLATE.md references all 4 sub-processors: Vast.ai, OpenAI, OpenRouter, MinIO (RESEARCH §Validation Architecture line 654 grep gate)"
    - "LGPD-SUBPROCESSORS.md (canonical) extended to include MinIO as 4th sub-processor — single source of truth alignment with letter template"
    - "MinIO classification note (Codex MEDIUM): the letter template marks MinIO as 'sub-processador/componente de infraestrutura (a confirmar com jurídico)' since MinIO is self-hosted internally; legal team performs the final classification. The canonical SUBPROCESSORS.md doc retains the full-disclosure rationale row."
    - "Provider-model details indirection (Codex LOW): the letter template uses generic role names (e.g. 'fallback tier-1 para LLM') and points the reader to gateway/docs/LGPD-SUBPROCESSORS.md for current model slugs (Qwen 3.6 GGUF, Whisper large-v3, etc.) instead of inlining them — eliminating drift between letter and canonical when models rotate."
    - "Evidence file convention `.planning/legal/lgpd-signoff-{YYYY-MM-DD}-{tenant}.pdf` is gitignored; templates in same dir ARE committed"
    - "pt-BR doc voice inherited verbatim from gateway/docs/LGPD-SUBPROCESSORS.md (Pattern H)"
  must_haves:
    - gateway/docs/LGPD-SIGNOFF-PROCESS.md exists with 'Quem assina', 'O que é entregue', 'Onde arquivar', 'Cadência' sections
    - gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md exists with placeholder fields ({{TENANT_SLUG}}, {{DATA_CLASS}}, {{DATE}}, {{SIGNATORY_*}}) and references all 4 sub-processors
    - LGPD-SIGNOFF-LETTER-TEMPLATE.md MinIO bullet flagged "(a confirmar com jurídico)" or equivalent classification-pending wording
    - LGPD-SIGNOFF-LETTER-TEMPLATE.md model/provider technical details DELEGATED to LGPD-SUBPROCESSORS.md via cross-ref (no inline model slug duplication of more than the bare minimum)
    - gateway/docs/LGPD-SUBPROCESSORS.md (canonical) contains MinIO row + data-path justification ("Last updated" bumped to 2026-05-27)
    - .planning/legal/.gitignore exists; root .gitignore already covers `.planning/legal/*.pdf` from plan 11-01
    - grep "Vast.ai\|OpenAI\|OpenRouter\|MinIO" gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md returns ≥4 lines
    - grep "MinIO" gateway/docs/LGPD-SUBPROCESSORS.md returns ≥1 line
  key_links:
    - from: gateway/docs/LGPD-SIGNOFF-PROCESS.md
      to: gateway/docs/LGPD-SUBPROCESSORS.md
      via: cross-ref "Lista atualizada de sub-processadores"
      pattern: "LGPD-SUBPROCESSORS"
    - from: gateway/docs/LGPD-SIGNOFF-PROCESS.md
      to: gateway/docs/LGPD-REVIEW-CHECKLIST.md
      via: cross-ref "Controles técnicos"
      pattern: "LGPD-REVIEW-CHECKLIST"
    - from: gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md
      to: gateway/docs/LGPD-SUBPROCESSORS.md
      via: 4-sub-processor list MUST match canonical doc; model slugs cross-ref'd via "Para detalhes técnicos atuais ver…"
      pattern: "MinIO"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed doc → external legal review | template + process leave git; signed PDFs return as binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-LGPD-01 | Information Disclosure | signed PDFs accidentally committed to git | mitigate | `.planning/legal/*.pdf` gitignored at root + `.planning/legal/.gitignore` reinforces directory boundary; template-only checked in |
| T-11-LGPD-02 | Repudiation | sign-off provenance lost | mitigate | LGPD-SIGNOFF-PROCESS.md mandates evidence path `.planning/legal/lgpd-signoff-{YYYY-MM-DD}-{tenant}.pdf` naming convention; cadência anual review documented |
| T-11-LGPD-03 | Tampering | letter template lists sub-processor missing from canonical SUBPROCESSORS.md (drift) | mitigate | Task 11-03-03 aligns canonical doc by adding MinIO row; grep gate enforces consistency; letter cross-refs canonical for model slugs (Codex LOW) |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@gateway/docs/LGPD-SUBPROCESSORS.md
@gateway/docs/LGPD-REVIEW-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <id>11-03-01</id>
  <title>Task 1: LGPD-SIGNOFF-PROCESS.md authoring</title>
  <files>gateway/docs/LGPD-SIGNOFF-PROCESS.md, .planning/legal/.gitignore</files>
  <read_first>
    - gateway/docs/LGPD-SUBPROCESSORS.md (pt-BR doc voice, header convention, sub-processor terminology — Pattern H)
    - gateway/docs/LGPD-REVIEW-CHECKLIST.md (related Phase 09 doc, cross-ref target)
    - 11-CONTEXT.md D-16, D-17 + 11-PATTERNS.md §LGPD-SIGNOFF-PROCESS.md skeleton
  </read_first>
  <action>
    Create gateway/docs/LGPD-SIGNOFF-PROCESS.md per CONTEXT.md D-16 skeleton + PATTERNS Pattern H pt-BR voice:

    H1 title "LGPD — Processo de Sign-off"
    `**Last updated: 2026-05-27.**` (current planning date)
    `## Purpose` — pt-BR paragraph stating the process precedes ativação de tenants `data_class: sensitive` em produção.
    `## Quem assina` — bulleted: Encarregado de Dados (DPO) da Ifix; Diretor jurídico; Owner técnico do gateway (Plataforma).
    `## O que é entregue ao jurídico` — bulleted cross-refs: gateway/docs/LGPD-SUBPROCESSORS.md; gateway/docs/LGPD-REVIEW-CHECKLIST.md; gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md.
    `## Onde arquivar a carta assinada` — single line: `.planning/legal/lgpd-signoff-{YYYY-MM-DD}-{tenant}.pdf` — gitignored binário (CONTEXT.md D-16 line 62).
    `## Cadência` — pt-BR paragraph: revisão anual + revisão event-driven em mudanças de sub-processador.
    `## Referências cruzadas` — list of: LGPD-SUBPROCESSORS.md, LGPD-REVIEW-CHECKLIST.md, RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md.

    Then create .planning/legal/.gitignore with content `*.pdf` (directory marker; the actual exclude rule is already in root .gitignore from 11-01).
  </action>
  <verify>
    <automated>test -f gateway/docs/LGPD-SIGNOFF-PROCESS.md && test -f .planning/legal/.gitignore && grep -c "Quem assina\|O que é entregue\|Onde arquivar\|Cadência" gateway/docs/LGPD-SIGNOFF-PROCESS.md | grep -E "^\s*[4-9]"</automated>
  </verify>
  <done>LGPD-SIGNOFF-PROCESS.md has all 4 required H2 sections; cross-refs LGPD-SUBPROCESSORS + LGPD-REVIEW-CHECKLIST (≥2 backlinks); evidence path with `{YYYY-MM-DD}-{tenant}` placeholders matches CONTEXT.md D-16 verbatim; .planning/legal/.gitignore exists.</done>
</task>

<task type="auto">
  <id>11-03-02</id>
  <title>Task 2: LGPD-SIGNOFF-LETTER-TEMPLATE.md authoring (MinIO classification-pending + provider-detail indirection)</title>
  <files>gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md</files>
  <read_first>
    - gateway/docs/LGPD-SUBPROCESSORS.md (sub-processor table + pt-BR voice — Pattern H)
    - 11-PATTERNS.md §LGPD-SIGNOFF-LETTER-TEMPLATE.md (4-sub-processor invariant for PRD-05 grep gate)
    - 11-CONTEXT.md D-16 + 11-RESEARCH.md §Validation Architecture line 654 (grep gate)
  </read_first>
  <action>
    Create gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md per Pattern H + CONTEXT.md D-16. Structure as a legal-ready letter template in pt-BR with replaceable placeholders. Required content:

    H1 "Carta de Sign-off LGPD — ifix-ai-gateway"
    `**Last updated: 2026-05-27.**`
    `## Identificação` — placeholders: `{{TENANT_SLUG}}`, `{{DATA_CLASS}}` (normal|sensitive), `{{SIGNOFF_DATE}}` (YYYY-MM-DD), `{{SIGNATORY_DPO}}`, `{{SIGNATORY_LEGAL}}`, `{{SIGNATORY_PLATFORM}}`.
    `## Escopo` — pt-BR paragraph stating o gateway ifix-ai processa requisições do tenant `{{TENANT_SLUG}}` (classe `{{DATA_CLASS}}`) e roteia dados de prompt/áudio/embedding para upstreams locais (pod Vast.ai) e externos quando aplicável.
    `## Sub-processadores declarados` — list referencing ALL 4 sub-processors (PRD-05 grep gate per RESEARCH line 654). Use generic role descriptors; the letter explicitly delegates current model slugs to the canonical doc to avoid drift (Codex LOW):
      - **Vast.ai** — provedor de GPU primária; processa payloads tier-0 dentro do pod efêmero; sem persistência além do ciclo de vida do pod. Detalhes técnicos atuais (shape de GPU, allowlist) em `gateway/docs/LGPD-SUBPROCESSORS.md`.
      - **OpenAI** — fallback tier-1 para STT e embeddings; apenas tenants `data_class: normal` por D-19 / RES-08. Modelos atuais em `gateway/docs/LGPD-SUBPROCESSORS.md`.
      - **OpenRouter** — fallback tier-1 para LLM; apenas tenants `data_class: normal`; bloqueio explícito para `sensitive` por RES-08. Modelo de roteamento atual em `gateway/docs/LGPD-SUBPROCESSORS.md`.
      - **MinIO (sub-processador/componente de infraestrutura — a confirmar com jurídico, Codex MEDIUM)** — armazenamento de pesos de modelos open-source consumidos pelo pod; nenhum dado de cliente persistido. Detalhes técnicos em `gateway/docs/LGPD-SUBPROCESSORS.md`. **Nota**: a classificação MinIO (sub-processador vs componente de infraestrutura interno) é decisão do jurídico Ifix, dado que MinIO é self-hosted internamente sem fornecedor externo; a inclusão aqui é divulgação completa (full-disclosure).
    `## Garantia "never-external" para tenants sensíveis` — bold callout: tenants `data_class: sensitive` (telefonia, cobrancas) NUNCA roteiam para OpenAI / OpenRouter; quando tier-0 indisponível retornam HTTP 503 `upstream_unavailable_for_sensitive_tenant` (referência: gateway/internal/proxy/dispatcher.go + RES-08 invariant).
    `## Base legal e retenção` — placeholder for jurídico to fill: `{{LEGAL_BASIS}}`, `{{RETENTION_PERIOD}}`.
    `## Assinaturas` — 3 named signature lines: DPO da Ifix / Diretor jurídico / Owner técnico do gateway, with `{{DATE}}` next to each.

    Cite the canonical sub-processor doc in a closing "Referências": gateway/docs/LGPD-SUBPROCESSORS.md (model slugs + technical detail authority).
  </action>
  <verify>
    <automated>grep -c "Vast.ai\|OpenAI\|OpenRouter\|MinIO" gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md | awk '{if ($1 >= 4) exit 0; else exit 1}' && grep -E "\{\{TENANT_SLUG\}\}|\{\{DATA_CLASS\}\}|\{\{SIGNATORY_DPO\}\}" gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md && grep -E "a confirmar com jurídico|confirmar com.*jurídico" gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md && grep -c "gateway/docs/LGPD-SUBPROCESSORS.md" gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md | awk '{if ($1 >= 3) exit 0; else exit 1}'</automated>
  </verify>
  <done>Template exists; grep for the 4 sub-processors returns ≥4 lines (PRD-05 RESEARCH §Validation Architecture line 654 gate); template has placeholder fields {{TENANT_SLUG}} + {{DATA_CLASS}} + {{SIGNOFF_DATE}} + {{SIGNATORY_*}}; contains pt-BR "never-external" callout for sensitive tenants; MinIO bullet flagged "(a confirmar com jurídico)" (Codex MEDIUM); model-detail indirection to LGPD-SUBPROCESSORS.md via ≥3 cross-refs (Codex LOW).</done>
</task>

<task type="auto">
  <id>11-03-03</id>
  <title>Task 3: Add MinIO to canonical LGPD-SUBPROCESSORS.md</title>
  <files>gateway/docs/LGPD-SUBPROCESSORS.md</files>
  <read_first>
    - gateway/docs/LGPD-SUBPROCESSORS.md (current 81 LOC — 3 sub-processors: Vast.ai, OpenAI, OpenRouter; pt-BR voice; Markdown table shape)
    - gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md (task 11-03-02 output — 4-sub-processor list including MinIO)
    - 11-CONTEXT.md D-16 + 11-RESEARCH.md §Validation Architecture line 654 (4-sub-processor grep gate must be consistent across both letter template AND canonical doc)
    - ~/.claude/CLAUDE.md §MinIO S3 (endpoint, bucket `ai-gateway`, weights consumption — pod/scripts context)
  </read_first>
  <action>
    Edit gateway/docs/LGPD-SUBPROCESSORS.md to add MinIO as 4th sub-processor. Preserve existing pt-BR voice (Pattern H) and Markdown table shape.

    1. Bump header: change `**Last updated: 2026-05-14.**` to `**Last updated: 2026-05-27.**`.

    2. Append a 4th row to the `## Sub-processadores` table (between OpenRouter row and the "Garantia never-external" callout block):
    ```
    | **MinIO**       | Armazenamento S3-compatible de pesos de modelos (`s3.ifixtelecom.com.br`, bucket `ai-gateway`); self-hosted internamente — classificação sub-processador vs componente interno a confirmar com jurídico | **Nenhum dado de cliente** — apenas artefatos de modelo open-source pré-treinados (Qwen 3.6 GGUF, Whisper large-v3, BGE-M3, Chatterbox) | **Sempre** no boot do pod Vast.ai — pod baixa os pesos do bucket via `mc cp`/curl antes de servir tráfego. |
    ```

    3. Add a one-sentence rationale paragraph immediately after the table OR as a footnote under the Mechanism section explaining MinIO's data-path:
    "**MinIO bucket `ai-gateway` em s3.ifixtelecom.com.br armazena pesos GGUF/safetensors do primary pod — nenhum dado de cliente persistido, apenas artefatos de modelo open-source pré-treinados.** A inclusão aqui é divulgação completa (full-disclosure) do plano de dados, mesmo que o sub-processador não receba PII por design. A classificação final (sub-processador formal vs componente de infraestrutura interno) é decisão do jurídico Ifix dado o caráter self-hosted."

    4. Preserve EVERY other line of the file unchanged (CR invariant — Mechanism section + Mapeamento tenant table + Related Docs section).

    Acceptance: `grep -q "MinIO" gateway/docs/LGPD-SUBPROCESSORS.md` exits 0; row count under `## Sub-processadores` table jumps from 3 to 4.
  </action>
  <verify>
    <automated>grep -q "MinIO" gateway/docs/LGPD-SUBPROCESSORS.md && grep -c "^\| \*\*" gateway/docs/LGPD-SUBPROCESSORS.md | awk '{if ($1 >= 4) exit 0; else exit 1}' && grep -q "Last updated: 2026-05-27" gateway/docs/LGPD-SUBPROCESSORS.md</automated>
  </verify>
  <done>MinIO row present in `## Sub-processadores` table; data-path justification sentence committed (full-disclosure + classification-pending note); "Last updated" header bumped to 2026-05-27; existing 3-row content preserved; sensitive-tenant "never-external" callout intact; both letter template AND canonical doc now list the same 4 sub-processors (drift eliminated).</done>
</task>

</tasks>

<verification>
- `grep -q "Vast.ai\|OpenAI\|OpenRouter\|MinIO" gateway/docs/LGPD-SIGNOFF-*.md` returns 0 exit (PRD-05 RESEARCH gate).
- `grep -q "MinIO" gateway/docs/LGPD-SUBPROCESSORS.md` returns 0 exit (canonical doc alignment).
- `grep -q "a confirmar com jurídico\|confirmar com.*jurídico" gateway/docs/LGPD-SIGNOFF-LETTER-TEMPLATE.md` returns 0 exit (Codex MEDIUM classification note).
- `grep -c "## " gateway/docs/LGPD-SIGNOFF-PROCESS.md` returns ≥4 H2 sections.
- `git check-ignore .planning/legal/lgpd-signoff-2026-05-27-telefonia.pdf` returns the path (gitignore active).
</verification>

<success_criteria>
- Both LGPD docs exist with pt-BR doc voice from Pattern H.
- 4 sub-processors enumerated in letter template (grep gate green).
- Letter template flags MinIO classification as jurídico-pending (Codex MEDIUM).
- Letter template delegates model/provider technical details to LGPD-SUBPROCESSORS.md (Codex LOW — eliminates drift).
- Canonical LGPD-SUBPROCESSORS.md extended with MinIO row (drift between letter template and canonical doc eliminated).
- Evidence path convention documented per D-16 verbatim.
- D-17 external sign-off gate explicitly stated as out-of-scope for code work.
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies — doc-only this plan. (Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.)
- Truth 2: No speculative language; pt-BR voice consistent with LGPD-SUBPROCESSORS.md.
- Truth 8 (cross-cutting): NO raw API keys, tenant identifiers, headers, or PII in committed LGPD docs; templates use placeholders only.
- Templates committed; signed PDFs stay gitignored.
</cross_cutting_truths>

<output>
Create `.planning/phases/11-prod-hardening/11-03-SUMMARY.md` with file paths, byte counts, and grep results for the 4-sub-processor gate AND the canonical doc MinIO addition AND the "a confirmar com jurídico" classification note.
</output>
