---
phase: 11
plan: 11-07
slug: chaos-kill-primary-live-uat
wave: 2
depends_on: [11-06]
requirements: [PRD-02]
files_modified:
  - path: scripts/chaos/vast-delete.sh
    kind: NEW
  - path: .planning/phases/11-prod-hardening/11-07-EVIDENCE.md
    kind: NEW
autonomous: false
estimated_duration: 90
---

<objective>
Execute PRD-02 chaos test: mid-load Vast API DELETE raw of the active primary lifecycle's instance. Gateway must discover failure via natural probe timeout (NOT operator-driven force-open). Breaker `local-llm` opens by observation; FSM advances Ready → Draining → Asleep; tier-1 OpenRouter takes LLM traffic. Strict allowed-error-class budget enforced for T+0..T+60s [reviews MEDIUM #2]. Cleanup: observe reconciliation for a FIXED 90-second window with polled FSM/breaker snapshots BEFORE any manual `force-up` [reviews MEDIUM #1]. Vast DELETE is treated as idempotent with explicit 404 handling + connect timeout + single retry [reviews LOW #5]. Primary instance ID extracted via JSON-preferred path with documented text fallback [reviews LOW #3]. ZERO raw `ifix_sk_...` / Bearer token literals in plan or evidence — env-var labels only, pre-commit grep gate enforced [reviews HIGH #4]. Ship the chaos hook script + evidence. Spend ~$0.30 (primary re-up cycle post-chaos).
</objective>

<must_haves>
  truths:
    - "D-07: PRD-02 kill primary = Vast API DELETE raw (curl -X DELETE Bearer); host yank realistic"
    - "Natural breaker observation (NOT force-open); FSM Ready → Draining → Asleep auto"
    - "Pitfall 2: invisible-failover SLO acknowledges a 5-10s blip window between DELETE and breaker observation — 503 transient acceptable, 5xx panic NOT"
    - "[reviews MEDIUM #2] Allowed error classes T+0..T+60s: HTTP 503 `upstream_unavailable_transient` PERMITTED up to 60s; HTTP 504 `gateway_timeout` PERMITTED up to 30s; HTTP 503 `upstream_unavailable_for_sensitive_tenant` PERMITTED any time (RES-08 expected); HTTP 503 `breaker_open` PERMITTED while breaker propagates; HTTP 500 panic NOT permitted (hard gate); HTTP 502 bad_gateway NOT permitted (hard gate — upstream connection corruption)"
    - "[reviews MEDIUM #1] Observe-then-intervene: operator MUST poll `gatewayctl primary state` + `local-llm.state` every 5s for FIXED 90s window BEFORE any manual intervention; if primary auto-recovers within 90s (FSM advanced past Draining without operator action) record `auto_recovery: true` and DO NOT run `force-up` (controller worked); only if at t+90s FSM still Draining AND breaker still Open AND no auto re-spin observed may operator run `gatewayctl primary force-up` (record `manual_intervention: true` with timestamp)"
    - "[reviews LOW #3] JSON output preference for instance ID extraction: script tries `gatewayctl primary state --json | jq -e '.instance_id'` first; if --json flag unsupported or returns null, falls back to explicit awk regex `/^instance_id:/ {print $2}` with logged warning; if both yield empty, aborts with clear error"
    - "[reviews LOW #5] Vast DELETE idempotent + resilient: 200 = killed; 404 = idempotent gone (log `idempotent_already_deleted: true`, treat as success); 5xx = retry 1× after 2s backoff then fail; curl uses --connect-timeout 10 --max-time 30"
    - "[reviews HIGH #4] Zero raw secrets: NO `ifix_sk_...` literals, NO Bearer token literals, NO 60-char hex strings in plan text or evidence; Vast API token referenced as `${VAST_AI_API_KEY}` env var; tenant keys referenced as `${IFIX_KEY_*}` env-var labels; pre-commit grep gate `grep -rE 'ifix_sk_|Bearer [a-f0-9]{60}'` MUST return zero matches"
    - "Pattern E bash skeleton: set -euo pipefail + ISO-8601 log() + env validation + idempotent"
  must_haves:
    - scripts/chaos/vast-delete.sh exists, executable, runs from ops-claude or any host with $VAST_AI_API_KEY env
    - vast-delete.sh tries `gatewayctl primary state --json` first; documents fallback to text-grep extraction with awk regex
    - vast-delete.sh handles Vast DELETE 404 as idempotent success; sets curl --connect-timeout 10 --max-time 30; retries once on 5xx with 2s sleep
    - vast-delete.sh runs FIXED 90s polling observation loop (5s interval) capturing `primary_state` + `breaker_state` BEFORE any operator intervention
    - 11-07-EVIDENCE.md committed with: timeline (UTC), DELETE response code, 90s polling snapshot table, time-to-breaker-OPEN, P95 during chaos window, allowed-error-classes histogram (zero 500 + zero 502 required), auto_recovery vs manual_intervention flag, Vast cleanup confirmation
    - post-chaos primary state restored (auto OR via documented manual `force-up` after 90s observation)
    - Zero `ifix_sk_...` literals AND zero `Bearer [a-f0-9]{60}` literals in plan or evidence (pre-commit grep gate)
  key_links:
    - from: scripts/chaos/vast-delete.sh
      to: https://console.vast.ai/api/v0/instances/{id}/
      via: curl -X DELETE --connect-timeout 10 --max-time 30 -H "Authorization: Bearer $VAST_AI_API_KEY"
      pattern: "console.vast.ai/api/v0/instances"
    - from: chaos script polling
      to: https://ai-gateway.converse-ai.app/v1/health/upstreams
      via: curl + jq local-llm.state
      pattern: "local-llm.state"
    - from: scripts/chaos/vast-delete.sh primary state extraction
      to: gatewayctl primary state --json (preferred) OR awk text parse (fallback)
      via: subprocess invocation
      pattern: "primary state --json|/\\^instance_id:/"
    - from: scripts/chaos/vast-delete.sh observe-first window
      to: gatewayctl primary state + /v1/health/upstreams
      via: 5s polling for 90s capturing FSM + breaker snapshots
      pattern: "OBSERVE-FIRST|sleep 5"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude -> Vast API | Bearer token from $VAST_AI_API_KEY env (never literal) |
| chaos timing window | 5-10s between DELETE 200 and breaker observation OPEN — 503 transient window expected |
| operator -> reconciler | 90s observe window prevents manual force-up from racing the controller [reviews MEDIUM #1] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-CHAOS-01 | Denial of Service | Vast pod orphaned + billing continues | mitigate | Hard exit-on-error in vast-delete.sh; retry once on 5xx with 2s backoff; --connect-timeout 10 --max-time 30; explicit verification of vast instance count == 0 post-DELETE; 404 treated as idempotent success [reviews LOW #5] |
| T-11-CHAOS-02 | Information Disclosure | Vast API key in shell history | mitigate | curl uses -H "Authorization: Bearer $VAST_AI_API_KEY" (env var); never inline literal; script asserts env present and fails fast if absent |
| T-11-CHAOS-03 | Denial of Service | Chaos triggers cascade affecting other tenants beyond intended SLO budget | mitigate | Allowed-error-classes budget [reviews MEDIUM #2]: zero 500 + zero 502 = hard gate; 503 transient + 504 timeout permitted within windows; histogram captured in evidence |
| T-11-CHAOS-06 | Information Disclosure | tenant API keys appear in plan or evidence | mitigate | [reviews HIGH #4] Cross-cutting hard rule — env-var label placeholders only (`${IFIX_KEY_*}`, `${VAST_AI_API_KEY}`); evidence redacts to first-4-char prefix; pre-commit grep gate `grep -rE 'ifix_sk_\|Bearer [a-f0-9]{60}'` enforced before commit |
| T-11-CHAOS-07 | Tampering | manual force-up fights the reconciler causing FSM oscillation | mitigate | [reviews MEDIUM #1] Observe-first sequence: FIXED 90-second polling window (5s interval, FSM + breaker snapshots) before any manual intervention; intervention only if at t+90s FSM still Draining AND breaker still Open |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@.planning/phases/11-prod-hardening/11-06-PLAN.md
@pod/scripts/vast-ai.sh
</context>

<tasks>

<task type="auto">
  <id>11-07-01</id>
  <title>Task 1: Author scripts/chaos/vast-delete.sh [reviews LOW #3, LOW #5, MEDIUM #1, HIGH #4]</title>
  <files>scripts/chaos/vast-delete.sh</files>
  <read_first>
    - pod/scripts/vast-ai.sh (auth + api helper pattern lines 14-37; Pattern E + Bearer auth)
    - scripts/deploy/bootstrap-postgres.sh (set/log/env-validation skeleton — Pattern E)
    - 11-PATTERNS.md vast-delete.sh (Pattern E body + Vast DELETE shape + cleanup advice)
    - 11-RESEARCH.md Pattern 4 + System Architecture Diagram PRD-02 path + Assumption A1 (DELETE idempotency unconfirmed)
    - 11-RESEARCH.md Code Examples lines 594-619 (DELETE chaos hook body draft)
    - 11-CONTEXT.md D-07
    - gateway/cmd/gatewayctl/primary.go (verify if `state --json` subcommand exists OR if --json flag is supported; if not, plan task 11-07-04 follow-up or cross-ref 11-04)
  </read_first>
  <acceptance_criteria>
    - test -x scripts/chaos/vast-delete.sh
    - bash -n scripts/chaos/vast-delete.sh exits 0
    - grep -c "VAST_AI_API_KEY" scripts/chaos/vast-delete.sh ≥ 2 (env validation + curl header)
    - grep -c "console.vast.ai/api/v0/instances" scripts/chaos/vast-delete.sh ≥ 1
    - grep -E "primary state --json|jq -e '.instance_id'" scripts/chaos/vast-delete.sh returns ≥ 1 line (JSON-preferred extraction)
    - grep -E "awk '/\\^instance_id:/ \\{print \\$2\\}'|grep .* instance_id" scripts/chaos/vast-delete.sh returns ≥ 1 line (text fallback)
    - grep "idempotent_already_deleted" scripts/chaos/vast-delete.sh returns ≥ 1 line (404 handling)
    - grep -E "\\-\\-connect-timeout 10|\\-\\-max-time 30" scripts/chaos/vast-delete.sh returns ≥ 2 lines (timeout flags)
    - grep -c "OBSERVE-FIRST" scripts/chaos/vast-delete.sh ≥ 2 (instruction block + polling loop tag)
    - grep -E "sleep 5" scripts/chaos/vast-delete.sh returns ≥ 1 line (5s polling interval)
    - grep -cE 'ifix_sk_[a-z0-9]{20,}|Bearer [a-f0-9]{60}' scripts/chaos/vast-delete.sh == 0 (no raw secret literals)
    - Script never logs the value of $VAST_AI_API_KEY (no `echo .*VAST_AI_API_KEY` patterns that print the variable)
  </acceptance_criteria>
  <action>
    Create scripts/chaos/vast-delete.sh per 11-PATTERNS.md vast-delete.sh + Pattern E.

    Required structure (named directives — no fenced code blocks; implementation excerpts live in `<read_first>` files):

    1. Shebang + set -euo pipefail.
    2. log() function to stderr, ISO-8601 + tag [vast-delete]. Never echo $VAST_AI_API_KEY value.
    3. Env validation: VAST_AI_API_KEY required (fail-fast with specific pointer per Pattern E); VAST_BASE defaults to https://console.vast.ai/api/v0; GATEWAY_HOST defaults to n8n-ia-vm. Never log the key value.
    4. [reviews LOW #3] Primary instance ID extraction with JSON output preference:
       - First attempt: invoke `ssh "$GATEWAY_HOST" docker exec ifix-ai-gateway /gatewayctl primary state --json` and parse with `jq -e '.instance_id'`. If jq returns non-null exit 0, capture INSTANCE_ID.
       - Fallback if `--json` flag unsupported (jq fails OR ssh returns non-zero with "unknown flag"): invoke plain `gatewayctl primary state` and parse with `awk '/^instance_id:/ {print $2}'` (exact regex documented in inline comment).
       - Log a WARN line "primary state --json unsupported; using awk text fallback" when falling back.
       - If both yield empty/null, log ERROR "no active primary lifecycle (instance_id empty); aborting" and exit 1.
       - If `--json` truly missing in 11-04 gatewayctl, add follow-up note in script comment referencing 11-04 to ship --json support.
    5. Log "killing primary INSTANCE_ID via Vast API DELETE" (NO Bearer token, NO Vast key value).
    6. [reviews LOW #5] HTTP DELETE with idempotency + timeout + retry:
       - curl -X DELETE -sS --connect-timeout 10 --max-time 30 -w "%{http_code}\n" -o /tmp/vast-delete-resp.json against `${VAST_BASE}/instances/${INSTANCE_ID}/` with Authorization: Bearer $VAST_AI_API_KEY.
       - Status handling:
         - 200 or 204 = killed; log `delete_status: killed`.
         - 404 = idempotent gone; log `delete_status: idempotent_already_deleted` (treat as success per RESEARCH Assumption A1).
         - 5xx = sleep 2; retry once. If second attempt still 5xx, log ERROR + exit 1.
         - Other 4xx (401/403) = log ERROR with status + exit 1.
       - Sanitize response body before logging: `jq -c 'del(.api_key, .token, .ssh_key)'` if jq+JSON parse succeeds; otherwise log "(non-JSON body, suppressed)".
    7. [reviews MEDIUM #1, MEDIUM #2] OBSERVE-FIRST polling loop — header comment block tagged `# OBSERVE-FIRST`:
       - For i in {0..18} (19 samples × 5s = 90s window):
         - t_rel = i*5
         - Poll `/v1/health/upstreams` → capture `local-llm.state`
         - Poll `ssh "$GATEWAY_HOST" docker exec ifix-ai-gateway /gatewayctl primary state` (text or --json) → capture FSM phase
         - Append row to /tmp/observe-window.tsv: `t+${t_rel}s\t${local_llm_state}\t${fsm_phase}`
         - If `local_llm_state == "open"` AND `OPEN_AT` unset, set OPEN_AT=$t_rel
         - sleep 5
       - After loop: print full /tmp/observe-window.tsv to stderr labelled `OBSERVE-FIRST WINDOW [t+0s..t+90s]`.
       - Decision logic:
         - If FSM phase at t+90s is in {Asleep, ProvisioningNew, NewReady, Ready}: set auto_recovery=true; print stderr block "AUTO-RECOVERY OBSERVED — DO NOT run gatewayctl primary force-up".
         - If FSM still in {Draining, Ready (DELETE not yet observed)} AND breaker still OPEN: set auto_recovery=false; print stderr block "MANUAL INTERVENTION PERMITTED at t+90s — operator may now run: gatewayctl primary force-down --reason chaos_cleanup ; gatewayctl primary force-up". Script does NOT auto-invoke force-up — operator decision only.
    8. [reviews HIGH #4] Final Pattern E summary block to stdout (parseable; NO secrets):
       - `delete_status=<killed|idempotent_already_deleted>`
       - `open_at=<seconds_or_null>`
       - `vast_instance_id=<id>` (id only, no token)
       - `observe_window_s=90`
       - `auto_recovery=<true|false>`
       - `fsm_at_t90s=<phase>`
       - `breaker_at_t90s=<closed|half_open|open>`

    Make executable.
  </action>
  <verify>
    <automated>test -x scripts/chaos/vast-delete.sh && bash -n scripts/chaos/vast-delete.sh && grep -E "VAST_AI_API_KEY|console.vast.ai/api/v0/instances|primary state --json|idempotent_already_deleted|--connect-timeout 10|OBSERVE-FIRST|sleep 5" scripts/chaos/vast-delete.sh | wc -l | awk '{if ($1 >= 8) exit 0; else exit 1}' && ! grep -E 'ifix_sk_[a-z0-9]{20,}|Bearer [a-f0-9]{60}' scripts/chaos/vast-delete.sh</automated>
  </verify>
  <done>vast-delete.sh executable + syntax-clean; grep returns ≥8 lines (env validation + Vast endpoint + JSON-preferred extraction + 404 idempotent handling + connect timeout + OBSERVE-FIRST + 5s polling); no raw secret literals; fail-fast on missing VAST_AI_API_KEY; 90s observe-first window with FSM+breaker snapshots before manual intervention; auto_recovery decision logic emitted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-07-02</id>
  <title>Task 2: Execute PRD-02 chaos test mid-load [reviews MEDIUM #1, MEDIUM #2, HIGH #4]</title>
  <read_first>
    - scripts/chaos/vast-delete.sh (this plan task 1 output)
    - 11-CONTEXT.md D-07 + Specifics line 161 ($5 spend cap inherited)
    - 11-RESEARCH.md Pitfall 2 (5-10s blip acceptance) + Architecture Patterns PRD-02 diagram
    - .planning/phases/11-prod-hardening/11-06-EVIDENCE.md (baseline from previous plan)
  </read_first>
  <acceptance_criteria>
    - Pre-flight: `gatewayctl primary state` reports lifecycle Ready + ≥30 min TTL before chaos.
    - load-replay.py started with 600s duration; warm period ≥3 min before chaos trigger; primary stays Ready + breakers closed during warm.
    - vast-delete.sh stdout summary captured to /tmp/vast-delete.log; contains parseable fields: delete_status, open_at, vast_instance_id, observe_window_s=90, auto_recovery, fsm_at_t90s, breaker_at_t90s.
    - [reviews MEDIUM #1] 90s observe window outcome recorded: operator did NOT invoke `gatewayctl primary force-up` before t+90s post-DELETE under any circumstances; if force-up invoked at all, evidence shows it occurred AFTER t+90s with documented FSM state justifying intervention.
    - [reviews MEDIUM #2] Allowed-error-classes histogram extracted from /tmp/chaos-pre.json for window T+0..T+60s:
      - HTTP 503 `upstream_unavailable_transient` count ≤ acceptable per Pitfall 2
      - HTTP 504 `gateway_timeout` count ≤ acceptable (≤30s window)
      - HTTP 503 `upstream_unavailable_for_sensitive_tenant` count documented (RES-08 expected)
      - HTTP 503 `breaker_open` count documented (normal propagation)
      - HTTP 500 panic count == 0 (HARD GATE — fail if > 0)
      - HTTP 502 bad_gateway count == 0 (HARD GATE — fail if > 0)
    - Vast spend incremental ≤ $1 for this plan.
    - Post-chaos: vast instance count for primary lifecycle = 0 (verified via Vast API list call).
    - Primary lifecycle restored to Ready (auto OR via documented force-up after 90s).
    - [reviews HIGH #4] Throughout: no `ifix_sk_...` value pasted into terminal, shell history, evidence, or this plan; tenant keys sourced from env vars at runtime only.
  </acceptance_criteria>
  <what-built>
    Live chaos test against prod. Operator launches a fresh load-replay.py for ~5 min to warm baseline, then triggers vast-delete.sh, observes invisible failover via the script's 90s FSM+breaker polling window [reviews MEDIUM #1], lets test continue ~5 min more, then stops load and inspects allowed-error-classes histogram [reviews MEDIUM #2]. Measures the ~5-10s blip window between DELETE and breaker OPEN as Pitfall 2 expectation. Observe-first hard rule: NO manual force-up before t+90s under any circumstances.
  </what-built>
  <how-to-verify>
    1. Pre-check: `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state'` shows lifecycle Ready + at least 30 min TTL remaining.
    2. [reviews HIGH #4] Set tenant env keys from operator's local secret store. NEVER paste raw `ifix_sk_...` values into this plan, terminal commands typed manually, shell history, or evidence. Use env-var labels only:
       - export IFIX_KEY_CONVERSEAI="..."   # operator sources from local-only ~/.claude/CLAUDE.md or password manager
       - export IFIX_KEY_CHAT_IFIX="..."
       - export IFIX_KEY_CAMPANHAS="..."
       - export VAST_AI_API_KEY="..."        # required for chaos script
       Operator runs `set +o history` before pasting if using shell that records history; runs `set -o history` after.
    3. Start load (10-min run): on ops-claude tmux session "chaos-load", invoke `python scripts/integration-smoke/load-replay.py` with flags `--gateway-url https://ai-gateway.converse-ai.app --fixture /tmp/load-fixture.jsonl --duration 600 --max-concurrency 30 --out /tmp/chaos-pre.json`. Authorization built per-request at runtime from `IFIX_KEY_${TENANT_SLUG^^}` env vars; no tenant-secret in argv. Let warm for ~3 min (verify primary stays Ready + breakers closed via `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state'`).
    4. Trigger chaos (separate terminal): `bash scripts/chaos/vast-delete.sh 2>&1 | tee /tmp/vast-delete.log`. The script auto-runs the 90s polling observation loop AND prints the OBSERVE-FIRST decision block.
    5. [reviews MEDIUM #2] Observe timeline + allowed-error-classes window T+0..T+60s:
       - T+0: DELETE returns 200 OR 404 (idempotent gone — both success)
       - T+5..10: gateway probe loop detects timeout
       - T+10..30: breaker `local-llm` transitions to OPEN (natural observation; script records OPEN_AT)
       - T+30..60: FSM enters Draining; tier-1 OpenRouter starts handling LLM
       - T+60..300: load-replay continues; tier-1 path serves chat completions
       Allowed-error-classes table (HARD GATES marked):

       | Class | T+0..T+60s | Verdict |
       |-------|-----------|---------|
       | HTTP 503 `upstream_unavailable_transient` | permitted up to 60s | acceptable |
       | HTTP 504 `gateway_timeout` | permitted up to 30s | acceptable |
       | HTTP 503 `upstream_unavailable_for_sensitive_tenant` | permitted any time | expected (RES-08) |
       | HTTP 503 `breaker_open` | permitted while propagating | acceptable |
       | HTTP 500 panic | 0 required | HARD GATE — regression if > 0 |
       | HTTP 502 bad_gateway | 0 required | HARD GATE — upstream connection corruption if > 0 |

    6. While chaos active: monitor `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state'` every 30s — expect Draining → Asleep → ProvisioningNew transition. Cross-reference with script's /tmp/observe-window.tsv FSM snapshots.
    7. [reviews MEDIUM #1] OBSERVE-FIRST cleanup — HARD RULE: WAIT until t+90s post-DELETE before any manual intervention. The script's polling window does this automatically; operator does NOT type any gatewayctl commands during the 90s. At t+90s, read the script's AUTO-RECOVERY decision block:
       - If script printed "AUTO-RECOVERY OBSERVED": controller is reconciling correctly; do NOT run force-up; record `auto_recovery: true` in evidence and let it self-recover.
       - If script printed "MANUAL INTERVENTION PERMITTED": operator may now run `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary force-down --reason chaos_cleanup'` then `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary force-up'`. Record `manual_intervention: true` with t+90s+Δ timestamp in evidence.
       Record the t+90s FSM state + breaker state + decision in EVIDENCE.md verbatim from script output.
    8. Stop load-replay (Ctrl+C) once chaos has run ~5 min after DELETE OR let it complete the 600s duration.
    9. Inspect /tmp/chaos-pre.json: extract per-request error histogram for window [start, start+60s after DELETE]. Use jq query like `[.requests[] | select(.timestamp_unix >= ($DELETE_UNIX) and .timestamp_unix <= ($DELETE_UNIX + 60)) | {status, error_class}] | group_by(.status, .error_class) | map({status: .[0].status, class: .[0].error_class, count: length})`. Save histogram to /tmp/chaos-error-classes.json.
    10. Verify HARD GATES from histogram: `jq '[.[] | select(.status == 500)] | length' /tmp/chaos-error-classes.json` MUST return 0; `jq '[.[] | select(.status == 502 and .class == "bad_gateway")] | length' /tmp/chaos-error-classes.json` MUST return 0. If either ≠ 0: chaos FAILED; record as regression in evidence with full error sample (sanitized — no headers, no request bodies).
    11. Verify primary back to Ready: `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state'` shows lifecycle in provisioning/ready cycle.
    12. Verify Vast instance count: `curl -sS -H "Authorization: Bearer $VAST_AI_API_KEY" https://console.vast.ai/api/v0/instances/ | jq '[.instances[] | select(.id == '"$VAST_INSTANCE_ID"')] | length'` MUST return 0 (or 404 from list — both valid).
    13. Capture Vast spend (this run + force-up new pod): operator notes in 11-07-EVIDENCE.md.
    14. [reviews HIGH #4] Before closing checkpoint: `set +o history` clear any temp env var traces; verify shell history does not contain `ifix_sk_` or 60-char hex strings via `history | grep -cE 'ifix_sk_|Bearer [a-f0-9]{60}'` returns 0.

    Abort criteria:
    - Vast spend exceeds $1 incremental for this plan.
    - Gateway returns ≥1 HTTP 500 panic OR ≥1 HTTP 502 bad_gateway during T+0..T+60s window (HARD GATE).
    - FSM stuck Draining > 10 min even after force-down+force-up.
    - Operator detects raw secret leak into terminal/history/evidence (purge, rotate key, document).
  </how-to-verify>
  <resume-signal>Type "chaos PRD-02 PASS — DELETE→OPEN=X.Xs; tier-1 took over; 90s observe outcome={auto_recovery|manual_intervention at t+Ns}; zero 500 + zero 502 in T+0..T+60s histogram; spend=$Y.YY; primary restored; no secret leak" or describe any HARD GATE failure or cleanup gap.</resume-signal>
</task>

<task type="auto">
  <id>11-07-03</id>
  <title>Task 3: Commit chaos evidence + sanitization grep gate [reviews HIGH #4]</title>
  <files>.planning/phases/11-prod-hardening/11-07-EVIDENCE.md</files>
  <read_first>
    - Output of task 11-07-02 (operator timeline + metrics + 90s window TSV)
    - /tmp/vast-delete.log + /tmp/chaos-pre.json + /tmp/chaos-error-classes.json + /tmp/observe-window.tsv
    - 11-CONTEXT.md D-07
  </read_first>
  <acceptance_criteria>
    - .planning/phases/11-prod-hardening/11-07-EVIDENCE.md exists.
    - File contains required sections: Header, Metadata, Timeline table, 90s observation window snapshot table, Allowed-error-classes histogram, SLO verdict, Cleanup confirmation, Cross-refs, Sign-off, Sanitization attestation.
    - [reviews HIGH #4] Pre-commit grep gate: `grep -rE 'ifix_sk_[a-z0-9]{20,}|Bearer [a-f0-9]{60}|postgres://[^@]+@' .planning/phases/11-prod-hardening/11-07-EVIDENCE.md .planning/phases/11-prod-hardening/11-07-PLAN.md` MUST return zero matches (exit 1 if any match).
    - Timeline includes T+0, T+OPEN_AT, T+FSM_Draining, T+90s observation outcome, T+restore.
    - 90s observation window table reproduced from /tmp/observe-window.tsv (19 rows × {t+Ns, local_llm_state, fsm_phase}).
    - Allowed-error-classes histogram shows: HTTP 500 panic count = 0 (or explicit regression flag); HTTP 502 bad_gateway count = 0 (or explicit regression flag).
    - auto_recovery vs manual_intervention flag recorded with timestamp justification.
    - Vast cleanup attestation: instance count = 0 post-DELETE.
    - Sign-off line with operator + date.
    - git commit message: "docs(11-07): PRD-02 chaos primary kill evidence".
  </acceptance_criteria>
  <action>
    Author .planning/phases/11-prod-hardening/11-07-EVIDENCE.md with sections:

    1. Header: `# Phase 11 PRD-02 Chaos: Primary Kill Live UAT`
    2. Metadata: date, operator name, target gateway (`ai-gateway.converse-ai.app`), Vast instance killed (id only, no token), Vast spend total $USD.
    3. Timeline (UTC HH:MM:SS) — table with columns Time | Event | Source:
       - T+0 | curl DELETE → 200 OR 404 idempotent | vast-delete.sh log
       - T+OPEN_AT s | local-llm.state observed OPEN (natural) | vast-delete.sh observation loop
       - T+Ys | FSM Draining | gatewayctl primary state
       - T+Zs | tier-1 first chat completion via OpenRouter | gateway audit_log row (request_id only — NO body)
       - T+90s | OBSERVE-FIRST outcome: FSM = {Asleep \| Draining \| ProvisioningNew \| Ready} + breaker = {open \| half_open \| closed} + decision = {auto_recovery \| manual_intervention} | vast-delete.sh AUTO-RECOVERY block
       - T+Ws (only if manual cleanup occurred AFTER t+90s) | force-down + force-up issued | operator log
    4. [reviews MEDIUM #1] 90-second observation window snapshot — full table reproduced from /tmp/observe-window.tsv:
       | t_rel | local_llm.state | FSM phase |
       (19 rows × 5s interval from t+0s through t+90s)
       Plus decision row: `t+90s decision: auto_recovery={true|false}; manual_intervention={true|false}; justification: <text>`.
    5. [reviews MEDIUM #2] Allowed-error-classes histogram T+0..T+60s — table from /tmp/chaos-error-classes.json:

       | Status | Error class | Count | Verdict |
       |--------|-------------|-------|---------|
       | 503 | upstream_unavailable_transient | N | permitted (≤60s window) |
       | 504 | gateway_timeout | N | permitted (≤30s window) |
       | 503 | upstream_unavailable_for_sensitive_tenant | N | expected (RES-08) |
       | 503 | breaker_open | N | permitted |
       | 500 | panic | 0 | HARD GATE required — pass/fail |
       | 502 | bad_gateway | 0 | HARD GATE required — pass/fail |

    6. SLO verdict per D-04: P95 chat during full window vs after-cutover window. May exceed P95 budget in first 60s post-DELETE — document as expected per Pitfall 2.
    7. Cleanup confirmation: vast instance count = 0 post-DELETE (jq output); primary lifecycle recovered (self OR via manual force-up at t+90s+Δ).
    8. Deviations: anything outside Pitfall 2 expected blip (track in RUNBOOK-INCIDENTS class 1 section).
    9. Cross-ref: scripts/chaos/vast-delete.sh, /tmp/chaos-pre.json (operator-archived off-repo), /tmp/observe-window.tsv (operator-archived), 11-06-EVIDENCE.md.
    10. Sign-off: operator + date.
    11. [reviews HIGH #4] Sanitization attestation block:
        > Attestation: this evidence file contains NO raw API keys, NO Bearer token values, NO Authorization header values, NO request bodies, NO response bodies, NO DSNs, NO PII. Tenant references use env-var labels (e.g. `${IFIX_KEY_CONVERSEAI}`) or first-4-char prefix only. Vast API key referenced as `${VAST_AI_API_KEY}`. Pre-commit grep gate passed: `grep -rE 'ifix_sk_[a-z0-9]{20,}|Bearer [a-f0-9]{60}|postgres://[^@]+@' 11-07-EVIDENCE.md 11-07-PLAN.md` returned zero matches.

    Pre-commit grep gate (MUST pass before `git add`):
    - `! grep -rE 'ifix_sk_[a-z0-9]{20,}|Bearer [a-f0-9]{60}|postgres://[^@]+@' .planning/phases/11-prod-hardening/11-07-EVIDENCE.md .planning/phases/11-prod-hardening/11-07-PLAN.md`

    If grep matches: redact (replace with env-var label or first-4-char prefix) and re-run grep until zero matches. THEN `git add` + `git commit -m "docs(11-07): PRD-02 chaos primary kill evidence"`.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/11-07-EVIDENCE.md && ! grep -rE 'ifix_sk_[a-z0-9]{20,}|Bearer [a-f0-9]{60}|postgres://[^@]+@' .planning/phases/11-prod-hardening/11-07-EVIDENCE.md .planning/phases/11-prod-hardening/11-07-PLAN.md && grep -c "Allowed-error-classes\|allowed-error-classes\|HARD GATE" .planning/phases/11-prod-hardening/11-07-EVIDENCE.md | awk '{if ($1 >= 2) exit 0; else exit 1}' && grep -c "OBSERVE-FIRST\|observation window\|auto_recovery\|manual_intervention" .planning/phases/11-prod-hardening/11-07-EVIDENCE.md | awk '{if ($1 >= 2) exit 0; else exit 1}'</automated>
  </verify>
  <done>Evidence committed; no raw secret strings in plan or evidence (grep gate passed); timeline + 90s observation window table + allowed-error-classes histogram with HARD GATE verdicts + auto_recovery/manual_intervention flag + cleanup confirmation + sanitization attestation all present; cross-refs to load-test baseline and chaos script.</done>
</task>

</tasks>

<verification>
- bash -n scripts/chaos/vast-delete.sh exits 0.
- grep "console.vast.ai/api/v0" scripts/chaos/vast-delete.sh returns ≥1.
- grep "primary state --json" scripts/chaos/vast-delete.sh returns ≥1 (JSON output preference wired) [reviews LOW #3].
- grep "idempotent_already_deleted" scripts/chaos/vast-delete.sh returns ≥1 (404 handling) [reviews LOW #5].
- grep "--connect-timeout 10" scripts/chaos/vast-delete.sh returns ≥1 (timeout flag) [reviews LOW #5].
- grep "OBSERVE-FIRST" scripts/chaos/vast-delete.sh returns ≥2 (instruction block + polling tag) [reviews MEDIUM #1].
- 11-07-EVIDENCE.md committed.
- Vast spend within $1 incremental cap for this plan.
- Primary FSM restored to Ready post-chaos (auto OR via manual force-up AFTER 90s) [reviews MEDIUM #1].
- Zero HTTP 500 panic during T+0..T+60s chaos window (HARD GATE) [reviews MEDIUM #2].
- Zero HTTP 502 bad_gateway during T+0..T+60s chaos window (HARD GATE) [reviews MEDIUM #2].
- Zero `ifix_sk_...` literals + zero `Bearer [a-f0-9]{60}` literals in plan or evidence (pre-commit grep gate) [reviews HIGH #4].
</verification>

<success_criteria>
- PRD-02 invisible-failover proved: tier-1 took over, P95 stayed within SLO (operator-acceptable per Pitfall 2 expectations).
- vast-delete.sh shipped with JSON-preferred extraction + text fallback [reviews LOW #3] + Vast DELETE idempotency + timeout/retry [reviews LOW #5] + 90s OBSERVE-FIRST polling window [reviews MEDIUM #1].
- Allowed-error-classes window honoured (zero 500 panic, zero 502 bad_gateway HARD GATES) [reviews MEDIUM #2].
- 90s observation window prevented operator from racing the controller (auto_recovery vs manual_intervention recorded) [reviews MEDIUM #1].
- Evidence canonical example for RUNBOOK-INCIDENTS class 1 (11-09 ingests).
- Zero raw secrets anywhere in plan or evidence (pre-commit grep gate passed) [reviews HIGH #4].
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies — bash + curl + jq already present. (Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.)
- Truth 3: Chaos script has idempotent cleanup advice + 90s OBSERVE-FIRST window with polled FSM/breaker snapshots before manual intervention.
- Truth 4: Vast API key via env var; never literal in shell history; --connect-timeout 10 --max-time 30 + single 5xx retry.
- Truth 5: Live UAT plan autonomous=false; operator runs + observes.
- Truth 6: gateway image stays at :latest-dev until D-18.4 retrigger.
- Truth 8 (cross-cutting) [reviews HIGH #4]: NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in plan instructions or evidence; tenant references via env-var labels (e.g. `${IFIX_KEY_CONVERSEAI}`) or first-4-char prefix only; pre-commit grep gate `grep -rE 'ifix_sk_|Bearer [a-f0-9]{60}'` enforced.
- Truth 9 (cross-cutting) [reviews MEDIUM #2]: Allowed-error-classes window T+0..T+60s post-DELETE: 503 transient + 504 timeout + 503 sensitive_block + 503 breaker_open permitted; 500 panic + 502 bad_gateway = HARD GATES (zero tolerance).
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-07-SUMMARY.md with: vast-delete.sh size, JSON-preference wired (yes/no), 404 idempotency wired (yes/no), 90s OBSERVE-FIRST wired (yes/no), timeline summary, gates verdict (allowed-error-classes HARD GATE pass/fail), auto_recovery vs manual_intervention outcome, spend total, sanitization attestation, link to 11-07-EVIDENCE.md.
</output>
