---
phase: 11
plan: 11-07
slug: chaos-kill-primary-live-uat
wave: 2
depends_on: [11-06]
requirements: [PRD-02]
files_modified:
  - path: scripts/chaos/vast-delete.sh
    kind: NEW
  - path: .planning/phases/11-prod-hardening/11-07-EVIDENCE.md
    kind: NEW
autonomous: false
estimated_duration: 75
---

<objective>
Execute PRD-02 chaos test: mid-load Vast API DELETE raw of the active primary lifecycle's instance. Gateway must discover failure via natural probe timeout (NOT operator-driven force-open); breaker `local-llm` opens by observation; FSM advances Ready -> Draining -> Asleep; tier-1 OpenRouter takes LLM traffic. Measure invisible-failover SLO: P95 within D-04 budget, error rate <1% non-503, zero 5xx panic. Ship the chaos hook script + evidence. Spend ~$0.30 (primary re-up cycle post-chaos).
</objective>

<must_haves>
  truths:
    - "D-07: PRD-02 kill primary = Vast API DELETE raw (curl -X DELETE Bearer); host yank realistic"
    - "Natural breaker observation (NOT force-open); FSM Ready -> Draining -> Asleep auto"
    - "Pitfall 2: invisible-failover SLO acknowledges a 5-10s blip window between DELETE and breaker observation — 503 transient acceptable, 5xx panic NOT"
    - "Cleanup mandatory: Vast instance destroyed; primary force-up to restore Ready state for next operator"
    - "Pattern E bash skeleton: set -euo pipefail + ISO-8601 log() + env validation + idempotent"
  must_haves:
    - scripts/chaos/vast-delete.sh exists, executable, runs from ops-claude or any host with $VAST_AI_API_KEY env
    - 11-07-EVIDENCE.md committed with: timeline (UTC), DELETE response code, time-to-breaker-OPEN, P95 during chaos window, error rate, Vast cleanup confirmation
    - post-chaos primary state restored (gatewayctl primary force-up succeeded; FSM=Ready again) OR plan to restore documented
  key_links:
    - from: scripts/chaos/vast-delete.sh
      to: https://console.vast.ai/api/v0/instances/{id}/
      via: curl -X DELETE -H "Authorization: Bearer $VAST_AI_API_KEY"
      pattern: "console.vast.ai/api/v0/instances"
    - from: chaos script polling
      to: https://ai-gateway.converse-ai.app/v1/health/upstreams
      via: curl + jq local-llm.state
      pattern: "local-llm.state"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude -> Vast API | Bearer token from $VAST_AI_API_KEY env (never literal) |
| chaos timing window | 5-10s between DELETE 200 and breaker observation OPEN — 503 transient window expected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-CHAOS-01 | Denial of Service | Vast pod orphaned + billing continues | mitigate | Hard exit-on-error in vast-delete.sh; retry once on 5xx; explicit verification of vast instance count == 0 post-DELETE; cleanup task includes "if FSM stuck Draining, manual destroy via gatewayctl primary force-down --reason chaos_cleanup" |
| T-11-CHAOS-02 | Information Disclosure | Vast API key in shell history | mitigate | curl uses -H "Authorization: Bearer $VAST_AI_API_KEY" (env var); never inline literal; script asserts env present and fails fast if absent |
| T-11-CHAOS-03 | Denial of Service | Chaos triggers cascade affecting other tenants beyond intended SLO budget | mitigate | Pitfall 2 sets expectation: ~5-10s blip window; if errors exceed budget operator documents in EVIDENCE.md as regression candidate (NOT chaos-expected blip) |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@.planning/phases/11-prod-hardening/11-06-PLAN.md
@pod/scripts/vast-ai.sh
</context>

<tasks>

<task type="auto">
  <id>11-07-01</id>
  <title>Task 1: Author scripts/chaos/vast-delete.sh</title>
  <files>scripts/chaos/vast-delete.sh</files>
  <read_first>
    - pod/scripts/vast-ai.sh (auth + api helper pattern lines 14-37; Pattern E + Bearer auth)
    - scripts/deploy/bootstrap-postgres.sh (set/log/env-validation skeleton — Pattern E)
    - 11-PATTERNS.md vast-delete.sh (Pattern E body + Vast DELETE shape + cleanup advice)
    - 11-RESEARCH.md Pattern 4 + System Architecture Diagram PRD-02 path
    - 11-RESEARCH.md Code Examples lines 594-619 (DELETE chaos hook body draft)
    - 11-CONTEXT.md D-07
  </read_first>
  <action>
    Create scripts/chaos/vast-delete.sh per 11-PATTERNS.md vast-delete.sh + Pattern E:

    Required structure:
    1. Shebang + set -euo pipefail.
    2. log() function to stderr, ISO-8601 + tag [vast-delete].
    3. Env validation: : "${VAST_AI_API_KEY:?missing}" + VAST_BASE default https://console.vast.ai/api/v0. Optionally check ssh n8n-ia-vm connectivity (ssh n8n-ia-vm echo ok).
    4. Resolve primary instance id from gateway: INSTANCE_ID=$(ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state' | grep -oP 'vast_instance_id=\K\d+'). If empty, log "no active primary lifecycle; aborting" and exit 1.
    5. Log "killing primary INSTANCE_ID via Vast API DELETE" (no Bearer token in log).
    6. HTTP DELETE: HTTP=$(curl -s -o /tmp/vast-delete.json -w '%{http_code}' -X DELETE "${VAST_BASE}/instances/${INSTANCE_ID}/" -H "Authorization: Bearer ${VAST_AI_API_KEY}"). Per RESEARCH Assumption A1: treat 200 = killed, 404 = idempotent gone (already destroyed), 5xx = retry once with 2s sleep. Other status -> log + exit 1.
    7. Log "DELETE result: status=$HTTP body=$(cat /tmp/vast-delete.json)" (sanitize: jq -r 'del(.api_key, .token)' if present).
    8. Poll gateway upstream state every 5s up to 60s waiting for local-llm.state in {"open"} (NOT force-open — natural observation): END=$(($(date +%s) + 60)); loop curl https://ai-gateway.converse-ai.app/v1/health/upstreams | jq -r '.upstreams["local-llm"].state'. Log each tick. On natural OPEN observed, log "natural-OPEN observed; chaos active" and exit 0. On timeout, log "WARN: breaker did not OPEN within 60s — gateway may be probing on longer interval; advancing anyway" and exit 0 with note.
    9. Cleanup advice (printed via final summary block): if FSM stuck in Draining > 5 min, run gatewayctl primary force-down --reason chaos_cleanup; to restore service afterward, gatewayctl primary force-up.

    Apply Pattern E summary block at end (stdout = parseable summary; stderr = log). Make executable.
  </action>
  <verify>
    <automated>test -x scripts/chaos/vast-delete.sh && bash -n scripts/chaos/vast-delete.sh && grep -E "set -euo pipefail|VAST_AI_API_KEY:?missing|console.vast.ai/api/v0/instances|Authorization: Bearer|local-llm" scripts/chaos/vast-delete.sh | wc -l | awk '{if ($1 >= 5) exit 0; else exit 1}'</automated>
  </verify>
  <done>vast-delete.sh executable + syntax-clean; grep passes for 5+ required patterns; running without VAST_AI_API_KEY exits with fail-fast pointer; never includes literal Bearer token; cleanup advice present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-07-02</id>
  <title>Task 2: Execute PRD-02 chaos test mid-load</title>
  <read_first>
    - scripts/chaos/vast-delete.sh (this plan task 1 output)
    - 11-CONTEXT.md D-07 + Specifics line 161 ($5 spend cap inherited)
    - 11-RESEARCH.md Pitfall 2 (5-10s blip acceptance) + Architecture Patterns PRD-02 diagram
    - .planning/phases/11-prod-hardening/11-06-EVIDENCE.md (baseline from previous plan)
  </read_first>
  <what-built>
    Live chaos test against prod. Operator launches a fresh load-replay.py for ~5 min to warm baseline, then triggers vast-delete.sh, observes invisible failover, lets test continue ~5 min more, then stops load and inspects metrics. Measures the ~5-10s blip window between DELETE and breaker OPEN as Pitfall 2 expectation.
  </what-built>
  <how-to-verify>
    1. Pre-check: ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state' shows lifecycle Ready + at least 30 min TTL remaining.
    2. Start load (10-min run, smaller fixture window): on ops-claude tmux "chaos-load": python scripts/integration-smoke/load-replay.py --gateway-url https://ai-gateway.converse-ai.app --fixture /tmp/load-fixture.jsonl --duration 600 --max-concurrency 30 --out /tmp/chaos-pre.json. Let it warm for ~3 min (verify primary stays Ready + breakers closed).
    3. Trigger chaos (separate terminal): bash scripts/chaos/vast-delete.sh. Capture stdout/stderr to /tmp/vast-delete.log.
    4. Observe timeline:
       - T+0: DELETE returns 200
       - T+5..10: gateway probe loop detects timeout
       - T+10..30: breaker local-llm transitions to OPEN (natural observation)
       - T+30..60: FSM enters Draining; tier-1 OpenRouter starts handling LLM
       - T+60..300: load-replay continues; tier-1 path serves chat completions
    5. While chaos active: monitor ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state' every 30s — expect Draining -> Asleep transition.
    6. Stop load-replay (Ctrl+C) once chaos has run ~5 min after DELETE OR let it complete the 600s duration.
    7. Inspect /tmp/chaos-pre.json: jq '.summary, .gates' /tmp/chaos-pre.json. Expected: gates may show error_rate_lt_1pct=false if blip 5xx exceeded budget (operator documents as expected per Pitfall 2 if total > zero); zero_5xx_panic MUST be true (panic = real regression).
    8. Run cleanup: gatewayctl primary force-up to restore Ready state for next operator. Verify ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state' shows lifecycle in provisioning/ready cycle.
    9. Capture Vast spend (this run + force-up new pod): operator notes in 11-07-EVIDENCE.md.

    Abort criteria:
    - Vast spend exceeds $1 incremental for this plan.
    - Gateway returns 5xx panic during chaos window (NOT 503 transient — that's expected).
    - FSM stuck Draining > 10 min.
  </how-to-verify>
  <resume-signal>Type "chaos PRD-02 PASS — DELETE→OPEN=X.Xs; tier-1 took over; spend=$Y.YY; primary restored" or describe any 5xx panic or cleanup gap.</resume-signal>
</task>

<task type="auto">
  <id>11-07-03</id>
  <title>Task 3: Commit chaos evidence</title>
  <files>.planning/phases/11-prod-hardening/11-07-EVIDENCE.md</files>
  <read_first>
    - Output of task 11-07-02 (operator timeline + metrics)
    - /tmp/vast-delete.log + /tmp/chaos-pre.json
    - 11-CONTEXT.md D-07
  </read_first>
  <action>
    Author .planning/phases/11-prod-hardening/11-07-EVIDENCE.md with sections:
    - Header: # Phase 11 PRD-02 Chaos: Primary Kill Live UAT
    - Metadata: date, operator name, target gateway, Vast instance killed (id), Vast spend total $USD.
    - Timeline (UTC HH:MM:SS):
      | Time | Event | Source |
      | T+0 | curl DELETE → 200 | vast-delete.sh log |
      | T+Xs | local-llm.state observed OPEN (natural) | health-poll loop |
      | T+Ys | FSM Draining | gatewayctl primary state |
      | T+Zs | tier-1 first chat completion via OpenRouter | gateway audit_log row |
    - Metrics during chaos window: error_count by status_code; 503 count (expected); 5xx panic count (must be 0).
    - SLO verdict per D-04: P95 chat during full window vs after-cutover window. May exceed P95 budget in first 60s post-DELETE — document expected per Pitfall 2.
    - Cleanup confirmation: vast instance count = 0 post-DELETE OR force-up new pod state.
    - Deviations: anything outside Pitfall 2 expected blip (track in RUNBOOK-INCIDENTS class 1 section).
    - Cross-ref: scripts/chaos/vast-delete.sh, /tmp/chaos-pre.json (operator-archived), 11-06-EVIDENCE.md.
    - Sign-off: operator + date.

    git add + commit with "docs(11-07): PRD-02 chaos primary kill evidence". Strip secret strings (grep test before commit).
  </action>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/11-07-EVIDENCE.md && ! grep -E "sk_[a-z]+_|Bearer [A-Za-z0-9_]+|postgres://" .planning/phases/11-prod-hardening/11-07-EVIDENCE.md</automated>
  </verify>
  <done>Evidence committed; no secret strings present; timeline + SLO verdict + cleanup confirmation documented; cross-refs to load-test baseline and chaos script.</done>
</task>

</tasks>

<verification>
- bash -n scripts/chaos/vast-delete.sh exits 0.
- grep "console.vast.ai/api/v0" scripts/chaos/vast-delete.sh returns 1.
- 11-07-EVIDENCE.md committed.
- Vast spend within $1 incremental cap for this plan.
- Primary FSM restored to Ready post-chaos OR explicit operator-restore plan in evidence.
- Zero 5xx panic during chaos window (hard gate; differentiated from 503 transient blip).
</verification>

<success_criteria>
- PRD-02 invisible-failover proved: tier-1 took over, P95 stayed within SLO (operator-acceptable per Pitfall 2 expectations).
- vast-delete.sh shipped with idempotent cleanup advice.
- Evidence canonical example for RUNBOOK-INCIDENTS class 1 (11-09 ingests).
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies — bash + curl + jq already present. (Dashboard 2FA UI adds 4 packages scoped to 11-02 only.)
- Truth 3: Chaos script has idempotent cleanup advice + mandatory cleanup task in same plan.
- Truth 4: Vast API key via env var; never literal in shell history.
- Truth 5: Live UAT plan autonomous=false; operator runs + observes.
- Truth 6: gateway image stays at :latest-dev until D-18.4 retrigger.
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-07-SUMMARY.md with: vast-delete.sh size, timeline summary, gates verdict, spend total, link to 11-07-EVIDENCE.md.
</output>
