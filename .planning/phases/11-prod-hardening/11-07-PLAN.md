---
phase: 11
plan: 11-07
slug: chaos-kill-primary-live-uat
wave: 2
depends_on: [11-06]
requirements: [PRD-02]
files_modified:
  - path: scripts/chaos/vast-delete.sh
    kind: NEW
  - path: .planning/phases/11-prod-hardening/11-07-EVIDENCE.md
    kind: NEW
autonomous: false
estimated_duration: 90
---

<objective>
Execute PRD-02 chaos test: mid-load Vast API DELETE raw of the active primary lifecycle's instance. Gateway must discover failure via natural probe timeout (NOT operator-driven force-open). Breaker `local-llm` opens by observation; FSM advances Ready → Draining → Asleep; tier-1 OpenRouter takes LLM traffic. Measure invisible-failover SLO with defined allowed-error-classes during the first 60s post-DELETE (reviews MEDIUM). Cleanup: observe reconciliation for a fixed 90-second window BEFORE manual `force-up` (reviews MEDIUM — avoid fighting the controller). Ship the chaos hook script (which prefers `gatewayctl primary state --json` if subcommand supports it — reviews LOW) + evidence. Spend ~$0.30 (primary re-up cycle post-chaos). All tenant API keys referenced via env-var labels — zero `ifix_sk_...` literals in plan instructions or evidence (reviews HIGH).
</objective>

<must_haves>
  truths:
    - "D-07: PRD-02 kill primary = Vast API DELETE raw (curl -X DELETE Bearer); host yank realistic"
    - "Natural breaker observation (NOT force-open); FSM Ready → Draining → Asleep auto"
    - "Pitfall 2: invisible-failover SLO acknowledges a 5-10s blip window between DELETE and breaker observation — 503 transient acceptable, 5xx panic NOT"
    - "Allowed error classes during T+0..T+60s (reviews MEDIUM): HTTP 503 `upstream_unavailable_for_sensitive_tenant` AND HTTP 503 `breaker_open` PERMITTED; HTTP 500 panic NOT permitted; HTTP 502 bare proxy NOT permitted"
    - "Cleanup observe-first (reviews MEDIUM): operator waits a fixed 90-second reconciliation window BEFORE invoking gatewayctl primary force-up; only intervenes if FSM is stuck Draining > 90s"
    - "JSON output preference (reviews LOW): the script tries `gatewayctl primary state --json` first; if subcommand returns non-zero or unsupported flag, falls back to grep-based extraction with a logged warning"
    - "Cross-cutting hard rule: NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in plan instructions or evidence. Tenant references via env-var labels (e.g. ${TENANT_CONVERSEAI_KEY_ENV}) only; evidence redacts to first-4-char prefix"
    - "Pattern E bash skeleton: set -euo pipefail + ISO-8601 log() + env validation + idempotent"
  must_haves:
    - scripts/chaos/vast-delete.sh exists, executable, runs from ops-claude or any host with $VAST_AI_API_KEY env
    - vast-delete.sh tries `gatewayctl primary state --json` first; documents fallback to text-grep extraction
    - 11-07-EVIDENCE.md committed with: timeline (UTC), DELETE response code, time-to-breaker-OPEN, P95 during chaos window, error-class breakdown (503 categories AND any 5xx panic count), 90-second observe window outcome, Vast cleanup confirmation
    - post-chaos primary state restored (gatewayctl primary force-up only AFTER 90s observation) OR plan to restore documented
    - Zero `ifix_sk_...` literals in plan or evidence; tenant references use env-var labels
  key_links:
    - from: scripts/chaos/vast-delete.sh
      to: https://console.vast.ai/api/v0/instances/{id}/
      via: curl -X DELETE -H "Authorization: Bearer $VAST_AI_API_KEY"
      pattern: "console.vast.ai/api/v0/instances"
    - from: chaos script polling
      to: https://ai-gateway.converse-ai.app/v1/health/upstreams
      via: curl + jq local-llm.state
      pattern: "local-llm.state"
    - from: scripts/chaos/vast-delete.sh primary state extraction
      to: gatewayctl primary state --json (preferred) OR text parse (fallback)
      via: subprocess invocation
      pattern: "primary state.*--json|primary state"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude -> Vast API | Bearer token from $VAST_AI_API_KEY env (never literal) |
| chaos timing window | 5-10s between DELETE 200 and breaker observation OPEN — 503 transient window expected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-CHAOS-01 | Denial of Service | Vast pod orphaned + billing continues | mitigate | Hard exit-on-error in vast-delete.sh; retry once on 5xx; explicit verification of vast instance count == 0 post-DELETE; cleanup task includes "if FSM stuck Draining > 90s, run gatewayctl primary force-down --reason chaos_cleanup" |
| T-11-CHAOS-02 | Information Disclosure | Vast API key in shell history | mitigate | curl uses -H "Authorization: Bearer $VAST_AI_API_KEY" (env var); never inline literal; script asserts env present and fails fast if absent |
| T-11-CHAOS-03 | Denial of Service | Chaos triggers cascade affecting other tenants beyond intended SLO budget | mitigate | Pitfall 2 sets expectation: ~5-10s blip window; if errors exceed budget operator documents in EVIDENCE.md as regression candidate (NOT chaos-expected blip) |
| T-11-CHAOS-06 | Information Disclosure | tenant API keys appear in plan or evidence | mitigate | Cross-cutting hard rule — env-var label placeholders only; evidence redacts to first-4-char prefix; pre-commit grep gate |
| T-11-CHAOS-07 | Tampering | manual force-up fights the reconciler causing FSM oscillation | mitigate | Observe-first sequence: 90-second fixed window before any manual intervention; intervention only if FSM Draining > 90s (reviews MEDIUM) |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@.planning/phases/11-prod-hardening/11-06-PLAN.md
@pod/scripts/vast-ai.sh
</context>

<tasks>

<task type="auto">
  <id>11-07-01</id>
  <title>Task 1: Author scripts/chaos/vast-delete.sh (JSON output preference + observe-first)</title>
  <files>scripts/chaos/vast-delete.sh</files>
  <read_first>
    - pod/scripts/vast-ai.sh (auth + api helper pattern lines 14-37; Pattern E + Bearer auth)
    - scripts/deploy/bootstrap-postgres.sh (set/log/env-validation skeleton — Pattern E)
    - 11-PATTERNS.md vast-delete.sh (Pattern E body + Vast DELETE shape + cleanup advice)
    - 11-RESEARCH.md Pattern 4 + System Architecture Diagram PRD-02 path
    - 11-RESEARCH.md Code Examples lines 594-619 (DELETE chaos hook body draft)
    - 11-CONTEXT.md D-07
    - gateway/cmd/gatewayctl/primary.go (verify if `state --json` subcommand exists OR if --json flag is supported)
  </read_first>
  <action>
    Create scripts/chaos/vast-delete.sh per 11-PATTERNS.md vast-delete.sh + Pattern E.

    Required structure (named directives — implementation code excerpts in `<read_first>` files):

    1. Shebang + set -euo pipefail.
    2. log() function to stderr, ISO-8601 + tag [vast-delete].
    3. Env validation: VAST_AI_API_KEY required (fail-fast with specific pointer per Pattern E); VAST_BASE defaults to https://console.vast.ai/api/v0; GATEWAY_HOST defaults to n8n-ia-vm. Never log the key value.
    4. Primary instance ID extraction with JSON output preference (reviews LOW):
       - First attempt: invoke `gatewayctl primary state --json` over ssh; parse with jq `.vast_instance_id`.
       - Fallback if the --json flag is not supported or returns empty: invoke plain `gatewayctl primary state` and grep with the regex `vast_instance_id=\K\d+`.
       - If both yield empty, log "no active primary lifecycle (instance_id empty); aborting" and exit 1.
    5. Log "killing primary INSTANCE_ID via Vast API DELETE" (no Bearer token, no Vast key value).
    6. HTTP DELETE: curl -X DELETE against `${VAST_BASE}/instances/${INSTANCE_ID}/` with Authorization: Bearer $VAST_AI_API_KEY. Status handling per RESEARCH Assumption A1: 200 = killed, 404 = idempotent gone, 5xx = retry once with 2s sleep, else log + exit 1.
    7. Log result, sanitizing the body with `jq -c 'del(.api_key, .token)'` if present.
    8. NATURAL observation loop: poll `/v1/health/upstreams` every 5s for up to 90s. Record the relative timestamp at which `local-llm.state == "open"` was first observed. If 90s elapse without OPEN, log a WARN with the run-on-longer-probe-interval explanation and continue.
    9. Observe-first reconciliation window (reviews MEDIUM) — print a clear stderr instruction block stating: the controller has up to 90 seconds to advance FSM Draining → Asleep → ProvisioningNew; DO NOT run `gatewayctl primary force-up` before t+90s post-DELETE; only if FSM is still Draining at t+90s does the operator manually invoke `gatewayctl primary force-down --reason chaos_cleanup` then `force-up`; if FSM is already Asleep or ProvisioningNew by t+90s, the controller is healthy and no manual intervention is needed.
    10. Final Pattern E summary block to stdout (parseable): delete_status, open_at, vast_instance_id, observe_window_s=90.

    Make executable.
  </action>
  <verify>
    <automated>test -x scripts/chaos/vast-delete.sh && bash -n scripts/chaos/vast-delete.sh && grep -E "set -euo pipefail|VAST_AI_API_KEY:?missing|console.vast.ai/api/v0/instances|Authorization: Bearer|local-llm|primary state --json|OBSERVE-FIRST" scripts/chaos/vast-delete.sh | wc -l | awk '{if ($1 >= 6) exit 0; else exit 1}'</automated>
  </verify>
  <done>vast-delete.sh executable + syntax-clean; grep returns ≥6 lines (env validation + Vast endpoint + Bearer auth + local-llm health probe + JSON-pref subcommand + OBSERVE-FIRST instruction); fail-fast on missing VAST_AI_API_KEY; never includes literal Bearer token; 90s reconcile window documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-07-02</id>
  <title>Task 2: Execute PRD-02 chaos test mid-load (observe-first cleanup, env-var keys)</title>
  <read_first>
    - scripts/chaos/vast-delete.sh (this plan task 1 output)
    - 11-CONTEXT.md D-07 + Specifics line 161 ($5 spend cap inherited)
    - 11-RESEARCH.md Pitfall 2 (5-10s blip acceptance) + Architecture Patterns PRD-02 diagram
    - .planning/phases/11-prod-hardening/11-06-EVIDENCE.md (baseline from previous plan)
  </read_first>
  <what-built>
    Live chaos test against prod. Operator launches a fresh load-replay.py for ~5 min to warm baseline, then triggers vast-delete.sh, observes invisible failover, lets test continue ~5 min more, then stops load and inspects metrics. Measures the ~5-10s blip window between DELETE and breaker OPEN as Pitfall 2 expectation. Observe-first for 90s before any manual cleanup.
  </what-built>
  <how-to-verify>
    1. Pre-check: `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state'` shows lifecycle Ready + at least 30 min TTL remaining.
    2. Set tenant env keys (operator pulls from local-only ~/.claude/CLAUDE.md or password manager — never inlined here):
       ```
       export IFIX_KEY_CONVERSEAI="$(... operator copy ...)"
       export IFIX_KEY_CHAT_IFIX="$(... operator copy ...)"
       export IFIX_KEY_CAMPANHAS="$(... operator copy ...)"
       ```
       NEVER paste raw `ifix_sk_...` values into this plan, shell history, or evidence.
    3. Start load (10-min run, smaller fixture window): on ops-claude tmux "chaos-load":
       ```
       python scripts/integration-smoke/load-replay.py \
         --gateway-url https://ai-gateway.converse-ai.app \
         --fixture /tmp/load-fixture.jsonl \
         --duration 600 \
         --max-concurrency 30 \
         --out /tmp/chaos-pre.json
       ```
       (Authorization built per-request at runtime from env IFIX_KEY_${TENANT_SLUG^^}; nothing tenant-secret in argv.)
       Let it warm for ~3 min (verify primary stays Ready + breakers closed).
    4. Trigger chaos (separate terminal): `bash scripts/chaos/vast-delete.sh`. Capture stdout/stderr to /tmp/vast-delete.log. The script auto-runs the 90s observation loop AND prints the OBSERVE-FIRST instruction.
    5. Observe timeline (allowed-error-classes window T+0..T+60s per reviews MEDIUM):
       - T+0: DELETE returns 200 (or 404 if idempotent gone)
       - T+5..10: gateway probe loop detects timeout
       - T+10..30: breaker local-llm transitions to OPEN (natural observation; the script records OPEN_AT)
       - T+30..60: FSM enters Draining; tier-1 OpenRouter starts handling LLM
       - T+60..300: load-replay continues; tier-1 path serves chat completions
       Allowed errors during T+0..T+60s (acceptance criteria):
         | Class | Permitted | Notes |
         | HTTP 503 `upstream_unavailable_for_sensitive_tenant` | yes | sensitive class, expected per RES-08 |
         | HTTP 503 `breaker_open` | yes | normal class, brief while breaker propagates |
         | HTTP 500 panic | NO | hard regression |
         | HTTP 502 bare proxy | NO | hard regression (proxy should classify) |
    6. While chaos active: monitor `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state'` every 30s — expect Draining → Asleep transition.
    7. OBSERVE-FIRST cleanup (reviews MEDIUM): WAIT until t+90s post-DELETE before any manual intervention. At t+90s:
       - If FSM in {Asleep, ProvisioningNew, NewReady, Ready}: controller is reconciling correctly; do NOT run force-up; let it self-recover.
       - If FSM still Draining: run `gatewayctl primary force-down --reason chaos_cleanup` then `gatewayctl primary force-up` to nudge a fresh provisioning cycle.
       Record the t+90s FSM state in EVIDENCE.md.
    8. Stop load-replay (Ctrl+C) once chaos has run ~5 min after DELETE OR let it complete the 600s duration.
    9. Inspect /tmp/chaos-pre.json: `jq '.summary, .gates' /tmp/chaos-pre.json`. Expected: gates may show `error_rate_lt_1pct=false` if blip 5xx exceeded budget (operator documents as expected per Pitfall 2 if total > zero); `zero_5xx_panic` MUST be true (panic = real regression). Capture error-class breakdown from per-request records.
    10. Verify primary back to Ready: `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state'` shows lifecycle in provisioning/ready cycle.
    11. Capture Vast spend (this run + force-up new pod): operator notes in 11-07-EVIDENCE.md.

    Abort criteria:
    - Vast spend exceeds $1 incremental for this plan.
    - Gateway returns 5xx panic during chaos window (NOT 503 transient — that's expected).
    - FSM stuck Draining > 10 min even after force-down+force-up.
  </how-to-verify>
  <resume-signal>Type "chaos PRD-02 PASS — DELETE→OPEN=X.Xs; tier-1 took over; 90s observe outcome={self-recovered|manual force-up}; spend=$Y.YY; primary restored" or describe any 5xx panic or cleanup gap.</resume-signal>
</task>

<task type="auto">
  <id>11-07-03</id>
  <title>Task 3: Commit chaos evidence (redacted, env-var labels only)</title>
  <files>.planning/phases/11-prod-hardening/11-07-EVIDENCE.md</files>
  <read_first>
    - Output of task 11-07-02 (operator timeline + metrics)
    - /tmp/vast-delete.log + /tmp/chaos-pre.json
    - 11-CONTEXT.md D-07
  </read_first>
  <action>
    Author .planning/phases/11-prod-hardening/11-07-EVIDENCE.md with sections:
    - Header: # Phase 11 PRD-02 Chaos: Primary Kill Live UAT
    - Metadata: date, operator name, target gateway, Vast instance killed (id), Vast spend total $USD.
    - Timeline (UTC HH:MM:SS):
      | Time | Event | Source |
      | T+0 | curl DELETE → 200 | vast-delete.sh log |
      | T+Xs | local-llm.state observed OPEN (natural) | vast-delete.sh observation loop |
      | T+Ys | FSM Draining | gatewayctl primary state |
      | T+Zs | tier-1 first chat completion via OpenRouter | gateway audit_log row (request_id only) |
      | T+90s | OBSERVE-FIRST outcome: FSM state at the 90s mark = {Asleep \| Draining \| ProvisioningNew \| Ready} | gatewayctl primary state |
      | T+Ws (only if manual cleanup needed) | force-down + force-up issued | operator log |
    - Allowed-error-classes window summary (T+0..T+60s):
      | Class | Count | Verdict |
      | HTTP 503 upstream_unavailable_for_sensitive_tenant | N | permitted |
      | HTTP 503 breaker_open | M | permitted |
      | HTTP 500 panic | 0 | required (else regression) |
      | HTTP 502 bare proxy | 0 | required (else regression) |
    - SLO verdict per D-04: P95 chat during full window vs after-cutover window. May exceed P95 budget in first 60s post-DELETE — document expected per Pitfall 2.
    - Cleanup confirmation: vast instance count = 0 post-DELETE; primary lifecycle recovered (self OR via manual force-up).
    - Deviations: anything outside Pitfall 2 expected blip (track in RUNBOOK-INCIDENTS class 1 section).
    - Cross-ref: scripts/chaos/vast-delete.sh, /tmp/chaos-pre.json (operator-archived), 11-06-EVIDENCE.md.
    - Sign-off: operator + date.
    - Cross-cutting attestation: no raw API keys, headers, request bodies, response bodies, DSNs, or PII; tenant references via env-var labels (e.g. ${TENANT_CONVERSEAI_KEY_ENV}) OR first-4-char prefix.

    git add + commit with "docs(11-07): PRD-02 chaos primary kill evidence". Pre-commit grep gate: `! grep -E "ifix_sk_[a-z0-9]{20,}|Bearer [A-Za-z0-9_]{20,}|postgres://" .planning/phases/11-prod-hardening/11-07-EVIDENCE.md` MUST pass.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/11-07-EVIDENCE.md && ! grep -E "ifix_sk_[a-z0-9]{20,}|Bearer [A-Za-z0-9_]{20,}|postgres://" .planning/phases/11-prod-hardening/11-07-EVIDENCE.md</automated>
  </verify>
  <done>Evidence committed; no raw secret strings; timeline + SLO verdict + allowed-error-classes table + 90s observe outcome + cleanup confirmation documented; cross-refs to load-test baseline and chaos script.</done>
</task>

</tasks>

<verification>
- bash -n scripts/chaos/vast-delete.sh exits 0.
- grep "console.vast.ai/api/v0" scripts/chaos/vast-delete.sh returns 1.
- grep "primary state --json" scripts/chaos/vast-delete.sh returns at least 1 (JSON output preference wired).
- grep "OBSERVE-FIRST" scripts/chaos/vast-delete.sh returns at least 1 (90s observe instruction).
- 11-07-EVIDENCE.md committed.
- Vast spend within $1 incremental cap for this plan.
- Primary FSM restored to Ready post-chaos OR explicit operator-restore plan in evidence.
- Zero 5xx panic during chaos window (hard gate; differentiated from 503 transient blip).
- Zero `ifix_sk_...` literals in plan or evidence.
</verification>

<success_criteria>
- PRD-02 invisible-failover proved: tier-1 took over, P95 stayed within SLO (operator-acceptable per Pitfall 2 expectations).
- vast-delete.sh shipped with JSON output preference + 90s observe-first cleanup instruction.
- Allowed-error-classes window honoured (no 500 panic, no 502 bare proxy).
- Evidence canonical example for RUNBOOK-INCIDENTS class 1 (11-09 ingests).
- No raw tenant keys anywhere in plan or evidence.
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies — bash + curl + jq already present. (Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.)
- Truth 3: Chaos script has idempotent cleanup advice + observe-first 90s window before manual intervention.
- Truth 4: Vast API key via env var; never literal in shell history.
- Truth 5: Live UAT plan autonomous=false; operator runs + observes.
- Truth 6: gateway image stays at :latest-dev until D-18.4 retrigger.
- Truth 8 (cross-cutting): NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in plan instructions or evidence; tenant references via env-var labels or first-4-char prefix only.
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-07-SUMMARY.md with: vast-delete.sh size, JSON-preference wired (yes/no), timeline summary, gates verdict, error-class breakdown, 90s observe outcome, spend total, link to 11-07-EVIDENCE.md.
</output>
