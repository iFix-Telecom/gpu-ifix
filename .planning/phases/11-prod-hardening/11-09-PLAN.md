---
phase: 11
plan: 11-09
slug: runbook-incidents-and-postmortem
wave: 3
depends_on: [11-06, 11-07, 11-08]
requirements: [PRD-04]
files_modified:
  - path: gateway/docs/RUNBOOK-INCIDENTS.md
    kind: NEW
  - path: gateway/docs/POSTMORTEM-TEMPLATE.md
    kind: NEW
autonomous: true
estimated_duration: 90
---

<objective>
Synthesize PRD-04 full incident-response runbook covering exactly 4 incident classes (D-11) cross-referencing 7 sibling runbooks and the Google SRE blameless 9-section postmortem template (D-10). Ingest the canonical evidence from Wave 2 (11-06 load-test baseline, 11-07 chaos primary kill timeline, 11-08 chaos OpenRouter DROP timeline) as the recipe examples for each class. Doc-only deliverable; closes the PRD-04 full sub-item carried from Phase 10.
</objective>

<must_haves>
  truths:
    - "D-10: Google SRE blameless 9-section template (Summary, Impact, Root Cause(s), Trigger, Detection, Resolution, Action Items, Timeline, Lessons)"
    - "D-11: exactly 4 incident classes — Primary pod down, OpenRouter/OpenAI degraded, Audit/billing pipeline broken, Rate-limit/quota lockout tenant"
    - "D-04 SLO v1.0 verbatim as Mental Model anchor (P95 chat ≤5s, embed ≤1s, STT ≤10s, error <1% non-503, zero 5xx panic)"
    - "Pattern G runbook header convention; 5 elements in order: H1, scope+triggers, last updated, Related runbooks list, Mental Model 30s"
    - "RESEARCH §Validation Architecture PRD-04 grep gate: grep -q 'Primary pod down\\|OpenRouter / OpenAI degraded\\|Audit/billing pipeline broken\\|Rate-limit / quota lockout' gateway/docs/RUNBOOK-INCIDENTS.md"
  must_haves:
    - gateway/docs/RUNBOOK-INCIDENTS.md exists with 4 class sections matching D-11 names verbatim
    - gateway/docs/POSTMORTEM-TEMPLATE.md has all 9 D-10 sections in order
    - Cross-ref bullets in RUNBOOK-INCIDENTS to each of the 7 sibling runbooks (RUNBOOK-DEPLOY, RUNBOOK-FAILOVER, RUNBOOK-PRIMARY-POD, RUNBOOK-EMERGENCY-POD, RUNBOOK-OBSERVABILITY-ALERTING, RUNBOOK-QUOTAS-BILLING, RUNBOOK-CLIENT-INTEGRATION, RUNBOOK-CLIENT-INTEGRATION-SENSITIVE)
    - Class 3 (Audit/billing pipeline broken) cites commit 5bd79d1 (`r.Header.Del("Accept-Encoding")` Phase 10 fix per CONTEXT.md D-11 line 49)
  key_links:
    - from: gateway/docs/RUNBOOK-INCIDENTS.md Class 1
      to: gateway/docs/RUNBOOK-PRIMARY-POD.md + RUNBOOK-FAILOVER.md
      via: Markdown cross-ref bullets
      pattern: "RUNBOOK-PRIMARY-POD\\.md|RUNBOOK-FAILOVER\\.md"
    - from: gateway/docs/RUNBOOK-INCIDENTS.md Class 3
      to: gateway/internal/proxy/director.go:80 + commit 5bd79d1
      via: cite commit in diagnose section
      pattern: "5bd79d1"
    - from: gateway/docs/POSTMORTEM-TEMPLATE.md
      to: postmortem instance files (future)
      via: 9-section skeleton ready for fill-in
      pattern: "## 9. Lessons Learned"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> operator decision-making | runbook drives incident response under stress; accuracy invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-DOC-01 | Information Disclosure | RUNBOOK contains tenant-identifying details | mitigate | Use placeholder {TENANT_SLUG} in templates; no real tenant data in committed docs |
| T-11-DOC-02 | Tampering | Stale runbook leads operator to wrong action | mitigate | Mental Model section anchors to SLO v1.0 D-04 thresholds with explicit "Last updated: 2026-MM-DD" header; review cadence documented |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@.planning/phases/11-prod-hardening/11-06-PLAN.md
@.planning/phases/11-prod-hardening/11-07-PLAN.md
@.planning/phases/11-prod-hardening/11-08-PLAN.md
@gateway/docs/RUNBOOK-FAILOVER.md
@gateway/docs/RUNBOOK-PRIMARY-POD.md
@gateway/docs/RUNBOOK-EMERGENCY-POD.md
@gateway/docs/RUNBOOK-OBSERVABILITY-ALERTING.md
@gateway/docs/RUNBOOK-QUOTAS-BILLING.md
@gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md
@gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md
@gateway/docs/RUNBOOK-DEPLOY.md
</context>

<tasks>

<task type="auto">
  <id>11-09-01</id>
  <title>Task 1: Author RUNBOOK-INCIDENTS.md (4 classes + Mental Model + cross-refs)</title>
  <files>gateway/docs/RUNBOOK-INCIDENTS.md</files>
  <read_first>
    - gateway/docs/RUNBOOK-FAILOVER.md (Pattern G header + Mental Model 30s + Quick Diagnosis flow)
    - gateway/docs/RUNBOOK-PRIMARY-POD.md (cross-ref sibling list shape)
    - 11-PATTERNS.md RUNBOOK-INCIDENTS.md (4 D-11 classes verbatim spelling for grep gate + Pattern G 5-element header)
    - 11-CONTEXT.md D-04 + D-11 + canonical_refs section
    - 11-RESEARCH.md Validation Architecture PRD-04 row (grep gate command exactly)
    - .planning/phases/11-prod-hardening/11-06-EVIDENCE.md (load-test baseline canonical recipe — class 4 anchor)
    - .planning/phases/11-prod-hardening/11-07-EVIDENCE.md (chaos primary kill canonical recipe — class 1 anchor)
    - .planning/phases/11-prod-hardening/11-08-EVIDENCE.md (chaos OpenRouter DROP canonical recipe — class 2 anchor)
  </read_first>
  <action>
    Create gateway/docs/RUNBOOK-INCIDENTS.md per 11-PATTERNS.md RUNBOOK-INCIDENTS.md structure + Pattern G header convention.

    Pattern G header (5 elements):
    1. # Incident Response Runbook
    2. **Phase 11 (`ifix-ai-gateway`) production incident playbook.** Read this when: bulleted triggers (new incident suspected, historical incident post-mortem'd, shift handoff).
    3. **Last updated: 2026-05-27.** (current planning date)
    4. Related runbooks: bulleted cross-ref list to ALL 7 sibling runbooks (RUNBOOK-DEPLOY, RUNBOOK-FAILOVER, RUNBOOK-PRIMARY-POD, RUNBOOK-EMERGENCY-POD, RUNBOOK-OBSERVABILITY-ALERTING, RUNBOOK-QUOTAS-BILLING, RUNBOOK-CLIENT-INTEGRATION, RUNBOOK-CLIENT-INTEGRATION-SENSITIVE).
    5. ## Mental Model (30 seconds) — SLO v1.0 thresholds table per D-04 verbatim:
       | Metric | Threshold | Source |
       | P95 chat completion | ≤5s | D-04 |
       | P95 embed | ≤1s | D-04 |
       | P95 STT | ≤10s | D-04 |
       | Error rate (non-503) | <1% | D-04 |
       | 5xx panic count | 0 | D-04 |

    Followed by the 4 D-11 incident classes (EXACT spelling per RESEARCH grep gate line 653):

    ## Class 1: Primary pod down
    Detection signals: gateway_primary_state{state="asleep"} Prometheus; Sentry breadcrumb subsystem:primary, shutdown_reason:health_timeout; /v1/health/upstreams shows local-llm.state="open"; FSM in Draining/Asleep.
    Diagnose: ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state'; check Vast.ai dashboard for instance status (CDI faults, host-side); review recent reconciler logs.
    Mitigate: wait for FSM observation auto-recovery OR gatewayctl primary force-up; if Vast inventory low: revise PRIMARY_VAST_PRICE_CAP_DPH or PRIMARY_VAST_MACHINE_ALLOWLIST per RUNBOOK-PRIMARY-POD.md.
    Verify: /v1/health/upstreams local-llm.state="closed"; X-Upstream:local-llm header on chat-completions.
    Canonical example: .planning/phases/11-prod-hardening/11-07-EVIDENCE.md (chaos primary kill UAT).
    Cross-ref: gateway/docs/RUNBOOK-PRIMARY-POD.md + gateway/docs/RUNBOOK-FAILOVER.md + gateway/docs/RUNBOOK-EMERGENCY-POD.md.

    ## Class 2: OpenRouter / OpenAI degraded
    Detection signals: breaker openrouter-chat or openai-* OPEN; 503 spike per-route; sensitive tenants returning sensitive_block (expected per RES-08 invariant).
    Diagnose: curl /v1/health/upstreams; check OpenRouter status page; tcpdump on n8n-ia-vm if suspected egress block.
    Mitigate: wait for provider recovery OR manual gatewayctl breaker force-close --upstream=openrouter-chat AFTER provider confirmed healthy (escape hatch); ensure sensitive tenants understand 503 is expected during tier-1 degradation.
    Verify: openrouter-chat state transitions to half-open then closed; per-route P95 returns to baseline.
    Canonical example: .planning/phases/11-prod-hardening/11-08-EVIDENCE.md (chaos OpenRouter DROP UAT).
    Cross-ref: gateway/docs/RUNBOOK-FAILOVER.md + gateway/docs/RUNBOOK-CLIENT-INTEGRATION-SENSITIVE.md.

    ## Class 3: Audit/billing pipeline broken
    Detection signals: audit_log row count drops to zero while gateway returns 200; billing_events rows with source="partial"; UTF8 0x8b gzip-magic-byte errors in container logs.
    Diagnose: SELECT count(*) FROM ai_gateway_prod.audit_log WHERE ts > NOW() - INTERVAL '5 minutes'; cross-check with /admin/metrics request count. Check gateway/internal/proxy/director.go:80 — Accept-Encoding header MUST be deleted (commit 5bd79d1 fix).
    Mitigate: if regression — verify director.go:80 has `r.Header.Del("Accept-Encoding")` per Phase 10 commit 5bd79d1; redeploy if missing. If billing event lag: drain BullMQ queue + re-run partial events.
    Verify: audit_log row count matches request count within 1 minute window; no UTF8 errors in container logs.
    Reference commit: 5bd79d1 (Phase 10 audit/billing 0x8b gzip bug fix).
    Cross-ref: gateway/docs/RUNBOOK-OBSERVABILITY-ALERTING.md + gateway/docs/RUNBOOK-QUOTAS-BILLING.md.

    ## Class 4: Rate-limit / quota lockout tenant
    Detection signals: gateway_rate_limit_rejected_total{tenant=X} spike; client reports 429 with X-Retry-After; per-tenant request count exceeds quota.
    Diagnose: gatewayctl tenant get-quota --tenant=X; check gateway/internal/quota state; identify if abusive client OR misconfigured quota.
    Mitigate: gatewayctl tenant set-quota --tenant=X --tokens=N OR gatewayctl breaker force-close --upstream=Y (escape hatch operator); coordinate with tenant operator.
    Verify: 429 rate decays; per-tenant P95 + error returns to baseline.
    Canonical example: .planning/phases/11-prod-hardening/11-06-EVIDENCE.md (load-test baseline — rate-limit not triggered but baseline P95 reference).
    Cross-ref: gateway/docs/RUNBOOK-QUOTAS-BILLING.md + gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md.

    Final section ## Postmortem cross-ref linking to gateway/docs/POSTMORTEM-TEMPLATE.md (created in task 11-09-02).

    Final section ## SLO v1.0 anchor — restate D-04 thresholds + reference to load-test PRD-01 baseline as the canonical "SLO measured PASS" evidence point.
  </action>
  <verify>
    <automated>test -f gateway/docs/RUNBOOK-INCIDENTS.md && grep -q "Primary pod down" gateway/docs/RUNBOOK-INCIDENTS.md && grep -q "OpenRouter / OpenAI degraded" gateway/docs/RUNBOOK-INCIDENTS.md && grep -q "Audit/billing pipeline broken" gateway/docs/RUNBOOK-INCIDENTS.md && grep -q "Rate-limit / quota lockout" gateway/docs/RUNBOOK-INCIDENTS.md && grep -q "5bd79d1" gateway/docs/RUNBOOK-INCIDENTS.md && grep -c "RUNBOOK-" gateway/docs/RUNBOOK-INCIDENTS.md | awk '{if ($1 >= 7) exit 0; else exit 1}'</automated>
  </verify>
  <done>RUNBOOK-INCIDENTS.md exists; the 4-class grep gate from RESEARCH §Validation Architecture line 653 passes (all 4 D-11 names found); commit 5bd79d1 cited for class 3; ≥7 RUNBOOK-* cross-refs; Mental Model 30s section opens content; D-04 SLO table verbatim.</done>
</task>

<task type="auto">
  <id>11-09-02</id>
  <title>Task 2: Author POSTMORTEM-TEMPLATE.md (Google SRE 9-section blameless)</title>
  <files>gateway/docs/POSTMORTEM-TEMPLATE.md</files>
  <read_first>
    - 11-PATTERNS.md POSTMORTEM-TEMPLATE.md (9-section exact spelling per D-10)
    - 11-CONTEXT.md D-10 ("Sections: Summary, Impact, Root Cause(s), Trigger, Detection, Resolution, Action Items, Timeline, Lessons")
    - 11-RESEARCH.md §Common Pitfalls anti-pattern "POSTMORTEM template sem `Lessons`"
    - .planning/phases/10-prod-deploy-ai-gateway/10-VERIFICATION.md (postmortem-like pitfalls_hit + deviations precedent)
  </read_first>
  <action>
    Create gateway/docs/POSTMORTEM-TEMPLATE.md per 11-PATTERNS.md POSTMORTEM-TEMPLATE.md exactly — Google SRE blameless 9-section spelling.

    Header:
    # Postmortem: {Incident Title}
    **Date:** YYYY-MM-DD
    **Duration:** {start UTC} → {end UTC} ({wall-clock minutes})
    **Severity:** SEV-{1|2|3}
    **Author:** {operator}
    **Status:** draft | review | published

    9 sections in order (DO NOT add or omit; D-10 invariant):

    ## 1. Summary
    {2-3 sentences: what broke, who was affected, how long, root cause one-liner}

    ## 2. Impact
    {tenants affected, requests failed, revenue impact if any, SLO budget burned per D-04}

    ## 3. Root Cause(s)
    {technical cause(s); blameless — describe systems, not people}

    ## 4. Trigger
    {specific event that started the incident — deploy, traffic spike, dep change, ...}

    ## 5. Detection
    {how it was first noticed — alert? client report? internal observation? time-to-detect}

    ## 6. Resolution
    {step-by-step what fixed it; cite commits / runbook steps}

    ## 7. Action Items
    | # | Action | Owner | Due | Status |
    |---|--------|-------|-----|--------|
    | 1 | ... | @user | YYYY-MM-DD | open |

    ## 8. Timeline
    {UTC timestamps, terse events; pull from Sentry, audit_log, operator notes}

    ## 9. Lessons Learned
    {insights that don't map to a single action item — "the system surprised us by..."}

    Closing section ## How to use this template — instruction block: copy this file to .planning/postmortems/postmortem-{YYYY-MM-DD}-{slug}.md before filling in; commit per-incident; never edit this template in-place (treat as authoritative skeleton).

    Cross-ref: gateway/docs/RUNBOOK-INCIDENTS.md.
  </action>
  <verify>
    <automated>test -f gateway/docs/POSTMORTEM-TEMPLATE.md && grep -c "^## [1-9]\. " gateway/docs/POSTMORTEM-TEMPLATE.md | awk '{if ($1 == 9) exit 0; else exit 1}' && grep -q "Lessons Learned" gateway/docs/POSTMORTEM-TEMPLATE.md && grep -q "Action Items" gateway/docs/POSTMORTEM-TEMPLATE.md</automated>
  </verify>
  <done>Template exists with exactly 9 numbered ## sections; "Lessons Learned" present (Pitfall anti-pattern guard); Action Items table skeleton with Owner/Due/Status; header metadata block with Severity SEV-1/2/3 placeholder.</done>
</task>

</tasks>

<verification>
- grep -q "Primary pod down\|OpenRouter / OpenAI degraded\|Audit/billing pipeline broken\|Rate-limit / quota lockout" gateway/docs/RUNBOOK-INCIDENTS.md exits 0 (PRD-04 grep gate).
- grep -c "^## [1-9]\. " gateway/docs/POSTMORTEM-TEMPLATE.md returns 9 (D-10 9-section invariant).
- Cross-refs to 7 sibling runbooks present.
- Markdown link rot check (optional): markdown-link-check gateway/docs/RUNBOOK-INCIDENTS.md returns 0 broken.
</verification>

<success_criteria>
- PRD-04 full incident runbook + Google SRE postmortem template shipped.
- 4 D-11 classes verbatim, commit 5bd79d1 cited for class 3.
- Wave 2 evidence (11-06/07/08) ingested as canonical examples.
- Templates ready for incident-day fill-in.
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies — doc-only this plan. (Dashboard 2FA UI adds 4 packages scoped to 11-02 only.)
- Truth 2: pt-BR/en mixed acceptable per existing RUNBOOK voice; no speculative language.
- Truth 5: This plan autonomous (executor authors docs; review optional async).
- depends_on [11-06, 11-07, 11-08] ensures Wave 2 evidence is committed before this plan starts.
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-09-SUMMARY.md with: RUNBOOK-INCIDENTS.md byte count, POSTMORTEM-TEMPLATE.md byte count, grep gate results, 7-sibling cross-ref check.
</output>
