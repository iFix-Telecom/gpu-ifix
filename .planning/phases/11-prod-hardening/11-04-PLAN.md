---
phase: 11
plan: 11-04
slug: gatewayctl-and-phase10-fold
wave: 1
depends_on: []
requirements: [PRD-04]
secondary_decisions: [D-18.1, D-18.2, D-18.3]
files_modified:
  - path: gateway/cmd/gatewayctl/debug.go
    kind: NEW
  - path: gateway/cmd/gatewayctl/debug_test.go
    kind: NEW
  - path: gateway/cmd/gatewayctl/key.go
    kind: MOD
  - path: gateway/cmd/gatewayctl/key_test.go
    kind: MOD
  - path: gateway/cmd/gatewayctl/main.go
    kind: MOD
  - path: gateway/cmd/gateway/main.go
    kind: MOD
  - path: gateway/internal/admin/debug_panic.go
    kind: NEW
  - path: gateway/internal/admin/debug_panic_test.go
    kind: NEW
  - path: gateway/db/queries/auth.sql
    kind: MOD
  - path: gateway/internal/db/gen/auth.sql.go
    kind: MOD
  - path: gateway/internal/db/gen/querier.go
    kind: MOD
  - path: scripts/integration-smoke/smoke-sensitive-failover.py
    kind: MOD
autonomous: true
estimated_duration: 165
---

<objective>
Fold the Phase 10 deferred items (D-18.1, D-18.2, D-18.3 — D-18.4 doc lives in 11-05): smoke-sensitive-failover.py race-fix accepting FORCED_OPEN as equivalent to natural-open AND repointing the gatewayctl induce mode to the now-shipped `gatewayctl breaker force-open` from Phase 06.9 Plan 04; `gatewayctl debug emit-error` subcommand with an automated HTTP integration test proving the `/admin/debug/panic` route is admin-gated (reviews HIGH #1); `gatewayctl key list` operator inspection table with sqlc query columns explicitly verified BEFORE writing the dispatcher and with the regenerated sqlc Go output committed alongside the SQL (reviews MEDIUM #2); smoke script parameterized via env vars so it is not hard-coded to `n8n-ia-vm` (reviews MEDIUM #3); all tests are real assertions, no `t.Skip` placeholders (reviews LOW #4). All three D-18 items contribute to PRD-04 full-runbook diagnostic surface that 11-09 consumes.
</objective>

<must_haves>
  truths:
    - "D-18.1: smoke-sensitive-failover.py polling loop accepts {open, forced-open, FORCED_OPEN}; defensive whitelist explicit (Pitfall 8); closed and half-open NEVER in accepted set"
    - "D-18.1 second half: gatewayctl induce mode invokes real `breaker force-open` (Phase 06.9 Plan 04 landed at gateway/cmd/gatewayctl/breaker.go:117)"
    - "D-18.2: gatewayctl debug emit-error -> POST /admin/debug/panic -> httpx.Recoverer catches -> sentry.CurrentHub().Recover + Flush(500ms) -> event lands in Sentry ifix-ai-gateway-prod (id 4511455942017024)"
    - "D-18.2 reviews HIGH #1: /admin/debug/panic MUST have an automated integration test proving (a) unauthenticated request returns 401/403 (admin auth gate alive), (b) authenticated admin request returns 500 via Recoverer with sanitized envelope body (panic recovery alive + stacktrace NOT leaked). Both assertions encoded as concrete Go test cases in debug_panic_test.go. Code placement alone is NOT acceptable."
    - "D-18.3 reviews MEDIUM #2: sqlc query columns MUST be audited FIRST (read existing auth.sql + gen/auth.sql.go) BEFORE writing the dispatcher. Existing `ListActiveKeysAll` does NOT include tenant_slug/created_at/last_used_at (verified by reading gateway/db/queries/auth.sql:21-28). Plan delivers TWO new queries `ListActiveKeysAllWithMeta` + `ListActiveKeysByTenantWithMeta` in the same `.sql` file AND the regenerated `gateway/internal/db/gen/auth.sql.go` is committed in this plan (both files appear in files_modified)."
    - "D-18.3: gatewayctl key list returns aligned table via stdlib text/tabwriter; never prints raw key (only key_prefix); RFC3339 UTC timestamps"
    - "reviews MEDIUM #3: smoke-sensitive-failover.py reads GATEWAYCTL_PATH + GATEWAYCTL_CONTAINER + GATEWAYCTL_SSH_HOST from env with sensible defaults so the script works against any environment (default = local docker exec on ifix-ai-gateway container; set GATEWAYCTL_SSH_HOST=n8n-ia-vm for prod). Defaults documented in docstring."
    - "Pattern D 5 invariants honored: runCmd(ctx, args, log) int signature, exit codes 0/1/2, flag.NewFlagSet, loadAndPool + defer pool.Close, stdout=parseable + stderr=log"
    - "reviews LOW #4: Tests are real assertions, not Skip() placeholders. debug_panic_test.go uses httptest + middleware composition to drive real HTTP requests; key_test.go uses a stubbed Querier interface returning canned rows (default path = hermetic unit test); grep `t.Skip` on both test files MUST return 0."
  must_haves:
    - gateway/cmd/gatewayctl/debug.go exists with runDebug dispatcher + runDebugEmitError function
    - gateway/cmd/gatewayctl/main.go switch includes case "debug" + extended usage()
    - gateway/cmd/gateway/main.go router registers POST /admin/debug/panic under the admin chain (Recoverer outermost) so the operator endpoint is reachable in the running binary
    - gateway/internal/admin/debug_panic.go exports DebugPanicHandler that panics inside Recoverer chain
    - gateway/internal/admin/debug_panic_test.go contains TWO automated HTTP test cases — (a) unauth → 401/403 (b) auth + Recoverer → 500 with sanitized body, panic message NOT leaked
    - gateway/db/queries/auth.sql contains a new `ListActiveKeysAllWithMeta :many` query that joins api_keys with the tenants table for tenant_slug AND selects status, data_class, created_at, last_used_at (and a tenant-filtered `ListActiveKeysByTenantWithMeta :many` variant for the --tenant filter)
    - gateway/internal/db/gen/auth.sql.go regenerated to expose `ListActiveKeysAllWithMeta` + `ListActiveKeysByTenantWithMeta` Go funcs returning structs with fields {ID, TenantID, TenantSlug, KeyPrefix, Status, DataClass, CreatedAt, LastUsedAt}
    - gateway/internal/db/gen/querier.go regenerated to include the two new method signatures on the Querier interface
    - gateway/cmd/gatewayctl/key.go dispatcher includes case "list" -> runKeyList; usage string lists create|revoke|list
    - gateway/cmd/gatewayctl/key_test.go contains a real test (not Skip()) using a stubbed Querier interface returning canned Rows; asserts header line + row count + that key_hash substring is absent from stdout
    - scripts/integration-smoke/smoke-sensitive-failover.py constants block declares OPEN_LIKE_STATES frozenset with exactly {"open","forced-open","FORCED_OPEN"}
    - smoke-sensitive-failover.py declares GATEWAYCTL_PATH / GATEWAYCTL_CONTAINER / GATEWAYCTL_SSH_HOST env-driven constants AND induce_failure_via_gatewayctl invokes subprocess.run with gatewayctl breaker force-open --upstream=local-llm --ttl=300s using those env vars
  key_links:
    - from: gateway/cmd/gatewayctl/debug.go
      to: gateway/internal/admin/debug_panic.go
      via: HTTP POST /admin/debug/panic with X-Admin-Key header
      pattern: "/admin/debug/panic"
    - from: gateway/internal/admin/debug_panic.go
      to: gateway/internal/httpx/recoverer.go
      via: panic() recovered by Recoverer middleware wrapping admin chain
      pattern: "Recoverer"
    - from: gateway/internal/admin/debug_panic_test.go
      to: gateway/internal/admin/debug_panic.go + httpx.Recoverer + admin.Middleware
      via: httptest.NewServer with the composed chain; raw http.Get/Post
      pattern: "httptest.NewServer|http.NewRequest"
    - from: gateway/cmd/gateway/main.go
      to: gateway/internal/admin/debug_panic.go
      via: r.With(httpx.Recoverer, admin.Middleware).Post("/admin/debug/panic", admin.DebugPanicHandler(log).ServeHTTP)
      pattern: "/admin/debug/panic"
    - from: gateway/cmd/gatewayctl/key.go runKeyList
      to: gateway/internal/db/gen/auth.sql.go ListActiveKeysAllWithMeta / ListActiveKeysByTenantWithMeta
      via: sqlc-generated Go queries via loadAndPool + gen.New(pool)
      pattern: "ListActiveKeys.*WithMeta"
    - from: scripts/integration-smoke/smoke-sensitive-failover.py induce mode
      to: gateway/cmd/gatewayctl/breaker.go force-open subcommand
      via: subprocess.run gatewayctl breaker force-open (path from env GATEWAYCTL_PATH, optional SSH wrapper)
      pattern: "breaker.*force-open"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> /admin/debug/panic | X-Admin-Key gated; synthetic panic exercises Recoverer chain |
| /admin/debug/panic without admin auth | MUST return 401/403 — test proves this |
| gateway container -> Sentry org Ifix project ifix-ai-gateway-prod | panic events with admin context (admin_key_id label) |
| operator -> DB read | gatewayctl key list reads ai_gateway.api_keys via sqlc (new WithMeta queries) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-OPS-01 | Elevation of Privilege | /admin/debug/panic unauthenticated | mitigate | Handler mounted under admin.Middleware (X-Admin-Key required); AUTOMATED INTEGRATION TEST debug_panic_test.go drives raw HTTP requests through the composed chain and asserts unauth → 401/403 + auth → 500; code placement alone is not relied upon (reviews HIGH #1) |
| T-11-OPS-02 | Information Disclosure | gatewayctl key list raw key leak | mitigate | Print key_prefix only (never raw API key); stdlib text/tabwriter; query selects key_prefix column (NOT key_hash, NOT raw key); test asserts grep-able pattern AND asserts absence of key_hash substring in captured stdout |
| T-11-OPS-03 | Tampering | breaker force-open via smoke harness without audit | mitigate | gateway/cmd/gatewayctl/breaker.go:117 force-open writes Redis key + audit row (Phase 06.9); smoke now invokes the audited path instead of stub |
| T-11-OPS-04 | Denial of Service | panic handler crashes if Recoverer not in chain | mitigate | debug_panic.go docstring explicit about wrap order (Recoverer outermost); test asserts 500 response (NOT crash) by composing the chain in httptest and verifying response body is the sanitized error envelope (no panic stacktrace in body) |
| T-11-OPS-08 | Information Disclosure | panic stacktrace leaks into HTTP response | mitigate | Recoverer's WriteOpenAIError emits sanitized envelope only; debug_panic_test.go asserts response body does NOT contain "synthetic panic" or "goroutine" tokens |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@gateway/cmd/gatewayctl/admin_key.go
@gateway/cmd/gatewayctl/admin_key_test.go
@gateway/cmd/gatewayctl/breaker.go
@gateway/cmd/gatewayctl/key.go
@gateway/cmd/gatewayctl/main.go
@gateway/cmd/gateway/main.go
@gateway/internal/httpx/recoverer.go
@gateway/internal/admin/middleware.go
@gateway/internal/db/gen/auth.sql.go
@gateway/internal/db/gen/querier.go
@gateway/db/queries/auth.sql
@scripts/integration-smoke/smoke-sensitive-failover.py
</context>

<interfaces>
From gateway/cmd/gatewayctl/admin_key.go:
- runAdminKey(ctx context.Context, args []string, log *slog.Logger) int
- runAdminKeyList(ctx context.Context, args []string, log *slog.Logger) int  (pattern for runKeyList)

From gateway/cmd/gatewayctl/breaker.go (Phase 06.9 Plan 04, verified line 117):
- runBreakerForceOpen(ctx context.Context, args []string, log *slog.Logger) int
- flags: --upstream=NAME --ttl=DURATION

From gateway/internal/admin/middleware.go:
- Middleware(...) http.Handler — fronts admin routes with X-Admin-Key auth
- FromContext(ctx context.Context) (*AdminContext, bool)
- AdminContext has AdminKeyID uuid.UUID and Label string

From gateway/internal/httpx/recoverer.go:
- Recoverer(base *slog.Logger) func(http.Handler) http.Handler — recovers panic, calls sentry.CurrentHub().Recover(rec) + sentry.Flush(500ms), then WriteOpenAIError(w, 500, "api_error", "internal_error", ...)

From gateway/db/queries/auth.sql (verified 2026-05-27 — these are the existing queries):
- ListActiveKeysByTenant :many → returns id, tenant_id, key_hash, key_lookup_hash, key_prefix, status, data_class, created_at, revoked_at, last_used_at (NO tenant_slug)
- ListActiveKeysAll :many → returns id, tenant_id, key_hash, key_lookup_hash, key_prefix, status, data_class (NO created_at, NO last_used_at, NO tenant_slug)
- GetActiveKeyByLookupHash :one (hot path)
- GetAPIKeyByID :one
- TouchKeyLastUsed :exec

From gateway/internal/db/gen/ (sqlc output directory — verified files present):
- auth.sql.go (generated; will gain ListActiveKeysAllWithMeta + ListActiveKeysByTenantWithMeta funcs + Row structs)
- querier.go (Querier interface; will gain two matching method signatures)

Therefore the operator-friendly `key list` table needs NEW queries that JOIN on tenants for slug AND project the full meta set. Both the `.sql` source AND the regenerated `.sql.go` + `querier.go` MUST be committed in this plan (reviews MEDIUM #2 — closes the files_modified drift).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <id>11-04-00</id>
  <title>Task 0 [reviews MEDIUM #2]: sqlc audit + add ListActiveKeysAllWithMeta / ListActiveKeysByTenantWithMeta queries + regenerate Go output</title>
  <files>gateway/db/queries/auth.sql, gateway/internal/db/gen/auth.sql.go, gateway/internal/db/gen/querier.go</files>
  <read_first>
    - gateway/db/queries/auth.sql (full file — confirm the column set of the existing ListActiveKeysAll + ListActiveKeysByTenant matches the <interfaces> block; if drift detected, surface in SUMMARY)
    - gateway/internal/db/gen/auth.sql.go (current shape of generated Row structs + method signatures — needed to predict diff)
    - gateway/internal/db/gen/querier.go (Querier interface; the two new methods MUST be appended in stable position)
    - gateway/db/migrations (verify `ai_gateway.tenants` exists and has `slug` column; if the project uses `ai_gateway.tenants_admin` instead — see `tenants_admin.sql.go` in gen/ — executor MUST audit which table holds the operator-visible slug and JOIN against the correct one)
    - 11-PATTERNS.md key.go section (operator-table requirements)
    - 11-CONTEXT.md D-18.3
  </read_first>
  <acceptance_criteria>
    - `grep -E "ListActiveKeysAllWithMeta|ListActiveKeysByTenantWithMeta" gateway/db/queries/auth.sql | wc -l` returns >= 2 (both query name comments present).
    - Each new query selects EXACTLY the projection {id, tenant_id, tenant_slug, key_prefix, status, data_class, created_at, last_used_at} — verified by `grep -A 2 "ListActiveKeysAllWithMeta" gateway/db/queries/auth.sql` showing the SELECT list.
    - Neither new query selects `key_hash` or `key_lookup_hash` (secret-bearing columns) — `grep -B 1 -A 6 "WithMeta" gateway/db/queries/auth.sql | grep -E "key_hash|key_lookup_hash" | wc -l` returns 0.
    - `cd gateway && sqlc generate` exits 0.
    - `grep -E "func .* ListActiveKeysAllWithMeta|func .* ListActiveKeysByTenantWithMeta" gateway/internal/db/gen/auth.sql.go | wc -l` returns >= 2.
    - `grep -E "ListActiveKeysAllWithMeta|ListActiveKeysByTenantWithMeta" gateway/internal/db/gen/querier.go | wc -l` returns >= 2.
    - Generated Row structs include the fields {ID, TenantID, TenantSlug, KeyPrefix, Status, DataClass, CreatedAt, LastUsedAt} — verified by `grep -A 10 "ListActiveKeysAllWithMetaRow" gateway/internal/db/gen/auth.sql.go`.
    - `cd gateway && go vet ./internal/db/gen/...` clean.
  </acceptance_criteria>
  <action>
    Step 1 — Audit. Read gateway/db/queries/auth.sql top to bottom. Cross-check the <interfaces> block above: ListActiveKeysAll lacks tenant_slug + created_at + last_used_at; ListActiveKeysByTenant lacks tenant_slug. If the audit finds the existing queries DO already include those columns (drift since this plan was written), STOP and surface in SUMMARY before adding redundant queries.

    Step 2 — Confirm tenants table. Inspect `gateway/db/migrations/` to confirm which schema/table exposes the operator-readable slug. Candidates: `ai_gateway.tenants` (slug column) OR `ai_gateway.tenants_admin` (see `gateway/internal/db/gen/tenants_admin.sql.go`). Use whichever JOIN target actually has the slug + foreign-key relationship to api_keys.tenant_id. If neither matches, surface the gap and STOP — do not invent a JOIN.

    Step 3 — Append two new query blocks at the END of gateway/db/queries/auth.sql (existing queries untouched — the hot-path GetActiveKeyByLookupHash MUST remain unmodified). Each block named via sqlc `-- name:` directive: `ListActiveKeysAllWithMeta :many` and `ListActiveKeysByTenantWithMeta :many`. Projection identical for both: `SELECT k.id, k.tenant_id, t.slug AS tenant_slug, k.key_prefix, k.status, k.data_class, k.created_at, k.last_used_at FROM ai_gateway.api_keys k JOIN ai_gateway.tenants t ON t.id = k.tenant_id WHERE k.status = 'active'` plus `AND k.tenant_id = $1` for the tenant-filtered variant. Order all-variant by `t.slug ASC, k.last_used_at DESC NULLS LAST`; order tenant-variant by `k.last_used_at DESC NULLS LAST`. Each query gets a docstring comment explaining it is diagnostic-only and explicitly NOT on the hot path.

    Step 4 — Run `cd gateway && sqlc generate`. Confirm `internal/db/gen/auth.sql.go` now contains exported funcs `ListActiveKeysAllWithMeta(ctx) ([]ListActiveKeysAllWithMetaRow, error)` and `ListActiveKeysByTenantWithMeta(ctx, tenantID uuid.UUID) ([]ListActiveKeysByTenantWithMetaRow, error)`. Confirm `internal/db/gen/querier.go` has the two new method signatures on the Querier interface.

    Step 5 — Run `go vet ./internal/db/gen/...` and `go build ./...` to confirm the generated code compiles in the workspace.
  </acceptance_criteria>
  <verify>
    <automated>cd gateway && grep -E "ListActiveKeysAllWithMeta|ListActiveKeysByTenantWithMeta" db/queries/auth.sql | grep -v '^--' | wc -l | awk '{if ($1 >= 2) exit 0; else exit 1}' && (sqlc generate 2>&1 | tee /tmp/sqlc.log; grep -q "error" /tmp/sqlc.log && exit 1 || true) && grep -E "func .* ListActiveKeysAllWithMeta|func .* ListActiveKeysByTenantWithMeta" internal/db/gen/auth.sql.go | wc -l | awk '{if ($1 >= 2) exit 0; else exit 1}' && grep -E "ListActiveKeysAllWithMeta|ListActiveKeysByTenantWithMeta" internal/db/gen/querier.go | wc -l | awk '{if ($1 >= 2) exit 0; else exit 1}' && go vet ./internal/db/gen/... && go build ./...</automated>
  </verify>
  <done>Both new sqlc queries present in auth.sql; sqlc regen produces matching Go funcs + Row structs with required fields and no secret-bearing columns; querier.go interface extended; workspace builds clean. Task 1 + Task 2 may now consume the regenerated API.</done>
</task>

<task type="auto" tdd="true">
  <id>11-04-01</id>
  <title>Task 1 [reviews HIGH #1 + LOW #4]: gatewayctl debug subcommand + /admin/debug/panic handler + AUTOMATED unauth-gate + recovery test</title>
  <files>gateway/cmd/gatewayctl/debug.go, gateway/cmd/gatewayctl/debug_test.go, gateway/internal/admin/debug_panic.go, gateway/internal/admin/debug_panic_test.go, gateway/cmd/gatewayctl/main.go, gateway/cmd/gateway/main.go</files>
  <read_first>
    - gateway/cmd/gatewayctl/admin_key.go (dispatcher pattern lines 50-66 + flag parsing)
    - gateway/cmd/gatewayctl/main.go (switch dispatch lines 48-101 + usage() lines 20-46)
    - gateway/cmd/gateway/main.go (router setup — locate the existing admin chain mount so the new route is registered consistently)
    - gateway/internal/admin/middleware.go (FromContext + AdminContext shape; Middleware constructor signature — needed to compose chain in test AND to decide whether to factor a testable AuthFunc seam)
    - gateway/internal/httpx/recoverer.go (Recoverer wrap pattern + Sentry chain + WriteOpenAIError body shape)
    - 11-PATTERNS.md sections for debug.go + debug_panic.go (handler skeleton + wrap order invariant)
    - 11-RESEARCH.md Pattern 1 + Architecture Patterns D-18.2 panic path diagram
  </read_first>
  <acceptance_criteria>
    - File gateway/cmd/gatewayctl/debug.go exists; `grep -c "runDebugEmitError" gateway/cmd/gatewayctl/debug.go` returns >= 1; `grep -c "case \"emit-error\"" gateway/cmd/gatewayctl/debug.go` returns 1.
    - File gateway/internal/admin/debug_panic.go exists; `grep -c "synthetic panic emitted by gatewayctl debug emit-error" gateway/internal/admin/debug_panic.go` returns 1; `grep -c "func DebugPanicHandler" gateway/internal/admin/debug_panic.go` returns 1.
    - File gateway/internal/admin/debug_panic_test.go exists with TWO test functions:
      * `TestDebugPanic_Unauthenticated_Returns401Or403` — issues `http.NewRequest("POST", srv.URL+"/admin/debug/panic", nil)` with NO X-Admin-Key header against an httptest.NewServer composing httpx.Recoverer(log) → admin.Middleware(...) → admin.DebugPanicHandler(log); asserts `resp.StatusCode` is 401 OR 403; reads body and asserts it does NOT contain substring "synthetic panic" AND does NOT contain substring "goroutine".
      * `TestDebugPanic_Authenticated_Returns500ViaRecoverer` — same composed chain (or hermetic variant injecting an AdminContext via a test seam); issues authenticated POST; asserts `resp.StatusCode == 500`; parses body as JSON and asserts `error.type == "api_error"` AND `error.code == "internal_error"`; asserts body does NOT contain "synthetic panic" OR "goroutine".
    - `grep -E "^[[:space:]]*t\.Skip" gateway/internal/admin/debug_panic_test.go | wc -l` returns 0 (no skips).
    - `grep -E "^[[:space:]]*t\.Skip" gateway/cmd/gatewayctl/debug_test.go | wc -l` returns 0 (no skips).
    - gateway/cmd/gateway/main.go registers POST /admin/debug/panic under the admin chain — `grep -c "/admin/debug/panic" gateway/cmd/gateway/main.go` returns >= 1.
    - gateway/cmd/gatewayctl/main.go switch contains `case "debug"` — `grep -c "case \"debug\"" gateway/cmd/gatewayctl/main.go` returns 1.
    - `cd gateway && go test ./internal/admin/ -run TestDebugPanic -count=1 -race -timeout=60s` exits 0 with BOTH cases reported PASS.
    - `cd gateway && go test ./cmd/gatewayctl/ -run TestRunDebug -count=1 -race -timeout=60s` exits 0.
    - `cd gateway && go vet ./...` clean.
    - `cd gateway && go build ./cmd/gatewayctl/ && go build ./cmd/gateway/` both succeed.
  </acceptance_criteria>
  <action>
    Create gateway/cmd/gatewayctl/debug.go per 11-PATTERNS.md debug.go pattern. Package main. Define runDebug(ctx context.Context, args []string, log *slog.Logger) int dispatcher: usage error when args empty; switch arg[0] case "emit-error" returns runDebugEmitError; default unknown subcommand exit 2. Define runDebugEmitError with flag.NewFlagSet("debug emit-error", flag.ExitOnError), --gateway string defaulting to env AI_GATEWAY_URL, --admin-key string defaulting to env AI_GATEWAY_ADMIN_KEY, both required (exit 2 on missing). http.NewRequestWithContext POST {gateway}/admin/debug/panic with header X-Admin-Key. http.DefaultClient.Do with a 10s timeout. On net error log error and return 1. Assert resp.StatusCode == 500 (Recoverer always returns 500 on panic) otherwise log expected vs got and return 1. On success log.Info("debug emit-error sent", "gateway", gwURL, "status", resp.StatusCode) and return 0. NEVER log the admin key value.

    Create gateway/cmd/gatewayctl/debug_test.go — real assertions, NOT placeholder. Cases:
    - TestRunDebug_MissingFlags_ExitsTwo: invoke runDebugEmitError with empty args + clean env → expect return 2.
    - TestRunDebug_Success_ExitsZero: httptest.NewServer that returns 500 on POST /admin/debug/panic; invoke runDebugEmitError pointing at it → expect return 0.
    - TestRunDebug_WrongStatus_ExitsOne: httptest.NewServer that returns 200 → expect return 1.

    Create gateway/internal/admin/debug_panic.go per 11-PATTERNS.md handler skeleton. Package admin. Package doc comment states the handler is operator-only synthetic panic emitter mounted at POST /admin/debug/panic under admin auth + Recoverer; if a refactor inverts wrap order the panic crashes the process. DebugPanicHandler(log *slog.Logger) http.Handler returns http.HandlerFunc that: calls ac, _ := FromContext(r.Context()), log.WarnContext with admin_key_id and label fields when ac present, then panic("synthetic panic emitted by gatewayctl debug emit-error").

    Create gateway/internal/admin/debug_panic_test.go — automated HTTP integration test (reviews HIGH #1 gate). Compose the real chain via standard net/http wrapping (or chi if already in use): `handler := httpx.Recoverer(log)(admin.Middleware(deps)(admin.DebugPanicHandler(log)))`. Bind via `httptest.NewServer(handler)`. Two test funcs:
    1. TestDebugPanic_Unauthenticated_Returns401Or403: http.NewRequest POST srv.URL+"/admin/debug/panic" with NO X-Admin-Key; http.DefaultClient.Do; assert StatusCode ∈ {401, 403}; io.ReadAll(resp.Body); assert NOT contains "synthetic panic" + NOT contains "goroutine".
    2. TestDebugPanic_Authenticated_Returns500ViaRecoverer: seed an admin key via the existing test helper in `gateway/internal/admin/middleware_test.go` IF one exists; otherwise factor a minimal hermetic seam — either (a) add a small `WithAuthFunc(fn)` option to admin.Middleware that the test passes a canned auth function to, or (b) bypass middleware in the test and assert the Recoverer+handler pair alone returns 500 with sanitized body — and document that the unauth case (test 1) covers the middleware enforcement. Whichever path: build the request with valid auth, assert StatusCode == 500, parse body as JSON, assert error.type == "api_error" AND error.code == "internal_error", assert body NOT contains "synthetic panic" + NOT contains "goroutine".

    NEITHER test may use t.Skip. If a hermetic seam is genuinely impossible without live infra (it should be possible — Recoverer + a panicking http.Handler need no DB), surface in SUMMARY and STOP rather than skipping.

    Edit gateway/cmd/gatewayctl/main.go: add case "debug" in switch calling os.Exit(runDebug(ctx, args, log)). Extend usage() to mention `debug emit-error` and `key list`.

    Edit gateway/cmd/gateway/main.go: register POST /admin/debug/panic in the router under the admin chain so X-Admin-Key middleware fires before the handler. Wrap order MUST be Recoverer(adminMiddleware(DebugPanicHandler)) — Recoverer outermost per debug_panic.go docstring invariant. If gateway router uses chi.Mux, register via r.With(httpx.Recoverer(log), admin.Middleware(...)).Post("/admin/debug/panic", admin.DebugPanicHandler(log).ServeHTTP). Locate the existing admin mount point (e.g. `/admin/metrics`) and mirror its registration style.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/admin/ -run TestDebugPanic -count=1 -race -timeout=60s -v 2>&1 | tee /tmp/dp.log && grep -E "PASS.*TestDebugPanic_Unauthenticated_Returns401Or403|--- PASS: TestDebugPanic_Unauthenticated_Returns401Or403" /tmp/dp.log && grep -E "PASS.*TestDebugPanic_Authenticated_Returns500ViaRecoverer|--- PASS: TestDebugPanic_Authenticated_Returns500ViaRecoverer" /tmp/dp.log && go test ./cmd/gatewayctl/ -run TestRunDebug -count=1 -race -timeout=60s && go vet ./... && go build ./cmd/gatewayctl/ && go build ./cmd/gateway/ && grep -c "synthetic panic emitted by gatewayctl debug emit-error" internal/admin/debug_panic.go | grep -q "^1$" && grep -c "/admin/debug/panic" cmd/gateway/main.go | awk '{if ($1 >= 1) exit 0; else exit 1}' && grep -c "case \"debug\"" cmd/gatewayctl/main.go | grep -q "^1$" && grep -cE "^[[:space:]]*t\.Skip" internal/admin/debug_panic_test.go | grep -q "^0$"</automated>
  </verify>
  <done>Both TestDebugPanic_Unauthenticated_Returns401Or403 AND TestDebugPanic_Authenticated_Returns500ViaRecoverer pass with real assertions (not Skip); debug_test.go tests pass; go vet clean; gatewayctl + gateway binaries build; /admin/debug/panic registered in gateway main.go under admin chain; usage() extended.</done>
</task>

<task type="auto" tdd="true">
  <id>11-04-02</id>
  <title>Task 2 [reviews MEDIUM #2 + LOW #4]: gatewayctl key list subcommand consuming the new WithMeta queries (real stub test, no Skip)</title>
  <files>gateway/cmd/gatewayctl/key.go, gateway/cmd/gatewayctl/key_test.go</files>
  <read_first>
    - gateway/cmd/gatewayctl/key.go (current dispatcher lines 17-31; create/revoke subcommands already present)
    - gateway/cmd/gatewayctl/admin_key.go (runAdminKeyList lines 240-280 — exact mirror target)
    - gateway/internal/db/gen/auth.sql.go (verify Task 0 produced ListActiveKeysAllWithMeta + ListActiveKeysByTenantWithMeta and their Row structs)
    - gateway/internal/db/gen/querier.go (Querier interface — the production wrapper to be stubbed in the unit test)
    - 11-PATTERNS.md key.go (insertion sites + runKeyList target body)
    - 11-CONTEXT.md D-18.3
  </read_first>
  <acceptance_criteria>
    - `grep -c "case \"list\":" gateway/cmd/gatewayctl/key.go` returns 1.
    - `grep -c "runKeyList" gateway/cmd/gatewayctl/key.go` returns >= 2 (dispatcher arm + func definition).
    - `grep -E "key_prefix|KeyPrefix" gateway/cmd/gatewayctl/key.go | wc -l` returns >= 1; `grep -E "key_hash|KeyHash" gateway/cmd/gatewayctl/key.go | wc -l` returns 0 (no secret-bearing column referenced).
    - Usage line lists `create|revoke|list`: `grep -c "create|revoke|list" gateway/cmd/gatewayctl/key.go` returns >= 1.
    - gateway/cmd/gatewayctl/key_test.go contains a real TestRunKeyList_* test (not Skip):
      * Defines a small `keyLister` interface (or reuses an existing one) consumed by runKeyList.
      * Test stub returns 2 canned Rows with distinct tenant slugs.
      * Test captures os.Stdout via os.Pipe, invokes runKeyList against the stub, reads the captured output, asserts header line contains "ID" + "TENANT" + "PREFIX" + "STATUS" + "DATA_CLASS" + "CREATED" + "LAST_USED".
      * Asserts the captured stdout contains EXACTLY 2 data rows.
      * Asserts the captured stdout does NOT contain substring "key_hash".
    - `grep -cE "^[[:space:]]*t\.Skip" gateway/cmd/gatewayctl/key_test.go` returns 0.
    - `cd gateway && go test ./cmd/gatewayctl/ -run TestRunKeyList -count=1 -race -timeout=60s` exits 0.
    - `cd gateway && go vet ./... && go build ./cmd/gatewayctl/` succeed.
  </acceptance_criteria>
  <action>
    Step 1 — Edit gateway/cmd/gatewayctl/key.go:
    - Dispatcher switch lines 22-30 add `case "list":` returning `runKeyList(ctx, args[1:], log)`.
    - Usage string line 19 extend to "Usage: gatewayctl key create|revoke|list [flags]".
    - After runKeyRevoke (line 150 area) append runKeyList(ctx context.Context, args []string, log *slog.Logger) int mirroring admin_key.go:runAdminKeyList.
      Inside: flag.NewFlagSet("key list", flag.ExitOnError); --tenant string default ""; fs.Parse. loadAndPool(ctx, log) then defer pool.Close; q := gen.New(pool).
      If tenant slug provided, resolve slug → tenant UUID via existing helper (look for resolveTenantSlug in tenant.go OR query tenants table directly). Then rows = q.ListActiveKeysByTenantWithMeta(ctx, tenantUUID). Else rows = q.ListActiveKeysAllWithMeta(ctx).
      Use a small private interface `keyLister` with the two methods so the test can substitute a stub:
        type keyLister interface { ListActiveKeysAllWithMeta(ctx context.Context) ([]gen.ListActiveKeysAllWithMetaRow, error); ListActiveKeysByTenantWithMeta(ctx context.Context, tenantID uuid.UUID) ([]gen.ListActiveKeysByTenantWithMetaRow, error) }
      Factor the rendering into a `renderKeyList(out io.Writer, rows []someRowType) error` helper so the test can drive rendering directly.
      tabwriter.NewWriter(os.Stdout, 0, 2, 2, ' ', 0). Header line "ID\tTENANT\tPREFIX\tSTATUS\tDATA_CLASS\tCREATED\tLAST_USED". Per row fmt.Fprintf with ID.String, TenantSlug, KeyPrefix, Status, DataClass, CreatedAt.UTC().Format(time.RFC3339), LastUsedAt.Time.UTC().Format(time.RFC3339) when r.LastUsedAt.Valid else "-". Return tw.Flush() exit code.

    Step 2 — Write gateway/cmd/gatewayctl/key_test.go TestRunKeyList suite (REAL test, not Skip — reviews LOW #4):
    - Define a `stubKeyLister` type implementing the keyLister interface, returning a canned slice of 2 ListActiveKeysAllWithMetaRow values with distinct tenant slugs (e.g. "uat10-test", "chat-ifix"), known UUIDs, KeyPrefix values like "ifix_sk_aaaa****1111", status "active", data_class "normal"/"sensitive", CreatedAt 2026-01-01T00:00:00Z, LastUsedAt valid 2026-05-01T12:00:00Z.
    - TestRunKeyList_NoTenantFilter_RendersAllRows: invoke renderKeyList (or runKeyList wired to the stub) with the canned rows; capture stdout via os.Pipe + io.ReadAll OR pass a bytes.Buffer to renderKeyList directly; assert header contains all 7 column tokens; assert output has exactly 2 data lines (count non-header non-empty lines); assert output does NOT contain "key_hash".
    - TestRunKeyList_NoSkipPlaceholders: meta-test or simply omit any t.Skip call — verified by the grep gate above.

    Pure-unit path (preferred) — exercise `renderKeyList(&buf, stubRows)` directly. This avoids the loadAndPool path and keeps the test hermetic + fast. Document in a comment that the loadAndPool-backed integration variant is exercised in Phase 11-06 / 11-10 live UAT.
  </action>
  <verify>
    <automated>cd gateway && go test ./cmd/gatewayctl/ -run TestRunKeyList -count=1 -race -timeout=60s -v && go vet ./... && go build ./cmd/gatewayctl/ && grep -c "case \"list\":" cmd/gatewayctl/key.go | grep -q "^1$" && grep -c "runKeyList" cmd/gatewayctl/key.go | awk '{if ($1 >= 2) exit 0; else exit 1}' && grep -cE "key_hash|KeyHash" cmd/gatewayctl/key.go | grep -q "^0$" && grep -cE "^[[:space:]]*t\.Skip" cmd/gatewayctl/key_test.go | grep -q "^0$"</automated>
  </verify>
  <done>auth.sql + sqlc regen consumed by runKeyList via WithMeta queries; go test TestRunKeyList passes with real stub-backed assertions (no Skip); go vet clean; gatewayctl binary builds; no key_hash references in key.go; usage updated.</done>
</task>

<task type="auto">
  <id>11-04-03</id>
  <title>Task 3 [reviews MEDIUM #3]: smoke-sensitive-failover.py race fix + parameterized gatewayctl repoint (D-18.1 + env-var portability)</title>
  <files>scripts/integration-smoke/smoke-sensitive-failover.py</files>
  <read_first>
    - scripts/integration-smoke/smoke-sensitive-failover.py (full file, focus on Constants block, ensure_tier0_open function at line 269, induce_failure_via_gatewayctl at lines 307-327, docstring at line 193)
    - 11-PATTERNS.md smoke-sensitive-failover.py (4 insertion sites table + Pitfall 8 defensive whitelist)
    - 11-RESEARCH.md Pitfall 8 + State of the Art "Deprecated" row (gatewayctl breaker force-open landed Phase 06.9 Plan 04 at gateway/cmd/gatewayctl/breaker.go:117)
    - gateway/cmd/gatewayctl/breaker.go (force-open subcommand surface: --upstream=NAME --ttl=DURATION)
  </read_first>
  <acceptance_criteria>
    - `grep -c "OPEN_LIKE_STATES = frozenset" scripts/integration-smoke/smoke-sensitive-failover.py` returns 1.
    - OPEN_LIKE_STATES contains exactly {"open", "forced-open", "FORCED_OPEN"} — `grep -A 1 "OPEN_LIKE_STATES = frozenset" scripts/integration-smoke/smoke-sensitive-failover.py` shows those three literals.
    - Defensive assertions present — `grep -E "assert .closed. not in OPEN_LIKE_STATES" scripts/integration-smoke/smoke-sensitive-failover.py | wc -l` returns >= 1 AND `grep -E "assert .half-open. not in OPEN_LIKE_STATES" scripts/integration-smoke/smoke-sensitive-failover.py | wc -l` returns >= 1.
    - Env-driven constants declared — `grep -cE "GATEWAYCTL_PATH = os.environ.get|GATEWAYCTL_CONTAINER = os.environ.get|GATEWAYCTL_SSH_HOST = os.environ.get" scripts/integration-smoke/smoke-sensitive-failover.py` returns 3.
    - Defaults documented in the module docstring — `grep -E "GATEWAYCTL_PATH|GATEWAYCTL_CONTAINER|GATEWAYCTL_SSH_HOST" scripts/integration-smoke/smoke-sensitive-failover.py | wc -l` returns >= 6 (3 declarations + at least 3 docstring mentions).
    - induce_failure_via_gatewayctl uses subprocess.run with breaker force-open — `grep -E "subprocess.run.*breaker|breaker.*force-open" scripts/integration-smoke/smoke-sensitive-failover.py | wc -l` returns >= 1.
    - No hard-coded `n8n-ia-vm` host in induce_failure_via_gatewayctl body — `awk '/def induce_failure_via_gatewayctl/,/^def /' scripts/integration-smoke/smoke-sensitive-failover.py | grep -c "n8n-ia-vm" | grep -q "^0$"`.
    - `python scripts/integration-smoke/smoke-sensitive-failover.py --help` exits 0.
  </acceptance_criteria>
  <action>
    Edit scripts/integration-smoke/smoke-sensitive-failover.py per 11-PATTERNS.md 4 insertion sites + reviews MEDIUM #3 parameterization:

    1. Constants block (after line 116 AUDIT_UPSTREAM_BLOCKED): add `OPEN_LIKE_STATES = frozenset({"open", "forced-open", "FORCED_OPEN"})`. Use frozenset (immutable) per Pattern A. Add defensive module-level assertions immediately after: `assert "closed" not in OPEN_LIKE_STATES; assert "half-open" not in OPEN_LIKE_STATES` (Pitfall 8 defensive whitelist). Also declare parameterization env constants right below:
       GATEWAYCTL_PATH = os.environ.get("GATEWAYCTL_PATH", "/gatewayctl")
       GATEWAYCTL_CONTAINER = os.environ.get("GATEWAYCTL_CONTAINER", "ifix-ai-gateway")
       GATEWAYCTL_SSH_HOST = os.environ.get("GATEWAYCTL_SSH_HOST", "")  # empty = run locally via docker exec

    2. Module docstring (line 193 region): extend with an "Environment overrides" subsection that documents the three env vars + their defaults + example invocations:
       - default (local docker exec): bare invocation, runs `docker exec ifix-ai-gateway /gatewayctl ...`
       - prod-from-ops-claude: `GATEWAYCTL_SSH_HOST=n8n-ia-vm python smoke-sensitive-failover.py ...`
       - custom binary path: `GATEWAYCTL_PATH=/usr/local/bin/gatewayctl python smoke-sensitive-failover.py ...`
       This addresses reviews MEDIUM #3 (no hard-coded n8n-ia-vm).

    3. ensure_tier0_open function (line 269 region): replace condition `last_state == "open"` with `last_state in OPEN_LIKE_STATES` so polling accepts the equivalent open-like variants (D-18.1 + D-09).

    4. induce_failure_via_gatewayctl function (lines 307-327): replace the existing stub/error-out body with a real subprocess.run invocation. Build the command list parameterized by env:
       if GATEWAYCTL_SSH_HOST:
           cmd = ["ssh", GATEWAYCTL_SSH_HOST, "docker", "exec", GATEWAYCTL_CONTAINER, GATEWAYCTL_PATH, "breaker", "force-open", "--upstream=local-llm", "--ttl=300s"]
       else:
           cmd = ["docker", "exec", GATEWAYCTL_CONTAINER, GATEWAYCTL_PATH, "breaker", "force-open", "--upstream=local-llm", "--ttl=300s"]
       result = subprocess.run(cmd, capture_output=True, text=True, check=True, timeout=30)

       Capture stdout/stderr into structlog. Return success once the force-open audit row is written. DO NOT hard-code `n8n-ia-vm` anywhere in this function — operator sets GATEWAYCTL_SSH_HOST=n8n-ia-vm at invocation time when targeting prod.

    Preserve EVERY other line of the file unchanged (CR-01 invariant, schema validation tail, threat-T-09-* annotations).
  </action>
  <verify>
    <automated>grep -c "OPEN_LIKE_STATES = frozenset" scripts/integration-smoke/smoke-sensitive-failover.py | grep -q "^1$" && grep -cE "GATEWAYCTL_PATH = os.environ.get|GATEWAYCTL_CONTAINER = os.environ.get|GATEWAYCTL_SSH_HOST = os.environ.get" scripts/integration-smoke/smoke-sensitive-failover.py | grep -q "^3$" && grep -cE "subprocess.run.*breaker|breaker.*force-open" scripts/integration-smoke/smoke-sensitive-failover.py | awk '{if ($1 >= 1) exit 0; else exit 1}' && awk '/def induce_failure_via_gatewayctl/,/^def /' scripts/integration-smoke/smoke-sensitive-failover.py | grep -c "n8n-ia-vm" | grep -q "^0$" && python scripts/integration-smoke/smoke-sensitive-failover.py --help 2>&1 | head -5</automated>
  </verify>
  <done>OPEN_LIKE_STATES frozenset declared with exactly the 3 expected states; "closed" and "half-open" absent (assertion lines present); GATEWAYCTL_PATH/CONTAINER/SSH_HOST env vars wired (parameterization); induce_failure_via_gatewayctl invokes subprocess.run with `breaker force-open --upstream=local-llm --ttl=300s`, no hard-coded n8n-ia-vm; --help still works; docstring documents env overrides with examples; existing 8-section spine preserved.</done>
</task>

</tasks>

<verification>
- `cd gateway && go test ./... -count=1 -race -timeout=120s` exits 0 (debug_panic_test 2 cases + key_test TestRunKeyList all pass with real assertions; no Skip).
- `cd gateway && go build ./cmd/gatewayctl/ && go build ./cmd/gateway/` both succeed.
- `cd gateway && sqlc generate` exits 0 and produces no diff after the Task 0 regen (idempotent).
- grep "OPEN_LIKE_STATES = frozenset" scripts/integration-smoke/smoke-sensitive-failover.py returns 1.
- grep "GATEWAYCTL_PATH\|GATEWAYCTL_CONTAINER\|GATEWAYCTL_SSH_HOST" scripts/integration-smoke/smoke-sensitive-failover.py returns ≥6 (3 declarations + 3 docstring mentions).
- grep "breaker.*force-open" scripts/integration-smoke/smoke-sensitive-failover.py returns at least 1.
- grep "synthetic panic emitted by gatewayctl debug emit-error" gateway/internal/admin/debug_panic.go returns 1.
- grep -E "^[[:space:]]*t\.Skip" in gateway/internal/admin/debug_panic_test.go AND gateway/cmd/gatewayctl/key_test.go AND gateway/cmd/gatewayctl/debug_test.go ALL return 0 (real tests, no placeholders).
- grep "ListActiveKeysAllWithMeta\|ListActiveKeysByTenantWithMeta" gateway/db/queries/auth.sql returns ≥2 (non-comment lines).
- grep "ListActiveKeysAllWithMeta\|ListActiveKeysByTenantWithMeta" gateway/internal/db/gen/auth.sql.go returns ≥2 (func definitions).
- grep "ListActiveKeysAllWithMeta\|ListActiveKeysByTenantWithMeta" gateway/internal/db/gen/querier.go returns ≥2 (interface method signatures).
- grep "/admin/debug/panic" gateway/cmd/gateway/main.go returns ≥1 (route registered).
- Live UAT (deferred to 11-10): gatewayctl debug emit-error → Sentry event lands within 5s with admin_key_id label.
</verification>

<success_criteria>
- 3 new Go files (debug.go, debug_test.go, debug_panic.go) + 1 new test file (debug_panic_test.go) + 5 modified files (key.go, key_test.go, gatewayctl/main.go, gateway/main.go, auth.sql) + 2 regenerated sqlc Go files (gen/auth.sql.go, gen/querier.go) + 1 modified Python file (smoke-sensitive-failover.py).
- /admin/debug/panic has TWO automated tests passing in CI: unauth → 401/403 AND auth → 500-via-Recoverer with sanitized body (reviews HIGH #1).
- gatewayctl key list backed by new WithMeta queries with explicit column coverage; sqlc regen committed; querier.go interface extended (reviews MEDIUM #2).
- smoke-sensitive-failover.py: D-09 race fix + D-18.1 repoint + env-var parameterization (no hard-coded n8n-ia-vm) complete (reviews MEDIUM #3).
- All gatewayctl + admin unit tests pass with REAL assertions (zero t.Skip in any new test file); binaries build (reviews LOW #4).
- D-18.1, D-18.2, D-18.3 implementations grep-able.
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies (sentry-go, slog, text/tabwriter already in tree). Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.
- Truth 2: No speculative language in code comments.
- Truth 3: Chaos scripts (vast-delete.sh, openrouter-iptables-drop.sh) live in 11-07/11-08 plans.
- Truth 4: Vast API key never literal in shell history (smoke uses env var).
- Truth 5: This plan is autonomous (no live UAT — that happens in 11-10).
- Truth 8 (cross-cutting): NO raw API keys, raw key_hash values, full Authorization headers, or DSNs in test output or query projections. New WithMeta queries select key_prefix only (never key_hash); key_test.go asserts absence of `key_hash` substring in captured stdout.
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-04-SUMMARY.md when done with: files modified (including the regenerated gen/auth.sql.go + gen/querier.go), go test output snippet showing TestDebugPanic 2 cases PASS + TestRunKeyList PASS + TestRunDebug PASS, grep evidence for the 3 D-18 fold sub-items + WithMeta queries + env-var parameterization, and a confirmation line stating "no t.Skip in new test files".
</output>
