---
phase: 11
plan: 11-04
slug: gatewayctl-and-phase10-fold
wave: 1
depends_on: []
requirements: [PRD-04]
secondary_decisions: [D-18.1, D-18.2, D-18.3]
files_modified:
  - path: gateway/cmd/gatewayctl/debug.go
    kind: NEW
  - path: gateway/cmd/gatewayctl/debug_test.go
    kind: NEW
  - path: gateway/cmd/gatewayctl/key.go
    kind: MOD
  - path: gateway/cmd/gatewayctl/key_test.go
    kind: MOD
  - path: gateway/cmd/gatewayctl/main.go
    kind: MOD
  - path: gateway/internal/admin/debug_panic.go
    kind: NEW
  - path: scripts/integration-smoke/smoke-sensitive-failover.py
    kind: MOD
autonomous: true
estimated_duration: 120
---

<objective>
Fold the Phase 10 deferred items (D-18.1, D-18.2, D-18.3 — D-18.4 doc lives in 11-05): smoke-sensitive-failover.py race-fix accepting FORCED_OPEN as equivalent to natural-open AND repointing the gatewayctl induce mode to the now-shipped `gatewayctl breaker force-open` from Phase 06.9 Plan 04; `gatewayctl debug emit-error` subcommand exercising the httpx.Recoverer + Sentry chain; `gatewayctl key list` operator inspection table. All three contribute to PRD-04 full-runbook diagnostic surface that 11-09 consumes.
</objective>

<must_haves>
  truths:
    - "D-18.1: smoke-sensitive-failover.py polling loop accepts {open, forced-open, FORCED_OPEN}; defensive whitelist explicit (Pitfall 8); closed and half-open NEVER in accepted set"
    - "D-18.1 second half: gatewayctl induce mode invokes real `breaker force-open` (Phase 06.9 Plan 04 landed at gateway/cmd/gatewayctl/breaker.go:117)"
    - "D-18.2: gatewayctl debug emit-error -> POST /admin/debug/panic -> httpx.Recoverer catches -> sentry.CurrentHub().Recover + Flush(500ms) -> event lands in Sentry ifix-ai-gateway-prod (id 4511455942017024)"
    - "D-18.3: gatewayctl key list returns aligned table via stdlib text/tabwriter; never prints raw key (only key_prefix); RFC3339 UTC timestamps"
    - "Pattern D 5 invariants honored: runCmd(ctx, args, log) int signature, exit codes 0/1/2, flag.NewFlagSet, loadAndPool + defer pool.Close, stdout=parseable + stderr=log"
  must_haves:
    - gateway/cmd/gatewayctl/debug.go exists with runDebug dispatcher + runDebugEmitError function
    - gateway/cmd/gatewayctl/main.go switch includes case "debug" + extended usage()
    - gateway/internal/admin/debug_panic.go exports DebugPanicHandler that panics inside Recoverer chain
    - gateway/cmd/gatewayctl/key.go dispatcher includes case "list" -> runKeyList; usage string lists create|revoke|list
    - scripts/integration-smoke/smoke-sensitive-failover.py constants block declares OPEN_LIKE_STATES frozenset with exactly {"open","forced-open","FORCED_OPEN"}
    - smoke-sensitive-failover.py induce_failure_via_gatewayctl invokes subprocess.run with gatewayctl breaker force-open --upstream=local-llm --ttl=300s
  key_links:
    - from: gateway/cmd/gatewayctl/debug.go
      to: gateway/internal/admin/debug_panic.go
      via: HTTP POST /admin/debug/panic with X-Admin-Key header
      pattern: "/admin/debug/panic"
    - from: gateway/internal/admin/debug_panic.go
      to: gateway/internal/httpx/recoverer.go
      via: panic() recovered by Recoverer middleware wrapping admin chain
      pattern: "Recoverer"
    - from: gateway/cmd/gatewayctl/key.go runKeyList
      to: gateway/db/queries/auth.sql ListActiveKeysByTenant / ListActiveKeysAll
      via: sqlc-generated Go queries via loadAndPool + gen.New(pool)
      pattern: "ListActiveKeys"
    - from: scripts/integration-smoke/smoke-sensitive-failover.py induce mode
      to: gateway/cmd/gatewayctl/breaker.go force-open subcommand
      via: subprocess.run gatewayctl breaker force-open
      pattern: "breaker.*force-open"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> /admin/debug/panic | X-Admin-Key gated; synthetic panic exercises Recoverer chain |
| gateway container -> Sentry org Ifix project ifix-ai-gateway-prod | panic events with admin context (admin_key_id label) |
| operator -> DB read | gatewayctl key list reads ai_gateway.api_keys via sqlc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-OPS-01 | Elevation of Privilege | /admin/debug/panic unauthenticated | mitigate | Handler mounted under admin.Middleware (X-Admin-Key required); admin auth gate already audited Phase 02; debug_panic.go retrieves AdminContext via FromContext for breadcrumb |
| T-11-OPS-02 | Information Disclosure | gatewayctl key list raw key leak | mitigate | Print key_prefix only (never raw API key); stdlib text/tabwriter; UI-SPEC line 258 and 11-PATTERNS.md key.go runKeyList |
| T-11-OPS-03 | Tampering | breaker force-open via smoke harness without audit | mitigate | gateway/cmd/gatewayctl/breaker.go:117 force-open writes Redis key + audit row (Phase 06.9); smoke now invokes the audited path instead of stub |
| T-11-OPS-04 | Denial of Service | panic handler crashes if Recoverer not in chain | mitigate | debug_panic.go docstring explicit about wrap order (Recoverer outermost); test enforces 500 response (not crash) |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@gateway/cmd/gatewayctl/admin_key.go
@gateway/cmd/gatewayctl/admin_key_test.go
@gateway/cmd/gatewayctl/breaker.go
@gateway/cmd/gatewayctl/key.go
@gateway/cmd/gatewayctl/main.go
@gateway/internal/httpx/recoverer.go
@gateway/internal/admin/middleware.go
@gateway/db/queries/auth.sql
@scripts/integration-smoke/smoke-sensitive-failover.py
</context>

<interfaces>
From gateway/cmd/gatewayctl/admin_key.go:
- runAdminKey(ctx context.Context, args []string, log *slog.Logger) int
- runAdminKeyList(ctx context.Context, args []string, log *slog.Logger) int  (pattern for runKeyList)

From gateway/cmd/gatewayctl/breaker.go (Phase 06.9 Plan 04, verified line 117):
- runBreakerForceOpen(ctx context.Context, args []string, log *slog.Logger) int
- flags: --upstream=NAME --ttl=DURATION

From gateway/internal/admin/middleware.go:
- FromContext(ctx context.Context) (*AdminContext, bool)
- AdminContext has AdminKeyID uuid.UUID and Label string

From gateway/internal/httpx/recoverer.go:
- Recoverer(base *slog.Logger) func(http.Handler) http.Handler — recovers panic, calls sentry.CurrentHub().Recover(rec) + sentry.Flush(500ms), then WriteOpenAIError(w, 500, "api_error", "internal_error", ...)

From gateway/db/queries/auth.sql (verified):
- ListActiveKeysByTenant :many
- ListActiveKeysAll :many
- GetAPIKeyByID :one
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <id>11-04-01</id>
  <title>Task 1: gatewayctl debug subcommand + /admin/debug/panic handler</title>
  <files>gateway/cmd/gatewayctl/debug.go, gateway/cmd/gatewayctl/debug_test.go, gateway/internal/admin/debug_panic.go, gateway/cmd/gatewayctl/main.go</files>
  <read_first>
    - gateway/cmd/gatewayctl/admin_key.go (dispatcher pattern lines 50-66 + flag parsing)
    - gateway/cmd/gatewayctl/admin_key_test.go (placeholder test pattern, full 14 lines)
    - gateway/cmd/gatewayctl/main.go (switch dispatch lines 48-101 + usage() lines 20-46)
    - gateway/internal/admin/middleware.go (FromContext + AdminContext shape)
    - gateway/internal/httpx/recoverer.go (Recoverer wrap pattern + Sentry chain)
    - 11-PATTERNS.md sections for debug.go + debug_panic.go (handler skeleton + wrap order invariant)
    - 11-RESEARCH.md Pattern 1 + Architecture Patterns D-18.2 panic path diagram
  </read_first>
  <behavior>
    - Test 1 (debug_test.go): TestDebugEmitErrorPlaceholder skipped (integration coverage in 11 HUMAN-UAT) — same shape as admin_key_test.go to keep build green.
    - runDebugEmitError exits 0 when --gateway and --admin-key provided AND POST returns 500; exits 1 when status != 500; exits 2 when flags missing.
    - DebugPanicHandler panics with literal string "synthetic panic emitted by gatewayctl debug emit-error" (Sentry event search-string).
  </behavior>
  <action>
    Create gateway/cmd/gatewayctl/debug.go per 11-PATTERNS.md debug.go pattern. Package main. Define runDebug(ctx context.Context, args []string, log *slog.Logger) int dispatcher: usage error when args empty; switch arg[0] case "emit-error" returns runDebugEmitError; default unknown subcommand exit 2. Define runDebugEmitError with flag.NewFlagSet("debug emit-error", flag.ExitOnError), --gateway string defaulting to env AI_GATEWAY_URL, --admin-key string defaulting to env AI_GATEWAY_ADMIN_KEY, both required (exit 2 on missing). http.NewRequestWithContext POST {gateway}/admin/debug/panic with header X-Admin-Key. http.DefaultClient.Do. On net error log error and return 1. Assert resp.StatusCode == 500 (Recoverer always returns 500 on panic) otherwise log expected vs got and return 1. On success log.Info("debug emit-error sent", "gateway", gwURL, "status", resp.StatusCode) and return 0.

    Create gateway/cmd/gatewayctl/debug_test.go per 11-PATTERNS.md placeholder skip pattern: package main_test; import testing; TestDebugEmitErrorPlaceholder calls t.Skip("integration coverage lives in 11 HUMAN-UAT against deployed gateway").

    Create gateway/internal/admin/debug_panic.go per 11-PATTERNS.md handler skeleton. Package admin. Package doc comment states the handler is operator-only synthetic panic emitter mounted at POST /admin/debug/panic under admin auth + Recoverer; if a refactor inverts wrap order the panic crashes the process. DebugPanicHandler(log *slog.Logger) http.Handler returns http.HandlerFunc that: calls ac, _ := FromContext(r.Context()), log.WarnContext with admin_key_id and label fields, then panic("synthetic panic emitted by gatewayctl debug emit-error").

    Edit gateway/cmd/gatewayctl/main.go: add case "debug" in switch (lines 48-101) calling os.Exit(runDebug(ctx, args, log)). Extend usage() to mention debug emit-error and key list.

    Wire the handler at POST /admin/debug/panic in gateway/cmd/gateway/main.go router under the admin chain so X-Admin-Key middleware fires before the handler. Wrap order MUST be Recoverer(adminMiddleware(DebugPanicHandler)) — Recoverer outermost per debug_panic.go docstring invariant. If gateway router uses chi.Mux, register via r.With(httpx.Recoverer(log), admin.Middleware(...)).Post("/admin/debug/panic", admin.DebugPanicHandler(log).ServeHTTP).
  </action>
  <verify>
    <automated>cd gateway && go test ./cmd/gatewayctl/... -run TestDebugEmitErrorPlaceholder -count=1 && go vet ./... && go build ./cmd/gatewayctl/ && go build ./cmd/gateway/</automated>
  </verify>
  <done>go test runs Skip on TestDebugEmitErrorPlaceholder (exit 0); go vet clean; gatewayctl + gateway binaries build; grep "synthetic panic emitted by gatewayctl debug emit-error" gateway/internal/admin/debug_panic.go returns 1; grep "/admin/debug/panic" gateway/cmd/gateway/main.go returns at least 1; grep for case "debug" in gateway/cmd/gatewayctl/main.go returns 1.</done>
</task>

<task type="auto" tdd="true">
  <id>11-04-02</id>
  <title>Task 2: gatewayctl key list subcommand</title>
  <files>gateway/cmd/gatewayctl/key.go, gateway/cmd/gatewayctl/key_test.go</files>
  <read_first>
    - gateway/cmd/gatewayctl/key.go (current dispatcher lines 17-31; create/revoke subcommands already present)
    - gateway/cmd/gatewayctl/admin_key.go (runAdminKeyList lines 240-280 — exact mirror target)
    - gateway/db/queries/auth.sql (ListActiveKeysByTenant + ListActiveKeysAll definitions)
    - gateway/internal/db/gen/ (sqlc-generated Go signatures: ListActiveKeysByTenantRow, ListActiveKeysAllRow)
    - 11-PATTERNS.md key.go (insertion sites + runKeyList target body)
    - 11-CONTEXT.md D-18.3
  </read_first>
  <behavior>
    - Test 1 (key_test.go): TestRunKeyList compiles and exits via Skip if integration not wired (placeholder; real DB integration test optional).
    - runKeyList outputs aligned header row "ID TENANT PREFIX STATUS DATA_CLASS CREATED LAST_USED" followed by 1 row per active API key.
    - When --tenant SLUG filter is provided, only that tenant's rows print; otherwise all active keys.
    - Raw API key is NEVER printed (only key_prefix column).
  </behavior>
  <action>
    Edit gateway/cmd/gatewayctl/key.go. Step 1: dispatcher switch lines 22-30 add case "list" returning runKeyList(ctx, args[1:], log). Step 2: usage string line 19 extend to "Usage: gatewayctl key create|revoke|list [flags]". Step 3: after runKeyRevoke (line 150 area) append runKeyList(ctx context.Context, args []string, log *slog.Logger) int mirroring admin_key.go:runAdminKeyList lines 244-280. Inside: flag.NewFlagSet("key list", flag.ExitOnError); --tenant string default ""; fs.Parse. loadAndPool(ctx, log) then defer pool.Close; q := gen.New(pool). If tenant slug provided, resolve tenant slug to tenant UUID via existing helper (look for resolveTenantSlug in tenant.go OR query tenants table directly), then rows = q.ListActiveKeysByTenant(ctx, tenantUUID). Else rows = q.ListActiveKeysAll(ctx). tabwriter.NewWriter(os.Stdout, 0, 2, 2, ' ', 0). Header line "ID\tTENANT\tPREFIX\tSTATUS\tDATA_CLASS\tCREATED\tLAST_USED". Per row fmt.Fprintf with ID.String, TenantID.String or TenantSlug if available, KeyPrefix, Status, DataClass, CreatedAt.UTC().Format(time.RFC3339), LastUsedAt.Time.UTC().Format(time.RFC3339) when r.LastUsedAt.Valid else "-". Return tw.Flush() exit code.

    Edit gateway/cmd/gatewayctl/key_test.go: append TestRunKeyList placeholder skip per Pattern D + 11-PATTERNS.md debug_test.go convention. Format: package main_test; TestRunKeyList(t *testing.T) calls t.Skip("integration coverage lives in 11 HUMAN-UAT against deployed gateway").
  </action>
  <verify>
    <automated>cd gateway && go test ./cmd/gatewayctl/... -count=1 -race -timeout=60s && go vet ./... && go build ./cmd/gatewayctl/</automated>
  </verify>
  <done>go test green; go vet clean; gatewayctl binary builds; grep "case \"list\":" gateway/cmd/gatewayctl/key.go returns 1; grep "runKeyList" gateway/cmd/gatewayctl/key.go returns at least 2 (dispatcher + definition); grep "key_prefix\|KeyPrefix" gateway/cmd/gatewayctl/key.go returns at least 1.</done>
</task>

<task type="auto">
  <id>11-04-03</id>
  <title>Task 3: smoke-sensitive-failover.py race fix + gatewayctl repoint (D-18.1)</title>
  <files>scripts/integration-smoke/smoke-sensitive-failover.py</files>
  <read_first>
    - scripts/integration-smoke/smoke-sensitive-failover.py (full file, focus on Constants block, ensure_tier0_open function at line 269, induce_failure_via_gatewayctl at lines 307-327, docstring at line 193)
    - 11-PATTERNS.md smoke-sensitive-failover.py (4 insertion sites table + Pitfall 8 defensive whitelist)
    - 11-RESEARCH.md Pitfall 8 + State of the Art "Deprecated" row (gatewayctl breaker force-open landed Phase 06.9 Plan 04 at gateway/cmd/gatewayctl/breaker.go:117)
    - gateway/cmd/gatewayctl/breaker.go (force-open subcommand surface: --upstream=NAME --ttl=DURATION)
  </read_first>
  <action>
    Edit scripts/integration-smoke/smoke-sensitive-failover.py per 11-PATTERNS.md 4 insertion sites:

    1. Constants block (after line 116 AUDIT_UPSTREAM_BLOCKED): add OPEN_LIKE_STATES = frozenset({"open", "forced-open", "FORCED_OPEN"}). Use frozenset (immutable) per Pattern A. Add defensive module-level assertions immediately after: assert "closed" not in OPEN_LIKE_STATES; assert "half-open" not in OPEN_LIKE_STATES (Pitfall 8 defensive whitelist).

    2. ensure_tier0_open function (line 269 region): replace condition last_state == "open" with last_state in OPEN_LIKE_STATES so polling accepts the equivalent open-like variants (D-18.1 + D-09).

    3. induce_failure_via_gatewayctl function (lines 307-327): replace the existing stub/error-out body with a real subprocess.run invocation. Compose command list ["gatewayctl_path", "breaker", "force-open", "--upstream=local-llm", "--ttl=300s"] where gatewayctl_path comes from config (env GATEWAYCTL_PATH default /gatewayctl when running inside container, /usr/local/bin/gatewayctl on host) or CLI arg. Pass via SSH wrapper if running from ops-claude: subprocess.run(["ssh", "n8n-ia-vm", "docker", "exec", "ifix-ai-gateway", "/gatewayctl", "breaker", "force-open", "--upstream=local-llm", "--ttl=300s"], check=True, timeout=30). Capture stdout/stderr for structlog. Return success once the force-open audit row is written.

    4. Docstring (line 193 region): update mode description for gatewayctl induce — explicit reference to Phase 06.9 Plan 04 force-open subcommand (no longer error-out stub); --ttl=300s rationale (5 minutes covers the smoke run window).

    Preserve EVERY other line of the file unchanged (CR-01 invariant, schema validation tail, threat-T-09-* annotations).
  </action>
  <verify>
    <automated>grep "OPEN_LIKE_STATES = frozenset" scripts/integration-smoke/smoke-sensitive-failover.py && grep -E "subprocess.run.*breaker.*force-open|gatewayctl.*breaker.*force-open" scripts/integration-smoke/smoke-sensitive-failover.py && python -c "import sys; sys.path.insert(0, 'scripts/integration-smoke'); from importlib import import_module; m = __import__('smoke_sensitive_failover' if False else 'sys'); print('module import skipped — direct exec check'); " ; python scripts/integration-smoke/smoke-sensitive-failover.py --help 2>&1 | head -5</automated>
  </verify>
  <done>OPEN_LIKE_STATES frozenset declared with exactly the 3 expected states; "closed" and "half-open" absent from accepted set (assertion lines present); induce_failure_via_gatewayctl invokes subprocess.run with gatewayctl breaker force-open --upstream=local-llm --ttl=300s; --help still works; existing 8-section spine preserved.</done>
</task>

</tasks>

<verification>
- cd gateway && go test ./cmd/gatewayctl/... -count=1 -race -timeout=60s exits 0.
- cd gateway && go build ./cmd/gatewayctl/ && go build ./cmd/gateway/ both succeed.
- grep "OPEN_LIKE_STATES = frozenset" scripts/integration-smoke/smoke-sensitive-failover.py returns 1.
- grep "gatewayctl.*breaker.*force-open" scripts/integration-smoke/smoke-sensitive-failover.py returns at least 1.
- grep "synthetic panic emitted by gatewayctl debug emit-error" gateway/internal/admin/debug_panic.go returns 1.
- Live UAT (deferred to 11-10): gatewayctl debug emit-error → Sentry event lands within 5s with admin_key_id label.
</verification>

<success_criteria>
- 3 new Go files (debug.go, debug_test.go, debug_panic.go) + 3 modified Go files (key.go, key_test.go, main.go + gateway main.go).
- smoke-sensitive-failover.py D-09 race fix + D-18.1 repoint complete.
- All gatewayctl unit tests pass; binaries build.
- D-18.1, D-18.2, D-18.3 implementations grep-able.
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies (sentry-go, slog, text/tabwriter already in tree). Dashboard 2FA UI adds 4 packages scoped to 11-02 only.
- Truth 2: No speculative language in code comments.
- Truth 3: Chaos scripts (vast-delete.sh, openrouter-iptables-drop.sh) live in 11-07/11-08 plans.
- Truth 4: Vast API key never literal in shell history (smoke uses env var).
- Truth 5: This plan is autonomous (no live UAT — that happens in 11-10).
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-04-SUMMARY.md when done with files modified, go test output snippet, grep evidence for the 3 D-18 fold sub-items.
</output>
