---
phase: 11
plan: 11-04
slug: gatewayctl-and-phase10-fold
wave: 1
depends_on: []
requirements: [PRD-04]
secondary_decisions: [D-18.1, D-18.2, D-18.3]
files_modified:
  - path: gateway/cmd/gatewayctl/debug.go
    kind: NEW
  - path: gateway/cmd/gatewayctl/debug_test.go
    kind: NEW
  - path: gateway/cmd/gatewayctl/key.go
    kind: MOD
  - path: gateway/cmd/gatewayctl/key_test.go
    kind: MOD
  - path: gateway/cmd/gatewayctl/main.go
    kind: MOD
  - path: gateway/internal/admin/debug_panic.go
    kind: NEW
  - path: gateway/internal/admin/debug_panic_test.go
    kind: NEW
  - path: gateway/db/queries/auth.sql
    kind: MOD
  - path: scripts/integration-smoke/smoke-sensitive-failover.py
    kind: MOD
autonomous: true
estimated_duration: 150
---

<objective>
Fold the Phase 10 deferred items (D-18.1, D-18.2, D-18.3 — D-18.4 doc lives in 11-05): smoke-sensitive-failover.py race-fix accepting FORCED_OPEN as equivalent to natural-open AND repointing the gatewayctl induce mode to the now-shipped `gatewayctl breaker force-open` from Phase 06.9 Plan 04; `gatewayctl debug emit-error` subcommand with an automated HTTP integration test proving the `/admin/debug/panic` route is admin-gated (reviews HIGH); `gatewayctl key list` operator inspection table with sqlc query columns explicitly verified (reviews MEDIUM). All three contribute to PRD-04 full-runbook diagnostic surface that 11-09 consumes.
</objective>

<must_haves>
  truths:
    - "D-18.1: smoke-sensitive-failover.py polling loop accepts {open, forced-open, FORCED_OPEN}; defensive whitelist explicit (Pitfall 8); closed and half-open NEVER in accepted set"
    - "D-18.1 second half: gatewayctl induce mode invokes real `breaker force-open` (Phase 06.9 Plan 04 landed at gateway/cmd/gatewayctl/breaker.go:117)"
    - "D-18.2: gatewayctl debug emit-error -> POST /admin/debug/panic -> httpx.Recoverer catches -> sentry.CurrentHub().Recover + Flush(500ms) -> event lands in Sentry ifix-ai-gateway-prod (id 4511455942017024)"
    - "D-18.2 reviews HIGH: /admin/debug/panic MUST have an automated integration test proving (a) unauthenticated request returns 401/403 (admin auth gate alive), (b) authenticated request returns 500 via Recoverer (panic recovery alive). Code placement alone is NOT acceptable."
    - "D-18.3: gatewayctl key list returns aligned table via stdlib text/tabwriter; never prints raw key (only key_prefix); RFC3339 UTC timestamps"
    - "D-18.3 reviews MEDIUM: sqlc query columns MUST be verified to include tenant_slug, created_at, last_used_at, status, data_class BEFORE writing the dispatcher. Existing `ListActiveKeysAll` does NOT include those columns (verified by reading gateway/db/queries/auth.sql:21-28). Plan delivers an extended query `ListActiveKeysAllWithMeta` in the same `.sql` file."
    - "Pattern D 5 invariants honored: runCmd(ctx, args, log) int signature, exit codes 0/1/2, flag.NewFlagSet, loadAndPool + defer pool.Close, stdout=parseable + stderr=log"
    - "Tests are real assertions, not placeholder Skip() (reviews LOW): debug_panic_test.go uses httptest + chi.Mux composition to drive real HTTP requests; key_test.go uses a transactional psycopg-style fixture OR a stubbed Querier if integration is too heavy for unit pass."
    - "Smoke parameterization (reviews MEDIUM): smoke-sensitive-failover.py reads GATEWAYCTL_PATH + GATEWAYCTL_CONTAINER + GATEWAYCTL_SSH_HOST from env with sensible defaults so the script works against any environment (not hard-coded to n8n-ia-vm)."
  must_haves:
    - gateway/cmd/gatewayctl/debug.go exists with runDebug dispatcher + runDebugEmitError function
    - gateway/cmd/gatewayctl/main.go switch includes case "debug" + extended usage()
    - gateway/internal/admin/debug_panic.go exports DebugPanicHandler that panics inside Recoverer chain
    - gateway/internal/admin/debug_panic_test.go contains TWO automated HTTP test cases — (a) unauth → 401/403 (b) auth + Recoverer → 500 with sanitized body, panic message NOT leaked
    - gateway/db/queries/auth.sql contains a new `ListActiveKeysAllWithMeta :many` query that joins api_keys with tenants table for tenant_slug AND selects status, data_class, created_at, last_used_at (and a tenant-filtered `ListActiveKeysByTenantWithMeta :many` variant for the --tenant filter)
    - gateway/cmd/gatewayctl/key.go dispatcher includes case "list" -> runKeyList; usage string lists create|revoke|list
    - gateway/cmd/gatewayctl/key_test.go contains a real test (not Skip()) using a stubbed Querier interface OR a fixture-backed integration test; if integration is gated by a missing DB, the test logs the gate clearly and the SUMMARY documents the coverage gap
    - scripts/integration-smoke/smoke-sensitive-failover.py constants block declares OPEN_LIKE_STATES frozenset with exactly {"open","forced-open","FORCED_OPEN"}
    - smoke-sensitive-failover.py induce_failure_via_gatewayctl invokes subprocess.run with gatewayctl breaker force-open --upstream=local-llm --ttl=300s; path/container/SSH host parameterized via env vars
  key_links:
    - from: gateway/cmd/gatewayctl/debug.go
      to: gateway/internal/admin/debug_panic.go
      via: HTTP POST /admin/debug/panic with X-Admin-Key header
      pattern: "/admin/debug/panic"
    - from: gateway/internal/admin/debug_panic.go
      to: gateway/internal/httpx/recoverer.go
      via: panic() recovered by Recoverer middleware wrapping admin chain
      pattern: "Recoverer"
    - from: gateway/internal/admin/debug_panic_test.go
      to: gateway/internal/admin/debug_panic.go + httpx.Recoverer + admin.Middleware
      via: httptest.NewServer with the composed chain; raw http.Get/Post
      pattern: "httptest.NewServer|http.NewRequest"
    - from: gateway/cmd/gatewayctl/key.go runKeyList
      to: gateway/db/queries/auth.sql ListActiveKeysAllWithMeta / ListActiveKeysByTenantWithMeta
      via: sqlc-generated Go queries via loadAndPool + gen.New(pool)
      pattern: "ListActiveKeys.*WithMeta"
    - from: scripts/integration-smoke/smoke-sensitive-failover.py induce mode
      to: gateway/cmd/gatewayctl/breaker.go force-open subcommand
      via: subprocess.run gatewayctl breaker force-open (path from env GATEWAYCTL_PATH)
      pattern: "breaker.*force-open"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> /admin/debug/panic | X-Admin-Key gated; synthetic panic exercises Recoverer chain |
| /admin/debug/panic without admin auth | MUST return 401/403 — test proves this |
| gateway container -> Sentry org Ifix project ifix-ai-gateway-prod | panic events with admin context (admin_key_id label) |
| operator -> DB read | gatewayctl key list reads ai_gateway.api_keys via sqlc (new WithMeta queries) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-OPS-01 | Elevation of Privilege | /admin/debug/panic unauthenticated | mitigate | Handler mounted under admin.Middleware (X-Admin-Key required); AUTOMATED INTEGRATION TEST debug_panic_test.go drives raw HTTP requests through the composed chain and asserts unauth → 401/403 + auth → 500; code placement alone is not relied upon (reviews HIGH) |
| T-11-OPS-02 | Information Disclosure | gatewayctl key list raw key leak | mitigate | Print key_prefix only (never raw API key); stdlib text/tabwriter; query selects key_prefix column (NOT key_hash, NOT raw key); test asserts grep-able pattern |
| T-11-OPS-03 | Tampering | breaker force-open via smoke harness without audit | mitigate | gateway/cmd/gatewayctl/breaker.go:117 force-open writes Redis key + audit row (Phase 06.9); smoke now invokes the audited path instead of stub |
| T-11-OPS-04 | Denial of Service | panic handler crashes if Recoverer not in chain | mitigate | debug_panic.go docstring explicit about wrap order (Recoverer outermost); test asserts 500 response (NOT crash) by composing the chain in httptest and verifying response body is the sanitized error envelope (no panic stacktrace in body) |
| T-11-OPS-08 | Information Disclosure | panic stacktrace leaks into HTTP response | mitigate | Recoverer's WriteOpenAIError emits sanitized envelope only; debug_panic_test.go asserts response body does NOT contain "synthetic panic" or "goroutine" tokens |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@gateway/cmd/gatewayctl/admin_key.go
@gateway/cmd/gatewayctl/admin_key_test.go
@gateway/cmd/gatewayctl/breaker.go
@gateway/cmd/gatewayctl/key.go
@gateway/cmd/gatewayctl/main.go
@gateway/internal/httpx/recoverer.go
@gateway/internal/admin/middleware.go
@gateway/db/queries/auth.sql
@scripts/integration-smoke/smoke-sensitive-failover.py
</context>

<interfaces>
From gateway/cmd/gatewayctl/admin_key.go:
- runAdminKey(ctx context.Context, args []string, log *slog.Logger) int
- runAdminKeyList(ctx context.Context, args []string, log *slog.Logger) int  (pattern for runKeyList)

From gateway/cmd/gatewayctl/breaker.go (Phase 06.9 Plan 04, verified line 117):
- runBreakerForceOpen(ctx context.Context, args []string, log *slog.Logger) int
- flags: --upstream=NAME --ttl=DURATION

From gateway/internal/admin/middleware.go:
- Middleware(...) http.Handler — fronts admin routes with X-Admin-Key auth
- FromContext(ctx context.Context) (*AdminContext, bool)
- AdminContext has AdminKeyID uuid.UUID and Label string

From gateway/internal/httpx/recoverer.go:
- Recoverer(base *slog.Logger) func(http.Handler) http.Handler — recovers panic, calls sentry.CurrentHub().Recover(rec) + sentry.Flush(500ms), then WriteOpenAIError(w, 500, "api_error", "internal_error", ...)

From gateway/db/queries/auth.sql (verified 2026-05-27 — these are the existing queries):
- ListActiveKeysByTenant :many → returns id, tenant_id, key_hash, key_lookup_hash, key_prefix, status, data_class, created_at, revoked_at, last_used_at (NO tenant_slug)
- ListActiveKeysAll :many → returns id, tenant_id, key_hash, key_lookup_hash, key_prefix, status, data_class (NO created_at, NO last_used_at, NO tenant_slug)
- GetActiveKeyByLookupHash :one (hot path)
- GetAPIKeyByID :one
- TouchKeyLastUsed :exec

Therefore the operator-friendly `key list` table needs NEW queries that JOIN on tenants for slug AND project the full meta set.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <id>11-04-01</id>
  <title>Task 1: gatewayctl debug subcommand + /admin/debug/panic handler + AUTOMATED unauth-gate test</title>
  <files>gateway/cmd/gatewayctl/debug.go, gateway/cmd/gatewayctl/debug_test.go, gateway/internal/admin/debug_panic.go, gateway/internal/admin/debug_panic_test.go, gateway/cmd/gatewayctl/main.go</files>
  <read_first>
    - gateway/cmd/gatewayctl/admin_key.go (dispatcher pattern lines 50-66 + flag parsing)
    - gateway/cmd/gatewayctl/main.go (switch dispatch lines 48-101 + usage() lines 20-46)
    - gateway/internal/admin/middleware.go (FromContext + AdminContext shape; Middleware constructor signature)
    - gateway/internal/httpx/recoverer.go (Recoverer wrap pattern + Sentry chain + WriteOpenAIError body shape)
    - 11-PATTERNS.md sections for debug.go + debug_panic.go (handler skeleton + wrap order invariant)
    - 11-RESEARCH.md Pattern 1 + Architecture Patterns D-18.2 panic path diagram
  </read_first>
  <behavior>
    - runDebugEmitError exits 0 when --gateway and --admin-key provided AND POST returns 500; exits 1 when status != 500; exits 2 when flags missing.
    - DebugPanicHandler panics with literal string "synthetic panic emitted by gatewayctl debug emit-error" (Sentry event search-string).
    - debug_panic_test.go runs two HTTP integration cases against an in-process httptest.NewServer composing httpx.Recoverer + admin.Middleware + DebugPanicHandler:
      a. Unauthenticated POST /admin/debug/panic (no X-Admin-Key header) → response status MUST be 401 OR 403; response body MUST NOT contain "synthetic panic".
      b. Authenticated POST with a valid X-Admin-Key (the test seeds a transient admin key in the test DB OR mocks the AdminContext) → response status MUST be 500; response body MUST be the WriteOpenAIError sanitized envelope (no panic stacktrace, no "goroutine" token).
    - The test fails the build (NOT skipped) when run in CI; if the test requires a DB harness that is unavailable, the test uses a stub `admin.Middleware`-shaped interface that returns canned auth outcomes — the integration assertion is preserved regardless of DB availability.
  </behavior>
  <action>
    Create gateway/cmd/gatewayctl/debug.go per 11-PATTERNS.md debug.go pattern. Package main. Define runDebug(ctx context.Context, args []string, log *slog.Logger) int dispatcher: usage error when args empty; switch arg[0] case "emit-error" returns runDebugEmitError; default unknown subcommand exit 2. Define runDebugEmitError with flag.NewFlagSet("debug emit-error", flag.ExitOnError), --gateway string defaulting to env AI_GATEWAY_URL, --admin-key string defaulting to env AI_GATEWAY_ADMIN_KEY, both required (exit 2 on missing). http.NewRequestWithContext POST {gateway}/admin/debug/panic with header X-Admin-Key. http.DefaultClient.Do with a 10s timeout. On net error log error and return 1. Assert resp.StatusCode == 500 (Recoverer always returns 500 on panic) otherwise log expected vs got and return 1. On success log.Info("debug emit-error sent", "gateway", gwURL, "status", resp.StatusCode) and return 0. NEVER log the admin key value.

    Create gateway/cmd/gatewayctl/debug_test.go — a real unit test that exercises argument parsing and exit codes, NOT a Skip() placeholder. Cases:
    - missing flags → exit 2
    - successful POST (mock http.RoundTripper returns 500) → exit 0
    - non-500 response → exit 1
    Use httptest.NewServer to back the mock gateway URL.

    Create gateway/internal/admin/debug_panic.go per 11-PATTERNS.md handler skeleton. Package admin. Package doc comment states the handler is operator-only synthetic panic emitter mounted at POST /admin/debug/panic under admin auth + Recoverer; if a refactor inverts wrap order the panic crashes the process. DebugPanicHandler(log *slog.Logger) http.Handler returns http.HandlerFunc that: calls ac, _ := FromContext(r.Context()), log.WarnContext with admin_key_id and label fields, then panic("synthetic panic emitted by gatewayctl debug emit-error").

    Create gateway/internal/admin/debug_panic_test.go — automated HTTP integration test (reviews HIGH gate). Compose the real chain `httpx.Recoverer(log)(admin.Middleware(...)(admin.DebugPanicHandler(log)))` via chi or net/http wrapping. Use `httptest.NewServer` to bind a real port. Two cases:
    1. `TestDebugPanic_Unauthenticated_Returns401Or403`: build a `http.NewRequest("POST", srv.URL+"/admin/debug/panic", nil)` with NO X-Admin-Key header; expect `resp.StatusCode` in {401, 403}; read body and assert it does NOT contain "synthetic panic" or "goroutine".
    2. `TestDebugPanic_Authenticated_Returns500ViaRecoverer`: build the same request WITH X-Admin-Key set to a fixture-seeded admin key (or use a stub middleware that injects an AdminContext directly for a hermetic unit test); expect `resp.StatusCode == 500`; read body and assert it matches the OpenAI-error JSON envelope (`error.type == "api_error"` AND `error.code == "internal_error"`) and does NOT contain "synthetic panic" or "goroutine".

    Choose hermetic-unit vs DB-fixture for case 2 based on what the existing test harness offers — if `gateway/internal/admin/middleware_test.go` already has a key-seeding helper, reuse it; otherwise stub `admin.Middleware` with a constructor that accepts a fake auth function (factor into the production middleware if not already factored — minimal refactor). Either way, the test MUST NOT be Skip().

    Edit gateway/cmd/gatewayctl/main.go: add case "debug" in switch (lines 48-101) calling os.Exit(runDebug(ctx, args, log)). Extend usage() to mention debug emit-error and key list.

    Wire the handler at POST /admin/debug/panic in gateway/cmd/gateway/main.go router under the admin chain so X-Admin-Key middleware fires before the handler. Wrap order MUST be Recoverer(adminMiddleware(DebugPanicHandler)) — Recoverer outermost per debug_panic.go docstring invariant. If gateway router uses chi.Mux, register via r.With(httpx.Recoverer(log), admin.Middleware(...)).Post("/admin/debug/panic", admin.DebugPanicHandler(log).ServeHTTP).
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/admin/ -run "TestDebugPanic" -count=1 -race -timeout=60s && go test ./cmd/gatewayctl/ -run "TestRunDebug" -count=1 -race -timeout=60s && go vet ./... && go build ./cmd/gatewayctl/ && go build ./cmd/gateway/</automated>
  </verify>
  <done>Both TestDebugPanic_Unauthenticated_Returns401Or403 AND TestDebugPanic_Authenticated_Returns500ViaRecoverer pass with real assertions (not Skip); debug_test.go tests pass; go vet clean; gatewayctl + gateway binaries build; grep "synthetic panic emitted by gatewayctl debug emit-error" gateway/internal/admin/debug_panic.go returns 1; grep "/admin/debug/panic" gateway/cmd/gateway/main.go returns at least 1; grep for case "debug" in gateway/cmd/gatewayctl/main.go returns 1; grep `t.Skip` in debug_panic_test.go returns 0 (no skip).</done>
</task>

<task type="auto" tdd="true">
  <id>11-04-02</id>
  <title>Task 2: New sqlc queries (WithMeta) + gatewayctl key list subcommand</title>
  <files>gateway/db/queries/auth.sql, gateway/cmd/gatewayctl/key.go, gateway/cmd/gatewayctl/key_test.go</files>
  <read_first>
    - gateway/db/queries/auth.sql (CURRENT shape — see <interfaces>: ListActiveKeysAll lacks created_at/last_used_at/tenant_slug; ListActiveKeysByTenant lacks tenant_slug)
    - gateway/cmd/gatewayctl/key.go (current dispatcher lines 17-31; create/revoke subcommands already present)
    - gateway/cmd/gatewayctl/admin_key.go (runAdminKeyList lines 240-280 — exact mirror target)
    - gateway/internal/db/gen/ (sqlc-generated Go signatures after regeneration)
    - 11-PATTERNS.md key.go (insertion sites + runKeyList target body)
    - 11-CONTEXT.md D-18.3
    - gateway/db/migrations (verify `ai_gateway.tenants` table exists and has a `slug` column; if the project uses `ai_gateway.tenants_admin` instead, executor swaps the JOIN target accordingly)
  </read_first>
  <behavior>
    - auth.sql gains TWO new query blocks: `ListActiveKeysAllWithMeta :many` and `ListActiveKeysByTenantWithMeta :many`, both JOINing api_keys → tenants for slug + selecting status, data_class, key_prefix, created_at, last_used_at (NOT key_hash, NOT key_lookup_hash — those are secret-bearing).
    - sqlc regeneration produces matching `ListActiveKeysAllWithMetaRow` / `ListActiveKeysByTenantWithMetaRow` types.
    - runKeyList outputs aligned header row "ID TENANT PREFIX STATUS DATA_CLASS CREATED LAST_USED" followed by 1 row per active API key.
    - When --tenant SLUG filter is provided, runKeyList calls ListActiveKeysByTenantWithMeta (resolving slug → tenant UUID via existing helper); otherwise calls ListActiveKeysAllWithMeta.
    - Raw API key is NEVER printed (only key_prefix column).
    - key_test.go contains a REAL test (not Skip()) — either a sqlc-backed integration test against an ephemeral test DB OR a stubbed Querier interface that returns canned rows. The test asserts header format + row count + that no raw key_hash leaks into stdout.
  </behavior>
  <action>
    Step 1: Extend gateway/db/queries/auth.sql by APPENDING two new queries (do NOT modify existing ones — they remain for the hot path):

    ```sql
    -- name: ListActiveKeysAllWithMeta :many
    -- Diagnostic / admin-surface query for `gatewayctl key list` (D-18.3). Projects
    -- only NON-SECRET-BEARING columns + tenant slug via JOIN. Never used on the
    -- hot path (hot path = GetActiveKeyByLookupHash).
    SELECT k.id, k.tenant_id, t.slug AS tenant_slug, k.key_prefix, k.status, k.data_class, k.created_at, k.last_used_at
    FROM ai_gateway.api_keys k
    JOIN ai_gateway.tenants t ON t.id = k.tenant_id
    WHERE k.status = 'active'
    ORDER BY t.slug ASC, k.last_used_at DESC NULLS LAST;

    -- name: ListActiveKeysByTenantWithMeta :many
    -- Same projection as ListActiveKeysAllWithMeta but filtered by tenant_id.
    -- Used by `gatewayctl key list --tenant=<slug>` after slug→UUID resolution.
    SELECT k.id, k.tenant_id, t.slug AS tenant_slug, k.key_prefix, k.status, k.data_class, k.created_at, k.last_used_at
    FROM ai_gateway.api_keys k
    JOIN ai_gateway.tenants t ON t.id = k.tenant_id
    WHERE k.tenant_id = $1 AND k.status = 'active'
    ORDER BY k.last_used_at DESC NULLS LAST;
    ```

    If the tenants table on the deployed schema is named differently (e.g. `ai_gateway.tenants_admin`), executor swaps the JOIN target and adjusts the docstring. Verify by `psql -c '\dt ai_gateway.*'` against the dev DSN.

    Step 2: Regenerate sqlc: `cd gateway && sqlc generate`. Verify the generated `internal/db/gen/auth.sql.go` contains `ListActiveKeysAllWithMeta` and `ListActiveKeysByTenantWithMeta` exported functions returning slices of the matching Row types.

    Step 3: Edit gateway/cmd/gatewayctl/key.go:
    - Dispatcher switch lines 22-30 add case "list" returning runKeyList(ctx, args[1:], log).
    - Usage string line 19 extend to "Usage: gatewayctl key create|revoke|list [flags]".
    - After runKeyRevoke (line 150 area) append runKeyList(ctx context.Context, args []string, log *slog.Logger) int mirroring admin_key.go:runAdminKeyList.
      Inside: flag.NewFlagSet("key list", flag.ExitOnError); --tenant string default ""; fs.Parse. loadAndPool(ctx, log) then defer pool.Close; q := gen.New(pool).
      If tenant slug provided, resolve slug → tenant UUID via existing helper (look for resolveTenantSlug in tenant.go OR query tenants table directly). Then rows = q.ListActiveKeysByTenantWithMeta(ctx, tenantUUID). Else rows = q.ListActiveKeysAllWithMeta(ctx).
      tabwriter.NewWriter(os.Stdout, 0, 2, 2, ' ', 0). Header line "ID\tTENANT\tPREFIX\tSTATUS\tDATA_CLASS\tCREATED\tLAST_USED". Per row fmt.Fprintf with ID.String, TenantSlug, KeyPrefix, Status, DataClass, CreatedAt.UTC().Format(time.RFC3339), LastUsedAt.Time.UTC().Format(time.RFC3339) when r.LastUsedAt.Valid else "-". Return tw.Flush() exit code.

    Step 4: Edit gateway/cmd/gatewayctl/key_test.go (REAL test, not Skip — reviews LOW). Two options:
    (a) Integration test gated by env `KEY_LIST_TEST_DSN`: if env set, open pool, seed N rows, run runKeyList, capture stdout via os.Pipe + io.ReadAll, assert header line + N data rows + no occurrences of "key_hash" substring in output.
    (b) Stub Querier test: extract a small `keyLister` interface that runKeyList consumes (production wrapper passes gen.Queries; test passes a stub returning canned Rows). Assert stdout shape via same pipe+read pattern.
    Pick (b) by default for fast unit pass; document the (a) integration variant in a TODO comment if (a) is not run.
  </action>
  <verify>
    <automated>cd gateway && grep -E "ListActiveKeysAllWithMeta|ListActiveKeysByTenantWithMeta" gateway/db/queries/auth.sql | wc -l | awk '{if ($1 >= 2) exit 0; else exit 1}' && (sqlc generate || echo "sqlc generate skipped — verify manually") && go test ./cmd/gatewayctl/... -count=1 -race -timeout=60s && go vet ./... && go build ./cmd/gatewayctl/ && grep -E "^\s*t\.Skip" gateway/cmd/gatewayctl/key_test.go | wc -l | grep -E "^\s*0$"</automated>
  </verify>
  <done>auth.sql contains both WithMeta queries; sqlc regenerated cleanly; go test green; go vet clean; gatewayctl binary builds; grep "case \"list\":" gateway/cmd/gatewayctl/key.go returns 1; grep "runKeyList" gateway/cmd/gatewayctl/key.go returns at least 2; grep "key_prefix\|KeyPrefix" gateway/cmd/gatewayctl/key.go returns at least 1; grep `t.Skip` in key_test.go returns 0 (no skip).</done>
</task>

<task type="auto">
  <id>11-04-03</id>
  <title>Task 3: smoke-sensitive-failover.py race fix + parameterized gatewayctl repoint (D-18.1)</title>
  <files>scripts/integration-smoke/smoke-sensitive-failover.py</files>
  <read_first>
    - scripts/integration-smoke/smoke-sensitive-failover.py (full file, focus on Constants block, ensure_tier0_open function at line 269, induce_failure_via_gatewayctl at lines 307-327, docstring at line 193)
    - 11-PATTERNS.md smoke-sensitive-failover.py (4 insertion sites table + Pitfall 8 defensive whitelist)
    - 11-RESEARCH.md Pitfall 8 + State of the Art "Deprecated" row (gatewayctl breaker force-open landed Phase 06.9 Plan 04 at gateway/cmd/gatewayctl/breaker.go:117)
    - gateway/cmd/gatewayctl/breaker.go (force-open subcommand surface: --upstream=NAME --ttl=DURATION)
  </read_first>
  <action>
    Edit scripts/integration-smoke/smoke-sensitive-failover.py per 11-PATTERNS.md 4 insertion sites + reviews MEDIUM parameterization:

    1. Constants block (after line 116 AUDIT_UPSTREAM_BLOCKED): add OPEN_LIKE_STATES = frozenset({"open", "forced-open", "FORCED_OPEN"}). Use frozenset (immutable) per Pattern A. Add defensive module-level assertions immediately after: `assert "closed" not in OPEN_LIKE_STATES; assert "half-open" not in OPEN_LIKE_STATES` (Pitfall 8 defensive whitelist). Also declare parameterization env constants:
       ```
       GATEWAYCTL_PATH = os.environ.get("GATEWAYCTL_PATH", "/gatewayctl")
       GATEWAYCTL_CONTAINER = os.environ.get("GATEWAYCTL_CONTAINER", "ifix-ai-gateway")
       GATEWAYCTL_SSH_HOST = os.environ.get("GATEWAYCTL_SSH_HOST", "")  # empty → run locally
       ```
       Document in module docstring that these env vars allow the smoke to target arbitrary hosts/containers (reviews MEDIUM).

    2. ensure_tier0_open function (line 269 region): replace condition `last_state == "open"` with `last_state in OPEN_LIKE_STATES` so polling accepts the equivalent open-like variants (D-18.1 + D-09).

    3. induce_failure_via_gatewayctl function (lines 307-327): replace the existing stub/error-out body with a real subprocess.run invocation. Build the command list parameterized by env:
       ```
       if GATEWAYCTL_SSH_HOST:
           cmd = ["ssh", GATEWAYCTL_SSH_HOST, "docker", "exec", GATEWAYCTL_CONTAINER, GATEWAYCTL_PATH, "breaker", "force-open", "--upstream=local-llm", "--ttl=300s"]
       else:
           cmd = [GATEWAYCTL_PATH, "breaker", "force-open", "--upstream=local-llm", "--ttl=300s"]
       result = subprocess.run(cmd, capture_output=True, text=True, check=True, timeout=30)
       ```
       Capture stdout/stderr for structlog. Return success once the force-open audit row is written. DO NOT hard-code `n8n-ia-vm` — operator sets GATEWAYCTL_SSH_HOST=n8n-ia-vm at invocation time.

    4. Docstring (line 193 region): update mode description for gatewayctl induce — explicit reference to Phase 06.9 Plan 04 force-open subcommand (no longer error-out stub); --ttl=300s rationale (5 minutes covers the smoke run window); parameterization via GATEWAYCTL_PATH/CONTAINER/SSH_HOST env vars (with examples for n8n-ia-vm and for local-host invocations).

    Preserve EVERY other line of the file unchanged (CR-01 invariant, schema validation tail, threat-T-09-* annotations).
  </action>
  <verify>
    <automated>grep "OPEN_LIKE_STATES = frozenset" scripts/integration-smoke/smoke-sensitive-failover.py && grep -E "GATEWAYCTL_PATH|GATEWAYCTL_CONTAINER|GATEWAYCTL_SSH_HOST" scripts/integration-smoke/smoke-sensitive-failover.py | wc -l | awk '{if ($1 >= 3) exit 0; else exit 1}' && grep -E "subprocess.run.*breaker.*force-open|breaker.*force-open" scripts/integration-smoke/smoke-sensitive-failover.py && python scripts/integration-smoke/smoke-sensitive-failover.py --help 2>&1 | head -5</automated>
  </verify>
  <done>OPEN_LIKE_STATES frozenset declared with exactly the 3 expected states; "closed" and "half-open" absent (assertion lines present); GATEWAYCTL_PATH/CONTAINER/SSH_HOST env vars wired (parameterization); induce_failure_via_gatewayctl invokes subprocess.run with `breaker force-open --upstream=local-llm --ttl=300s`; --help still works; existing 8-section spine preserved.</done>
</task>

</tasks>

<verification>
- `cd gateway && go test ./... -count=1 -race -timeout=120s` exits 0 (debug_panic_test cases + key_test cases all pass with real assertions).
- `cd gateway && go build ./cmd/gatewayctl/ && go build ./cmd/gateway/` both succeed.
- grep "OPEN_LIKE_STATES = frozenset" scripts/integration-smoke/smoke-sensitive-failover.py returns 1.
- grep "GATEWAYCTL_PATH\|GATEWAYCTL_CONTAINER\|GATEWAYCTL_SSH_HOST" scripts/integration-smoke/smoke-sensitive-failover.py returns ≥3.
- grep "breaker.*force-open" scripts/integration-smoke/smoke-sensitive-failover.py returns at least 1.
- grep "synthetic panic emitted by gatewayctl debug emit-error" gateway/internal/admin/debug_panic.go returns 1.
- grep `t.Skip` in gateway/internal/admin/debug_panic_test.go AND gateway/cmd/gatewayctl/key_test.go BOTH return 0 (real tests, not placeholders).
- grep "ListActiveKeysAllWithMeta\|ListActiveKeysByTenantWithMeta" gateway/db/queries/auth.sql returns 2.
- Live UAT (deferred to 11-10): gatewayctl debug emit-error → Sentry event lands within 5s with admin_key_id label.
</verification>

<success_criteria>
- 3 new Go files (debug.go, debug_test.go, debug_panic.go) + 1 new test file (debug_panic_test.go) + 3 modified Go files (key.go, key_test.go, main.go + gateway main.go) + 1 modified SQL file (auth.sql).
- /admin/debug/panic has automated unauth gate test passing in CI.
- gatewayctl key list backed by new WithMeta queries with explicit column coverage.
- smoke-sensitive-failover.py D-09 race fix + D-18.1 repoint + parameterization complete.
- All gatewayctl unit tests pass with REAL assertions (zero t.Skip); binaries build.
- D-18.1, D-18.2, D-18.3 implementations grep-able.
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies (sentry-go, slog, text/tabwriter already in tree). Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.
- Truth 2: No speculative language in code comments.
- Truth 3: Chaos scripts (vast-delete.sh, openrouter-iptables-drop.sh) live in 11-07/11-08 plans.
- Truth 4: Vast API key never literal in shell history (smoke uses env var).
- Truth 5: This plan is autonomous (no live UAT — that happens in 11-10).
- Truth 8 (cross-cutting): NO raw API keys, raw key_hash values, full Authorization headers, or DSNs in test output or query projections. New WithMeta queries select key_prefix only (never key_hash).
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-04-SUMMARY.md when done with files modified, go test output snippet showing debug_panic_test 2 cases PASS + key_test PASS, grep evidence for the 3 D-18 fold sub-items + WithMeta queries.
</output>
