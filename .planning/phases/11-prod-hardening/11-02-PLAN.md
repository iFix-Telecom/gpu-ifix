---
phase: 11
plan: 11-02
slug: dashboard-sso-hardening
wave: 1
depends_on: []
requirements: [PRD-06]
files_modified:
  - path: dashboard/src/lib/auth.ts
    kind: MOD
  - path: dashboard/src/lib/auth-client.ts
    kind: MOD
  - path: dashboard/src/lib/schema.ts
    kind: MOD
  - path: dashboard/src/lib/allowlist.ts
    kind: NEW
  - path: dashboard/src/lib/allowlist.test.ts
    kind: NEW
  - path: dashboard/src/lib/auth.test.ts
    kind: NEW
  - path: dashboard/src/middleware.ts
    kind: MOD
  - path: dashboard/src/app/login/page.tsx
    kind: MOD
  - path: dashboard/src/app/2fa/enroll/page.tsx
    kind: NEW
  - path: dashboard/src/app/2fa/challenge/page.tsx
    kind: NEW
  - path: dashboard/package.json
    kind: MOD
autonomous: false
estimated_duration: 180
scope_note: "5 tasks exceed the 2-3 target — single plan retained (precedent: Phase 10-06 HUMAN-UAT 11+ steps). Tightly coupled — middleware + auth.ts + pages share the same Better Auth contract; splitting causes cross-plan rollback risk on a failed migrate. Pre-install slopcheck audit folded into Task 11-02-04 Step 1."
---

<objective>
Ship PRD-06 dashboard SSO hardening end-to-end: better-auth twoFactor plugin (D-12), built-in rateLimit customRules (D-14), session expiresIn reduced to 30min with cookieCache (D-15), domain allowlist via databaseHooks (D-13), 5 net-new auth-flow UI surfaces per 11-UI-SPEC.md, and a [BLOCKING] schema migration task against `ai_dashboard_prod` to add twoFactor + twoFactor_backupCodes tables BEFORE the dashboard container rolls. autonomous=false because the migration step requires operator-supplied prod DSN and pre-roll coordination.
</objective>

<must_haves>
  truths:
    - "D-12: TOTP 2FA mandatory via better-auth twoFactor plugin (issuer='Ifix AI Gateway', SHA-1 default per @better-auth/utils/dist/otp.mjs:12)"
    - "D-13: signUp domain allowlist enforced via databaseHooks.user.create.before, env-driven DASHBOARD_ALLOWED_EMAIL_DOMAINS default ifixtelecom.com.br"
    - "D-14: built-in rateLimit (NOT plugin) with customRules `/sign-in/email` 5/15min, `/sign-up/email` 5/15min, `/two-factor/verify-totp` 5/60s"
    - "D-15: session expiresIn=30min (vs prior 7 days), updateAge=5min, cookieCache enabled maxAge=60s"
    - "Drizzle schema extension via @better-auth/cli migrate (Assumption A6); BLOCKING migrate task gates dashboard container roll"
    - "ZERO new server-side runtime dependencies (gateway Go binary, scripts Python smokes). Dashboard adds 4 new packages strictly required for PRD-06 2FA UI: `input-otp` + `@radix-ui/react-dialog` (via `npx shadcn add input-otp dialog`) + `qrcode` + `@types/qrcode` (via `bun add qrcode @types/qrcode`). All 4 packages MUST pass slopcheck pre-install."
  must_haves:
    - dashboard/src/lib/auth.ts contains `twoFactor({ issuer: "Ifix AI Gateway" })` in plugins array
    - dashboard/src/lib/auth.ts contains top-level `rateLimit` block (NOT imported as plugin)
    - dashboard/src/lib/auth.ts session.expiresIn=1800 (30*60 seconds)
    - dashboard/src/lib/auth-client.ts imports twoFactorClient and registers it
    - dashboard/src/lib/allowlist.ts exports isAllowedEmail named function
    - dashboard/src/app/2fa/enroll/page.tsx implements 3 steps (qr → verify → backup) per UI-SPEC
    - dashboard/src/app/2fa/enroll/page.tsx imports from `qrcode` and renders `<img>` with `data:image/png;base64,` src
    - dashboard/src/app/2fa/challenge/page.tsx implements 6-digit OTP challenge per UI-SPEC
    - dashboard/src/app/login/page.tsx renders rate-limit Alert and session-expired Alert per UI-SPEC
    - dashboard/src/middleware.ts two-stage check + matcher excludes /2fa and /signup
    - dashboard/package.json gains `qrcode` + `@types/qrcode` deps; `input-otp` + `@radix-ui/react-dialog` via shadcn add
    - All 4 net-new packages slopcheck-audited `[OK]` (no `[ASSUMED]`/`[SUS]`/`[SLOP]`)
    - Migration task verified against ai_dashboard_prod creates twoFactor table + twoFactorEnabled column
  key_links:
    - from: dashboard/src/lib/auth.ts
      to: dashboard/src/lib/allowlist.ts
      via: databaseHooks.user.create.before → isAllowedEmail(user.email)
      pattern: "isAllowedEmail"
    - from: dashboard/src/app/2fa/enroll/page.tsx
      to: dashboard/src/lib/auth-client.ts
      via: authClient.twoFactor.enable + verifyTotp
      pattern: "authClient.twoFactor"
    - from: dashboard/src/app/2fa/enroll/page.tsx
      to: qrcode npm package
      via: QRCode.toDataURL(totpURI) rendered as <img src=...>
      pattern: "from ['\"]qrcode['\"]"
    - from: dashboard/src/middleware.ts
      to: better-auth session cookie + cookieCache
      via: getSessionCookie + twoFactorVerified check
      pattern: "twoFactorVerified|twoFactorEnabled"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/auth/* | unauthenticated POSTs for sign-in/sign-up/verify-totp |
| Browser → /2fa/* | authenticated-but-unverified session boundary |
| dashboard container → DO Postgres ai_dashboard_prod | TOTP secrets at rest (encrypted by plugin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-AUTH-01 | Spoofing | /sign-in/email credential stuffing | mitigate | rateLimit customRules `/sign-in/email: { window: 900, max: 5 }` per-IP; generic error message "E-mail ou senha inválidos" (UI-SPEC line 183) |
| T-11-AUTH-02 | Tampering | TOTP code replay within window | mitigate | better-auth twoFactor plugin auto-stores last-used counter and rejects re-use (verified node_modules/better-auth/dist/plugins/two-factor/totp/index.mjs) |
| T-11-AUTH-03 | Spoofing | TOTP brute-force on /two-factor/verify-totp | mitigate | rateLimit customRules `/two-factor/verify-totp: { window: 60, max: 5 }` |
| T-11-AUTH-04 | Spoofing | Email allowlist bypass via signUp | mitigate | databaseHooks.user.create.before runs server-side; rejects non-`@ifixtelecom.com.br` domains; D-13 |
| T-11-AUTH-05 | Tampering | Backup code re-use | mitigate | better-auth plugin auto-consumes each code one-time (verified plugin schema) |
| T-11-AUTH-06 | Spoofing | Session fixation post-2FA | mitigate | better-auth regenerates session ID on signIn + twoFactor verify endpoints |
| T-11-AUTH-07 | Information Disclosure | TOTP secret leak via response logs | mitigate | secret rendered ONCE on enroll step 1; never logged; QR + secret displayed client-side only |
| T-11-AUTH-08 | Denial of Service | rateLimit exhaustion locks admins out | accept | 4 admins total; lockout window 15min; documented in RUNBOOK-INCIDENTS class 4 |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | Pre-install slopcheck audit (Task 11-02-04 Step 1) for input-otp + @radix-ui/react-dialog + qrcode + @types/qrcode; all 4 must return `[OK]` before install runs; commit results to plan evidence |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@.planning/phases/11-prod-hardening/11-UI-SPEC.md
@dashboard/src/lib/auth.ts
@dashboard/src/lib/auth-client.ts
@dashboard/src/lib/schema.ts
@dashboard/src/lib/utils.ts
@dashboard/src/lib/smoke.test.ts
@dashboard/src/middleware.ts
@dashboard/src/app/login/page.tsx
@dashboard/components.json
@dashboard/package.json
</context>

<interfaces>
<!-- Better Auth instance + client signatures the executor needs. Verified from node_modules 2026-05-27. -->

From `better-auth/plugins`:
```
twoFactor(options: { issuer: string, digits?: number, period?: number }): Plugin
```

From `better-auth/client/plugins`:
```
twoFactorClient(): ClientPlugin
authClient.twoFactor.enable({ password }): Promise<{ data: { totpURI, secret, backupCodes[] }, error }>
authClient.twoFactor.verifyTotp({ code }): Promise<{ data, error }>
```

From `qrcode` (server-render in client component via dynamic import or in client effect):
```
import QRCode from 'qrcode';
QRCode.toDataURL(text: string, opts?: { errorCorrectionLevel?: 'L'|'M'|'Q'|'H', margin?: number, width?: number }): Promise<string>
// returns 'data:image/png;base64,iVBORw0KGgo...'
```

Top-level betterAuth({...}) config keys used in Phase 11:
```
rateLimit: { enabled: boolean, window: number, max: number, customRules: { [path: string]: { window: number, max: number } }, storage?: "memory" | "secondary-storage" }
session: { expiresIn: number, updateAge?: number, cookieCache?: { enabled: boolean, maxAge: number } }
databaseHooks: { user: { create: { before: (user) => Promise<{ data: User }> } } }
plugins: Plugin[]
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <id>11-02-01</id>
  <title>Task 1: Domain allowlist utility + vitest</title>
  <files>dashboard/src/lib/allowlist.ts, dashboard/src/lib/allowlist.test.ts</files>
  <read_first>
    - dashboard/src/lib/utils.ts (named-export convention, no default export)
    - dashboard/src/lib/smoke.test.ts (vitest skeleton — describe/expect/it + @/lib path alias)
    - 11-RESEARCH.md §Code Examples (allowlist body verbatim) + §Pitfall 1 references
    - 11-PATTERNS.md §allowlist.ts + §allowlist.test.ts (named export; 4 test cases minimum)
  </read_first>
  <behavior>
    - Test 1: isAllowedEmail("admin@ifixtelecom.com.br") returns true
    - Test 2: isAllowedEmail("user@gmail.com") returns false
    - Test 3: isAllowedEmail("no-at-sign") returns false; isAllowedEmail("") returns false (malformed)
    - Test 4: isAllowedEmail("Admin@IFIXTELECOM.COM.BR") returns true (case-insensitive)
  </behavior>
  <action>
    Write the 4 vitest cases first (RED) using `import { describe, expect, it } from "vitest"` and `import { isAllowedEmail } from "@/lib/allowlist"`. Then implement allowlist.ts with named export `isAllowedEmail(email: string): boolean`. Env-driven via `process.env.DASHBOARD_ALLOWED_EMAIL_DOMAINS` (comma-separated lowercase domains; default `"ifixtelecom.com.br"`). Use `lastIndexOf("@")` (NOT regex) per D-13 + 11-RESEARCH §Don't Hand-Roll. JSDoc explains env-var override.
  </action>
  <verify>
    <automated>cd dashboard && bun test src/lib/allowlist.test.ts 2>&1 | grep -E "4 pass|✓.*case-insensitive"</automated>
  </verify>
  <done>allowlist.ts exports isAllowedEmail; 4 vitest cases pass; `grep "DASHBOARD_ALLOWED_EMAIL_DOMAINS" dashboard/src/lib/allowlist.ts` returns 1 line.</done>
</task>

<task type="auto" tdd="true">
  <id>11-02-02</id>
  <title>Task 2: Extend auth.ts with twoFactor + rateLimit + session + allowlist hook</title>
  <files>dashboard/src/lib/auth.ts, dashboard/src/lib/auth-client.ts, dashboard/src/lib/auth.test.ts</files>
  <read_first>
    - dashboard/src/lib/auth.ts (current 26 LOC — preserve baseURL/secret/database/advanced/export type Auth)
    - dashboard/src/lib/auth-client.ts (current shape — register twoFactorClient)
    - 11-RESEARCH.md §Pattern 2 (full target auth.ts body lines 262-317) + §State of the Art row 1 (rateLimit is BUILT-IN, not plugin)
    - 11-PATTERNS.md §dashboard/src/lib/auth.ts (6 insertion sites table)
    - 11-CONTEXT.md D-12, D-13, D-14, D-15 + specifics line 159 (issuer "Ifix AI Gateway")
    - 11-RESEARCH.md §Pitfall 4 (autoSignIn:false + middleware two-stage check) + §Pitfall 5 (existing 7-day sessions linger)
  </read_first>
  <behavior>
    - auth.ts: exports `auth` with twoFactor plugin registered with issuer "Ifix AI Gateway"
    - auth.ts: emailAndPassword has `autoSignIn: false` (D-12 + Pitfall 4)
    - auth.ts: session.expiresIn === 1800 (D-15)
    - auth.ts: top-level rateLimit.customRules includes "/sign-in/email", "/sign-up/email", "/two-factor/verify-totp"
    - auth.ts: databaseHooks.user.create.before throws when isAllowedEmail returns false
    - auth-client.ts: imports + registers twoFactorClient plugin
    - auth.test.ts: smoke test verifies the config object contains the expected keys (no network)
  </behavior>
  <action>
    Write auth.test.ts FIRST (RED) using `import { describe, expect, it } from "vitest"` + `import { auth } from "@/lib/auth"`. Cases (sample by introspection — better-auth exposes config readables): (a) `auth.options.session.expiresIn === 30 * 60`; (b) `auth.options.plugins.some((p: any) => p.id === "two-factor")`; (c) `auth.options.rateLimit.customRules["/sign-in/email"].max === 5`; (d) `auth.options.rateLimit.customRules["/sign-in/email"].window === 900`; (e) `auth.options.emailAndPassword.autoSignIn === false`. If `auth.options` shape is not stable, fall back to runtime smoke: `await auth.api.signUpEmail({ body: { email: "x@gmail.com", password: "x", name: "x" } })` rejects with allowlist error.

    Then edit auth.ts per 11-PATTERNS.md insertion-sites table:
    1. Add imports: `import { twoFactor } from "better-auth/plugins"; import { isAllowedEmail } from "./allowlist";`
    2. emailAndPassword: `{ enabled: true, autoSignIn: false }` (Pitfall 4)
    3. Replace session: `{ expiresIn: 30 * 60, updateAge: 5 * 60, cookieCache: { enabled: true, maxAge: 60 } }` (D-15)
    4. Add top-level rateLimit: `{ enabled: true, window: 60, max: 100, customRules: { "/sign-in/email": { window: 900, max: 5 }, "/sign-up/email": { window: 900, max: 5 }, "/two-factor/verify-totp": { window: 60, max: 5 } }, storage: "memory" }` (D-14; storage starts memory — secondary-storage Redis optional follow-up)
    5. Add plugins: `[twoFactor({ issuer: "Ifix AI Gateway" })]` (D-12; issuer string locked per CONTEXT.md specifics line 159; SHA-1 default per @better-auth/utils/dist/otp.mjs:12)
    6. Add databaseHooks: `user.create.before` throws `Error("E-mail fora do allowlist @ifixtelecom.com.br")` when `!isAllowedEmail(user.email)` else returns `{ data: user }` (D-13)
    7. Preserve baseURL, secret, database, advanced.database.generateId, `export type Auth = typeof auth;` verbatim.

    Edit auth-client.ts to import `twoFactorClient` from `better-auth/client/plugins` and add it to the client `plugins` array. Pattern from RESEARCH §Code Examples line 519-547 + better-auth docs.
  </action>
  <verify>
    <automated>cd dashboard && bun test src/lib/auth.test.ts 2>&1 | tail -20 && grep -E 'twoFactor\(|rateLimit:|expiresIn: 30 \* 60|databaseHooks' src/lib/auth.ts | wc -l | grep -E "^\s*[4-9]"</automated>
  </verify>
  <done>auth.test.ts cases pass; auth.ts has all 6 insertion sites; grep `twoFactor(` returns ≥1; grep `rateLimit:` returns 1; grep `expiresIn: 30 \* 60` returns 1; grep `customRules` returns 1; grep `databaseHooks` returns 1; auth-client.ts contains `twoFactorClient`.</done>
</task>

<task type="auto">
  <id>11-02-03</id>
  <title>Task 3: Schema extension (Drizzle mirror) + middleware two-stage check + login extensions</title>
  <files>dashboard/src/lib/schema.ts, dashboard/src/middleware.ts, dashboard/src/app/login/page.tsx</files>
  <read_first>
    - dashboard/src/lib/schema.ts (current 77 LOC; text-PK convention not UUID)
    - dashboard/src/middleware.ts (current 29 LOC; matcher excludes login + api/auth)
    - dashboard/src/app/login/page.tsx (current 99 LOC; Card + form skeleton)
    - 11-PATTERNS.md §dashboard/src/lib/schema.ts + §dashboard/src/middleware.ts + §dashboard/src/app/login/page.tsx (insertion sites)
    - 11-RESEARCH.md §Pitfall 4 + §Pitfall 5 + §Pitfall 7
    - 11-UI-SPEC.md §Error states (pt-BR copy rate-limit + session-expired) + §Inheritance Notes
  </read_first>
  <action>
    schema.ts: Do NOT attempt in-place pgTable extension (Drizzle pgTable is immutable per PATTERNS.md §schema.ts). Choose one of:
    (a) Edit the existing user pgTable inline to add `twoFactorEnabled: boolean('two_factor_enabled').default(false).notNull()` column (single-statement column addition, NOT pgTable re-declaration);
    (b) Defer to `@better-auth/cli migrate` which auto-adds the column and the new twoFactor / backupCodes tables.

    Recommend (b) per Pitfall 7 / Assumption A6 — letting the CLI handle schema bootstrapping reduces drift risk and keeps Drizzle mirror declarations optional (IDE IntelliSense only). If you choose to mirror declarations for IntelliSense, append a NEW `export const twoFactor = pgTable("twoFactor", { id: text("id").primaryKey(), secret: text("secret").notNull(), backupCodes: text("backup_codes").notNull(), userId: text("user_id").notNull().references(() => user.id, { onDelete: "cascade" }) })` block (additive — does NOT touch the existing user pgTable). Preserve text PK convention (NOT UUID — convention at line 14).

    middleware.ts: update matcher line 28 to `"/((?!login|signup|2fa|api/auth|_next|favicon).*)"`. Replace middleware body with two-stage gate (RESEARCH Pitfall 4 + UI-SPEC line 276):
    - if !session → if pathname doesn't start with `/login` redirect to `/login?session_expired=1` else next()
    - if session present but no 2FA-verified flag → cannot read DB in Edge runtime; rely on session cookie payload via `getSessionCookie` enrichment. If twoFactorEnabled flag absent in cookie → redirect to `/2fa/enroll`; if enabled but not verified → redirect to `/2fa/challenge`. (The twoFactor plugin updates session.twoFactorVerified upon verify; cookieCache maxAge=60s minimizes DB hits.)

    login/page.tsx: imports (line 11-22 area) add `import { Alert, AlertDescription } from "@/components/ui/alert"; import { useSearchParams } from "next/navigation";`. After `useRouter()` line 24 add `const params = useSearchParams(); const sessionExpired = params.get("session_expired") === "1"; const rateLimited = params.get("rate_limited") === "1";`. Inside `<CardContent>` BEFORE the existing `<form>` add two conditional Alert blocks per UI-SPEC §Error states copy:
    - rateLimited: `<Alert variant="destructive">` with copy "Muitas tentativas. Aguarde {n}s antes de tentar novamente." (countdown reads Retry-After from prior 429 if available via state).
    - sessionExpired: `<Alert variant="default">` with copy "Sessão encerrada por inatividade. Faça login novamente."
    Preserve existing form layout/copy verbatim (UI-SPEC line 273).
  </action>
  <verify>
    <automated>grep -E "two-factor|two_factor_enabled|twoFactor" dashboard/src/lib/schema.ts && grep -E "session_expired|rate_limited|Alert" dashboard/src/app/login/page.tsx && grep -E "2fa|signup" dashboard/src/middleware.ts</automated>
  </verify>
  <done>schema.ts contains twoFactor table OR twoFactorEnabled column declaration (additive only — user pgTable still single-declaration); middleware.ts matcher excludes /2fa and /signup AND has two-stage check (grep twoFactorVerified|twoFactorEnabled); login/page.tsx imports Alert + useSearchParams and renders 2 conditional Alert blocks above the form; existing form copy unchanged (grep "Gateway ifix-ai" still returns 1).</done>
</task>

<task type="auto">
  <id>11-02-04</id>
  <title>Task 4: 2FA enroll + challenge pages (UI-SPEC implementation)</title>
  <files>dashboard/src/app/2fa/enroll/page.tsx, dashboard/src/app/2fa/challenge/page.tsx, dashboard/package.json</files>
  <read_first>
    - dashboard/src/app/login/page.tsx (Card + form skeleton, max-w-sm centered layout, pt-BR copy)
    - 11-UI-SPEC.md §Visual Hierarchy + §Component Inventory + §Copywriting Contract (titles, descriptions, error states)
    - 11-PATTERNS.md §Pattern F (Dashboard Page Layout Card-centered auth) + §enroll/page.tsx + §challenge/page.tsx
    - 11-RESEARCH.md §Code Examples (authClient.twoFactor.enable → verifyTotp sequence) + §Pattern 2 + §Package Legitimacy Audit
    - dashboard/package.json (verify `qrcode` + `@types/qrcode` absent before install; verify `input-otp` + `@radix-ui/react-dialog` absent before shadcn add)
  </read_first>
  <action>
    Step 1 — Pre-install slopcheck audit (BLOCKING gate before any install command runs):
    Run the canonical slopcheck command per CLAUDE.md / Phase 11 RESEARCH §Package Legitimacy Audit on ALL 4 net-new packages:
    `slopcheck input-otp @radix-ui/react-dialog qrcode @types/qrcode`
    (or the project-canonical equivalent — e.g. `node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs slopcheck input-otp @radix-ui/react-dialog qrcode @types/qrcode` if `slopcheck` is not on PATH).
    Acceptance: ALL 4 packages return `[OK]`. If any returns `[ASSUMED]` or `[SUS]`, insert a `checkpoint:human-verify` gate against npmjs.com/package/{name} BEFORE proceeding. If any returns `[SLOP]`, abort the task and escalate via /gsd:plan-phase 11 --gaps.
    Save the slopcheck output to `.planning/phases/11-prod-hardening/11-02-slopcheck.txt` as evidence; reference it in the 11-02-SUMMARY.md.

    Step 2 — Install net-new shadcn blocks + qrcode lib (only after slopcheck passes):
    `cd dashboard && npx shadcn add input-otp dialog` (UI-SPEC §Component Inventory lines 226-228; pulls `input-otp` + `@radix-ui/react-dialog` into deps).
    `cd dashboard && bun add qrcode @types/qrcode` (locked choice per BLOCKER-2 resolution — `qrcode` is the canonical Node-ecosystem QR generator; renders to `data:image/png;base64,...` via `QRCode.toDataURL`).
    Verify after install: `grep -E '"qrcode"|"@types/qrcode"|"input-otp"|"@radix-ui/react-dialog"' dashboard/package.json` returns 4 lines.

    Step 3 — Create dashboard/src/app/2fa/enroll/page.tsx (3 steps state machine `step: "qr"|"verify"|"backup"`):
    - `"use client";` directive line 1.
    - Imports: useRouter, useState, useEffect from react, Card+CardContent+CardDescription+CardHeader+CardTitle, Button, InputOTP (new shadcn block), Dialog (new shadcn block), authClient from @/lib/auth-client, ShieldCheck + Copy from lucide-react, AND `import QRCode from 'qrcode'`.
    - Layout: `<main className="flex min-h-screen items-center justify-center p-6">` + `<Card className="w-full max-w-sm">` (UI-SPEC Pattern F).
    - Step "qr": CardTitle "Configurar 2FA" + ShieldCheck icon, CardDescription per UI-SPEC line 174. Call `await authClient.twoFactor.enable({ password })` which returns `{ totpURI, secret, backupCodes }`. Render QR via:
      ```
      const [qrDataURL, setQrDataURL] = useState<string>('');
      useEffect(() => {
        if (!totpURI) return;
        QRCode.toDataURL(totpURI, { errorCorrectionLevel: 'M', margin: 1, width: 256 }).then(setQrDataURL);
      }, [totpURI]);
      // then in JSX:
      {qrDataURL && <img src={qrDataURL} alt="TOTP QR" className="mx-auto" width={256} height={256} />}
      ```
      Acceptance: at runtime, DOM contains `<img>` whose `src` begins with `data:image/png;base64,`. Separator + manual-entry secret in `font-mono`. Primary CTA "Já escaneei, continuar" (UI-SPEC line 162).
    - Step "verify": CardTitle "Confirmar código", InputOTP 6-digit slot block, on submit `await authClient.twoFactor.verifyTotp({ code })`. Error copy "Código incorreto. Confirme o código atual no seu app autenticador e tente novamente." (UI-SPEC line 184). Primary CTA "Confirmar código".
    - Step "backup": CardTitle "Códigos de backup", Dialog modal containing 10 backup codes in font-mono rows with per-row Copy button + "Copiar tudo" button (clipboard.writeText backupCodes.join("\n"); sonner toast "10 códigos copiados para a área de transferência."). Primary CTA "Salvar e continuar" → router.push("/") + router.refresh().

    Step 4 — Create dashboard/src/app/2fa/challenge/page.tsx (single-step verify):
    - `"use client";` directive line 1.
    - Imports analog to enroll minus QR/Dialog/qrcode (uses InputOTP only).
    - CardTitle "Verificação em duas etapas" (UI-SPEC line 177) + ShieldCheck icon, CardDescription "Digite o código de 6 dígitos do seu app autenticador."
    - InputOTP 6-digit input. On submit `await authClient.twoFactor.verifyTotp({ code })`. On error: setError per UI-SPEC line 184. On success: router.push("/") + router.refresh().

    Both pages use tabular-nums on OTP digits + monospace stack on font-mono regions (UI-SPEC §Typography). Preserve dark theme inherited from Phase 07 (UI-SPEC §Theme).
  </action>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/11-02-slopcheck.txt && test -f dashboard/src/app/2fa/enroll/page.tsx && test -f dashboard/src/app/2fa/challenge/page.tsx && grep -l "authClient.twoFactor" dashboard/src/app/2fa/*/page.tsx | wc -l | grep -E "^\s*2" && grep -E "from ['\"]qrcode['\"]|QRCode.toDataURL" dashboard/src/app/2fa/enroll/page.tsx && grep -E '"qrcode"|"@types/qrcode"|"input-otp"|"@radix-ui/react-dialog"' dashboard/package.json | wc -l | grep -E "^\s*4"</automated>
  </verify>
  <done>Slopcheck evidence file committed with all 4 packages `[OK]`; both pages exist and import authClient.twoFactor; enroll has 3 steps + imports `qrcode` + renders `<img src="data:image/png;base64,...">`; challenge has 1 step; grep "Configurar 2FA\|Verificação em duas etapas" returns ≥2 lines; input-otp + dialog shadcn blocks present at dashboard/src/components/ui/; dashboard/package.json contains all 4 net-new deps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-02-05</id>
  <title>Task 5: [BLOCKING] Run @better-auth/cli migrate against ai_dashboard_prod</title>
  <read_first>
    - 11-RESEARCH.md §Pitfall 7 (migrate CLI auto-discovery — must run against PROD DSN, not DEV)
    - 11-RESEARCH.md §Assumption A6 (CLI auto-migrate compatibility) + suggested verification task line 763
    - scripts/deploy/migrate-dashboard.sh (existing migrate harness from Phase 10)
    - 11-CONTEXT.md (Phase 10 already created bd_ai_dashboard_prod; this is incremental ALTER)
  </read_first>
  <what-built>
    Schema extensions for better-auth twoFactor plugin (twoFactor table + user.twoFactorEnabled column) need to land in `ai_dashboard_prod` schema (DO Postgres). Operator must run the better-auth CLI migrate with the prod DSN. This is BLOCKING because dashboard image cannot roll without these tables — POST /api/auth/two-factor/verify-totp returns 500 with "relation twoFactor does not exist" if migrate is skipped (Pitfall 7 warning sign).
  </what-built>
  <how-to-verify>
    1. Operator opens a temp .env with DASHBOARD_DATABASE_URL=$PROD_DSN_DASHBOARD (DO bd_ai_dashboard_prod, mode 600).
    2. Dry-run first per Pitfall 7 mitigation: `cd dashboard && BETTER_AUTH_NO_INTERACTIVE=1 DASHBOARD_DATABASE_URL=$PROD_DSN_DASHBOARD bunx @better-auth/cli@latest migrate --dry-run` and inspect the SQL.
    3. Real migrate: `cd dashboard && BETTER_AUTH_NO_INTERACTIVE=1 DASHBOARD_DATABASE_URL=$PROD_DSN_DASHBOARD bunx @better-auth/cli@latest migrate --y`.
    4. Verify the new objects landed: `psql $PROD_DSN_DASHBOARD -c "\d dashboard_auth.twoFactor"` returns the table description + `psql $PROD_DSN_DASHBOARD -c "\d dashboard_auth.user" | grep two_factor_enabled` returns 1 row.
    5. Operator destroys the temp .env (mode 600 + rm) — Vast/OR/OpenAI keys never leak into shell history.
    6. Fallback (Assumption A6 wrong): if CLI doesn't auto-discover plugin schema, run manual ALTER per `dashboard/src/lib/schema.ts` mirror declarations; if even that fails the task is flagged for follow-up `/gsd-quick`. Surface the error verbatim to operator.

    Operator MUST also coordinate Pitfall 5 cleanup: existing 7-day sessions linger. Optional one-line `psql $PROD_DSN_DASHBOARD -c "DELETE FROM dashboard_auth.session WHERE expires_at > NOW() + INTERVAL '30 minutes';"` after deploy (forces re-login of all 4 admins; coordinate via WhatsApp pre-window).
  </how-to-verify>
  <resume-signal>Type "approved — twoFactor table + twoFactorEnabled column verified in ai_dashboard_prod" or describe the migrate error verbatim if it fails.</resume-signal>
</task>

</tasks>

<verification>
- `cd dashboard && bun test` exits 0 (allowlist.test.ts + auth.test.ts pass).
- `cd dashboard && bun run lint` exits 0 (no new violations).
- `cd dashboard && bun run build` exits 0 (Next build with new 2FA routes).
- `grep -c "twoFactor\|rateLimit\|expiresIn: 30 \* 60" dashboard/src/lib/auth.ts` returns ≥3.
- `psql $PROD_DSN_DASHBOARD -c "\dt dashboard_auth.twoFactor"` returns 1 row (post-migrate).
- `.planning/phases/11-prod-hardening/11-02-slopcheck.txt` committed with all 4 packages `[OK]`.
- `grep -E '"qrcode"|"@types/qrcode"|"input-otp"|"@radix-ui/react-dialog"' dashboard/package.json` returns 4 lines.
</verification>

<success_criteria>
- auth.ts + auth-client.ts + schema.ts + middleware.ts + login/page.tsx edited per insertion sites.
- 2 new 2FA pages + allowlist.ts + allowlist.test.ts + auth.test.ts created.
- @better-auth/cli migrate verified against ai_dashboard_prod.
- All vitest cases pass.
- D-12 + D-13 + D-14 + D-15 implementations grep-able in auth.ts.
- 4 net-new dashboard packages pre-install slopcheck audited and installed (input-otp, @radix-ui/react-dialog, qrcode, @types/qrcode).
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies (gateway Go binary, scripts Python smokes). Dashboard adds 4 net-new packages strictly required for PRD-06 2FA UI: `input-otp` + `@radix-ui/react-dialog` (via shadcn add) + `qrcode` + `@types/qrcode` (via bun add). All 4 slopcheck-audited `[OK]` pre-install.
- Truth 2: pt-BR copy from UI-SPEC §Error states locked verbatim (no English fallback).
- Truth 6: gateway/dashboard images stay at `:latest-dev` until 11-10 GHA retrigger fix (D-18.4).
- BLOCKING migrate task ensures DB has tables before container rolls.
</cross_cutting_truths>

<output>
Create `.planning/phases/11-prod-hardening/11-02-SUMMARY.md` when done with: files modified, vitest output snippet, `bun run build` outcome, slopcheck evidence file path + all-`[OK]` confirmation, migrate dry-run output (sanitized — no DSN), confirmation operator ran prod migrate.
</output>
