---
phase: 11
plan: 11-02
slug: dashboard-sso-hardening
wave: 1
depends_on: []
requirements: [PRD-06]
files_modified:
  - path: dashboard/src/lib/auth.ts
    kind: MOD
  - path: dashboard/src/lib/auth-client.ts
    kind: MOD
  - path: dashboard/src/lib/schema.ts
    kind: MOD
  - path: dashboard/src/lib/allowlist.ts
    kind: NEW
  - path: dashboard/src/lib/allowlist.test.ts
    kind: NEW
  - path: dashboard/src/lib/auth.test.ts
    kind: NEW
  - path: dashboard/src/middleware.ts
    kind: MOD
  - path: dashboard/src/middleware.test.ts
    kind: NEW
  - path: dashboard/src/app/login/page.tsx
    kind: MOD
  - path: dashboard/src/app/signup/page.tsx
    kind: MOD
  - path: dashboard/src/app/first-login/page.tsx
    kind: NEW
  - path: dashboard/src/app/2fa/enroll/page.tsx
    kind: NEW
  - path: dashboard/src/app/2fa/challenge/page.tsx
    kind: NEW
  - path: dashboard/src/app/2fa/backup/page.tsx
    kind: NEW
  - path: dashboard/src/app/signed-out/page.tsx
    kind: NEW
  - path: dashboard/src/app/settings/operadores/page.tsx
    kind: NEW
  - path: dashboard/src/components/auth/auth-shell.tsx
    kind: NEW
  - path: dashboard/src/components/auth/otp-row.tsx
    kind: NEW
  - path: dashboard/package.json
    kind: MOD
  - path: .planning/phases/11-prod-hardening/11-02-ui-inventory.md
    kind: NEW
  - path: .planning/phases/11-prod-hardening/11-02-slopcheck.txt
    kind: NEW
  - path: .planning/phases/11-prod-hardening/11-02-staging-smoke.md
    kind: NEW
autonomous: false
estimated_duration: 360
scope_note: "8 tasks exceed the 2-3 target — single plan retained (precedent: Phase 10-06 HUMAN-UAT 11+ steps). UI-SPEC v2 (rewrite from prototype `Front Ai-gateway.zip`) expands surface from 5→15 net-new auth screens + 1 Settings tab. Tightly coupled — middleware + auth.ts + 7 new route files + Operadores tab share the same Better Auth contract; splitting causes cross-plan rollback risk on a failed migrate. Pre-install slopcheck audit folded into Task 11-02-04 Step 1. Reviews v2 feedback addressed: UI-SPEC v2 re-alignment (HIGH-1), explicit cookieCache + edge-readable claim wiring + Playwright redirect test (HIGH-2), single canonical schema source = Better Auth CLI ONLY (HIGH-3), UI-primitive audit before any install (MEDIUM-4), behavior-based tests not auth.options introspection (MEDIUM-5), explicit Redis-vs-memory rate-limit storage decision with runbook cross-ref (MEDIUM-6), 2FA recovery cross-ref note (LOW-7), [BLOCKING] schema migrate resolved to ONE canonical command (`bunx @better-auth/cli@latest migrate`)."
---

<objective>
Ship PRD-06 dashboard SSO hardening end-to-end aligned with UI-SPEC v2 (rewrite from prototype `Front Ai-gateway.zip`, 2026-05-27T13:00Z): better-auth twoFactor plugin (D-12), built-in rateLimit customRules (D-14), session expiresIn reduced to 30min with cookieCache exposing twoFactorEnabled / twoFactorVerified claims to the Edge runtime (D-15 + middleware contract), domain allowlist via databaseHooks (D-13), 15 net-new auth-flow UI surfaces + 1 Settings → Operadores tab per 11-UI-SPEC.md, ONE canonical schema migrate command (Better Auth CLI), a Playwright route-test gate verifying middleware redirect behavior end-to-end, and a BLOCKING staging smoke gate BEFORE prod migration (reviews HIGH) so the irreversible prod schema change on `ai_dashboard_prod` is rehearsed end-to-end first. autonomous=false because migration + staging smoke require operator-supplied DSNs.
</objective>

<must_haves>
  truths:
    - "D-12: TOTP 2FA mandatory via better-auth twoFactor plugin (issuer='Ifix AI Gateway', SHA-1 default per @better-auth/utils/dist/otp.mjs:12)"
    - "D-13: signUp domain allowlist enforced via databaseHooks.user.create.before, env-driven DASHBOARD_ALLOWED_EMAIL_DOMAINS default ifixtelecom.com.br"
    - "D-14: built-in rateLimit (NOT plugin) with customRules `/sign-in/email` 5/15min, `/sign-up/email` 5/15min, `/two-factor/verify-totp` 5/60s; storage decision explicit per [reviews MEDIUM #6] (Redis via existing infra-redis-1 PREFERRED when REDIS_URL present in container env; memory fallback documented as restart-resets-counters caveat with cross-ref to RUNBOOK-INCIDENTS.md class 4)"
    - "D-15: session expiresIn=1800s (30min, vs prior 7 days), updateAge=300s (5min), cookieCache enabled maxAge=60s, AND cookieCache surfaces twoFactorEnabled + twoFactorVerified claims to the Edge runtime so middleware can gate without DB calls [reviews HIGH #2]"
    - "UI-SPEC v2 alignment [reviews HIGH #1]: 15 net-new auth screens (login pending / login rate-limited / login session-expired / signup allowlist rejection / first-login landing / TOTP enroll QR step 1 / TOTP enroll verify step 2 / TOTP enroll backup codes step 3 / TOTP challenge / TOTP challenge invalid / TOTP verify success transient / backup code entry / signed-out landing) + Settings → Operadores tab. Visual baseline LOCKED inherited from Phase 07. Dark only."
    - "Schema source-of-truth SINGLE [reviews HIGH #3 BLOCKING resolved]: Better Auth CLI `bunx @better-auth/cli@latest migrate` is THE canonical command. Plan does NOT modify schema.ts twoFactor declarations. Plan does NOT assert schema.ts contains `twoFactor`. Plan does NOT run `drizzle-kit push`. ONE command, ONE source of truth."
    - "Cookie-claim contract [reviews HIGH #2]: middleware MUST read twoFactorEnabled + twoFactorVerified from the session cookie cache (NOT from a DB call in Edge runtime). The auth.ts config wires these claims explicitly via Better Auth `session.cookieCache` + `additionalFields` (or session callback if installed version doesn't expose additionalFields) so they materialize in the cookie. Playwright test (Task 11-02-05A) verifies redirect targets: enroll required → /2fa/enroll; challenge required → /2fa/challenge; verified → app."
    - "Staging smoke BEFORE prod migrate [reviews HIGH]: a dedicated staging target (Option 1 separate schema on bd_ai_dashboard_prod, Option 2 dev DSN, OR Option 3 ephemeral DO fork) MUST receive the migrate first, with end-to-end enroll→challenge flow verified, BEFORE the irreversible prod migration runs."
    - "shadcn block decisions per UI-SPEC v2 [reviews MEDIUM #4]: `input-otp` = INSTALL only if existing `input` primitive cannot satisfy 6-slot paste-handling + per-digit a11y AFTER inventory; `dialog` = PREFER INLINE (prototype renders backup codes inline in LoginCard, no modal); `form` = DO NOT INSTALL. Inventory MANDATORY before any install runs."
    - "ZERO new server-side runtime dependencies (gateway Go binary, scripts Python smokes). Dashboard adds AT MOST 4 packages strictly required for PRD-06 2FA UI: `input-otp` (conditional per inventory) + `@radix-ui/react-dialog` (only if inline approach rejected, default skip) + `qrcode` + `@types/qrcode`. Every actually-installed package MUST pass slopcheck pre-install."
    - "Tests use STABLE public surface (route tests + auth.api smoke tests + Playwright HTTP-level redirect assertions) NOT `auth.options` introspection [reviews MEDIUM #5]. Behavior asserted: HTTP response codes, cookie presence, redirect targets."
    - "2FA device-loss recovery path [reviews LOW #7]: backup-code-entry route (`/2fa/backup`) ships in this plan; full reset SOP cross-referenced to RUNBOOK-2FA-RECOVERY.md (delivered by 11-09 Task 11-09-03) + admin reset via 11-05 seed-admins.sh. Not a 11-02 implementation task — CONTEXT note for downstream phases."
  must_haves:
    - dashboard/src/lib/auth.ts contains `twoFactor({ issuer: "Ifix AI Gateway" })` in plugins array
    - dashboard/src/lib/auth.ts contains top-level `rateLimit` block (NOT imported as plugin) with explicit `storage` selection (REDIS_URL → "secondary-storage" via ioredis ELSE "memory" with caveat comment + RUNBOOK cross-ref)
    - dashboard/src/lib/auth.ts session.expiresIn=1800 (30*60 seconds) AND cookieCache.enabled=true AND twoFactorEnabled + twoFactorVerified surface to the session cookie via additionalFields (Option A) OR session callback (Option B); chosen option documented in inline comment
    - dashboard/src/lib/auth-client.ts imports twoFactorClient and registers it
    - dashboard/src/lib/allowlist.ts exports isAllowedEmail named function
    - dashboard/src/components/auth/auth-shell.tsx exports AuthShell wrapper (centered card + 40×40 header logo + footer line per UI-SPEC §Visual Hierarchy)
    - dashboard/src/components/auth/otp-row.tsx exports OtpRow component (6-slot grouped digits, paste-handling, per-digit a11y per UI-SPEC §Layout Constraints — 44×52 slots, 8px gap, blinking caret)
    - dashboard/src/app/2fa/enroll/page.tsx implements 3-step state machine (qr → verify → backup) per UI-SPEC §screens 8/9/10
    - dashboard/src/app/2fa/challenge/page.tsx implements 6-digit OTP challenge with "Usar código de backup" ghost link per UI-SPEC §screen 11
    - dashboard/src/app/2fa/backup/page.tsx implements alphanumeric `xxxx-xxxx` backup-code entry per UI-SPEC §screen 14
    - dashboard/src/app/first-login/page.tsx implements welcome card + 2FA-obrigatório warning + CTA per UI-SPEC §screen 7
    - dashboard/src/app/signed-out/page.tsx implements logout landing per UI-SPEC §screen 15
    - dashboard/src/app/signup/page.tsx renders allowlist rejection Alert per UI-SPEC §screen 6
    - dashboard/src/app/login/page.tsx renders pending spinner + rate-limit Alert + session-expired Alert per UI-SPEC §screens 3/4/5
    - dashboard/src/app/settings/operadores/page.tsx implements 4-column stat strip + operator table per UI-SPEC §Settings → Operadores Tab
    - dashboard/src/middleware.ts two-stage check + matcher excludes /2fa, /first-login, /signed-out, /signup, /login, /api/auth, /_next, /favicon AND reads twoFactor claims from session cookie cache (NO DB call in Edge runtime)
    - dashboard/src/middleware.test.ts asserts 5 cases: session lacking twoFactorVerified claim → redirect /2fa/challenge; session lacking twoFactorEnabled claim → redirect /2fa/enroll; no session → /login?session_expired=1; loop-guard on /2fa/challenge no self-redirect; full-claim session → next()
    - dashboard/package.json gains only the packages that survived the Step-0 inventory; every installed net-new package slopcheck-audited `[OK]`
    - Staging migrate verified BEFORE prod migrate; prod migrate verified against ai_dashboard_prod creates twoFactor table + twoFactorEnabled column via SINGLE command `bunx @better-auth/cli@latest migrate`
    - Playwright route test (Task 11-02-05A) asserts: unauth /  → 307 to /login; auth-no-2fa / → 307 to /2fa/enroll; auth-2fa-unverified / → 307 to /2fa/challenge; auth-fully-verified / → 200
  key_links:
    - from: dashboard/src/lib/auth.ts
      to: dashboard/src/lib/allowlist.ts
      via: databaseHooks.user.create.before → isAllowedEmail(user.email)
      pattern: "isAllowedEmail"
    - from: dashboard/src/app/2fa/enroll/page.tsx
      to: dashboard/src/lib/auth-client.ts
      via: authClient.twoFactor.enable + verifyTotp
      pattern: "authClient.twoFactor"
    - from: dashboard/src/app/2fa/enroll/page.tsx
      to: qrcode npm package
      via: QRCode.toDataURL(totpURI) rendered as <img src=...>
      pattern: "from ['\"]qrcode['\"]"
    - from: dashboard/src/middleware.ts
      to: better-auth session cookie + cookieCache claims
      via: getSessionCookie + twoFactorVerified + twoFactorEnabled read
      pattern: "twoFactorVerified|twoFactorEnabled"
    - from: dashboard/src/app/settings/operadores/page.tsx
      to: dashboard/src/lib/auth.ts admin API
      via: list users + count active sessions + 2FA enrollment status
      pattern: "auth.api"
    - from: dashboard/src/components/auth/auth-shell.tsx
      to: 14 auth-route page.tsx files (login, signup, first-login, 2fa/*, signed-out)
      via: import { AuthShell } from "@/components/auth/auth-shell"
      pattern: "AuthShell"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/auth/* | unauthenticated POSTs for sign-in/sign-up/verify-totp |
| Browser → /2fa/* + /first-login + /signed-out | authenticated-but-unverified session boundary |
| dashboard container → DO Postgres ai_dashboard_prod | TOTP secrets at rest (encrypted by plugin) |
| Edge runtime middleware → session cookie cache | twoFactor claims must be cookie-readable (no DB call in Edge) |
| Dashboard container → infra-redis-1 (rate-limit storage if REDIS_URL set) | Trust internal Docker network; counters not PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-AUTH-01 | Spoofing | /sign-in/email credential stuffing | mitigate | rateLimit customRules `/sign-in/email: { window: 900, max: 5 }` per-IP; generic error message "E-mail ou senha inválidos" (UI-SPEC §Inline error copy) |
| T-11-AUTH-02 | Tampering | TOTP code replay within window | mitigate | better-auth twoFactor plugin auto-stores last-used counter and rejects re-use (verified node_modules/better-auth/dist/plugins/two-factor/totp/index.mjs) |
| T-11-AUTH-03 | Spoofing | TOTP brute-force on /two-factor/verify-totp | mitigate | rateLimit customRules `/two-factor/verify-totp: { window: 60, max: 5 }` |
| T-11-AUTH-04 | Spoofing | Email allowlist bypass via signUp | mitigate | databaseHooks.user.create.before runs server-side; rejects non-`@ifixtelecom.com.br` domains; D-13 |
| T-11-AUTH-05 | Tampering | Backup code re-use | mitigate | better-auth plugin auto-consumes each code one-time (verified plugin schema) |
| T-11-AUTH-06 | Spoofing | Session fixation post-2FA | mitigate | better-auth regenerates session ID on signIn + twoFactor verify endpoints |
| T-11-AUTH-07 | Information Disclosure | TOTP secret leak via response logs | mitigate | secret rendered ONCE on enroll step 1; never logged; QR + secret displayed client-side only |
| T-11-AUTH-08 | Denial of Service | rateLimit exhaustion locks admins out | accept | 4 admins total; lockout window 15min; documented in RUNBOOK-INCIDENTS class 4; 2FA recovery path documented in RUNBOOK-2FA-RECOVERY (11-09) |
| T-11-AUTH-09 | Spoofing | middleware redirect-loop if cookie claim missing | mitigate | Cookie-claim contract enforced via auth.ts cookieCache + additionalFields configuration; middleware.test.ts asserts no-loop behaviour; Playwright route test (11-02-05A) covers enroll→challenge round-trip end-to-end; staging smoke covers same flow against real DB |
| T-11-AUTH-10 | Denial of Service | rate-limit memory storage resets on container restart bypasses lockout | accept | 4 admins; documented limitation in auth.ts comment + RUNBOOK-INCIDENTS class 4; Redis-backed storage available as drop-in when REDIS_URL set (already on infra-redis-1 in stack) [reviews MEDIUM #6] |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | Pre-install slopcheck audit (Task 11-02-04 Step 1) for every package that survived the Step-0 inventory; all must return `[OK]` before install runs; commit results to plan evidence |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@.planning/phases/11-prod-hardening/11-UI-SPEC.md
@dashboard/src/lib/auth.ts
@dashboard/src/lib/auth-client.ts
@dashboard/src/lib/schema.ts
@dashboard/src/lib/utils.ts
@dashboard/src/lib/smoke.test.ts
@dashboard/src/middleware.ts
@dashboard/src/app/login/page.tsx
@dashboard/components.json
@dashboard/package.json
</context>

<interfaces>
<!-- Better Auth instance + client signatures the executor needs. Verified from node_modules 2026-05-27. -->

From `better-auth/plugins`:
```
twoFactor(options: { issuer: string, digits?: number, period?: number }): Plugin
```

From `better-auth/client/plugins`:
```
twoFactorClient(): ClientPlugin
authClient.twoFactor.enable({ password }): Promise<{ data: { totpURI, secret, backupCodes[] }, error }>
authClient.twoFactor.verifyTotp({ code }): Promise<{ data, error }>
authClient.twoFactor.verifyBackupCode({ code }): Promise<{ data, error }>
```

From `qrcode`:
```
import QRCode from 'qrcode';
QRCode.toDataURL(text: string, opts?: { errorCorrectionLevel?: 'L'|'M'|'Q'|'H', margin?: number, width?: number }): Promise<string>
// returns 'data:image/png;base64,iVBORw0KGgo...'
```

Top-level betterAuth({...}) config keys used in Phase 11:
```
rateLimit: { enabled: boolean, window: number, max: number, customRules: { [path: string]: { window: number, max: number } }, storage?: "memory" | "secondary-storage" }
session: {
  expiresIn: number,
  updateAge?: number,
  cookieCache?: { enabled: boolean, maxAge: number },
  additionalFields?: { [key: string]: { type: "string"|"boolean"|"number", required?: boolean, defaultValue?: any } }
}
databaseHooks: { user: { create: { before: (user) => Promise<{ data: User }> } } }
plugins: Plugin[]
```

Cookie-claim wiring options (executor MUST pick the one that works against the installed Better Auth version; document choice inline):
- Option A: `session.additionalFields = { twoFactorEnabled: { type: "boolean", defaultValue: false }, twoFactorVerified: { type: "boolean", defaultValue: false } }` + cookieCache enabled → claims surface in cookie automatically.
- Option B: session callback (if supported) that copies `user.twoFactorEnabled` + per-session `twoFactorVerified` flag into the cookie payload.

From UI-SPEC v2 §Component Inventory — auth-shell + otp-row primitives (NEW Phase 11 components):
```
// auth-shell.tsx — wraps every auth-route page
export function AuthShell({ children }: { children: ReactNode }): JSX.Element
// Layout: flex min-h-screen flex-col items-center justify-center gap-md p-lg + 40×40 header logo (Zap icon, primary-tinted bg) + footer line "Ifix · AI Gateway · ai-dashboard.converse-ai.app"

// otp-row.tsx — 6-slot OTP input
export function OtpRow({ value, onChange, state, autoFocus }: {
  value: string;            // current 6-char string
  onChange: (v: string) => void;
  state?: "default" | "invalid" | "success";
  autoFocus?: boolean;
}): JSX.Element
// Renders 6 slot divs with .otp .slot tokens; paste-handling for full 6-char paste; per-digit arrow nav; blinking caret on active slot
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <id>11-02-01</id>
  <title>Task 1: Domain allowlist utility + vitest [reviews unchanged — preserved from prior version]</title>
  <files>dashboard/src/lib/allowlist.ts, dashboard/src/lib/allowlist.test.ts</files>
  <read_first>
    - dashboard/src/lib/utils.ts (named-export convention, no default export)
    - dashboard/src/lib/smoke.test.ts (vitest skeleton — describe/expect/it + @/lib path alias)
    - 11-RESEARCH.md §Code Examples (allowlist body verbatim) + §Pitfall 1 references
    - 11-PATTERNS.md §allowlist.ts + §allowlist.test.ts (named export; 4 test cases minimum)
  </read_first>
  <behavior>
    - Test 1: isAllowedEmail("admin@ifixtelecom.com.br") returns true
    - Test 2: isAllowedEmail("user@gmail.com") returns false
    - Test 3: isAllowedEmail("no-at-sign") returns false; isAllowedEmail("") returns false (malformed)
    - Test 4: isAllowedEmail("Admin@IFIXTELECOM.COM.BR") returns true (case-insensitive)
  </behavior>
  <acceptance_criteria>
    - `bun test src/lib/allowlist.test.ts` reports 4 passing cases
    - `grep "DASHBOARD_ALLOWED_EMAIL_DOMAINS" dashboard/src/lib/allowlist.ts` returns ≥1 line
    - allowlist.ts uses `lastIndexOf("@")` (NOT regex) per D-13 + 11-RESEARCH §Don't Hand-Roll
  </acceptance_criteria>
  <action>
    Write the 4 vitest cases first (RED) using `import { describe, expect, it } from "vitest"` and `import { isAllowedEmail } from "@/lib/allowlist"`. Then implement allowlist.ts with named export `isAllowedEmail(email: string): boolean`. Env-driven via `process.env.DASHBOARD_ALLOWED_EMAIL_DOMAINS` (comma-separated lowercase domains; default `"ifixtelecom.com.br"`). Use `lastIndexOf("@")` (NOT regex) per D-13 + 11-RESEARCH §Don't Hand-Roll. JSDoc explains env-var override.
  </action>
  <verify>
    <automated>cd dashboard && bun test src/lib/allowlist.test.ts 2>&1 | grep -E "4 pass|✓.*case-insensitive"</automated>
  </verify>
  <done>allowlist.ts exports isAllowedEmail; 4 vitest cases pass; `grep "DASHBOARD_ALLOWED_EMAIL_DOMAINS" dashboard/src/lib/allowlist.ts` returns 1 line.</done>
</task>

<task type="auto" tdd="true">
  <id>11-02-02</id>
  <title>Task 2: Extend auth.ts with twoFactor + rateLimit (Redis-or-memory decision) + session + cookie claims + allowlist hook [reviews HIGH #2, HIGH #3, MEDIUM #5, MEDIUM #6]</title>
  <files>dashboard/src/lib/auth.ts, dashboard/src/lib/auth-client.ts, dashboard/src/lib/auth.test.ts</files>
  <read_first>
    - dashboard/src/lib/auth.ts (current 26 LOC — preserve baseURL/secret/database/advanced/export type Auth)
    - dashboard/src/lib/auth-client.ts (current shape — register twoFactorClient)
    - 11-RESEARCH.md §Pattern 2 (full target auth.ts body) + §State of the Art row 1 (rateLimit is BUILT-IN, not plugin)
    - 11-PATTERNS.md §dashboard/src/lib/auth.ts (insertion sites table)
    - 11-CONTEXT.md D-12, D-13, D-14, D-15 + specifics line 159 (issuer "Ifix AI Gateway")
    - 11-RESEARCH.md §Pitfall 4 (autoSignIn:false + middleware two-stage check) + §Pitfall 5 (existing 7-day sessions linger)
    - node_modules/better-auth/dist/index.d.ts (check whether `additionalFields` lives under `session` or `user` for the installed version — drives Option A vs B)
    - 11-UI-SPEC.md §Anchors for 11-02-PLAN.md (route paths + middleware exclusion list + session claim names)
  </read_first>
  <behavior>
    - auth.ts: exports `auth` with twoFactor plugin registered with issuer "Ifix AI Gateway"
    - auth.ts: emailAndPassword has `autoSignIn: false` (D-12 + Pitfall 4)
    - auth.ts: session.expiresIn === 1800 (D-15) AND updateAge === 300
    - auth.ts: top-level rateLimit.customRules includes "/sign-in/email", "/sign-up/email", "/two-factor/verify-totp"
    - auth.ts: rateLimit.storage decision EXPLICIT [reviews MEDIUM #6]: if `process.env.REDIS_URL` present → "secondary-storage" with ioredis adapter (existing dep in stack via infra-redis-1); else "memory" with inline comment "WARNING: counters reset on container restart — see RUNBOOK-INCIDENTS.md class 4"
    - auth.ts: databaseHooks.user.create.before throws when isAllowedEmail returns false
    - auth.ts: cookieCache.enabled=true AND maxAge=60 AND twoFactorEnabled + twoFactorVerified surface to the cookie via Option A (`session.additionalFields`) OR Option B (session callback) — chosen option documented in inline comment block immediately above session block [reviews HIGH #2]
    - auth-client.ts: imports + registers twoFactorClient plugin
    - auth.test.ts: STABLE-API tests only [reviews MEDIUM #5] — NO `auth.options` introspection. Exercises `auth.api.signUpEmail`, `auth.api.signInEmail`, `auth.api.getSession` via in-process Better Auth runtime. Cases:
      a. signUpEmail with email outside allowlist → rejects with allowlist error (assert HTTP-like response shape `{ error: { message: /allowlist/i } }`)
      b. signUpEmail with email inside allowlist → succeeds (data.user present)
      c. After successful signIn before TOTP enroll, `auth.api.getSession` payload includes boolean `twoFactorEnabled` (default false) AND boolean `twoFactorVerified` (default false) — proves cookie-claim wiring landed
      d. Rate-limit smoke: 6 sequential signInEmail calls with wrong password → 6th yields rate-limit error (proves customRules wired; behavior assertion, not config introspection)
  </behavior>
  <acceptance_criteria>
    - `bun test src/lib/auth.test.ts` reports all 4 cases passing
    - `grep -E 'twoFactor\(|rateLimit:|expiresIn: 30 \* 60|databaseHooks|twoFactorEnabled|twoFactorVerified|cookieCache' dashboard/src/lib/auth.ts` returns ≥7 hits
    - `grep -E '"memory"|"secondary-storage"' dashboard/src/lib/auth.ts` returns ≥1 hit (proves storage decision explicit)
    - `grep -E "twoFactorClient" dashboard/src/lib/auth-client.ts` returns ≥1 hit
    - auth.test.ts contains NO `auth.options` references (`grep -c "auth.options" dashboard/src/lib/auth.test.ts` returns 0)
  </acceptance_criteria>
  <action>
    Write auth.test.ts FIRST (RED) using `import { describe, expect, it } from "vitest"` + `import { auth } from "@/lib/auth"`. Use ONLY the stable public surface (`auth.api.signUpEmail`, `auth.api.signInEmail`, `auth.api.getSession`) — do NOT introspect `auth.options` (reviews MEDIUM #5). For case (c) inspect the session payload returned by `auth.api.getSession`; for case (d) loop 6 calls and assert the final response is rate-limited (status 429 OR error.code matches rate-limit shape per installed Better Auth version).

    Then edit auth.ts per 11-PATTERNS.md insertion-sites table with the cookie-claim wiring + explicit storage decision:
    1. Add imports: twoFactor from better-auth/plugins; isAllowedEmail from ./allowlist.
    2. emailAndPassword: enable=true, autoSignIn=false (Pitfall 4).
    3. Replace session block: expiresIn=30*60, updateAge=5*60, cookieCache={enabled:true, maxAge:60}, additionalFields={twoFactorEnabled:{type:"boolean",defaultValue:false}, twoFactorVerified:{type:"boolean",defaultValue:false}} (Option A). If Option A is not supported by installed version, fall back to Option B (session callback copying claims from user + per-session flag) and document the choice in a comment immediately above the session block. The middleware contract (Task 3) depends on these two boolean fields being readable from the cookie payload — that is the invariant; the wiring mechanism is a choice between A and B.
    4. Add top-level rateLimit block: enabled=true, window=60, max=100, customRules={"/sign-in/email":{window:900,max:5}, "/sign-up/email":{window:900,max:5}, "/two-factor/verify-totp":{window:60,max:5}}, storage=(process.env.REDIS_URL ? "secondary-storage" : "memory"). When storage resolves to "memory", include inline comment cross-referencing RUNBOOK-INCIDENTS.md class 4 and noting restart-resets-counters. When storage resolves to "secondary-storage", wire ioredis adapter per Better Auth `secondaryStorage` interface (existing infra-redis-1 in stack — confirmed reachable from dashboard container at REDIS_URL).
    5. Add plugins: [twoFactor({issuer:"Ifix AI Gateway"})] (D-12; issuer string locked per CONTEXT.md specifics line 159; SHA-1 default per @better-auth/utils/dist/otp.mjs:12).
    6. Add databaseHooks: user.create.before throws Error("E-mail fora do allowlist @ifixtelecom.com.br") when !isAllowedEmail(user.email) else returns {data:user} (D-13).
    7. Preserve baseURL, secret, database, advanced.database.generateId, `export type Auth = typeof auth;` verbatim.

    Edit auth-client.ts to import `twoFactorClient` from `better-auth/client/plugins` and add it to the client `plugins` array.
  </action>
  <verify>
    <automated>cd dashboard && bun test src/lib/auth.test.ts 2>&1 | tail -20 && grep -cE 'twoFactor\(|rateLimit:|expiresIn: 30 \* 60|databaseHooks|twoFactorEnabled|twoFactorVerified|cookieCache' src/lib/auth.ts | awk '{if ($1 >= 7) exit 0; else exit 1}' && grep -cE '"memory"|"secondary-storage"' src/lib/auth.ts | awk '{if ($1 >= 1) exit 0; else exit 1}'</automated>
  </verify>
  <done>auth.test.ts cases pass (all 4 stable-API cases including claim-shape assertion and rate-limit case); auth.ts grep returns ≥7 hits across the 7 anchors; storage decision grep ≥1; auth-client.ts contains `twoFactorClient`; NO `auth.options` references in auth.test.ts.</done>
</task>

<task type="auto">
  <id>11-02-03</id>
  <title>Task 3: Schema mirror policy (CLI canonical) + middleware two-stage check + middleware.test.ts + login extensions [reviews HIGH #3]</title>
  <files>dashboard/src/lib/schema.ts, dashboard/src/middleware.ts, dashboard/src/middleware.test.ts, dashboard/src/app/login/page.tsx</files>
  <read_first>
    - dashboard/src/lib/schema.ts (current 77 LOC; text-PK convention not UUID)
    - dashboard/src/middleware.ts (current 29 LOC; matcher excludes login + api/auth)
    - dashboard/src/app/login/page.tsx (current 99 LOC; Card + form skeleton)
    - 11-PATTERNS.md §dashboard/src/lib/schema.ts + §dashboard/src/middleware.ts + §dashboard/src/app/login/page.tsx (insertion sites)
    - 11-RESEARCH.md §Pitfall 4 + §Pitfall 5 + §Pitfall 7
    - 11-UI-SPEC.md §Error states (pt-BR copy rate-limit + session-expired) + §screens 3/4/5 (login pending/rate-limited/session-expired) + §Inheritance Notes + §Anchors (middleware exclusion list)
  </read_first>
  <acceptance_criteria>
    - schema.ts top-of-file comment documents "Better Auth CLI is canonical schema source-of-truth — do NOT add twoFactor declarations here"
    - schema.ts contains NO `twoFactor` pgTable declarations (no IntelliSense mirror per [reviews HIGH #3] resolved-to-CLI-only)
    - middleware.ts matcher excludes `login`, `signup`, `2fa`, `first-login`, `signed-out`, `api/auth`, `_next`, `favicon`
    - middleware.ts reads `twoFactorEnabled` + `twoFactorVerified` from session cookie (NO DB call; NO fetch to /api/auth/*)
    - middleware.test.ts has 5 cases passing (a-e) including loop-guard
    - login/page.tsx imports Alert + useSearchParams and renders 3 conditional states (pending spinner / rate-limited / session-expired) per UI-SPEC §screens 3/4/5
    - existing form copy unchanged (`grep -c "Gateway ifix-ai" dashboard/src/app/login/page.tsx` returns ≥1)
  </acceptance_criteria>
  <action>
    schema.ts — SCHEMA SOURCE-OF-TRUTH RULE [reviews HIGH #3 resolved]: the canonical schema source is the Better Auth CLI `bunx @better-auth/cli@latest migrate`. The Drizzle schema.ts file does NOT contain twoFactor declarations. Add a top-of-file comment block stating: "Better Auth CLI is the canonical schema source-of-truth for twoFactor tables. Do NOT add twoFactor / two_factor_enabled / backup_codes / two_factor_backup declarations here. Downstream code that needs to reference these tables uses Better Auth's runtime API (`auth.api.*`), not Drizzle direct queries. See 11-02-PLAN.md acceptance criteria."

    middleware.ts: update matcher line 28 to `"/((?!login|signup|2fa|first-login|signed-out|api/auth|_next|favicon).*)"` (per UI-SPEC v2 §Anchors). Replace middleware body with two-stage gate (RESEARCH Pitfall 4 + UI-SPEC line 434), reading session claims from the COOKIE CACHE (Task 2's auth.ts wiring made these readable in Edge runtime):
    - if !session OR cookie invalid → if pathname doesn't start with `/login` redirect to `/login?session_expired=1` else next()
    - if session present but `session.twoFactorEnabled !== true` → redirect to `/2fa/enroll`
    - if session present, twoFactorEnabled=true, but `session.twoFactorVerified !== true` → redirect to `/2fa/challenge`
    - else next()

    Hard rule: middleware MUST NOT call the DB or any /api/auth/* endpoint. Reads are from the session cookie only. If the cookie does not contain the claims, the configuration in Task 2 is wrong — the test below catches that.

    middleware.test.ts (NEW): vitest unit tests against the middleware function (mock NextRequest with controlled cookie payloads). Cases:
    a. Cookie absent → expects NextResponse.redirect to /login?session_expired=1.
    b. Cookie present with twoFactorEnabled=false → expects redirect to /2fa/enroll.
    c. Cookie present with twoFactorEnabled=true, twoFactorVerified=false → expects redirect to /2fa/challenge.
    d. Cookie present with both true → expects NextResponse.next() (no redirect).
    e. Loop guard: a request to /2fa/challenge with cookie state (twoFactorEnabled=true, twoFactorVerified=false) must NOT redirect back to /2fa/challenge (matcher exclusion + middleware exit) — proves no redirect-loop [reviews HIGH #2].

    login/page.tsx: imports add `import { Alert, AlertDescription } from "@/components/ui/alert"; import { useSearchParams } from "next/navigation";`. After `useRouter()` add `const params = useSearchParams(); const sessionExpired = params.get("session_expired") === "1"; const rateLimited = params.get("rate_limited") === "1";`. Inside `<CardContent>` BEFORE the existing `<form>` add conditional Alert blocks per UI-SPEC §Banner/alert copy:
    - rateLimited: Alert variant="destructive" with copy "Muitas tentativas. Aguarde {n}s antes de tentar novamente." (countdown reads Retry-After from prior 429 if available via state). UI-SPEC §screen 4.
    - sessionExpired: Alert variant="default" + Clock icon (muted-foreground) with copy "Sessão encerrada por inatividade. Faça login novamente." UI-SPEC §screen 5.
    - pending state: inline 14×14 spinner before "Entrando…" label per UI-SPEC §screen 3 (existing button disabled state + spinner span). Spec already shows .spinner CSS — use Tailwind `animate-spin` + `border-2 border-t-transparent rounded-full`.
    Preserve existing form layout/copy verbatim (UI-SPEC §Inheritance Notes).
  </action>
  <verify>
    <automated>cd dashboard && bun test src/middleware.test.ts 2>&1 | tail -15 && grep -E "twoFactorEnabled|twoFactorVerified" src/middleware.ts && grep -E "session_expired|rate_limited|Alert|animate-spin|Entrando" src/app/login/page.tsx && grep -E "2fa|first-login|signed-out|signup" src/middleware.ts && grep -c "twoFactor" src/lib/schema.ts | awk '{if ($1 == 0) exit 0; else exit 1}'</automated>
  </verify>
  <done>middleware.test.ts has 5 cases passing (a-e) including loop-guard; middleware.ts matcher excludes /2fa AND /signup AND /first-login AND /signed-out AND reads twoFactorEnabled+twoFactorVerified from session cookie (no DB call); schema.ts top-of-file comment documents CLI-canonical rule AND grep "twoFactor" returns 0 hits in schema.ts; login/page.tsx imports Alert + useSearchParams and renders rate-limited + session-expired + pending spinner; existing form copy unchanged.</done>
</task>

<task type="auto">
  <id>11-02-04</id>
  <title>Task 4: UI primitive inventory + slopcheck + install + AuthShell/OtpRow components + 7 new auth-route pages [reviews HIGH #1, MEDIUM #4]</title>
  <files>dashboard/src/app/2fa/enroll/page.tsx, dashboard/src/app/2fa/challenge/page.tsx, dashboard/src/app/2fa/backup/page.tsx, dashboard/src/app/first-login/page.tsx, dashboard/src/app/signed-out/page.tsx, dashboard/src/app/signup/page.tsx, dashboard/src/components/auth/auth-shell.tsx, dashboard/src/components/auth/otp-row.tsx, dashboard/package.json, .planning/phases/11-prod-hardening/11-02-ui-inventory.md, .planning/phases/11-prod-hardening/11-02-slopcheck.txt</files>
  <read_first>
    - dashboard/src/app/login/page.tsx (Card + form skeleton, max-w-sm centered layout, pt-BR copy)
    - 11-UI-SPEC.md §Visual Hierarchy + §Component Inventory + §Copywriting Contract (titles, descriptions, error states) + §Settings → Operadores Tab (NOT this task — Task 11-02-05) + §Inheritance Notes (route file list)
    - 11-PATTERNS.md §Pattern F (Dashboard Page Layout Card-centered auth) + §enroll/page.tsx + §challenge/page.tsx
    - 11-RESEARCH.md §Code Examples (authClient.twoFactor.enable → verifyTotp sequence) + §Pattern 2 + §Package Legitimacy Audit
    - dashboard/package.json (current dependency list)
    - dashboard/src/components/ui/*.tsx (inventory existing radix/shadcn primitives — REQUIRED for Step 0)
  </read_first>
  <acceptance_criteria>
    - 11-02-ui-inventory.md exists and per-package decision (KEEP_EXISTING / INSTALL_NEW) is documented for input-otp, dialog, qrcode, @types/qrcode
    - 11-02-slopcheck.txt exists; every INSTALL_NEW package returns `[OK]`
    - dashboard/src/components/auth/auth-shell.tsx exports `AuthShell` named function per UI-SPEC §Visual Hierarchy (40×40 logo + footer line)
    - dashboard/src/components/auth/otp-row.tsx exports `OtpRow` named function with paste-handling + per-digit a11y
    - 5 new page.tsx files exist: enroll, challenge, backup, first-login, signed-out
    - signup/page.tsx renders allowlist rejection Alert per UI-SPEC §screen 6
    - enroll/page.tsx has 3-step state machine ("qr"|"verify"|"backup") OR uses sub-routes (executor discretion per UI-SPEC §Inheritance Notes)
    - All 6 auth pages import `AuthShell` (`grep -c "AuthShell" dashboard/src/app/{2fa/*,first-login,signed-out,signup}/page.tsx | grep -v ":0"` shows ≥6 files)
    - Only INSTALL_NEW packages appear in dashboard/package.json beyond baseline
  </acceptance_criteria>
  <action>
    Step 0 — UI primitive inventory [reviews MEDIUM #4]: REQUIRED before any install runs.
    Run `ls dashboard/src/components/ui/` and `grep -E '"@radix-ui|"input-otp"|"qrcode"' dashboard/package.json`. For each of the 4 candidate net-new packages, determine if an existing primitive covers the function:
    - input-otp: check if a 6-digit OTP component exists OR if `<input type="text" inputMode="numeric" pattern="[0-9]{6}">` with custom CSS (per UI-SPEC §Layout Constraints OTP slot tokens 44×52, gap 8px, blinking caret) suffices. UI-SPEC v2 explicitly says: "INSTALL ONLY IF existing input primitive can't render 6-slot OTP grid with paste-handling + per-digit a11y". The prototype uses custom `.otp .slot` divs over plain `<input>` — that pattern works without input-otp. DEFAULT DECISION: INSTALL_NEW input-otp (paste-handling + a11y are non-trivial to hand-roll correctly); but if inventory finds existing solution, KEEP_EXISTING.
    - @radix-ui/react-dialog: check if dashboard/src/components/ui/dialog.tsx exists. UI-SPEC v2 explicitly says: "PREFER INLINE (prototype shows backup codes inline in LoginCard)". DEFAULT DECISION: KEEP_EXISTING (no install) — backup codes render inline in enroll step 3 LoginCard.
    - qrcode: check if any existing dep (react-qr-code, qr-code-styling) is pulled. DEFAULT DECISION: INSTALL_NEW qrcode (required for TOTP QR rendering).
    - @types/qrcode: only needed if qrcode is INSTALL_NEW.

    For each candidate, log the inventory decision to `.planning/phases/11-prod-hardening/11-02-ui-inventory.md` with format:
    ```
    ## input-otp
    Decision: KEEP_EXISTING | INSTALL_NEW
    Rationale: <existing primitive that covers OR justification for new>
    ```
    The final installed set is the candidates marked `INSTALL_NEW` only.

    Step 1 — Pre-install slopcheck audit (BLOCKING gate before any install command runs):
    For each package marked `INSTALL_NEW` in Step 0, run slopcheck per CLAUDE.md / Phase 11 RESEARCH §Package Legitimacy Audit:
    `slopcheck <name1> <name2> ...` (or canonical equivalent `node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs slopcheck ...`).
    Acceptance: ALL INSTALL_NEW packages return `[OK]`. If any returns `[ASSUMED]` or `[SUS]`, insert a checkpoint:human-verify gate against npmjs.com/package/{name} BEFORE proceeding. If any returns `[SLOP]`, abort the task and escalate via /gsd:plan-phase 11 --gaps.
    Save the slopcheck output to `.planning/phases/11-prod-hardening/11-02-slopcheck.txt` as evidence.

    Step 2 — Install net-new packages (only those flagged INSTALL_NEW, only after slopcheck passes):
    For shadcn primitives (input-otp if flagged): `cd dashboard && npx shadcn add input-otp`.
    For npm deps (qrcode + @types/qrcode if flagged): `cd dashboard && bun add qrcode @types/qrcode`.
    Verify after install: `grep -E '"qrcode"|"@types/qrcode"|"input-otp"' dashboard/package.json` returns at least the count flagged INSTALL_NEW.

    Step 3 — Create dashboard/src/components/auth/auth-shell.tsx (NEW shared component per UI-SPEC §Visual Hierarchy + §Layout Constraints):
    - "use client" directive; named export `AuthShell({ children }: { children: ReactNode })`.
    - Layout: outer `<main className="flex min-h-screen flex-col items-center justify-center gap-4 p-6 bg-background">` + 40×40 Zap-icon logo (primary-tinted background `color-mix(--primary 22% --card)`, 10px border-radius, 1px primary border at 40% opacity) above children + footer line `<p className="text-xs text-muted-foreground mt-4">Ifix · AI Gateway · ai-dashboard.converse-ai.app</p>`.

    Step 4 — Create dashboard/src/components/auth/otp-row.tsx (NEW per UI-SPEC §Layout Constraints OTP slot tokens):
    - "use client" directive; named export `OtpRow({ value, onChange, state, autoFocus })`.
    - If INSTALL_NEW input-otp chosen: wrap shadcn `<InputOTP maxLength={6}>` with 6 `<InputOTPSlot>` children per slot.
    - If KEEP_EXISTING: 6-div pattern per UI-SPEC §Layout Constraints (44×52 slot, 8px gap, blinking caret via `.caret` animation, paste-handling via `onPaste` reading clipboard text → splits across slots → focuses last filled slot).
    - State variants: default / invalid (red border + ring) / success (primary border + tinted fill) per UI-SPEC §Color §badge variants.

    Step 5 — Create dashboard/src/app/2fa/enroll/page.tsx (3-step state machine per UI-SPEC §screens 8/9/10):
    - "use client"; imports useRouter, useState, useEffect, AuthShell, OtpRow, authClient, ShieldCheck + Copy from lucide-react, QRCode from qrcode.
    - Wrap content in `<AuthShell>`. Inner `<Card className="w-full max-w-sm">`.
    - Step "qr": CardTitle "Configurar 2FA" + ShieldCheck icon, CardDescription per UI-SPEC §screen 8. Call `await authClient.twoFactor.enable({ password })` returning { totpURI, secret, backupCodes }. Render QR via QRCode.toDataURL(totpURI) — DOM contains `<img>` whose `src` begins with `data:image/png;base64,`. Manual-entry secret pill below QR (font-mono, padding 6px 10px, border-radius 6, 1px --border-strong, letter-spacing 0.08em, weight 600) with inline Copy ghost button. Metadata line "emissor: Ifix AI Gateway · algoritmo: SHA1 · 30s" (11px label muted). Primary CTA "Já escaneei, continuar" (UI-SPEC §Primary CTAs).
    - Step "verify": CardTitle "Confirmar código", OtpRow 6-digit input + Clock icon + 30s countdown "novo código em 17s" (tabnum). On submit `await authClient.twoFactor.verifyTotp({ code })`. Error copy "Código incorreto. Confirme o código atual no seu app autenticador e tente novamente." (UI-SPEC §Inline error copy). Primary CTA "Confirmar código".
    - Step "backup": CardTitle "Códigos de backup", alert-warning compact (padding 10px 12px) "Os códigos só são exibidos uma vez. Copie e salve antes de continuar.", 2-column grid (gap 6px) of 10 codes (01..10 prefix muted, xxxx-xxxx code font-mono weight 600, padding 5px 8px, border-radius 5px) with per-row Copy ghost button + "Copiar tudo" secondary button (sonner toast "10 códigos copiados para a área de transferência."). Primary CTA "Salvar e continuar" → router.push("/") + router.refresh().

    Step 6 — Create dashboard/src/app/2fa/challenge/page.tsx (single-step verify per UI-SPEC §screens 11/12/13):
    - "use client"; imports analog to enroll minus QR/qrcode.
    - `<AuthShell>` + `<Card className="w-full max-w-sm">`. CardTitle "Verificação em duas etapas" + ShieldCheck icon, CardDescription "Digite o código de 6 dígitos do seu app autenticador."
    - OtpRow 6-digit + countdown. On submit `await authClient.twoFactor.verifyTotp({ code })`. On error: setError per UI-SPEC §screen 12 (state="invalid" slots + inline CircleAlert + error copy). On success: state="success" 800ms transient (UI-SPEC §screen 13) THEN router.push("/") + router.refresh().
    - Ghost link "Usar código de backup" → router.push("/2fa/backup") per UI-SPEC §Primary CTAs.

    Step 7 — Create dashboard/src/app/2fa/backup/page.tsx (alphanumeric backup code entry per UI-SPEC §screen 14):
    - "use client"; `<AuthShell>` + `<Card max-w-sm>`. CardTitle "Código de backup", CardDescription "Digite um dos 10 códigos de backup gerados durante o cadastro 2FA. Cada código funciona apenas uma vez."
    - Single monospace `<Input>` with placeholder "xxxx-xxxx". On submit `await authClient.twoFactor.verifyBackupCode({ code })`. Primary CTA "Entrar com backup" + ghost secondary "Voltar ao app autenticador" → router.push("/2fa/challenge").

    Step 8 — Create dashboard/src/app/first-login/page.tsx (welcome gate per UI-SPEC §screen 7):
    - "use client"; `<AuthShell>` + `<Card max-w-sm>`. CardTitle "Bem-vindo, {name}" (read from session), CardDescription per UI-SPEC §screen 7.
    - alert-warning "2FA obrigatório para esta conta. Todas as contas @ifixtelecom.com.br são obrigadas a configurar TOTP no primeiro acesso." + 3-item bullet list (UI-SPEC §screen 7).
    - Primary CTA "Configurar 2FA agora" + ArrowRight 14px trailing icon → router.push("/2fa/enroll").

    Step 9 — Create dashboard/src/app/signed-out/page.tsx (logout landing per UI-SPEC §screen 15):
    - Server component OK. `<AuthShell>` + `<Card max-w-sm>`. CardTitle "Sessão encerrada", CardDescription per UI-SPEC §screen 15.
    - Alert default with CircleCheck primary icon: "Logout concluído com sucesso. Cookies de sessão removidos."
    - Primary CTA "Entrar novamente" → `<Link href="/login">` + ghost "Abrir runbook de operações" → `<Link href="https://github.com/.../RUNBOOK-INCIDENTS.md">` (external).

    Step 10 — Modify dashboard/src/app/signup/page.tsx (allowlist rejection per UI-SPEC §screen 6):
    - If signup/page.tsx exists, extend; if not, create new analog to login/page.tsx wrapped in AuthShell.
    - On signUp error from databaseHooks allowlist rejection (Task 2), render Alert variant="destructive" with CircleAlert + copy "Cadastro restrito a contas @ifixtelecom.com.br. Solicite acesso à equipe de operações para criar uma conta interna." (UI-SPEC §Banner/alert copy). E-mail input border red on rejection state.

    All pages use tabular-nums on OTP digits + monospace stack on font-mono regions (UI-SPEC §Typography). Preserve dark theme inherited from Phase 07 (UI-SPEC §Theme mode — dark only, no toggle).
  </action>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/11-02-ui-inventory.md && test -f .planning/phases/11-prod-hardening/11-02-slopcheck.txt && test -f dashboard/src/app/2fa/enroll/page.tsx && test -f dashboard/src/app/2fa/challenge/page.tsx && test -f dashboard/src/app/2fa/backup/page.tsx && test -f dashboard/src/app/first-login/page.tsx && test -f dashboard/src/app/signed-out/page.tsx && test -f dashboard/src/components/auth/auth-shell.tsx && test -f dashboard/src/components/auth/otp-row.tsx && grep -l "authClient.twoFactor" dashboard/src/app/2fa/*/page.tsx | wc -l | awk '{if ($1 >= 3) exit 0; else exit 1}' && grep -l "AuthShell" dashboard/src/app/2fa/*/page.tsx dashboard/src/app/first-login/page.tsx dashboard/src/app/signed-out/page.tsx | wc -l | awk '{if ($1 >= 5) exit 0; else exit 1}'</automated>
  </verify>
  <done>Inventory + slopcheck evidence files committed; AuthShell + OtpRow components exist with named exports; 5 new pages (enroll, challenge, backup, first-login, signed-out) exist; signup/page.tsx renders allowlist Alert; all 5+1 pages import AuthShell; enroll has 3-step flow + QR `<img data:image/png;base64,...>`; challenge has 6-digit OTP + backup-code ghost link; backup has alphanumeric input; first-login has welcome card + CTA; signed-out has logout confirmation; only INSTALL_NEW packages appear in dashboard/package.json beyond baseline.</done>
</task>

<task type="auto">
  <id>11-02-05</id>
  <title>Task 5: Settings → Operadores tab (NEW Phase 11) [reviews HIGH #1 UI-SPEC v2]</title>
  <files>dashboard/src/app/settings/operadores/page.tsx</files>
  <read_first>
    - 11-UI-SPEC.md §Settings → Operadores Tab (full section — stat strip + table columns + row examples + privacy rules)
    - 11-UI-SPEC.md §Inheritance Notes (Phase 07 Settings panel + tab bar)
    - dashboard/src/lib/auth.ts (auth.api.listUsers + auth.api.listSessions surfaces, if exposed)
    - 11-CONTEXT.md D-13 (allowlist scope = 4 operators)
  </read_first>
  <acceptance_criteria>
    - dashboard/src/app/settings/operadores/page.tsx exists and renders 4-column stat strip (Operadores / 2FA ativos / Sessões abertas / Rate-limit /login) per UI-SPEC §Settings → Operadores Tab
    - Page renders operator table with columns: Operador (avatar+name+email), Função (badge), Último login (relative tabnum), 2FA (status badge), Sessões (count tabnum), action menu
    - Page uses existing `.tbl` class (Phase 07 reuse) + existing `badge`/`card` shadcn blocks (no new install)
    - Page reads user list via stable Better Auth API (`auth.api.listUsers` OR direct DB query through Drizzle `user` table — executor discretion documented in inline comment)
    - Footer note "provisionados via scripts/dashboard/seed-admins.sh" (cross-ref 11-05) present
    - NO TOTP secrets / backup codes / password hashes / raw user UUIDs displayed (per UI-SPEC §Privacy / redaction rules)
  </acceptance_criteria>
  <action>
    Create dashboard/src/app/settings/operadores/page.tsx as server component (default Next.js 15 App Router pattern). Layout per UI-SPEC §Settings → Operadores Tab:
    1. Page header: title "Configurações", subtitle "ai-dashboard.converse-ai.app · {N} operadores · TOTP obrigatório", right-actions RefreshCw + Bell ghost icons.
    2. Tab bar: 4 tabs (Geral / Integrações / Chaves admin / Operadores active) — reuse existing Phase 07 tab pattern with 2px primary border-bottom on active.
    3. 4-column stat strip (gridTemplateColumns "repeat(4, 1fr)", gap 16px):
       - Operadores: count from listUsers, sub "allowlist @ifixtelecom.com.br"
       - 2FA ativos: count where twoFactorEnabled=true, sub "{N} pendente enroll" tone="warning" if any pending
       - Sessões abertas: count from listSessions, sub "idle timeout 30 min"
       - Rate-limit /login: "5/15min", sub "por IP · D-14"
    4. Operadores table using `.tbl` class. Columns per UI-SPEC: Operador (28×28 avatar initials primary-tinted + name + e-mail), Função (badge owner=warning / operator=neutral), Último login (relative copy "agora"/"há 3h"/"há 2d"/"nunca" — NOT raw timestamps per privacy rules), 2FA (badge healthy "ativo" / warning "aguardando enroll"), Sessões (count, num right-aligned tabnum), action menu (`···` ghost button — actions out of scope for this task).
    5. Row height 36px, hover tint `var(--row-hover)`, 1px `--border` bottom per UI-SPEC §Layout Constraints.
    6. Top-right action: "+ Provisionar operador" btn-primary btn-sm (modal pattern is FUTURE — Phase 11 ships button-no-op OR routes to seed-admins.sh runbook section; executor discretion documented inline).
    7. Footer note inside card: `.t-label .muted` "provisionados via scripts/dashboard/seed-admins.sh" cross-refs 11-05 plan.

    Data fetch: use server-side Better Auth admin API if exposed (`auth.api.listUsers({ headers: await headers() })`). If not exposed in installed Better Auth version, fall back to direct Drizzle query against `user` table (existing schema.ts). Document the choice in inline comment. NEVER expose: TOTP secrets, backup codes (post-enroll), password hashes, IP addresses, session cookie values, raw Better Auth user-id UUIDs (per UI-SPEC §Privacy / redaction rules).
  </action>
  <verify>
    <automated>test -f dashboard/src/app/settings/operadores/page.tsx && grep -E "Operadores|2FA ativos|Sessões abertas|Rate-limit" dashboard/src/app/settings/operadores/page.tsx | wc -l | awk '{if ($1 >= 4) exit 0; else exit 1}' && grep -E "seed-admins.sh|provisionados" dashboard/src/app/settings/operadores/page.tsx</automated>
  </verify>
  <done>operadores/page.tsx exists; 4-column stat strip rendered (4 labels grep-able); operator table uses .tbl + existing badge primitives; footer note cross-refs seed-admins.sh; NO new package installs beyond Task 4 set.</done>
</task>

<task type="auto">
  <id>11-02-05A</id>
  <title>Task 5A: Playwright route test — middleware redirect behavior end-to-end [reviews HIGH #2]</title>
  <files>dashboard/tests/e2e/auth-redirect.spec.ts</files>
  <read_first>
    - dashboard/package.json (check if @playwright/test already installed; if not, MUST be flagged in Task 4 Step 0 inventory)
    - 11-UI-SPEC.md §Anchors (route paths + middleware exclusion list)
    - dashboard/src/middleware.ts (after Task 3 edit)
    - dashboard/src/lib/auth.ts (after Task 2 edit)
  </read_first>
  <acceptance_criteria>
    - dashboard/tests/e2e/auth-redirect.spec.ts exists with 4 Playwright test cases
    - Test 1: unauthenticated GET / → 307 redirect to /login?session_expired=1
    - Test 2: authenticated session WITHOUT twoFactorEnabled cookie claim GET / → 307 redirect to /2fa/enroll
    - Test 3: authenticated session WITH twoFactorEnabled but WITHOUT twoFactorVerified cookie claim GET / → 307 redirect to /2fa/challenge
    - Test 4: authenticated session WITH both claims GET / → 200 (dashboard renders, no redirect)
    - Tests assert HTTP-level behavior (response codes + Location headers) NOT Better Auth internals
  </acceptance_criteria>
  <action>
    Pre-requisite check: `grep '"@playwright/test"' dashboard/package.json`. If absent, this package must have been flagged INSTALL_NEW in Task 4 Step 0 inventory and slopcheck-audited (Playwright is widely-known, expected `[OK]`). If still absent at this point, install via `cd dashboard && bun add -d @playwright/test && bunx playwright install --with-deps chromium` BUT only after slopcheck `[OK]` recorded in 11-02-slopcheck.txt.

    Create dashboard/tests/e2e/auth-redirect.spec.ts with 4 cases using `test('description', async ({ request }) => {...})`:

    Test 1 — unauthenticated:
    - `const res = await request.get('/', { maxRedirects: 0 });`
    - `expect(res.status()).toBe(307); expect(res.headers().location).toContain('/login');`

    Test 2 — authenticated, twoFactorEnabled=false:
    - Pre-create a session cookie with twoFactorEnabled:false, twoFactorVerified:false using Better Auth in-test signUp+signIn helper (use `auth.api.signUpEmail` + `auth.api.signInEmail`).
    - Issue GET / with that cookie. Assert 307 → /2fa/enroll.

    Test 3 — authenticated, twoFactorEnabled=true, twoFactorVerified=false:
    - Pre-enroll a test user (signUp + verify allowlist + enroll TOTP using known test-mode TOTP seed) so DB shows twoFactorEnabled=true. Sign in fresh (new session has twoFactorVerified=false).
    - Issue GET / with that cookie. Assert 307 → /2fa/challenge.

    Test 4 — authenticated, fully verified:
    - Complete TOTP verify in the same test using a test-known TOTP seed. Issue GET / with the post-verify cookie.
    - Assert 200 (dashboard renders).

    Each test asserts HTTP response codes + Location headers ONLY. No `auth.options` introspection [reviews MEDIUM #5]. Tests run against `bun run dev` server (Playwright config picks port 3000 OR test-specific port).
  </action>
  <verify>
    <automated>cd dashboard && bunx playwright test tests/e2e/auth-redirect.spec.ts 2>&1 | tail -20 | grep -E "4 passed|passed.*4"</automated>
  </verify>
  <done>auth-redirect.spec.ts exists with 4 cases; all 4 cases pass against running dev server; tests assert HTTP behavior only (no Better Auth internals introspection).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-02-06</id>
  <title>Task 6: [BLOCKING] Staging smoke — `bunx @better-auth/cli@latest migrate` against staging schema FIRST, validate enroll→challenge flow end-to-end [reviews HIGH BLOCKING]</title>
  <read_first>
    - 11-RESEARCH.md §Pitfall 7 (migrate CLI auto-discovery)
    - 11-RESEARCH.md §Assumption A6 (CLI auto-migrate compatibility)
    - scripts/deploy/migrate-dashboard.sh (existing migrate harness from Phase 10 — reference only; Phase 11 uses CLI direct)
    - 11-CONTEXT.md (Phase 10 already created bd_ai_dashboard_prod; this is incremental ALTER)
  </read_first>
  <what-built>
    [reviews HIGH BLOCKING] Staging gate: the prod migration against `ai_dashboard_prod` is irreversible mid-step. Operator MUST rehearse the SINGLE CANONICAL command `bunx @better-auth/cli@latest migrate` + the 2FA enroll → challenge round-trip against a STAGING target first, and only proceed to prod (Task 11-02-07) after the staging smoke is green. Staging options (operator picks one):
    - Option 1: Separate schema on the same DO Postgres instance — `dashboard_auth_staging` on bd_ai_dashboard_prod. Lowest cost; isolated from prod data.
    - Option 2: Dev DSN (`bd_ai_dashboard_dev` if exists) — re-purposed temporarily for the smoke.
    - Option 3: Ephemeral DO database fork (most isolated, highest cost) — only if Options 1/2 unavailable.
  </what-built>
  <how-to-verify>
    1. Operator picks a staging target and notes it in the resume signal (option number + schema name).
    2. Operator opens a temp .env with DASHBOARD_DATABASE_URL=$STAGING_DSN (mode 600).
    3. Dry-run: `cd dashboard && BETTER_AUTH_NO_INTERACTIVE=1 DASHBOARD_DATABASE_URL=$STAGING_DSN bunx @better-auth/cli@latest migrate --dry-run`. Inspect SQL.
    4. Real migrate against staging (SINGLE canonical command): `cd dashboard && BETTER_AUTH_NO_INTERACTIVE=1 DASHBOARD_DATABASE_URL=$STAGING_DSN bunx @better-auth/cli@latest migrate --y`. NO drizzle-kit push. NO schema.ts mirror sync. ONE command.
    5. Verify staging objects:
       - `psql $STAGING_DSN -c "\d dashboard_auth.twoFactor"` returns the table description.
       - `psql $STAGING_DSN -c "\d dashboard_auth.user"` shows `two_factor_enabled` column.
    6. Bring up the dashboard container (locally OR a staging deploy) pointed at $STAGING_DSN.
    7. END-TO-END FLOW SMOKE (manual OR via Playwright spec from Task 11-02-05A):
       a. Sign up a test admin with @ifixtelecom.com.br email → allowlist accepts.
       b. After signIn the middleware MUST redirect to /2fa/enroll (because twoFactorEnabled=false in cookie claim) — NOT a redirect loop.
       c. Complete enroll: scan QR with authenticator, type 6-digit code, save backup codes.
       d. Logout.
       e. Login again → middleware MUST redirect to /2fa/challenge (because twoFactorVerified=false on new session) — NOT a redirect loop.
       f. Enter TOTP code → reach dashboard home.
       g. RATE-LIMIT spot check: `for i in $(seq 1 6); do curl -s -o /dev/null -w "%{http_code}\n" -X POST $STAGING_BASE/api/auth/sign-in/email -H "Content-Type: application/json" -d '{"email":"x@ifixtelecom.com.br","password":"wrong"}'; done` — 6th MUST be 429.
       h. ALLOWLIST spot check: signUp with foo@gmail.com → 400/422 with allowlist error.
       i. BACKUP-CODE spot check: from /2fa/challenge click "Usar código de backup" → /2fa/backup → enter one of the 10 saved backup codes → reach dashboard.
    8. Save staging smoke evidence to `.planning/phases/11-prod-hardening/11-02-staging-smoke.md` (sanitized — no raw passwords, no DSN values; only outcomes per step).
    9. Operator destroys the temp .env (mode 600 + rm).

    Abort criteria — DO NOT advance to Task 11-02-07 (prod migrate) if any of:
    - Migrate dry-run shows unexpected DROP/ALTER on existing tables.
    - End-to-end redirect-loop observed (i.e., /2fa/challenge keeps redirecting to itself, or /2fa/enroll loops).
    - twoFactorEnabled / twoFactorVerified claims not present in the session cookie inspected via browser devtools (proves Task 2 cookie wiring is wrong — Option A must fall back to Option B OR vice versa).
    - Rate-limit returns 200 on the 6th attempt (proves rateLimit customRules not wired).
    - Backup-code path (step i) fails (proves /2fa/backup wiring broken).
  </how-to-verify>
  <resume-signal>Type "staging smoke PASS — option={1|2|3} schema=<name>; enroll→challenge no-loop; rate-limit 429 verified; allowlist 400 verified; backup-code path verified" OR describe blocker with the failing step number.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-02-07</id>
  <title>Task 7: [BLOCKING] Run `bunx @better-auth/cli@latest migrate` against ai_dashboard_prod — ONE canonical command [reviews HIGH BLOCKING resolved]</title>
  <read_first>
    - 11-RESEARCH.md §Pitfall 7 (migrate CLI auto-discovery — must run against PROD DSN, not DEV)
    - 11-RESEARCH.md §Assumption A6 (CLI auto-migrate compatibility) + suggested verification task
    - scripts/deploy/migrate-dashboard.sh (reference only; this task uses CLI direct)
    - 11-CONTEXT.md (Phase 10 already created bd_ai_dashboard_prod; this is incremental ALTER)
    - .planning/phases/11-prod-hardening/11-02-staging-smoke.md (evidence from Task 11-02-06 — MUST be green)
  </read_first>
  <what-built>
    Prod migration via SINGLE canonical command `bunx @better-auth/cli@latest migrate` (per Task 3 schema source-of-truth rule). This task runs ONLY after Task 11-02-06 staging smoke is green. NO Drizzle schema modifications. NO `drizzle-kit push`. ONE command, ONE source of truth.
  </what-built>
  <how-to-verify>
    Pre-condition: Task 11-02-06 staging-smoke evidence file exists and shows PASS for all 9 steps. If absent or partial, ABORT — do not run prod migrate.

    1. Operator opens a temp .env with DASHBOARD_DATABASE_URL=$PROD_DSN_DASHBOARD (DO bd_ai_dashboard_prod, mode 600).
    2. Dry-run against prod: `cd dashboard && BETTER_AUTH_NO_INTERACTIVE=1 DASHBOARD_DATABASE_URL=$PROD_DSN_DASHBOARD bunx @better-auth/cli@latest migrate --dry-run`. Compare against staging dry-run output — should be structurally identical.
    3. Real migrate (ONE canonical command): `cd dashboard && BETTER_AUTH_NO_INTERACTIVE=1 DASHBOARD_DATABASE_URL=$PROD_DSN_DASHBOARD bunx @better-auth/cli@latest migrate --y`.
    4. Verify the new objects landed: `psql $PROD_DSN_DASHBOARD -c "\d dashboard_auth.twoFactor"` returns the table description + `psql $PROD_DSN_DASHBOARD -c "\d dashboard_auth.user" | grep two_factor_enabled` returns 1 row.
    5. Operator destroys the temp .env (mode 600 + rm).
    6. Optional Pitfall 5 cleanup: existing 7-day sessions linger. `psql $PROD_DSN_DASHBOARD -c "DELETE FROM dashboard_auth.session WHERE expires_at > NOW() + INTERVAL '30 minutes';"` after deploy (forces re-login of all 4 admins; coordinate via WhatsApp pre-window).
    7. Fallback (Assumption A6 wrong): if CLI doesn't auto-discover plugin schema, surface the error verbatim to operator AND check 11-02-staging-smoke evidence for the same error — if staging caught it, the fallback was documented there.
  </how-to-verify>
  <resume-signal>Type "approved — twoFactor table + two_factor_enabled column verified in ai_dashboard_prod; session cleanup {ran|deferred}" OR describe migrate error verbatim.</resume-signal>
</task>

</tasks>

<verification>
- `cd dashboard && bun test` exits 0 (allowlist.test.ts + auth.test.ts + middleware.test.ts pass).
- `cd dashboard && bunx playwright test tests/e2e/auth-redirect.spec.ts` exits 0 (4 cases pass) [reviews HIGH #2].
- `cd dashboard && bun run lint` exits 0 (no new violations).
- `cd dashboard && bun run build` exits 0 (Next build with new 2FA + first-login + signed-out + signup + settings/operadores routes).
- `grep -cE "twoFactor\(|rateLimit:|expiresIn: 30 \* 60|twoFactorEnabled|twoFactorVerified|cookieCache" dashboard/src/lib/auth.ts` returns ≥7.
- `grep -cE '"memory"|"secondary-storage"' dashboard/src/lib/auth.ts` returns ≥1 (explicit storage decision).
- `grep -c "twoFactor" dashboard/src/lib/schema.ts` returns 0 (CLI is canonical, no IntelliSense mirror).
- `psql $PROD_DSN_DASHBOARD -c "\dt dashboard_auth.twoFactor"` returns 1 row (post-prod-migrate).
- `.planning/phases/11-prod-hardening/11-02-staging-smoke.md` committed showing staging PASS BEFORE prod migrate.
- `.planning/phases/11-prod-hardening/11-02-slopcheck.txt` committed with all INSTALL_NEW packages `[OK]`.
- `.planning/phases/11-prod-hardening/11-02-ui-inventory.md` committed with KEEP_EXISTING/INSTALL_NEW decisions per package.
- middleware.test.ts loop-guard case (e) passes — no redirect loop on /2fa/challenge.
- All 7 new auth pages exist: 2fa/enroll, 2fa/challenge, 2fa/backup, first-login, signed-out, signup (extended), settings/operadores.
- AuthShell + OtpRow shared components exist and are imported by all 6+ auth pages.
</verification>

<success_criteria>
- auth.ts + auth-client.ts + schema.ts + middleware.ts + login/page.tsx edited per insertion sites.
- 7 new route pages + 2 shared auth components + allowlist.ts + allowlist.test.ts + auth.test.ts + middleware.test.ts + Playwright spec created per UI-SPEC v2.
- Settings → Operadores tab renders 4-column stat strip + operator table.
- Staging smoke verified BEFORE prod migrate.
- `bunx @better-auth/cli@latest migrate` (ONE canonical command) verified against ai_dashboard_prod — no Drizzle push, no schema.ts twoFactor mirror.
- Playwright route tests assert redirect targets via HTTP (4 cases pass) — proves cookie-claim wiring landed.
- All vitest cases pass (including stable-API auth.test.ts and middleware loop-guard).
- D-12 + D-13 + D-14 + D-15 implementations grep-able in auth.ts AND cookie-claim wiring grep-able AND rate-limit storage decision grep-able.
- Only `INSTALL_NEW`-flagged dashboard packages pre-install slopcheck audited and installed.
- 2FA device-loss recovery cross-ref note in CONTEXT (backup-code-entry route ships in this plan; full reset SOP delivered by 11-09 + 11-05).
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies (gateway Go binary, scripts Python smokes). Dashboard adds AT MOST 4 net-new packages strictly required for PRD-06 2FA UI (gated by Step 0 inventory + slopcheck `[OK]` pre-install) PLUS @playwright/test for e2e route tests (also slopcheck-gated).
- Truth 2: pt-BR copy from UI-SPEC v2 §Copywriting Contract locked verbatim (no English fallback).
- Truth 6: gateway/dashboard images stay at `:latest-dev` until 11-10 GHA retrigger fix (D-18.4).
- Truth 8 (cross-cutting): NO raw passwords, secrets, or DSNs in commit history, plan files, or evidence. Test passwords use disposable random strings; production credentials never appear in plan instructions.
- Schema source-of-truth: Better Auth CLI migrate is THE canonical command (single source of truth); no Drizzle mirror, no `drizzle-kit push`.
- Cookie-claim contract: middleware reads twoFactorEnabled+twoFactorVerified from session cookie cache; auth.ts wires this via additionalFields OR session callback; Playwright spec asserts behavior end-to-end.
- BLOCKING staging smoke before prod migrate ensures irreversible prod schema change is rehearsed.
- 2FA device-loss recovery: backup-code-entry route (/2fa/backup) ships in this plan; full operator reset SOP via 11-05 seed-admins.sh + RUNBOOK-2FA-RECOVERY.md (11-09-03).
</cross_cutting_truths>

<output>
Create `.planning/phases/11-prod-hardening/11-02-SUMMARY.md` when done with: files modified count + paths, vitest output snippet (allowlist + auth + middleware tests), Playwright output snippet (4 redirect cases), `bun run build` outcome, inventory decisions per package (KEEP_EXISTING vs INSTALL_NEW), slopcheck evidence file path + all-`[OK]` confirmation for INSTALL_NEW set, staging smoke evidence file reference, prod migrate dry-run output (sanitized — no DSN), confirmation operator ran `bunx @better-auth/cli@latest migrate` against prod (ONE canonical command).
</output>
