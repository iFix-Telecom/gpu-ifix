---
phase: 11
plan: 11-10
slug: human-uat-and-verification
wave: 3
depends_on: [11-02, 11-04, 11-05, 11-06, 11-07, 11-08, 11-09]
requirements: [PRD-04, PRD-06]
files_modified:
  - path: .planning/phases/11-prod-hardening/11-HUMAN-UAT.md
    kind: NEW
  - path: .planning/phases/11-prod-hardening/11-VERIFICATION.md
    kind: NEW
autonomous: false
estimated_duration: 150
scope_note: "5 tasks borderline — matches Phase 10-06 HUMAN-UAT precedent (8 scenarios S0 + S1-S6 + S8 + author scenario sheet + final rollup); splitting causes evidence-chain disconnect between scenario sheet → UAT execution → VERIFICATION rollup → state advance. Reviews MEDIUM (M3): S0 (GHA workflow_dispatch + image pull, formerly S7) is RE-ORDERED to a Wave 3 PREREQUISITE gate that MUST run BEFORE S1-S6 + S8 (sequential dependency, not parallel). If S0 FAILS, S1-S6 + S8 DO NOT execute; phase status = blocked, not passed_partial. Reviews MEDIUM (M1): all tenant API key references use env-var labels ${IFIX_KEY_<TENANT_SLUG^^>}; zero `ifix_sk_...` literals in 11-HUMAN-UAT.md or 11-VERIFICATION.md (post-commit grep gate). Reviews MEDIUM (M2): state advance uses canonical `gsd-sdk query phase complete 11` (NOT manual STATE.md edits); ROADMAP.md plan-checkbox flips are done by hand with diff-only commits. Reviews LOW (L4): REDACTED EVIDENCE rule enforced for 2FA scenarios."
---

<objective>
Final phase sign-off. Operator-driven manual UAT covering: (a) dashboard 2FA enroll → logout → login with TOTP end-to-end (PRD-06); (b) rate-limit /sign-in/email returns 429 after 5 attempts (D-14); (c) signUp rejects non-@ifixtelecom.com.br (D-13); (d) gatewayctl debug emit-error → Sentry event lands in ifix-ai-gateway-prod (D-18.2); (e) gatewayctl key list returns aligned table (D-18.3); (f) per-env keys diff verification using SANITIZED first-4-char prefix recipe (D-19); (g) dashboard admin seeded via seed-admins.sh (D-13); (h) GHA workflow_dispatch produces :v1.0.0 image — RE-ORDERED to S0 PREREQUISITE gate (reviews MEDIUM M3). All tenant API keys referenced via env-var labels (`${IFIX_KEY_TELEFONIA}`, `${IFIX_KEY_CHAT_IFIX}`, etc.) — zero `ifix_sk_...` literals in scenario docs or evidence (reviews MEDIUM M1). All 2FA evidence captures REDACTED confirmation screens only — NEVER QR codes, TOTP digits, or backup codes (reviews LOW L4). Roll all evidence into 11-VERIFICATION.md using canonical `gsd-sdk query phase complete 11` for state advance (reviews MEDIUM M2 — manual STATE.md edits PROHIBITED). Status: passed (or passed_partial with documented exceptions). Cascade-close not required (Phase 10 already closed 02/03/04/05 deferrals); this plan closes Phase 11 itself.
</objective>

<must_haves>
  truths:
    - "All 19 D-XX decisions covered via the cumulative Phase 11 plan set (11-01..11-09); 11-10 verifies the live operator-side residual"
    - "11-VERIFICATION.md status: passed OR passed_partial per Phase 06.5/Phase 10 precedent format"
    - "PRD-04 (full): RUNBOOK-INCIDENTS.md + POSTMORTEM-TEMPLATE.md + RUNBOOK-2FA-RECOVERY.md verified live (grep gate + cross-ref test)"
    - "PRD-06: dashboard 2FA UAT signed by operator (TOTP enroll + challenge + backup-codes display + rate-limit 429 + signUp rejection)"
    - "D-18.2: Sentry event observed within 5s of gatewayctl debug emit-error invocation (id 4511455942017024 project)"
    - "D-18.4 RE-ORDERED [reviews MEDIUM M3]: GHA workflow_dispatch + :v1.0.0 image pull is a WAVE 3 PREREQUISITE gate (S0) that runs BEFORE S1-S6 + S8 SEQUENTIALLY. If S0 FAILS → ABORT (do NOT execute S1-S6, S8); phase status = blocked, not passed_partial."
    - "Spend cap $5 absolute Phase 11 — total spend across 11-06/11-07/11-08 stays within"
    - "Cross-cutting hard rule [reviews MEDIUM M1]: NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in scenario docs, evidence, or commits. Tenant references via env-var labels `${IFIX_KEY_<TENANT_SLUG^^>}` only (operator sets via `source /etc/ifix/keys.env` (0600) BEFORE running scenario; `unset IFIX_KEY_*` after scenario completes). Evidence redacts to first-4-char prefix. [reviews LOW L4]: NO QR codes, 2FA TOTP digits, or backup codes captured in evidence; ONLY confirmation messages and metadata."
    - "Canonical state-advance command [reviews MEDIUM M2]: USE `gsd-sdk query phase complete 11` for STATE.md mutation (CONFIRMED VERB — exists in current gsd-tools). Manual STATE.md edits are PROHIBITED (can corrupt GSD state). ROADMAP.md plan-checkbox flips (`[ ]` → `[x]`) are done by hand with `git diff` review BEFORE commit."
  must_haves:
    - .planning/phases/11-prod-hardening/11-HUMAN-UAT.md committed with operator-signed scenario sheet (S0 PREREQUISITE first + S1..S6 + S8)
    - .planning/phases/11-prod-hardening/11-VERIFICATION.md committed with status: passed/passed_partial + per-PRD verdict
    - Phase 11 closed via canonical `gsd-sdk query phase complete 11` (NOT manual STATE.md edit)
    - Zero `ifix_sk_...` literals in 11-HUMAN-UAT.md OR 11-VERIFICATION.md (grep gate enforced)
    - 11-HUMAN-UAT.md contains "REDACTED" rule appearing ≥3 times (S1 enroll scenario, S3 rate-limit scenario, evidence appendix)
    - S0 ordered first in scenario sheet with explicit "PREREQUISITE GATE — ABORT ON FAIL" header
  key_links:
    - from: 11-HUMAN-UAT.md S0 (prerequisite) → S1..S8 sign-offs
      to: 11-VERIFICATION.md rollup
      via: cross-ref + verdict table + ordering check
      pattern: "S[0-8]"
    - from: 11-VERIFICATION.md
      to: all Wave 2 evidence files (11-06/07/08-EVIDENCE.md) + 11-02-staging-smoke.md
      via: cross-ref bullets
      pattern: "EVIDENCE\\.md|staging-smoke"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live prod dashboard + gateway | manual auth flow exercises real endpoints |
| operator -> Sentry web UI | post-test verification of panic-path event landing |
| operator -> GHA + GHCR | workflow_dispatch + image pull (PREREQUISITE gate) |
| operator -> local secret store /etc/ifix/keys.env | tenant API keys sourced into env vars BEFORE scenario; unset AFTER |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-UAT-01 | Spoofing | UAT confirms wrong dashboard instance | mitigate | Operator MUST hit ai-dashboard.converse-ai.app explicitly (NOT dev); verify Better Auth URL in browser; confirm dashboard image tag :latest-dev or :v1.0.0 post-S0 retrigger |
| T-11-UAT-02 | Information Disclosure | TOTP secret / QR / backup codes captured in evidence | mitigate | [reviews LOW L4] Explicit REDACTED EVIDENCE rule in 11-HUMAN-UAT.md: NEVER screenshot the QR code, secret string, 6-digit TOTP digits, or backup codes. QR-code "evidence" = post-enrollment SUCCESS screen only. Backup-codes "evidence" = "codes saved successfully" confirmation only. Evidence audit task greps for "REDACTED" rule ≥3 occurrences. |
| T-11-UAT-03 | Repudiation | UAT scenario marked PASS without evidence | mitigate | Each scenario S0..S8 requires a non-sensitive evidence form (curl HTTP status, Sentry event URL, GHA run ID, manifest digest, sanitized diff output, written confirmation) saved into 11-HUMAN-UAT.md |
| T-11-UAT-04 | Information Disclosure | Raw API key literals in scenario docs | mitigate | [reviews MEDIUM M1] ALL curl examples use env-var labels `${IFIX_KEY_<TENANT_SLUG^^>}` (e.g. `${IFIX_KEY_TELEFONIA}`, `${IFIX_KEY_CHAT_IFIX}`); operator sources `/etc/ifix/keys.env` (0600) BEFORE session; unsets env after. Post-commit grep `! grep -E 'ifix_sk_[a-z0-9]{30,}' 11-HUMAN-UAT.md 11-VERIFICATION.md` returns ZERO matches. |
| T-11-UAT-05 | Tampering | Manual STATE.md edits corrupt GSD state | mitigate | [reviews MEDIUM M2] Task 11-10-04 USES canonical `gsd-sdk query phase complete 11` (verb confirmed via `gsd-sdk query phase complete <N>`). Manual STATE.md edits PROHIBITED. ROADMAP.md is the ONLY file edited by hand (plan-checkbox flips only); `git diff .planning/ROADMAP.md` reviewed BEFORE commit. |
| T-11-UAT-06 | DoS | S0 GHA failure mid-phase blocks UAT closure | mitigate | [reviews MEDIUM M3] S0 PREREQUISITE GATE: runs FIRST. If S0 FAILS, ABORT (do not execute S1-S6, S8); phase = blocked. Restore release artifacts via gateway/docs/RUNBOOK-DEPLOY.md §GHA retrigger procedure; resume from S0 on next attempt. |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@.planning/phases/11-prod-hardening/11-VALIDATION.md
@.planning/phases/11-prod-hardening/11-UI-SPEC.md
@.planning/phases/11-prod-hardening/11-02-PLAN.md
@.planning/phases/11-prod-hardening/11-04-PLAN.md
@.planning/phases/11-prod-hardening/11-05-PLAN.md
@.planning/phases/11-prod-hardening/11-06-PLAN.md
@.planning/phases/11-prod-hardening/11-07-PLAN.md
@.planning/phases/11-prod-hardening/11-08-PLAN.md
@.planning/phases/11-prod-hardening/11-09-PLAN.md
@.planning/phases/10-prod-deploy-ai-gateway/10-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <id>11-10-01</id>
  <title>Task 1: Author 11-HUMAN-UAT.md scenario sheet [reviews MEDIUM M1 + M3 + LOW L4] (S0 PREREQUISITE gate first, env-var labels for all keys, REDACTED EVIDENCE rule)</title>
  <files>.planning/phases/11-prod-hardening/11-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/10-prod-deploy-ai-gateway/10-VERIFICATION.md (precedent format for scenario sign-off — pitfalls_hit + deviations sections)
    - 11-VALIDATION.md Manual-Only Verifications table (test instructions per UAT scenario)
    - 11-CONTEXT.md D-12..D-19 (sources for each scenario; tenant slugs for env-var labels)
    - 11-UI-SPEC.md (dashboard 2FA UI screens UAT must traverse)
  </read_first>
  <acceptance_criteria>
    - File exists: `.planning/phases/11-prod-hardening/11-HUMAN-UAT.md`
    - Scenario count = 8: `grep -cE "^## S[0-8] " 11-HUMAN-UAT.md` returns 8
    - S0 ordered FIRST (line number of S0 header < line numbers of S1..S6, S8 headers); verify via `grep -nE "^## S[0-8] " 11-HUMAN-UAT.md | head -1` shows S0
    - S0 contains literal string "PREREQUISITE GATE" and "ABORT" in its header or body
    - S0 contains explicit "If S0 FAILS, ABORT — do NOT proceed to S1-S6, S8" callout (case-insensitive match acceptable)
    - Zero raw key literals: `grep -E 'ifix_sk_[a-z0-9]{30,}' 11-HUMAN-UAT.md` returns ZERO matches
    - Env-var labels present: `grep -cE '\$\{IFIX_KEY_[A-Z_]+\}' 11-HUMAN-UAT.md` returns ≥4 (at least 4 distinct tenant references)
    - REDACTED rule appears ≥3 times: `grep -ic REDACTED 11-HUMAN-UAT.md` returns ≥3 (S1 enroll, S3 rate-limit, evidence appendix)
    - Pre-UAT checklist instructs operator: `source /etc/ifix/keys.env` BEFORE session, `unset IFIX_KEY_*` AFTER
  </acceptance_criteria>
  <action>
    Create .planning/phases/11-prod-hardening/11-HUMAN-UAT.md with the structure described below. Use Markdown headers. NO inline code blocks containing literal `ifix_sk_...` strings — always reference keys via `${IFIX_KEY_<TENANT_SLUG^^>}` env-var labels.

    Header section: title "Phase 11 — HUMAN-UAT Scenario Sheet"; metadata lines for Phase, Operator, Date, Status (in_progress | passed | passed_partial | failed | blocked).

    Pre-UAT environment check (operator signs first) — checkbox list including:
    - Wave 1 plans (11-01..11-05) committed and on develop
    - Wave 2 plans (11-06/07/08) committed with EVIDENCE files
    - Wave 3 plan 11-09 committed (RUNBOOK-INCIDENTS + POSTMORTEM-TEMPLATE + RUNBOOK-2FA-RECOVERY)
    - 11-02-staging-smoke.md committed showing staging smoke green BEFORE prod migrate
    - Operator has Sentry web access (ifix-ai-gateway-prod project 4511455942017024)
    - Operator has Vast.ai dashboard access
    - Operator has GitHub repo write permission (gh CLI authenticated)
    - Operator has Authenticator app (1Password / Google Authenticator) ready
    - $5 Phase 11 spend cap acknowledged
    - **Tenant API keys**: operator MUST run `source /etc/ifix/keys.env` (file must be mode 0600, owner-only) to populate `${IFIX_KEY_CONVERSEAI}`, `${IFIX_KEY_TELEFONIA}`, `${IFIX_KEY_CHAT_IFIX}`, `${IFIX_KEY_COBRANCAS}`, `${IFIX_KEY_CAMPANHAS}`, `${IFIX_KEY_VOICE_API}`, `${IFIX_KEY_UAT10_TEST}` BEFORE running any scenario. After all scenarios complete, operator MUST run `unset IFIX_KEY_CONVERSEAI IFIX_KEY_TELEFONIA IFIX_KEY_CHAT_IFIX IFIX_KEY_COBRANCAS IFIX_KEY_CAMPANHAS IFIX_KEY_VOICE_API IFIX_KEY_UAT10_TEST`. Inline literal `ifix_sk_...` keys are PROHIBITED in this sheet and in shell history (use `export HISTFILE=/dev/null` for the session).

    ### Cross-cutting REDACTED EVIDENCE rule (appears at top, prominently)

    > **REDACTED EVIDENCE ONLY**: For 2FA scenarios (S1, S2, S3), NEVER screenshot or paste:
    > - The TOTP QR code itself (capture the post-enrollment SUCCESS screen only — e.g. "2FA enabled" confirmation)
    > - The 6-digit TOTP code (capture textual confirmation: "code accepted, redirected to /")
    > - The 10 backup codes (capture the "codes saved successfully" / "10 códigos copiados" toast confirmation only)
    > - Passwords (including disposable test passwords)
    > - Any `Bearer ifix_sk_...` Authorization header value
    >
    > Evidence MUST be textual confirmation or sanitized HTTP status sequences.

    ## S0 — GHA workflow_dispatch produces :v1.0.0 image (D-18.4) — PREREQUISITE GATE [reviews MEDIUM M3]

    > **PREREQUISITE GATE — RUN FIRST. ABORT ON FAIL.**
    > This scenario alters release artifacts (ghcr.io image tags). It MUST run BEFORE S1-S6, S8 (sequential dependency, not parallel). Running it concurrently with auth/security UAT muddles which run produced which evidence.
    >
    > **If S0 FAILS, ABORT — do NOT proceed to S1-S6, S8. Phase status = `blocked`, NOT `passed_partial`.** Restore release artifacts via gateway/docs/RUNBOOK-DEPLOY.md §GHA retrigger procedure; resume from S0 on next attempt.

    Steps:
    1. `gh workflow run build-gateway.yml --ref v1.0.0 -f tag=v1.0.0` → capture run id.
    2. `gh run watch <run-id>` → completes successfully.
    3. `docker pull ghcr.io/ifixtelecom/ifix-ai-gateway:v1.0.0` → succeeds; capture manifest digest.
    4. Repeat steps 1-3 for build-dashboard.yml.

    Evidence: paste gh run id + docker pull manifest digest (digest is non-secret).
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    **If FAIL**: ABORT Wave 3 UAT. Phase status = blocked. Do NOT continue to S1-S6, S8.

    ## S1 — Dashboard 2FA enroll end-to-end (D-12, PRD-06) [REDACTED EVIDENCE ONLY]
    Steps:
    1. Operator hits https://ai-dashboard.converse-ai.app/signup with email @ifixtelecom.com.br + disposable password (NEVER a real admin password; NEVER paste the password into this sheet).
    2. Verify allowlist accepts; redirected to /2fa/enroll.
    3. Scan QR with authenticator; type 6-digit code on /2fa/enroll step 2. **REDACTED EVIDENCE RULE**: DO NOT screenshot the QR code, the secret string, or the 6-digit code. Capture textual confirmation only ("QR rendered, 6-digit code accepted").
    4. Backup codes shown in Dialog; copy via "Copiar tudo" button (sonner toast "10 códigos copiados"). **REDACTED EVIDENCE RULE**: DO NOT screenshot the backup codes themselves. Capture only the "10 códigos copiados" toast confirmation. Operator manually transfers codes to password manager out-of-band.
    5. Click "Salvar e continuar" → redirected to /.
    Evidence (REDACTED form only): textual sentence — e.g. "QR rendered (post-success screen captured, NOT the QR itself), 6-digit code accepted, 10 backup codes displayed (toast captured, NOT the codes themselves), redirected to home". NO TOTP digits, QR images, or backup codes in this evidence.
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S2 — Dashboard 2FA challenge on subsequent login (D-12, PRD-06) [REDACTED EVIDENCE ONLY]
    Steps:
    1. Logout from dashboard.
    2. Hit /login → e-mail + disposable password → 200 → middleware redirects to /2fa/challenge.
    3. Type 6-digit TOTP code → router.push("/") → reach dashboard home. **REDACTED EVIDENCE RULE**: NO TOTP digit capture.
    Evidence (REDACTED form only): textual confirmation; "logout OK, login redirected to /2fa/challenge, TOTP accepted (digit NOT captured), reached dashboard home".
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S3 — Rate-limit /sign-in/email returns 429 after 5 attempts (D-14, PRD-06) [REDACTED EVIDENCE ONLY]
    Steps:
    1. Operator MUST use a DISPOSABLE test email (e.g. `uat-rate-limit@ifixtelecom.com.br`) and a random wrong password (NEVER a real admin password). The shell loop posts to `https://ai-dashboard.converse-ai.app/api/auth/sign-in/email` 6 times with `Content-Type: application/json` body `{"email":"uat-rate-limit@ifixtelecom.com.br","password":"<random-wrong-value>"}` and captures HTTP status only. Use `curl -s -o /dev/null -w '%{http_code}\n'` to suppress response bodies. **REDACTED EVIDENCE RULE**: NEVER paste the password into this sheet or commit history.
    2. Verify: 6th request returns 429.
    3. Verify `X-Retry-After` header is present via `curl -I`.
    Evidence (REDACTED form only): paste exact HTTP status sequence (e.g. "401 401 401 401 401 429") — NEVER paste the password or any request body.
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S4 — signUp rejects non-@ifixtelecom (D-13, PRD-06)
    Steps:
    1. POST `https://ai-dashboard.converse-ai.app/api/auth/sign-up/email` with header `Content-Type: application/json` and body `{"email":"foo@gmail.com","password":"<random-disposable>","name":"foo"}`.
    2. Verify HTTP 400 or 422 with body containing "fora do allowlist" or "email_domain_not_allowed".
    Evidence: paste HTTP status + sanitized body excerpt (error message only — never the disposable password).
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S5 — gatewayctl debug emit-error → Sentry event lands (D-18.2)
    Steps:
    1. Operator sets `AI_GATEWAY_ADMIN_KEY` env on n8n-ia-vm session (NEVER inline literal). Then: `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl debug emit-error --gateway=http://localhost:8080 --admin-key=$AI_GATEWAY_ADMIN_KEY'` returns exit 0 + status=500.
    2. Open Sentry UI for ifix-ai-gateway-prod project 4511455942017024; search recent events for "synthetic panic emitted by gatewayctl debug emit-error".
    3. Confirm event landed within ~5s of step 1.
    Evidence: paste Sentry event URL (non-secret).
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S6 — gatewayctl key list returns aligned table (D-18.3)
    Steps:
    1. `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl key list'` — expect tabwriter-aligned columns: ID, TENANT, PREFIX, STATUS, DATA_CLASS, CREATED, LAST_USED.
    2. Verify NO raw API key string appears in output (PREFIX column is the first-4-char marker only).
    3. Verify --tenant filter: `docker exec ifix-ai-gateway /gatewayctl key list --tenant=converseai` returns subset.
    Evidence: paste output but TRUNCATE PREFIX column to first 4 chars (e.g. `ifix...`) before pasting — defense-in-depth even though key_prefix itself is already non-secret.
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S8 — Per-env keys diff verification (D-19) — uses sanitized first-4-char prefix recipe
    Steps:
    1. Operator already executed the D-19 rotation procedure per gateway/docs/RUNBOOK-DEPLOY.md (Phase 11 11-05).
    2. Run the SANITIZED diff (first-4-char prefixes only — per 11-05 recipe; NEVER expose raw values):
       Run `diff` on two `awk`-sanitized streams from `ssh n8n-ia-vm 'sudo grep -E "^UPSTREAM_.*_AUTH_BEARER" /opt/ai-gateway-prod/.env'` and `ssh vps-ifix-vm 'sudo grep -E "^UPSTREAM_.*_AUTH_BEARER" /opt/ai-gateway-dev/.env'`, with awk pipeline `awk -F= '{key=$1; val=$2; printf "%s=%s...\n", key, substr(val,1,4)}'` on each stream BEFORE diff. The diff output thus contains ONLY var names + first-4-char prefixes (never raw key values).
    3. Verify non-empty diff (4 lines differ at the prefix level → confirms dev↔prod separation).
    4. Verify seed-admins.sh ran successfully (dashboard_auth.user has 4 @ifixtelecom rows). Use sanitized SELECT: `psql $PROD_DSN -c "SELECT email FROM dashboard_auth.user WHERE email LIKE '%@ifixtelecom.com.br'" | wc -l` → ≥4. NEVER inline `$PROD_DSN` literal — operator sources from env.
    Evidence: paste sanitized diff output (only var names + first-4 prefixes; no raw key values) + count.
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    Cumulative spend rollup section:
    - 11-06 (load-test): $X.XX
    - 11-07 (chaos primary kill): $Y.YY
    - 11-08 (chaos OpenRouter DROP): $0.00 (no Vast spend unless PASS B ran)
    - TOTAL: $Z.ZZ within $5 cap

    ### Evidence Appendix — REDACTED EVIDENCE RULE summary [reviews LOW L4]

    Before commit, operator MUST verify:
    - No QR code images attached
    - No TOTP 6-digit numbers in text
    - No 10-set alphanumeric backup code strings in text
    - No `ifix_sk_...` literals
    - No raw passwords (disposable or real)
    - REDACTED EVIDENCE rule statement appears in this sheet ≥3 times (S1, S3, this appendix)

    Sign-off line: Operator confirms S0 prerequisite gate PASSED + 7 scenarios complete; phase ready for VERIFICATION.md rollup.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/11-HUMAN-UAT.md && [ "$(grep -cE '^## S[0-8] ' .planning/phases/11-prod-hardening/11-HUMAN-UAT.md)" -eq 8 ] && [ "$(grep -nE '^## S[0-8] ' .planning/phases/11-prod-hardening/11-HUMAN-UAT.md | head -1 | grep -c '## S0 ')" -eq 1 ] && grep -q 'PREREQUISITE GATE' .planning/phases/11-prod-hardening/11-HUMAN-UAT.md && grep -qiE 'ABORT.*do NOT proceed' .planning/phases/11-prod-hardening/11-HUMAN-UAT.md && ! grep -E 'ifix_sk_[a-z0-9]{30,}' .planning/phases/11-prod-hardening/11-HUMAN-UAT.md && [ "$(grep -cE '\$\{IFIX_KEY_[A-Z_]+\}' .planning/phases/11-prod-hardening/11-HUMAN-UAT.md)" -ge 4 ] && [ "$(grep -ic REDACTED .planning/phases/11-prod-hardening/11-HUMAN-UAT.md)" -ge 3 ]</automated>
  </verify>
  <done>Sheet exists with 8 scenarios (S0 PREREQUISITE GATE + S1-S6 + S8); S0 ordered first with explicit ABORT-on-fail callout; each scenario has Steps + Evidence + Verdict line; pre-UAT environment checklist instructs operator on `source /etc/ifix/keys.env` BEFORE / `unset IFIX_KEY_*` AFTER; spend rollup section present; ZERO `ifix_sk_...` literals; ≥4 distinct `${IFIX_KEY_*}` env-var labels; REDACTED rule appears ≥3 times.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-10-02</id>
  <title>Task 2: Execute S0 PREREQUISITE gate (ABORT-on-fail) + S1..S6 + S8 scenarios + fill 11-HUMAN-UAT.md [reviews MEDIUM M3 + LOW L4]</title>
  <read_first>
    - .planning/phases/11-prod-hardening/11-HUMAN-UAT.md (this plan task 1 output)
    - 11-VALIDATION.md Manual-Only Verifications table
  </read_first>
  <what-built>
    Operator-driven execution of S0 PREREQUISITE gate + S1-S6 + S8 against live prod systems. S0 runs FIRST as a strict prerequisite; if S0 fails, ABORT — DO NOT proceed to S1-S6, S8; phase status = `blocked`, not `passed_partial`. Each subsequent scenario must be exercised, evidence captured per REDACTED EVIDENCE RULE (no TOTP digits, no QR codes, no backup codes, no raw API keys, no passwords), verdict signed.
  </what-built>
  <how-to-verify>
    Step 1 (PREREQUISITE): Execute S0 (GHA retrigger + image pull). **If S0 FAILS, ABORT** — set Status: blocked in the sheet header; commit ONLY the S0-failed sheet; do NOT execute S1-S6, S8; restore release artifacts via gateway/docs/RUNBOOK-DEPLOY.md §GHA retrigger procedure; resume from S0 on next attempt.

    Step 2 (only if S0 PASSED): source `/etc/ifix/keys.env` (mode 0600) into shell session. Set `HISTFILE=/dev/null` to prevent shell history leakage. Then run S1, S2, S3, S4, S5, S6, S8 in order per 11-HUMAN-UAT.md. Fill each scenario's Evidence section + Verdict line with PASS/FAIL + initials + timestamp.

    Step 3: After all scenarios, update header Status field:
    - S0 PASS + S1..S6 + S8 all PASS → Status: passed
    - S0 PASS + 1-2 FAIL in S1-S6/S8 with documented justification (e.g., D-19 Vast labels not supported per Open Question #4 fallback) → Status: passed_partial
    - S0 PASS + ≥3 FAIL in S1-S6/S8 → Status: failed; trigger /gsd:plan-phase 11 --gaps
    - S0 FAIL → Status: blocked (NEVER passed_partial when S0 fails)

    Step 4: `unset IFIX_KEY_CONVERSEAI IFIX_KEY_TELEFONIA IFIX_KEY_CHAT_IFIX IFIX_KEY_COBRANCAS IFIX_KEY_CAMPANHAS IFIX_KEY_VOICE_API IFIX_KEY_UAT10_TEST AI_GATEWAY_ADMIN_KEY PROD_DSN` to clear secrets from session.

    Step 5 (pre-commit grep gates — operator MUST run before commit; commit BLOCKED if any fail):
    - `! grep -E 'ifix_sk_[a-z0-9]{30,}' .planning/phases/11-prod-hardening/11-HUMAN-UAT.md` — must pass (zero raw keys)
    - `! grep -E 'Bearer [A-Za-z0-9_]{30,}' .planning/phases/11-prod-hardening/11-HUMAN-UAT.md` — must pass (zero raw bearer tokens)
    - `! grep -E 'postgres://[^[:space:]]{10,}' .planning/phases/11-prod-hardening/11-HUMAN-UAT.md` — must pass (zero raw DSNs)
    - `[ "$(grep -ic REDACTED .planning/phases/11-prod-hardening/11-HUMAN-UAT.md)" -ge 3 ]` — must pass (REDACTED rule preserved)
    - Manual visual scan for 6-digit numbers in 2FA scenario sections (TOTP digits) and 10-set alphanumeric strings (backup codes) — remove if present.

    Step 6: Commit 11-HUMAN-UAT.md fully filled.
  </how-to-verify>
  <resume-signal>Type "UAT complete — Status: passed/passed_partial/blocked; sheet committed; REDACTED-EVIDENCE grep gate PASS; raw-key grep PASS" or describe blocker. If S0 failed, type "BLOCKED on S0 — release artifacts pending restore".</resume-signal>
</task>

<task type="auto">
  <id>11-10-03</id>
  <title>Task 3: Author 11-VERIFICATION.md final phase rollup [reviews MEDIUM M1 — zero raw keys]</title>
  <files>.planning/phases/11-prod-hardening/11-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/10-prod-deploy-ai-gateway/10-VERIFICATION.md (format precedent — status, pitfalls_hit, deviations, evidence cross-ref)
    - .planning/phases/11-prod-hardening/11-HUMAN-UAT.md (operator-filled sheet from task 11-10-02)
    - .planning/phases/11-prod-hardening/11-06-EVIDENCE.md, 11-07-EVIDENCE.md, 11-08-EVIDENCE.md, 11-02-staging-smoke.md
    - 11-CONTEXT.md (all 19 D-XX decisions for coverage rollup)
    - .planning/REQUIREMENTS.md §Traceability (PRD-01..06 row updates)
  </read_first>
  <acceptance_criteria>
    - File exists: `.planning/phases/11-prod-hardening/11-VERIFICATION.md`
    - Status line matches: `grep -E '^status: (passed|passed_partial|failed|blocked)$'` returns 1 line
    - All 6 PRDs accounted for: `grep -c 'PRD-0[1-6]'` returns ≥6
    - D-01..D-19 coverage rollup table present (≥19 D-XX references)
    - Zero raw key literals: `grep -E 'ifix_sk_[a-z0-9]{30,}|postgres://[^[:space:]]{10,}'` returns ZERO matches
    - Cross-cutting attestation block present (states "no raw keys, headers, bodies, DSNs, PII; no TOTP digits or backup codes")
    - S0 PREREQUISITE outcome explicitly recorded in D-18.4 row
  </acceptance_criteria>
  <action>
    Author .planning/phases/11-prod-hardening/11-VERIFICATION.md per 10-VERIFICATION.md format precedent.

    Frontmatter (YAML): `phase: 11`, `slug: prod-hardening`, `status: passed | passed_partial | failed | blocked`, `closed_at: YYYY-MM-DDTHH:MMZ`, `spend_total_usd: X.XX`, `operator: {name}`.

    Sections (Markdown headers):

    ## Summary
    One-paragraph rollup. Phase 11 endurece prod pós-Phase-10: PRD-01 load test 30min sustained com SLO v1.0 D-04 PASS (5 latency/error gates measured + zero_5xx_panic verified externally via Sentry), PRD-02 chaos primary kill com invisible failover (observe-first 90s cleanup), PRD-03 chaos OpenRouter DROP container-netns-scoped com sensitive 503 + normal-tenant outcome documented honestly, PRD-04 RUNBOOK-INCIDENTS.md + POSTMORTEM-TEMPLATE.md + RUNBOOK-2FA-RECOVERY.md shipped, PRD-05 LGPD signoff doc-only deliverables shipped, PRD-06 dashboard 2FA + rate-limit + allowlist + session hardening live (staging smoke verified BEFORE prod migrate). D-18 Phase 10 deferred items folded (smoke race fix, gatewayctl debug + key list with new WithMeta queries + automated unauth-gate test, GHA retrigger as PREREQUISITE gate). D-19 per-env keys separated (sanitized first-4-char diff verified).

    ## Per-PRD Verdict (table)
    Columns: PRD | Status | Evidence
    Rows: PRD-01 (passed | load-report-YYYY-MM-DD.json + 11-06-EVIDENCE.md), PRD-02 (passed | 11-07-EVIDENCE.md), PRD-03 (passed | 11-08-EVIDENCE.md), PRD-04 full (passed | gateway/docs/RUNBOOK-INCIDENTS.md + POSTMORTEM-TEMPLATE.md + RUNBOOK-2FA-RECOVERY.md), PRD-05 (passed | gateway/docs/LGPD-SIGNOFF-PROCESS.md + LGPD-SIGNOFF-LETTER-TEMPLATE.md), PRD-06 (passed | dashboard/src/lib/auth.ts + 2fa pages + middleware + 11-02-staging-smoke.md + 11-HUMAN-UAT.md S1-S4).

    ## D-XX Coverage Rollup (table)
    Columns: Decision | Plan(s) covering | Status
    Rows covering D-01..D-19. Specifically:
    - D-18.4 row MUST reference "11-05 + 11-10 S0 PREREQUISITE gate" and record S0 outcome (passed/failed/blocked).
    - D-12..D-15 row MUST reference "11-02 + 11-10 S1-S3 + S4".
    - D-18.2 row MUST reference "11-04 + 11-10 S5".
    - D-18.3 row MUST reference "11-04 + 11-10 S6".
    - D-19 row MUST reference "11-05 + 11-10 S8".

    ## Spend Rollup
    Bullets for 11-06, 11-07, 11-08 spend + total ≤ $5 absolute cap.

    ## Pitfalls Hit
    Bullets — fill from Wave 2 EVIDENCE files.

    ## Deviations
    Bullets — fill if any D-XX coverage went passed_partial (explain per CONTEXT.md fallback clauses where applicable).

    ## Cascade Close
    Statement: Phase 11 does NOT close additional Phase 02/03/04/05 deferrals (Phase 10 already closed those via 5bd79d1 + audit/billing fold). Phase 11 closes: Phase 10 deferred items (D-18.1..D-18.4) folded; Phase 11 itself (PRD-01..PRD-06).

    ## References
    Bullets cross-ref to: 11-HUMAN-UAT.md, 11-02-staging-smoke.md, 11-06/07/08-EVIDENCE.md, gateway/docs/RUNBOOK-INCIDENTS.md, gateway/docs/POSTMORTEM-TEMPLATE.md, gateway/docs/RUNBOOK-2FA-RECOVERY.md, gateway/docs/LGPD-SIGNOFF-*.md, gateway/docs/RUNBOOK-DEPLOY.md.

    ## Cross-cutting Attestation [reviews MEDIUM M1 + LOW L4]
    Statement: "No raw API keys, headers, request bodies, response bodies, DSNs, passwords, or PII in this VERIFICATION file. Tenant references via env-var labels `${IFIX_KEY_*}` OR first-4-char prefix only. No TOTP digits, QR codes, or backup codes captured anywhere in Phase 11 evidence."

    Sign-off: operator + closed_at timestamp.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/11-VERIFICATION.md && grep -E '^status: (passed|passed_partial|failed|blocked)$' .planning/phases/11-prod-hardening/11-VERIFICATION.md && [ "$(grep -c 'PRD-0[1-6]' .planning/phases/11-prod-hardening/11-VERIFICATION.md)" -ge 6 ] && ! grep -E 'ifix_sk_[a-z0-9]{30,}|postgres://[^[:space:]]{10,}' .planning/phases/11-prod-hardening/11-VERIFICATION.md && grep -q 'Cross-cutting Attestation' .planning/phases/11-prod-hardening/11-VERIFICATION.md && grep -q 'D-18.4' .planning/phases/11-prod-hardening/11-VERIFICATION.md</automated>
  </verify>
  <done>11-VERIFICATION.md exists; status field set (passed/passed_partial/failed/blocked); all 6 PRDs accounted for; D-XX rollup table covers D-01..D-19; D-18.4 row records S0 outcome; spend section populated; ZERO raw key/DSN strings; Cross-cutting Attestation block present.</done>
</task>

<task type="auto">
  <id>11-10-04</id>
  <title>Task 4: Commit verification + advance phase state via canonical `gsd-sdk query phase complete 11` [reviews MEDIUM M2 — manual STATE.md edits PROHIBITED]</title>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/11-prod-hardening/11-VERIFICATION.md (operator-signed status from task 11-10-03)
    - .planning/STATE.md (current state — Phase 11 marker; MUST NOT be edited by hand)
    - .planning/ROADMAP.md (Phase 11 entry — plan checkboxes flipped by hand only)
  </read_first>
  <acceptance_criteria>
    - Commit lands with message "docs(11): close phase prod-hardening — status: {passed|passed_partial}"
    - STATE.md mutated EXCLUSIVELY via `gsd-sdk query phase complete 11` (NOT via Edit/Write tool) — verified by absence of STATE.md hand-edits in the diff narrative
    - ROADMAP.md plan-checkbox diff is hand-edit `[ ]` → `[x]` only (no other content changes); verified via `git diff .planning/ROADMAP.md` reviewed BEFORE commit
    - `gsd-sdk query state.load` shows Phase 11 marked complete after Task 4 ends
    - If status=blocked or status=failed: Task 4 SKIPS `phase complete` invocation (do NOT advance state on failure); instead, trigger `/gsd:plan-phase 11 --gaps` workflow
  </acceptance_criteria>
  <action>
    Step 1 — Commit evidence files first: `git add .planning/phases/11-prod-hardening/11-HUMAN-UAT.md .planning/phases/11-prod-hardening/11-VERIFICATION.md && git commit -m "docs(11): close phase prod-hardening — status: {passed|passed_partial}"`.

    Step 2 — State advance via canonical SDK command [reviews MEDIUM M2]: Read the `status:` line from 11-VERIFICATION.md frontmatter. Branch on value:
    - If `status: passed` OR `status: passed_partial`: run `gsd-sdk query phase complete 11`. This is the CANONICAL command (verb confirmed in current gsd-tools — `gsd-sdk query phase complete <N>` mutates STATE.md atomically and returns JSON `{completed_phase, phase_name, plans_executed, next_phase, ...}`). Manual STATE.md edits are PROHIBITED — they can corrupt GSD state (e.g., progress counters drift, last_updated stale, Current Position misaligned). Do NOT use Edit/Write tool on STATE.md.
    - If `status: blocked` OR `status: failed`: SKIP `phase complete` invocation. Do NOT advance state. Instead, surface the blocker to the operator and instruct: "Phase 11 blocked/failed — trigger `/gsd:plan-phase 11 --gaps` to plan gap-closure work."

    Step 3 — ROADMAP.md plan-checkbox hand edit (the ONLY hand edit permitted): for each plan 11-01..11-10 that completed, flip the checkbox `- [ ]` → `- [x]` in the Phase 11 entry of `.planning/ROADMAP.md`. Use the Edit tool. Run `git diff .planning/ROADMAP.md` and visually verify the diff contains ONLY checkbox flips (no other content changes). If the diff shows unexpected changes, abort and re-edit.

    Step 4 — Commit ROADMAP.md: `git add .planning/ROADMAP.md && git commit -m "state(11): mark Phase 11 plan checkboxes complete"`. Note: STATE.md was already committed by `gsd-sdk query phase complete 11` in Step 2 (the SDK command commits the STATE.md mutation itself per its standard behavior); if for any reason STATE.md is in working tree after Step 2, do NOT amend — investigate, because the SDK should have handled the commit.

    Step 5 — Confirm: run `gsd-sdk query state.load` (or equivalent) and verify the output shows Phase 11 marked complete and Current Position advanced (next_phase is null since Phase 11 is the last phase per gsd-tools output `is_last_phase: true`).

    Step 6 — If status=failed/blocked in Step 2: do NOT advance — instead trigger `/gsd:plan-phase 11 --gaps` (handled outside this plan; this task ends here with operator-facing instruction).
  </action>
  <verify>
    <automated>grep -E "Phase 11.*(complete|completed)|\\[x\\].*11-" .planning/ROADMAP.md && gsd-sdk query state.load 2>/dev/null | grep -qE 'Phase.*11|prod-hardening|completed_phases.*[0-9]'</automated>
  </verify>
  <done>11-VERIFICATION.md + 11-HUMAN-UAT.md committed; STATE.md mutated via canonical `gsd-sdk query phase complete 11` (NOT hand-edited); ROADMAP.md plan checkboxes flipped by hand with diff reviewed; `gsd-sdk query state.load` confirms Phase 11 complete; if status=blocked/failed, state advance SKIPPED and operator instructed to run `/gsd:plan-phase 11 --gaps`.</done>
</task>

</tasks>

<verification>
- 11-HUMAN-UAT.md committed with operator-signed S0 (PREREQUISITE gate, ABORT-on-fail) + S1..S6 + S8.
- 11-VERIFICATION.md status: passed/passed_partial/failed/blocked populated.
- All 6 PRDs have a verdict row.
- D-01..D-19 covered in rollup table; D-18.4 row records S0 outcome.
- ROADMAP.md Phase 11 plan checkboxes flipped via hand-edit with diff reviewed.
- STATE.md advanced via canonical `gsd-sdk query phase complete 11` (NOT hand-edited).
- Spend ≤ $5 absolute Phase 11 cap.
- ZERO raw `ifix_sk_...` literals in 11-HUMAN-UAT.md or 11-VERIFICATION.md (grep gate).
- ≥4 distinct `${IFIX_KEY_*}` env-var labels in 11-HUMAN-UAT.md.
- REDACTED rule appears ≥3 times in 11-HUMAN-UAT.md (S1, S3, evidence appendix).
- No TOTP digits, QR codes, or backup codes in any evidence.
</verification>

<success_criteria>
- Phase 11 closed end-to-end via canonical SDK command (or `blocked`/`failed` with gap-closure trigger).
- All locked decisions D-01..D-19 accounted for with evidence; D-18.4 S0 PREREQUISITE outcome explicit.
- Live UAT for PRD-06 (S1-S4), D-18 fold (S5-S6), D-18.4 release (S0 PREREQUISITE gate), D-19 (S8) signed with REDACTED evidence only.
- v1 milestone progresses (Phase 11 is last phase per gsd-tools `is_last_phase: true`).
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies — all deliverables doc-only or already-deployed code. (Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.)
- Truth 5: autonomous=false — operator drives UAT.
- Truth 6: gateway/dashboard images either at :latest-dev or freshly :v1.0.0 post-S0 retrigger.
- Truth 8 [reviews MEDIUM M1 + LOW L4]: NO raw API keys, TOTP digits, QR codes, backup codes, passwords, headers, request bodies, response bodies, DSNs, or PII in scenario docs or evidence; ALL tenant keys via env-var labels `${IFIX_KEY_<TENANT_SLUG^^>}`; REDACTED EVIDENCE rule enforced for 2FA scenarios.
- Truth 9 [reviews MEDIUM M2]: STATE.md mutation EXCLUSIVELY via `gsd-sdk query phase complete 11`; hand-edits PROHIBITED. ROADMAP.md hand-edits limited to plan-checkbox flips with diff review.
- Truth 10 [reviews MEDIUM M3]: S0 PREREQUISITE gate runs FIRST sequentially; if S0 FAILS, ABORT — phase = `blocked`, NOT `passed_partial`.
- depends_on covers all Wave 1 + Wave 2 + Wave 3-prior plans.
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-10-SUMMARY.md when done with: paths committed (11-HUMAN-UAT.md + 11-VERIFICATION.md + ROADMAP.md; STATE.md committed by SDK), final phase status, total spend, links to all per-PRD evidence files, confirmation that `gsd-sdk query phase complete 11` was used (NOT manual STATE.md edit), S0 PREREQUISITE outcome.
</output>
