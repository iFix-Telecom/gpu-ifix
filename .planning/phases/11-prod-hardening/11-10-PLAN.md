---
phase: 11
plan: 11-10
slug: human-uat-and-verification
wave: 3
depends_on: [11-02, 11-04, 11-05, 11-06, 11-07, 11-08, 11-09]
requirements: [PRD-04, PRD-06]
files_modified:
  - path: .planning/phases/11-prod-hardening/11-HUMAN-UAT.md
    kind: NEW
  - path: .planning/phases/11-prod-hardening/11-VERIFICATION.md
    kind: NEW
autonomous: false
estimated_duration: 150
scope_note: "5 tasks borderline — matches Phase 10-06 HUMAN-UAT precedent (8 scenarios S1-S8 + author scenario sheet + final rollup); splitting causes evidence-chain disconnect between scenario sheet → UAT execution → VERIFICATION rollup → state advance. Reviews MEDIUM: S7 (GHA retrigger) is RE-ORDERED to run as a Wave 3 PREREQUISITE gate BEFORE S1-S6 + S8 scenario execution (because S7 alters release artifacts; running it concurrently with auth/security UAT muddles closure)."
---

<objective>
Final phase sign-off. Operator-driven manual UAT covering: (a) dashboard 2FA enroll → logout → login with TOTP end-to-end (PRD-06); (b) rate-limit /sign-in/email returns 429 after 5 attempts (D-14); (c) signUp rejects non-@ifixtelecom.com.br (D-13); (d) gatewayctl debug emit-error → Sentry event lands in ifix-ai-gateway-prod (D-18.2); (e) gatewayctl key list returns aligned table (D-18.3); (f) per-env keys diff verification using SANITIZED first-4-char prefix recipe (D-19); (g) dashboard admin seeded via seed-admins.sh (D-13); (h) GHA workflow_dispatch produces :v1.0.0 image — RE-ORDERED to a Wave 3 PREREQUISITE gate (reviews MEDIUM). All tenant API keys referenced via env-var labels — zero `ifix_sk_...` literals in scenario docs (reviews MEDIUM). Roll all evidence into 11-VERIFICATION.md using canonical `gsd-sdk` commands where they exist (reviews MEDIUM — avoid manual STATE.md/ROADMAP.md edits that risk state corruption). Status: passed (or passed_partial with documented exceptions). Cascade-close not required (Phase 10 already closed 02/03/04/05 deferrals); this plan closes Phase 11 itself.
</objective>

<must_haves>
  truths:
    - "All 19 D-XX decisions covered via the cumulative Phase 11 plan set (11-01..11-09); 11-10 verifies the live operator-side residual"
    - "11-VERIFICATION.md status: passed OR passed_partial per Phase 06.5/Phase 10 precedent format"
    - "PRD-04 (full): RUNBOOK-INCIDENTS.md + POSTMORTEM-TEMPLATE.md + RUNBOOK-2FA-RECOVERY.md verified live (grep gate + cross-ref test)"
    - "PRD-06: dashboard 2FA UAT signed by operator (TOTP enroll + challenge + backup-codes display + rate-limit 429 + signUp rejection)"
    - "D-18.2: Sentry event observed within 5s of gatewayctl debug emit-error invocation (id 4511455942017024 project)"
    - "D-18.4 RE-ORDERED (reviews MEDIUM): GHA workflow_dispatch + :v1.0.0 image pull is a WAVE 3 PREREQUISITE gate (S0) that runs BEFORE S1-S6 + S8 — if S7→S0 fails, Wave 3 UAT pauses until release artifacts are restored"
    - "Spend cap $5 absolute Phase 11 — total spend across 11-06/11-07/11-08 stays within"
    - "Cross-cutting hard rule: NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in scenario docs, evidence, or commits. Tenant references via env-var labels (e.g. ${TENANT_TELEFONIA_KEY_ENV}) only; evidence redacts to first-4-char prefix. NO 2FA screenshots showing TOTP digits or backup codes; ONLY confirmation messages and metadata."
    - "Canonical state-advance commands (reviews MEDIUM): if `gsd-sdk query state.complete-phase` and `gsd-sdk query roadmap.update-status` exist, USE them. Manual STATE.md/ROADMAP.md edits are a fallback ONLY when canonical commands are not available; in that case the diff is reviewed via `git diff` before commit."
  must_haves:
    - .planning/phases/11-prod-hardening/11-HUMAN-UAT.md committed with operator-signed scenario sheet (S0 prerequisite + S1..S6 + S8)
    - .planning/phases/11-prod-hardening/11-VERIFICATION.md committed with status: passed/passed_partial + per-PRD verdict
    - Phase 11 closes (state advance via canonical gsd-sdk command preferred; manual edit only if SDK lacks the command)
    - Zero `ifix_sk_...` literals in 11-HUMAN-UAT.md or 11-VERIFICATION.md (pre-commit grep gate)
    - No 2FA TOTP digits or backup codes captured in evidence (textual confirmation only)
  key_links:
    - from: 11-HUMAN-UAT.md S0/S1..S8 sign-offs
      to: 11-VERIFICATION.md rollup
      via: cross-ref + verdict table
      pattern: "S[0-8]"
    - from: 11-VERIFICATION.md
      to: all Wave 2 evidence files (11-06/07/08-EVIDENCE.md) + 11-02-staging-smoke.md
      via: cross-ref bullets
      pattern: "EVIDENCE\\.md|staging-smoke"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live prod dashboard + gateway | manual auth flow exercises real endpoints |
| operator -> Sentry web UI | post-test verification of panic-path event landing |
| operator -> GHA + GHCR | workflow_dispatch + image pull |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-UAT-01 | Spoofing | UAT confirms wrong dashboard instance | mitigate | Operator MUST hit ai-dashboard.converse-ai.app explicitly (NOT dev); verify Better Auth URL in browser; confirm dashboard image tag :latest-dev or :v1.0.0 post-retrigger |
| T-11-UAT-02 | Information Disclosure | TOTP secret screenshot shared accidentally | mitigate | UAT instructions explicitly say "do not screenshot the QR code, secret string, 6-digit TOTP digits, or backup codes — type into authenticator directly"; secret rendered once on enroll step 1; evidence captures TEXTUAL confirmation only |
| T-11-UAT-03 | Repudiation | UAT scenario marked PASS without evidence | mitigate | Each scenario S0..S8 requires a non-sensitive evidence form (curl HTTP status, Sentry event URL, GHA run ID, manifest digest, sanitized diff output, written confirmation) saved into 11-HUMAN-UAT.md |
| T-11-UAT-04 | Information Disclosure | Raw API key literals in scenario docs | mitigate | Reviews MEDIUM — all curl examples use env-var labels (${TENANT_*_KEY_ENV}); operator sets env per session; never inline literal `ifix_sk_...` |
| T-11-UAT-05 | Tampering | Manual STATE.md/ROADMAP.md edits corrupt GSD state | mitigate | Reviews MEDIUM — task 11-10-04 prefers canonical `gsd-sdk query` commands; manual edits only as a fallback with `git diff` review BEFORE commit |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-PATTERNS.md
@.planning/phases/11-prod-hardening/11-VALIDATION.md
@.planning/phases/11-prod-hardening/11-UI-SPEC.md
@.planning/phases/11-prod-hardening/11-02-PLAN.md
@.planning/phases/11-prod-hardening/11-04-PLAN.md
@.planning/phases/11-prod-hardening/11-05-PLAN.md
@.planning/phases/11-prod-hardening/11-06-PLAN.md
@.planning/phases/11-prod-hardening/11-07-PLAN.md
@.planning/phases/11-prod-hardening/11-08-PLAN.md
@.planning/phases/11-prod-hardening/11-09-PLAN.md
@.planning/phases/10-prod-deploy-ai-gateway/10-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <id>11-10-01</id>
  <title>Task 1: Author 11-HUMAN-UAT.md scenario sheet (S0 prerequisite + S1..S6 + S8)</title>
  <files>.planning/phases/11-prod-hardening/11-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/10-prod-deploy-ai-gateway/10-VERIFICATION.md (precedent format for scenario sign-off — pitfalls_hit + deviations sections)
    - 11-VALIDATION.md Manual-Only Verifications table (test instructions per UAT scenario)
    - 11-CONTEXT.md D-12..D-19 (sources for each scenario)
    - 11-UI-SPEC.md (dashboard 2FA UI screens UAT must traverse)
  </read_first>
  <action>
    Create .planning/phases/11-prod-hardening/11-HUMAN-UAT.md with sections:

    Header:
    # Phase 11 — HUMAN-UAT Scenario Sheet
    **Phase:** 11 prod-hardening
    **Operator:** {fill}
    **Date:** {fill YYYY-MM-DD}
    **Status:** in_progress | passed | passed_partial | failed

    Pre-UAT environment check (operator signs first):
    - [ ] Wave 1 plans (11-01..11-05) committed and on develop
    - [ ] Wave 2 plans (11-06/07/08) committed with EVIDENCE files
    - [ ] Wave 3 plan 11-09 committed (RUNBOOK-INCIDENTS + POSTMORTEM-TEMPLATE + RUNBOOK-2FA-RECOVERY)
    - [ ] 11-02-staging-smoke.md committed showing staging smoke green BEFORE prod migrate
    - [ ] Operator has Sentry web access (ifix-ai-gateway-prod project 4511455942017024)
    - [ ] Operator has Vast.ai dashboard access
    - [ ] Operator has GitHub repo write permission (gh CLI authenticated)
    - [ ] Operator has Authenticator app (1Password / Google Authenticator) ready
    - [ ] $5 Phase 11 spend cap acknowledged
    - [ ] Tenant API keys exported to env (IFIX_KEY_CONVERSEAI, IFIX_KEY_TELEFONIA, IFIX_KEY_CHAT_IFIX, etc.) for raw-curl scenarios — NEVER inline literal keys in this sheet or in shell commands

    ## S0 — GHA workflow_dispatch produces :v1.0.0 image (D-18.4) — PREREQUISITE gate (RE-ORDERED per reviews MEDIUM)

    Why first: this scenario alters release artifacts (ghcr.io image tags). Running it concurrently with auth/security UAT muddles which run produced which evidence. If S0 fails, pause Wave 3 UAT, restore release artifacts, then resume.

    Steps:
    1. `gh workflow run build-gateway.yml --ref v1.0.0 -f tag=v1.0.0` → capture run id.
    2. `gh run watch <run-id>` → completes successfully.
    3. `docker pull ghcr.io/ifixtelecom/ifix-ai-gateway:v1.0.0` → succeeds; capture manifest digest.
    4. Same for build-dashboard.yml.

    Evidence: paste gh run id + docker pull manifest digest (digest is non-secret).
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    If S0 FAIL: pause Wave 3, fall back to gateway/docs/RUNBOOK-DEPLOY.md §GHA retrigger procedure (D-18.4); resume Wave 3 only when both images are available.

    ## S1 — Dashboard 2FA enroll end-to-end (D-12, PRD-06)
    Steps:
    1. Operator hits https://ai-dashboard.converse-ai.app/signup with email @ifixtelecom.com.br + password.
    2. Verify allowlist accepts; redirected to /2fa/enroll.
    3. Scan QR with authenticator; type 6-digit code on /2fa/enroll step 2 — DO NOT screenshot the QR or the 6-digit code.
    4. Backup codes shown in Dialog; copy via "Copiar tudo" button (sonner toast "10 códigos copiados") — DO NOT screenshot the backup codes; the operator manually transfers to a password manager.
    5. Click "Salvar e continuar" → redirected to /.
    Evidence: textual confirmation only — e.g. "QR rendered, 6-digit code accepted, 10 backup codes displayed in Dialog, toast observed, redirected to home". NO TOTP digits or backup codes captured.
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S2 — Dashboard 2FA challenge on subsequent login (D-12, PRD-06)
    Steps:
    1. Logout from dashboard.
    2. Hit /login → e-mail + password → 200 → middleware redirects to /2fa/challenge.
    3. Type 6-digit TOTP code → router.push("/") → reach dashboard home.
    Evidence: textual confirmation; no TOTP digit capture.
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S3 — Rate-limit /sign-in/email returns 429 after 5 attempts (D-14, PRD-06)
    Steps:
    1. Run shell (use a DISPOSABLE test email + random wrong password — never a real admin password):
       ```
       for i in $(seq 1 6); do
         curl -s -o /dev/null -w "%{http_code}\n" \
           -X POST https://ai-dashboard.converse-ai.app/api/auth/sign-in/email \
           -H "Content-Type: application/json" \
           -d '{"email":"uat-rate-limit@ifixtelecom.com.br","password":"<random-wrong-value>"}'
         sleep 1
       done
       ```
    2. Verify: 6th request returns 429.
    3. Verify X-Retry-After header is present (curl -I).
    Evidence: paste exact HTTP status sequence (e.g. "401 401 401 401 401 429") — NEVER paste the password.
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S4 — signUp rejects non-@ifixtelecom (D-13, PRD-06)
    Steps:
    1. `curl -X POST https://ai-dashboard.converse-ai.app/api/auth/sign-up/email -H "Content-Type: application/json" -d '{"email":"foo@gmail.com","password":"<random-disposable>","name":"foo"}'`
    2. Verify HTTP 400 or 422 with body containing "fora do allowlist" or "email_domain_not_allowed".
    Evidence: paste HTTP status + sanitized body excerpt (error message only — never the password).
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S5 — gatewayctl debug emit-error → Sentry event lands (D-18.2)
    Steps:
    1. `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl debug emit-error --gateway=http://localhost:8080 --admin-key=$AI_GATEWAY_ADMIN_KEY'` returns exit 0 + status=500. (Operator sets AI_GATEWAY_ADMIN_KEY env on n8n-ia-vm — never inline.)
    2. Open Sentry UI for ifix-ai-gateway-prod project 4511455942017024; search recent events for "synthetic panic emitted by gatewayctl debug emit-error".
    3. Confirm event landed within ~5s of step 1.
    Evidence: paste Sentry event URL (non-secret).
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S6 — gatewayctl key list returns aligned table (D-18.3)
    Steps:
    1. `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl key list'` — expect tabwriter-aligned columns ID TENANT PREFIX STATUS DATA_CLASS CREATED LAST_USED.
    2. Verify NO raw API key string appears (only key_prefix — first-4-char-ish marker).
    3. Verify --tenant filter: `docker exec ifix-ai-gateway /gatewayctl key list --tenant=converseai` returns subset.
    Evidence: paste output but TRUNCATE PREFIX column to first 4 chars (e.g. `ifix...`) before pasting — defense-in-depth even though key_prefix itself is already non-secret.
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    ## S8 — Per-env keys diff verification (D-19)
    Steps:
    1. Operator already executed the D-19 rotation procedure per gateway/docs/RUNBOOK-DEPLOY.md (Phase 11 11-05).
    2. Run the SANITIZED diff (first-4-char prefixes only — per 11-05 recipe; NEVER expose raw values):
       ```
       diff \
         <(ssh n8n-ia-vm 'sudo grep -E "^UPSTREAM_.*_AUTH_BEARER" /opt/ai-gateway-prod/.env' | awk -F= '{key=$1; val=$2; printf "%s=%s...\n", key, substr(val,1,4)}') \
         <(ssh vps-ifix-vm 'sudo grep -E "^UPSTREAM_.*_AUTH_BEARER" /opt/ai-gateway-dev/.env' | awk -F= '{key=$1; val=$2; printf "%s=%s...\n", key, substr(val,1,4)}')
       ```
    3. Verify non-empty diff (4 lines differ at the prefix level → confirms separation).
    4. Verify seed-admins.sh ran successfully (dashboard_auth.user has 4 @ifixtelecom rows). Use sanitized SELECT: `psql $PROD_DSN -c "SELECT email FROM dashboard_auth.user WHERE email LIKE '%@ifixtelecom.com.br'" | wc -l` → ≥4.
    Evidence: paste sanitized diff output (only var names + first-4 prefixes; no raw key values) + count.
    Verdict: [PASS] / [FAIL] — {operator initials} {timestamp}

    Cumulative spend rollup section:
    - 11-06 (load-test): $X.XX
    - 11-07 (chaos primary kill): $Y.YY
    - 11-08 (chaos OpenRouter DROP): $0.00 (no Vast spend unless PASS B ran)
    - TOTAL: $Z.ZZ within $5 cap

    Sign-off line: Operator confirms S0 prerequisite + 7 scenarios complete; phase ready for VERIFICATION.md rollup.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/11-HUMAN-UAT.md && grep -cE "^## S[0-8] " .planning/phases/11-prod-hardening/11-HUMAN-UAT.md | awk '{if ($1 == 8) exit 0; else exit 1}' && ! grep -E "ifix_sk_[a-z0-9]{20,}" .planning/phases/11-prod-hardening/11-HUMAN-UAT.md && grep -q "PREREQUISITE gate" .planning/phases/11-prod-hardening/11-HUMAN-UAT.md</automated>
  </verify>
  <done>Sheet exists with 8 scenarios (S0 prereq + S1-S6 + S8); each scenario has Steps + Evidence + Verdict line; S0 explicitly labelled PREREQUISITE gate and ordered first; pre-UAT environment checklist present; spend rollup section present; no `ifix_sk_...` literals.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-10-02</id>
  <title>Task 2: Execute S0 prerequisite + S1..S6 + S8 scenarios + fill 11-HUMAN-UAT.md</title>
  <read_first>
    - .planning/phases/11-prod-hardening/11-HUMAN-UAT.md (this plan task 1 output)
    - 11-VALIDATION.md Manual-Only Verifications table
  </read_first>
  <what-built>
    Operator-driven execution of S0 prerequisite + S1-S6 + S8 against live prod systems. S0 runs FIRST; if S0 fails, pause until release artifacts restored. Each scenario must be exercised, evidence captured (sanitized — no TOTP digits, no backup codes, no raw API keys), verdict signed.
  </what-built>
  <how-to-verify>
    1. Execute S0 (GHA retrigger + image pull). If FAIL → pause; restore release artifacts via gateway/docs/RUNBOOK-DEPLOY.md §GHA retrigger procedure; resume from S0.
    2. After S0 PASS: run S1, S2, S3, S4, S5, S6, S8 in order per 11-HUMAN-UAT.md. Fill each scenario's Evidence section + Verdict line with PASS/FAIL + initials + timestamp.
    3. After all scenarios: update header Status field accordingly:
       - S0 PASS + S1..S6 + S8 all PASS → Status: passed
       - 1-2 FAIL with documented justification (e.g., D-19 Vast labels not supported per Open Question #4 fallback) → Status: passed_partial
       - ≥3 FAIL OR S0 unresolvable → Status: failed; trigger /gsd:plan-phase 11 --gaps

    Pre-commit grep gates (operator MUST run before commit):
    - `! grep -E "ifix_sk_[a-z0-9]{20,}|Bearer [A-Za-z0-9_]{20,}|postgres://|password.*[A-Za-z0-9]{8,}" .planning/phases/11-prod-hardening/11-HUMAN-UAT.md` — must pass.
    - Manual visual scan for TOTP digits (6-digit numbers) and backup-code-shaped strings (10 sets of alphanumerics) — remove if present.

    Commit 11-HUMAN-UAT.md fully filled.
  </how-to-verify>
  <resume-signal>Type "UAT complete — Status: passed/passed_partial; sheet committed; redacted-evidence grep gate PASS" or describe blocker.</resume-signal>
</task>

<task type="auto">
  <id>11-10-03</id>
  <title>Task 3: Author 11-VERIFICATION.md final phase rollup</title>
  <files>.planning/phases/11-prod-hardening/11-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/10-prod-deploy-ai-gateway/10-VERIFICATION.md (format precedent — status, pitfalls_hit, deviations, evidence cross-ref)
    - .planning/phases/11-prod-hardening/11-HUMAN-UAT.md (operator-filled sheet from task 11-10-02)
    - .planning/phases/11-prod-hardening/11-06-EVIDENCE.md, 11-07-EVIDENCE.md, 11-08-EVIDENCE.md, 11-02-staging-smoke.md
    - 11-CONTEXT.md (all 19 D-XX decisions for coverage rollup)
    - .planning/REQUIREMENTS.md §Traceability (PRD-01..06 row updates)
  </read_first>
  <action>
    Author .planning/phases/11-prod-hardening/11-VERIFICATION.md per 10-VERIFICATION.md format precedent.

    Header:
    ---
    phase: 11
    slug: prod-hardening
    status: passed | passed_partial | failed
    closed_at: YYYY-MM-DDTHH:MMZ
    spend_total_usd: X.XX
    operator: {name}
    ---

    Sections:

    ## Summary
    Phase 11 endurece prod pós-Phase-10: PRD-01 load test 30min sustained com SLO v1.0 D-04 PASS (5 latency/error gates measured + zero_5xx_panic verified externally via Sentry), PRD-02 chaos primary kill com invisible failover (observe-first 90s cleanup), PRD-03 chaos OpenRouter DROP container-netns-scoped com sensitive 503 + normal-tenant outcome documented honestly, PRD-04 RUNBOOK-INCIDENTS.md + POSTMORTEM-TEMPLATE.md + RUNBOOK-2FA-RECOVERY.md shipped, PRD-05 LGPD signoff doc-only deliverables shipped, PRD-06 dashboard 2FA + rate-limit + allowlist + session hardening live (staging smoke verified BEFORE prod migrate). D-18 Phase 10 deferred items folded (smoke race fix, gatewayctl debug + key list with new WithMeta queries + automated unauth-gate test, GHA retrigger). D-19 per-env keys separated (sanitized first-4-char diff verified).

    ## Per-PRD Verdict
    | PRD | Status | Evidence |
    | PRD-01 | passed | .planning/phases/11-prod-hardening/load-report-YYYY-MM-DD.json + 11-06-EVIDENCE.md |
    | PRD-02 | passed | 11-07-EVIDENCE.md (chaos primary kill timeline + 90s observe outcome + cleanup) |
    | PRD-03 | passed | 11-08-EVIDENCE.md (sensitive 503 + normal-tenant outcome + container-netns cleanup) |
    | PRD-04 (full) | passed | gateway/docs/RUNBOOK-INCIDENTS.md (4 D-11 classes + unknown-triage entry-point) + POSTMORTEM-TEMPLATE.md + RUNBOOK-2FA-RECOVERY.md |
    | PRD-05 | passed | gateway/docs/LGPD-SIGNOFF-PROCESS.md + LGPD-SIGNOFF-LETTER-TEMPLATE.md (jurídico sign-off external gate per D-17) |
    | PRD-06 | passed | dashboard/src/lib/auth.ts + 2fa pages + middleware + 11-02-staging-smoke.md + 11-HUMAN-UAT.md S1-S4 |

    ## D-XX Coverage Rollup
    | Decision | Plan(s) covering | Status |
    | D-01..D-06 | 11-01 + 11-06 | passed |
    | D-07 | 11-07 | passed |
    | D-08 + D-09 | 11-04 + 11-08 | passed |
    | D-10 + D-11 | 11-09 | passed |
    | D-12..D-15 | 11-02 + 11-10 S1-S3 | passed |
    | D-16 + D-17 | 11-03 | passed (Phase 11 part) — external sign-off out of scope |
    | D-18.1 | 11-04 | passed |
    | D-18.2 | 11-04 + 11-10 S5 | passed |
    | D-18.3 | 11-04 + 11-10 S6 | passed |
    | D-18.4 | 11-05 + 11-10 S0 (prerequisite gate) | passed |
    | D-19 | 11-05 + 11-10 S8 | passed (or passed_partial if Vast per-key labels fallback used per Open Question #4) |

    ## Spend Rollup
    - 11-06 PRD-01 load-test Vast: $X.XX
    - 11-07 PRD-02 chaos primary kill Vast (DELETE + observe-first + optional force-up cycle): $Y.YY
    - 11-08 PRD-03 chaos OpenRouter DROP: $0.00 (or PASS B incremental if ran)
    - Total Phase 11: $Z.ZZ (within $5 absolute cap per CONTEXT.md specifics line 161)

    ## Pitfalls Hit
    - {fill from Wave 2 EVIDENCE files}

    ## Deviations
    - {fill if any D-XX coverage went passed_partial — explain per CONTEXT.md fallback clauses where applicable}

    ## Cascade Close
    Phase 11 does NOT close additional Phase 02/03/04/05 deferrals (Phase 10 already closed those via 5bd79d1 + audit/billing fold). Phase 11 closes:
    - Phase 10 deferred items (D-18.1..D-18.4) folded.
    - Phase 11 itself (PRD-01..PRD-06).

    ## References
    - 11-HUMAN-UAT.md (sign-off sheet)
    - 11-02-staging-smoke.md (Better Auth staging smoke pre-prod-migrate)
    - 11-06/07/08-EVIDENCE.md (Wave 2 evidence)
    - gateway/docs/RUNBOOK-INCIDENTS.md (PRD-04 doc)
    - gateway/docs/POSTMORTEM-TEMPLATE.md
    - gateway/docs/RUNBOOK-2FA-RECOVERY.md
    - gateway/docs/LGPD-SIGNOFF-*.md (PRD-05 docs)
    - gateway/docs/RUNBOOK-DEPLOY.md (D-18.4 + D-19 sections + sanitized diff recipe)

    ## Cross-cutting attestation
    No raw API keys, headers, request bodies, response bodies, DSNs, or PII in this VERIFICATION file. Tenant references via env-var labels OR first-4-char prefix. No TOTP digits or backup codes captured.

    Sign-off: operator + closed_at timestamp.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/11-VERIFICATION.md && grep -E "^status: (passed|passed_partial|failed)$" .planning/phases/11-prod-hardening/11-VERIFICATION.md && grep -c "PRD-0[1-6]" .planning/phases/11-prod-hardening/11-VERIFICATION.md | awk '{if ($1 >= 6) exit 0; else exit 1}' && ! grep -E "ifix_sk_[a-z0-9]{20,}|postgres://" .planning/phases/11-prod-hardening/11-VERIFICATION.md</automated>
  </verify>
  <done>11-VERIFICATION.md exists; status field set (passed/passed_partial/failed); all 6 PRDs accounted for; D-XX rollup table covers D-01..D-19; spend section populated; no secret strings present; cross-cutting attestation block present.</done>
</task>

<task type="auto">
  <id>11-10-04</id>
  <title>Task 4: Commit verification + advance phase state (canonical SDK commands preferred)</title>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/11-prod-hardening/11-VERIFICATION.md (operator-signed status)
    - .planning/STATE.md (current state — Phase 11 marker)
    - .planning/ROADMAP.md (Phase 11 entry to mark complete)
  </read_first>
  <action>
    Step 1: git add + commit 11-VERIFICATION.md and 11-HUMAN-UAT.md with message "docs(11): close phase prod-hardening — status: {passed|passed_partial}".

    Step 2 — canonical state-advance (reviews MEDIUM): probe for SDK commands FIRST.
    ```
    if gsd-sdk query state.complete-phase --help 2>/dev/null | head -1 | grep -q complete-phase; then
      gsd-sdk query state.complete-phase --phase 11 --status passed
    else
      echo "FALLBACK: state.complete-phase not exposed; editing STATE.md manually"
      # Manual edit (fallback only):
      # edit .planning/STATE.md to mark Phase 11 complete and advance Current Position
      # Run `git diff .planning/STATE.md` and inspect before commit
    fi
    ```

    Step 3 — roadmap update with canonical command preferred:
    ```
    if gsd-sdk query roadmap.update-status --help 2>/dev/null | head -1 | grep -q update-status; then
      gsd-sdk query roadmap.update-status --phase 11 --status complete --completed-at "$(date -u +%Y-%m-%d)"
    else
      echo "FALLBACK: roadmap.update-status not exposed; editing ROADMAP.md manually"
      # Manual edit (fallback): update Phase 11 entry to mark plans complete (10/10) + closure date
      # Run `git diff .planning/ROADMAP.md` and inspect before commit
    fi
    ```

    Step 4: git add + commit STATE.md + ROADMAP.md with "state(11): advance — phase prod-hardening complete".

    Step 5: run `gsd-sdk query state.load` to confirm state advanced correctly.

    Step 6: if status=failed, do NOT advance — instead trigger /gsd:plan-phase 11 --gaps (handled outside this plan).
  </action>
  <verify>
    <automated>grep -E "Phase 11.*(complete|completed)" .planning/ROADMAP.md && grep -E "Phase 11|prod-hardening" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md + ROADMAP.md reflect Phase 11 closure (via canonical SDK command if available; manual edit otherwise with diff reviewed); commits landed; state.load confirms current position advanced.</done>
</task>

</tasks>

<verification>
- 11-HUMAN-UAT.md committed with operator-signed S0 + S1..S6 + S8.
- 11-VERIFICATION.md status: passed/passed_partial/failed populated.
- All 6 PRDs have a verdict row.
- D-01..D-19 covered in rollup table.
- ROADMAP.md Phase 11 marked complete (preferred via canonical SDK command).
- STATE.md current position advanced.
- Spend ≤ $5 absolute Phase 11 cap.
- No raw `ifix_sk_...` literals in 11-HUMAN-UAT.md or 11-VERIFICATION.md.
- No TOTP digits or backup codes in evidence.
</verification>

<success_criteria>
- Phase 11 closed end-to-end.
- All locked decisions D-01..D-19 accounted for with evidence.
- Live UAT for PRD-06 (S1-S4), D-18 fold (S5-S6), D-18.4 release (S0 prerequisite), D-19 (S8) signed.
- v1 milestone progresses (depending on remaining phases / milestone state).
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies — all deliverables doc-only or already-deployed code. (Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.)
- Truth 5: autonomous=false — operator drives UAT.
- Truth 6: gateway/dashboard images either at :latest-dev or freshly :v1.0.0 post-S0 retrigger.
- Truth 8 (cross-cutting): NO raw API keys, TOTP digits, backup codes, passwords, headers, request bodies, response bodies, DSNs, or PII in scenario docs or evidence; redacted evidence only.
- depends_on covers all Wave 1 + Wave 2 + Wave 3-prior plans.
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-10-SUMMARY.md when done with: paths committed (11-HUMAN-UAT.md + 11-VERIFICATION.md + STATE.md + ROADMAP.md), final phase status, total spend, links to all per-PRD evidence files, state-advance command used (canonical SDK vs manual fallback).
</output>
