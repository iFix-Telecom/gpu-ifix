---
phase: 11
plan: 11-06
slug: load-test-live-uat
wave: 2
depends_on: [11-01]
requirements: [PRD-01]
files_modified:
  - path: .planning/phases/11-prod-hardening/load-report-2026-MM-DD.json
    kind: NEW
  - path: .planning/phases/11-prod-hardening/11-06-EVIDENCE.md
    kind: NEW
autonomous: false
estimated_duration: 120
---

<objective>
Execute the PRD-01 live load test against prod gateway ai-gateway.converse-ai.app using the load-replay.py + audit-log-export.py scaffolded in 11-01. A 2-minute DRY-RUN precedes the 30-minute sustained run (reviews MEDIUM — validates replay shape + fixture row-count + route-mix distribution before spending Vast budget). Primary tier-0 (Vast 2×RTX 3090) UP throughout. Capture the JSON report as committed evidence (per-route upstream distribution included — reviews MEDIUM) and a brief markdown summary. Operator must observe + abort if Vast spend exceeds CONTEXT.md specifics cap ($5 absolute for Phase 11; $1-3 expected for this plan). Tenant API keys resolve at runtime from env (IFIX_KEY_*); no keys in plan instructions or evidence.
</objective>

<must_haves>
  truths:
    - "D-01: replay audit_log dev sanitizado (sensitive tenants excluded from baseline per Open Question #1)"
    - "D-02: tier-0 + tier-1 mix; Vast primary UP throughout"
    - "D-03: 30 min sustained duration, replay peak window (~14-15 BRT)"
    - "D-04 SLO v1.0 thresholds — P95 chat ≤5s, P95 embed ≤1s, P95 STT ≤10s, error rate <1% non-503, zero 5xx panic"
    - "D-05: load-replay.py + audit-log-export.py from scripts/integration-smoke/ (Phase 11 plan 11-01 scaffolded)"
    - "D-06: load generator host = ops-claude VM (10.10.10.10), HTTPS path through edge Traefik → n8n-ia-vm:8080"
    - "Pitfall 1: sanitization invariant — sensitive tenant rows skipped"
    - "Spend cap $5 absolute Phase 11; operator aborts if Vast spend exceeds"
    - "DRY-RUN gate (reviews MEDIUM): 2-min replay with full fixture validates row-count + route-mix + replay shape BEFORE committing to the 30-min sustained run"
    - "Per-route upstream distribution (reviews MEDIUM): load-report.json summary.routes.<route> includes upstreams: {<upstream_label>: count} for tier-0/tier-1 attribution at SLO eval time"
    - "Cross-cutting hard rule: NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in committed plan instructions, fixtures, schemas, evidence, or load reports. Tenant keys read from env IFIX_KEY_* at replay time; evidence redacts to first-4-char prefix only."
  must_haves:
    - .planning/phases/11-prod-hardening/load-report-YYYY-MM-DD.json committed with full per-route metrics + per-route upstream distribution + gates section
    - 11-06-EVIDENCE.md committed with operator log of: dry-run timestamp + fixture row count + route-mix summary, then 30-min start/end ts (UTC), Vast spend total ($USD), gates pass/fail per D-04 with tier-0/tier-1 attribution, any deviations
    - all 6 gates green per D-04 OR explicit operator sign-off acknowledging exceptions; gates.zero_5xx_panic set true by operator AFTER Sentry/log check (external observation per 11-01)
    - Vast primary FSM state observed=Ready throughout test
    - No `ifix_sk_...` literal in evidence; keys resolved from env IFIX_KEY_*
  key_links:
    - from: ops-claude python scripts/integration-smoke/load-replay.py
      to: https://ai-gateway.converse-ai.app
      via: HTTPS POST through edge Traefik + n8n-ia-vm:8080 host-port bypass; Authorization built at runtime from env IFIX_KEY_${TENANT_SLUG^^}
      pattern: "ai-gateway.converse-ai.app"
    - from: load-report-YYYY-MM-DD.json gates section
      to: load-replay-report-schema.json gates contract
      via: jsonschema Draft 2020-12 validation
      pattern: "p95_chat_ms_le_5000"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude -> prod gateway HTTPS | replay request bodies cross this boundary; sanitized fixture only; keys from env |
| operator -> Vast.ai account | spend accumulation during test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-LOAD-04 | Information Disclosure | replayed payloads leak in prod audit_log_content | mitigate | Audit-log-export.py (11-01) excludes sensitive tenants AND replaces tool_calls[].function.arguments with placeholder before fixture write; per Pitfall 1 |
| T-11-LOAD-05 | Denial of Service | replay accidentally saturates other tenants | mitigate | max_concurrency default 50; audit_log preserves original timing so peak isn't artificially inflated |
| T-11-LOAD-06 | Tampering | gateway under load uses wrong Vast pod shape | mitigate | Pre-flight assert: ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state' shows lifecycle Ready + 2x RTX 3090 allowlist matches |
| T-11-LOAD-07 | Information Disclosure | load-report.json contains raw bodies/keys/headers | mitigate | Schema enforced (11-01) — additionalProperties:false everywhere; report writer emits only the allowed metric record columns; pre-commit grep gate on evidence file |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-01-PLAN.md
@scripts/integration-smoke/load-replay.py
@scripts/integration-smoke/audit-log-export.py
@scripts/integration-smoke/load-replay-report-schema.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-06-01</id>
  <title>Task 1: Pre-flight gates + 2-min DRY-RUN (reviews MEDIUM)</title>
  <read_first>
    - 11-CONTEXT.md D-01..D-06 + Specifics line 161 ($5 spend cap)
    - 11-RESEARCH.md Pitfall 2 (Vast DELETE race vs probe — applies to 11-07 not here, but operator aware)
    - 11-RESEARCH.md Environment Availability table (ssh access, dig, Vast key, OpenRouter key, gh CLI)
    - .planning/STATE.md Phase 06.8 final GPU shape (2×RTX 3090 allowlist 43803,55158 cap $0.60)
    - scripts/integration-smoke/load-replay.py + audit-log-export.py + load-replay-report-schema.json (Plan 11-01 artifacts — must be importable/help-displayable BEFORE Vast pod boot)
  </read_first>
  <what-built>
    Pre-flight environment check + 2-minute DRY-RUN before the 30-min sustained run. Operator confirms: (a) prod gateway healthy + Vast primary lifecycle Ready, (b) sanitized fixture exported + non-empty + sensitive-tenant rows excluded, (c) ops-claude can reach https://ai-gateway.converse-ai.app, (d) operator has approved spend cap, (e) DRY-RUN proves replay shape + route mix matches expectations BEFORE the full 30-min commit.
  </what-built>
  <how-to-verify>
    Step 0 (AUTOMATED — runs before operator engagement; gates the human portion):
    a. `python scripts/integration-smoke/load-replay.py --help` exits 0 and prints all 7 args (gateway-url, fixture, duration, max-concurrency, out, speedup, audio-stub).
    b. `python scripts/integration-smoke/audit-log-export.py --help` exits 0.
    c. Synthesize a minimal empty report to validate schema shape works:
       ```
       cat > /tmp/sample-report.json <<'EOF'
       {
         "schema_version": "1.0.0",
         "started_at": "2026-05-27T14:00:00Z",
         "finished_at": "2026-05-27T14:30:00Z",
         "target": { "gateway_url": "https://ai-gateway.converse-ai.app", "tenant": "converseai" },
         "git_sha": "0000000",
         "summary": { "routes": {}, "total_requests": 0, "error_count": 0 },
         "errors": [],
         "gates": { "p95_chat_ms_le_5000": true, "p95_embed_ms_le_1000": true, "p95_stt_ms_le_10000": true, "error_rate_lt_1pct": true, "zero_5xx_panic": null, "all_passed": null },
         "gates_external_inputs": { "sentry_url": "https://ifix.sentry.io/issues/?project=4511455942017024", "log_query": "fluent-bit query for panic|recover", "verified_at": null }
       }
       EOF
       jsonschema -i /tmp/sample-report.json scripts/integration-smoke/load-replay-report-schema.json
       ```
       Exit 0 means the schema accepts a minimal valid report; failure → 11-01 scaffolding broken; do NOT spend Vast.

    Operator-driven steps (after Step 0 passes):
    1. ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state' returns lifecycle id + state=Ready + ≥2 hours TTL remaining.
    2. curl -s https://ai-gateway.converse-ai.app/v1/health/upstreams | jq '.upstreams | keys' returns local-llm, local-stt, local-embed, openrouter-chat, openai-whisper, openai-embed.
    3. Export sanitized fixture:
       `python scripts/integration-smoke/audit-log-export.py --dsn $AI_GATEWAY_DEV_DSN --window-start 2026-05-26T14:00:00Z --window-end 2026-05-26T15:00:00Z --out /tmp/load-fixture.jsonl`
       (~1k rows expected for 1h peak window.)
    4. Sanity checks on the fixture (reviews MEDIUM — row-count + route-mix BEFORE full run):
       - `jq -c 'select(.data_class == "sensitive")' /tmp/load-fixture.jsonl | wc -l` returns 0 (Pitfall 1 verification).
       - `wc -l /tmp/load-fixture.jsonl` returns at least 100 (baseline replay sufficient sample size).
       - Route-mix: `jq -r .route /tmp/load-fixture.jsonl | sort | uniq -c` — expect ≥1 row each for /v1/chat/completions, /v1/embeddings, /v1/audio/transcriptions. Operator records the distribution as DRY-RUN expectation.
       - No `_replay_api_key` field in fixture: `! jq '. | has("_replay_api_key")' /tmp/load-fixture.jsonl | grep true` (negative grep).
    5. Set env keys for ALL tenants present in the fixture. For each tenant_slug observed:
       `export IFIX_KEY_<TENANT_SLUG_UPPER>="$(... operator pulls from local-only ~/.claude/CLAUDE.md ...)"`
       Verify: `python -c "import os, json, sys; slugs=set(); [slugs.add(json.loads(l).get('tenant_slug')) for l in open('/tmp/load-fixture.jsonl')]; missing=[s for s in slugs if not os.environ.get(f'IFIX_KEY_{s.upper().replace(\"-\",\"_\")}')]; print('missing:', missing); sys.exit(1 if missing else 0)"` — exit 0.
    6. 2-MIN DRY-RUN (reviews MEDIUM):
       ```
       python scripts/integration-smoke/load-replay.py \
         --gateway-url https://ai-gateway.converse-ai.app \
         --fixture /tmp/load-fixture.jsonl \
         --duration 120 \
         --max-concurrency 20 \
         --out /tmp/dry-run-report.json
       ```
       Inspect /tmp/dry-run-report.json:
       - `jq '.summary.routes | keys' /tmp/dry-run-report.json` matches the expected route mix from step 4.
       - `jq '.summary.routes | to_entries | map({route: .key, n: .value.n, p95: .value.p95_ms, upstreams: .value.upstreams})' /tmp/dry-run-report.json` shows per-route upstream distribution.
       - `jq '.summary.total_requests' /tmp/dry-run-report.json` ≥ 50 (2 min should produce enough samples).
       - `jq '.gates' /tmp/dry-run-report.json` — at least the latency gates should be defined; zero_5xx_panic null is expected at this point.
    7. Operator decides: GO (proceed to Task 11-06-02 30-min run) OR NO-GO (route mix wrong / fixture too sparse / dry-run hit errors → fix and re-run preflight). Document decision in 11-06-EVIDENCE.md preflight section.
    8. Operator confirms via WhatsApp/voice "approved spend cap $5 absolute; load-test starting at <timestamp>".
  </how-to-verify>
  <verify>
    <automated>python scripts/integration-smoke/load-replay.py --help >/dev/null 2>&1 && python scripts/integration-smoke/audit-log-export.py --help >/dev/null 2>&1 && python -c "import json, jsonschema; sample = {'schema_version': '1.0.0', 'started_at': '2026-05-27T14:00:00Z', 'finished_at': '2026-05-27T14:30:00Z', 'target': {'gateway_url': 'https://ai-gateway.converse-ai.app', 'tenant': 'converseai'}, 'git_sha': '0000000', 'summary': {'routes': {}, 'total_requests': 0, 'error_count': 0}, 'errors': [], 'gates': {'p95_chat_ms_le_5000': True, 'p95_embed_ms_le_1000': True, 'p95_stt_ms_le_10000': True, 'error_rate_lt_1pct': True, 'zero_5xx_panic': None, 'all_passed': None}, 'gates_external_inputs': {'sentry_url': 'https://ifix.sentry.io', 'log_query': 'panic', 'verified_at': None}}; schema = json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); jsonschema.Draft202012Validator(schema).validate(sample); print('preflight static-check OK')"</automated>
  </verify>
  <resume-signal>Type "preflight approved — fixture=NNN rows; routes={chat:X,embed:Y,stt:Z}; dry-run OK; primary Ready; env keys verified; spend cap acknowledged" or describe blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-06-02</id>
  <title>Task 2: 30-min sustained replay + observe metrics</title>
  <read_first>
    - 11-CONTEXT.md D-03 + D-04 (30min sustained + SLO v1.0 thresholds verbatim)
    - 11-RESEARCH.md Architecture Patterns System Architecture Diagram (load test traffic path)
    - scripts/integration-smoke/load-replay.py (the runner from 11-01)
  </read_first>
  <what-built>
    The 30-minute sustained load replay against prod gateway. Operator launches load-replay.py from ops-claude (env IFIX_KEY_* already set from preflight), monitors gateway health + Vast pod metrics + Sentry breadcrumbs in parallel terminal, and verifies the run completes without breakers tripping. This plan is baseline, NOT chaos — chaos lives in 11-07/11-08.
  </what-built>
  <how-to-verify>
    1. On ops-claude in tmux session "load-test":
       ```
       python scripts/integration-smoke/load-replay.py \
         --gateway-url https://ai-gateway.converse-ai.app \
         --fixture /tmp/load-fixture.jsonl \
         --duration 1800 \
         --max-concurrency 50 \
         --out .planning/phases/11-prod-hardening/load-report-$(date +%Y-%m-%d).json
       ```
       (Tenant Authorization is built at request time from env IFIX_KEY_${TENANT_SLUG^^}. No keys in argv.)
    2. In parallel terminal: `watch -n 30 'ssh n8n-ia-vm "docker exec ifix-ai-gateway /gatewayctl primary state; curl -s localhost:8080/v1/health/upstreams | jq"'` — verify primary stays Ready and no breaker OPEN unexpectedly.
    3. In parallel terminal: monitor Vast spend in https://cloud.vast.ai/instances — abort if hourly burn rate × test duration would exceed $1 (alert) or $3 (operator-decide).
    4. After 30 min: load-replay.py prints summary to stdout AND writes load-report-YYYY-MM-DD.json. Inspect:
       - `jq '.gates' .planning/phases/11-prod-hardening/load-report-*.json` — 5 latency/error gates should be true; zero_5xx_panic null until step 5.
       - `jq '.summary.routes | to_entries | map({route: .key, n: .value.n, p50: .value.p50_ms, p95: .value.p95_ms, p99: .value.p99_ms, upstreams: .value.upstreams})' …` — per-route latency AND upstream distribution (reviews MEDIUM tier attribution).
    5. EXTERNAL panic gate (reviews LOW + 11-01 schema): operator queries Sentry for project ifix-ai-gateway-prod for the run window (started_at→finished_at). If zero panic events AND zero gateway-log lines matching "panic" / "goroutine" → set `gates.zero_5xx_panic = true` AND `gates_external_inputs.verified_at = <timestamp>` AND recompute `gates.all_passed = AND of 5 gates`. Use `jq` to mutate then re-validate against the schema.
    6. If any gate is false: operator decides — re-run after fix OR document as a Phase 11 gap in 11-06-EVIDENCE.md (re-route to /gsd:plan-phase 11 --gaps if blocker).
    7. Capture total Vast spend from dashboard: write to 11-06-EVIDENCE.md.
  </how-to-verify>
  <resume-signal>Type "load-test PASS — all_passed=true; spend=$X.YY; report at load-report-YYYY-MM-DD.json; zero_5xx_panic verified via Sentry" OR describe gate failures.</resume-signal>
</task>

<task type="auto">
  <id>11-06-03</id>
  <title>Task 3: Commit evidence files (redacted)</title>
  <files>.planning/phases/11-prod-hardening/load-report-YYYY-MM-DD.json, .planning/phases/11-prod-hardening/11-06-EVIDENCE.md</files>
  <read_first>
    - Output of task 11-06-02 (load-report-YYYY-MM-DD.json)
    - 11-RESEARCH.md Validation Architecture (sampling rate)
    - 11-VALIDATION.md task 11-06-XX row
  </read_first>
  <action>
    Step 1: copy load-report from /tmp or current working directory to .planning/phases/11-prod-hardening/load-report-$(date +%Y-%m-%d).json (if not already written there).

    Step 2: author .planning/phases/11-prod-hardening/11-06-EVIDENCE.md with the following sections:
    - Header: # Phase 11 PRD-01 Load-Test Evidence
    - Metadata: date YYYY-MM-DD, operator name, ops-claude tmux session id, fixture path + row count, gateway target URL.
    - DRY-RUN summary (reviews MEDIUM): timestamp, duration, total_requests, route-mix observed, GO/NO-GO decision rationale.
    - 30-min Run summary: start_ts UTC, end_ts UTC, total_requests, error_count.
    - Per-route metrics with upstream distribution (reviews MEDIUM tier attribution):
      | Route | n | p50_ms | p95_ms | p99_ms | upstreams (label: count) |
      | /v1/chat/completions | X | … | … | … | local-llm: N, openrouter-chat: M |
      | /v1/embeddings | X | … | … | … | local-embed: N, openai-embed: M |
      | /v1/audio/transcriptions | X | … | … | … | local-stt: N, openai-whisper: M |
    - Gates verdict (D-04 5 latency/error + 1 external panic = 6 booleans):
      - p95_chat_ms_le_5000: …
      - p95_embed_ms_le_1000: …
      - p95_stt_ms_le_10000: …
      - error_rate_lt_1pct: …
      - zero_5xx_panic: … (Sentry URL + log query attestation per gates_external_inputs)
      - all_passed: …
    - Vast spend: total $USD; instance id used; hours billed; per-hour rate.
    - Deviations / pitfalls hit: any unexpected breaker flap, retries triggered, host-side anomaly. Reference RUNBOOK-INCIDENTS class 1 if primary FSM moved.
    - Cross-ref: load-report-YYYY-MM-DD.json (sibling file).
    - Sign-off: operator + date.
    - Cross-cutting attestation: no raw API keys, headers, request bodies, response bodies, DSNs, or PII; tenant references via env-var labels OR first-4-char prefix only.

    Step 3: git add + commit both files with message "docs(11-06): PRD-01 load-test evidence — load-report + EVIDENCE.md". Pre-commit grep gate: `! grep -E "ifix_sk_[a-z0-9]{20,}|Bearer [A-Za-z0-9_]{20,}|postgres://" .planning/phases/11-prod-hardening/11-06-EVIDENCE.md` MUST pass.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/load-report-$(date +%Y-%m-%d).json && test -f .planning/phases/11-prod-hardening/11-06-EVIDENCE.md && python -c "import json, jsonschema; r = json.load(open([f for f in __import__('glob').glob('.planning/phases/11-prod-hardening/load-report-*.json')][0])); s = json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); jsonschema.Draft202012Validator(s).validate(r); print('schema valid'); print('all_passed=', r['gates']['all_passed'])" && ! grep -E "ifix_sk_[a-z0-9]{20,}|Bearer [A-Za-z0-9_]{20,}|postgres://" .planning/phases/11-prod-hardening/11-06-EVIDENCE.md</automated>
  </verify>
  <done>Both files committed; jsonschema validation passes; gates.all_passed=true OR explicit deviation noted in EVIDENCE.md with operator sign-off; no secret strings in EVIDENCE.md.</done>
</task>

</tasks>

<verification>
- load-report-YYYY-MM-DD.json passes jsonschema validation against scripts/integration-smoke/load-replay-report-schema.json.
- .gates.all_passed === true (or operator-signed deviation) AFTER external panic-gate verification.
- DRY-RUN run summary present in EVIDENCE.md.
- Per-route upstream distribution present in EVIDENCE.md.
- Vast spend within $5 absolute cap.
- Primary FSM Ready throughout.
- 11-06-EVIDENCE.md grep for raw secrets returns 0.
</verification>

<success_criteria>
- PRD-01 SLO v1.0 D-04 6 gates green on 30-min sustained replay (5 measured + 1 external observation).
- DRY-RUN preceded full run.
- Per-route upstream distribution captured (tier attribution).
- Evidence committed for later runbook ingestion (11-09 uses this as canonical example).
- Spend within budget. No raw keys in evidence.
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies for execution. (Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.)
- Truth 4: tenant keys in env vars IFIX_KEY_* — NOT in CLI args, NOT in JSONL fixture, NOT in evidence (Pattern A discipline).
- Truth 5: This plan is autonomous=false (operator runs + observes).
- Truth 8 (cross-cutting): NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in committed plan instructions, fixtures, schemas, evidence, or load reports.
- Spend cap $5 absolute Phase 11 — operator aborts if exceeded.
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-06-SUMMARY.md when done with: paths committed, jsonschema validation result, gates verdict, spend total, per-route upstream tally, link to 11-06-EVIDENCE.md.
</output>
