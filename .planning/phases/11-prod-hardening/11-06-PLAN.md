---
phase: 11
plan: 11-06
slug: load-test-live-uat
wave: 2
depends_on: [11-01]
requirements: [PRD-01]
files_modified:
  - path: .planning/phases/11-prod-hardening/load-report-2026-MM-DD.json
    kind: NEW
  - path: .planning/phases/11-prod-hardening/11-06-EVIDENCE.md
    kind: NEW
autonomous: false
estimated_duration: 150
---

<objective>
Execute the PRD-01 live load test against prod gateway `ai-gateway.converse-ai.app` using the load-replay.py + audit-log-export.py scaffolded in 11-01 (now revised to JOIN `audit_log_content.prompt`, resolve tenant keys from env at runtime, and replay STT as multipart with the deterministic fixture OGG). A two-stage preflight precedes the 30-minute sustained run (per reviews MEDIUM #1 + LOW #3 + LOW #4 — 11-01 deliverable assertion, fixture row-count + route-mix check, and a 2-min DRY-RUN that must produce a non-empty validated report BEFORE Vast spend). Primary tier-0 (Vast 2×RTX 3090) MUST be Ready throughout. Capture the JSON report as committed evidence with per-route AND per-upstream P50/P95/P99 distribution (reviews MEDIUM #2 — tier-0/tier-1 attribution at SLO eval time) and a brief markdown summary. Cross-cutting hard rule (reviews HIGH #5 — consensus Top Action #3): zero `ifix_sk_...` literals, raw bodies, Authorization headers, DSNs, or PII in plan instructions, evidence, or load reports. Tenant keys resolved at runtime from env `IFIX_KEY_*`. Operator observes + aborts if Vast spend exceeds the Phase 11 absolute cap ($5; $1-3 expected for this plan).
</objective>

<must_haves>
  truths:
    - "D-01: replay audit_log dev sanitizado (sensitive tenants excluded from baseline per Open Question #1)"
    - "D-02: tier-0 + tier-1 mix; Vast primary 2×RTX 3090 UP throughout"
    - "D-03: 30 min sustained duration, replay peak window (~14-15 BRT)"
    - "D-04 SLO v1.0 thresholds — P95 chat ≤5s, P95 embed ≤1s, P95 STT ≤10s, error rate <1% non-503, zero 5xx panic"
    - "D-05: load-replay.py + audit-log-export.py from scripts/integration-smoke/ (Phase 11 plan 11-01 scaffolded; this plan asserts 11-01 deliverables exist BEFORE preflight starts)"
    - "D-06: load generator host = ops-claude VM (10.10.10.10), HTTPS path through edge Traefik → n8n-ia-vm:8080"
    - "Pitfall 1: sanitization invariant — sensitive tenant rows skipped"
    - "Spend cap $5 absolute Phase 11; operator aborts if Vast spend exceeds"
    - "[reviews MEDIUM #1] 11-01 deliverables pre-flight: load-replay.py + audit-log-export.py + load-replay-report-schema.json + fixtures/whatsapp-sample.ogg + .planning/load-test-fixtures/ all present and importable BEFORE any Vast spend"
    - "[reviews LOW #4] Fixture row-count + route-mix gate: fixture has ≥1000 rows AND contains all 5 route classes (chat, embed, STT, tool-call shape, stream) BEFORE the 30-min run; if not, abort and re-export with a wider window or different peak"
    - "[reviews LOW #3] DRY-RUN gate: 2-min replay with the real fixture produces /tmp/dry-run-report.json with total_requests > 0 AND error_rate < 5% AND summary.routes contains /v1/chat/completions; failure aborts BEFORE 30-min commit"
    - "[reviews MEDIUM #2] Per-route AND per-upstream distribution: load-report.json summary.routes.<route>.upstreams.<upstream_label>.{n,p50_ms,p95_ms,p99_ms} populated for tier-0 (local-llm/local-embed/local-stt) AND tier-1 (openrouter-chat/openai-embed/openai-whisper) so D-04 gate failures can be attributed to a specific upstream"
    - "[reviews HIGH #5] Secret + body hygiene — cross-cutting hard rule: NO raw API keys (`ifix_sk_...`, `sk-or-...`, `sk-...`), no Authorization header values, no request bodies, no response bodies, no DSNs, no PII anywhere in plan instructions, fixtures, schemas, load reports, or EVIDENCE.md. Tenant keys ALWAYS referenced as env-var labels (e.g. `$IFIX_KEY_TELEFONIA`). Evidence sanitization audit task enforces this with grep + jq gates."
  must_haves:
    - .planning/phases/11-prod-hardening/load-report-YYYY-MM-DD.json committed with full per-route + per-upstream metrics + gates section
    - 11-06-EVIDENCE.md committed with operator log of: (a) 11-01 deliverables preflight check, (b) fixture row-count + route-mix summary, (c) DRY-RUN timestamp + summary, (d) 30-min start/end ts (UTC), (e) Vast spend total ($USD), (f) gates pass/fail per D-04 with tier-0/tier-1 upstream attribution, (g) deviations, (h) secret hygiene attestation
    - all 6 gates green per D-04 OR explicit operator sign-off acknowledging exceptions; gates.zero_5xx_panic set true by operator AFTER Sentry/log check (external observation per 11-01 schema design)
    - Vast primary FSM state observed=Ready throughout test
    - Per-route upstream distribution present in both load-report-*.json AND EVIDENCE.md table
    - Zero `ifix_sk_`, `sk-or-`, `sk-`, `Bearer `, `postgres://`, `password`, `dsn` literals in committed plan, evidence, or report files (grep-enforced)
    - load-report-*.json `requests` array (if present) MUST NOT contain keys `body`, `authorization`, `headers`, `prompt_text`, `response_text` (jq-enforced)
  key_links:
    - from: ops-claude python scripts/integration-smoke/load-replay.py
      to: https://ai-gateway.converse-ai.app
      via: HTTPS POST through edge Traefik + n8n-ia-vm:8080 host-port bypass; Authorization built at runtime from env IFIX_KEY_${TENANT_SLUG^^}
      pattern: "ai-gateway.converse-ai.app"
    - from: load-report-YYYY-MM-DD.json gates section
      to: scripts/integration-smoke/load-replay-report-schema.json gates contract
      via: jsonschema Draft 2020-12 validation
      pattern: "p95_chat_ms_le_5000"
    - from: load-report-YYYY-MM-DD.json summary.routes.<route>.upstreams
      to: gateway X-Upstream response header captured per-request by load-replay.py
      via: per-request metric record `upstream` field aggregated by route
      pattern: "upstreams"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude -> prod gateway HTTPS | replay request bodies cross this boundary; sanitized fixture only; tenant keys read from env at request time, never in argv |
| operator -> Vast.ai account | spend accumulation during test; operator monitors dashboard + aborts on cap breach |
| load-report-*.json -> committed git tree | report file committed for runbook ingestion (11-09); MUST NOT carry raw bodies, headers, or keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-LOAD-04 | Information Disclosure | replayed payloads leak in prod audit_log_content | mitigate | Audit-log-export.py (11-01) excludes sensitive tenants AND replaces tool_calls[].function.arguments with placeholder before fixture write; per Pitfall 1 |
| T-11-LOAD-05 | Denial of Service | replay accidentally saturates other tenants | mitigate | max_concurrency default 50; audit_log preserves original timing so peak isn't artificially inflated; --speedup gated by operator judgment |
| T-11-LOAD-06 | Tampering | gateway under load uses wrong Vast pod shape | mitigate | Pre-flight assert: ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state' shows lifecycle Ready + 2×RTX 3090 allowlist matches |
| T-11-LOAD-07 | Information Disclosure | load-report.json contains raw bodies/keys/headers | mitigate | Schema enforced (11-01) — additionalProperties:false everywhere; report writer emits only the allowed metric record columns; sanitization audit task (this plan, task 11-06-04) runs grep + jq gates BEFORE git commit |
| T-11-LOAD-08 | Information Disclosure | operator pastes raw `ifix_sk_...` into EVIDENCE.md by habit | mitigate | Plan instructions only reference env-var labels; pre-commit grep gate fails on any `ifix_sk_[a-z0-9]{20,}` / `sk-or-` / `sk-[A-Z]` / `Bearer ` / `postgres://` substring in EVIDENCE.md or load-report-*.json |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-prod-hardening/11-CONTEXT.md
@.planning/phases/11-prod-hardening/11-RESEARCH.md
@.planning/phases/11-prod-hardening/11-01-PLAN.md
@scripts/integration-smoke/load-replay.py
@scripts/integration-smoke/audit-log-export.py
@scripts/integration-smoke/load-replay-report-schema.json
@scripts/integration-smoke/fixtures/whatsapp-sample.ogg
</context>

<tasks>

<task type="auto">
  <id>11-06-01</id>
  <title>Task 1 [reviews MEDIUM #1]: Assert 11-01 deliverables present + importable</title>
  <files>(no file modifications — read-only assertion task; output is exit-code + stdout captured into 11-06-EVIDENCE.md preflight section)</files>
  <read_first>
    - .planning/phases/11-prod-hardening/11-01-PLAN.md success_criteria (all 5 reviews findings closed: HIGH #1 JOIN, MEDIUM #2 env-var keys, MEDIUM #3 multipart STT, LOW #4 panic null, LOW #5 --speedup)
    - scripts/integration-smoke/load-replay.py (must exist + 7 args)
    - scripts/integration-smoke/audit-log-export.py (must exist + 5 args)
    - scripts/integration-smoke/load-replay-report-schema.json (must exist + valid Draft 2020-12)
    - scripts/integration-smoke/fixtures/whatsapp-sample.ogg (must exist + OGG signature)
    - .planning/load-test-fixtures/.gitignore (must exist — directory marker)
  </read_first>
  <action>
    Cross-AI-flagged dependency assertion (reviews MEDIUM #1): before any operator engagement or Vast spend, prove that plan 11-01 actually shipped the artifacts this plan consumes. Run the five sub-checks below from ops-claude shell; any failure means 11-01 is incomplete and the orchestrator MUST re-run /gsd:execute-plan 11-01 before proceeding to task 11-06-02.

    Sub-checks (each MUST exit 0):

    1. Audit-log exporter importable + JOIN present:
       `python scripts/integration-smoke/audit-log-export.py --help` exits 0 AND prints 5 args (`--dsn`, `--window-start`, `--window-end`, `--out`, `--include-sensitive`).
       Source assertion: `grep -E "LEFT JOIN ai_gateway\.audit_log_content|JOIN .* audit_log_content" scripts/integration-smoke/audit-log-export.py` returns ≥1 match (closes reviews HIGH #1 from 11-01 review cycle — body source-of-truth JOIN present).

    2. Load-replay engine importable + 7 args (including `--speedup` and `--audio-stub`):
       `python scripts/integration-smoke/load-replay.py --help` exits 0 AND lists `gateway-url`, `fixture`, `duration`, `max-concurrency`, `out`, `speedup`, `audio-stub`.
       Source assertion (env-var key resolution): `grep -E "IFIX_KEY_" scripts/integration-smoke/load-replay.py` returns ≥1 match.
       Source assertion (multipart STT): `grep -E "files\s*=" scripts/integration-smoke/load-replay.py` returns ≥1 match.

    3. Report schema valid Draft 2020-12 + panic gate is external observation:
       `python -c "import json, jsonschema; s=json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); jsonschema.Draft202012Validator.check_schema(s); assert s['properties']['gates']['properties']['zero_5xx_panic']['type']==['boolean','null']; assert 'gates_external_inputs' in s['properties']; print('OK')"` exits 0.

    4. STT fixture audio present + OGG signature:
       `test -f scripts/integration-smoke/fixtures/whatsapp-sample.ogg && file scripts/integration-smoke/fixtures/whatsapp-sample.ogg | grep -iE "ogg|opus"` exits 0.

    5. Fixture directory marker + gitignore rules present:
       `test -f .planning/load-test-fixtures/.gitignore && grep -E "load-test-fixtures/\*\.jsonl" .gitignore` exits 0.

    Record each sub-check result (PASS/FAIL + relevant grep/help excerpt) in 11-06-EVIDENCE.md preflight section. If any sub-check fails, exit 1 and HALT — do NOT proceed to task 11-06-02. The remediation is: re-run /gsd:execute-plan 11-01 to fix the gap. Reference 11-01-SUMMARY.md for byte-counts + closes-tags.
  </action>
  <acceptance_criteria>
    - All 5 sub-checks pass (exit 0 each).
    - 11-01 deliverables preflight section drafted (in scratch / shell capture; written to EVIDENCE.md in task 11-06-05).
    - Zero Vast spend incurred by this task (no HTTPS calls to prod gateway, no Vast API calls).
  </acceptance_criteria>
  <verify>
    <automated>python scripts/integration-smoke/audit-log-export.py --help 2>&1 | grep -cE "dsn|window-start|window-end|out|include-sensitive" | grep -E "^\s*[5-9]" && grep -E "LEFT JOIN ai_gateway\.audit_log_content|JOIN .* audit_log_content" scripts/integration-smoke/audit-log-export.py && python scripts/integration-smoke/load-replay.py --help 2>&1 | grep -cE "gateway-url|fixture|duration|max-concurrency|out|speedup|audio-stub" | grep -E "^\s*[7-9]" && grep -E "IFIX_KEY_" scripts/integration-smoke/load-replay.py && grep -E "files\s*=" scripts/integration-smoke/load-replay.py && python -c "import json, jsonschema; s=json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); jsonschema.Draft202012Validator.check_schema(s); assert s['properties']['gates']['properties']['zero_5xx_panic']['type']==['boolean','null']; assert 'gates_external_inputs' in s['properties']" && test -f scripts/integration-smoke/fixtures/whatsapp-sample.ogg && file scripts/integration-smoke/fixtures/whatsapp-sample.ogg | grep -iE "ogg|opus" && test -f .planning/load-test-fixtures/.gitignore && grep -E "load-test-fixtures/\*\.jsonl" .gitignore</automated>
  </verify>
  <done>All 5 sub-checks exit 0; preflight evidence row drafted; HALT-on-failure semantics honored. Closes reviews MEDIUM #1.</done>
</task>

<task type="auto">
  <id>11-06-02</id>
  <title>Task 2 [reviews LOW #4]: Export fixture + row-count + route-mix gate</title>
  <files>/tmp/load-fixture.jsonl (LOCAL — gitignored under .planning/load-test-fixtures/* convention if moved there; this task writes to /tmp by default)</files>
  <read_first>
    - 11-CONTEXT.md D-01 + Pitfall 1 (sensitive tenants excluded, sanitized JSONL)
    - scripts/integration-smoke/audit-log-export.py (Task 11-01-01 artifact)
    - 11-RESEARCH.md §Environment Availability (ssh access, dig, Vast key, OpenRouter key, gh CLI)
    - 11-RESEARCH.md §Validation Architecture (peak window heuristic ~14-15 BRT)
  </read_first>
  <action>
    Export the sanitized fixture from prod audit_log dev window AND prove it has sufficient row count + route diversity BEFORE committing Vast spend (reviews LOW #4 — Codex flagged that empty/static fixtures don't prove real report shape).

    Step A — Export. Operator picks a 1-hour peak window from the dev audit_log (~14-15 BRT typical traffic spike). DSN read from env `AI_GATEWAY_DEV_DSN` (Pattern A secret-once — never logged, never argv-defaulted):
    `python scripts/integration-smoke/audit-log-export.py --dsn "$AI_GATEWAY_DEV_DSN" --window-start 2026-05-26T14:00:00Z --window-end 2026-05-26T15:00:00Z --out /tmp/load-fixture.jsonl`

    Step B — Row-count gate (reviews LOW #4): `wc -l /tmp/load-fixture.jsonl` MUST return ≥1000 lines (sufficient sample to drive 30 min × ~5 RPS sustained without re-cycling). If <1000, operator widens the window OR picks a different peak hour OR enables `--include-sensitive` only if the run is internal-only AND operator signs off (default stays off per D-01 + Pitfall 1).

    Step C — Route-mix gate (reviews LOW #4): the fixture MUST exercise all 5 route classes the gateway serves. Run:
    `jq -r '.route' /tmp/load-fixture.jsonl | sort -u`
    Operator confirms the output contains at minimum `/v1/chat/completions`, `/v1/embeddings`, `/v1/audio/transcriptions`. If `/v1/chat/completions` appears, also verify the streaming + tool-call shapes are present in `_sanitized_body`:
    - Streaming: `jq -c 'select(.route=="/v1/chat/completions") | select(._sanitized_body.stream==true)' /tmp/load-fixture.jsonl | wc -l` ≥1.
    - Tool-call: `jq -c 'select(.route=="/v1/chat/completions") | select(._sanitized_body.tools != null or ._sanitized_body.tool_choice != null)' /tmp/load-fixture.jsonl | wc -l` ≥1 (post-sanitization the arguments field is `_replay_placeholder` but the structural keys remain — proves the gateway tool-call validation path is exercised).
    If any of the 5 route shapes is missing, the fixture does NOT represent realistic prod traffic and the operator MUST widen the export window. Do NOT proceed to Task 11-06-03 DRY-RUN with an incomplete fixture.

    Step D — Sanitization invariants (Pitfall 1 verification + reviews HIGH #5 secret hygiene at fixture level):
    - `jq -c 'select(.data_class == "sensitive")' /tmp/load-fixture.jsonl | wc -l` returns 0 (sensitive tenants skipped).
    - `! jq -e '. | has("_replay_api_key")' /tmp/load-fixture.jsonl 2>/dev/null | grep true` (negative grep — no API-key field in fixture; closes reviews HIGH #5 at the fixture layer).
    - `! grep -E 'ifix_sk_[a-z0-9]{20,}|sk-or-|Bearer [A-Za-z0-9_]{20,}' /tmp/load-fixture.jsonl` (negative grep — no raw key materials anywhere in the JSONL bytes).

    Step E — Tenant key env-var preflight (reviews HIGH #5): collect the unique `tenant_slug` set from the fixture and assert every required `IFIX_KEY_${TENANT_SLUG^^}` env var is exported in the operator shell BEFORE task 11-06-03:
    `python -c "import os, json, sys; slugs=sorted({json.loads(l).get('tenant_slug') for l in open('/tmp/load-fixture.jsonl') if l.strip()}); missing=[s for s in slugs if not os.environ.get(f'IFIX_KEY_{s.upper().replace(\"-\",\"_\")}')]; print('tenants in fixture:', slugs); print('missing env vars:', missing); sys.exit(1 if missing else 0)"`
    Operator sources keys from local-only `~/.claude/CLAUDE.md` (Tenant API keys section) — NEVER copies key value into any committed file. Diagnostic printout shows env-var NAME + 4-char prefix only.

    Record the fixture path + row count + route-mix output + missing-env-var check result in scratch (written to EVIDENCE.md in task 11-06-05).
  </action>
  <acceptance_criteria>
    - /tmp/load-fixture.jsonl exists with ≥1000 lines.
    - All 5 route classes present (chat, embed, STT, chat-stream, chat-tool-call).
    - Sensitive tenant rows count = 0.
    - No `_replay_api_key` field in any JSONL line.
    - No `ifix_sk_...` / `sk-or-...` / `Bearer ` substring in fixture bytes.
    - All required `IFIX_KEY_*` env vars exported in operator shell (preflight Python check exits 0).
  </acceptance_criteria>
  <verify>
    <automated>test -f /tmp/load-fixture.jsonl && [ "$(wc -l < /tmp/load-fixture.jsonl)" -ge 1000 ] && jq -r '.route' /tmp/load-fixture.jsonl | sort -u | grep -E "/v1/chat/completions" && jq -r '.route' /tmp/load-fixture.jsonl | sort -u | grep -E "/v1/embeddings" && jq -r '.route' /tmp/load-fixture.jsonl | sort -u | grep -E "/v1/audio/transcriptions" && [ "$(jq -c 'select(.data_class == "sensitive")' /tmp/load-fixture.jsonl | wc -l)" -eq 0 ] && ! grep -qE 'ifix_sk_[a-z0-9]{20,}|sk-or-|Bearer [A-Za-z0-9_]{20,}' /tmp/load-fixture.jsonl && python -c "import os, json, sys; slugs=sorted({json.loads(l).get('tenant_slug') for l in open('/tmp/load-fixture.jsonl') if l.strip()}); missing=[s for s in slugs if not os.environ.get(f'IFIX_KEY_{s.upper().replace(chr(45),chr(95))}')]; sys.exit(1 if missing else 0)"</automated>
  </verify>
  <done>Fixture exported + 6 acceptance criteria pass. Closes reviews LOW #4 (row-count + route-mix gate). Operator has tenant keys in env, never in fixture.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-06-03</id>
  <title>Task 3 [reviews LOW #3]: 2-min DRY-RUN with concrete acceptance criteria + GO/NO-GO gate</title>
  <read_first>
    - 11-CONTEXT.md D-03 + D-04 (30 min sustained, SLO thresholds)
    - 11-CONTEXT.md Specifics line 161 ($5 absolute spend cap)
    - scripts/integration-smoke/load-replay.py (the runner from 11-01)
    - .planning/STATE.md Phase 06.8 final GPU shape (2×RTX 3090 allowlist 43803,55158 cap $0.60)
    - /tmp/load-fixture.jsonl (output of task 11-06-02 — already validated)
  </read_first>
  <what-built>
    Two-minute DRY-RUN of the real fixture against the real prod gateway, producing /tmp/dry-run-report.json with concrete acceptance criteria operator must verify before committing to the 30-minute sustained run + Vast spend.

    Concrete reviews-LOW-#3 acceptance criteria (operator inspects /tmp/dry-run-report.json):
    - `jq '.summary.total_requests' /tmp/dry-run-report.json` returns an integer > 0 (replay actually fired).
    - `jq '.summary.error_count / .summary.total_requests * 100 < 5' /tmp/dry-run-report.json` returns `true` (error rate < 5%; baseline-noise tolerance — stricter <1% gate applies only to the 30-min run).
    - `jq '.summary.routes | has("/v1/chat/completions")' /tmp/dry-run-report.json` returns `true` (chat route exercised).
    - `jq '.summary.routes | keys' /tmp/dry-run-report.json` matches expected route set from task 11-06-02 step C.
    - jsonschema-validates against scripts/integration-smoke/load-replay-report-schema.json.

    If ANY criterion fails, operator aborts BEFORE Vast spend — fixture or gateway environment is wrong, fix and re-run preflight from task 11-06-01.
  </what-built>
  <how-to-verify>
    Step 1 — Prod gateway health + Vast primary state preflight:
    a. `ssh n8n-ia-vm 'docker exec ifix-ai-gateway /gatewayctl primary state'` returns lifecycle id + state=Ready + ≥2 hours TTL remaining + 2×RTX 3090 shape (offer ids 43803,55158 cap $0.60). If state != Ready, operator runs `/gatewayctl primary force-up` and waits BEFORE proceeding.
    b. `curl -s https://ai-gateway.converse-ai.app/v1/health/upstreams | jq '.upstreams | keys | sort'` returns the 6 upstreams: local-llm, local-stt, local-embed, openrouter-chat, openai-whisper, openai-embed.

    Step 2 — Operator spend cap acknowledgement:
    Operator confirms via WhatsApp/voice "approved spend cap $5 absolute Phase 11; load-test starting at <ISO timestamp>". Recorded in EVIDENCE.md task 11-06-05.

    Step 3 — 2-MIN DRY-RUN. From ops-claude shell (env IFIX_KEY_* already exported per task 11-06-02 step E):
    `python scripts/integration-smoke/load-replay.py --gateway-url https://ai-gateway.converse-ai.app --fixture /tmp/load-fixture.jsonl --duration 120 --max-concurrency 20 --out /tmp/dry-run-report.json`
    (No tenant key in argv — keys resolved at request time from env IFIX_KEY_${TENANT_SLUG^^} per 11-01 design.)

    Step 4 — Inspect /tmp/dry-run-report.json against the 5 concrete acceptance criteria above. If `summary.total_requests == 0` → fixture or HTTPS path broken; abort. If `error_count / total_requests >= 0.05` → gateway or fixture sanitization broken; abort. If `/v1/chat/completions` absent → fixture route-mix did not flow; abort.

    Step 5 — Per-route upstream attribution preview (reviews MEDIUM #2 — surfaces tier distribution early so 30-min run isn't a surprise):
    `jq '.summary.routes | to_entries | map({route: .key, n: .value.n, p95: .value.p95_ms, upstreams: .value.upstreams})' /tmp/dry-run-report.json`
    Operator notes whether both `local-*` (tier-0) AND `openrouter-*`/`openai-*` (tier-1) labels appear — if only one tier present, the 30-min run will not exercise the mix and SLO attribution will be incomplete; consider increasing duration or speedup.

    Step 6 — Operator decides GO/NO-GO. Documents decision + rationale in scratch for EVIDENCE.md.
  </how-to-verify>
  <verify>
    <automated>test -f /tmp/dry-run-report.json && python -c "import json, jsonschema; r=json.load(open('/tmp/dry-run-report.json')); s=json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); jsonschema.Draft202012Validator(s).validate(r); assert r['summary']['total_requests'] > 0, 'total_requests==0'; tot=r['summary']['total_requests']; err=r['summary']['error_count']; assert err/tot < 0.05, f'error rate {err/tot*100:.1f}% >= 5%'; assert '/v1/chat/completions' in r['summary']['routes'], 'chat route missing'; print('DRY-RUN OK: n=', tot, 'err=', err, 'routes=', list(r['summary']['routes'].keys()))"</automated>
  </verify>
  <resume-signal>Type "DRY-RUN GO — n=NNN; err=N; routes={chat:X,embed:Y,stt:Z}; upstreams seen={tier-0:..., tier-1:...}; primary Ready; spend cap acknowledged" OR "NO-GO — <reason>".</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <id>11-06-04</id>
  <title>Task 4 [reviews MEDIUM #2]: 30-min sustained replay + per-route + per-upstream metric capture + external panic gate</title>
  <read_first>
    - 11-CONTEXT.md D-03 + D-04 (30 min sustained + SLO v1.0 thresholds verbatim)
    - 11-RESEARCH.md Architecture Patterns System Architecture Diagram (load test traffic path)
    - scripts/integration-smoke/load-replay.py (the runner from 11-01)
    - scripts/integration-smoke/load-replay-report-schema.json gates + gates_external_inputs blocks
  </read_first>
  <what-built>
    The 30-minute sustained load replay against prod gateway with per-route + per-upstream P50/P95/P99 distribution captured for SLO attribution (reviews MEDIUM #2). Operator launches load-replay.py from ops-claude (env IFIX_KEY_* already set from task 11-06-02), monitors gateway health + Vast pod metrics + Sentry breadcrumbs in parallel terminal, and verifies the run completes without breakers tripping. After the run, operator performs the external Sentry+log observation to flip `gates.zero_5xx_panic` (the engine writes null per 11-01 design — recoverers don't leak panic strings so body-content inference is forbidden). This plan is baseline UAT, NOT chaos — chaos lives in 11-07/11-08.
  </what-built>
  <how-to-verify>
    Step 1 — Launch in tmux session "load-test":
    `python scripts/integration-smoke/load-replay.py --gateway-url https://ai-gateway.converse-ai.app --fixture /tmp/load-fixture.jsonl --duration 1800 --max-concurrency 50 --out .planning/phases/11-prod-hardening/load-report-$(date +%Y-%m-%d).json`
    Tenant Authorization is built at request time from env IFIX_KEY_${TENANT_SLUG^^}. NO keys in argv (reviews HIGH #5).

    Step 2 — In parallel terminal: `watch -n 30 'ssh n8n-ia-vm "docker exec ifix-ai-gateway /gatewayctl primary state; curl -s localhost:8080/v1/health/upstreams | jq"'` — verify primary stays Ready and no breaker OPEN unexpectedly.

    Step 3 — In parallel terminal: monitor Vast spend at https://cloud.vast.ai/instances — abort if hourly burn rate × test duration would exceed $1 (alert) or $3 (operator-decide) or $5 (HARD ABORT per Phase 11 cap).

    Step 4 — After 30 min: load-replay.py prints summary to stdout AND writes load-report-YYYY-MM-DD.json. Inspect per-route + per-upstream distribution (reviews MEDIUM #2):
    a. `jq '.gates' .planning/phases/11-prod-hardening/load-report-*.json` — 4 latency/error gates should be true; `zero_5xx_panic` is null until step 5; `all_passed` is null until step 5.
    b. Per-route P50/P95/P99: `jq '.summary.routes | to_entries | map({route: .key, n: .value.n, p50_ms: .value.p50_ms, p95_ms: .value.p95_ms, p99_ms: .value.p99_ms})' …`
    c. **Per-route per-upstream distribution (reviews MEDIUM #2 — tier attribution):** `jq '.summary.routes | to_entries | map({route: .key, upstreams: .value.upstreams})' …` MUST show, for `/v1/chat/completions`, both `local-llm` (tier-0) AND `openrouter-chat` (tier-1) as upstream labels with their per-upstream count. If only one tier appears, the SLO eval cannot attribute breaches to a specific tier — document as a known limitation (Phase 12 candidate: forced tier mix during baseline).
    d. Acceptance assertion: `jq '.summary.routes."/v1/chat/completions".upstreams | keys' …` returns at minimum `["local-llm"]` (tier-0 alone is acceptable if Vast was healthy throughout; tier-1 only appears if breaker flapped — operator notes which path was exercised).

    Step 5 — EXTERNAL panic gate (reviews LOW #4 from 11-01 cycle, applied here): operator queries Sentry for project ifix-ai-gateway-prod (https://ifix.sentry.io/issues/?project=4511455942017024) filtered to the run window `started_at`→`finished_at`. AND counts gateway log lines: `ssh n8n-ia-vm 'docker logs ifix-ai-gateway --since <started_at_iso> --until <finished_at_iso> 2>&1 | grep -cE "^panic:|goroutine [0-9]+ \[running\]"'`. If BOTH return 0 → flip `gates.zero_5xx_panic = true` AND `gates_external_inputs.verified_at = <ISO timestamp>` AND recompute `gates.all_passed = AND of 5 gates`. Use `jq` to mutate the report file then re-validate against the schema. If either count > 0 → set `zero_5xx_panic = false`, `all_passed = false`, and document the panic detail in EVIDENCE.md (panic content is OK in EVIDENCE.md only if redacted to error class — no goroutine bytes, no raw stack frames with PII).

    Step 6 — If any gate is false: operator decides — re-run after fix OR document as a Phase 11 gap in 11-06-EVIDENCE.md (re-route to /gsd:plan-phase 11 --gaps if blocker).

    Step 7 — Capture total Vast spend from dashboard (https://cloud.vast.ai/billing). Record dollar amount + instance id + hours billed + per-hour rate for EVIDENCE.md.
  </how-to-verify>
  <verify>
    <automated>ls -1 .planning/phases/11-prod-hardening/load-report-*.json 2>/dev/null | head -1 | xargs -I{} python -c "import json, jsonschema, sys; r=json.load(open('{}')); s=json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); jsonschema.Draft202012Validator(s).validate(r); assert '/v1/chat/completions' in r['summary']['routes'], 'chat route missing'; upstreams=r['summary']['routes']['/v1/chat/completions'].get('upstreams', {}); assert len(upstreams) >= 1, 'no upstream attribution for chat route'; print('upstreams seen for chat:', list(upstreams.keys())); print('gates:', r['gates'])"</automated>
  </verify>
  <resume-signal>Type "load-test PASS — all_passed=true; spend=$X.YY; report at load-report-YYYY-MM-DD.json; upstreams seen per chat route: [<labels>]; zero_5xx_panic verified via Sentry+log" OR describe gate failures.</resume-signal>
</task>

<task type="auto">
  <id>11-06-05</id>
  <title>Task 5 [reviews HIGH #5]: Author EVIDENCE.md + evidence sanitization audit + commit (Consensus Top Action #3)</title>
  <files>.planning/phases/11-prod-hardening/load-report-YYYY-MM-DD.json, .planning/phases/11-prod-hardening/11-06-EVIDENCE.md</files>
  <read_first>
    - Output of task 11-06-04 (load-report-YYYY-MM-DD.json with per-upstream distribution + 6 gates)
    - 11-RESEARCH.md Validation Architecture (sampling rate guidance)
    - 11-VALIDATION.md task 11-06-XX row (if present)
    - 11-RESEARCH.md §Package Legitimacy Audit (cross-cutting evidence file conventions)
  </read_first>
  <action>
    Step 1 — Copy load-report from /tmp or current working directory to `.planning/phases/11-prod-hardening/load-report-$(date +%Y-%m-%d).json` (if not already written there).

    Step 2 — Author `.planning/phases/11-prod-hardening/11-06-EVIDENCE.md` with the following sections (Portuguese OK for operator notes; English required for the audit attestation block — runbook readers may be jurídico/external):

    - **Header:** `# Phase 11 PRD-01 Load-Test Evidence`
    - **Metadata:** date YYYY-MM-DD, operator name, ops-claude tmux session id, fixture path + row count, gateway target URL.
    - **11-01 Deliverables Preflight (task 11-06-01):** 5 sub-checks PASS/FAIL with grep/help excerpts.
    - **Fixture Validation (task 11-06-02):** row count, route-mix distribution (jq output), sensitive-row count (must be 0), tenant_slugs found in fixture (NAME list only — no key values), missing-env-var check result.
    - **DRY-RUN Summary (task 11-06-03):** timestamp, duration, total_requests, error_count, error rate %, routes seen, upstream labels observed, GO/NO-GO decision + rationale.
    - **30-min Run Summary (task 11-06-04):** start_ts UTC, end_ts UTC, total_requests, error_count.
    - **Per-route metrics with upstream distribution (reviews MEDIUM #2 tier attribution):**

      | Route | n | p50_ms | p95_ms | p99_ms | upstreams (label: count) |
      |-------|---|--------|--------|--------|--------------------------|
      | /v1/chat/completions | … | … | … | … | local-llm: N, openrouter-chat: M |
      | /v1/embeddings | … | … | … | … | local-embed: N, openai-embed: M |
      | /v1/audio/transcriptions | … | … | … | … | local-stt: N, openai-whisper: M |

      If a tier is absent for any route, note in the row: "(tier-1 not exercised — primary healthy throughout)" or similar attribution.

    - **Gates verdict (D-04 5 latency/error + 1 external panic = 6 booleans):**
      - p95_chat_ms_le_5000: …
      - p95_embed_ms_le_1000: …
      - p95_stt_ms_le_10000: …
      - error_rate_lt_1pct: …
      - zero_5xx_panic: … (Sentry URL + log query attestation per gates_external_inputs; `verified_at` timestamp)
      - all_passed: …
    - **Vast spend:** total $USD; instance id used; hours billed; per-hour rate.
    - **Deviations / pitfalls hit:** any unexpected breaker flap, retries triggered, host-side anomaly. Reference RUNBOOK-INCIDENTS class 1 if primary FSM moved.
    - **Cross-ref:** load-report-YYYY-MM-DD.json (sibling file).
    - **Sign-off:** operator name + date.
    - **Cross-cutting Secret Hygiene Attestation (reviews HIGH #5 / Consensus Top Action #3):**
      ```
      ATTESTATION: This evidence file and the sibling load-report-YYYY-MM-DD.json contain
      ZERO raw API keys, ZERO Authorization header values, ZERO request bodies, ZERO response
      bodies, ZERO DSN strings, ZERO PII. All tenant references use env-var labels
      (IFIX_KEY_<TENANT>) or first-4-char prefixes. Sentry references use issue URL only.
      Log grep results redact to error class. Verified by: <operator> at <ISO timestamp>.
      ```

    Step 3 — **Evidence sanitization audit (reviews HIGH #5 — the load-bearing gate of Consensus Top Action #3).** Before staging files for commit, run ALL the following checks; ANY failure blocks the commit:

    a. Grep gate on EVIDENCE.md:
    `! grep -E 'ifix_sk_[a-z0-9]{20,}|sk-or-[a-zA-Z0-9_-]{20,}|sk-[A-Z][a-zA-Z0-9_-]{20,}|Bearer [A-Za-z0-9_]{20,}|postgres://[^[:space:]]+|password\s*[=:]\s*\S+|dsn\s*[=:]\s*postgres' .planning/phases/11-prod-hardening/11-06-EVIDENCE.md`

    b. Grep gate on load-report:
    `! grep -E 'ifix_sk_[a-z0-9]{20,}|sk-or-[a-zA-Z0-9_-]{20,}|sk-[A-Z][a-zA-Z0-9_-]{20,}|Bearer [A-Za-z0-9_]{20,}|postgres://[^[:space:]]+' .planning/phases/11-prod-hardening/load-report-*.json`

    c. jq schema-key gate on load-report — forbidden keys MUST NOT appear anywhere in the report tree. If the report has a `requests` array, verify no element has `body`, `authorization`, `headers`, `prompt_text`, or `response_text` keys:
    `python -c "import json, sys; r=json.load(open([f for f in __import__('glob').glob('.planning/phases/11-prod-hardening/load-report-*.json')][0])); forbidden={'body','authorization','headers','prompt_text','response_text','api_key','bearer','dsn','password','secret','cookie'}; def walk(o, path=''):
       if isinstance(o, dict):
           for k,v in o.items():
               if k.lower() in forbidden: print(f'FORBIDDEN key at {path}.{k}'); sys.exit(1)
               walk(v, f'{path}.{k}')
       elif isinstance(o, list):
           for i,el in enumerate(o): walk(el, f'{path}[{i}]')
    walk(r); print('schema key audit OK')"`

    d. jsonschema validation: `python -c "import json, jsonschema; r = json.load(open([f for f in __import__('glob').glob('.planning/phases/11-prod-hardening/load-report-*.json')][0])); s = json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); jsonschema.Draft202012Validator(s).validate(r); print('schema valid; all_passed=', r['gates']['all_passed'])"`

    Step 4 — Commit. `git add .planning/phases/11-prod-hardening/load-report-*.json .planning/phases/11-prod-hardening/11-06-EVIDENCE.md && git commit -m "docs(11-06): PRD-01 load-test evidence — load-report + EVIDENCE.md (per-upstream distribution + secret-hygiene audited)"`. Commit message intentionally does NOT include test verdict numbers — those live in the file body.
  </action>
  <acceptance_criteria>
    - Both files exist at the expected paths.
    - jsonschema validation passes on load-report.
    - EVIDENCE.md grep-audit (sub-step 3a) returns 0 matches.
    - load-report grep-audit (sub-step 3b) returns 0 matches.
    - load-report schema-key audit (sub-step 3c) finds no forbidden keys.
    - EVIDENCE.md contains all 11 sections (Header, Metadata, 11-01 Preflight, Fixture, DRY-RUN, 30-min, Per-route table, Gates, Vast spend, Sign-off, Attestation).
    - EVIDENCE.md contains the cross-cutting attestation block verbatim (grep "ATTESTATION" returns ≥1 match).
    - Commit succeeds.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/11-prod-hardening/load-report-$(date +%Y-%m-%d).json && test -f .planning/phases/11-prod-hardening/11-06-EVIDENCE.md && python -c "import json, jsonschema; r = json.load(open([f for f in __import__('glob').glob('.planning/phases/11-prod-hardening/load-report-*.json')][0])); s = json.load(open('scripts/integration-smoke/load-replay-report-schema.json')); jsonschema.Draft202012Validator(s).validate(r); print('schema valid; all_passed=', r['gates']['all_passed'])" && ! grep -E 'ifix_sk_[a-z0-9]{20,}|sk-or-[a-zA-Z0-9_-]{20,}|Bearer [A-Za-z0-9_]{20,}|postgres://[^[:space:]]+' .planning/phases/11-prod-hardening/11-06-EVIDENCE.md && ! grep -E 'ifix_sk_[a-z0-9]{20,}|sk-or-[a-zA-Z0-9_-]{20,}|Bearer [A-Za-z0-9_]{20,}|postgres://[^[:space:]]+' .planning/phases/11-prod-hardening/load-report-*.json && grep -E "ATTESTATION" .planning/phases/11-prod-hardening/11-06-EVIDENCE.md</automated>
  </verify>
  <done>All 8 acceptance criteria pass; both files committed; jsonschema validation passes; gates.all_passed=true OR explicit deviation noted in EVIDENCE.md with operator sign-off; no secret strings in either file; no forbidden body/header keys in load-report. Closes reviews HIGH #5 (Consensus Top Action #3 secret hygiene at evidence layer).</done>
</task>

</tasks>

<verification>
- load-report-YYYY-MM-DD.json passes jsonschema validation against scripts/integration-smoke/load-replay-report-schema.json.
- `.gates.all_passed === true` (or operator-signed deviation) AFTER external panic-gate verification.
- 11-01 deliverables preflight assertion passed BEFORE any Vast spend (task 11-06-01).
- Fixture row-count ≥1000 + all 5 route classes present (task 11-06-02).
- DRY-RUN report total_requests > 0, error rate < 5%, chat route present (task 11-06-03).
- 30-min run captured per-route AND per-upstream distribution (task 11-06-04).
- EVIDENCE.md + load-report contain ZERO raw API keys / Authorization / bodies / DSNs / PII (task 11-06-05 grep + schema-key audit).
- Vast spend within $5 absolute cap.
- Primary FSM Ready throughout.
</verification>

<success_criteria>
- PRD-01 SLO v1.0 D-04 6 gates green on 30-min sustained replay (5 measured + 1 external observation).
- 11-01 deliverables preflight + fixture validation + DRY-RUN preceded the full run.
- Per-route per-upstream distribution captured (tier-0/tier-1 attribution for SLO breach diagnostics).
- Evidence committed for later runbook ingestion (11-09 uses this as canonical example).
- Spend within budget. Zero raw keys / bodies / headers in evidence (Consensus Top Action #3 honored).
</success_criteria>

<cross_cutting_truths>
- Truth 1: ZERO new server-side runtime dependencies for execution. (Dashboard 2FA UI adds up to 4 packages scoped to 11-02 only.)
- Truth 4: tenant keys in env vars IFIX_KEY_* — NOT in CLI args, NOT in JSONL fixture, NOT in evidence (Pattern A discipline + reviews HIGH #5).
- Truth 5: This plan is autonomous=false (operator runs + observes tasks 11-06-03 and 11-06-04; tasks 11-06-01, 11-06-02, 11-06-05 are autonomous).
- Truth 8 (cross-cutting, load-bearing): NO raw API keys, headers, request bodies, response bodies, DSNs, or PII in committed plan instructions, fixtures, schemas, evidence, or load reports — grep + jq enforced at task 11-06-05 commit gate.
- Truth 9 (D-B6 invariant inherited from 11-01): STT replay uses the committed fixture OGG file; audio bytes are NEVER reconstructed from DB.
- Spend cap $5 absolute Phase 11 — operator aborts if exceeded.
</cross_cutting_truths>

<output>
Create .planning/phases/11-prod-hardening/11-06-SUMMARY.md when done with: paths committed, jsonschema validation result, gates verdict, spend total, per-route + per-upstream tally, link to 11-06-EVIDENCE.md, explicit `closes reviews` tags (HIGH #5, MEDIUM #1, MEDIUM #2, LOW #3, LOW #4) for traceability into the next review cycle.
</output>
