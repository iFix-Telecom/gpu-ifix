---
phase: 17-dashboard-pod-config-control-owner-controla-todas-as-configs
plan: 06
type: execute
wave: 6
depends_on: ["17-05"]
files_modified:
  - dashboard/src/components/ui/switch.tsx
  - dashboard/src/components/app-sidebar.tsx
  - dashboard/src/app/(dashboard)/operacao/config/page.tsx
  - dashboard/src/components/pod-config-controls.tsx
  - dashboard/src/components/pod-config-live-panel.tsx
autonomous: true
requirements: [POD-CFG-08, POD-CFG-09, POD-CFG-12, POD-CFG-13, POD-CFG-14, POD-CFG-15]
user_setup: []
must_haves:
  truths:
    - "The /operacao/config page shows four surfaces: live provisioning panel (top), 16-hot-field editor, bounds editor, structural read-only display"
    - "The RSC page fetches the CURRENT pod config server-side via fetchPodConfig() and passes currentValues (16 hot fields) + bounds (9 min/max pairs) as props into the controls island — so the editor renders live DB values, not stale boot env (the island has no other data source)"
    - "Owner sees per-field pencil edit affordances; operator sees identical values with NO edit controls (cosmetic gate; server action re-checks requireOwner)"
    - "Dangerous edits (cap-down, schedule-narrow-with-pod-up, Disabled=true, empty days, restrictive allowlist) open a one-click alert-dialog with the SPECIFIC pt-BR impact string from UI-SPEC; NO type-to-confirm"
    - "The bounds editor (Campo|Mín|Máx table) is owner-editable; operator read-only"
    - "The 19 structural fields render read-only (weights keys/SHA in truncated mono); NO edit affordance, NO restart button"
    - "The live panel polls fetchPrimaryLifecycle every 10s, reusing fsm.ts tiers + StaleIndicator + skeleton/error states"
    - "Sidebar gains a 'Config do pod' entry (SlidersHorizontal) visible to both roles"
  artifacts:
    - path: "dashboard/src/app/(dashboard)/operacao/config/page.tsx"
      provides: "owner-aware config page hosting the four surfaces; fetches current pod config server-side"
      contains: "fetchPodConfig"
    - path: "dashboard/src/components/pod-config-controls.tsx"
      provides: "hot-field editor + bounds editor + dangerous confirms"
      contains: "updatePodConfig"
    - path: "dashboard/src/components/pod-config-live-panel.tsx"
      provides: "live provisioning panel (10s poll)"
      contains: "fetchPrimaryLifecycle"
  key_links:
    - from: "dashboard/src/components/pod-config-controls.tsx"
      to: "updatePodConfig / updatePodConfigBound"
      via: "server action call on save"
      pattern: "updatePodConfig"
    - from: "dashboard/src/components/pod-config-live-panel.tsx"
      to: "fetchPrimaryLifecycle"
      via: "React Query refetchInterval 10000"
      pattern: "refetchInterval"
    - from: "dashboard/src/components/app-sidebar.tsx"
      to: "/operacao/config"
      via: "nav entry"
      pattern: "/operacao/config"
---

<objective>
Ship the owner-aware `/operacao/config` page with the four UI surfaces from 17-UI-SPEC.md:
(A) the 16-hot-field editor with per-field inline edit + dangerous-action confirms (D-04),
(B) the owner-editable bounds table (D-03), (C) the live provisioning panel polling
fetchPrimaryLifecycle (D-05), (D) the 19 structural fields read-only (D-01). Operator sees
everything read-only; owner gets edit affordances; the server action (Plan 17-05) is the
authoritative gate. One net-new shadcn install: `switch`.

Purpose: the daily-pain fix surfaces in the UI — owner edits blocklist/cap/schedule in seconds,
diagnoses the bad-host flap (2026-06-29) via the live panel, no SSH+sed.
Output: page + controls island + live panel + switch + sidebar nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dashboard-pod-config-control-owner-controla-todas-as-configs/17-UI-SPEC.md
@.planning/phases/17-dashboard-pod-config-control-owner-controla-todas-as-configs/17-PATTERNS.md
@.planning/phases/17-dashboard-pod-config-control-owner-controla-todas-as-configs/17-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: switch install + sidebar nav + page shell (owner-gate) + live panel (Surface C) + structural display (Surface D)</name>
  <files>dashboard/src/components/ui/switch.tsx, dashboard/src/components/app-sidebar.tsx, dashboard/src/app/(dashboard)/operacao/config/page.tsx, dashboard/src/components/pod-config-live-panel.tsx</files>
  <read_first>
    - dashboard/src/app/(dashboard)/operacao/page.tsx (FULL — the live-panel React Query + refetchInterval 10000 + StaleIndicator + skeleton/error idiom + layout shell flex-col gap-8 + h1 28/600; PATTERNS.md lines 280-287)
    - dashboard/src/components/operacao-fsm-panel.tsx (Field idiom lines 78-87 + primaryStateClass/primaryStateLabel FSM badge classes — reuse verbatim; PATTERNS.md lines 294-310)
    - dashboard/src/lib/viewer.ts (getViewerRole — owner cosmetic gate; PATTERNS.md line 284)
    - dashboard/src/lib/gateway.ts (fetchPrimaryLifecycle + PrimaryLifecycleResponse for the live panel; fetchPodConfig + PodConfigResponse for the current config values + bounds — Plan 17-05)
    - dashboard/src/components/app-sidebar.tsx:36-41 (nav array; PATTERNS.md lines 348-352)
    - 17-UI-SPEC.md §Live provisioning panel, §Structural read-only display, §Page placement, §Net-new shadcn blocks (switch slopcheck)
  </read_first>
  <action>
    Slopcheck then `npx shadcn add switch` (official registry — the ONE justified net-new primitive, UI-SPEC §Net-new blocks; record
    the slopcheck result in the SUMMARY). Add the sidebar nav entry after "Operação": `{ href:"/operacao/config", label:"Config do pod",
    icon: SlidersHorizontal }` (visible to both roles). Create the RSC page `(dashboard)/operacao/config/page.tsx`: layout shell
    `flex flex-col gap-8`, h1 "Config do pod" (28/600) + the header hint copy from UI-SPEC. Read `getViewerRole` AND
    `await fetchPodConfig()` server-side (Plan 17-05) to get the CURRENT config values + bounds. Pass `isOwner` + `currentValues`
    (the 16 hot fields from `config`) + `bounds` (the 9 min/max pairs) as props into the controls island (Surfaces A+B, Task 2/3 —
    the island has no other data source) and the structural read-only values into Surface D. Order: Surface C (live panel) at top,
    then the controls island, then Surface D.
    Create `pod-config-live-panel.tsx` (Surface C) reusing the operacao poll idiom: React Query `refetchInterval: 10000` over
    `fetchPrimaryLifecycle`, FSM state Badge via `primaryStateClass`/`primaryStateLabel` (reuse, do NOT redefine), the OPEN lifecycle
    event trail as a vertical step list (tier-colored dot + 14px label + 12px tabular-nums timestamp from the events jsonb), +
    StaleIndicator, + skeleton/empty/error states with the exact pt-BR copy from UI-SPEC. Build Surface D (structural read-only) as a
    muted Field grid of the 19 structural values (weights keys/SHA → `font-mono text-[12px] text-muted-foreground` truncated w/ title),
    footnote "Alterado via redeploy/env — não editável aqui." NO edit affordance, NO restart button anywhere. Read-only for BOTH roles.
  </action>
  <acceptance_criteria>
    - `dashboard/src/components/ui/switch.tsx` exists (shadcn switch installed)
    - `grep -q "/operacao/config" dashboard/src/components/app-sidebar.tsx`
    - Page reads getViewerRole, fetches current config, and renders Surface C + D: `grep -q "getViewerRole" dashboard/src/app/(dashboard)/operacao/config/page.tsx` and `grep -q "fetchPodConfig" dashboard/src/app/(dashboard)/operacao/config/page.tsx`
    - Live panel polls: `grep -q "refetchInterval" dashboard/src/components/pod-config-live-panel.tsx` and reuses primaryStateClass (no redefinition)
    - NO restart affordance: `! grep -qi "restart\|reiniciar" dashboard/src/components/pod-config-live-panel.tsx dashboard/src/app/(dashboard)/operacao/config/page.tsx`
    - `cd dashboard && bun run build` succeeds (or `next build` / typecheck passes for the new route)
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run build && grep -q "/operacao/config" src/components/app-sidebar.tsx && grep -q "fetchPodConfig" "src/app/(dashboard)/operacao/config/page.tsx" && ! grep -qi "reiniciar\|restart" src/components/pod-config-live-panel.tsx</automated>
  </verify>
  <done>Page shell + owner-gate + live panel + structural read-only shipped; switch installed; nav added.</done>
</task>

<task type="auto">
  <name>Task 2: Hot-field editor (Surface A) + dangerous-action confirms (D-04)</name>
  <files>dashboard/src/components/pod-config-controls.tsx</files>
  <read_first>
    - dashboard/src/app/settings/operadores/operator-controls.tsx (FULL — owner edit affordances: alert-dialog imports/usage, sonner, inline 14×14 Spinner, handleSubmit pattern; PATTERNS.md lines 290-314)
    - dashboard/src/components/operacao-fsm-panel.tsx (Field idiom for read-only value rendering)
    - dashboard/src/lib/admin-actions.ts (updatePodConfig signature — Plan 17-05)
    - dashboard/src/components/ui/switch.tsx (installed Task 1 — for reject_private_ip + Disabled)
    - 17-UI-SPEC.md §Config HOT editor, §Copywriting Contract (field labels, input formats, inline errors, success toasts), §Dangerous confirmations (the 5 specific impact strings + confirm button labels)
  </read_first>
  <action>
    Create `pod-config-controls.tsx` (client island, receives `isOwner` + `currentValues` (the 16 hot fields) + `bounds` (9 min/max pairs) as props — both fetched server-side by the page via fetchPodConfig, Task 1). Render the 16 hot
    fields in three grouped cards (Seleção de oferta · Orçamentos e timeouts · Agenda) using the Field idiom for read-only display.
    For owner: each field trails a neutral `Pencil` → swaps to its typed control + Salvar(primary)/Cancelar(ghost) inline (per-field
    edit = one server action = one audit row = one toast). Controls: CSV Input (blocklist/allowlist), number Input (caps/budgets/hours/
    lead/grace, with unit suffix), `switch` (reject_private_ip + Disabled), 7 Day toggle Buttons. On Salvar call
    `updatePodConfig({field, value, oldValue})`; success → sonner toast (use the "vale a partir do próximo provisionamento" variant for
    blocklist/allowlist/caps/host_id/reject/coldstart/port-bind, plain variant otherwise — UI-SPEC success toasts); error → inline
    12px destructive error with the exact UI-SPEC copy. For the 5 DANGEROUS actions (cap-down, schedule-narrow-while-pod-up,
    Disabled=true, empty Days, allowlist-excludes-all-known) open an `alert-dialog` carrying the SPECIFIC impact string + the named
    confirm button (UI-SPEC §Dangerous confirmations), default-focus Cancelar, destructive confirm, pending=spinner; commit only on
    confirm. NO type-to-confirm. Operator: no pencils/switches/Salvar — values render read-only. Reuse the 14×14 Spinner verbatim.
  </action>
  <acceptance_criteria>
    - All 16 hot field labels from UI-SPEC present; `grep -q "updatePodConfig" dashboard/src/components/pod-config-controls.tsx`
    - alert-dialog used for dangerous actions with specific strings: `grep -q "drenar o pod" dashboard/src/components/pod-config-controls.tsx` (schedule-narrow impact) and `grep -q "abaixo do mercado" pod-config-controls.tsx` (cap-down)
    - NO type-to-confirm: `! grep -qi "digite\|type to confirm\|confirmar digitando" dashboard/src/components/pod-config-controls.tsx`
    - Operator path renders no edit controls (gated on isOwner) — verify the pencil/Salvar render is conditioned on isOwner
    - `cd dashboard && bun run build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run build && grep -q "updatePodConfig" src/components/pod-config-controls.tsx && grep -q "drenar o pod" src/components/pod-config-controls.tsx && ! grep -qi "digite" src/components/pod-config-controls.tsx</automated>
  </verify>
  <done>16-hot-field editor with per-field inline edit + the 5 dangerous one-click confirms; operator read-only.</done>
</task>

<task type="auto">
  <name>Task 3: Bounds editor (Surface B)</name>
  <files>dashboard/src/components/pod-config-controls.tsx</files>
  <read_first>
    - dashboard/src/components/pod-config-controls.tsx (the island from Task 2 — extend it with the bounds table)
    - dashboard/src/components/ui/table.tsx (present — Campo|Mín|Máx)
    - dashboard/src/lib/admin-actions.ts (updatePodConfigBound — Plan 17-05)
    - 17-UI-SPEC.md §Bounds editor (table, per-cell inline edit, min<max server-enforced) + inline error copy (mín ≥ máx, DownHour==UpHour)
  </read_first>
  <action>
    Extend `pod-config-controls.tsx` with a "Limites de validação" card containing a `table` (Campo | Mín | Máx) for the 9 numeric
    bounded fields (caps, coldstart, port-bind, cooldown, budget, UpHour, DownHour, grace, lead). Non-bounded fields show `—`.
    For owner: Mín/Máx cells carry an inline Pencil → number Input + Salvar/Cancelar (same pending/success/error states as Surface A).
    On Salvar call `updatePodConfigBound({field, value, oldValue})`; success → `Limite de {campo} atualizado.` toast; error → inline
    destructive copy ("O mínimo precisa ser menor que o máximo." for min≥max). Operator: cells render read-only, no pencils. Reuse the
    same Spinner + edit-submit pattern from Task 2.
  </action>
  <acceptance_criteria>
    - Bounds table renders the 9 numeric fields with Mín/Máx; `grep -q "updatePodConfigBound" dashboard/src/components/pod-config-controls.tsx`
    - min≥max inline error copy present: `grep -q "menor que o máximo" dashboard/src/components/pod-config-controls.tsx`
    - Owner-only edit (cells gated on isOwner); operator sees read-only values
    - `cd dashboard && bun run build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && bun run build && grep -q "updatePodConfigBound" src/components/pod-config-controls.tsx && grep -q "menor que o máximo" src/components/pod-config-controls.tsx</automated>
  </verify>
  <done>Owner-editable bounds table wired to updatePodConfigBound; operator read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (operator) → edit UI | UI hiding is cosmetic; the server action (Plan 17-05) is the authoritative requireOwner gate. |
| displayed values → browser | Only pod POLICY knobs + public-grade weights SHAs render; never secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-19 | Elevation of Privilege | operator finds a hidden edit affordance | mitigate | UI gate on isOwner is cosmetic; every Salvar calls a server action that re-checks requireOwner (Plan 17-05) — UI hiding is not the security boundary |
| T-17-20 | Information Disclosure | secrets rendered in the structural panel | mitigate | Only the 19 structural POLICY fields + public weights SHAs (truncated mono) display; VAST/MINIO/DSN keys never rendered (UI-SPEC §Privacy/Redaction) |
| T-17-21 | Tampering (fat-finger) | accidental prod-bricking edit | mitigate | One-click alert-dialog with a specific impact string on the 5 dangerous actions before commit (D-04); confirm is UX, security is server-side |
| T-17-22 | Supply chain | shadcn `switch` install | mitigate | Slopcheck recorded; official shadcn registry only, no third-party registry (UI-SPEC §Registry Safety); blocking review if the registry resolves off-official |
| T-17-SC | Tampering | npm install (switch primitive) | mitigate | `npx shadcn add switch` is the only install; from the official registry (components.json registries:{}); record install evidence in SUMMARY |
</threat_model>

<verification>
- `cd dashboard && bun run build` succeeds (all four surfaces typecheck + compile)
- `cd dashboard && bun run lint` clean for the new files
- Phase-gate manual UAT (VALIDATION.md): owner edits blocklist in the dashboard → prod reconciler skips the host on next provision; live panel shows the trail.
</verification>

<success_criteria>
The /operacao/config page ships all four surfaces: live panel (10s poll), 16-hot-field editor with
dangerous one-click confirms, owner-editable bounds table, structural read-only display. Operator is
read-only everywhere; owner edits route through the audited server action. No restart button, no
structural-edit form, no type-to-confirm.
</success_criteria>

<output>
Create `.planning/phases/17-dashboard-pod-config-control-owner-controla-todas-as-configs/17-06-SUMMARY.md` when done
</output>
