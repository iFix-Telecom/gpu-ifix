---
phase: 17-dashboard-pod-config-control-owner-controla-todas-as-configs
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - gateway/internal/podconfig/loader.go
  - gateway/internal/podconfig/loader_test.go
  - gateway/internal/podconfig/listen.go
  - gateway/internal/podconfig/types.go
  - gateway/internal/obs/metrics.go
autonomous: true
requirements: [POD-CFG-03]
must_haves:
  truths:
    - "podconfig.Loader keeps an atomic.Pointer[snapshot] of the 16 hot fields + bounds + a pre-parsed ScheduleRule"
    - "Refresh swaps the snapshot on success; on query error it KEEPS the last-good snapshot and increments an error metric (never serves zero-config)"
    - "Refresh re-parses the ScheduleRule from the snapshot using the STRUCTURAL timezone (hot UpHour/DownHour/Days/Lead/Grace/Disabled)"
    - "ListenAndReload listens on the pod_config_changed channel via a dedicated pgx.Conn and calls Refresh on each NOTIFY, surviving transient DB errors"
    - "Load() is lock-free (single atomic.Pointer load), zero synchronous DB call on the hot path"
  artifacts:
    - path: "gateway/internal/podconfig/loader.go"
      provides: "in-memory snapshot loader mirroring upstreams.Loader"
      exports: ["Loader", "NewLoader"]
      min_lines: 80
    - path: "gateway/internal/podconfig/listen.go"
      provides: "LISTEN/NOTIFY reload goroutine on pod_config_changed"
      exports: ["ListenAndReload"]
    - path: "gateway/internal/podconfig/loader_test.go"
      provides: "snapshot refresh + last-good-on-error + schedule reparse coverage"
  key_links:
    - from: "gateway/internal/podconfig/loader.go"
      to: "gen.GetPodConfig"
      via: "loaderQueries interface"
      pattern: "GetPodConfig"
    - from: "gateway/internal/podconfig/listen.go"
      to: "pod_config_changed"
      via: "pgxlisten Handle"
      pattern: "pod_config_changed"
---

<objective>
Build the `podconfig` package — the hot-reload engine — by mirroring `upstreams.Loader` +
`upstreams.listen.go` verbatim (PATTERNS.md §loader.go / §listen.go). It serves the 16 hot
fields + owner-editable bounds + a pre-parsed schedule rule from an atomic snapshot, refreshed
on `pod_config_changed` NOTIFY, with the load-bearing last-good-on-error invariant (RESEARCH
Pitfall 1 — provisioning must NEVER block or read zero-config on a DB hiccup).

Purpose: the reconciler (Plan 17-03) reads `loader.Load()` on the hot path instead of `r.cfg`.
Output: `podconfig` package (loader, listen, types) + a reload metric + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dashboard-pod-config-control-owner-controla-todas-as-configs/17-PATTERNS.md
@.planning/phases/17-dashboard-pod-config-control-owner-controla-todas-as-configs/17-RESEARCH.md
@.planning/phases/17-dashboard-pod-config-control-owner-controla-todas-as-configs/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: podconfig.Loader + types + reload metric (last-good-on-error)</name>
  <files>gateway/internal/podconfig/loader.go, gateway/internal/podconfig/types.go, gateway/internal/podconfig/loader_test.go, gateway/internal/obs/metrics.go</files>
  <behavior>
    - Refresh with a healthy query: snapshot swapped, Load() returns the new 16 hot values + bounds; "ok" metric incremented.
    - Refresh with a query error: snapshot UNCHANGED (Load() still returns the previous good values); error returned; "error" metric incremented; NO panic, NO zero-value snapshot served.
    - Refresh re-parses ScheduleRule from snapshot; a snapshot whose schedule yields a bad rule returns error and keeps last-good (never swaps in a broken rule).
    - Initial NewLoader Refresh failure is fatal (caller os.Exit) — seed in Plan 17-03 guarantees a row exists.
    - Load() returns a stable pointer; concurrent Load() during a Refresh never tears (atomic.Pointer).
  </behavior>
  <read_first>
    - gateway/internal/upstreams/loader.go (FULL — copy structure: snapshot struct, loaderQueries interface, NewLoader + initial Refresh fail-fast, Refresh last-good-on-error at ~loader.go:108-199, Load/Get accessors; PATTERNS.md §loader.go lines 39-104. DROP the tier0Override machinery — pod_config has no override layer.)
    - gateway/internal/upstreams/loader_test.go (FULL — test harness with a fake loaderQueries; mirror its table-driven cases for ok/error/last-good)
    - gateway/internal/obs/metrics.go (find UpstreamsReloadTotal at ~loader.go:111 call site; add a sibling PodConfigReloadTotal counter with label "result" {ok,error})
    - gateway/internal/db/gen/pod_config.sql.go (gen.PodConfig row type — produced by 17-01; rowToPodConfig + rowToBounds map it into the snapshot)
    - 17-RESEARCH.md §Reconciler tick loop (safe-fallback rule, lines 163-170) + Pitfall 1
  </read_first>
  <action>
    Create package `podconfig`. In types.go define `PodConfig` (the 16 hot fields, typed per the interfaces block in 17-01),
    `PodConfigBounds` (min/max per the 9 numeric fields), and reuse `primary.ScheduleRule` shape (or a local mirror) for the
    pre-parsed rule. In loader.go define the unexported `snapshot{ cfg PodConfig; rule ScheduleRule; bounds PodConfigBounds }`,
    a `loaderQueries interface { GetPodConfig(ctx) (gen.PodConfig, error) }`, and `Loader{ pool, q, snap atomic.Pointer[snapshot], log }`.
    Implement `NewLoader(ctx, pool, log)` doing an initial Refresh that returns error on failure (caller fatals). Implement
    `Refresh(ctx)`: GetPodConfig → on error increment `obs.PodConfigReloadTotal{result="error"}` and RETURN without swapping
    (keep last-good — THE critical invariant, PATTERNS.md lines 81-101); on success build snapshot via rowToPodConfig/rowToBounds,
    re-parse the schedule rule (see Plan 17-03's ParseScheduleFromSnapshot, or inline parse here using the STRUCTURAL timezone passed in),
    and on parse error also keep last-good + error metric; on full success `snap.Store(s)` + "ok" metric + info log.
    Provide `Load() *snapshot` and convenience accessors `Cfg()`, `Rule()`, `Bounds()` doing a single `snap.Load()`.
    Add `PodConfigReloadTotal` to obs/metrics.go mirroring UpstreamsReloadTotal registration.
    NOTE: the structural timezone is NOT in pod_config (D-02). NewLoader takes the tz string (from config.Config) so the rule
    re-parse uses it; the loader never changes tz at runtime (D-03a — fail-fast LoadLocation can never fire post-boot).
    Write loader_test.go covering the five behaviors with a fake loaderQueries (no DB).
  </action>
  <acceptance_criteria>
    - `gateway/internal/podconfig/loader.go` exports Loader + NewLoader; `grep -q "atomic.Pointer" gateway/internal/podconfig/loader.go`
    - Last-good invariant present: the error branch in Refresh does NOT call snap.Store (verify by test, not just grep)
    - `obs.PodConfigReloadTotal` registered in gateway/internal/obs/metrics.go
    - `cd gateway && go test ./internal/podconfig/ -run Loader` passes including the last-good-on-error case
    - `cd gateway && gofmt -l internal/podconfig/` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>cd gateway && gofmt -l internal/podconfig/ && go test ./internal/podconfig/ -run Loader</automated>
  </verify>
  <done>Loader serves an atomic snapshot, keeps last-good on error, reload metric wired; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: podconfig.ListenAndReload (pod_config_changed channel)</name>
  <files>gateway/internal/podconfig/listen.go</files>
  <read_first>
    - gateway/internal/upstreams/listen.go (FULL, ~69 lines — copy the entire file structure; PATTERNS.md §listen.go lines 108-135. Keep: dedicated pgx.Conn via Connect, ReconnectDelay 5s, "return nil on handler error to keep the listener alive". Change channel upstreams_changed → pod_config_changed + doc comments.)
    - gateway/internal/podconfig/loader.go (the Refresh this listener calls — Task 1)
  </read_first>
  <action>
    Create listen.go with `ListenAndReload(ctx, dsn string, loader *Loader, onReload func(), log *slog.Logger) error`
    mirroring upstreams.ListenAndReload exactly. Use `pgxlisten.Listener` with `Connect` opening a dedicated `pgx.Conn`
    (NOT the pool), `ReconnectDelay: 5*time.Second`, and `Handle("pod_config_changed", ...)` whose handler calls
    `loader.Refresh(ctx)`; on Refresh error LOG and `return nil` (keep the listener alive through a transient DB hiccup —
    PATTERNS.md line 125). Call `onReload()` if non-nil (the reconciler reads Load() live each tick so it passes nil —
    PATTERNS.md line 135). Return ctx.Err() on clean cancel; return the listener error only on unexpected exit.
  </action>
  <acceptance_criteria>
    - `gateway/internal/podconfig/listen.go` exports ListenAndReload
    - `grep -q "pod_config_changed" gateway/internal/podconfig/listen.go` and NO occurrence of "upstreams_changed"
    - Handler returns nil on Refresh error (listener survives) — `grep -q "return nil" listen.go` in the handler block
    - `cd gateway && go build ./internal/podconfig/ && gofmt -l internal/podconfig/listen.go` clean
  </acceptance_criteria>
  <verify>
    <automated>cd gateway && go build ./internal/podconfig/ && grep -c "pod_config_changed" internal/podconfig/listen.go && ! grep -q "upstreams_changed" internal/podconfig/listen.go && gofmt -l internal/podconfig/listen.go</automated>
  </verify>
  <done>ListenAndReload listens on pod_config_changed, refreshes on NOTIFY, survives transient errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB read → reconciler hot path | A transient DB failure must not propagate a zero/empty config to provisioning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Denial of Service | config-read failure starves provisioning (cap=0 / empty blocklist) | mitigate | Refresh never swaps on error — keeps last-good atomic snapshot; reconciler reads in-memory Load(), never a synchronous DB call (RESEARCH Pitfall 1; Task 1 behavior 2) |
| T-17-05 | Denial of Service | LISTEN connection drop stops hot-reload silently | mitigate | Dedicated pgx.Conn + ReconnectDelay 5s + handler returns nil on transient error (Task 2, mirrors upstreams) |
| T-17-06 | Tampering | broken schedule rule (bad parse) swapped into live snapshot | mitigate | Refresh keeps last-good if rule re-parse fails; tz is structural so LoadLocation fail-fast never fires at runtime (D-03a) |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs; pgx/pgxlisten already vendored (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd gateway && go test ./internal/podconfig/...` passes
- `cd gateway && go build ./... && gofmt -l internal/podconfig/ internal/obs/` clean
</verification>

<success_criteria>
podconfig package serves an atomic snapshot of the 16 hot fields + bounds + parsed schedule rule,
refreshed on NOTIFY, with last-good-on-error retention and a reload metric. The reconciler can now
read Load() on the hot path.
</success_criteria>

<output>
Create `.planning/phases/17-dashboard-pod-config-control-owner-controla-todas-as-configs/17-02-SUMMARY.md` when done
</output>
