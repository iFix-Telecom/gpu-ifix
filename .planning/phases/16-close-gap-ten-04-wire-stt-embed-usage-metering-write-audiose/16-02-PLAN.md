---
phase: 16-close-gap-ten-04-wire-stt-embed-usage-metering
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - gateway/cmd/gateway/main.go
  - gateway/internal/quota/counters_test.go
autonomous: true
requirements: [TEN-04, OBS-09]

must_haves:
  truths:
    - "The local embed proxy (NewEmbeddingsProxy) is constructed with the usageInterceptor"
    - "The local STT proxy (NewAudioProxy) is constructed with the usageInterceptor"
    - "The 3 STT tier-1 fallbacks (gemini, groq, openai-whisper) compose the usageInterceptor into their ModifyResponse alongside any existing one"
    - "The dynamic-override STT proxy (NewDynamicOverrideSTTProxy) is constructed with the usageInterceptor"
    - "A tenant whose usage_counters audio_seconds exceeds the daily audio-minute limit is blocked by CheckQuotaToday; same for embeds"
  artifacts:
    - path: "gateway/cmd/gateway/main.go"
      provides: "All 6 STT/embed proxy construction sites passing usageInterceptor"
      contains: "usageInterceptor"
    - path: "gateway/internal/quota/counters_test.go"
      provides: "Quota-trip test proving audio + embed counters now gate (was impossible with 0 source)"
      contains: "AudioSeconds"
  key_links:
    - from: "gateway/cmd/gateway/main.go (NewEmbeddingsProxy / NewAudioProxy / NewDynamicOverrideSTTProxy / build*STTProxy)"
      to: "proxy.UsageInterceptor.Intercept"
      via: "variadic ...ProxyResponseInterceptor → ComposeInterceptors → ModifyResponse"
      pattern: "NewEmbeddingsProxy\\(.*usageInterceptor|NewAudioProxy\\(.*usageInterceptor"
---

<objective>
Wire the producer from Plan 16-01 into the request path: pass `usageInterceptor` to the
6 STT/embed proxy construction sites in main.go that today pass ZERO usage interceptor
(only chat proxies do). Two sites already accept variadic interceptors and just need the
argument; the 3 tier-1 STT fallbacks build a raw `httputil.ReverseProxy` with their own
`ModifyResponse` (gemini flattens its envelope) and must COMPOSE the usage interceptor in
without clobbering the existing one. Then prove end-to-end that the audio + embed quota
dimensions now actually trip (previously impossible — the source counter was always 0).

Purpose: connect the metering producer to the live STT/embed traffic so usage_counters
populate and TEN-04 quotas enforce + OBS-09 /consumo stops showing 0.
Output: 6 wired construction sites + a quota-trip integration test + the grep-proof flip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-close-gap-ten-04-wire-stt-embed-usage-metering-write-audiose/16-CONTEXT.md
@.planning/phases/16-close-gap-ten-04-wire-stt-embed-usage-metering-write-audiose/16-01-PLAN.md

<interfaces>
<!-- The 6 sites + the variadic capability. Verified against real code. -->

usageInterceptor is already constructed at main.go:548 (proxy.NewUsageInterceptor(...)).
Chat proxies already pass it (:570, :652, :660). The 6 STT/embed sites do NOT.

Sites that ALREADY accept `...ProxyResponseInterceptor` (just pass the arg):
  proxy.NewEmbeddingsProxy(cfg.UpstreamEmbedURL, log)                 // main.go:576  → add usageInterceptor
  proxy.NewAudioProxy(cfg.UpstreamSTTURL, log, resolver)             // main.go:587  → add usageInterceptor
  proxy.NewDynamicOverrideSTTProxy(fn, 0, transport, resolver, log)  // main.go:686  → add usageInterceptor

Sites that build a raw httputil.ReverseProxy with their OWN ModifyResponse — these helpers
must take usageInterceptor and COMPOSE it (proxy.ComposeInterceptors already exists; the
analog is buildOpenRouterChatProxy at main.go:1551/:1575 which composes audit+toolcall+usage):
  buildGeminiSTTProxy(...)  // main.go:1618 — currently sets ModifyResponse: modifyResponse (envelope flatten)
  buildGroqWhisperProxy(...) // main.go:1647 — no ModifyResponse today
  buildOpenAIWhisperProxy(...) // main.go:1671 — no ModifyResponse today

proxy.ComposeInterceptors(ics ...ProxyResponseInterceptor) func(*http.Response) error
  — order-preserving; first arg runs first. For gemini, the envelope-flatten ModifyResponse
  must run BEFORE the usage interceptor so the usage interceptor sees the flattened
  `{"text":...}` (NOTE: gemini flattens to {"text"} and drops duration → AudioSecondsMs10
  stays 0 for gemini, which is acceptable for this plan; local Speaches + openai-whisper
  carry duration). The gemini modifyResponse is a func(*http.Response) error, not a
  ProxyResponseInterceptor — wrap it (a tiny interceptorFunc adapter) or chain it manually
  inside a single ModifyResponse closure that calls modifyResponse first then
  usageInterceptor.Intercept.

quota/counters.go CheckQuotaToday/CheckQuotaMonth + the countersQueries interface (stubbable
without Postgres — see existing counters_test.go for the fake) is where the trip-test lives.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass usageInterceptor to all 6 STT/embed proxy construction sites</name>
  <files>gateway/cmd/gateway/main.go</files>
  <read_first>
    - gateway/cmd/gateway/main.go (the 6 sites: :576, :587, :686, :1618, :1647, :1671; the usageInterceptor construction :548; the chat analog :570/:652/:660; and buildOpenRouterChatProxy :1551-:1575 which shows the compose pattern for a raw ReverseProxy builder)
    - gateway/internal/proxy/embeddings.go + audio.go + dynamic_override.go (confirm the variadic `...ProxyResponseInterceptor` + ComposeInterceptors(interceptors...) tail — these 3 take the arg directly)
    - gateway/internal/proxy/interceptor.go (ProxyResponseInterceptor interface + ComposeInterceptors — for composing into the gemini/groq/whisper raw builders)
    - gateway/internal/proxy/gemini_stt_director.go (:108-262 — the existing modifyResponse that must run BEFORE the usage interceptor)
  </read_first>
  <action>
    Thread the already-constructed `usageInterceptor` (main.go:548) into all 6 sites:

    1. main.go:576 — `proxy.NewEmbeddingsProxy(cfg.UpstreamEmbedURL, log, usageInterceptor)`.
    2. main.go:587 — `proxy.NewAudioProxy(cfg.UpstreamSTTURL, log, resolver, usageInterceptor)`.
    3. main.go:686 — `proxy.NewDynamicOverrideSTTProxy(fn, 0, transport, resolver, log, usageInterceptor)`.
    4. buildGeminiSTTProxy (:1618): add a `usageInterceptor *proxy.UsageInterceptor` param;
       set `ModifyResponse` to a closure that first calls the existing `modifyResponse(resp)`
       (envelope flatten) and, if it returns nil, then calls `usageInterceptor.Intercept(resp)`.
       Update the call site (:716) to pass usageInterceptor.
    5. buildGroqWhisperProxy (:1647) + buildOpenAIWhisperProxy (:1671): add the param and set
       `ModifyResponse: proxy.ComposeInterceptors(usageInterceptor)`; update their call sites
       (groq + :698 whisper) to pass usageInterceptor.

    Do NOT change interceptor signatures or the chat sites. Do NOT alter the WhisperAbortGuard
    / ToolCallTerminalGuard wrappers. Order matters only for gemini (flatten THEN usage).
    Verify the build — these are the wiring half of TEN-04 + OBS-09 per D-3.
  </action>
  <verify>
    <automated>cd gateway && go build ./... && go vet ./cmd/...</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go build ./...` exits 0
    - `grep -cE "NewEmbeddingsProxy\(.*usageInterceptor|NewAudioProxy\(.*usageInterceptor|NewDynamicOverrideSTTProxy\(.*usageInterceptor" gateway/cmd/gateway/main.go` >= 3
    - Each of buildGeminiSTTProxy / buildGroqWhisperProxy / buildOpenAIWhisperProxy references usageInterceptor: `grep -c usageInterceptor gateway/cmd/gateway/main.go` increased by >= 6 vs pre-edit (3 constructor args + 3 builder params/uses)
    - gemini's existing envelope-flatten modifyResponse is still invoked (grep shows modifyResponse referenced inside buildGeminiSTTProxy)
    - `cd gateway && gofmt -l cmd/gateway/main.go` prints nothing
  </acceptance_criteria>
  <done>All 6 STT/embed proxy sites construct with usageInterceptor; gemini flatten precedes usage; build + vet green.</done>
</task>

<task type="auto">
  <name>Task 2: Quota-trip integration test — audio + embed dimensions now actually block</name>
  <files>gateway/internal/quota/counters_test.go</files>
  <read_first>
    - gateway/internal/quota/counters_test.go (the existing countersQueries fake + CheckQuotaToday/Month token tests — mirror the fake-row style)
    - gateway/internal/quota/counters.go (:84,87,111,114 — the audio_seconds/60 + embeds conversions under test)
    - gateway/internal/db/gen/usage_counters.sql.go (GetUsageCountersTodayRow/MonthRow field names AudioSeconds int64, EmbedsCount int64 — what the fake returns)
  </read_first>
  <action>
    Add tests proving the audio + embed quota dimensions trip — previously impossible because
    the source counter was always 0 (this is the TEN-04 payoff). Using the existing
    countersQueries fake:

    - TestCheckQuotaTodayAudioMinutesTrips: fake GetUsageCountersToday returns AudioSeconds=120
      (=2 min), QuotaLimits{DailyAudioMinutes:2} → expect ErrQuotaExceededDailyAudioMinutes;
      and a boundary case AudioSeconds=119 (=1 min via int(119/60)) with limit 2 → expect nil.
    - TestCheckQuotaTodayEmbedsTrips: fake returns EmbedsCount=50, QuotaLimits{DailyEmbeds:50}
      → expect ErrQuotaExceededDailyEmbeds; EmbedsCount=49 → nil.
    - TestCheckQuotaMonthAudioAndEmbedsTrip: same against GetUsageCountersMonth with
      MonthlyAudioMinutes / MonthlyEmbeds → expect the monthly sentinels.

    These assert the consumer end of the chain the Plan 16-01 producer now feeds. Keep them
    Postgres-free via the interface fake.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/quota/... -run 'Quota.*(Audio|Embeds)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go test ./internal/quota/... -count=1` exits 0
    - New tests exist: `grep -cE "func Test(CheckQuotaTodayAudioMinutesTrips|CheckQuotaTodayEmbedsTrips|CheckQuotaMonthAudioAndEmbedsTrip)" gateway/internal/quota/counters_test.go` >= 3
    - Tests assert the audio/embed sentinels: `grep -nE "ErrQuotaExceededDailyAudioMinutes|ErrQuotaExceededDailyEmbeds|ErrQuotaExceededMonthlyAudioMinutes|ErrQuotaExceededMonthlyEmbeds" gateway/internal/quota/counters_test.go` returns matches
  </acceptance_criteria>
  <done>Audio + embed quota-trip tests green for daily and monthly; the dimension that could never trip now provably does.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression + grep-proof the producer flip (0 → matches)</name>
  <files>(none — verification gate; no production code change)</files>
  <read_first>
    - gateway/internal/proxy/interceptor_usage.go (Plan 16-01 producers — the grep targets)
    - gateway/cmd/gateway/main.go (the 6 wired sites)
  </read_first>
  <action>
    Run the gateway test suite (unit + the proxy/quota packages touched) to confirm no
    regression from the wiring, then capture the grep-proof that the producer sites now exist
    (the CONTEXT success criterion: grep for AudioSecondsMs10.Store/.Add + EmbedsCount.Store/.Add
    was 0, must now be >= 1). This task adds NO production code — it is the verification gate.
    If any chat-path or dispatcher test regressed, fix the wiring (do not weaken the test).
  </action>
  <verify>
    <automated>cd gateway && go build ./... && go test ./internal/proxy/... ./internal/quota/... ./internal/billing/... -count=1 && grep -rnE "AudioSecondsMs10\.(Store|Add)|EmbedsCount\.(Store|Add)" internal/ --include='*.go' | grep -v _test.go | grep -c .</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go test ./internal/proxy/... ./internal/quota/... ./internal/billing/... -count=1` exits 0
    - `grep -rnE "AudioSecondsMs10\.(Store|Add)|EmbedsCount\.(Store|Add)" gateway/internal/ --include='*.go' | grep -v _test.go` returns >= 2 lines (was 0 at phase start)
    - `cd gateway && go build ./...` exits 0 (full tree, including cmd/gateway with the 6 wired sites)
  </acceptance_criteria>
  <done>Suite green, no chat-path regression, grep-proof flipped from 0 to producer sites present. TEN-04/OBS-09 metering wired end-to-end in code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway proxy ModifyResponse chain | Composing a new interceptor must not break the existing gemini envelope-flatten or the WhisperAbortGuard contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | wiring order on gemini (usage before flatten would parse wrong shape) | mitigate | Closure calls modifyResponse (flatten) FIRST, then usageInterceptor.Intercept — asserted by build + the gemini director test staying green |
| T-16-05 | Denial of Service | added interceptor on STT/embed hot path | accept | Interceptor only buffers the already-cap-bounded JSON body (jsonBufferCap 128 KiB, Plan 16-01); same cost the chat path already pays |
| T-16-06 | Repudiation | audio/embed billing rows now produced — must carry tenant/request id | mitigate | FinalizeRequest already attributes tenant_id/api_key_id/request_id from auth ctx (interceptor_usage.go:224-232) — no change needed; covered by existing billing tests |

No npm/pip/cargo installs in this plan — package legitimacy gate N/A.
</threat_model>

<verification>
- `cd gateway && go build ./... && go test ./internal/proxy/... ./internal/quota/... ./internal/billing/... -count=1` green.
- 6 STT/embed proxy sites in main.go reference usageInterceptor.
- grep for producer sites flips from 0 to >= 2.
- Audio + embed quota-trip tests pass for daily and monthly.
</verification>

<success_criteria>
- All 6 STT/embed proxies wired with the usage interceptor (was 0).
- Per-tenant audio + embed quotas provably trip (daily + monthly).
- No chat/token billing-path regression.
- grep-proof: AudioSecondsMs10.Store/.Add + EmbedsCount.Store/.Add present (was 0).
</success_criteria>

<output>
Create `.planning/phases/16-close-gap-ten-04-wire-stt-embed-usage-metering-write-audiose/16-02-SUMMARY.md` when done.
</output>
