---
phase: 16-close-gap-ten-04-wire-stt-embed-usage-metering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/internal/auditctx/override.go
  - gateway/internal/proxy/audio_duration.go
  - gateway/internal/proxy/audio_duration_test.go
  - gateway/internal/proxy/stt_request_audio_middleware.go
  - gateway/internal/proxy/stt_request_audio_middleware_test.go
  - gateway/internal/proxy/interceptor_usage.go
  - gateway/internal/proxy/interceptor_usage_test.go
autonomous: true
requirements: [TEN-04, OBS-09]

must_haves:
  truths:
    - "An STT verbose_json response carrying a duration field causes AudioSecondsMs10 to be written non-zero from that duration"
    - "A DEFAULT-FORMAT STT response ({\"text\":\"...\"} with NO duration field) STILL causes AudioSecondsMs10 to be written non-zero, derived from the request audio (the common-case client meters, not 0)"
    - "An embed JSON response causes EmbedsCount to be written = number of data[] elements"
    - "A 90-second audio request produces DailyAudioMinutes=1 through the documented unit chain (×10 store → /10 flush → /60 minutes)"
    - "An audio-only or embed-only request (zero tokens) still enqueues a billing.Event (the all-zero guard no longer drops it)"
    - "The chat/token path still extracts tokens_in/tokens_out exactly as before (no regression)"
  artifacts:
    - path: "gateway/internal/proxy/interceptor_usage.go"
      provides: "Producer that writes AudioSecondsMs10 (response duration ELSE request-derived) + EmbedsCount in the JSON-buffer Close path, route-dispatched"
      contains: "AudioSecondsMs10.Store"
    - path: "gateway/internal/proxy/audio_duration.go"
      provides: "DeriveAudioSeconds — duration estimate from request audio (WAV RIFF header; else byte/bitrate estimate)"
      contains: "func DeriveAudioSeconds"
    - path: "gateway/internal/proxy/stt_request_audio_middleware.go"
      provides: "Middleware parsing the /v1/audio/transcriptions multipart once, stamping derived request-audio seconds into ctx"
      contains: "WithRequestAudioSeconds"
    - path: "gateway/internal/auditctx/override.go"
      provides: "Context helper WithRequestAudioSeconds / RequestAudioSecondsFrom"
      contains: "WithRequestAudioSeconds"
    - path: "gateway/internal/proxy/interceptor_usage_test.go"
      provides: "Unit tests: duration-present → minutes, NO-duration → request-derived non-zero, batch → embeds, zero-token enqueue, chat no-regression"
      contains: "EmbedsCount"
  key_links:
    - from: "gateway/internal/proxy/interceptor_usage.go (usageJSONBuffer.Close)"
      to: "billing.RequestUsage.AudioSecondsMs10 / .EmbedsCount"
      via: "response duration ELSE auditctx.RequestAudioSecondsFrom(reqCtx); atomic Store keyed by route"
      pattern: "AudioSecondsMs10\\.Store|EmbedsCount\\.Store"
    - from: "gateway/internal/proxy/stt_request_audio_middleware.go"
      to: "gateway/internal/proxy/interceptor_usage.go (Close fallback)"
      via: "auditctx.WithRequestAudioSeconds stamped pre-proxy, read at finalize"
      pattern: "WithRequestAudioSeconds|RequestAudioSecondsFrom"
---

<objective>
Make the existing `UsageInterceptor` WRITE the audio + embed usage atomics it already
reads at flush time, IMPLEMENTING LOCKED CONTEXT DECISION #2 IN FULL: audio seconds come
from the response `duration` field WHERE PRESENT, **ELSE derived from the request audio
length**. This matters because the gateway does NOT force `response_format=verbose_json`
(confirmed: `grep -rn verbose_json internal/ cmd/` finds nothing), so the DEFAULT
OpenAI/Speaches/Whisper transcription response is `{"text":"..."}` with NO duration field.
A producer that only reads response `duration` would meter 0 for the common-case client —
the bug would NOT be fixed and success criterion #1 would fail.

Today `usageJSONBuffer.Close` parses only the chat-token shape; STT and embed responses
flow through the SAME JSON-buffer path but their billable dimensions are silently dropped.
This plan adds the producers IN the existing interceptor (mirroring the chat-path token
extractor — locked decision #1: no new billing path), PLUS the request-audio derivation
fallback (a small middleware + duration helper), and proves the unit conversion + the
no-duration case with tests. Wiring the proxies to invoke this interceptor is Plan 16-02
(depends on this plan).

Purpose: turn the dead audio/embed metering dimension into live data — non-zero even for
default-format STT.
Output: request-audio duration helper + stamping middleware + route-dispatched producers in
interceptor_usage.go + tests locking the unit math AND the no-duration fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-close-gap-ten-04-wire-stt-embed-usage-metering-write-audiose/16-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- The unit chain + the existing patterns to reuse. Verified against the real code. -->

billing/accountant.go — the atomics this plan must write:
  AudioSecondsMs10 atomic.Int64  // audio_seconds × 10 (deciseconds). NEVER produced today.
  EmbedsCount      atomic.Int64  // count of embed inputs. NEVER produced today.
  // TokensIn / TokensOut already produced by the chat path — DO NOT TOUCH.

interceptor_usage.go — the flush already reads (DO NOT change the read or the guard):
  audioSeconds := float64(usage.AudioSecondsMs10.Load()) / 10.0   // :207
  embedsCount  := usage.EmbedsCount.Load()                         // :208
  // all-zero guard :210 already includes audio/embed terms.
  // usageJSONBuffer carries reqCtx (b.reqCtx) + reqPath (b.reqPath) — Close has both.

quota/counters.go — the conversions the producer's unit MUST agree with:
  int(row.AudioSeconds / 60) >= lim.DailyAudioMinutes   // :84 (and :111 monthly)
  int(row.EmbedsCount)       >= lim.DailyEmbeds          // :87 (and :114 monthly)
  ⇒ AudioSecondsMs10 = int64(durationSeconds * 10);  EmbedsCount = len(data[])

PROVEN multipart + response-duration pattern (audit/middleware.go:224-258):
  populateAudioMetadata does r.ParseMultipartForm(32<<20) → files["file"][0].Size (AudioSizeBytes)
  + .Header.Get("Content-Type") (AudioMime); and decodes the response
  `whisperPartial{ Duration *float64 json:"duration" }`. REUSE both shapes — do not invent
  a parallel parser.

CONTEXT-STAMP pattern to mirror (auditctx/override.go): WithBillingUpstream / BillingUpstreamFrom
  — a typed context key + With*/...From pair. Add WithRequestAudioSeconds / RequestAudioSecondsFrom
  the same way. The interceptor reads it via b.reqCtx in Close.

routeToBillingRoute(path) already exists (interceptor_usage.go:333):
  "/v1/audio..." → "stt"; "/v1/embeddings" → "embed"; "/v1/chat..." → "chat".

WHY a middleware + context (not re-read the body in the interceptor): the interceptor runs
at httputil ModifyResponse time, where resp.Request.Body has already been streamed to the
upstream — re-reading it is unreliable. The middleware parses the multipart ONCE before the
proxy runs (where ParseMultipartForm works), derives seconds, and stamps the ctx. The
interceptor reads the stamped value as the ELSE branch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Request-audio duration helper + context-stamping STT middleware</name>
  <files>gateway/internal/proxy/audio_duration.go, gateway/internal/proxy/audio_duration_test.go, gateway/internal/proxy/stt_request_audio_middleware.go, gateway/internal/proxy/stt_request_audio_middleware_test.go, gateway/internal/auditctx/override.go</files>
  <read_first>
    - gateway/internal/audit/middleware.go (:224-258 populateAudioMetadata — the proven ParseMultipartForm → files["file"][0].Size + Content-Type path to reuse)
    - gateway/internal/auditctx/override.go (the WithBillingUpstream/BillingUpstreamFrom typed-key pattern to mirror for WithRequestAudioSeconds)
    - gateway/internal/proxy/audio.go (NewAudioProxy — confirms /v1/audio/transcriptions is buffered multipart, never streamed)
    - gateway/internal/proxy/dynamic_override.go (:92-120 rewriteRequestBody / io.NopCloser(bytes.NewReader(...)) + ContentLength sync — the body-restore pattern to mirror)
  </read_first>
  <behavior>
    - DeriveAudioSeconds(audioBytes, mime) for a valid 16-bit PCM mono 16 kHz WAV of known sample count returns the true duration (RIFF/`fmt `/`data` header parse: dataChunkBytes / (sampleRate * channels * bytesPerSample)) within a small tolerance
    - DeriveAudioSeconds for a non-WAV / compressed mime (audio/mpeg, audio/ogg) returns a byte/bitrate estimate (documented constant bitrate fallback, e.g. AudioSizeBytes / assumedBytesPerSecond) — non-zero for a non-empty body
    - DeriveAudioSeconds for empty/garbage bytes returns 0 (no panic)
    - The middleware, given a multipart POST to /v1/audio/transcriptions with a file part, stamps RequestAudioSecondsFrom(ctx) == DeriveAudioSeconds(filebytes, mime); a non-audio route or non-multipart request passes through with no stamp (RequestAudioSecondsFrom == 0)
    - The middleware MUST restore the request body it consumed (re-wrap so the downstream proxy still forwards the audio byte-identical — mirror dynamic_override.go's rewriteRequestBody / io.NopCloser(bytes.NewReader(...)) + ContentLength sync)
  </behavior>
  <action>
    1. In auditctx/override.go add a typed key + `WithRequestAudioSeconds(ctx, float64)` and
       `RequestAudioSecondsFrom(ctx) float64` (returns 0 when unset), mirroring
       WithBillingUpstream/BillingUpstreamFrom exactly.
    2. Create audio_duration.go with `func DeriveAudioSeconds(audio []byte, mime string) float64`.
       WAV path: parse the RIFF container (`RIFF`…`WAVE`, locate `fmt ` chunk → channels,
       sampleRate, bitsPerSample; locate `data` chunk → its byte length) and compute
       seconds = dataBytes / (sampleRate * channels * (bitsPerSample/8)). Non-WAV path: a
       documented constant-bitrate estimate (define assumedKbps as a named const with a
       comment that it is an over/under-estimate accepted for quota purposes). Return 0 on
       parse failure / empty input. No external deps.
    3. Create stt_request_audio_middleware.go: an `func RequestAudioSecondsMiddleware(log) func(http.Handler) http.Handler`
       that, for multipart /v1/audio/transcriptions requests, reads the body (cap with the
       same 32<<20 ParseMultipartForm ceiling the audit path uses), extracts the "file" part
       bytes + Content-Type, computes DeriveAudioSeconds, stamps the ctx via
       WithRequestAudioSeconds, AND restores the consumed body so the proxy forwards it
       unchanged. Non-multipart / non-audio routes pass through untouched. This is the
       request-derivation ELSE branch feeding Task 3's fallback (locked decision #2).
    Tests: audio_duration_test.go (WAV known-duration + compressed estimate + empty→0);
    stt_request_audio_middleware_test.go (multipart POST → ctx stamped non-zero + body still
    readable downstream; non-audio route → no stamp).
  </action>
  <verify>
    <automated>cd gateway && go build ./... && go test ./internal/proxy/... -run 'DeriveAudioSeconds|RequestAudioSecondsMiddleware|RequestAudio' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go build ./...` exits 0
    - `grep -c "func DeriveAudioSeconds" gateway/internal/proxy/audio_duration.go` == 1
    - `grep -cE "func WithRequestAudioSeconds|func RequestAudioSecondsFrom" gateway/internal/auditctx/override.go` >= 2
    - `go test ./internal/proxy/... -run 'DeriveAudioSeconds|RequestAudio' -count=1` exits 0
    - The middleware test proves the downstream body is still readable (audio forwarded byte-identical) AND ctx stamp is non-zero for a WAV request
    - `cd gateway && gofmt -l internal/proxy/audio_duration.go internal/proxy/stt_request_audio_middleware.go internal/auditctx/override.go` prints nothing
  </acceptance_criteria>
  <done>DeriveAudioSeconds (WAV exact + compressed estimate) + the stamping middleware + the auditctx helper exist and are tested; request body survives byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route-dispatched STT + embed usage producers in the JSON-buffer Close path</name>
  <files>gateway/internal/proxy/interceptor_usage.go</files>
  <read_first>
    - gateway/internal/proxy/interceptor_usage.go (THE FILE — usageJSONBuffer.Close :516-553, sseUsageFrame :351, ExtractFromBody :152, routeToBillingRoute :333, flush reads :207-208 + guard :210-215, and that b.reqCtx/b.reqPath are available in Close)
    - gateway/internal/billing/accountant.go (AudioSecondsMs10 "×10 deciseconds" godoc)
    - gateway/internal/quota/counters.go (:84,87 the /60 + direct-embed conversions)
    - gateway/internal/audit/middleware.go (:241-254 the whisperPartial Duration decode to mirror)
    - gateway/internal/auditctx/override.go (RequestAudioSecondsFrom added in Task 1 — the ELSE source)
  </read_first>
  <behavior>
    - STT verbose_json resp `{"text":"oi","duration":90.0}` on /v1/audio/... → AudioSecondsMs10 == 900
    - STT DEFAULT resp `{"text":"oi"}` (NO duration) on /v1/audio/... WHEN ctx carries RequestAudioSeconds==30.0 → AudioSecondsMs10 == 300 (response-absent → request-derived; locked decision #2 ELSE branch)
    - STT resp with NO duration AND no ctx stamp → AudioSecondsMs10 == 0 (degrade, no panic)
    - Embed resp `{"data":[{...},{...}],"usage":{...}}` on /v1/embeddings → EmbedsCount == 2
    - Chat resp (token shape) → TokensIn/TokensOut unchanged; AudioSecondsMs10==0, EmbedsCount==0
  </behavior>
  <action>
    In usageJSONBuffer.Close (and consistently in ExtractFromBody so both non-streaming entry
    points agree), after the existing token extraction, dispatch on routeToBillingRoute(b.reqPath):

    - route "stt": decode the buffered body with `Duration *float64 json:"duration"` (mirror
      audit/middleware.go whisperPartial). Resolve seconds = response Duration WHEN present and > 0,
      ELSE auditctx.RequestAudioSecondsFrom(b.reqCtx) (the Task 1 derivation). When the resolved
      seconds > 0, call `b.usage.AudioSecondsMs10.Store(int64(seconds * 10))`. The ×10 factor is
      mandatory for the :207 `/10.0` flush + counters.go `/60` math (unit chain in <interfaces>).
    - route "embed": decode `Data []json.RawMessage json:"data"` (count only) → call
      `b.usage.EmbedsCount.Store(int64(len(Data)))`. One per data[] element (locked decision #2).
    - route "chat" (default): no new writes.

    Extend the sseUsageFrame type with the optional Duration + Data fields so a single
    json.Unmarshal of the buffered body serves all routes; keep jsonBufferCap intact. Do NOT
    edit the flush reads :207-208 or the all-zero guard :210-215 — they already account for
    audio/embed. Implements the TEN-04 + OBS-09 producer half per D-1/D-2 including the
    locked ELSE-derive branch.
  </action>
  <verify>
    <automated>cd gateway && go build ./... && go vet ./internal/proxy/...</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go build ./...` exits 0
    - `grep -nE "AudioSecondsMs10\.Store|EmbedsCount\.Store" gateway/internal/proxy/interceptor_usage.go` returns >= 2 matches (was 0 tree-wide)
    - `grep -c "RequestAudioSecondsFrom" gateway/internal/proxy/interceptor_usage.go` >= 1 (the ELSE branch is wired)
    - The flush reads (:207-208) and all-zero guard (:210-215) are UNCHANGED (git diff shows no edits in that block)
    - No new production file created in this task — change confined to interceptor_usage.go
  </acceptance_criteria>
  <done>STT route writes AudioSecondsMs10 from response-duration-ELSE-request-derived; embed route writes EmbedsCount=len(data[]); chat token extraction untouched; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests — duration-present, NO-duration request-derived, batch→embeds, zero-token enqueue, chat no-regression</name>
  <files>gateway/internal/proxy/interceptor_usage_test.go</files>
  <read_first>
    - gateway/internal/proxy/interceptor_usage_test.go (mkResp + the OpenAI/llama token tests :39-90 for the Intercept→io.Copy→assert→Close style; note Close deletes the slot — inspect before Close)
    - gateway/internal/proxy/interceptor_usage.go (the producers from Task 2)
    - gateway/internal/auditctx/override.go (WithRequestAudioSeconds — to stamp the ctx in the no-duration test)
    - gateway/internal/quota/counters.go (:84,87 — encode the /60 + direct-embed expectation explicitly)
  </read_first>
  <behavior>
    - TestSTTProducerDurationPresent: /v1/audio/transcriptions resp `{"text":"oi","duration":90.0}` → AudioSecondsMs10 == 900; assert float64(900)/10.0/60 == 1 (DailyAudioMinutes a 60s-limit tenant trips)
    - TestSTTProducerNoDurationDerivesFromRequest: resp `{"text":"oi"}` (NO duration), request ctx stamped WithRequestAudioSeconds(30.0) → AudioSecondsMs10 == 300 (proves default-format STT meters non-zero — REVISION 1 / success criterion #1)
    - TestEmbedProducerWritesEmbedsCount: /v1/embeddings resp with data[] length 3 → EmbedsCount == 3
    - TestSTTNoDurationNoCtxLeavesZero: no duration, no ctx stamp → AudioSecondsMs10 == 0, Close finalizes without panic
    - TestAudioOnlyRequestStillEnqueues: audio-only (tokens 0, AudioSecondsMs10>0) does NOT hit the all-zero early-return (assert the guard input `audioSeconds==0` is false, or use a flusher fake)
    - TestChatPathNoRegression: /v1/chat/completions token-shape resp → TokensIn/TokensOut as before, AudioSecondsMs10==0, EmbedsCount==0
  </behavior>
  <action>
    Add the six tests using the existing mkResp/discardLog helpers + the drain-then-inspect-
    before-Close pattern. For each, set Content-Type `application/json` and the request URL
    path so the interceptor's reqPath drives routeToBillingRoute (extend mkResp or set
    req.URL.Path). For TestSTTProducerNoDurationDerivesFromRequest, build the request ctx with
    auditctx.WithRequestAudioSeconds(ctx, 30.0) before mkResp so the ELSE branch reads it. The
    unit-conversion assertions (90s→1min; 30s→AudioSecondsMs10 300; batch-3→3 embeds) are the
    TEN-04 trap-guard and MUST be explicit in the test bodies.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/proxy/... -run 'STTProducer|EmbedProducer|STTNoDuration|AudioOnly|ChatPath|Usage' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go test ./internal/proxy/... -count=1` exits 0
    - The six new test funcs exist: `grep -cE "func Test(STTProducerDurationPresent|STTProducerNoDurationDerivesFromRequest|EmbedProducerWritesEmbedsCount|STTNoDurationNoCtxLeavesZero|AudioOnlyRequestStillEnqueues|ChatPathNoRegression)" gateway/internal/proxy/interceptor_usage_test.go` >= 6
    - The no-duration→derived assertion is present: `grep -n "WithRequestAudioSeconds" gateway/internal/proxy/interceptor_usage_test.go` returns >= 1 and the test asserts AudioSecondsMs10 == 300
    - Unit-conversion assertions present: `grep -nE "/ *10\.0|/ *60|900|300|== *1\b|== *3\b" gateway/internal/proxy/interceptor_usage_test.go` returns matches
    - `cd gateway && gofmt -l internal/proxy/interceptor_usage_test.go` prints nothing
  </acceptance_criteria>
  <done>go test green; duration-present, default-format-derived (non-zero), batch→embed, zero-token enqueue, and chat no-regression all asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream STT/embed → gateway interceptor | Upstream response JSON parsed for duration/data[]; oversized body must not OOM or crash close |
| client request audio → STT middleware | Untrusted multipart audio decoded for duration; malformed container must not panic or be re-forwarded corrupted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Denial of Service | usageJSONBuffer parse of upstream body | mitigate | Reuse jsonBufferCap (128 KiB) bound; oversized → source=partial, never OOM |
| T-16-02 | Tampering | upstream/client reports inflated duration to inflate billing/quota | accept | STT providers first-party/contracted; request-derived duration is bounded by the actual uploaded bytes — same trust extended to token counts |
| T-16-07 | Denial of Service | STT middleware reading + buffering request audio | mitigate | Cap with the same 32<<20 ParseMultipartForm ceiling the audit path already uses; body restored via io.NopCloser to preserve forwarding |
| T-16-08 | Tampering | malformed WAV/compressed container in DeriveAudioSeconds | mitigate | Header parse is bounds-checked; parse failure / empty input returns 0 (no panic), proven by the empty/garbage unit test |

No npm/pip/cargo installs in this plan — package legitimacy gate N/A.
</threat_model>

<verification>
- `cd gateway && go build ./... && go test ./internal/proxy/... -count=1` green.
- `grep -rnE "AudioSecondsMs10\.(Store|Add)|EmbedsCount\.(Store|Add)" gateway/ --include='*.go' | grep -v _test.go` >= 2 (was 0).
- Default-format STT (no response duration) meters non-zero via request-derived seconds (test proven).
- Flush reads + all-zero guard unchanged.
</verification>

<success_criteria>
- AudioSecondsMs10 written = int64(seconds × 10): seconds = response duration ELSE request-derived (never 0 for a real audio request).
- EmbedsCount = len(data[]) for embed routes.
- Unit-conversion tests assert 90s→DailyAudioMinutes 1, 30s-derived→AudioSecondsMs10 300, batch-3→DailyEmbeds 3.
- Audio/embed-only (zero-token) request still enqueues a billing event.
- Chat token path unchanged.
</success_criteria>

<output>
Create `.planning/phases/16-close-gap-ten-04-wire-stt-embed-usage-metering-write-audiose/16-01-SUMMARY.md` when done.
</output>
