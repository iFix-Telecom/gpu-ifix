---
phase: 06-auto-provisioning-emergency-pod-vast-ai
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - gateway/internal/config/config.go
  - gateway/internal/emerg/errors.go
  - gateway/internal/emerg/vast/errors.go
  - gateway/internal/emerg/mirror.go
  - gateway/internal/obs/metrics.go
  - .planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-WAVE0-GATES.md
autonomous: false
requirements: [PRV-01, PRV-02, PRV-05]
threat_refs: [T-6-01]
validates_sc: []
tags: [go, scaffolding, observability, config, emergency-pod]
user_setup:
  - service: vast.ai
    why: "Confirmar VAST_AI_API_KEY já configurada como GitHub Secret + variável de ambiente do stack ai-gateway-dev no Portainer (CLAUDE.md token store)"
    env_vars:
      - name: VAST_AI_API_KEY
        source: "CLAUDE.md token store / GitHub Secret IfixTelecom/gpu-ifix"
      - name: VAST_PRICE_CAP_DPH
        source: "operator decide (default 0.40 = $0.40/h)"
      - name: MONTHLY_EMERGENCY_BUDGET_BRL
        source: "operator decide (default 200 = R$200)"
      - name: USD_TO_BRL_RATE
        source: "operator decide (default 5.0)"

must_haves:
  truths:
    - "Phase 6 tem `github.com/go-redsync/redsync/v4 v4.16.0` resolvido em go.mod sem breaking change em builds existentes"
    - "Sentinel errors do package `emerg` e `emerg/vast` existem antes de qualquer implementação depender deles"
    - "Config (env) expõe 11 novas variáveis Phase 6 com defaults conservadores (W5 revisão 2026-05-13)"
    - "7 obs collectors emergency_* registrados via promauto sem colisão com nomes gateway_* existentes"
    - "namespace constants `emerg.Namespace = \"gw:emerg:\"` definida"
    - "Operator confirmou VAST_AI_API_KEY existe + VAST_PRICE_CAP_DPH + MONTHLY_EMERGENCY_BUDGET_BRL + USD_TO_BRL_RATE — valores gravados em 06-WAVE0-GATES.md"
  artifacts:
    - path: "gateway/internal/emerg/errors.go"
      provides: "sentinel errors do package emerg (Phase 6)"
      contains: "ErrOfferRaceLost|ErrHealthTimeout|ErrInstanceTerminal|ErrNoOffersBelowCap|ErrLeaderLost|ErrLifecycleSingleton"
    - path: "gateway/internal/emerg/vast/errors.go"
      provides: "sentinel errors do sub-package vast"
      contains: "ErrOfferGone|ErrInstanceNotFound|ErrRateLimited|ErrUnauthorized|VastError"
    - path: "gateway/internal/emerg/mirror.go"
      provides: "namespace constant"
      contains: "Namespace = \"gw:emerg:\""
    - path: "gateway/internal/config/config.go"
      provides: "11 env vars novas (VAST_AI_API_KEY, VAST_PRICE_CAP_DPH, EMERGENCY_POD_IMAGE_TAG, PRIMARY_HOST_ID, MONTHLY_EMERGENCY_BUDGET_BRL, USD_TO_BRL_RATE, PROVISION_TRIGGER_FAILED_OVER_SECONDS, PROVISION_HEALTHY_DURATION_SECONDS, PROVISION_IDLE_GRACE_SECONDS, PROVISION_COLDSTART_BUDGET_SECONDS, VAST_API_QPS_LIMIT)"
    - path: "gateway/internal/obs/metrics.go"
      provides: "7 promauto collectors Phase 6"
      contains: "GatewayEmergencyState|GatewayEmergencyLifecyclesTotal|GatewayEmergencyActivePod|GatewayEmergencyProvisionDurationSeconds|GatewayEmergencyCostDPH|GatewayEmergencyMonthCostBRL|GatewayVastAPIRequestsTotal"
    - path: ".planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-WAVE0-GATES.md"
      provides: "Operator confirmation record (price cap, budget, BRL rate, image tag, primary host_id se conhecido)"
  key_links:
    - from: "gateway/internal/obs/metrics.go"
      to: "prometheus/client_golang promauto"
      via: "promauto.NewGaugeVec / NewCounterVec / NewHistogram"
      pattern: "gateway_(emergency_|vast_api_)"
    - from: "gateway/internal/config/config.go"
      to: "Config struct fields novos"
      via: "VastAIAPIKey/VastPriceCapDPH/PrimaryHostID/MonthlyEmergencyBudgetBRL/USDToBRLRate/Provision*Seconds/EmergencyPodImageTag/VastAPIQPSLimit"
      pattern: "Vast|Provision|Emergency"
---

<objective>
Preparar o terreno para Fase 6: adicionar `go-redsync/redsync v4.16.0` ao go.mod, criar sentinel errors dos pacotes novos (`internal/emerg/`, `internal/emerg/vast/`), criar `mirror.go` com namespace constant, estender config com 11 env vars novas, registrar 7 collectors Prometheus emergency_* + vast_api_*, e fechar 1 operator gate gravando decisões em `06-WAVE0-GATES.md`.

Purpose: Entregar fundação "zero-refactor" que permite os demais plans (02..11) rodarem em paralelo sem conflitos — se go.mod, env vars e collectors não estão prontos no começo, qualquer task downstream pode acabar quebrando builds no merge ou criando métricas duplicadas.

Output: go.mod/go.sum atualizados (1 nova dep direta); 3 arquivos novos (`emerg/errors.go`, `emerg/vast/errors.go`, `emerg/mirror.go`); 2 arquivos modificados (`config.go`, `obs/metrics.go`); 1 arquivo de gate criado (`06-WAVE0-GATES.md`). Nenhuma lógica funcional — stubs + constantes + types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-CONTEXT.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-RESEARCH.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-PATTERNS.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-VALIDATION.md

<interfaces>
<!-- Estruturas existentes que este plan estende -->

De `gateway/internal/breaker/errors.go` (padrão Phase 3 — replicar para emerg):
  package godoc + `var (...)` block com sentinel errors usando `errors.New`.

De `gateway/internal/breaker/mirror.go` (1 linha — replicar para emerg):
  `package breaker` + `const Namespace = "gw:breaker:"` (Phase 6 vira `"gw:emerg:"`).

De `gateway/internal/config/config.go` (lines ~60-80 — padrão Phase 3+5):
  `Config` struct com fields como `ProbeIntervalSeconds`, `ShedTickIntervalMs`, `ShedDcgmTimeoutMs`. Phase 6 adiciona análogos.

De `gateway/internal/obs/metrics.go` (padrão promauto Phase 3+5):
  `var BreakerState = promauto.NewGaugeVec(prometheus.GaugeOpts{Name: "gateway_breaker_state", Help: "..."}, []string{"upstream"})`.

De `go.mod`:
  `github.com/redis/go-redis/v9 v9.18.0` (existing — redsync `redis/goredis/v9` driver depende disso).
  `github.com/cenkalti/backoff/v5 v5.0.3` (existing — disponível para vast/client.go retry).
  Phase 6 adiciona: `github.com/go-redsync/redsync/v4 v4.16.0` (verified current via `proxy.golang.org/github.com/go-redsync/redsync/v4/@latest`, published 2026-02-25).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar redsync/v4 + sentinel errors + namespace + config env vars</name>
  <files>go.mod, go.sum, gateway/internal/emerg/errors.go, gateway/internal/emerg/vast/errors.go, gateway/internal/emerg/mirror.go, gateway/internal/config/config.go</files>
  <read_first>
    - go.mod (verificar deps existentes — redis/go-redis/v9 v9.18.0 deve estar presente)
    - gateway/internal/breaker/errors.go (padrão sentinel exato a replicar)
    - gateway/internal/breaker/mirror.go (padrão namespace const)
    - gateway/internal/config/config.go (estrutura Config + Load() — onde adicionar 11 fields novos)
    - 06-PATTERNS.md seções 10-11 (excerpt de errors.go + mirror.go com line numbers)
    - 06-RESEARCH.md "Standard Stack > New deps required" (versão redsync v4.16.0 confirmada)
    - 06-CONTEXT.md decisões D-A1, D-A4, D-A5, D-B1, D-C1, D-D1, D-D2, D-D4 (defaults dos env vars)
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go build ./...` compila sem erro após `go get github.com/go-redsync/redsync/v4@v4.16.0`
    - Test 2: `cd gateway && go test ./internal/emerg/... ./internal/emerg/vast/...` compila stubs (mesmo sem teste real, build deve passar)
    - Test 3: `grep -c "ErrOfferRaceLost\|ErrHealthTimeout\|ErrInstanceTerminal\|ErrNoOffersBelowCap\|ErrLeaderLost\|ErrLifecycleSingleton" gateway/internal/emerg/errors.go` retorna ≥6
    - Test 4: `grep -c "ErrOfferGone\|ErrInstanceNotFound\|ErrRateLimited\|ErrUnauthorized\|VastError" gateway/internal/emerg/vast/errors.go` retorna ≥5
    - Test 5: `grep "Namespace = \"gw:emerg:\"" gateway/internal/emerg/mirror.go` retorna 1 match
    - Test 6: `cd gateway && go test ./internal/config/... -run TestConfigLoadDefaults` valida que defaults novos casam com D-A1/D-A4/D-C1/D-D1/D-D2/D-D4
  </behavior>
  <action>
    1. **go.mod/go.sum:** rodar `cd /home/pedro/projetos/pedro/gpu-ifix && go get github.com/go-redsync/redsync/v4@v4.16.0` (versão exata per 06-RESEARCH.md "Standard Stack > New deps required"). Não atualizar outras deps.
    2. **gateway/internal/emerg/errors.go (NOVO):** package emerg + godoc breve ("Package emerg implements the Phase 6 emergency-pod reconciler") + `import "errors"` + `var (...)` block com 6 sentinels: `ErrOfferRaceLost`, `ErrHealthTimeout`, `ErrInstanceTerminal`, `ErrNoOffersBelowCap`, `ErrLeaderLost`, `ErrLifecycleSingleton`. Mensagens conforme 06-PATTERNS.md seção 10. Não criar `lifecycle.go`/`fsm.go` ainda — outros plans cuidam.
    3. **gateway/internal/emerg/vast/errors.go (NOVO):** package vast + `import ("errors"; "fmt"; "net/http")` + sentinels `ErrOfferGone`, `ErrInstanceNotFound`, `ErrRateLimited`, `ErrUnauthorized` + struct `VastError{Status int; Code string; Msg string}` + método `Error() string` retornando `"vast: HTTP " + http.StatusText(e.Status) + ": " + e.Msg`. Conforme 06-PATTERNS.md seção 10.
    4. **gateway/internal/emerg/mirror.go (NOVO):** 3 linhas — `package emerg` + `const Namespace = "gw:emerg:"`. Replicar literalmente o `breaker/mirror.go` trocando `breaker` → `emerg` e `gw:breaker:` → `gw:emerg:`.
    5. **gateway/internal/config/config.go:** estender struct `Config` com 11 fields novos (ordem alfabética entre os Phase 6 fields, comentários de origem):
       - `EmergencyPodImageTag string` — env `EMERGENCY_POD_IMAGE_TAG` default `"v1.0"` (D-spec specifics)
       - `MonthlyEmergencyBudgetBRL float64` — env `MONTHLY_EMERGENCY_BUDGET_BRL` default `200.0` (D-D2)
       - `PrimaryHostID int64` — env `PRIMARY_HOST_ID` default `0` (0 = unknown, filter não aplica) (D-A2)
       - `ProvisionColdStartBudgetSeconds int` — env `PROVISION_COLDSTART_BUDGET_SECONDS` default `600` (D-A4)
       - `ProvisionHealthyDurationSeconds int` — env `PROVISION_HEALTHY_DURATION_SECONDS` default `300` (D-D1)
       - `ProvisionIdleGraceSeconds int` — env `PROVISION_IDLE_GRACE_SECONDS` default `300` (D-D1)
       - `ProvisionTriggerFailedOverSeconds int` — env `PROVISION_TRIGGER_FAILED_OVER_SECONDS` default `120` (D-C1)
       - `USDToBRLRate float64` — env `USD_TO_BRL_RATE` default `5.0` (D-D4)
       - `VastAIAPIKey string` — env `VAST_AI_API_KEY` (D-A5; sem default — empty = Phase 6 desabilitado, log warning at boot)
       - `VastAPIQPSLimit int` — env `VAST_API_QPS_LIMIT` default `1` (RESEARCH Open Question 12)
       - `VastPriceCapDPH float64` — env `VAST_PRICE_CAP_DPH` default `0.40` (D-A2)
       Adicionar leitura no `Load()` seguindo padrão Phase 5 (helpers `parseInt`/`parseFloat`/`getenv` se existirem, ou inline). NÃO modificar `process.exit` para empty `VastAIAPIKey` (graceful degrade — Phase 6 reconciler vai ficar disabled, sem fail-loud no boot).
    6. **NÃO** criar reconciler/FSM/vast client/lifecycle ainda — somente errors + mirror + config + go.mod.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./... && go test ./internal/config/... -run TestConfigLoad</automated>
  </verify>
  <done>
    - `go.mod` lista `github.com/go-redsync/redsync/v4 v4.16.0` como direct dep
    - `gateway/internal/emerg/errors.go` exporta 6 sentinels via `errors.New`
    - `gateway/internal/emerg/vast/errors.go` exporta 4 sentinels + `VastError` struct
    - `gateway/internal/emerg/mirror.go` define `const Namespace = "gw:emerg:"`
    - `gateway/internal/config/config.go` Config struct ganha 11 fields novos com defaults exatos de D-A1/D-A4/D-A5/D-C1/D-D1/D-D2/D-D4
    - `cd gateway && go build ./...` exit 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registrar 7 collectors Prometheus emergency_* + vast_api_* em obs/metrics.go</name>
  <files>gateway/internal/obs/metrics.go</files>
  <read_first>
    - gateway/internal/obs/metrics.go (estrutura atual — ver onde Phase 3 e Phase 5 collectors foram adicionados; padrão `var X = promauto.NewGaugeVec(...)` global)
    - 06-PATTERNS.md "Padrão C — Prometheus collector registration via promauto" (lines 1228-1275 — 7 collectors com OPTS exatas)
    - 06-CONTEXT.md decisão D-E2 (especifica os 7 nomes de métricas)
    - 06-RESEARCH.md "Phase Requirements > PRV-05/D-E2" (justificativa cardinality)
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go build ./internal/obs/...` compila sem colisão de nomes
    - Test 2: `grep -c "GatewayEmergencyState\|GatewayEmergencyLifecyclesTotal\|GatewayEmergencyActivePod\|GatewayEmergencyProvisionDurationSeconds\|GatewayEmergencyCostDPH\|GatewayEmergencyMonthCostBRL\|GatewayVastAPIRequestsTotal" gateway/internal/obs/metrics.go` retorna ≥7
    - Test 3: TestMetricsRegistration (se existir) ou inline `prometheus.DefaultRegisterer.Gather()` mostra os 7 metrics names sem panic de duplicate registration
    - Test 4: Buckets do histogram `gateway_emergency_provision_duration_seconds` correspondem a `[30, 60, 120, 300, 600, 900]` (range coerente com 10min cold-start budget)
    - Test 5: Cardinality budget — `gateway_emergency_cost_dph{lifecycle_id=...}` é high-cardinality por design, mas só é set durante lifecycle ativo (1 série por lifecycle vivo, ≤1 por D-B5); operator pode dropar essa série em scrape config se cardinality crescer
  </behavior>
  <action>
    Editar `gateway/internal/obs/metrics.go` adicionando bloco "// Phase 6 — emergency pod metrics" no fim do arquivo (após bloco Phase 5). Para cada collector usar `var X = promauto.NewXxx(...)` global, conforme excerpt em 06-PATTERNS.md Padrão C lines 1234-1275:
    - **GatewayEmergencyState** (`promauto.NewGaugeVec`) — Name `gateway_emergency_state`, Help "Current emergency FSM state. 1 for active, 0 for others.", labels `["state"]`. Caller seta 1 no estado corrente, 0 nos outros 8 estados.
    - **GatewayEmergencyLifecyclesTotal** (`promauto.NewCounterVec`) — Name `gateway_emergency_lifecycles_total`, Help "Count of emergency lifecycles by trigger and shutdown reason.", labels `["trigger_reason", "shutdown_reason"]`.
    - **GatewayEmergencyActivePod** (`promauto.NewGaugeVec`) — Name `gateway_emergency_active_pod`, Help "1 when emergency pod is live serving traffic; 0 otherwise.", labels `["pod_url"]`.
    - **GatewayEmergencyProvisionDurationSeconds** (`promauto.NewHistogram`) — Name `gateway_emergency_provision_duration_seconds`, Help "Time from search to /health pass for emergency pod provisions.", Buckets `[]float64{30, 60, 120, 300, 600, 900}`.
    - **GatewayEmergencyCostDPH** (`promauto.NewGaugeVec`) — Name `gateway_emergency_cost_dph`, Help "Current emergency pod USD per hour cost.", labels `["lifecycle_id"]`.
    - **GatewayEmergencyMonthCostBRL** (`promauto.NewGauge`) — Name `gateway_emergency_month_cost_brl`, Help "Running monthly aggregate cost in BRL of closed lifecycles.", sem labels.
    - **GatewayVastAPIRequestsTotal** (`promauto.NewCounterVec`) — Name `gateway_vast_api_requests_total`, Help "Vast.ai REST requests by operation and HTTP status.", labels `["op", "status"]` (op = "search"|"create"|"get"|"destroy"|"ping"; status = "started"|"transport_error"|"200"|"404"|"429"|"500"|...).
    NÃO criar nenhum `prometheus.MustRegister` manual — `promauto` já registra. Conferir que cardinality estimada (`state` 7 valores (BLOCKER 3 revisão: 9→7, OFF_HOURS+MAINTENANCE deferidos); `trigger_reason` 2 × `shutdown_reason` 11 = 22 séries; `pod_url` 1 ativa; `lifecycle_id` cresce mas só durante live; `op` 5 × `status` ~10 = 50) cabe no budget OBS-02 (≤10k séries — consumido em Phase 7). Total Phase 6 ≈ 80 séries baseline.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./internal/obs/... && go test ./internal/obs/...</automated>
  </verify>
  <done>
    - `gateway/internal/obs/metrics.go` declara 7 vars globais novos com names `gateway_emergency_state`, `gateway_emergency_lifecycles_total`, `gateway_emergency_active_pod`, `gateway_emergency_provision_duration_seconds`, `gateway_emergency_cost_dph`, `gateway_emergency_month_cost_brl`, `gateway_vast_api_requests_total`
    - Build limpo (`go build ./internal/obs/...` exit 0)
    - Tests passam (`go test ./internal/obs/...` exit 0)
    - Nenhum panic de duplicate registration
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator Gate — confirmar valores de produção dos env vars Phase 6</name>
  <what-built>
    Wave 0 instalou go.mod/redsync v4 + sentinel errors + 7 collectors + 11 env vars. Falta o operator confirmar **valores de produção** para 5 grandezas críticas antes de continuar:
    1. `VAST_PRICE_CAP_DPH` (preço máximo $/hora) — default 0.40 (RESEARCH Pitfall 5; epsilon comparison aplicada)
    2. `MONTHLY_EMERGENCY_BUDGET_BRL` (orçamento mensal) — default 200 (D-D2; alerta Sentry, NÃO bloqueia provisioning)
    3. `USD_TO_BRL_RATE` (cotação USD→BRL para audit) — default 5.0 (D-D4; operador atualiza trimestralmente)
    4. `EMERGENCY_POD_IMAGE_TAG` (tag da imagem ifix-ai-pod) — default `v1.0` (Phase 1 publica `:v1.0` AND `:latest`)
    5. `PRIMARY_HOST_ID` (host_id do primário Vast para excluir do filter) — 0 = unknown (D-A2; sem exclusion se desconhecido)
  </what-built>
  <how-to-verify>
    Criar `.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-WAVE0-GATES.md` registrando as decisões abaixo. Operator confirma com 1 das 3 ações para cada item:
    1. Aceitar default → marcar `default-aceito` na coluna decisão
    2. Override → escrever valor escolhido
    3. Defer → marcar `defer-para-plan-NN` (apenas se realmente bloqueia escolha agora)

    Template a criar (operator preenche colunas Decisão + Notas):

    | # | Env var | Default | Decisão (default-aceito|override:VAL|defer:NN) | Notas |
    |---|---------|---------|------------------------------------------------|-------|
    | 1 | VAST_PRICE_CAP_DPH | 0.40 | __ | RES Pitfall 5 — epsilon `cap+0.0001` |
    | 2 | MONTHLY_EMERGENCY_BUDGET_BRL | 200 | __ | D-D2 — Sentry alert only, no auto-block |
    | 3 | USD_TO_BRL_RATE | 5.0 | __ | D-D4 — atualizar trimestralmente |
    | 4 | EMERGENCY_POD_IMAGE_TAG | v1.0 | __ | Phase 1 publica `:v1.0` + `:latest` |
    | 5 | PRIMARY_HOST_ID | 0 (unknown) | __ | D-A2 — exclusion só aplicada se conhecido |

    Adicionalmente, confirmar:
    - `VAST_AI_API_KEY` está como GitHub Secret no repo `IfixTelecom/gpu-ifix` (já confirmado em CLAUDE.md token store, mas operator deve revalidar via `gh secret list -R IfixTelecom/gpu-ifix | grep VAST_AI_API_KEY`)
    - Stack `ai-gateway-dev` no Portainer terá os 4 env vars adicionados via UI antes de Phase 6 LIVE UAT (Plan 11)
  </how-to-verify>
  <resume-signal>Type "approved" depois que `06-WAVE0-GATES.md` estiver preenchido e committado, OR descreva problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → env vars | Valores em Portainer UI/Secrets controlam guardrails de cost (`VAST_PRICE_CAP_DPH`, `MONTHLY_EMERGENCY_BUDGET_BRL`); valor incorreto = budget runaway |
| env file → process | API key (`VAST_AI_API_KEY`) deve carregar mas NUNCA ser logada (Validation T-6-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Information Disclosure | env var `VAST_AI_API_KEY` em log | mitigate | Sentinel errors em vast/errors.go NÃO incluem key; vast/client.go (Plan 06) usa `Authorization: Bearer ${key}` apenas em header HTTP, nunca em log/Sentry breadcrumb. Test em Plan 06 valida via `httptest.Server` inspection |
| T-6-W0-01 | Tampering | operator pode setar `VAST_PRICE_CAP_DPH=10.0` por engano | accept | Documentado em 06-WAVE0-GATES.md; Sentry alert quando month_cost > budget (D-D2) detecta drift; operator é única source of truth |
| T-6-W0-02 | DoS | `MONTHLY_EMERGENCY_BUDGET_BRL=0` desativaria alertas | accept | Default 200 razoável; operator é responsável por valor sensato; Phase 7 dashboard expõe budget vs spent em UI |
</threat_model>

<verification>
- Build limpo: `cd gateway && go build ./...`
- Config loader: `cd gateway && go test ./internal/config/...`
- Obs metrics: `cd gateway && go test ./internal/obs/...`
- 06-WAVE0-GATES.md preenchido por operator e committado
</verification>

<success_criteria>
- go.mod tem `github.com/go-redsync/redsync/v4 v4.16.0` direct
- 3 arquivos novos criados em `gateway/internal/emerg/` (errors.go, mirror.go, vast/errors.go)
- 11 env vars novos parseados em `Load()` com defaults exatos
- 7 collectors Prometheus registrados sem colisão
- 06-WAVE0-GATES.md preenchido com 5 decisões operator + VAST_AI_API_KEY confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-01-SUMMARY.md`
</output>
