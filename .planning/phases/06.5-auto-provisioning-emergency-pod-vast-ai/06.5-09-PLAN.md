---
phase: 06-auto-provisioning-emergency-pod-vast-ai
plan: 09
type: execute
wave: 7
depends_on: [06-08]
files_modified:
  - gateway/internal/emerg/budget.go
  - gateway/internal/emerg/budget_test.go
  - gateway/internal/emerg/reconciler.go
  - gateway/internal/integration_test/emerg_budget_alert_test.go
  - gateway/internal/integration_test/emerg_audit_completeness_test.go
autonomous: true
requirements: [PRV-05, PRV-10]
threat_refs: [T-6-05]
validates_sc: []
tags: [go, budget-alert, sentry, audit-log, observability]

must_haves:
  truths:
    - "Monthly budget check (D-D2) executa a cada 60s do reconciler tick (não a cada 1s — query DB cara): chama `q.GetMonthlyCostBRL(ctx)` + atualiza obs.GatewayEmergencyMonthCostBRL gauge; quando month_cost > MonthlyEmergencyBudgetBRL → emit Sentry warning com tag subsystem=emerg, alert=budget_exceeded, extra month_cost_brl + budget_brl"
    - "Budget alert dedupe (Pitfall 11): atomic.Int64 day-bucket; max 1 emit por dia (UTC). Implementação CORRETA per RESEARCH lines 1411-1417 (NÃO a versão buggy lines 1402-1409)"
    - "NÃO bloqueia provisioning automaticamente (D-D2): apenas Sentry warning. Operator decide via gatewayctl emerg force-stop (Plan 10)"
    - "Audit completeness: cada lifecycle row (after close) deve ter populated: started_at, first_health_pass_at OR null se never healthy, ended_at, trigger_reason, vast_offer_id (após accept), vast_instance_id (após create), accepted_dph (após accept), total_cost_brl (calc per D-D4), shutdown_reason, events JSONB com ≥3 entries (transition events accumulated), leader_replica"
    - "Test integration TestEmergBudgetAlert: pre-seed lifecycles com SUM(total_cost_brl)=R$199; trigger 1 mais lifecycle costing R$5 (mock health pass + cutback acelerado); aguardar 2s (próxima 60s tick com test acceleration); assert Sentry hub received message contendo 'budget_exceeded'"
    - "Test integration TestEmergAuditCompleteness: full lifecycle happy path; após close, query lifecycle row + verificar todos os 11 campos esperados não-null (exceto first_health_pass_at OK ser null em failure paths)"
    - "Sentry test: usar `sentry.CurrentHub().BindClient(testClient)` com test transport para introspect events emitted (Phase 6 acessa via sentry-go internals OR um stub `sentry.Hub` em deps se deps.Sentry hub é injetável)"
  artifacts:
    - path: "gateway/internal/emerg/budget.go"
      provides: "checkBudget method + budgetAlertDedupe day-bucket + Sentry emit"
      contains: "checkBudget|budgetAlertDedupe|shouldEmit|sentry.CurrentHub|GetMonthlyCostBRL|GatewayEmergencyMonthCostBRL"
    - path: "gateway/internal/emerg/budget_test.go"
      provides: "Unit test budgetAlertDedupe.shouldEmit Pitfall 11 correct version"
      contains: "TestBudgetDedupe|TestBudgetDedupe_OncePerDay|TestBudgetDedupe_RaceFree"
    - path: "gateway/internal/emerg/reconciler.go"
      provides: "60s tick wrapper para checkBudget (lastBudgetCheckUnix atomic.Int64)"
      contains: "lastBudgetCheckUnix|checkBudget"
    - path: "gateway/internal/integration_test/emerg_budget_alert_test.go"
      provides: "Test integration: month cost cross threshold → Sentry warning"
      contains: "TestEmergBudgetAlert|sentry.*budget_exceeded"
    - path: "gateway/internal/integration_test/emerg_audit_completeness_test.go"
      provides: "Test all 11 audit columns populated after lifecycle close"
      contains: "TestEmergAuditCompleteness|first_health_pass_at|total_cost_brl|leader_replica|events"
  key_links:
    - from: "gateway/internal/emerg/budget.go checkBudget"
      to: "github.com/getsentry/sentry-go"
      via: "hub := sentry.CurrentHub().Clone(); hub.Scope().SetTag(...); hub.CaptureMessage(...)"
      pattern: "sentry\\..*Tag.*subsystem.*emerg.*budget"
    - from: "gateway/internal/emerg/budget.go"
      to: "gateway/internal/db/gen GetMonthlyCostBRL"
      via: "r.q.GetMonthlyCostBRL(ctx)"
      pattern: "GetMonthlyCostBRL"
---

<objective>
Implementar **monthly budget alert** (D-D2 / PRV-05) com dedupe correto Pitfall 11 + provar **audit log completeness** (PRV-10) — todos os 11 fields da emergency_lifecycles row populated após lifecycle close. Estes são os dois remaining gaps para PRV-05 ("guardrails" — preço cap já em Plan 06; budget alert aqui) e PRV-10 ("audit completo").

Purpose: Sem budget alert, operator descobre estouro só na fatura Vast.ai (atrasado 30 dias). Sem audit completo, Phase 7 dashboard timeline fica sem dados — o ponto de PRV-10 é prover o eventlog que Phase 7 consome direto.

Output: 1 source novo (budget.go) + 1 modificação reconciler (60s tick wrapper) + 2 integration tests (budget alert via Sentry hub + audit completeness verificando 11 fields).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-CONTEXT.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-RESEARCH.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-PATTERNS.md

<interfaces>
<!-- From Plans 02-08 -->

De `gateway/internal/db/gen/emergency_lifecycles.sql.go` (Plan 02):
  - q.GetMonthlyCostBRL(ctx) (numeric.Numeric, error) — SUM(total_cost_brl) closed, current month

De `gateway/internal/obs/metrics.go` (Plan 01):
  - GatewayEmergencyMonthCostBRL (gauge sem labels)

De `gateway/internal/config/config.go` (Plan 01):
  - Cfg.MonthlyEmergencyBudgetBRL (default 200)

De `gateway/internal/emerg/reconciler.go` (Plans 04-08):
  - Reconciler struct + runOneTick + Subscribe goroutine

De `github.com/getsentry/sentry-go` (Phase 2 D-A4):
  - sentry.CurrentHub() retorna *sentry.Hub global (initialized em internal/obs/sentry.go)
  - hub.Clone() retorna isolado scope-clone
  - hub.Scope().SetTag/SetExtra/CaptureMessage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: budget.go (checkBudget + dedupe Pitfall 11) + reconciler 60s tick wrapper + unit tests</name>
  <files>gateway/internal/emerg/budget.go, gateway/internal/emerg/budget_test.go, gateway/internal/emerg/reconciler.go</files>
  <read_first>
    - gateway/internal/emerg/reconciler.go (Plans 04-08)
    - gateway/internal/db/gen/emergency_lifecycles.sql.go (Plan 02 — GetMonthlyCostBRL signature)
    - 06-PATTERNS.md seção 7 lines 561-613 (budget.go skeleton com dedupe)
    - 06-RESEARCH.md Pitfall 11 lines 1398-1417 (CORRECT vs BUGGY version — usar lines 1411-1417)
    - 06-CONTEXT.md decisão D-D2 (Sentry warning, NÃO bloqueia, dedupe 1/dia)
    - 06-PATTERNS.md "Padrão B" lines 1213-1219 (Sentry hub.Clone + SetTag pattern)
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race ./internal/emerg/ -run TestBudget` exit 0
    - Test 2: TestBudgetDedupe_OncePerDay — 100 calls a `dedupe.shouldEmit()` no mesmo dia retornam só 1 true; outros 99 false
    - Test 3: TestBudgetDedupe_NewDay — chamar shouldEmit (true); avançar dia simulado (manipular time.Now() via interface ou direto modificar lastEmittedDay.Store(today-1)); call shouldEmit retorna true (novo dia)
    - Test 4: TestBudgetDedupe_RaceFree — 1000 goroutines call shouldEmit() simultâneo; total true == 1 (CAS atômico garante exatidão)
    - Test 5: TestCheckBudget_BelowThreshold — mockQ.GetMonthlyCostBRL retorna R$50; cfg.MonthlyEmergencyBudgetBRL=200; assert NÃO Sentry emit (mock hub) + obs gauge atualizado (50)
    - Test 6: TestCheckBudget_AboveThreshold — mockQ retorna R$250; assert Sentry hub recebeu 1 CaptureMessage com tags subsystem=emerg, alert=budget_exceeded + extras month_cost_brl=250 + budget_brl=200
  </behavior>
  <action>
    1. **gateway/internal/emerg/budget.go (NOVO):** copiar 06-PATTERNS.md seção 7 (CORRECT version):
       ```
       package emerg

       import (
           "context"
           "sync/atomic"
           "time"

           "github.com/getsentry/sentry-go"

           "github.com/ifixtelecom/gpu-ifix/gateway/internal/obs"
       )

       type budgetAlertDedupe struct {
           lastEmittedDay atomic.Int64 // unix epoch / 86400
       }

       // shouldEmit returns true at most once per UTC-day.
       // CORRECT version per RESEARCH Pitfall 11 lines 1411-1417 — NOT the buggy version lines 1402-1409.
       func (b *budgetAlertDedupe) shouldEmit() bool {
           today := time.Now().UTC().Unix() / 86400
           last := b.lastEmittedDay.Load()
           if last == today { return false }
           return b.lastEmittedDay.CompareAndSwap(last, today)
       }

       func (r *Reconciler) checkBudget(ctx context.Context) {
           costNumeric, err := r.q.GetMonthlyCostBRL(ctx)
           if err != nil {
               r.deps.Log.Warn("monthly cost query failed", "err", err)
               return
           }
           // pgtype.Numeric → float64 via .Float64Value() (sqlc/pgx pattern)
           costFloat, _ := costNumeric.Float64Value()
           obs.GatewayEmergencyMonthCostBRL.Set(costFloat.Float64)
           if costFloat.Float64 <= r.deps.Cfg.MonthlyEmergencyBudgetBRL { return }
           if !r.budgetDedupe.shouldEmit() { return }
           hub := sentry.CurrentHub().Clone()
           hub.Scope().SetTag("subsystem", "emerg")
           hub.Scope().SetTag("alert", "budget_exceeded")
           hub.Scope().SetExtra("month_cost_brl", costFloat.Float64)
           hub.Scope().SetExtra("budget_brl", r.deps.Cfg.MonthlyEmergencyBudgetBRL)
           hub.CaptureMessage("monthly emergency budget exceeded")
           r.deps.Log.Warn("budget alert emitted", "month_cost_brl", costFloat.Float64, "budget_brl", r.deps.Cfg.MonthlyEmergencyBudgetBRL)
       }
       ```

    2. **gateway/internal/emerg/reconciler.go MODIFY:** adicionar field `budgetDedupe *budgetAlertDedupe` em Reconciler struct + init em NewReconciler `r.budgetDedupe = &budgetAlertDedupe{}` + field `lastBudgetCheckUnix atomic.Int64` para 60s gating. Em runOneTick, após evaluateTick (independent de leader status — todos checkam), adicionar:
       ```
       // Budget check rate-limited a 60s
       if now.Unix() - r.lastBudgetCheckUnix.Load() >= 60 {
           r.lastBudgetCheckUnix.Store(now.Unix())
           r.checkBudget(ctx)
       }
       ```
       Decisão: budget check apenas pelo leader OR todas as réplicas? Se cada réplica chama, alert dedupe ainda funciona (1/dia por réplica), mas Sentry vê N events (N = número de réplicas). **Limitar ao leader** para evitar duplicação:
       ```
       if r.isLeader.Load() && now.Unix() - r.lastBudgetCheckUnix.Load() >= 60 {
           r.lastBudgetCheckUnix.Store(now.Unix())
           r.checkBudget(ctx)
       }
       ```

    3. **gateway/internal/emerg/budget_test.go (NOVO):** unit tests cobrindo 6 behaviors. Para Sentry test, usar `sentry.NewClient(sentry.ClientOptions{Transport: testTransport})` ou `sentry.Init(...)` com Transport mock que captura events em channel. Padrão: criar `recordingTransport` struct implementing `sentry.Transport` interface com `events chan *sentry.Event`.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race ./internal/emerg/ -run TestBudget</automated>
  </verify>
  <done>
    - budget.go com checkBudget + budgetAlertDedupe (Pitfall 11 CORRECT)
    - reconciler 60s tick wrapper, leader-only
    - 6 unit tests passam sob `-race`
    - Build limpo
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests budget alert (Sentry) + audit completeness</name>
  <files>gateway/internal/integration_test/emerg_budget_alert_test.go, gateway/internal/integration_test/emerg_audit_completeness_test.go</files>
  <read_first>
    - gateway/internal/emerg/budget.go (Task 1)
    - gateway/internal/integration_test/emerg_provision_happy_test.go (Plan 06 — full lifecycle test)
    - 06-VALIDATION.md tabela linhas 6-09-01 + 6-09-02 + 6-10-01
    - 06-PATTERNS.md "Key invariants for planner" linha 10 (Sentry init noop se DSN vazio; testes usam test transport)
    - 06-RESEARCH.md "5+ Minimum Integration Test Scenarios" entries TestEmergBudgetAlert + TestEmergAuditCompleteness
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run "TestEmergBudgetAlert|TestEmergAuditCompleteness"` exit 0
    - Test 2: TestEmergBudgetAlert — pre-seed `INSERT INTO ai_gateway.emergency_lifecycles (started_at, ended_at, total_cost_brl, trigger_reason, shutdown_reason) VALUES (NOW(), NOW(), 199.0, 'manual_force', 'cutback_idle')`; init Sentry com testTransport; trigger 1 lifecycle full happy path com USDToBRLRate ajustado para gerar custo R$5 (forcing close); aguardar 2s (próxima 60s tick c/ test acceleration) AND `r.lastBudgetCheckUnix` reset; assert testTransport recebeu Event com Tags subsystem=emerg, alert=budget_exceeded + Extras month_cost > 200
    - Test 3: TestEmergAuditCompleteness — full happy path lifecycle; após close (idle destroy chega), query `SELECT * FROM ai_gateway.emergency_lifecycles WHERE id = $1`; assert: id NOT NULL, started_at NOT NULL, first_health_pass_at NOT NULL (mock pod healthy), ended_at NOT NULL, trigger_reason = 'failed_over_sustained', vast_offer_id NOT NULL, vast_instance_id = 12345, accepted_dph > 0, total_cost_brl > 0, shutdown_reason = 'cutback_idle', events JSONB tem ≥3 entries (offer_accepted + healthy + cutback), leader_replica NOT empty
    - Test 4: Test runtime < 60s
  </behavior>
  <action>
    1. **gateway/internal/integration_test/emerg_budget_alert_test.go (NOVO):** build tag integration. Test setup:
       - `pool, rdb := freshSchema(t, ctx)`
       - Pre-seed budget lifecycles: `pool.Exec(ctx, "INSERT INTO ai_gateway.emergency_lifecycles (started_at, ended_at, total_cost_brl, trigger_reason, shutdown_reason) VALUES (date_trunc('month', NOW()), date_trunc('month', NOW()) + interval '1 hour', 199.0, 'manual_force', 'cutback_idle')")`
       - Setup Sentry test transport:
         ```go
         transport := &recordingTransport{events: make(chan *sentry.Event, 10)}
         err := sentry.Init(sentry.ClientOptions{Dsn: "test", Transport: transport})
         require.NoError(t, err)
         defer sentry.Flush(2 * time.Second)
         ```
       - Build reconciler com `Cfg.MonthlyEmergencyBudgetBRL = 200`, `TickInterval=100ms`, demais aceleração ativa
       - Force trigger via `r.checkBudget(ctx)` direto (bypass 60s gating para teste rápido) OR aguardar 100ms × 600 ticks (60s)
       - Eventually channel recebe event: `select { case ev := <-transport.events: require.Contains(t, ev.Tags, "alert"); require.Equal(t, "budget_exceeded", ev.Tags["alert"]); case <-time.After(5*time.Second): t.Fatal("no Sentry event") }`
       - `recordingTransport` definido helper: implementa interface `sentry.Transport` (`SendEvent`, `Flush`, `Configure`) capturando em channel.

    2. **gateway/internal/integration_test/emerg_audit_completeness_test.go (NOVO):** build tag integration. Setup similar a TestEmergProvisionHappyPath (Plan 06) com aceleração full:
       - Trigger → reach EmergencyActive (mockVast happy)
       - publishBreakerEvent CLOSED → Recovering → idle 1s → Cooldown (Plan 08 ride)
       - Eventually FSM == Cooldown
       - `var l gen.EmergencyLifecycle` query: `SELECT id, started_at, first_health_pass_at, ended_at, trigger_reason, vast_offer_id, vast_instance_id, accepted_dph, total_cost_brl, shutdown_reason, events, leader_replica FROM ai_gateway.emergency_lifecycles ORDER BY id DESC LIMIT 1`
       - require.NotZero(t, l.ID); require.False(t, l.StartedAt.IsZero()); require.True(t, l.FirstHealthPassAt.Valid); require.True(t, l.EndedAt.Valid); require.Equal(t, "failed_over_sustained", l.TriggerReason); require.True(t, l.VastInstanceID.Valid); require.Equal(t, int64(12345), l.VastInstanceID.Int64); require.Greater(t, l.AcceptedDph.Float64Value().Float64, 0.0); require.Greater(t, l.TotalCostBrl.Float64Value().Float64, 0.0); require.Equal(t, "cutback_idle", l.ShutdownReason.String); require.NotEmpty(t, l.LeaderReplica.String)
       - JSONB events: parse `var events []map[string]any; json.Unmarshal(l.Events, &events); require.GreaterOrEqual(t, len(events), 3)` (offer_accepted + healthy + cutback as minimum)
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race -tags=integration ./internal/integration_test/ -run "TestEmergBudgetAlert|TestEmergAuditCompleteness"</automated>
  </verify>
  <done>
    - 2 integration tests passam sob `-race -tags=integration`
    - Sentry recordingTransport helper criado (reusável)
    - PRV-05 budget alert + PRV-10 audit completeness validados
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Budget query → DB | Read-only SUM aggregate; sem PII envolvido |
| Sentry hub → Sentry SaaS | Tags + Extras enviados via HTTPS para sentry.io; sem payload sensível (apenas total + budget number) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-05 | Information Disclosure | Sentry CaptureMessage incluir info de spending → competitive intel se Sentry account compromised | accept | Apenas total + budget; sem detail de offer ou instance; Sentry SaaS já é trusted infrastructure (Phase 2 D-A4) |
| T-6-W9-01 | DoS | Sentry SaaS down → CaptureMessage bloqueia 60s tick? | mitigate | sentry-go usa async transport por default (eventos em channel); CaptureMessage retorna imediato |
| T-6-W9-02 | Tampering | Operator pode setar MonthlyEmergencyBudgetBRL=0 → alert dispara em 1ª lifecycle | accept | Default 200; documented em 06-WAVE0-GATES.md (Plan 01); operator awareness |
</threat_model>

<verification>
- Budget unit: `cd gateway && go test -race ./internal/emerg/ -run TestBudget`
- Integration: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run "TestEmergBudgetAlert|TestEmergAuditCompleteness"`
- Build: `cd gateway && go build ./...`
</verification>

<success_criteria>
- budget.go com Pitfall 11 CORRECT version
- 60s tick wrapper leader-only para checkBudget
- TestEmergBudgetAlert prova Sentry warning emitted
- TestEmergAuditCompleteness valida 11 fields populated
</success_criteria>

<output>
After completion, create `.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-09-SUMMARY.md`
</output>
