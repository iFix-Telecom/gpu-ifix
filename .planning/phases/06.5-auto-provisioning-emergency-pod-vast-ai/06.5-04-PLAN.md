---
phase: 06-auto-provisioning-emergency-pod-vast-ai
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - gateway/internal/emerg/reconciler.go
  - gateway/internal/emerg/reconciler_test.go
  - gateway/internal/integration_test/emerg_leader_test.go
autonomous: true
requirements: [PRV-03]
threat_refs: [T-6-03]
validates_sc: [SC-2]
tags: [go, leader-election, redsync, reconciler, distributed-lock]

must_haves:
  truths:
    - "Reconciler implementa loop tick 1Hz (configurável via TickInterval para tests, default 1s) com 2 caminhos: não-leader → tenta Lock; leader → Extend a cada 10s"
    - "redsync NewMutex usa `WithExpiry(30*time.Second), WithTries(1), WithRetryDelay(0)` (D-B2)"
    - "Pitfall 4 enforced: ANY non-`(true, nil)` retorno de Extend → cede leadership (`isLeader.Store(false)` + log warn + return)"
    - "Pitfall 8 enforced: shutdown via ctx.Done() usa `releaseCtx, _ := context.WithTimeout(context.Background(), 2*time.Second)` SEPARADO do parent ctx para Unlock"
    - "Test integration: 2x reconciler.Run(ctx) compartilhando 1 Redis (testcontainers) — apenas 1 acquires Lock; outros aguardam (isLeader.Load() == false)"
    - "Reconciler tem Deps struct injetável: `{DB *pgxpool.Pool, Redis *redis.Client, Redsync *redsync.Redsync, FSM *emerg.FSM, Cfg Config, TickInterval time.Duration, Log *slog.Logger}` para testabilidade"
    - "evaluateTick(ctx, now) é stub neste plan — apenas log Debug; lógica real é Plan 05 (trigger), Plan 06 (provisioning), Plan 07 (cancel/recovery), Plan 08 (cutback)"
  artifacts:
    - path: "gateway/internal/emerg/reconciler.go"
      provides: "Reconciler com Run() loop, Lock/Extend mechanics, isLeader atomic flag, evaluateTick stub"
      contains: "type Reconciler struct|isLeader atomic.Bool|lastExtendUnix atomic.Int64|func.*Run.*ctx context.Context|mutex.LockContext|mutex.ExtendContext|UnlockContext|releaseCtx"
    - path: "gateway/internal/emerg/reconciler_test.go"
      provides: "TestReconcilerExtendQuorumLoss + TestReconcilerLockBlocking unit tests with miniredis"
      contains: "TestExtendQuorumLoss|TestReconcilerLockAcquire|TestReconcilerCedeOnExtendFail"
    - path: "gateway/internal/integration_test/emerg_leader_test.go"
      provides: "Integration test prova 2 réplicas, 1 leader (PRV-03 / SC-2)"
      contains: "TestEmergLeaderLockBlocks2ndReplica"
  key_links:
    - from: "gateway/internal/emerg/reconciler.go"
      to: "github.com/go-redsync/redsync/v4 Mutex"
      via: "mutex := r.redsync.NewMutex(redisx.EmergLockKey(), redsync.WithExpiry(30*time.Second), ...)"
      pattern: "redsync\\.With(Expiry|Tries|RetryDelay)"
    - from: "gateway/internal/emerg/reconciler.go"
      to: "gateway/internal/redisx/emerg.go"
      via: "redisx.EmergLockKey() + redisx.NewEmergRedsync(rdb)"
      pattern: "redisx\\.(Emerg|NewEmerg)"
---

<objective>
Implementar o loop reconciler que (a) elege líder via redsync v4 distributed lock + (b) renova lease a cada 10s + (c) trata todos os edge cases de Pitfall 4 (Extend `(false, nil)` quorum case) e Pitfall 8 (separate ctx para Unlock graceful). Este plan NÃO contém lógica FSM — `evaluateTick(ctx, now)` é stub que log Debug "leader tick" e retorna. Lógica de transition vem em Plans 05-08.

Purpose: Leader-election é a única peça do PRV-03 ("Apenas o leader avança o FSM"). Sem reconciler com lock funcionando + integration test provando que 2 réplicas → 1 leader, todo o restante da Phase 6 é split-brain potential. Este plan isola a complexidade do redsync para que Plans subsequentes consumam apenas `r.IsLeader()` API.

Output: 1 source file (reconciler com Run loop + lock state); 1 unit test (CAS edge cases); 1 integration test (2 réplicas live).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-CONTEXT.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-RESEARCH.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-PATTERNS.md

<interfaces>
<!-- Padrões existentes a usar -->

De `gateway/internal/shed/tick.go` (1Hz ticker pattern Phase 5 — analog para Run loop):
  - lines 94-118: ticker setup + select loop com ctx.Done() handling

De `gateway/internal/redisx/emerg.go` (Plan 06-03 criou):
  - `EmergLockKey() string` (= "gw:emerg:lock")
  - `NewEmergRedsync(rdb *redis.Client) *redsync.Redsync`

De `gateway/internal/emerg/fsm.go` (Plan 06-03 criou):
  - `type FSM struct { ... }`, `func NewFSM(...)`, `State()`, `Transition(...)`

De `gateway/internal/config/config.go` (Plan 06-01 criou 11 fields):
  - `Config{VastAIAPIKey, ProvisionTriggerFailedOverSeconds, ...}`

De `github.com/go-redsync/redsync/v4` (Plan 06-01 adicionou):
  - `mutex.LockContext(ctx) error` — nil = acquired; ErrFailed/ErrTaken = não conseguiu
  - `mutex.ExtendContext(ctx) (bool, error)` — `(true, nil)` ok; ANY OTHER = lost
  - `mutex.UnlockContext(ctx) (bool, error)` — graceful release
  - sentinels: `redsync.ErrLockAlreadyExpired`, `redsync.ErrFailed`, `redsync.ErrTaken`, `redsync.ErrNodeTaken`

De `gateway/internal/integration_test/setup_test.go`:
  - `freshSchema(t, ctx) (pool, rdb)` — testcontainers Postgres + Redis
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reconciler.go — Run loop + lock state + Pitfall 4 + Pitfall 8 + unit tests</name>
  <files>gateway/internal/emerg/reconciler.go, gateway/internal/emerg/reconciler_test.go</files>
  <read_first>
    - gateway/internal/shed/tick.go (lines 94-118 — ticker pattern + ctx.Done handling)
    - gateway/internal/emerg/fsm.go (Plan 06-03 — FSM struct para Deps)
    - gateway/internal/redisx/emerg.go (Plan 06-03 — EmergLockKey + NewEmergRedsync)
    - gateway/internal/config/config.go (Plan 06-01 — Config fields)
    - 06-PATTERNS.md seção 2 lines 137-217 (skeleton Run + runOneTick com Pitfall 4 + Pitfall 8 enforced)
    - 06-RESEARCH.md "Pattern 2: Leader-elected reconciler tick" lines 350-401
    - 06-RESEARCH.md "go-redsync/redsync v4 Patterns" lines 822-861 (recommended Extend pattern)
    - 06-RESEARCH.md Pitfall 4 (lines 1356-1360 — sempre check `ok` AND `err`)
    - 06-RESEARCH.md Pitfall 8 (lines 1380-1384 — separate ctx for Unlock)
    - 06-VALIDATION.md tabela linhas 6-04-01 e 6-04-02
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race ./internal/emerg/ -run TestReconciler` exit 0
    - Test 2: TestExtendQuorumLoss — mock `mutex.ExtendContext` retorna `(false, nil)` (quorum case Pitfall 4); reconciler detecta non-`(true, nil)` → `isLeader.Store(false)` + log warn
    - Test 3: TestReconcilerCedeOnExtendFail — mock retorna `(false, redsync.ErrLockAlreadyExpired)`; reconciler cede leadership; próximo tick re-tenta Lock
    - Test 4: TestReconcilerLockAcquire — single reconciler com miniredis; após 1ª tick, isLeader == true
    - Test 5: TestReconcilerSeparateUnlockCtx — cancel parent ctx; reconciler usa releaseCtx (background + 2s timeout) para UnlockContext; Unlock NÃO retorna `context.Canceled`
    - Test 6: Race detector limpo
  </behavior>
  <action>
    Criar `gateway/internal/emerg/reconciler.go` seguindo 06-PATTERNS.md seção 2 e 06-RESEARCH.md Pattern 2:

    1. **Imports:** `("context", "log/slog", "os", "sync/atomic", "time", "github.com/go-redsync/redsync/v4", "github.com/jackc/pgx/v5/pgxpool", "github.com/redis/go-redis/v9", "github.com/ifixtelecom/gpu-ifix/gateway/internal/config", "github.com/ifixtelecom/gpu-ifix/gateway/internal/redisx", gen "github.com/ifixtelecom/gpu-ifix/gateway/internal/db/gen")`

    2. **Deps struct (caller-injected para testabilidade):**
       ```
       type Deps struct {
           DB           *pgxpool.Pool
           Redis        *redis.Client
           Redsync      *redsync.Redsync   // se nil, reconciler chama redisx.NewEmergRedsync(Redis)
           FSM          *emerg.FSM (alias self-import — same package, sem import)
           Cfg          config.Config
           TickInterval time.Duration   // default 1s; tests override para 100ms
           Log          *slog.Logger
       }
       ```
       (Se preferir self-package, FSM é direto `*FSM` por estar no mesmo package.)

    3. **Reconciler struct:**
       ```
       type Reconciler struct {
           deps           Deps
           isLeader       atomic.Bool
           lastExtendUnix atomic.Int64
           replicaID      string  // os.Hostname() ao boot
           // outros fields para Plans 05-08 (tracker, activeLifecycle, q gen.Queries) — ADICIONAR como zero values agora; Plans posteriores wirearão
           q              *gen.Queries  // construído via gen.New(deps.DB) em NewReconciler
       }
       ```

    4. **Constructor:**
       ```
       func NewReconciler(deps Deps) *Reconciler {
           if deps.TickInterval == 0 { deps.TickInterval = 1 * time.Second }
           if deps.Log == nil { deps.Log = slog.Default() }
           if deps.Redsync == nil { deps.Redsync = redisx.NewEmergRedsync(deps.Redis) }
           hostname, _ := os.Hostname()
           return &Reconciler{deps: deps, replicaID: hostname, q: gen.New(deps.DB)}
       }
       ```

    5. **API exposta:**
       - `func (r *Reconciler) IsLeader() bool { return r.isLeader.Load() }`
       - `func (r *Reconciler) State() State { return r.deps.FSM.State() }` (proxy para FSM)
       - `func (r *Reconciler) ReplicaID() string { return r.replicaID }`

    6. **Run loop (per 06-PATTERNS.md seção 2 lines 153-184 + Pitfall 4/8 enforced):**
       ```
       func (r *Reconciler) Run(ctx context.Context) {
           log := r.deps.Log.With("subsystem", "emerg.reconciler", "replica", r.replicaID)
           interval := r.deps.TickInterval
           t := time.NewTicker(interval)
           defer t.Stop()
           mutex := r.deps.Redsync.NewMutex(redisx.EmergLockKey(),
               redsync.WithExpiry(30*time.Second),
               redsync.WithTries(1),
               redsync.WithRetryDelay(0),
           )
           log.Info("emerg reconciler started", "interval", interval)
           for {
               select {
               case <-ctx.Done():
                   // Pitfall 8: separate ctx para Unlock — parent já cancelled
                   releaseCtx, releaseCancel := context.WithTimeout(context.Background(), 2*time.Second)
                   if r.isLeader.Load() {
                       _, _ = mutex.UnlockContext(releaseCtx)
                   }
                   releaseCancel()
                   log.Info("emerg reconciler stopping")
                   return
               case now := <-t.C:
                   r.runOneTick(ctx, mutex, now, log)
               }
           }
       }
       ```

    7. **runOneTick (per 06-PATTERNS.md seção 2 lines 188-214 + Pitfall 4):**
       ```
       func (r *Reconciler) runOneTick(ctx context.Context, mutex *redsync.Mutex, now time.Time, log *slog.Logger) {
           if !r.isLeader.Load() {
               if err := mutex.LockContext(ctx); err != nil {
                   return // someone else holds it; non-leader path
               }
               r.isLeader.Store(true)
               r.lastExtendUnix.Store(now.Unix())
               log.Info("acquired leadership")
               // Plan 07 wires: r.recoverOrphanLifecycles(ctx)
           } else {
               // Renew every 10s = 1/3 TTL (D-B2). Use elapsed-based check.
               if now.Unix()-r.lastExtendUnix.Load() >= 10 {
                   ok, err := mutex.ExtendContext(ctx)
                   if err != nil || !ok {
                       // Pitfall 4: any non-(true, nil) = lost leadership
                       log.Warn("lost leadership", "err", err, "ok", ok)
                       r.isLeader.Store(false)
                       return
                   }
                   r.lastExtendUnix.Store(now.Unix())
               }
           }
           // Leader: evaluate FSM transitions (STUB neste plan; Plans 05-08 implementam)
           r.evaluateTick(ctx, now, log)
       }
       ```

    8. **evaluateTick (STUB neste plan):**
       ```
       func (r *Reconciler) evaluateTick(ctx context.Context, now time.Time, log *slog.Logger) {
           log.Debug("leader tick (stub — Plans 05-08 implement)", "state", r.deps.FSM.State().String())
       }
       ```
       Plans 05-08 modificarão este método incrementalmente.

    9. **`reconciler_test.go`:** unit tests com miniredis + mock injection:
       - TestExtendQuorumLoss — usar mock mutex (interface wrap se necessário) OU fazer integration test com miniredis + manipular TTL para forçar `(false, ErrLockAlreadyExpired)`. Simpler: testar em integration_test (Task 2).
       - TestReconcilerLockAcquire — start miniredis, NewReconciler(deps), `go r.Run(ctx)`, after 200ms `require.True(t, r.IsLeader())`
       - TestReconcilerSeparateUnlockCtx — `ctx, cancel := context.WithCancel(context.Background())`, start Run, wait Lock, `cancel()`; assert no panic + lock liberado em miniredis após release
       - Aceitar que TestExtendQuorumLoss é melhor coberto em integration_test (mock real do redsync é difícil); em unit, focar em teste do Run lifecycle.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race ./internal/emerg/ -run TestReconciler</automated>
  </verify>
  <done>
    - `internal/emerg/reconciler.go` exporta Reconciler, NewReconciler, Deps, IsLeader, State, ReplicaID, Run
    - Run loop usa Pitfall 4 (check `ok` AND `err`) + Pitfall 8 (separate ctx for Unlock)
    - evaluateTick é stub log Debug
    - 3+ unit tests passam sob `-race`
    - `cd gateway && go build ./...` exit 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — 2 réplicas, 1 leader (PRV-03 / SC-2)</name>
  <files>gateway/internal/integration_test/emerg_leader_test.go</files>
  <read_first>
    - gateway/internal/integration_test/setup_test.go (freshSchema helper)
    - gateway/internal/integration_test/breaker_state_machine_test.go (padrão integration test com Eventually)
    - 06-PATTERNS.md "No Analog Found" tabela `emerg_leader_test.go` (novel — instruções)
    - 06-VALIDATION.md tabela linha 6-04-01 (TestEmergLeaderLockBlocks2ndReplica)
    - 06-CONTEXT.md SC-2 (running 2 gateway replicas → never produces more than 1 emergency pod)
    - 06-RESEARCH.md "5+ Minimum Integration Test Scenarios" linha "TestEmergLeaderLockBlocks2ndReplica"
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run TestEmergLeaderLockBlocks2ndReplica` exit 0
    - Test 2: Test cria 2 reconcilers compartilhando 1 testcontainers Redis + 1 Postgres (via freshSchema); cada reconciler tem `os.Hostname()` distinto (override via Deps field se necessário)
    - Test 3: After 500ms (5x ticker @100ms), exatamente 1 reconciler retorna `IsLeader() == true`; outro retorna false
    - Test 4: Cancel ctx do leader; aguarda 200ms; outro reconciler eventually adquire lock (require.Eventually)
    - Test 5: Test executa em < 30s (acceleration via TickInterval=100ms)
  </behavior>
  <action>
    Criar `gateway/internal/integration_test/emerg_leader_test.go`:

    1. Build tag `//go:build integration` na primeira linha
    2. `package integration` (mesmo package dos outros integration tests)
    3. Imports: `("context", "os", "sync/atomic", "testing", "time", "github.com/stretchr/testify/require", "github.com/ifixtelecom/gpu-ifix/gateway/internal/emerg", "github.com/ifixtelecom/gpu-ifix/gateway/internal/config")`
    4. **TestEmergLeaderLockBlocks2ndReplica:**
       - `ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second); defer cancel()`
       - `pool, rdb := freshSchema(t, ctx)` (do setup_test.go)
       - Override hostname para teste: como `os.Hostname()` é fixo, criar 2 Reconcilers com Deps idêntico — redsync vai distinguir pela mutex.value (UUID interno do redsync). Para o test:
         - `fsm1 := emerg.NewFSM(slog.Default(), nil); fsm2 := emerg.NewFSM(slog.Default(), nil)`
         - `r1 := emerg.NewReconciler(emerg.Deps{DB: pool, Redis: rdb, FSM: fsm1, TickInterval: 100*time.Millisecond, Cfg: defaultTestCfg(t)})`
         - `r2 := emerg.NewReconciler(emerg.Deps{DB: pool, Redis: rdb, FSM: fsm2, TickInterval: 100*time.Millisecond, Cfg: defaultTestCfg(t)})`
       - `go r1.Run(ctx); go r2.Run(ctx)`
       - `require.Eventually(t, func() bool { return r1.IsLeader() != r2.IsLeader() }, 2*time.Second, 50*time.Millisecond, "exactly 1 leader expected")`
       - Assert exatamente 1 é leader: `leader, follower := r1, r2; if r2.IsLeader() { leader, follower = r2, r1 }; require.True(t, leader.IsLeader()); require.False(t, follower.IsLeader())`
       - **Failover:** crear `leaderCtx, leaderCancel := context.WithCancel(ctx)` e re-fazer Run com leaderCtx (refactor: spawn r1/r2 com seus próprios contexts). Cancel leaderCtx; `require.Eventually(t, func() bool { return follower.IsLeader() }, 5*time.Second, 100*time.Millisecond, "follower should acquire lock after leader cedes")`. (Nota: simpler test sem failover ainda satisfaz PRV-03 invariant; failover é nice-to-have.)
    5. Helper `defaultTestCfg(t *testing.T) config.Config` — retorna Config minimal com `ProvisionTriggerFailedOverSeconds: 1, ProvisionHealthyDurationSeconds: 1, ProvisionIdleGraceSeconds: 1, ProvisionColdStartBudgetSeconds: 5` (acceleration per Pitfall 13). Reusar em outros emerg integration tests.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race -tags=integration ./internal/integration_test/ -run TestEmergLeaderLockBlocks2ndReplica</automated>
  </verify>
  <done>
    - Integration test passa sob `-race -tags=integration`
    - 2 réplicas compartilhando 1 Redis → exatamente 1 leader
    - Failover: cancelar leader → outro adquire em < 5s
    - Helper `defaultTestCfg` disponível para outros tests Phase 6
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reconciler ↔ Redis Mutex | Lock TTL = 30s; renew = 10s. Network partition entre gateway e Redis → leader cede gracefully (não age) |
| Leader ↔ FSM | Leader é exclusivo writer do FSM transitions; non-leader observa via Pub/Sub (Plan 05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Tampering / Race | 2 réplicas advance FSM simultâneo (split-brain) | mitigate | redsync v4 Lock + TTL=30s; Pitfall 4 enforced (any non-`(true, nil)` Extend → cede); DB partial unique index (Plan 02) é defense-in-depth |
| T-6-W4-01 | DoS | Redis indisponível paralisa leader-election | accept | Sem Redis, gateway tem problemas maiores (rate-limit + breaker mirror + billing também falham); Phase 6 emergency provisioning unavailable é consistent failure mode |
| T-6-W4-02 | Tampering | Operator manualmente DELETE `gw:emerg:lock` em Redis CLI | accept | Documented op risk; auditável via Redis MONITOR; convention é usar `gatewayctl emerg force-*` (Plan 10) NÃO redis-cli |
</threat_model>

<verification>
- Unit tests: `cd gateway && go test -race ./internal/emerg/ -run TestReconciler`
- Integration test: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run TestEmergLeaderLockBlocks2ndReplica`
- Build: `cd gateway && go build ./...`
</verification>

<success_criteria>
- Reconciler.Run() loop com tick 1Hz + Lock/Extend mechanics + Pitfall 4 + Pitfall 8 enforced
- evaluateTick é stub (Plans 05-08 implementam)
- IsLeader() / State() / ReplicaID() exposed para callers
- TestEmergLeaderLockBlocks2ndReplica prova SC-2 invariant (2 réplicas → 1 leader)
- defaultTestCfg helper disponível para outros tests Phase 6
</success_criteria>

<output>
After completion, create `.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-04-SUMMARY.md`
</output>
