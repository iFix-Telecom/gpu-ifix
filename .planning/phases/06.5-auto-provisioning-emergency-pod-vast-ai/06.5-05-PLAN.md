---
phase: 06-auto-provisioning-emergency-pod-vast-ai
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-03, 06-04]
files_modified:
  - gateway/internal/emerg/subscribe.go
  - gateway/internal/emerg/subscribe_test.go
  - gateway/internal/emerg/tracker.go
  - gateway/internal/emerg/tracker_test.go
  - gateway/internal/emerg/reconciler.go
  - gateway/internal/integration_test/emerg_trigger_test.go
  - gateway/internal/integration_test/emerg_force_command_test.go
autonomous: true
requirements: [PRV-04]
threat_refs: []
validates_sc: [SC-1]
tags: [go, pubsub, breaker-subscriber, trigger, fsm]

must_haves:
  truths:
    - "Subscriber subscribe a `gw:breaker:events` (Phase 3 channel — verified em redisx/breaker.go linha 26) com reconnect-with-backoff loop (replicate breaker/subscribe.go integral)"
    - "localLlmTracker mantém in-memory state ('closed'|'half-open'|'open') + openSince atomic.Int64 (unix ts da entrada em OPEN)"
    - "ApplyEvent(BreakerEvent) só atua para `ev.Upstream == \"local-llm\"`; ignora STT/embed (D-C2 — chat é SLA anchor)"
    - "SustainedFailedOverSeconds() retorna `now - openSince` quando state=OPEN, senão 0"
    - "Reconciler.evaluateHealthy() (modificação ao stub Plan 04) chama `tracker.SustainedFailedOverSeconds()` cada tick; quando ≥ `cfg.ProvisionTriggerFailedOverSeconds` AND state=Healthy AND no live lifecycle (DB query) → transition Healthy→FailedOver→EmergencyProvisioning"
    - "Trigger NÃO usa STT/embed breakers (D-C2) NÃO usa shedding=ON (D-C2 — saturação ≠ falha)"
    - "Test integration: published `BreakerEvent{Upstream:\"local-llm\", State:\"open\"}` → após sustained ≥ 1s (test acceleration via env), reconciler transita FSM para EmergencyProvisioning"
    - "Test transient: published OPEN então CLOSED em < 1s → tracker reset; trigger NÃO dispara"
    - "(BLOCKER 2 revision 2026-05-13) SubscribeEmergCommands goroutine subscribe a `gw:emerg:events`; applyEmergCommand handler processa typed events `force_provision_request` (leader-only INSERT lifecycle row trigger_reason=manual_force + FSM transition Healthy→FailedOver→EmergencyProvisioning) and `force_destroy_request` (leader-only call destroyAndCloseLifecycle shutdown_reason=manual + transition→Cooldown). Non-leader observe-only."
  artifacts:
    - path: "gateway/internal/emerg/subscribe.go"
      provides: "Subscriber loop replicado de breaker/subscribe.go (subscribe gw:breaker:events)"
      contains: "func.*Subscribe.*ctx context.Context|redisx.SubscribeBreakerEvents|json.Unmarshal|BreakerEvent|reconnect"
    - path: "gateway/internal/emerg/tracker.go"
      provides: "localLlmTracker com state + openSince + SustainedFailedOverSeconds"
      contains: "type localLlmTracker|openSince atomic.Int64|ApplyEvent|SustainedFailedOverSeconds"
    - path: "gateway/internal/emerg/reconciler.go"
      provides: "evaluateTick estendido: chama evaluateHealthy → transition trigger se sustained ≥ threshold"
      contains: "evaluateHealthy|SustainedFailedOverSeconds|cfg.ProvisionTriggerFailedOverSeconds|StateEmergencyProvisioning"
    - path: "gateway/internal/integration_test/emerg_trigger_test.go"
      provides: "Test integration: sustained OPEN → trigger; transient OPEN → no trigger"
      contains: "TestEmergTriggerSustained|TestEmergTriggerTransient|publishBreakerEvent"
    - path: "gateway/internal/integration_test/emerg_force_command_test.go"
      provides: "(BLOCKER 2) Tests para force-provision/force-destroy event consumption end-to-end"
      contains: "TestEmergReconcilerHandlesForceProvisionEvent|TestEmergReconcilerForceProvisionRejectedNonLeader|TestEmergReconcilerHandlesForceDestroyEvent|TestEmergReconcilerForceDestroyNoOpWhenIdle"
  key_links:
    - from: "gateway/internal/emerg/subscribe.go"
      to: "gateway/internal/redisx/breaker.go BreakerEventsChannel"
      via: "redisx.SubscribeBreakerEvents(ctx, rdb)"
      pattern: "redisx\\.SubscribeBreakerEvents|gw:breaker:events"
    - from: "gateway/internal/emerg/reconciler.go evaluateHealthy"
      to: "gateway/internal/emerg/tracker.go SustainedFailedOverSeconds"
      via: "tracker.SustainedFailedOverSeconds() >= cfg.ProvisionTriggerFailedOverSeconds"
      pattern: "tracker\\.SustainedFailedOverSeconds"
---

<objective>
Implementar o **input** do FSM emergency: detectar quando `local-llm` breaker fica OPEN sustained por X segundos (default 120s, env-tunable D-C1) e disparar transição `Healthy → EmergencyProvisioning`. Reusa exatamente o subscriber pattern Phase 3 D-D1 (`gw:breaker:events` channel — verificado em redisx/breaker.go linha 26).

Purpose: Sem trigger, FSM nunca sai de Healthy — toda Phase 6 fica dormente. Este plan fecha o gap entre breaker (Phase 3) e reconciler (Plan 04 stub) entregando o sinal que justifica provisioning. Único trigger é `local-llm` chat (D-C2): STT/embed degradam graceful via fallback Phase 3; saturação Phase 5 já tem tier-1 como destino.

Output: 2 source novos (subscribe.go, tracker.go) + 2 modificações (reconciler.go evaluateTick + applyEmergCommand) + 2 unit tests + 2 integration tests cobrindo (a) sustained vs transient trigger e (b) force-provision/force-destroy event consumption (BLOCKER 2 revisão 2026-05-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-CONTEXT.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-RESEARCH.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-PATTERNS.md

<interfaces>
<!-- Padrões existentes a replicar exato -->

De `gateway/internal/breaker/subscribe.go` (Phase 3 D-D1 — 56 linhas integral, source para emerg/subscribe.go):
  - Reconnect-with-backoff loop com `redisx.SubscribeBreakerEvents(ctx, rdb)` + `ps.Channel()` + json.Unmarshal + `r.applyRemoteEvent(ev)` + 1s backoff em ps.Close()

De `gateway/internal/redisx/breaker.go` (Phase 3 — channel name + BreakerEvent type):
  - `func SubscribeBreakerEvents(ctx context.Context, rdb *redis.Client) *redis.PubSub`
  - `type BreakerEvent struct { Upstream string; State string; ... }` (verificar campos exatos em redisx/breaker.go)
  - `BreakerEventsChannel() string` (= "gw:breaker:events")

De `gateway/internal/emerg/reconciler.go` (Plan 06-04 criou):
  - Reconciler struct (vai ganhar field `tracker *localLlmTracker`)
  - `evaluateTick(ctx, now, log)` stub a ser modificado
  - `q *gen.Queries` para query DB (livelifecycle check)

De `gateway/internal/emerg/fsm.go` (Plan 06-03):
  - `Transition(from, to State, now time.Time, reason string)`
  - States: StateHealthy, StateFailedOver (transient), StateEmergencyProvisioning

De `gateway/internal/db/gen/emergency_lifecycles.sql.go` (Plan 06-02):
  - `q.ListLiveEmergencyLifecycles(ctx)` — verifica se já existe live lifecycle (D-C5 reconciler check)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: subscribe.go (replicar breaker/subscribe.go) + tracker.go + unit tests</name>
  <files>gateway/internal/emerg/subscribe.go, gateway/internal/emerg/subscribe_test.go, gateway/internal/emerg/tracker.go, gateway/internal/emerg/tracker_test.go</files>
  <read_first>
    - gateway/internal/breaker/subscribe.go (LER LINE-BY-LINE — replicar 1:1 trocando event type + tracker call)
    - gateway/internal/redisx/breaker.go (verificar exato `BreakerEvent` struct fields — Upstream, State, From, To, etc.)
    - gateway/internal/emerg/reconciler.go (Plan 06-04 — Reconciler struct para field tracker)
    - 06-PATTERNS.md seção 3 lines 222-298 (subscribe.go skeleton + tracker pattern com line numbers)
    - 06-RESEARCH.md "Internal patterns to reuse / Breaker Subscriber" lines 902-930 (localLlmTracker código completo)
    - 06-CONTEXT.md decisão D-C2 (signal = local-llm only)
    - 06-RESEARCH.md Pitfall 3 (Pub/Sub at-most-once — reconnect mandatory)
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race ./internal/emerg/ -run "TestSubscribe|TestTracker"` exit 0
    - Test 2: TestTracker_OpenSince — Apply BreakerEvent{Upstream:"local-llm", State:"open"} → openSince > 0; Apply 2nd OPEN event → openSince UNCHANGED (idempotent)
    - Test 3: TestTracker_OpenToClose — após OPEN, Apply BreakerEvent{State:"closed"} → openSince=0; SustainedFailedOverSeconds() == 0
    - Test 4: TestTracker_IgnoresOtherUpstreams — Apply BreakerEvent{Upstream:"local-stt", State:"open"} → state UNCHANGED (NÃO afeta tracker para `local-llm`); SustainedFailedOverSeconds() == 0
    - Test 5: TestTracker_SustainedFailedOver — set openSince = now-150s; SustainedFailedOverSeconds() retorna ≥ 150
    - Test 6: TestSubscribe_Reconnect — miniredis subscribe; close PubSub via mock; verifica reconnect loop dispara após 1s
  </behavior>
  <action>
    1. **gateway/internal/emerg/tracker.go (NOVO):** package emerg + imports `("sync/atomic", "time", "github.com/ifixtelecom/gpu-ifix/gateway/internal/redisx")`. Replicar 06-PATTERNS.md seção 3 lines 277-299:
       ```
       type localLlmTracker struct {
           state     atomic.Value // string: "closed" | "half-open" | "open"
           openSince atomic.Int64 // unix-seconds
       }

       func newLocalLlmTracker() *localLlmTracker {
           t := &localLlmTracker{}
           t.state.Store("closed")
           return t
       }

       func (t *localLlmTracker) ApplyEvent(ev redisx.BreakerEvent) {
           if ev.Upstream != "local-llm" { return }
           t.state.Store(ev.State)
           if ev.State == "open" {
               if t.openSince.Load() == 0 {
                   t.openSince.Store(time.Now().Unix())
               }
           } else {
               t.openSince.Store(0)
           }
       }

       func (t *localLlmTracker) SustainedFailedOverSeconds() int64 {
           s, _ := t.state.Load().(string)
           if s != "open" { return 0 }
           since := t.openSince.Load()
           if since == 0 { return 0 }
           return time.Now().Unix() - since
       }

       func (t *localLlmTracker) State() string {
           s, _ := t.state.Load().(string)
           return s
       }
       ```

    2. **gateway/internal/emerg/subscribe.go (NOVO):** copiar 06-PATTERNS.md seção 3 lines 222-272 (replica integral de breaker/subscribe.go):
       ```
       package emerg

       import (
           "context"
           "encoding/json"
           "time"

           "github.com/ifixtelecom/gpu-ifix/gateway/internal/redisx"
       )

       // Subscribe consumes gw:breaker:events to detect local-llm OPEN sustained.
       // (Phase 6 reads breaker channel — does NOT publish to gw:emerg:events here;
       // publishing is in fsm.go onChange.)
       func (r *Reconciler) Subscribe(ctx context.Context) {
           log := r.deps.Log.With("subsystem", "emerg.subscribe")
           for {
               if err := ctx.Err(); err != nil { return }
               ps := redisx.SubscribeBreakerEvents(ctx, r.deps.Redis)
               ch := ps.Channel()
               drained := false
               for !drained {
                   select {
                   case <-ctx.Done():
                       _ = ps.Close()
                       return
                   case msg, ok := <-ch:
                       if !ok { drained = true; break }
                       var ev redisx.BreakerEvent
                       if err := json.Unmarshal([]byte(msg.Payload), &ev); err != nil {
                           log.Warn("malformed breaker event", "payload", msg.Payload, "err", err)
                           continue
                       }
                       r.tracker.ApplyEvent(ev)
                       log.Debug("applied breaker event", "upstream", ev.Upstream, "state", ev.State)
                   }
               }
               _ = ps.Close()
               log.Warn("pubsub channel closed; reconnecting")
               select {
               case <-ctx.Done(): return
               case <-time.After(1 * time.Second):
               }
           }
       }
       ```

    3. **gateway/internal/emerg/reconciler.go MODIFY:** adicionar field `tracker *localLlmTracker` em struct Reconciler; init em NewReconciler com `tracker: newLocalLlmTracker()`. Plan 04 deixou stub `evaluateTick` — adicionar chamada `go r.Subscribe(ctx)` no Run(). **W11 ordering invariant (revisão 2026-05-13):** Subscribe goroutine MUST be spawned BEFORE the first ticker fires, otherwise Pub/Sub messages published during boot are lost (Pub/Sub is at-most-once, no replay). Required wiring sequence in `Run(ctx)`:
       ```
       func (r *Reconciler) Run(ctx context.Context) {
           // 1. Setup mutex (no I/O yet)
           mutex := r.deps.Redsync.NewMutex(...)
           // 2. Spawn subscribers FIRST (W11 ordering invariant)
           go r.Subscribe(ctx)              // gw:breaker:events
           // (Plan 05 Task 3 adds: go r.SubscribeEmergCommands(ctx) — gw:emerg:events)
           // 3. Setup ticker AFTER subscribers are running
           t := time.NewTicker(r.deps.TickInterval)
           defer t.Stop()
           // 4. Main loop
           for { select { ... } }
       }
       ```
       Add an explicit unit test `TestRunSpawnsSubscriberBeforeTicker` (in reconciler_test.go) that uses a mock Redis with publish-on-tick semantics: assert subscriber goroutine count == expected before first tick fires. Simpler verification: integration-test smoke (`TestEmergTriggerSustained` already covers practical case — if Subscribe weren't spawned first, the published BreakerEvent would be lost and trigger would never fire).

    4. **subscribe_test.go + tracker_test.go:** unit tests cobrindo behaviors listados acima. Para subscribe_test, usar miniredis + redisx.PublishBreakerEvent + Eventually para verificar tracker recebeu evento.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race ./internal/emerg/ -run "TestSubscribe|TestTracker"</automated>
  </verify>
  <done>
    - tracker.go: localLlmTracker com 4 methods (ApplyEvent, SustainedFailedOverSeconds, State, newLocalLlmTracker)
    - subscribe.go: replica breaker/subscribe.go integral, com `r.tracker.ApplyEvent(ev)`
    - Reconciler ganha field tracker + Subscribe goroutine spawn em Run()
    - 5+ unit tests passam sob `-race`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: evaluateHealthy + Trigger transition + integration test sustained vs transient</name>
  <files>gateway/internal/emerg/reconciler.go, gateway/internal/integration_test/emerg_trigger_test.go</files>
  <read_first>
    - gateway/internal/emerg/reconciler.go (Plan 06-04 + Task 1 acima)
    - gateway/internal/emerg/fsm.go (Plan 06-03)
    - gateway/internal/db/gen/emergency_lifecycles.sql.go (Plan 06-02 — q.ListLiveEmergencyLifecycles)
    - gateway/internal/integration_test/breaker_state_machine_test.go (padrão integration test + publishBreakerEvent helper se existir)
    - 06-CONTEXT.md decisão D-C1 (trigger threshold 120s default) + D-C5 (concurrency guards triple)
    - 06-VALIDATION.md tabela linhas 6-05-01 (TestEmergTriggerSustained) + 6-05-02 (TestTriggerTransient)
    - 06-RESEARCH.md "5+ Minimum Integration Test Scenarios" linha "TestEmergProvisionHappyPath" (similar setup)
    - 06-RESEARCH.md Pitfall 13 (test acceleration via env vars)
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run "TestEmergTrigger"` exit 0
    - Test 2: TestEmergTriggerSustained — single reconciler com `Cfg.ProvisionTriggerFailedOverSeconds=1`; publish BreakerEvent OPEN; aguardar 2s; assert FSM state == StateEmergencyProvisioning (NÃO Active ainda — Plan 06 fará vast call)
    - Test 3: TestEmergTriggerTransient — publish OPEN, immediately publish CLOSED (200ms later); aguardar 2s; assert FSM state == StateHealthy (trigger não disparou)
    - Test 4: TestEmergTriggerNoSpawnIfLiveLifecycle — pre-seed 1 live lifecycle no DB; publish OPEN sustained; assert reconciler NÃO transition (D-C5 reconciler check + log error)
    - Test 5: Test runtime < 30s
  </behavior>
  <action>
    1. **gateway/internal/emerg/reconciler.go MODIFY:** transformar `evaluateTick` stub do Plan 06-04 em dispatcher por estado:
       ```
       func (r *Reconciler) evaluateTick(ctx context.Context, now time.Time, log *slog.Logger) {
           switch r.deps.FSM.State() {
           case StateHealthy:
               r.evaluateHealthy(ctx, now, log)
           // Plans 06, 07, 08 vão adicionar cases para os outros 8 estados
           default:
               log.Debug("evaluateTick: state not yet handled (Plans 06-08)", "state", r.deps.FSM.State().String())
           }
       }

       func (r *Reconciler) evaluateHealthy(ctx context.Context, now time.Time, log *slog.Logger) {
           sustained := r.tracker.SustainedFailedOverSeconds()
           if sustained < int64(r.deps.Cfg.ProvisionTriggerFailedOverSeconds) {
               return
           }
           // D-C5 — reconciler check: NÃO spawn se já existe live lifecycle
           live, err := r.q.ListLiveEmergencyLifecycles(ctx)
           if err != nil {
               log.Error("query live lifecycles failed; skipping trigger", "err", err)
               return
           }
           if len(live) > 0 {
               log.Error("live lifecycle exists; trigger blocked (D-C5 reconciler check)", "count", len(live))
               return
           }
           log.Info("emergency trigger fired", "sustained_seconds", sustained, "threshold", r.deps.Cfg.ProvisionTriggerFailedOverSeconds)
           // Transition Healthy → FailedOver (transient state — apenas marker temporal) → EmergencyProvisioning
           r.deps.FSM.Transition(StateHealthy, StateFailedOver, now, "local_llm_open_sustained")
           r.deps.FSM.Transition(StateFailedOver, StateEmergencyProvisioning, now, "trigger_failed_over_sustained")
           // Plan 06 (lifecycle.go) wires: r.startProvisioning(ctx, lifecycleID)
           // Por enquanto, log apenas; transition em si já é evidência mensurável (test 2)
       }
       ```

    2. **gateway/internal/integration_test/emerg_trigger_test.go (NOVO):** build tag `//go:build integration`. Tests:
       - **TestEmergTriggerSustained:**
         - `pool, rdb := freshSchema(t, ctx)`
         - `cfg := defaultTestCfg(t)` (helper Plan 04) — `cfg.ProvisionTriggerFailedOverSeconds = 1`
         - `fsm := emerg.NewFSM(slog.Default(), nil)`
         - `r := emerg.NewReconciler(emerg.Deps{DB: pool, Redis: rdb, FSM: fsm, TickInterval: 100*time.Millisecond, Cfg: cfg})`
         - `go r.Run(ctx)`
         - `require.Eventually(t, r.IsLeader, 2*time.Second, 50*time.Millisecond)`
         - `publishBreakerEvent(t, rdb, "local-llm", "open")` (helper — usar redisx.PublishBreakerEvent direto)
         - `require.Eventually(t, func() bool { return fsm.State() == emerg.StateEmergencyProvisioning }, 5*time.Second, 100*time.Millisecond, "FSM should reach EMERGENCY_PROVISIONING after sustained OPEN ≥ 1s")`
       - **TestEmergTriggerTransient:**
         - Setup mesmo
         - `publishBreakerEvent(t, rdb, "local-llm", "open")`
         - `time.Sleep(200 * time.Millisecond)`
         - `publishBreakerEvent(t, rdb, "local-llm", "closed")`
         - `time.Sleep(2 * time.Second)`
         - `require.Equal(t, emerg.StateHealthy, fsm.State(), "FSM should remain HEALTHY after transient OPEN→CLOSED")`
       - **TestEmergTriggerNoSpawnIfLiveLifecycle:**
         - Setup mesmo + `pool.Exec(ctx, "INSERT INTO ai_gateway.emergency_lifecycles (trigger_reason) VALUES ('manual_force')")` antes de iniciar Reconciler
         - `publishBreakerEvent(t, rdb, "local-llm", "open")`
         - `time.Sleep(2 * time.Second)`
         - `require.Equal(t, emerg.StateHealthy, fsm.State(), "trigger blocked when live lifecycle exists (D-C5)")`
       - Helper `publishBreakerEvent(t *testing.T, rdb *redis.Client, upstream, state string)` no mesmo arquivo OR em `helpers_emerg_test.go` se outros tests reusarem.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race -tags=integration ./internal/integration_test/ -run TestEmergTrigger</automated>
  </verify>
  <done>
    - evaluateHealthy implementado com D-C1 threshold + D-C5 reconciler check
    - Transição Healthy→FailedOver→EmergencyProvisioning em 1 tick
    - 3 integration tests passam sob `-race -tags=integration`
    - publishBreakerEvent helper disponível para Plans 06+
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (BLOCKER 2 revision 2026-05-13): Subscribe to gw:emerg:events para consumir force-provision/force-destroy commands publicados pelo gatewayctl</name>
  <files>gateway/internal/emerg/subscribe.go, gateway/internal/emerg/reconciler.go, gateway/internal/integration_test/emerg_force_command_test.go</files>
  <read_first>
    - gateway/internal/redisx/emerg.go (Plan 06-03 — EmergEventsChannel + EmergEvent struct + SubscribeEmergEvents helper)
    - gateway/internal/emerg/subscribe.go (Task 1 — pattern to extend)
    - gateway/internal/emerg/reconciler.go (Plans 04+05 Tasks 1+2 — IsLeader, FSM, q, activeLifecycle access)
    - gateway/internal/db/gen/emergency_lifecycles.sql.go (Plan 02 — InsertEmergencyLifecycle for force-provision audit)
    - 06-CONTEXT.md decisão D-E1 (force-provision audit `trigger_reason='manual_force'`; force-destroy audit `shutdown_reason='manual'`)
    - 06-10-PLAN.md Task 1 (gatewayctl publishes EmergEvent{Type:"force_provision_request"|"force_destroy_request"} payloads)
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run TestEmergReconcilerHandlesForceProvisionEvent` exit 0
    - Test 2: TestEmergReconcilerHandlesForceProvisionEvent — single reconciler com leader lock acquired; publish EmergEvent{Type:"force_provision_request", Reason:"smoke"} via redisx.PublishEmergEvent; Eventually FSM == StateEmergencyProvisioning + DB has 1 row with `trigger_reason='manual_force'` em < 3s
    - Test 3: TestEmergReconcilerForceProvisionRejectedNonLeader — 2 reconcilers; non-leader também subscribe mas SÓ leader age (verificar: somente lifecycle row insert pelo replica que é leader; assert pool count == 1, NOT 2)
    - Test 4: TestEmergReconcilerForceDestroyNoOpWhenIdle — FSM == StateHealthy (no active lifecycle); publish force_destroy_request; assert mockVast.destroyHits == 0 + log Warn "no active lifecycle to destroy" + FSM permanece Healthy
    - **Test deferred to Plan 08 (wave-ordering — destroyAndCloseLifecycle helper lands in Plan 08):** TestEmergReconcilerHandlesForceDestroyEvent — pre-seed FSM em EMERGENCY_ACTIVE + activeLifecycle pointer; publish EmergEvent{Type:"force_destroy_request"}; Eventually mockVast.destroyHits == 1 + DB row close shutdown_reason='manual' + FSM == StateCooldown em < 3s. Plan 08 já cria destroyAndCloseLifecycle (Task 2); deve adicionar este integration test ao seu Task 2 test set.
    - Test 6: NÃO data races em -race
  </behavior>
  <action>
    1. **gateway/internal/emerg/subscribe.go MODIFY:** adicionar nova goroutine paralela `(r *Reconciler) SubscribeEmergCommands(ctx context.Context)` que subscribe a `gw:emerg:events` (analogous to existing breaker subscriber). Pattern (replicar reconnect-with-backoff):
       ```go
       func (r *Reconciler) SubscribeEmergCommands(ctx context.Context) {
           log := r.deps.Log.With("subsystem", "emerg.commands")
           for {
               if err := ctx.Err(); err != nil { return }
               ps := redisx.SubscribeEmergEvents(ctx, r.deps.Redis)
               ch := ps.Channel()
               drained := false
               for !drained {
                   select {
                   case <-ctx.Done():
                       _ = ps.Close(); return
                   case msg, ok := <-ch:
                       if !ok { drained = true; break }
                       var ev redisx.EmergEvent
                       if err := json.Unmarshal([]byte(msg.Payload), &ev); err != nil {
                           log.Warn("malformed emerg event", "payload", msg.Payload, "err", err); continue
                       }
                       r.applyEmergCommand(ctx, ev, log)
                   }
               }
               _ = ps.Close()
               select { case <-ctx.Done(): return; case <-time.After(1*time.Second): }
           }
       }
       ```

    2. **gateway/internal/emerg/reconciler.go MODIFY:** adicionar `applyEmergCommand`:
       ```go
       func (r *Reconciler) applyEmergCommand(ctx context.Context, ev redisx.EmergEvent, log *slog.Logger) {
           // Leader-only filter: ignora self-published transition events (ReplicaID == r.replicaID && Type == "transition")
           // Para force-* commands, gatewayctl publica com ReplicaID="gatewayctl" — qualquer reconciler vê, mas só leader age.
           if !r.isLeader.Load() {
               log.Debug("non-leader observed emerg command; ignoring", "type", ev.Type)
               return
           }
           switch ev.Type {
           case "force_provision_request":
               // D-C5 reconciler check: já existe live lifecycle? abort com log
               live, err := r.q.ListLiveEmergencyLifecycles(ctx)
               if err != nil { log.Error("query live lifecycles failed", "err", err); return }
               if len(live) > 0 {
                   log.Warn("force-provision rejected: live lifecycle already exists", "live_count", len(live))
                   return
               }
               // Insert lifecycle row com trigger_reason='manual_force' BEFORE FSM transition (avoid race with concurrent triggers)
               _, err = r.q.InsertEmergencyLifecycle(ctx, gen.InsertEmergencyLifecycleParams{
                   TriggerReason: "manual_force",
                   LeaderReplica: pgtype.Text{String: r.replicaID, Valid: true},
               })
               if err != nil { log.Error("InsertEmergencyLifecycle (manual_force) failed", "err", err); return }
               // Transition: leverage existing path. Plan 06 Task 3 evaluateEmergencyProvisioning case will pick up activeLifecycle == nil and call startProvisioning. So FSM transition is sufficient:
               r.deps.FSM.Transition(StateHealthy, StateFailedOver, time.Now(), "manual_force_provision:"+ev.Reason)
               r.deps.FSM.Transition(StateFailedOver, StateEmergencyProvisioning, time.Now(), "manual_force_provision:"+ev.Reason)
               // NOTA: startProvisioning em Plan 06 já INSERTs lifecycle. Para evitar duplicate insert, force-provision path precisa skip-insert (fluxo: subscriber INSERTs, evaluateEmergencyProvisioning detecta activeLifecycle == nil mas DB row exists → startProvisioning recebe id existente OR refactor para load id). Implementação concreta delegada a Plan 06 Task 3 wiring author — esta task estabelece o ponto de entrada e o teste.
               log.Info("force-provision accepted", "reason", ev.Reason, "by_replica", ev.ReplicaID)
           case "force_destroy_request":
               lc := r.activeLifecycle.Load()
               if lc == nil {
                   log.Warn("force-destroy: no active lifecycle to destroy")
                   return
               }
               // Delegate to destroyAndCloseLifecycle (Plan 08 helper) com reason="manual"
               // Para Plan 05 Task 3 standalone, expor stub call: r.destroyAndCloseLifecycle(ctx, lc, "manual")
               // Plan 08 implementará destroyAndCloseLifecycle; até lá, subscriber pode logar "force-destroy queued; awaiting Plan 08 destroyAndCloseLifecycle".
               // Implementação real após Plan 08 merge: helper aceita "manual" como shutdown_reason.
               if err := r.destroyAndCloseLifecycle(ctx, lc, "manual"); err != nil {
                   log.Error("force-destroy failed", "id", lc.ID, "err", err)
                   return
               }
               r.deps.FSM.Transition(r.deps.FSM.State(), StateCooldown, time.Now(), "manual_force_destroy")
               log.Info("force-destroy accepted", "lifecycle_id", lc.ID, "by_replica", ev.ReplicaID)
           case "transition", "cancel_in_flight":
               // Self-or-cross-replica transition events; visibility-only para non-leader (já filtered acima)
               return
           default:
               log.Debug("unknown emerg event type; ignoring", "type", ev.Type)
           }
       }
       ```

    3. **gateway/internal/emerg/reconciler.go MODIFY (Run wiring):** adicionar `go r.SubscribeEmergCommands(ctx)` em Run() — paralelo ao `go r.Subscribe(ctx)` (Task 1 desta phase). Ordering: ambos subscribers iniciados ANTES do `for { ... }` ticker loop, depois do mutex setup. Wave 0 ordering invariant: Subscribe goroutines são spawned na primeira linha de Run() para garantir consumption antes do primeiro tick.

    4. **gateway/internal/integration_test/emerg_force_command_test.go (NOVO):** build tag integration. 4 tests cobrindo behavior 2-5 acima. Setup similar a TestEmergTriggerSustained (Task 2). Para TestEmergReconcilerForceProvisionRejectedNonLeader: 2 reconcilers sharing 1 Redis; após Eventually one is leader; publish event; Eventually `pool.QueryRow("SELECT COUNT(*) FROM ai_gateway.emergency_lifecycles WHERE trigger_reason='manual_force'")` retorna 1 (não 2).
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race -tags=integration ./internal/integration_test/ -run "TestEmergReconcilerHandlesForceProvisionEvent|TestEmergReconcilerForceProvisionRejectedNonLeader|TestEmergReconcilerForceDestroyNoOpWhenIdle"</automated>
  </verify>
  <done>
    - subscribe.go ganha SubscribeEmergCommands goroutine + applyEmergCommand handler em reconciler.go
    - Run() spawn 2 subscribers (gw:breaker:events + gw:emerg:events) ordered before ticker loop
    - Leader-only filter aplicado para force-* commands; non-leader observe-only
    - 3 integration tests passam sob `-race -tags=integration` (4º teste TestEmergReconcilerHandlesForceDestroyEvent deferido para Plan 08 — destroyAndCloseLifecycle helper lands lá)
    - BLOCKER 2 (revisão 2026-05-13) resolved: gatewayctl force-provision/force-destroy são FUNCIONAIS end-to-end (cli publish → reconciler consume → FSM transition + audit row)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pub/Sub stream → tracker | Subscriber confia em payload JSON publicado pelo breaker (Phase 3); malformed payload é loggado + ignorado (não panic) |
| Tracker → reconciler trigger | Tracker é in-memory; cada réplica tem o seu (não cross-replica state via Pub/Sub é sync) — leader vê seu próprio tracker; non-leader vê o seu tracker mas NÃO age |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-W5-01 | DoS | Subscriber bloqueado se Redis lento | mitigate | `ps.Channel()` é non-blocking; reconnect-with-backoff loop garante recuperação; subscriber em goroutine separada (não bloqueia Run) |
| T-6-W5-02 | Tampering | Payload BreakerEvent malformed (operator publica via redis-cli erroneamente) | mitigate | json.Unmarshal error → log Warn + continue; tracker NÃO panic |
| T-6-W5-03 | Information Disclosure | Subscriber loga payload em log Debug — pode incluir info de upstream sensível | accept | Payload Phase 3 só contém upstream name + state; sem PII |
</threat_model>

<verification>
- Unit tests: `cd gateway && go test -race ./internal/emerg/ -run "TestSubscribe|TestTracker"`
- Integration tests: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run TestEmergTrigger`
- Build: `cd gateway && go build ./...`
</verification>

<success_criteria>
- Subscriber subscribe a `gw:breaker:events` (Phase 3 channel)
- Tracker mantém openSince per-replica; só atua para `local-llm`
- evaluateHealthy disparara transição quando sustained ≥ threshold
- D-C5 reconciler check bloqueia spawn se live lifecycle já existe
- TestEmergTriggerSustained + TestEmergTriggerTransient + TestEmergTriggerNoSpawnIfLiveLifecycle passam
</success_criteria>

<output>
After completion, create `.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-05-SUMMARY.md`
</output>
