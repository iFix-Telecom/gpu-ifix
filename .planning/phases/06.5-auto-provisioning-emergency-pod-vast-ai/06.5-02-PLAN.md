---
phase: 06-auto-provisioning-emergency-pod-vast-ai
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - gateway/db/migrations/0019_emergency_lifecycles.sql
  - gateway/db/queries/emergency_lifecycles.sql
  - gateway/sqlc.yaml
  - gateway/internal/db/gen/emergency_lifecycles.sql.go
  - gateway/internal/db/gen/models.go
  - gateway/internal/integration_test/emerg_singleton_test.go
  - gateway/internal/integration_test/migrate_emerg_test.go
autonomous: true
requirements: [PRV-05, PRV-10]
threat_refs: [T-6-02]
validates_sc: []
tags: [go, database, schema, sqlc, audit]

must_haves:
  truths:
    - "Migration `0019_emergency_lifecycles.sql` aplicada cria tabela `ai_gateway.emergency_lifecycles` com 11 colunas + 4 índices"
    - "Coluna `first_health_pass_at TIMESTAMPTZ` existe (Pitfall 15 — D-D4 cost calc requer)"
    - "Partial unique index `emergency_live_singleton ON ((TRUE)) WHERE ended_at IS NULL` rejeita 2nd live row com unique violation (PRV-05 / D-B5)"
    - "sqlc gera 7 queries Go funcionais em `gateway/internal/db/gen/emergency_lifecycles.sql.go`: InsertEmergencyLifecycle, UpdateEmergencyLifecycleVastIDs, MarkEmergencyLifecycleHealthy, CloseEmergencyLifecycle, ListLiveEmergencyLifecycles, GetMonthlyCostBRL, ListEmergencyLifecycles"
    - "Migration up + down idempotentes (re-run ok via goose)"
  artifacts:
    - path: "gateway/db/migrations/0019_emergency_lifecycles.sql"
      provides: "DDL up/down para tabela emergency_lifecycles + 4 índices"
      contains: "CREATE TABLE.*emergency_lifecycles|first_health_pass_at|emergency_live_singleton|ended_at IS NULL"
    - path: "gateway/db/queries/emergency_lifecycles.sql"
      provides: "7 sqlc queries (Insert/Update*4 + List*2 + Get*1)"
      contains: "name: InsertEmergencyLifecycle|UpdateEmergencyLifecycleVastIDs|MarkEmergencyLifecycleHealthy|CloseEmergencyLifecycle|ListLiveEmergencyLifecycles|GetMonthlyCostBRL|ListEmergencyLifecycles"
    - path: "gateway/sqlc.yaml"
      provides: "queries list inclui emergency_lifecycles.sql"
      contains: "db/queries/emergency_lifecycles.sql"
    - path: "gateway/internal/db/gen/emergency_lifecycles.sql.go"
      provides: "Go bindings gerados por sqlc para 7 queries"
      contains: "InsertEmergencyLifecycle|GetMonthlyCostBRL|ListEmergencyLifecycles"
    - path: "gateway/internal/integration_test/emerg_singleton_test.go"
      provides: "Test que valida partial unique index"
      contains: "TestIntegration_EmergSingletonDBIndex|emergency_live_singleton"
    - path: "gateway/internal/integration_test/migrate_emerg_test.go"
      provides: "Test que valida migration aplica + colunas + índices criados"
      contains: "TestMigration0019|emergency_lifecycles|first_health_pass_at"
  key_links:
    - from: "gateway/db/migrations/0019_emergency_lifecycles.sql"
      to: "ai_gateway schema"
      via: "goose Up/Down + AI_GATEWAY_MIGRATE_ON_BOOT"
      pattern: "SET search_path = ai_gateway, public"
    - from: "gateway/db/queries/emergency_lifecycles.sql"
      to: "gateway/internal/db/gen/emergency_lifecycles.sql.go"
      via: "sqlc generate"
      pattern: "name: .* :one|:exec|:many"
---

<objective>
Criar a tabela de auditoria durável `ai_gateway.emergency_lifecycles` (PRV-10) com 11 colunas + 4 índices, incluindo a coluna `first_health_pass_at TIMESTAMPTZ` (Pitfall 15 — D-D4 cost calc) e o partial unique index `emergency_live_singleton` (D-B5 — defesa em camadas contra split-brain). Adicionar 7 sqlc queries para Insert/Update/List, regenerar bindings Go, e provar 2 invariantes via integration tests com testcontainers.

Purpose: Schema novo é a única peça **durable** da Phase 6 (Redis Hash + Pub/Sub são ephemeral; FSM é in-process). Sem a tabela, nada de PRV-10 (audit log completo) nem PRV-05 (singleton invariante). Plans subsequentes (04, 06, 07, 08, 09, 10) consomem essas queries.

Output: 1 migration .sql; 1 query .sql; modificação `sqlc.yaml`; arquivo Go gerado por sqlc; 2 integration tests provando index + migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-CONTEXT.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-RESEARCH.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-PATTERNS.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-VALIDATION.md

<interfaces>
<!-- Padrões existentes a replicar -->

De `gateway/db/migrations/0010_create_billing_events.sql` (DDL com indexes + COMMENT, padrão Phase 4):
  - `-- +goose Up` / `-- +goose Down` blocks com `StatementBegin/End`
  - `SET search_path = ai_gateway, public;`
  - `CREATE TABLE IF NOT EXISTS ai_gateway.X (...)` + `CREATE INDEX IF NOT EXISTS ...` + `COMMENT ON COLUMN ...`

De `gateway/db/queries/billing.sql` (sqlc query org pattern Phase 4):
  - `-- name: XxxYyy :one|:exec|:many` headers
  - `RETURNING id` em INSERTs com PK auto-gerada
  - `sqlc.arg('event_json')::jsonb` para JSONB append (Phase 5 padrão tenants_admin.sql)

De `gateway/sqlc.yaml` (verificar layout atual):
  - Lista `queries:` com paths relativos a `gateway/`
  - Append simples: `- db/queries/emergency_lifecycles.sql`

De `gateway/internal/integration_test/setup_test.go` (Phase 4):
  - `freshSchema(t, ctx)` reseta DB via `goose Down + Up`
  - `sharedPG`/`sharedRedis` containers compartilhados via `sync.Once`

De `gateway/internal/integration_test/migrate_test.go` (padrão de teste de migration):
  - Pattern para verificar tabela existe via `pg_class` query OU via `INFORMATION_SCHEMA.columns`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar migration 0019 + queries sqlc + regenerar bindings Go</name>
  <files>gateway/db/migrations/0019_emergency_lifecycles.sql, gateway/db/queries/emergency_lifecycles.sql, gateway/sqlc.yaml, gateway/internal/db/gen/emergency_lifecycles.sql.go, gateway/internal/db/gen/models.go</files>
  <read_first>
    - gateway/db/migrations/0010_create_billing_events.sql (padrão DDL + COMMENT + índices)
    - gateway/db/migrations/0017_evolve_upstreams_shed_thresholds.sql (padrão Phase 5 ALTER + JSONB)
    - gateway/db/queries/billing.sql (padrão sqlc names + RETURNING + sqlc.arg)
    - gateway/db/queries/upstreams.sql (padrão `:many` com WHERE filter)
    - gateway/sqlc.yaml (linha de `queries:` array — onde fazer append)
    - 06-PATTERNS.md seções 12-13 (DDL skeleton + sqlc queries com line numbers)
    - 06-RESEARCH.md "Code Examples / Migration 0019" (lines 1565-1619 — DDL completo verificado)
    - 06-RESEARCH.md "Pitfall 10" (migration number = 0019, NÃO 0017)
    - 06-RESEARCH.md "Pitfall 15" (coluna first_health_pass_at MANDATÓRIA)
    - 06-CONTEXT.md decisão D-B4 (schema spec) + D-B5 (partial unique index)
  </read_first>
  <behavior>
    - Test 1: `ls gateway/db/migrations/ | sort -V | tail -3` mostra `0017_evolve_upstreams_shed_thresholds.sql, 0018_audit_log_shed_values.sql, 0019_emergency_lifecycles.sql` — nenhum conflito de número
    - Test 2: `cd gateway && goose -dir db/migrations postgres "$TEST_DATABASE_URL" up` aplica até 0019 sem erro
    - Test 3: `psql $TEST_DATABASE_URL -c "\d ai_gateway.emergency_lifecycles"` mostra 11 colunas: id BIGSERIAL, started_at, first_health_pass_at, ended_at, trigger_reason, vast_offer_id, vast_instance_id, accepted_dph, total_cost_brl, shutdown_reason, events JSONB, leader_replica
    - Test 4: `psql -c "SELECT indexname FROM pg_indexes WHERE schemaname='ai_gateway' AND tablename='emergency_lifecycles'"` retorna 5 índices: PK + emergency_live_singleton + idx_emergency_lifecycles_started_at + idx_emergency_lifecycles_live + idx_emergency_lifecycles_month_cost
    - Test 5: `cd gateway && sqlc generate` regenera `internal/db/gen/emergency_lifecycles.sql.go` com 7 funções exportadas (InsertEmergencyLifecycle, UpdateEmergencyLifecycleVastIDs, MarkEmergencyLifecycleHealthy, CloseEmergencyLifecycle, ListLiveEmergencyLifecycles, GetMonthlyCostBRL, ListEmergencyLifecycles)
    - Test 6: `cd gateway && go build ./internal/db/gen/...` compila sem erro após sqlc generate
    - Test 7: Goose down (`goose down`) reverte 0019 → tabela some via `DROP TABLE IF EXISTS ... CASCADE`
  </behavior>
  <action>
    1. **gateway/db/migrations/0019_emergency_lifecycles.sql (NOVO):** copiar literalmente o DDL de 06-RESEARCH.md "Code Examples / Migration 0019" lines 1565-1619 (corrigido de Pitfall 10 e Pitfall 15). Estrutura:
       - `-- +goose Up` + `-- +goose StatementBegin`
       - `SET search_path = ai_gateway, public;`
       - `CREATE TABLE IF NOT EXISTS ai_gateway.emergency_lifecycles (...)` com 11 colunas exatas (id BIGSERIAL PK, started_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), **first_health_pass_at TIMESTAMPTZ** (Pitfall 15), ended_at TIMESTAMPTZ, trigger_reason TEXT NOT NULL, vast_offer_id BIGINT, vast_instance_id BIGINT, accepted_dph NUMERIC(6,4), total_cost_brl NUMERIC(10,4), shutdown_reason TEXT, events JSONB NOT NULL DEFAULT '[]'::JSONB, leader_replica TEXT)
       - 4 índices: `CREATE UNIQUE INDEX IF NOT EXISTS emergency_live_singleton ON ai_gateway.emergency_lifecycles ((TRUE)) WHERE ended_at IS NULL` + `idx_emergency_lifecycles_started_at` + `idx_emergency_lifecycles_live` + `idx_emergency_lifecycles_month_cost`
       - `COMMENT ON TABLE ...` + `COMMENT ON COLUMN ai_gateway.emergency_lifecycles.first_health_pass_at IS 'Timestamp when pod /health first returned healthy. Used as start of cost calc (D-D4): hours_active = ended_at - first_health_pass_at.';`
       - `-- +goose StatementEnd`
       - `-- +goose Down` + `-- +goose StatementBegin` + `DROP TABLE IF EXISTS ai_gateway.emergency_lifecycles CASCADE;` + `-- +goose StatementEnd`
       NÃO criar trigger NOTIFY (config emergency vem de env, não DB; per 06-PATTERNS.md seção 12).
    2. **gateway/db/queries/emergency_lifecycles.sql (NOVO):** copiar 06-PATTERNS.md seção 13 lines 919-967 — 7 queries com nomes exatos:
       - `InsertEmergencyLifecycle :one` — INSERT (started_at default NOW, trigger_reason, leader_replica) RETURNING id
       - `UpdateEmergencyLifecycleVastIDs :exec` — SET vast_offer_id, vast_instance_id, accepted_dph, events = events || sqlc.arg('event_json')::jsonb WHERE id = $1
       - `MarkEmergencyLifecycleHealthy :exec` — SET first_health_pass_at = NOW(), events = events || event_json WHERE id = $1
       - `CloseEmergencyLifecycle :exec` — SET ended_at = NOW(), shutdown_reason, total_cost_brl, events = events || event_json WHERE id = $1
       - `ListLiveEmergencyLifecycles :many` — SELECT (id, vast_offer_id, vast_instance_id, started_at, events) WHERE ended_at IS NULL (D-D5 leader recovery; partial unique index garante ≤1 row)
       - `GetMonthlyCostBRL :one` — SELECT COALESCE(SUM(total_cost_brl), 0)::numeric AS month_cost WHERE started_at >= date_trunc('month', NOW()) AND ended_at IS NOT NULL
       - `ListEmergencyLifecycles :many` — SELECT * (sem events JSONB) WHERE started_at >= $1 ORDER BY started_at DESC LIMIT $2 (gatewayctl emerg lifecycles)
    3. **gateway/sqlc.yaml:** append `- db/queries/emergency_lifecycles.sql` no array `queries:` (preservar ordering existente — adicionar como última entry).
    4. Rodar `cd /home/pedro/projetos/pedro/gpu-ifix/gateway && sqlc generate` para regenerar `internal/db/gen/emergency_lifecycles.sql.go` + atualizar `internal/db/gen/models.go` (struct `EmergencyLifecycle`). Não editar arquivos `gen/*` à mão — sqlc é a source.
    5. **NÃO** wirar migration no boot ainda — Phase 2 D-08 já tem `AI_GATEWAY_MIGRATE_ON_BOOT` flag genérico que aplica todas as migrations no diretório; nenhum opt-in adicional necessário.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && sqlc generate && go build ./internal/db/gen/...</automated>
  </verify>
  <done>
    - `0019_emergency_lifecycles.sql` existe com schema completo + 4 índices + `first_health_pass_at` column
    - `db/queries/emergency_lifecycles.sql` define 7 queries
    - `sqlc.yaml` lista `db/queries/emergency_lifecycles.sql`
    - `internal/db/gen/emergency_lifecycles.sql.go` gerado com 7 funções exportadas
    - `internal/db/gen/models.go` ganha struct `EmergencyLifecycle` com 11 fields
    - `cd gateway && go build ./...` exit 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — partial unique index + migration completeness</name>
  <files>gateway/internal/integration_test/emerg_singleton_test.go, gateway/internal/integration_test/migrate_emerg_test.go</files>
  <read_first>
    - gateway/internal/integration_test/setup_test.go (freshSchema helper Phase 4)
    - gateway/internal/integration_test/migrate_test.go (padrão de verificar tabela/coluna criada)
    - 06-PATTERNS.md seção 15 lines 1152-1166 (TestIntegration_EmergSingletonDBIndex template)
    - 06-VALIDATION.md tabela "Per-Task Verification Map" linhas 6-03-01 e 6-03-02
    - 06-CONTEXT.md decisão D-B5 (partial unique index spec)
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run TestMigration0019` exit 0 — verifica tabela criada com 11 colunas, 5 índices, COMMENT em first_health_pass_at
    - Test 2: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run TestEmergSingletonDBIndex` exit 0 — INSERT 1 row → ok; INSERT 2nd row com `ended_at IS NULL` → unique violation contendo string `"emergency_live_singleton"`
    - Test 3: Ambos os tests rodam sob `freshSchema(t, ctx)` (DB resetado antes; sem leak entre tests)
    - Test 4: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run TestEmerg` (prefix match) executa em < 30s
  </behavior>
  <action>
    1. **gateway/internal/integration_test/migrate_emerg_test.go (NOVO):** build tag `//go:build integration` na primeira linha. Test `TestMigration0019` que:
       - Chama `pool, _ := freshSchema(t, ctx)` (do setup_test.go Phase 4)
       - Query `SELECT column_name, data_type FROM information_schema.columns WHERE table_schema='ai_gateway' AND table_name='emergency_lifecycles' ORDER BY ordinal_position` — assert 11 colunas com nomes esperados (id, started_at, first_health_pass_at, ended_at, trigger_reason, vast_offer_id, vast_instance_id, accepted_dph, total_cost_brl, shutdown_reason, events, leader_replica)
       - Query `SELECT indexname FROM pg_indexes WHERE schemaname='ai_gateway' AND tablename='emergency_lifecycles' ORDER BY indexname` — assert ≥5 índices (PK + 4 created)
       - Query `SELECT col_description('ai_gateway.emergency_lifecycles'::regclass, ordinal_position) FROM information_schema.columns WHERE column_name='first_health_pass_at'` — assert COMMENT existe e referência D-D4
    2. **gateway/internal/integration_test/emerg_singleton_test.go (NOVO):** build tag `//go:build integration`. Test `TestEmergSingletonDBIndex` per 06-PATTERNS.md seção 15:
       - `freshSchema(t, ctx)` reset
       - `pool.Exec(ctx, "INSERT INTO ai_gateway.emergency_lifecycles (trigger_reason) VALUES ('manual_force')")` — `require.NoError`
       - 2nd `pool.Exec(ctx, "INSERT INTO ai_gateway.emergency_lifecycles (trigger_reason) VALUES ('manual_force')")` — `require.Error` + `require.Contains(t, err.Error(), "emergency_live_singleton")`
       - Adicional sanity: `pool.Exec(ctx, "UPDATE ai_gateway.emergency_lifecycles SET ended_at=NOW() WHERE id=(SELECT MIN(id) FROM ai_gateway.emergency_lifecycles)")` (close 1ª row) então 3ª INSERT deve agora succeed (single live invariant)
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race -tags=integration ./internal/integration_test/ -run "TestMigration0019|TestEmergSingletonDBIndex"</automated>
  </verify>
  <done>
    - 2 tests novos compilam e passam sob `-race -tags=integration`
    - Migration aplicada em testcontainers Postgres 16
    - Partial unique index rejeita 2nd live row
    - Tests cobrem PRV-05 (singleton) + PRV-10 (audit table existence)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciler → DB | INSERT em `emergency_lifecycles` é gated por leader-election (Plan 04); race conditions cobertas por D-B5 partial unique index (defesa em camadas) |
| DB → audit consumers | Phase 7 dashboard query expõe `events JSONB` — pode conter `vast_instance_id` etc. Sem PII direto, mas evidência de spend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-02 | Tampering / Race condition | INSERT 2 rows com `ended_at IS NULL` (split-brain) | mitigate | Partial unique index `emergency_live_singleton ON ((TRUE)) WHERE ended_at IS NULL` rejeita 2nd row; integration test prova; defesa em camadas com leader-election (Plan 04) primary |
| T-6-W2-01 | Information Disclosure | `events JSONB` pode crescer com payload sensível | accept | Phase 6 só armazena IDs Vast (não PII de tenant); convenção `events` deve ser opaque audit, não snapshot de request body |
| T-6-W2-02 | Tampering | DROP TABLE acidental via migration down | accept | `goose down` é destrutivo por design; operator NUNCA roda em prod sem backup; CASCADE limita blast radius à própria tabela |
</threat_model>

<verification>
- Migration aplica + reverte limpa: `cd gateway && goose -dir db/migrations postgres $URL up && goose -dir db/migrations postgres $URL down`
- sqlc generate sem warning: `cd gateway && sqlc generate`
- Build limpo: `cd gateway && go build ./...`
- Integration tests verdes: `cd gateway && go test -race -tags=integration ./internal/integration_test/ -run TestEmerg`
</verification>

<success_criteria>
- Migration 0019 aplica em testcontainers Postgres 16 + cria 11 colunas + 5 índices
- 7 sqlc queries Go funcionais em gen/
- Partial unique index bloqueia 2nd live lifecycle (PRV-05)
- COMMENT em `first_health_pass_at` referencia D-D4
- Integration tests passam sob `-race -tags=integration`
</success_criteria>

<output>
After completion, create `.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-02-SUMMARY.md`
</output>
