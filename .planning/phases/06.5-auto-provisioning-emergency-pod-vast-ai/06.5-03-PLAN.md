---
phase: 06-auto-provisioning-emergency-pod-vast-ai
plan: 03
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - gateway/internal/emerg/fsm.go
  - gateway/internal/emerg/fsm_test.go
  - gateway/internal/redisx/emerg.go
  - gateway/internal/redisx/emerg_test.go
autonomous: true
requirements: [PRV-02]
threat_refs: []
validates_sc: []
tags: [go, fsm, redis, atomic, mirror, emergency-pod]

must_haves:
  truths:
    - "FSM 7-estado in-process autoritativo (atomic.Int32 + atomic.Int64 enteredAt) com hot-path lockless via `State()` e `EnteredAt()`"
    - "Transitions via CAS (`compareAndSwap`); transitions inválidos são silently ignored (mesma semântica Phase 5 D-C1)"
    - "Cada transition emite (1) Sentry breadcrumb com Category=`emerg`, (2) slog.Info com from/to/reason, (3) onChange callback (caller-injected, usado pelo reconciler para publish/HSET/lifecycle audit), (4) obs.GatewayEmergencyState gauge update (1 no novo estado, 0 nos outros 6)"
    - "redisx/emerg.go expõe namespace constants `EmergEventsChannel = \"gw:emerg:events\"`, `EmergStateKey()`, `emergLockKey = \"gw:emerg:lock\"` + helpers `WriteEmergState`, `PublishEmergEvent`, `SubscribeEmergEvents`, `NewEmergRedsync` (factory wrapping `redsync.New(NewPool(rdb))`)"
    - "Redis ops timeout = 2s (`redisOpTimeout`, mesmo Phase 3+5 invariant); failures NÃO bloqueiam FSM (fail-soft + bumpa `obs.Gateway*MirrorFailures` se aplicável)"
  artifacts:
    - path: "gateway/internal/emerg/fsm.go"
      provides: "FSM 7-estado com atomic state + transition CAS + onChange callback"
      contains: "type State int32|StateHealthy|StateDegraded|StateFailedOver|StateEmergencyProvisioning|StateEmergencyActive|StateRecovering|StateCooldown|type FSM struct|CompareAndSwap|sentry.AddBreadcrumb"
    - path: "gateway/internal/emerg/fsm_test.go"
      provides: "TestFSMTransitions cobrindo 7 estados + transições válidas + onChange callback + obs.GatewayEmergencyState gauge"
      contains: "TestFSMTransitions|TestFSMOnChangeCallback"
    - path: "gateway/internal/redisx/emerg.go"
      provides: "Hash + Pub/Sub helpers + redsync pool factory"
      contains: "EmergEventsChannel|EmergStateKey|WriteEmergState|PublishEmergEvent|SubscribeEmergEvents|NewEmergRedsync|EmergEvent"
    - path: "gateway/internal/redisx/emerg_test.go"
      provides: "Test usando miniredis para round-trip Hash + Publish/Subscribe"
      contains: "TestWriteEmergState|TestPublishSubscribeEmergEvent"
  key_links:
    - from: "gateway/internal/emerg/fsm.go"
      to: "gateway/internal/obs/metrics.go GatewayEmergencyState"
      via: "obs.GatewayEmergencyState.WithLabelValues(state).Set(1) em transition"
      pattern: "obs\\.GatewayEmergencyState"
    - from: "gateway/internal/emerg/fsm.go"
      to: "github.com/getsentry/sentry-go"
      via: "sentry.AddBreadcrumb em transition"
      pattern: "sentry\\.AddBreadcrumb"
    - from: "gateway/internal/redisx/emerg.go"
      to: "github.com/go-redsync/redsync/v4"
      via: "NewEmergRedsync factory wraps redsync.New(NewPool(rdb))"
      pattern: "redsync\\.New|redsyncredis\\.NewPool"
---

<objective>
Construir as duas peças foundation que outros plans (04, 05, 06, 07, 08) consomem direto:
1. **FSM 7-estado** in-process autoritativo (atomic.Int32 + onChange callback) seguindo o padrão Phase 5 `internal/shed/fsm.go` exato — apenas expansão de 4 → 7 estados (BLOCKER 3 revisão: 9→7, OFF_HOURS+MAINTENANCE deferidos — ver CONTEXT `<deferred>`).
2. **`redisx/emerg.go`** com namespace constants + helpers Hash/Pub/Sub + factory `NewEmergRedsync` para wrappar `go-redsync/redsync v4` sem que callers tenham que importar `redsyncredis/v9`.

Purpose: FSM é o coração do reconciler (Plan 04+); `redisx/emerg.go` é o ponto único de entrada para Redis (mantém invariante "callers usam redisx, nunca go-redis direto"). Sem essas duas peças, Plan 04 (leader election) não compila.

Output: 4 arquivos novos: 2 source + 2 test. Nenhuma lógica funcional além de transition mechanics + Hash/Publish round-trip — wiring fica em Plan 04 (reconciler).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-CONTEXT.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-RESEARCH.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-PATTERNS.md

<interfaces>
<!-- Padrões existentes a replicar exato -->

De `gateway/internal/shed/fsm.go` (Phase 5 D-C1 — exact source para fsm.go):
  - lines 38-49: `type State int32` + `iota`-based enum
  - lines 110-138: `type FSM struct { state atomic.Int32; enteredAt atomic.Int64; cfg atomic.Pointer[Config]; onChange func(...); log *slog.Logger }`
  - lines 238-258: `transition(from, to State, now time.Time, reason string)` com CAS + obs gauge + slog.Info + onChange call

De `gateway/internal/redisx/breaker.go` (Phase 3 — 71 linhas integral, source para emerg.go layout):
  - lines 24-26: namespace consts `breakerEventsChannel = "gw:breaker:events"`
  - lines 28-36: `BreakerEvent struct` com JSON tags
  - lines 38-65: WriteBreakerState/PublishBreakerEvent/SubscribeBreakerEvents trio com 2s timeout

De `gateway/internal/redisx/shed.go` (Phase 5 — padrão expansion de breaker pattern):
  - lines 22-57: namespace constants block
  - lines 65-80: ShedEvent struct (composto + payload map)

De `gateway/internal/breaker/subscribe.go` (Phase 3 D-D1 — usado no reconciler em Plan 04):
  - lines 19-56: reconnect-with-backoff loop (Phase 6 vai replicar em emerg/subscribe.go Plan 04)

De `gateway/internal/obs/metrics.go` (Plan 06-01 já adicionou):
  - `var GatewayEmergencyState = promauto.NewGaugeVec(...)`

De `github.com/go-redsync/redsync/v4` (06-01 adicionou em go.mod):
  - `redsync.New(pool)` retorna `*redsync.Redsync`
  - `redsync/redis/goredis/v9.NewPool(rdb)` retorna pool compatível com go-redis v9
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FSM 7-estado em internal/emerg/fsm.go + unit tests</name>
  <files>gateway/internal/emerg/fsm.go, gateway/internal/emerg/fsm_test.go</files>
  <read_first>
    - gateway/internal/shed/fsm.go (LER LINE-BY-LINE — Phase 6 replica struct + transition CAS + onChange + State()/EnteredAt() com expansão 4→7 estados)
    - gateway/internal/shed/fsm_test.go (padrão de teste para CAS transitions + invalid transition silently ignored)
    - gateway/internal/obs/metrics.go (verificar GatewayEmergencyState foi criado em Plan 06-01)
    - 06-PATTERNS.md seção 1 (excerpt exato com line numbers shed/fsm.go)
    - 06-CONTEXT.md domain item 1 (lista os 7 estados na ordem após revisão BLOCKER 3 — OFF_HOURS+MAINTENANCE deferidos) + decisões D-B3 (tick 1s) + Section B
    - 06-RESEARCH.md "Pattern 1: 9-state FSM" (USAR APENAS os 7 estados ativos; ignorar StateOffHours/StateMaintenance — deferidos per BLOCKER 3) (lines 299-348 — código completo replicável)
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race ./internal/emerg/ -run TestFSMTransitions` exit 0
    - Test 2: TestFSMTransitions valida 7 estados via `s.String()` round-trip (StateHealthy→"healthy", StateDegraded→"degraded", StateFailedOver→"failed_over", StateEmergencyProvisioning→"emergency_provisioning", StateEmergencyActive→"emergency_active", StateRecovering→"recovering", StateCooldown→"cooldown")
    - Test 3: Test "TestFSMTransitionCAS" — 2 goroutines fazem CAS Healthy→Degraded simultâneo; só 1 vence; Estado final = Degraded; obs.GatewayEmergencyState.WithLabelValues("degraded") == 1, "healthy" == 0
    - Test 4: Test "TestFSMOnChangeCallback" — set onChange para capturar (from, to, reason); after `transition(StateHealthy, StateDegraded, now, "test_reason")`, callback foi chamada com argumentos corretos; sentry breadcrumbs adicionado (verificar via `sentry.CurrentHub().Scope().BreadcrumbsLen()` se disponível, ou mock injection)
    - Test 5: Test "TestFSMInvalidTransition" — `transition(StateActive, StateDegraded, now, "x")` quando state atual é StateHealthy é silently ignored (CAS falha); estado permanece StateHealthy; onChange NÃO é chamado
    - Test 6: Race detector limpo (`-race` flag) — sem data races em hot path `State()` paralelo + `transition`
  </behavior>
  <action>
    1. **gateway/internal/emerg/fsm.go (NOVO):** copiar literal o template de 06-PATTERNS.md seção 1 + 06-RESEARCH.md Pattern 1, com 7 estados (OFF_HOURS+MAINTENANCE não declarados — deferidos per BLOCKER 3 revisão):
       - `package emerg` + imports `("log/slog", "sync/atomic", "time", "fmt", "github.com/getsentry/sentry-go", "github.com/ifixtelecom/gpu-ifix/gateway/internal/obs")`
       - `type State int32` + 7 const iota: `StateHealthy=0, StateDegraded, StateFailedOver, StateEmergencyProvisioning, StateEmergencyActive, StateRecovering, StateCooldown` (ordem exata 06-CONTEXT.md domain item 1 após revisão BLOCKER 3 — 9-estado original reduzido para 7-estado)
       - `func (s State) String() string` com switch 7-cases retornando "healthy", "degraded", "failed_over", "emergency_provisioning", "emergency_active", "recovering", "cooldown" (default "unknown")
       - `type FSM struct { state atomic.Int32; enteredAt atomic.Int64; onChange func(from, to State, reason string); log *slog.Logger }` — Phase 6 NÃO precisa de `cfg atomic.Pointer[Config]` (config vem de env, sem hot-reload)
       - Constructor `func NewFSM(log *slog.Logger, onChange func(from, to State, reason string)) *FSM` — initial state StateHealthy, enteredAt=now.Unix()
       - `func (f *FSM) State() State { return State(f.state.Load()) }` — lockless hot-path
       - `func (f *FSM) EnteredAt() time.Time { return time.Unix(f.enteredAt.Load(), 0) }`
       - `func (f *FSM) Transition(from, to State, now time.Time, reason string)` (PUBLIC method — caller é reconciler) — MAS internamente chama `transition(from, to, now, reason)` que faz:
         - `if from == to { return }`
         - `if !f.state.CompareAndSwap(int32(from), int32(to)) { return }`
         - `f.enteredAt.Store(now.Unix())`
         - Set `obs.GatewayEmergencyState.WithLabelValues(to.String()).Set(1)` + `for _, s := range allStates if s != to { obs.GatewayEmergencyState.WithLabelValues(s.String()).Set(0) }`
         - `f.log.Info("emerg FSM transition", "from", from.String(), "to", to.String(), "reason", reason, "at", now.Format(time.RFC3339))`
         - `sentry.AddBreadcrumb(&sentry.Breadcrumb{Category: "emerg", Message: fmt.Sprintf("state %s→%s", from, to), Level: sentry.LevelInfo, Timestamp: now, Data: map[string]interface{}{"reason": reason}})`
         - `if f.onChange != nil { f.onChange(from, to, reason) }`
       - NÃO incluir `Evaluate(now)` mega-function ainda — Plan 04 (reconciler) cuida da lógica de transição. Este arquivo SÓ tem mechanics de transition + state read.
    2. **gateway/internal/emerg/fsm_test.go (NOVO):** unit tests cobrindo:
       - TestFSMTransitions — testar todas as 9 strings; testar transition Healthy→Degraded→FailedOver→EmergencyProvisioning→EmergencyActive→Recovering→Cooldown valida CAS sucesso e enteredAt updated
       - TestFSMTransitionCAS — 2 goroutines `go func() { fsm.Transition(StateHealthy, StateDegraded, now, "race") }()` × 2; assert apenas 1 chamada de onChange (CAS garante idempotência)
       - TestFSMOnChangeCallback — capture callback args via channel; assert from/to/reason corretos
       - TestFSMInvalidTransition — set state=Healthy; chamar `Transition(StateActive, StateDegraded, ...)`; assert state remains Healthy + onChange NÃO chamada
       - TestFSMObsGaugeReset — após Transition, todos os 7 gauges são checados: 1 no novo, 0 nos outros 6
       Usar `-race` flag (rodado pelo verify automated abaixo).
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race ./internal/emerg/ -run TestFSM</automated>
  </verify>
  <done>
    - `internal/emerg/fsm.go` exporta `State`, 7 const StateXxx, FSM struct, NewFSM, State(), EnteredAt(), Transition()
    - 5 unit tests passam sob `-race`
    - obs.GatewayEmergencyState gauge correto após transition
    - sentry.AddBreadcrumb chamado em cada transition (TestFSMOnChange verifica via mock se necessário)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: redisx/emerg.go — namespace + Hash/Pub/Sub helpers + redsync factory + tests com miniredis</name>
  <files>gateway/internal/redisx/emerg.go, gateway/internal/redisx/emerg_test.go</files>
  <read_first>
    - gateway/internal/redisx/breaker.go (LER LINE-BY-LINE — 71 linhas integral; replicar layout para emerg.go)
    - gateway/internal/redisx/breaker_test.go (padrão miniredis test — verificar HSet round-trip + Publish/Subscribe)
    - gateway/internal/redisx/shed.go (padrão event struct mais elaborado lines 65-80)
    - 06-PATTERNS.md seção 4 (excerpt completo de redisx/emerg.go com line numbers analog)
    - 06-CONTEXT.md decisão D-B1 (Redis namespace spec) + D-B2 (redsync TTL/renew)
    - 06-RESEARCH.md "go-redsync/redsync v4 Patterns" lines 782-880 (Construction + signature + recommended pattern)
    - 06-RESEARCH.md "Pattern 3: Cross-replica mirror (Pub/Sub + Hash + reconnect-with-backoff)" lines 404-444
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race ./internal/redisx/ -run TestEmerg` exit 0 (miniredis-backed)
    - Test 2: TestWriteEmergState — call `WriteEmergState(ctx, rdb, "emergency_active", "42", "http://1.2.3.4:9100", "12345", time.Now().Unix())`; HGetAll retorna 5 fields (state, lifecycle_id, pod_url, pod_instance_id, entered_at) com valores exatos
    - Test 3: TestPublishSubscribeEmergEvent — Subscribe → Publish `EmergEvent{Type:"transition", State:"healthy", LifecycleID:0, ReplicaID:"test-host"}` → assert recebido em < 1s + JSON unmarshal correto
    - Test 4: TestNewEmergRedsync — `NewEmergRedsync(rdb)` retorna `*redsync.Redsync` não-nil; `mutex := rs.NewMutex(emergLockKey, redsync.WithExpiry(30*time.Second))`; `mutex.LockContext(ctx)` em miniredis funciona (retorna nil error)
    - Test 5: 2s timeout enforced — quando rdb wraps slow operation (simulado), context.DeadlineExceeded é retornado em vez de bloquear indefinido
    - Test 6: Constants exportadas: `EmergEventsChannel = "gw:emerg:events"`, `EmergStateKey() == "gw:emerg:state"` — verificável via grep
  </behavior>
  <action>
    1. **gateway/internal/redisx/emerg.go (NOVO):** seguir 06-PATTERNS.md seção 4 lines 309-379 com adaptações:
       - `package redisx` + imports `("context", "encoding/json", "fmt", "time", "github.com/go-redsync/redsync/v4", redsyncredis "github.com/go-redsync/redsync/v4/redis/goredis/v9", "github.com/redis/go-redis/v9")`
       - Const block:
         - `EmergEventsChannel = "gw:emerg:events"` (capitalized — exportada)
         - `emergStateKeyPrefix = "gw:emerg:"` (lowercase — internal)
         - `emergLockKey = "gw:emerg:lock"` (lowercase; exposto via `EmergLockKey()` getter para callers que precisam — ex: gatewayctl)
         - NÃO redeclarar `redisOpTimeout` — já existe em `breaker.go` como package-level const (`= 2 * time.Second`); reusar.
       - `func EmergStateKey() string { return emergStateKeyPrefix + "state" }` (chave única — Hash com 5 fields, NÃO 1 hash por upstream como shed)
       - `func EmergLockKey() string { return emergLockKey }`
       - `type EmergEvent struct { Type string; State string; LifecycleID int64; Reason string; SinceUnix int64; ReplicaID string; Payload map[string]any }` com JSON tags conforme 06-PATTERNS.md seção 4 lines 335-343
       - `func WriteEmergState(ctx context.Context, rdb *redis.Client, state, lifecycleID, podURL, podInstanceID string, enteredUnix int64) error` — 2s timeout, HSet 5 fields conforme 06-PATTERNS.md
       - `func PublishEmergEvent(ctx context.Context, rdb *redis.Client, ev EmergEvent) error` — 2s timeout, Publish JSON
       - `func SubscribeEmergEvents(ctx context.Context, rdb *redis.Client) *redis.PubSub` — caller owns Close
       - `func NewEmergRedsync(rdb *redis.Client) *redsync.Redsync { pool := redsyncredis.NewPool(rdb); return redsync.New(pool) }` (factory wrapping para callers não importarem redsyncredis direto)
       - Reusar fail-fast `if rdb == nil { return fmt.Errorf("redisx: nil client") }` per breaker.go pattern
    2. **gateway/internal/redisx/emerg_test.go (NOVO):** usar `github.com/alicebob/miniredis/v2` (já em go.mod) para in-memory Redis. Tests:
       - TestWriteEmergState_RoundTrip — start miniredis, redis.NewClient(addr), call WriteEmergState, HGetAll retorna 5 fields esperados
       - TestPublishSubscribeEmergEvent_RoundTrip — Subscribe primeiro, Publish event, Channel() entrega payload em < 1s, json.Unmarshal valida struct
       - TestNewEmergRedsync_LockUnlock — `rs := NewEmergRedsync(rdb)`, `mtx := rs.NewMutex(EmergLockKey(), redsync.WithExpiry(5*time.Second))`, `err := mtx.LockContext(ctx)` retorna nil; `ok, err := mtx.UnlockContext(ctx)` retorna (true, nil)
       - TestRedisOpTimeout_Enforced — context com `WithTimeout(0*time.Second)` retorna DeadlineExceeded sem hang
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race ./internal/redisx/ -run TestEmerg</automated>
  </verify>
  <done>
    - `internal/redisx/emerg.go` exporta 7 símbolos públicos (EmergEventsChannel, EmergStateKey, EmergLockKey, EmergEvent, WriteEmergState, PublishEmergEvent, SubscribeEmergEvents, NewEmergRedsync)
    - 4 unit tests passam com miniredis sob `-race`
    - `cd gateway && go build ./...` exit 0 (verifica que outros packages que importam redisx não quebraram)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FSM transition → onChange callback | Callback é injetada pelo reconciler (Plan 04); FSM não confia em payloads externos — apenas invoca |
| Redis client → emerg state Hash | Hash é cross-replica observable; non-leader réplicas READ-only para visualização (gatewayctl) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-W3-01 | Tampering / Race | Multiple goroutines mutate FSM state | mitigate | atomic.Int32 + CompareAndSwap garante atomicidade; CAS-failure = silently noop (mesmo padrão Phase 5 D-C1) |
| T-6-W3-02 | DoS | Slow Redis bloqueia FSM hot path | mitigate | redisOpTimeout = 2s; falhas não propagam para FSM (fire-and-log, FSM continua operando in-process) |
| T-6-W3-03 | Information Disclosure | EmergEvent.Payload é `map[string]any` — pode logar dados sensíveis se caller passar PII | accept | Convenção: Payload contém só IDs Vast (offer_id, instance_id), nunca request body; revisão code-review |
</threat_model>

<verification>
- FSM tests: `cd gateway && go test -race ./internal/emerg/ -run TestFSM`
- redisx tests: `cd gateway && go test -race ./internal/redisx/ -run TestEmerg`
- Full build: `cd gateway && go build ./...`
</verification>

<success_criteria>
- 7 estados FSM declarados na ordem exata 06-CONTEXT.md
- Transition usa CompareAndSwap + onChange + obs gauge + sentry breadcrumb
- redisx/emerg.go expõe 7 símbolos para callers (Plan 04+)
- Testes passam sob `-race` (sem data races)
- Build limpo
</success_criteria>

<output>
After completion, create `.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-03-SUMMARY.md`
</output>
