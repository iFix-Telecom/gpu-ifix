---
phase: 06-auto-provisioning-emergency-pod-vast-ai
plan: 10
type: execute
wave: 7
depends_on: [06-08]
files_modified:
  - gateway/cmd/gatewayctl/emerg.go
  - gateway/cmd/gatewayctl/emerg_test.go
  - gateway/cmd/gatewayctl/main.go
  - gateway/cmd/gateway/main.go
autonomous: true
requirements: [PRV-08, PRV-10]
threat_refs: []
validates_sc: []
tags: [go, gatewayctl, cli, main-wiring, operator-tools]

must_haves:
  truths:
    - "gatewayctl emerg dispatcher implementa 4 subcomandos FUNCIONAIS (D-E1, BLOCKER 2 revisão 2026-05-13): `state` (read Redis Hash gw:emerg:state + JSON|table format), `force-provision` (publish typed `EmergEvent{Type:\"force_provision_request\"}` to `gw:emerg:events`; reconciler subscriber consumes leader-only e dispara `HEALTHY → EMERGENCY_PROVISIONING` com audit `trigger_reason=\"manual_force\"`), `force-destroy` (publish typed `EmergEvent{Type:\"force_destroy_request\"}`; reconciler consumes leader-only e dispara `EMERGENCY_ACTIVE → COOLDOWN + vast.DestroyInstance` com audit `shutdown_reason=\"manual\"`), `lifecycles` (query DB com --since duration + --limit + --format flag)"
    - "force-provision audit `trigger_reason='manual_force'`"
    - "force-destroy audit `shutdown_reason='manual'`"
    - "force-* commands rejeitam quando reconciler local NÃO é leader (graceful exit code 3 com msg 'not the leader replica; run on leader')"
    - "main.go (gateway server) wireia: NewReconciler com Deps completo + go reconciler.Run(ctx) + dispatcher Loader Override hook + boot validation `vast.Client.Ping(ctx)` se VastAIAPIKey != \"\" (D-A5; non-fatal warning if missing)"
    - "Test unit gatewayctl emerg state — formata JSON output corretamente; tests command parsing edge cases"
    - "Test unit `TestEmergForceProvisionPublishesEvent` — runEmergForceProvision publishes EmergEvent{Type:\"force_provision_request\"} to gw:emerg:events (miniredis subscribe assertion)"
    - "Test unit `TestEmergForceDestroyPublishesEvent` — runEmergForceDestroy publishes EmergEvent{Type:\"force_destroy_request\"} to gw:emerg:events"
    - "Test integration extension covered in Plan 06-05 (Task 3 added in revision): `TestEmergReconcilerHandlesForceProvisionEvent` — leader-only consumption + transition HEALTHY→EMERGENCY_PROVISIONING after publishing force_provision_request event"
    - "main.go integration test (smoke): build binary + run com env vars válidas + assert `/health` retorna 200 + reconciler goroutine não panicked após 5s"
  artifacts:
    - path: "gateway/cmd/gatewayctl/emerg.go"
      provides: "runEmerg dispatcher + 4 subcomando implementations + format flags"
      contains: "func runEmerg|runEmergState|runEmergForceProvision|runEmergForceDestroy|runEmergLifecycles|tabwriter"
    - path: "gateway/cmd/gatewayctl/emerg_test.go"
      provides: "Unit tests para command parsing + JSON output structure"
      contains: "TestEmergState_Format|TestEmergLifecycles_Filters"
    - path: "gateway/cmd/gatewayctl/main.go"
      provides: "Wire emerg subcommand no top-level switch"
      contains: "case \"emerg\""
    - path: "gateway/cmd/gateway/main.go"
      provides: "Wire emerg.NewReconciler + go reconciler.Run + dispatcher hook + Vast Ping at boot"
      contains: "emerg.NewReconciler|emerg.Reconciler|reconciler.Run|vast.NewClient|reconciler.SetLoaderHook"
  key_links:
    - from: "gateway/cmd/gateway/main.go"
      to: "gateway/internal/emerg/reconciler.go"
      via: "rcl := emerg.NewReconciler(emerg.Deps{...}); go rcl.Run(ctx); go rcl.Subscribe(ctx)"
      pattern: "emerg\\.NewReconciler|reconciler\\.Run"
    - from: "gateway/cmd/gateway/main.go"
      to: "gateway/internal/emerg/vast/client.go Ping"
      via: "if cfg.VastAIAPIKey != \"\" { vast.Ping(ctx) }"
      pattern: "vast\\.Ping|VastAIAPIKey"
    - from: "gateway/cmd/gatewayctl/emerg.go runEmergLifecycles"
      to: "gateway/internal/db/gen ListEmergencyLifecycles"
      via: "q.ListEmergencyLifecycles(ctx, params{since, limit})"
      pattern: "ListEmergencyLifecycles"
---

<objective>
Entregar (a) 4 gatewayctl subcomandos (D-E1) — interface operator para inspeção + override manual, e (b) wiring completo em main.go (gateway server) — Reconciler ganha vida ao boot. Sem este plan, Phase 6 está toda implementada mas inerte: gateway não inicia o reconciler; operator não tem como inspecionar state ou forçar action.

Purpose: Este plan faz Phase 6 ir live. main.go wiring é prerequisite para Plan 11 (HUMAN-UAT). gatewayctl é prerequisite para LIVE UAT scenarios (UAT-1..UAT-5 documentados em VALIDATION).

Output: 1 source novo (gatewayctl/emerg.go) + 1 test + 2 modificações (gatewayctl/main.go switch + gateway/main.go wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-CONTEXT.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-RESEARCH.md
@.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-PATTERNS.md

<interfaces>
<!-- Existing patterns to mirror -->

De `gateway/cmd/gatewayctl/upstreams.go` (Phase 3 D-G1 — exact source para estrutura de subcomando dispatcher + tabwriter pattern):
  - `func runUpstreams(ctx, args, log)` switch por subcommand
  - flag.NewFlagSet + ContinueOnError
  - tabwriter.NewWriter para table format

De `gateway/cmd/gatewayctl/shed.go` (Phase 5 — Redis-only state read pattern):
  - `runShedState` reads Redis HGetAll + format JSON|table

De `gateway/cmd/gatewayctl/main.go`:
  - top-level switch case por subcommand name; adicionar `case "emerg": runEmerg(ctx, args[1:], log)`

De `gateway/cmd/gateway/main.go` (Phase 5 wiring patterns lines 90-410):
  - shed.NewSet + go shedSet.ReconcileLoop(ctx, ...) + go shedSet.Run* (4 goroutines wired)
  - Phase 6 segue mesma estrutura: NewReconciler + go Run + go Subscribe (2 goroutines)

De `gateway/internal/db/gen/emergency_lifecycles.sql.go` (Plan 02):
  - q.ListEmergencyLifecycles(ctx, params)

De `gateway/internal/emerg/` (Plans 03-09):
  - emerg.NewReconciler + emerg.Deps struct
  - emerg.NewFSM constructor

De `gateway/internal/emerg/vast/client.go` (Plan 06):
  - vast.NewClient(apiKey) + Ping(ctx)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gatewayctl/emerg.go — 4 subcomandos com tabwriter + JSON format flag + unit tests</name>
  <files>gateway/cmd/gatewayctl/emerg.go, gateway/cmd/gatewayctl/emerg_test.go, gateway/cmd/gatewayctl/main.go</files>
  <read_first>
    - gateway/cmd/gatewayctl/upstreams.go (LER LINE-BY-LINE — 4 subcomandos pattern + dispatcher + flag parsing + tabwriter output)
    - gateway/cmd/gatewayctl/shed.go (Redis HGetAll read pattern para state subcommand)
    - gateway/cmd/gatewayctl/main.go (switch por subcommand name)
    - gateway/internal/redisx/emerg.go (Plan 03 — EmergStateKey + EmergEventsChannel + PublishEmergEvent)
    - gateway/internal/db/gen/emergency_lifecycles.sql.go (Plan 02 — ListEmergencyLifecycles signature)
    - 06-PATTERNS.md seção 14 lines 974-1046 (skeleton emerg.go com 4 subcomandos)
    - 06-CONTEXT.md decisão D-E1 (4 subcomandos spec)
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go test -race ./cmd/gatewayctl/... -run TestEmerg` exit 0
    - Test 2: `cd gateway && go build -o /tmp/gatewayctl ./cmd/gatewayctl/` exit 0
    - Test 3: `/tmp/gatewayctl emerg` (sem subcomando) imprime usage e exit code 2
    - Test 4: `/tmp/gatewayctl emerg unknown` exit code 2 + stderr msg "unknown subcommand: unknown"
    - Test 5: `/tmp/gatewayctl emerg state --format=json` (com env vars válidas para Redis) imprime JSON valid (mesmo se Redis Hash vazio — `{}` ou `{"state":""}`)
    - Test 6: `/tmp/gatewayctl emerg lifecycles --since 24h --limit 10 --format=table` imprime tabwriter output (header: ID, STARTED, ENDED, TRIGGER, VAST_ID, DPH, COST_BRL, REASON, REPLICA)
    - Test 7: `/tmp/gatewayctl emerg force-provision --reason "smoke"` publishes EmergEvent{Type:"force_provision_request"} to `gw:emerg:events` (verifiable via miniredis Subscribe in unit test) + prints msg "force-provision request published; reconciler tick (~1s) consumes event and starts provisioning" + exit 0. gatewayctl é client (não leader checker); reconciler subscriber (Plan 06-05 Task 3 extension) faz a leader-only filtering antes de agir. (Spec corrigido em BLOCKER 2 revisão 2026-05-13 — funcional, NÃO placeholder.)
    - Test 8: `/tmp/gatewayctl emerg force-destroy` publishes EmergEvent{Type:"force_destroy_request"} + prints msg "force-destroy request published" + exit 0
    - Test 9: TestEmergForceProvisionPublishesEvent — miniredis subscribe gw:emerg:events; call runEmergForceProvision; assert event recebido com Type="force_provision_request" + Reason==*reason flag value
    - Test 10: TestEmergForceDestroyPublishesEvent — equivalente para Type="force_destroy_request"
  </behavior>
  <action>
    1. **gateway/cmd/gatewayctl/emerg.go (NOVO):** seguir 06-PATTERNS.md seção 14:
       ```
       package main

       import (
           "context"
           "encoding/json"
           "errors"
           "flag"
           "fmt"
           "log/slog"
           "os"
           "text/tabwriter"
           "time"

           "github.com/jackc/pgx/v5/pgtype"
           "github.com/redis/go-redis/v9"

           "github.com/ifixtelecom/gpu-ifix/gateway/internal/config"
           gen "github.com/ifixtelecom/gpu-ifix/gateway/internal/db/gen"
           "github.com/ifixtelecom/gpu-ifix/gateway/internal/redisx"
       )

       func runEmerg(ctx context.Context, args []string, log *slog.Logger) int {
           if len(args) == 0 {
               fmt.Fprintln(os.Stderr, "Usage: gatewayctl emerg state|force-provision|force-destroy|lifecycles [flags]")
               return 2
           }
           switch args[0] {
           case "state":           return runEmergState(ctx, args[1:], log)
           case "force-provision": return runEmergForceProvision(ctx, args[1:], log)
           case "force-destroy":   return runEmergForceDestroy(ctx, args[1:], log)
           case "lifecycles":      return runEmergLifecycles(ctx, args[1:], log)
           default:
               fmt.Fprintf(os.Stderr, "unknown subcommand: %s\n", args[0])
               return 2
           }
       }
       ```

       Sub-handlers:

       **runEmergState:** flag --format=json|table (default table). Conecta Redis (helper `loadAndRedis(ctx, log)` reusar de upstreams.go). `m, err := rdb.HGetAll(ctx, redisx.EmergStateKey()).Result()`. Format:
       - JSON: `json.NewEncoder(os.Stdout).SetIndent("", "  "); enc.Encode(m)`
       - Table: tabwriter 2-col: `Key\tValue\n` + `state\tactive\n` etc.

       **runEmergForceProvision:** flag --reason. Connect Redis. Publish:
       ```go
       ev := redisx.EmergEvent{
           Type: "force_provision_request",
           Reason: *reason,
           SinceUnix: time.Now().Unix(),
           ReplicaID: "gatewayctl",
       }
       _ = redisx.PublishEmergEvent(ctx, rdb, ev)
       fmt.Println("force-provision request published; check `gatewayctl emerg state` next tick to see effect")
       ```
       **CRÍTICO (BLOCKER 2 revisão 2026-05-13 — D-E1 functional, NÃO placeholder):** force-provision MUST publish a typed `EmergEvent{Type:"force_provision_request", Reason: *reason, ReplicaID:"gatewayctl", SinceUnix: time.Now().Unix()}` event to `gw:emerg:events`. Reconciler subscriber (extended in Plan 06-05 Task 1, see "Subscribe ALSO consumes gw:emerg:events" addition) consumes this event leader-only and triggers FSM transition `HEALTHY → EMERGENCY_PROVISIONING` with audit `trigger_reason='manual_force'`. gatewayctl returns exit 0 + msg "force-provision request published; reconciler tick (~1s) consumes event and starts provisioning. Run `gatewayctl emerg state` to confirm transition."

       **runEmergForceDestroy:** mesmo padrão — publish `EmergEvent{Type:"force_destroy_request", Reason: "manual", ReplicaID:"gatewayctl", SinceUnix: time.Now().Unix()}`. Reconciler subscriber (Plan 06-05 + Plan 06-08 extensions) consumes leader-only: triggers `EMERGENCY_ACTIVE → COOLDOWN + vast.DestroyInstance` with audit `shutdown_reason='manual'`. (If not in EMERGENCY_ACTIVE/RECOVERING, command logs warning + becomes no-op; gatewayctl prints "no live emergency lifecycle to destroy".

       **runEmergLifecycles:** flags --since=7d (parse via `time.ParseDuration` se "7d" — usar custom parser pois `7d` não é Go duration; aceitar `168h` OR criar helper `parseDurationDays(s string) (time.Duration, error)` que aceita "7d"/"24h"/etc), --limit=50, --format=table|json. Connect DB pool (`loadAndPool(ctx, log)` helper reusar). `q := gen.New(pool)`. `since := time.Now().Add(-duration)`. `rows, err := q.ListEmergencyLifecycles(ctx, gen.ListEmergencyLifecyclesParams{StartedAt: pgtype.Timestamptz{Time: since, Valid: true}, Limit: int32(*limit)})`. Format:
       - Table: tabwriter cols `ID\tSTARTED\tENDED\tTRIGGER\tVAST_ID\tDPH\tCOST_BRL\tREASON\tREPLICA\n`
       - JSON: `json.NewEncoder(os.Stdout).SetIndent("", "  "); enc.Encode(rows)`

    2. **gateway/cmd/gatewayctl/main.go MODIFY:** adicionar case `"emerg":` no top-level switch (próximo a "shed", "upstreams" etc.):
       ```
       case "emerg":
           os.Exit(runEmerg(ctx, args[1:], log))
       ```

    3. **gateway/cmd/gatewayctl/emerg_test.go (NOVO):** unit tests:
       - TestEmergUsage_NoArgs — call `runEmerg(ctx, []string{}, log)` retorna 2 + stderr usage
       - TestEmergUnknownSubcommand — `runEmerg(ctx, []string{"foo"}, log)` retorna 2
       - TestEmergState_Format_JSON — usar miniredis, pre-popular HSet, run runEmergState com --format=json, capture stdout (testing.T helper redirect), validate JSON
       - TestEmergLifecycles_FilterParser — testar `parseDurationDays("7d")` retorna 168h; `parseDurationDays("24h")` retorna 24h; etc.
       - TestEmergForceProvision_PublishesEvent — miniredis subscribe gw:emerg:events; runEmergForceProvision; assert event recebido com Type="force_provision_request"
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -race ./cmd/gatewayctl/... -run TestEmerg && go build -o /tmp/gatewayctl ./cmd/gatewayctl/</automated>
  </verify>
  <done>
    - 4 subcomandos FUNCIONAIS (state, force-provision PUBLISHES typed event, force-destroy PUBLISHES typed event, lifecycles)
    - tabwriter + JSON formats
    - parseDurationDays helper para --since="7d"
    - main.go switch dispatch
    - 7+ unit tests passam (incl. TestEmergForceProvisionPublishesEvent + TestEmergForceDestroyPublishesEvent)
    - Binary build limpo
    - BLOCKER 2 (revisão 2026-05-13) resolved: force-* commands são FUNCIONAIS, NÃO placeholder logging-only
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: main.go (gateway server) wiring — NewReconciler + 2 goroutines + Vast Ping at boot</name>
  <files>gateway/cmd/gateway/main.go</files>
  <read_first>
    - gateway/cmd/gateway/main.go (LER linhas relevantes — Phase 5 wiring de shed.NewSet + go shedSet.ReconcileLoop)
    - gateway/internal/emerg/reconciler.go (Plan 04 + posteriores — NewReconciler + Deps struct + Run + Subscribe)
    - gateway/internal/emerg/vast/client.go (Plan 06 — NewClient + Ping)
    - gateway/internal/upstreams/loader.go (Plan 08 — OverrideTier0/RestoreTier0 já existem)
    - 06-CONTEXT.md decisão D-A5 (Vast Ping at boot)
    - 06-PATTERNS.md "File Classification" linha `gateway/cmd/gateway/main.go` (modificação)
  </read_first>
  <behavior>
    - Test 1: `cd gateway && go build -o /tmp/gateway ./cmd/gateway/` exit 0
    - Test 2: smoke test (em integration_test/gateway_e2e_test.go talvez já existe — adicionar caso emerg) — `binary --health` retorna 200 + log indica "emerg reconciler started"
    - Test 3: `cd gateway && go test ./cmd/gateway/...` (existing main_test.go) ainda passa (não regredido)
    - Test 4: Boot com `VAST_AI_API_KEY=""` (vazio) → log Warn "Vast.ai API key não configurada; emerg reconciler desabilitado" + reconciler NÃO inicia goroutines (graceful skip per RESEARCH "Environment Availability" — sem fail-loud)
    - Test 5: Boot com `VAST_AI_API_KEY="invalid"` → vast.Ping retorna error 401 → log Warn (NÃO crash); reconciler continua (operator pode corrigir env e restart)
    - Test 6: Boot success com key válida (mockVast) → log Info "vast.Ping ok; emerg reconciler started"
  </behavior>
  <action>
    Editar `gateway/cmd/gateway/main.go` adicionando bloco "// Phase 6 — emergency pod reconciler" após bloco shed Phase 5:

    1. **Imports adicionar:**
       ```
       "github.com/ifixtelecom/gpu-ifix/gateway/internal/emerg"
       "github.com/ifixtelecom/gpu-ifix/gateway/internal/emerg/vast"
       ```

    2. **Após carregar config + criar pool/rdb/upstreamsLoader, adicionar bloco:**
       ```
       // Phase 6 — emergency pod reconciler (auto-provisioning Vast.ai)
       var emergReconciler *emerg.Reconciler
       if cfg.VastAIAPIKey == "" {
           log.Warn("Vast.ai API key não configurada (VAST_AI_API_KEY); Phase 6 emergency reconciler desabilitado")
       } else {
           vastClient := vast.NewClient(cfg.VastAIAPIKey)
           // Boot validation per D-A5: vast.Ping; non-fatal warning se inválido
           pingCtx, pingCancel := context.WithTimeout(ctx, 30*time.Second)
           if err := vastClient.Ping(pingCtx); err != nil {
               log.Warn("vast.Ping falhou no boot; emergency reconciler ainda inicia mas pode falhar em runtime", "err", err)
           } else {
               log.Info("vast.Ping ok")
           }
           pingCancel()

           emergFSM := emerg.NewFSM(log, nil)  // onChange será setado depois com publish to Pub/Sub
           emergReconciler = emerg.NewReconciler(emerg.Deps{
               DB:           pool,
               Redis:        rdb,
               FSM:          emergFSM,
               Vast:         vastClient,
               Loader:       upstreamsLoader,  // (Plan 08 expõe OverrideTier0/RestoreTier0)
               Cfg:          *cfg,
               TickInterval: 1 * time.Second,
               Log:          log,
           })
           // Wire FSM onChange para publish em gw:emerg:events (cross-replica visibility)
           emergFSM.SetOnChange(func(from, to emerg.State, reason string) {
               // (helper a adicionar em emerg/fsm.go: SetOnChange(callback))
               ev := redisx.EmergEvent{
                   Type: "transition", State: to.String(),
                   Reason: reason, SinceUnix: time.Now().Unix(),
                   ReplicaID: emergReconciler.ReplicaID(),
               }
               _ = redisx.PublishEmergEvent(context.Background(), rdb, ev)
               // Best-effort HSet mirror em gw:emerg:state
               lc := emergReconciler.ActiveLifecycle()
               podURL, _ := emergReconciler.ActivePodURL()
               podInstanceID := ""
               lifecycleID := ""
               if lc != nil {
                   lifecycleID = strconv.FormatInt(lc.ID, 10)
               }
               _ = redisx.WriteEmergState(context.Background(), rdb, to.String(), lifecycleID, podURL, podInstanceID, time.Now().Unix())
           })
           // Spawn reconciler + subscriber goroutines
           go emergReconciler.Run(ctx)
           go emergReconciler.Subscribe(ctx)
           log.Info("emergency pod reconciler started")
       }
       ```
       Notas de implementação:
       - `emerg.FSM` precisa de método `SetOnChange(cb)` se Plan 03 não criou — adicionar como side-task aqui
       - `emergReconciler.ActiveLifecycle()` getter para acessar `r.activeLifecycle.Load()` — adicionar como side-task
       - `Loader` field em Deps adicionado em Plan 08 — confirmar antes de wire

    3. **Em shutdown (defer), garantir reconciler ctx é cancelled corretamente** — já coberto pelo `ctx` global; goroutines respeitam ctx.Done() per Plan 04.

    4. **NÃO modificar dispatcher.go aqui** — Plan 08 já fez (RegisterTraffic hook + audit_log).
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build -o /tmp/gateway ./cmd/gateway/ && go test ./cmd/gateway/...</automated>
  </verify>
  <done>
    - main.go wira NewReconciler + go Run + go Subscribe + FSM SetOnChange para Pub/Sub mirror
    - Vast.Ping at boot (warning não-fatal se falhar)
    - Empty VAST_AI_API_KEY gracefully desabilita reconciler
    - Build limpo
    - Test main.go ainda passa (não regredido)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → gatewayctl | CLI executa via `docker exec ifix-ai-gateway /gatewayctl ...`; access controlado por SSH ao host (IFIX ops) |
| gatewayctl → Redis | Publish de force_*_request events; Reconciler decide (leader-only) — gatewayctl não tem authority direto |
| main.go → Vast.Ping | Boot-time HTTPS call; falhas são non-fatal warnings (não crashloop o gateway) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-W10-01 | Tampering | Operator pode publish event direto via redis-cli (não via gatewayctl) | accept | Pub/Sub é trusted internal channel; convention é usar gatewayctl; auditável via Redis MONITOR |
| T-6-W10-02 | Information Disclosure | gatewayctl emerg lifecycles --format=json revela total_cost_brl, vast_offer_id | accept | Acesso é gated por `docker exec ifix-ai-gateway` — IFIX ops only; sem PII |
| T-6-W10-03 | DoS | Vast.Ping at boot bloqueia 30s se Vast lento | accept | non-fatal; gateway main.go continua boot mesmo com warning |
</threat_model>

<verification>
- gatewayctl tests: `cd gateway && go test -race ./cmd/gatewayctl/... -run TestEmerg`
- gateway build: `cd gateway && go build -o /tmp/gateway ./cmd/gateway/`
- Existing tests: `cd gateway && go test ./cmd/gateway/...`
- Full suite: `cd gateway && go test -race ./...`
</verification>

<success_criteria>
- gatewayctl emerg state|force-provision|force-destroy|lifecycles funcional
- main.go wira reconciler + 2 goroutines + Vast Ping
- VAST_AI_API_KEY="" gracefully disable reconciler
- Build limpo
- Existing tests não regredidos
</success_criteria>

<output>
After completion, create `.planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-10-SUMMARY.md`
</output>
