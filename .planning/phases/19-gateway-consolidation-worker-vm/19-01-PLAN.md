---
phase: 19-gateway-consolidation-worker-vm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "worker-vm:docker-network/ai-gateway (overlay, external)"
  - "worker-vm:recon-notes (Portainer swarm-string route + internal Traefik routing)"
autonomous: true
requirements: [NET-01, UI-01, CONS-01]
user_setup: []

must_haves:
  truths:
    - "The exact Portainer API route that creates a UI-editable swarm compose-string stack on endpoint 6 is confirmed and recorded"
    - "The worker-vm internal Traefik (v2.11) label/network scheme that routes Host(ai-gateway.converse-ai.app) at :80 to an overlay service is known"
    - "An external overlay network exists on worker-vm that gateway, embed, rerank, dashboard and internal Traefik can all attach to"
    - "A container on that overlay resolves the DNS name infra-redis-1 (alias) to a live Redis"
  artifacts:
    - "worker-vm external overlay network 'ai-gateway' (docker network ls shows driver=overlay, attachable)"
    - "recon note: confirmed Portainer stacks-create route (create/swarm/string vs legacy) + captured swarmID for endpoint 6"
    - "recon note: internal Traefik provider (docker/swarm), watched network, entrypoint name for :80, and required router labels"
  key_links:
    - from: "gateway swarm service (future)"
      to: "infra-redis-1:6379"
      via: "network alias on the ai-gateway overlay"
    - from: "traefik-internal_traefik (worker-vm)"
      to: "ai-gateway overlay"
      via: "network attach so it can route to gateway:8080"
---

<objective>
Establish the worker-vm swarm foundation the consolidated ai-gateway stacks depend on, and close the two live-confirm items flagged by research (A1: exact Portainer swarm-string API route; A2: internal Traefik Host routing at :80). This is prep — non-disruptive, runs entirely in parallel with the live prod gateway on n8n-ia-vm.

Purpose: Every later plan deploys via Portainer compose-string to endpoint 6 and routes through the worker-vm internal Traefik; both mechanics must be confirmed on the live build before we author stacks. The gateway env hardcodes `AI_GATEWAY_REDIS_ADDR=infra-redis-1:6379`, so a resolvable `infra-redis-1` and a shared overlay must exist first.
Output: An external `ai-gateway` overlay network on worker-vm, a working `infra-redis-1` alias, and recorded recon (confirmed API route + swarmID + Traefik routing scheme) that 19-02/19-03 consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-CONTEXT.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-RESEARCH.md

Access facts (from CLAUDE.md — do NOT paste secrets into artifacts):
- worker-vm = 10.10.10.50, `ssh worker-vm` (Docker Swarm, no GPU, overlay worker_intra, has redis_redis 7.4.2, no infra-redis-1, portainer_agent 2.39.1 = Portainer endpoint 6).
- Portainer: https://portainer3.ifixtelecom.com.br/api, header `X-API-Key: $PTR` (value in CLAUDE.md; export to env, never write value into any .planning file). Endpoint 6 = worker-vm.
- Internal Traefik service on worker-vm = `traefik-internal_traefik` (v2.11).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Portainer swarm-string create route + capture swarmID (A1)</name>
  <files>recon note (write to scratchpad, fold into SUMMARY)</files>
  <action>
    Export PTR from CLAUDE.md into the shell env (never echo the value into a committed file). Query the live Portainer build to determine the working swarm compose-string create route and capture the swarm ID for endpoint 6:
    - GET `$BASE/endpoints/6/docker/swarm` → extract `.ID` (swarmID).
    - GET `$BASE/stacks` to observe the running build's stack object shape (fields: EndpointId, SwarmId, Type, EntryPoint) — this tells you whether this build expects `POST /api/stacks/create/swarm/string?endpointId=6` (newer) or the legacy `POST /api/stacks?type=1&method=string&endpointId=6`.
    - Do NOT create a stack in this task. Only confirm which create route the build exposes (probe with an OPTIONS/GET or a deliberately-invalid minimal body and read the error shape) and record the exact route + required JSON body fields (name, swarmID, stackFileContent, env[]).
    Record: confirmed create route, swarmID, and that env vars go in the `env` array (not baked into stackFileContent).
  </action>
  <verify>
    <automated>ssh worker-vm true && test -n "$PTR" # PTR exported; swarmID captured in recon note; both curl calls returned 200 with parseable JSON</automated>
  </verify>
  <done>Confirmed Portainer swarm-string create route + swarmID for endpoint 6 recorded; A1 resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm internal Traefik Host routing at :80 (A2)</name>
  <files>recon note (scratchpad → SUMMARY)</files>
  <action>
    Inspect `traefik-internal_traefik` on worker-vm to determine how an edge forward arriving at `:80` gets routed to an overlay service by Host header. Establish:
    - which provider is enabled (docker/swarm provider `--providers.docker.swarmMode` or file-provider), the network Traefik reads for backends, and the entrypoint name bound to `:80`.
    - the exact router labels a downstream service needs: `traefik.enable=true`, `traefik.http.routers.<name>.rule=Host(\`ai-gateway.converse-ai.app\`)`, `traefik.http.routers.<name>.entrypoints=<web-entrypoint>`, `traefik.http.services.<name>.loadbalancer.server.port=8080`, and (swarm) `traefik.docker.network=<shared-overlay>`.
    Commands: `ssh worker-vm 'docker service inspect traefik-internal_traefik --format "{{json .Spec.TaskTemplate.ContainerSpec.Args}}"'` and inspect its attached networks + published ports. If the internal Traefik does NOT do host routing (only file-provider), record the fallback: edge Traefik must point at the gateway's published host port instead (A2 fallback).
    Record the confirmed routing scheme + the overlay network name Traefik watches (this becomes the shared network created in Task 3, or Traefik is attached to it).
  </action>
  <verify>
    <automated>ssh worker-vm 'docker service inspect traefik-internal_traefik --format "{{.Spec.Name}}"' # returns the service; recon note records provider + entrypoint + label scheme</automated>
  </verify>
  <done>Internal Traefik routing scheme (provider, :80 entrypoint, required labels, watched network) recorded; A2 resolved or fallback documented.</done>
</task>

<task type="auto">
  <name>Task 3: Create the shared 'ai-gateway' overlay network + verify infra-redis-1 alias resolves</name>
  <files>worker-vm:docker-network/ai-gateway</files>
  <action>
    Create an external, attachable overlay network on worker-vm that all consolidated stacks + internal Traefik share:
    `ssh worker-vm 'docker network create -d overlay --attachable ai-gateway'` (skip if it already exists).
    Validate that the `infra-redis-1` DNS name (used verbatim by the gateway env `AI_GATEWAY_REDIS_ADDR=infra-redis-1:6379`) will resolve on this overlay. The Redis itself is deployed inside the gateway stack in 19-02 (research: reuse redis:7.4.2 as a service with `networks: {ai-gateway: {aliases: [infra-redis-1]}}`), so here only prove the mechanism: launch a throwaway `redis:7.4.2` service attached to `ai-gateway` with alias `infra-redis-1`, then from a one-off container on `ai-gateway` run `redis-cli -h infra-redis-1 ping` → PONG, then remove the throwaway service.
    If internal Traefik (Task 2) needs to be attached to this overlay to reach backends, attach it: `docker service update --network-add ai-gateway traefik-internal_traefik` (only if Task 2 confirmed docker/swarm provider routing).
  </action>
  <verify>
    <automated>ssh worker-vm 'docker network ls --filter name=ai-gateway --format "{{.Name}} {{.Driver}}"' # → "ai-gateway overlay"; throwaway redis-cli ping returned PONG during test</automated>
  </verify>
  <done>External overlay `ai-gateway` exists; `infra-redis-1` alias resolution proven; throwaway redis removed; NET-01 mechanism confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude → Portainer API | X-API-Key admin token controls all endpoints; leak = full infra control |
| ops-claude → worker-vm SSH | root shell on the swarm node |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information disclosure | Portainer $PTR / SSH key | mitigate | Export secrets from CLAUDE.md into shell env only; never write values into scratchpad/SUMMARY/committed files (recon notes name the route, not the token) |
| T-19-01-02 | Tampering | overlay network / throwaway redis | accept | Prep resources only; throwaway redis removed after ping; no prod traffic touches this plan |
| T-19-01-SC | Tampering | package installs | n/a | No npm/pip/cargo installs — image redeploy only; Package Legitimacy Audit N/A |
</threat_model>

<verification>
- `ai-gateway` overlay network present on worker-vm (driver=overlay, attachable).
- Recon note records: confirmed Portainer swarm-string create route + swarmID; internal Traefik provider + :80 entrypoint + router label scheme + watched network (or documented fallback to published host port).
- infra-redis-1 alias resolution proven end-to-end (redis-cli ping PONG) and throwaway service torn down.
- Zero impact on the live prod gateway (n8n-ia-vm) — verify prod still serves `curl http://10.10.10.20:80/health` 200 after this plan.
</verification>

<success_criteria>
Foundation ready: overlay network live, redis-alias mechanism proven, and both research open-items (A1 Portainer route, A2 Traefik routing) confirmed and recorded so 19-02/19-03 can deploy compose-string stacks without further discovery.
</success_criteria>

<output>
Create `.planning/phases/19-gateway-consolidation-worker-vm/19-01-SUMMARY.md` when done. Include the confirmed Portainer create route, swarmID (value is not a secret), Traefik routing scheme, and the exact router labels downstream stacks must use.
</output>
