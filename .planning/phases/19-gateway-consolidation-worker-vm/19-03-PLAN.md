---
phase: 19-gateway-consolidation-worker-vm
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - "portainer:stack/ai-gateway-dashboard (endpoint 6, swarm compose-string)"
  - "portainer:stack/ai-gateway-rerank (endpoint 6, swarm compose-string)"
  - "bd_ai_gateway:model_aliases (additive port of classified prod-only client-facing aliases)"
autonomous: true
requirements: [UI-01, DB-02, CONS-01]

must_haves:
  truths:
    - "The dashboard runs on worker-vm as a UI-editable Portainer swarm stack, reachable via internal Traefik Host(ai-dashboard.converse-ai.app), with login/auth proven (DB connectivity), not just a 200/302"
    - "The rerank service runs on worker-vm (consolidated) and its PUBLIC gateway /rerank route (the one clients use) is verified, not only the backend /health"
    - "Every prod-only client-facing model_alias is CLASSIFIED (port / map-to-existing-upstream / add-upstream / reject) in a diff artifact, and each ported alias is verified with a REAL gateway call using the alias string (not just DB presence)"
  artifacts:
    - "Portainer swarm stack 'ai-gateway-dashboard' on endpoint 6"
    - "Portainer swarm stack 'ai-gateway-rerank' on endpoint 6"
    - "model_aliases diff artifact (source vs target, with per-alias classification) + inserted rows in bd_ai_gateway"
  key_links:
    - from: "dashboard service"
      to: "bd_ai_gateway"
      via: "dashboard DSN → bd_ai_gateway (sslmode no-verify + search_path public quirks)"
    - from: "gateway alias resolver"
      to: "ported model_aliases"
      via: "SQL insert keyed on composite PK (alias, upstream_name) + pg_notify reload; verified by real gateway call"
---

<objective>
Move the supporting surfaces onto worker-vm: the dashboard (UI-editable Portainer stack), the rerank service (consolidate rather than drop for now), and reconcile the client-facing `model_aliases` that live only in the prod DB into `bd_ai_gateway` so no merged-traffic model string 404s after cutover (research Pitfall 6). Runs in parallel with 19-02 (non-disruptive).

Purpose: Deliverable #3 (all stacks Portainer-managed) + close the alias-drift gap before prod tenants arrive. Rerank drop-vs-consolidate is a CONTEXT-deferred question — this plan consolidates (safe default); dropping it can be a later cleanup once /rerank consumers are confirmed absent.
Output: dashboard + rerank swarm stacks on endpoint 6; a classified alias diff artifact; prod-only client-facing aliases ported into bd_ai_gateway and proven via real gateway calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-CONTEXT.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-RESEARCH.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-01-SUMMARY.md

Baselines (pull live, secrets by name only):
- prod dashboard compose: `ssh n8n-ia-vm 'cat /opt/*dashboard*/docker-compose.yml'` (ifix-ai-dashboard)
- prod rerank compose: `ssh n8n-ia-vm 'cat /opt/ai-gateway-rerank/docker-compose.yml'`
- prod-only aliases context: memory stt-model-alias-whisper-large-v3 (whisper mirror), copilot alias gpt-5.4-mini-2026-03-17

model_aliases schema (verified from gateway/db/migrations/0005 + 0026): composite PK `(alias, upstream_name)`. Columns = alias TEXT, upstream TEXT CHECK(IN 'llm','stt','embed') [ROLE tag], target TEXT [provider model slug], created_at, upstream_name TEXT NOT NULL [upstream NAME: local-llm/openrouter-chat/openai-whisper/… — references an ai_gateway.upstreams row]. A ported alias only resolves if its `upstream_name` exists as an upstream in the target; the additive copy must therefore also confirm/port the referenced upstream. Alias reload = pg_notify (memory stt-model-alias-whisper-large-v3: canonical via `gatewayctl model-alias set`, else SQL insert + pg_notify).
Dashboard DSN quirks (memory ai-dashboard-access-and-2fa-recovery): sslmode no-verify + search_path public; a wrong search_path lets the dashboard render but fail after login — so login must be exercised, not just the landing page.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the ai-gateway-dashboard swarm stack + prove login/DB connectivity</name>
  <files>portainer:stack/ai-gateway-dashboard</files>
  <action>
    Convert the ifix-ai-dashboard standalone compose to a swarm compose-string stack on endpoint 6, attached to the `ai-gateway` overlay, with internal-Traefik router labels for Host(ai-dashboard.converse-ai.app) → dashboard service port (from 19-01 recon). DSN → bd_ai_gateway (apply the memory quirks: sslmode no-verify + search_path public). Env in the Portainer env array, values from worker-vm .env root-600. Image = the promoted dashboard image tag currently on n8n-ia-vm — pin by digest and record it. Force-pull if webhook does not (Pitfall 4).
    Do NOT flip the edge Traefik for the dashboard yet — that happens at cutover (19-05). Validate internally only, and prove DB connectivity by exercising the LOGIN/auth flow (not just the landing 200/302): POST the login endpoint with a known dashboard credential (or hit an authenticated API route that touches the DB) and confirm a session/authenticated 200 — this catches a wrong search_path/sslmode that would let the page render but fail after login.
  </action>
  <verify>
    <automated>ssh worker-vm 'docker service ls --filter name=ai-gateway-dashboard --format "{{.Replicas}}"' | grep 1/1; curl -s -o /dev/null -w "%{http_code}" -H "Host: ai-dashboard.converse-ai.app" http://10.10.10.50:80/ # → 200/302; plus an authenticated login/API call returns 200 (DB-backed), recorded in SUMMARY</automated>
  </verify>
  <done>Dashboard swarm stack running on endpoint 6, UI-editable, digest-pinned, reachable via internal Traefik host route; login/auth flow exercised proving bd_ai_gateway connectivity (not just landing page).</done>
</task>

<task type="auto">
  <name>Task 2: Deploy the ai-gateway-rerank swarm stack + verify the public /rerank gateway route</name>
  <files>portainer:stack/ai-gateway-rerank</files>
  <action>
    Convert `/opt/ai-gateway-rerank` to a swarm compose-string stack on endpoint 6, attached to the `ai-gateway` overlay, service name `rerank`, no host port (reachable by overlay DNS), image pinned by digest. Point the gateway env `UPSTREAM_RERANK_URL` (if present) at the worker-vm rerank service and force-reload the gateway.
    Consumer check (research Open Q3): grep gateway logs for /rerank consumers to RECORD whether any client depends on it — but note (Codex LOW) that log-absence is weak evidence, so consolidate regardless (safe default) and record the finding for a later drop decision. Do NOT drop rerank in this phase.
    VERIFY THE PUBLIC ROUTE clients actually use (Codex MEDIUM — not only the backend /health): hit the gateway's `/rerank` (or `/v1/rerank`) route via internal Traefik `http://10.10.10.50:80` with a small rerank payload → 200 with ranked results, confirming the gateway→rerank upstream wiring end-to-end. Record the exact route in SUMMARY (19-05 public smoke reuses it).
  </action>
  <verify>
    <automated>ssh worker-vm 'docker service ls --filter name=ai-gateway-rerank --format "{{.Replicas}}"' | grep 1/1; curl -s -o /dev/null -w "%{http_code}" -X POST http://10.10.10.50:80/rerank -H "Authorization: Bearer <dev_key>" -d '{"query":"x","documents":["a","b"]}' # → 200 via the gateway (client-facing route), not just backend /health</automated>
  </verify>
  <done>Rerank running on worker-vm (digest-pinned); gateway rerank upstream points at it; the PUBLIC /rerank gateway route verified 200; consumer status recorded (drop deferred).</done>
</task>

<task type="auto">
  <name>Task 3: Classify + reconcile prod-only client-facing model_aliases into bd_ai_gateway (real-call verified)</name>
  <files>bd_ai_gateway:model_aliases</files>
  <action>
    From ops-claude (DO whitelists 162.55.92.154), export both alias sets AND their referenced upstreams into a DIFF ARTIFACT (Codex MEDIUM):
    `psql "$SRC_PROD" -c "SELECT alias, upstream, upstream_name, target FROM ai_gateway.model_aliases ORDER BY alias, upstream_name"` and the same for `$DST`; also dump `SELECT name FROM ai_gateway.upstreams` for both. Compute the prod-only set (present in source, absent in target keyed on the composite (alias, upstream_name)).
    CLASSIFY every prod-only alias into exactly one bucket (record in the artifact):
      - PORT — its upstream_name already exists as an upstream in the target → additive INSERT (alias, upstream, target, upstream_name).
      - MAP-TO-EXISTING — client string should resolve to a semantically-equivalent target upstream already present → insert with the target's upstream_name.
      - ADD-UPSTREAM — a real client depends on it but its upstream_name is missing in the target → add the upstream row first, then the alias.
      - REJECT — dev-throwaway / no client dependency → do NOT port; note why.
    Known client-facing aliases to expect (memory): `gpt-5.4-mini-2026-03-17` + bare `gpt-5.4-mini` (copilot → maps to an OpenRouter deepseek target/upstream), `whisper` STT mirror. For canonical upstreams use `gatewayctl model-alias set`; otherwise SQL INSERT (on the composite PK) + `pg_notify` (upstreams_changed / model_aliases reload channel) so the running gateway picks them up without restart.
    VERIFY EACH PORTED ALIAS WITH A REAL GATEWAY CALL using the alias string (not just DB presence): for an LLM alias, `POST http://10.10.10.50:80/v1/chat/completions {"model":"gpt-5.4-mini-2026-03-17",…}` → 200 (not 404 unknown model); if `whisper` is ported, include an STT/transcription smoke (`POST /v1/audio/transcriptions model=whisper`) → 200. Record each alias's real-call result in SUMMARY.
  </action>
  <verify>
    <automated>psql "$DST" -tc "SELECT count(*) FROM ai_gateway.model_aliases WHERE alias IN ('gpt-5.4-mini-2026-03-17','gpt-5.4-mini','whisper')" | grep -qvE '^\s*0'; curl -s -o /dev/null -w "%{http_code}" -X POST http://10.10.10.50:80/v1/chat/completions -H "Authorization: Bearer <dev_key>" -d '{"model":"gpt-5.4-mini-2026-03-17","messages":[{"role":"user","content":"ping"}],"max_tokens":8}' | grep -q 200 # ported alias resolves via a REAL gateway call, not 404</automated>
  </verify>
  <done>Diff artifact records every prod-only alias with a classification (port/map/add-upstream/reject); ported aliases + any needed upstreams present in bd_ai_gateway and each verified with a real gateway call (chat + whisper STT if ported); alias-drift 404 risk (Pitfall 6) closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → dashboard | owner/operator auth (Better-Auth style), 2FA |
| ops-claude → bd_ai_gateway | psql writes model_aliases (+ maybe upstreams) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | model_aliases/upstreams writes on target DB | mitigate | Additive inserts only (no UPDATE/DELETE of target rows); classified diff-before-insert on composite PK; pg_notify reload + real-call verify |
| T-19-03-02 | Information disclosure | dashboard DSN/secrets | mitigate | Env array + worker-vm .env root-600; never in stackFileContent/committed files |
| T-19-03-03 | Spoofing | dashboard exposed pre-cutover | accept | Internal Traefik only (no edge route until 19-05); Host-header reachable inside overlay |
| T-19-03-04 | Denial of service | ported alias resolves to a missing/incompatible upstream → runtime 5xx | mitigate | classification requires upstream present (or ADD-UPSTREAM); real gateway call verifies resolution before cutover |
| T-19-03-SC | Tampering | package installs | n/a | Image redeploy only |
</threat_model>

<verification>
- dashboard + rerank swarm stacks running on endpoint 6, UI-editable, digest-pinned.
- Dashboard login/auth flow exercised (DB connectivity proven), not just landing 200/302.
- Public /rerank gateway route verified 200 (client-facing), not just backend /health.
- Alias diff artifact with per-alias classification; ported aliases + upstreams present and each real-call-verified (chat + whisper STT if ported).
- No modification to the target's dev-style upstream set beyond additive alias/upstream ports.
</verification>

<success_criteria>
All supporting surfaces (dashboard with proven login, rerank with a verified public route) consolidated onto worker-vm and Portainer-managed; the model_alias drift that would 404 merged prod traffic is closed additively in bd_ai_gateway, classified and proven via real gateway calls.
</success_criteria>

<output>
Create `.planning/phases/19-gateway-consolidation-worker-vm/19-03-SUMMARY.md` when done. List the alias diff + classification, per-alias real-call results, the rerank public route + consumer status, and the dashboard stack id + image digest + login-proof result.
</output>
</content>
