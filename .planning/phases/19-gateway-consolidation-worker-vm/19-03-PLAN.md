---
phase: 19-gateway-consolidation-worker-vm
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - "portainer:stack/ai-gateway-dashboard (endpoint 6, swarm compose-string)"
  - "portainer:stack/ai-gateway-rerank (endpoint 6, swarm compose-string)"
  - "bd_ai_gateway:model_aliases (additive port of prod-only client-facing aliases)"
autonomous: true
requirements: [UI-01, DB-02, CONS-01]

must_haves:
  truths:
    - "The dashboard runs on worker-vm as a UI-editable Portainer swarm stack, reachable via internal Traefik Host(ai-dashboard.converse-ai.app)"
    - "The rerank service runs on worker-vm (consolidated) so no client /rerank call breaks at decommission"
    - "Every prod-only client-facing model_alias (e.g. gpt-5.4-mini-2026-03-17, whisper STT mirror) exists in bd_ai_gateway"
  artifacts:
    - "Portainer swarm stack 'ai-gateway-dashboard' on endpoint 6"
    - "Portainer swarm stack 'ai-gateway-rerank' on endpoint 6"
    - "model_aliases rows in bd_ai_gateway covering the prod-only client-facing alias set"
  key_links:
    - from: "dashboard service"
      to: "bd_ai_gateway"
      via: "dashboard DSN → bd_ai_gateway"
    - from: "gateway alias resolver"
      to: "ported model_aliases"
      via: "SQL insert + pg_notify upstreams_changed / model_aliases reload"
---

<objective>
Move the supporting surfaces onto worker-vm: the dashboard (UI-editable Portainer stack), the rerank service (consolidate rather than drop for now), and reconcile the client-facing `model_aliases` that live only in the prod DB into `bd_ai_gateway` so no merged-traffic model string 404s after cutover (research Pitfall 6). Runs in parallel with 19-02 (non-disruptive).

Purpose: Deliverable #3 (all stacks Portainer-managed) + close the alias-drift gap before prod tenants arrive. Rerank drop-vs-consolidate is a CONTEXT-deferred question — this plan consolidates (safe default); dropping it can be a later cleanup once /rerank consumers are confirmed absent.
Output: dashboard + rerank swarm stacks on endpoint 6; prod-only client-facing aliases ported into bd_ai_gateway.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-CONTEXT.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-RESEARCH.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-01-SUMMARY.md

Baselines (pull live, secrets by name only):
- prod dashboard compose: `ssh n8n-ia-vm 'cat /opt/*dashboard*/docker-compose.yml'` (ifix-ai-dashboard)
- prod rerank compose: `ssh n8n-ia-vm 'cat /opt/ai-gateway-rerank/docker-compose.yml'`
- prod-only aliases context: memory stt-model-alias-whisper-large-v3 (whisper mirror), copilot alias gpt-5.4-mini-2026-03-17
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the ai-gateway-dashboard swarm stack (compose-string, endpoint 6)</name>
  <files>portainer:stack/ai-gateway-dashboard</files>
  <action>
    Convert the ifix-ai-dashboard standalone compose to a swarm compose-string stack on endpoint 6, attached to the `ai-gateway` overlay, with internal-Traefik router labels for Host(ai-dashboard.converse-ai.app) → dashboard service port (from 19-01 recon). DSN → bd_ai_gateway (sslmode; note the dashboard DSN quirks from memory ai-dashboard-access-and-2fa-recovery: sslmode no-verify + search_path public). Env in the Portainer env array, values from worker-vm .env root-600. Image = the promoted dashboard image tag currently on n8n-ia-vm. Force-pull if webhook does not (Pitfall 4).
    Do NOT flip the edge Traefik for the dashboard yet — that happens at cutover (19-05). Validate internally only.
  </action>
  <verify>
    <automated>ssh worker-vm 'docker service ls --filter name=ai-gateway-dashboard --format "{{.Replicas}}"' | grep 1/1; curl -s -o /dev/null -w "%{http_code}" -H "Host: ai-dashboard.converse-ai.app" http://10.10.10.50:80/ # → 200/302 (login)</automated>
  </verify>
  <done>Dashboard swarm stack running on endpoint 6, UI-editable, reachable via internal Traefik host route; reads bd_ai_gateway.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy the ai-gateway-rerank swarm stack (consolidate)</name>
  <files>portainer:stack/ai-gateway-rerank</files>
  <action>
    Convert `/opt/ai-gateway-rerank` to a swarm compose-string stack on endpoint 6, attached to the `ai-gateway` overlay, service name `rerank`, no host port (reachable by overlay DNS). Point the gateway env `UPSTREAM_RERANK_URL` (if present) at the worker-vm rerank service. Before finalizing, grep for /rerank consumers to record whether any client depends on it (research Open Q3): `ssh worker-vm 'docker service logs <gateway> 2>&1 | grep -c rerank'` + check gateway rerank route config. Consolidate regardless (safe); note in SUMMARY whether a later drop is warranted.
  </action>
  <verify>
    <automated>ssh worker-vm 'docker service ls --filter name=ai-gateway-rerank --format "{{.Replicas}}"' | grep 1/1; ssh worker-vm 'docker run --rm --network ai-gateway curlimages/curl -s -o /dev/null -w "%{http_code}" http://rerank:<port>/health' # → 200</automated>
  </verify>
  <done>Rerank running on worker-vm; gateway rerank upstream points at it; consumer status recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile prod-only client-facing model_aliases into bd_ai_gateway</name>
  <files>bd_ai_gateway:model_aliases</files>
  <action>
    Diff `model_aliases` (and `upstreams` for prod-only healthy targets) between `bd_ai_gateway_prod` and `bd_ai_gateway`. Keep the TARGET (dev-style) upstream set per locked env; ADDITIVELY port any prod-only, client-facing alias whose model string a real app depends on (memory: `gpt-5.4-mini-2026-03-17` + bare `gpt-5.4-mini` copilot alias; `whisper` STT mirror alias). From ops-claude (DO whitelists 162.55.92.154):
    `psql "$SRC_PROD" -c "SELECT alias, upstream, target FROM ai_gateway.model_aliases ORDER BY alias"` and same for target; compute the set-difference (prod-only).
    Insert the prod-only client-facing aliases into bd_ai_gateway. For aliases whose upstream isn't canonical, follow the memory pattern: `gatewayctl model-alias set` for canonical, otherwise SQL insert + `pg_notify` (`upstreams_changed` / model_aliases reload channel) so the running gateway picks them up without restart. Only port aliases that resolve to an upstream present in the target (or add the upstream too). Do NOT port dev-throwaway aliases.
    Verify a ported alias resolves through the worker-vm gateway.
  </action>
  <verify>
    <automated>psql "$DST" -c "SELECT count(*) FROM ai_gateway.model_aliases WHERE alias IN ('gpt-5.4-mini-2026-03-17','gpt-5.4-mini','whisper')" # → matches expected; a chat/transcription call using a ported alias via 10.10.10.50:80 returns 200 (not 404 unknown model)</automated>
  </verify>
  <done>Prod-only client-facing model_aliases present in bd_ai_gateway and resolving through the worker-vm gateway; alias-drift 404 risk (Pitfall 6) closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → dashboard | owner/operator auth (Better-Auth style), 2FA |
| ops-claude → bd_ai_gateway | psql writes model_aliases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | model_aliases writes on target DB | mitigate | Additive inserts only (no UPDATE/DELETE of target rows); diff-before-insert; pg_notify reload verified |
| T-19-03-02 | Information disclosure | dashboard DSN/secrets | mitigate | Env array + worker-vm .env root-600; never in stackFileContent/committed files |
| T-19-03-03 | Spoofing | dashboard exposed pre-cutover | accept | Internal Traefik only (no edge route until 19-05); Host-header reachable inside overlay |
| T-19-03-SC | Tampering | package installs | n/a | Image redeploy only |
</threat_model>

<verification>
- dashboard + rerank swarm stacks running on endpoint 6, UI-editable.
- Dashboard reachable via internal Traefik host route (login 200/302).
- Prod-only client-facing aliases ported and resolving (no 404) through worker-vm gateway.
- No modification to the target's dev-style upstream set beyond additive alias/upstream ports.
</verification>

<success_criteria>
All supporting surfaces (dashboard, rerank) consolidated onto worker-vm and Portainer-managed; the model_alias drift that would 404 merged prod traffic is closed additively in bd_ai_gateway.
</success_criteria>

<output>
Create `.planning/phases/19-gateway-consolidation-worker-vm/19-03-SUMMARY.md` when done. List ported aliases, rerank consumer status, and dashboard stack id.
</output>
