---
phase: 19-gateway-consolidation-worker-vm
plan: 05
type: execute
wave: 4
depends_on: ["19-04"]
files_modified:
  - "vps-ifix-vm:/home/pedro/projetos/pedro/infra/traefik-dynamic/ai-gateway-prod.yml (edge file-provider server URL)"
autonomous: false
requirements: [CUT-01]

must_haves:
  truths:
    - "https://ai-gateway.converse-ai.app is served by the worker-vm gateway (edge Traefik server URL → 10.10.10.50:80)"
    - "2-3 real prod tenant keys return 200 over the public hostname + write billing to bd_ai_gateway"
    - "ai-dashboard.converse-ai.app resolves to the worker-vm dashboard"
    - "DNS / Cloudflare unchanged (still 162.55.92.154); rollback is reverting the single edge line"
  artifacts:
    - "edited edge file-provider entry: service n8n-ia-prod-internal server http://10.10.10.50:80"
    - "cutover smoke log: public 200s + billing rows in bd_ai_gateway"
  key_links:
    - from: "edge Traefik (vps-ifix-vm)"
      to: "worker-vm internal Traefik :80"
      via: "file-provider server URL hot-reload (no restart)"
    - from: "public hostname"
      to: "worker-vm gateway"
      via: "DNS 162.55.92.154 (unchanged) → DNAT :443 → edge → 10.10.10.50:80"
---

<objective>
Flip the single ingress seam so the public hostname `ai-gateway.converse-ai.app` (and `ai-dashboard.converse-ai.app`) is served by the worker-vm gateway instead of n8n-ia-vm. This is a one-line edit of the edge Traefik file-provider on vps-ifix-vm, hot-reloaded — DNS/Cloudflare stay unchanged, and rollback is reverting that line. Gated by a BLOCKING pre-flight checkpoint because it moves PROD client traffic + billing.

Purpose: LOCKED — cutover via the edge Traefik server-URL line (research: the correct reversible seam), not DNS. n8n-ia-vm keeps running through a soak window so rollback stays instant until decommission (19-06).
Output: Public traffic served by worker-vm; smoke-verified with real tenant keys; rollback documented and one-command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-gateway-consolidation-worker-vm/19-RESEARCH.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-04-SUMMARY.md

Cutover seam (from 10-PATTERNS.md + research): edge Traefik v3.6 on vps-ifix-vm, file-provider `/home/pedro/projetos/pedro/infra/traefik-dynamic/ai-gateway-prod.yml`, service `n8n-ia-prod-internal` server `http://10.10.10.20:80` → change to `http://10.10.10.50:80`. Same file fronts ai-dashboard.converse-ai.app → point dashboard to worker-vm too. Hot-reload, no restart. Traefik ops rule (memory traefik-ops-rule-dns-first): DNS already resolves — a server-URL edit does not re-trigger cert issuance, so no LE rate-limit risk.
Real prod keys for smoke (CLAUDE.md): chat-ifix, converseai, converseai-finan-app.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: pre-cutover go/no-go</name>
  <action>PAUSE — blocking human-verify gate. Do not proceed to the next task until the resume-signal is given. Present the details below and wait for approval.</action>
  <what-built>The worker-vm gateway is green pre-cutover: /health 200, all 18 prod tenants migrated (19-04), real prod keys authenticate + bill against bd_ai_gateway, embed + dashboard live on worker-vm. Ready to flip the public ingress.</what-built>
  <how-to-verify>
    Confirm the go/no-go before moving PROD traffic:
    1. `curl http://10.10.10.50:80/health` → 200 (internal Traefik).
    2. 19-04 auth proof passed (prod keys 200 + billing to bd_ai_gateway; prod admin 200).
    3. Embed 200 from worker-vm; dashboard reachable internally.
    4. The exact edit: `ai-gateway-prod.yml` service `n8n-ia-prod-internal` server `http://10.10.10.20:80` → `http://10.10.10.50:80` (+ dashboard service if separate). Rollback = revert that line, hot-reload — instant, valid until 19-06 teardown.
    5. n8n-ia-vm stays UP through the soak window (rollback target).
    Approve to flip, or hold.
  </how-to-verify>
  <resume-signal>Type "approved" to edit the edge Traefik line, or "hold".</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Flip the edge Traefik file-provider server URL (hot-reload)</name>
  <files>vps-ifix-vm:/home/pedro/projetos/pedro/infra/traefik-dynamic/ai-gateway-prod.yml</files>
  <action>
    On vps-ifix-vm, back up the current file (`cp ai-gateway-prod.yml ai-gateway-prod.yml.bak-pre19-05`), then edit the service `n8n-ia-prod-internal` server URL `http://10.10.10.20:80` → `http://10.10.10.50:80`. If the same file fronts `ai-dashboard.converse-ai.app` via a separate service block, point it at the worker-vm dashboard (`http://10.10.10.50:80` with the dashboard Host route) too. Save — Traefik file-provider hot-reloads (no restart). Do NOT touch DNS/Cloudflare. Watch edge logs for the provider reload event and the first upstream 200.
  </action>
  <verify>
    <automated>ssh vps-ifix-vm 'grep -c "10.10.10.50:80" /home/pedro/projetos/pedro/infra/traefik-dynamic/ai-gateway-prod.yml' # ≥1; edge logs show file-provider reload, no error</automated>
  </verify>
  <done>Edge Traefik server URL points at worker-vm (10.10.10.50:80); hot-reload succeeded; backup of the file retained.</done>
</task>

<task type="auto">
  <name>Task 2: Public smoke with real prod keys + rollback note</name>
  <files>cutover smoke log (SUMMARY)</files>
  <action>
    Hit the PUBLIC hostname (not the internal IP) with 2-3 real prod tenant keys:
    - `POST https://ai-gateway.converse-ai.app/v1/chat/completions` with chat-ifix + converseai + converseai-finan-app keys → 200; confirm each writes a billing_events row in bd_ai_gateway (route=chat).
    - `GET https://ai-gateway.converse-ai.app/health` → 200.
    - `https://ai-dashboard.converse-ai.app/` → login 200/302 from worker-vm dashboard.
    Watch edge + gateway logs for the first minutes of live traffic; confirm billing writes land in bd_ai_gateway (not _prod). Record the rollback command explicitly in SUMMARY: revert the server URL to `http://10.10.10.20:80` + save (hot-reload) → traffic returns to n8n-ia-vm instantly. Keep n8n-ia-vm running until 19-06 soak passes.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer <chat-ifix_key>" -X POST https://ai-gateway.converse-ai.app/v1/chat/completions -d '{...}' # → 200; psql bd_ai_gateway shows new billing_events row</automated>
  </verify>
  <done>Public hostname served by worker-vm; real prod keys 200 + billing to bd_ai_gateway; dashboard public loads; rollback command documented; n8n-ia-vm still up as rollback target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → edge Traefik → worker-vm | live client traffic + billing crosses here at the flip |
| ops-claude → vps-ifix-vm | SSH edit of the edge file-provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Denial of service | mis-edit of edge file → all tenants 5xx | mitigate | Back up file before edit; BLOCKING pre-flight gate confirms worker-vm green; one-line revert + hot-reload rollback |
| T-19-05-02 | Tampering | accidental cert re-issue / LE rate-limit | mitigate | Server-URL-only edit (DNS unchanged) does not re-trigger TLS-ALPN; memory traefik-ops-rule-dns-first honored |
| T-19-05-03 | Repudiation | billing split across _prod/target during flip | accept | Sub-second hot-reload; brief overlap tolerable; both dbs on same cluster; post-flip all writes go to bd_ai_gateway |
| T-19-05-SC | Tampering | package installs | n/a | Config edit only, no installs |
</threat_model>

<verification>
- Edge file-provider server URL = 10.10.10.50:80 (+ dashboard); hot-reload clean, backup retained.
- Public `ai-gateway.converse-ai.app` returns 200 for 2-3 real prod keys + billing rows land in bd_ai_gateway.
- Public `ai-dashboard.converse-ai.app` served by worker-vm.
- DNS/Cloudflare unchanged; documented one-line rollback verified as available.
- n8n-ia-vm still serving as rollback target (not yet torn down).
</verification>

<success_criteria>
Production traffic is served by the consolidated worker-vm gateway over the unchanged public hostname, verified with real tenant keys and billing, with an instant one-line rollback held open through the soak window.
</success_criteria>

<output>
Create `.planning/phases/19-gateway-consolidation-worker-vm/19-05-SUMMARY.md` when done. Record the exact edit diff, smoke results, and the verbatim rollback command.
</output>
