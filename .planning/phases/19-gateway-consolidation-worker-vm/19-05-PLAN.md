---
phase: 19-gateway-consolidation-worker-vm
plan: 05
type: execute
wave: 4
depends_on: ["19-04"]
files_modified:
  - "vps-ifix-vm:/home/pedro/projetos/pedro/infra/traefik-dynamic/ai-gateway-prod.yml (edge file-provider server URL)"
autonomous: false
requirements: [CUT-01]

must_haves:
  truths:
    - "https://ai-gateway.converse-ai.app is served by the worker-vm gateway (edge Traefik server URL → 10.10.10.50:80)"
    - "A pre-flip preflight from the EDGE host proves Host routing works (curl 10.10.10.50:80 -H 'Host: ai-gateway.converse-ai.app' → 200) BEFORE the line is changed"
    - "The known prod tenant keys (CLAUDE.md) return 200 over the public hostname + write billing to bd_ai_gateway (the sensitive-tenant key's 503 = auth-OK, not breakage)"
    - "A cutover timestamp is recorded, and a billing/usage reconciliation procedure for a rollback window is documented"
    - "ai-dashboard.converse-ai.app resolves to the worker-vm dashboard"
    - "DNS / Cloudflare unchanged (still 162.55.92.154); rollback is reverting the single edge line, hot-reload"
    - "Post-cutover monitoring commands + rollback-trigger thresholds + a defined observation window are recorded"
  artifacts:
    - "edited edge file-provider entry: service n8n-ia-prod-internal server http://10.10.10.50:80 (+ dashboard block if separate)"
    - "cutover smoke log: public 200s + billing rows in bd_ai_gateway + recorded cutover_ts"
    - "rollback-reconciliation note (compare billing_events/usage_counters across DBs for [cutover_ts, rollback_ts])"
  key_links:
    - from: "edge Traefik (vps-ifix-vm)"
      to: "worker-vm internal Traefik :80"
      via: "file-provider server URL hot-reload (no restart)"
    - from: "public hostname"
      to: "worker-vm gateway"
      via: "DNS 162.55.92.154 (unchanged) → DNAT :443 → edge → 10.10.10.50:80"
---

<objective>
Flip the single ingress seam so the public hostname `ai-gateway.converse-ai.app` (and `ai-dashboard.converse-ai.app`) is served by the worker-vm gateway instead of n8n-ia-vm. This is a one-line edit of the edge Traefik file-provider on vps-ifix-vm, hot-reloaded — DNS/Cloudflare stay unchanged, and rollback is reverting that line. Gated by a BLOCKING pre-flight checkpoint because it moves PROD client traffic + billing.

Purpose: LOCKED — cutover via the edge Traefik server-URL line (research: the correct reversible seam), not DNS. n8n-ia-vm keeps running through a soak window so rollback stays instant until decommission (19-06). "Zero client breakage" is precisely "auth preserved + behavior parity" — sensitive tenants (telefonia/cobrancas) legitimately 503 when the primary is down (RES-08), same as current prod behavior; that is not a regression.
Output: Public traffic served by worker-vm; smoke-verified with the known prod tenant keys; a recorded cutover timestamp; a documented one-command rollback AND a billing/usage reconciliation procedure for the rollback window; live monitoring + abort thresholds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-gateway-consolidation-worker-vm/19-RESEARCH.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-04-SUMMARY.md

Cutover seam (from 10-PATTERNS.md + research): edge Traefik v3.6 on vps-ifix-vm, file-provider `/home/pedro/projetos/pedro/infra/traefik-dynamic/ai-gateway-prod.yml`, service `n8n-ia-prod-internal` server `http://10.10.10.20:80` → change to `http://10.10.10.50:80`. Same file fronts ai-dashboard.converse-ai.app → confirm whether it is the SAME service block or a SEPARATE one (record before the checkpoint) and point the dashboard at worker-vm too. Hot-reload, no restart. Traefik ops rule (memory traefik-ops-rule-dns-first): DNS already resolves — a server-URL edit does not re-trigger cert issuance, no LE rate-limit risk.
Smoke uses the known plaintext prod keys (CLAUDE.md, same set as 19-04): the normal-tenant keys → 200; one sensitive-tenant key → 503 auth-OK; the prod admin key. Resolve the exact slugs + keys from CLAUDE.md at execution time — do not inline the literals into this .planning file.
ops timers remain MASKED (from 19-04) through this cutover — 19-06 unmasks+repoints.
Both bd_ai_gateway (new, live target) and bd_ai_gateway_prod (old, rollback target) are on the same DO cluster — reconciliation is a cross-DB query from ops-claude.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: pre-cutover go/no-go (with edge preflight)</name>
  <action>PAUSE — blocking human-verify gate. Do not proceed to the next task until the resume-signal is given. Present the details below and wait for approval. Run the edge preflight (read-only, no flip) BEFORE presenting.</action>
  <what-built>The worker-vm gateway is green pre-cutover: /health 200, all 18 prod tenants migrated (19-04, hash-verbatim), the known prod keys authenticate + bill against bd_ai_gateway, embed + dashboard live on worker-vm. Ready to flip the public ingress.</what-built>
  <how-to-verify>
    Confirm the go/no-go before moving PROD traffic:
    1. EDGE PREFLIGHT (prove Host routing works BEFORE editing the line): from the EDGE host (vps-ifix-vm), `curl -s -o /dev/null -w "%{http_code}" http://10.10.10.50:80/health -H 'Host: ai-gateway.converse-ai.app'` → 200, and the same with a real chat key → 200. This proves edge→worker-vm Host-routed reachability so the flip cannot surprise on routing.
    2. 19-04 auth proof passed (known prod keys 200 + billing to bd_ai_gateway; sensitive-tenant sensitive-503 auth-OK; prod admin 200).
    3. Embed 200 from worker-vm; dashboard reachable internally. Record whether the dashboard is the SAME edge service block or a SEPARATE one.
    4. The exact edit: `ai-gateway-prod.yml` service `n8n-ia-prod-internal` server `http://10.10.10.20:80` → `http://10.10.10.50:80` (+ dashboard block if separate). Rollback = revert that line, hot-reload — instant, valid until 19-06 teardown.
    5. n8n-ia-vm stays UP through the soak window (rollback target). ops timers remain masked.
    6. Accept the rollback-billing-split model (Task 2 documents reconciliation): post-cutover writes land in bd_ai_gateway; a rollback would resume writes to _prod, splitting billing across the window.
    Approve to flip, or hold.
  </how-to-verify>
  <resume-signal>Type "approved" to edit the edge Traefik line, or "hold".</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Record cutover_ts, flip the edge Traefik file-provider server URL (hot-reload)</name>
  <files>vps-ifix-vm:/home/pedro/projetos/pedro/infra/traefik-dynamic/ai-gateway-prod.yml</files>
  <action>
    Record `cutover_ts` = UTC now (into SUMMARY) immediately before the edit — it bounds the reconciliation window. On vps-ifix-vm, back up the file (`cp ai-gateway-prod.yml ai-gateway-prod.yml.bak-pre19-05`), then edit the service `n8n-ia-prod-internal` server URL `http://10.10.10.20:80` → `http://10.10.10.50:80`. If the dashboard is a separate service block, point it at the worker-vm dashboard too. Save — Traefik file-provider hot-reloads (no restart). Do NOT touch DNS/Cloudflare. Watch edge logs for the provider reload event and the first upstream 200.
  </action>
  <verify>
    <automated>ssh vps-ifix-vm 'grep -c "10.10.10.50:80" /home/pedro/projetos/pedro/infra/traefik-dynamic/ai-gateway-prod.yml' # ≥1; edge logs show file-provider reload, no error</automated>
  </verify>
  <done>cutover_ts recorded; edge Traefik server URL points at worker-vm (10.10.10.50:80); hot-reload succeeded; backup of the file retained.</done>
</task>

<task type="auto">
  <name>Task 2: Public smoke + live monitoring + rollback & reconciliation notes</name>
  <files>cutover smoke + monitoring log (SUMMARY)</files>
  <action>
    Hit the PUBLIC hostname (not the internal IP) with the known prod tenant keys (resolve exact slugs + keys from CLAUDE.md at execution time):
    - `POST https://ai-gateway.converse-ai.app/v1/chat/completions` with the normal-tenant prod keys (small prompts) → 200; confirm each writes a billing_events row in bd_ai_gateway (route=chat). The sensitive-tenant key → 503 sensitive (auth-OK).
    - `GET https://ai-gateway.converse-ai.app/health` → 200. Public embeddings smoke: `POST /v1/embeddings` → 200 (verify embed surface, not just chat). If rerank is publicly exposed, hit its public route too (19-03 recorded the route).
    - `https://ai-dashboard.converse-ai.app/` → login 200/302 from worker-vm dashboard.

    OBSERVATION WINDOW + MONITORING (record commands + a rolling read for the first ~15 min, then spot-check across the soak): 
    - Traefik 5xx rate: `ssh vps-ifix-vm 'docker logs <edge-traefik> --since 5m 2>&1 | grep -c " 50[0-9] "'`.
    - Gateway 401/403 spike: `ssh worker-vm 'docker service logs <gateway> --since 5m 2>&1 | grep -cE " 40[13] "'`.
    - DB insert errors / redis errors: `ssh worker-vm 'docker service logs <gateway> --since 5m 2>&1 | grep -ciE "pg|dial|redis|insert|error"'`.
    - Latency: sample `curl -w "%{time_total}"` on a health/chat call.
    ROLLBACK-TRIGGER THRESHOLDS (abort → rollback if any sustained over ~2 min): edge 5xx rate >2% of requests; gateway 401/403 spike above the pre-cutover baseline (auth regression); any DB insert failures on billing writes; redis dial errors; p95 latency >2× the n8n-ia-vm baseline. Below thresholds for the observation window = cutover holds; hand off to the 19-06 soak.

    ROLLBACK COMMAND (record verbatim in SUMMARY): revert the server URL to `http://10.10.10.20:80` + save (hot-reload) → traffic returns to n8n-ia-vm instantly. Keep n8n-ia-vm running until 19-06 soak passes.

    ROLLBACK RECONCILIATION (record; default = accept-split-and-record, do NOT double-count): if rollback occurs at rollback_ts, requests in [cutover_ts, rollback_ts] already wrote billing_events + upserted usage_counters into bd_ai_gateway while post-rollback writes resume in bd_ai_gateway_prod. Default handling: keep the window's rows in bd_ai_gateway as the authoritative record for that window (append-only audit; do NOT replay into _prod → avoids double counting). For an audit reconciliation, export the window from bd_ai_gateway: `psql "$DST" -c "SELECT tenant_id,route,ts,cost_external_brl FROM ai_gateway.billing_events WHERE ts BETWEEN '<cutover_ts>' AND '<rollback_ts>'"` and record the count/sum alongside the _prod archive. usage_counters are per-(tenant,date) upserts the app rebuilds from source-of-truth each cycle — a brief split self-heals; no manual replay. If exact continuity is later required, the recorded window query is the replay source.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer <normal_tenant_key_from_CLAUDE.md>" -X POST https://ai-gateway.converse-ai.app/v1/chat/completions -d '{"model":"...","messages":[{"role":"user","content":"ping"}],"max_tokens":8}' | grep -q 200; psql "$DST" -tc "SELECT count(*) FROM ai_gateway.billing_events WHERE ts > now() - interval '5 min'" | grep -qvE '^\s*0\s*$' # public 200 + fresh billing row in bd_ai_gateway</automated>
  </verify>
  <done>Public hostname served by worker-vm; known prod keys 200 + billing to bd_ai_gateway (embeddings + dashboard also verified); cutover_ts recorded; monitoring commands + thresholds + observation window logged; rollback command + reconciliation procedure documented; n8n-ia-vm still up as rollback target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → edge Traefik → worker-vm | live client traffic + billing crosses here at the flip |
| ops-claude → vps-ifix-vm | SSH edit of the edge file-provider |
| bd_ai_gateway ↔ bd_ai_gateway_prod | billing/usage split across the rollback window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Denial of service | mis-edit of edge file → all tenants 5xx | mitigate | edge preflight proves Host routing pre-flip; back up file; BLOCKING gate; one-line revert + hot-reload rollback; live 5xx monitoring + abort threshold |
| T-19-05-02 | Tampering | accidental cert re-issue / LE rate-limit | mitigate | Server-URL-only edit (DNS unchanged) does not re-trigger TLS-ALPN; memory traefik-ops-rule-dns-first honored |
| T-19-05-03 | Repudiation | billing split across _prod/target on rollback | mitigate | cutover_ts recorded; documented reconciliation (accept-split-and-record, window export query, no double-count); usage_counters self-heal on rebuild |
| T-19-05-04 | Elevation of privilege | auth regression post-flip | mitigate | 401/403 spike monitored vs pre-cutover baseline as a rollback trigger; hashes proven verbatim in 19-04 |
| T-19-05-SC | Tampering | package installs | n/a | Config edit only, no installs |
</threat_model>

<verification>
- Edge preflight (from vps-ifix-vm) → 10.10.10.50:80 Host-routed 200 BEFORE the flip.
- Edge file-provider server URL = 10.10.10.50:80 (+ dashboard); hot-reload clean, backup retained; cutover_ts recorded.
- Public `ai-gateway.converse-ai.app` 200 for the known prod keys + billing rows in bd_ai_gateway; embeddings 200; sensitive-tenant sensitive-503 (auth-OK).
- Public `ai-dashboard.converse-ai.app` served by worker-vm.
- Monitoring commands + rollback thresholds + observation window recorded; rollback command + reconciliation procedure documented.
- DNS/Cloudflare unchanged; n8n-ia-vm still serving as rollback target; ops timers still masked.
</verification>

<success_criteria>
Production traffic is served by the consolidated worker-vm gateway over the unchanged public hostname, verified with the known tenant keys + embeddings + dashboard and billing, with a recorded cutover timestamp, an instant one-line rollback, a defined reconciliation procedure, and live monitoring held open through the soak window.
</success_criteria>

<output>
Create `.planning/phases/19-gateway-consolidation-worker-vm/19-05-SUMMARY.md` when done. Record the exact edit diff, cutover_ts, smoke results (chat/embed/dashboard), monitoring readings + thresholds, and the verbatim rollback + reconciliation procedure.
</output>
</content>
