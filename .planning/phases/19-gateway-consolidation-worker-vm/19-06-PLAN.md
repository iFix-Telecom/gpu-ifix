---
phase: 19-gateway-consolidation-worker-vm
plan: 06
type: execute
wave: 5
depends_on: ["19-05"]
files_modified:
  - "n8n-ia-vm:/opt/ai-gateway-prod, /opt/ai-gateway-embed, /opt/ai-gateway-rerank (compose down)"
  - "n8n-ia-vm:redis-gateway-prod (down)"
  - "portainer:stack/34 ai-gateway-dev (endpoint 3, delete)"
  - "ops-claude:systemd user timers (repoint primary schedule + daily report)"
  - "CLAUDE.md (topology note update)"
autonomous: false
requirements: [DEC-01]

must_haves:
  truths:
    - "The old prod gateway/embed/rerank/redis stacks on n8n-ia-vm are stopped and the dev stack 34 on vps-ifix-vm is deleted"
    - "ops-claude systemd user timers (primary schedule + daily report) target the consolidated worker-vm gateway / bd_ai_gateway"
    - "The public hostname still returns 200 (served by worker-vm) after teardown"
    - "bd_ai_gateway_prod is retained as a billing archive (NOT dropped)"
  artifacts:
    - "n8n-ia-vm: ai-gateway prod/embed/rerank/redis containers stopped (docker ps shows none)"
    - "Portainer: stack 34 removed from endpoint 3"
    - "updated ops-claude timers + CLAUDE.md topology reflecting worker-vm as the single gateway"
  key_links:
    - from: "ops-claude daily report timer"
      to: "worker-vm gateway / bd_ai_gateway"
      via: "repointed host/DSN so the report + primary schedule act on the consolidated stack"
    - from: "public hostname"
      to: "worker-vm (only remaining gateway)"
      via: "edge Traefik (already flipped in 19-05)"
---

<objective>
After a stable soak on the worker-vm gateway, decommission the now-redundant gateways: stop the n8n-ia-vm prod compose stacks (gateway/embed/rerank/redis-gateway-prod) and delete the vps-ifix-vm dev stack 34. Re-point the ops-claude systemd user timers (primary schedule + daily report) at the consolidated stack. Gated by a BLOCKING soak checkpoint — this removes the rollback target, so it only runs once cutover is proven stable.

Purpose: LOCKED — worker-vm becomes the ONLY gateway. Embed already moved to worker-vm (19-02) so teardown does not kill embeds (research Pitfall 1). billing_events history in bd_ai_gateway_prod is kept as archive.
Output: Single consolidated gateway on worker-vm; old stacks torn down; ops timers repointed; topology docs updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-gateway-consolidation-worker-vm/19-RESEARCH.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-05-SUMMARY.md

Teardown targets (research Runtime State Inventory):
- n8n-ia-vm: manual compose at /opt/ai-gateway-prod, /opt/ai-gateway-embed, /opt/ai-gateway-rerank + redis-gateway-prod container.
- vps-ifix-vm: Portainer stack 34 (ai-gateway-dev, endpoint 3, type compose).
- ops-claude systemd USER timers: primary schedule + daily 9:30 report (memory prod-primary-schedule-and-email) — currently may target the n8n-ia-vm prod host/DB.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: soak gate before decommission</name>
  <action>PAUSE — blocking human-verify gate. Do not proceed to the next task until the resume-signal is given. Present the details below and wait for approval.</action>
  <what-built>Cutover (19-05) is live: worker-vm serves the public hostname with real prod keys + billing to bd_ai_gateway. Embed confirmed serving from worker-vm. This checkpoint authorizes tearing down the rollback target (n8n-ia-vm stacks + dev stack 34).</what-built>
  <how-to-verify>
    Confirm the soak before removing rollback:
    1. Cutover stable for the agreed soak window (real client traffic observed, no edge 5xx spike, billing rows accumulating in bd_ai_gateway).
    2. Embed traffic served by worker-vm embed (not n8n-ia-vm) — check gateway logs / an embed call.
    3. You accept that after this, rollback = rebuild (n8n-ia-vm stacks gone). bd_ai_gateway_prod DB is kept as billing archive.
    Approve teardown, or extend the soak.
  </how-to-verify>
  <resume-signal>Type "approved" to decommission, or "soak longer".</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Repoint ops-claude systemd user timers at the consolidated gateway</name>
  <files>ops-claude:systemd user units (primary schedule + daily report)</files>
  <action>
    Inspect the ops-claude systemd USER timers/services for the primary-pod schedule and the daily 9:30 report (memory prod-primary-schedule-and-email). If any unit targets the n8n-ia-vm prod host or `bd_ai_gateway_prod`, repoint it at the worker-vm gateway (`10.10.10.50` / `ai-gateway.converse-ai.app`) and `bd_ai_gateway`. `systemctl --user daemon-reload` + restart the timers. Verify the report runs against the new gateway (dry-run or next-trigger inspection).
    If the timers only call the public hostname / admin API (already flipped), no change needed — record that.
  </action>
  <verify>
    <automated>systemctl --user list-timers | grep -E "report|primary"; # units reference worker-vm/bd_ai_gateway or the public hostname (not the old prod host); a dry-run report call succeeds</automated>
  </verify>
  <done>ops-claude timers target the consolidated gateway; daily report + primary schedule act on worker-vm / bd_ai_gateway.</done>
</task>

<task type="auto">
  <name>Task 2: Tear down old stacks + retain billing archive + update topology docs</name>
  <files>n8n-ia-vm compose stacks, portainer stack 34, CLAUDE.md</files>
  <action>
    With approval given:
    - n8n-ia-vm: `ssh n8n-ia-vm 'cd /opt/ai-gateway-prod && docker compose down'` and same for `/opt/ai-gateway-embed`, `/opt/ai-gateway-rerank`; stop/remove `redis-gateway-prod`. Confirm no ai-gateway containers remain (`docker ps`).
    - vps-ifix-vm: delete Portainer stack 34 (ai-gateway-dev) on endpoint 3 via the API (`DELETE $BASE/stacks/34?endpointId=3`) or UI.
    - Retain `bd_ai_gateway_prod` as billing archive — do NOT drop it.
    - Immediately re-confirm the public hostname is still served (by worker-vm): `curl https://ai-gateway.converse-ai.app/health` → 200 + a real-key chat 200 + billing to bd_ai_gateway.
    - Update CLAUDE.md topology notes (n8n-ia-vm no longer runs the gateway; worker-vm is the single consolidated ai-gateway; embed/rerank/dashboard on worker-vm; bd_ai_gateway is the live DB, bd_ai_gateway_prod archived). Note: CLAUDE.md is local-only, do not commit secrets.
  </action>
  <verify>
    <automated>ssh n8n-ia-vm 'docker ps --format "{{.Names}}" | grep -c -E "ifix-ai-gateway|ai-gateway-embed|ai-gateway-rerank|redis-gateway-prod"' | grep -q '^0$'; curl -s -o /dev/null -w "%{http_code}" https://ai-gateway.converse-ai.app/health | grep -q 200</automated>
  </verify>
  <done>Old prod stacks stopped on n8n-ia-vm; dev stack 34 deleted; public hostname still 200 via worker-vm; bd_ai_gateway_prod archived; CLAUDE.md topology updated. worker-vm is the single gateway.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude → n8n-ia-vm / vps-ifix-vm / Portainer | teardown control plane |
| bd_ai_gateway_prod | retained archive; must not be dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Denial of service | tearing down embed/gateway that still serves traffic | mitigate | BLOCKING soak gate confirms worker-vm embed live before n8n-ia-vm down; immediate post-teardown public 200 re-check |
| T-19-06-02 | Repudiation | dropping billing history | mitigate | bd_ai_gateway_prod explicitly retained as archive (not dropped) |
| T-19-06-03 | Tampering | removing rollback target prematurely | mitigate | Only after 19-05 soak approval; rollback becomes rebuild — acknowledged in checkpoint |
| T-19-06-SC | Tampering | package installs | n/a | Teardown only, no installs |
</threat_model>

<verification>
- n8n-ia-vm: zero ai-gateway/embed/rerank/redis-gateway-prod containers running.
- Portainer stack 34 removed from endpoint 3.
- Public `ai-gateway.converse-ai.app/health` 200 + real-key chat 200 + billing to bd_ai_gateway AFTER teardown.
- ops-claude timers repointed (or confirmed already public-hostname-based).
- bd_ai_gateway_prod retained; CLAUDE.md topology updated.
</verification>

<success_criteria>
worker-vm is the single, Portainer-managed, consolidated ai-gateway (gateway + embed + rerank + dashboard + infra-redis-1), serving all prod tenants over the unchanged public hostname; the old prod + dev gateways are decommissioned; billing history archived.
</success_criteria>

<output>
Create `.planning/phases/19-gateway-consolidation-worker-vm/19-06-SUMMARY.md` when done. Record teardown confirmations, timer repoint status, post-teardown public smoke, and the final consolidated topology.
</output>
