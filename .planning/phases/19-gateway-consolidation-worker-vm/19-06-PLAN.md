---
phase: 19-gateway-consolidation-worker-vm
plan: 06
type: execute
wave: 5
depends_on: ["19-05"]
files_modified:
  - "ops-claude:~/gw-decomm-archive-19/ (root-600 pre-teardown archive of old runtime config)"
  - "n8n-ia-vm:/opt/ai-gateway-prod, /opt/ai-gateway-embed, /opt/ai-gateway-rerank (compose down, dirs+volumes retained)"
  - "n8n-ia-vm:redis-gateway-prod (down)"
  - "portainer:stack/34 ai-gateway-dev (endpoint 3, delete)"
  - "ops-claude:systemd user timers (unmask + repoint primary schedule + daily report)"
  - "CLAUDE.md (topology note update)"
autonomous: false
requirements: [DEC-01]

must_haves:
  truths:
    - "Before ANY teardown, a root-owned archive captures old compose files, env VAR NAMES (not secret values), image DIGESTS, docker inspect / service inspect output, and recent logs — so post-decommission rollback = rebuild is possible"
    - "An emergency REBUILD procedure (exact source locations + steps) is documented in the SUMMARY"
    - "The soak gate is measurable: a concrete duration (24h / one business cycle) + pass metrics (no sustained 5xx, billing rows accumulating in bd_ai_gateway, embeddings + admin + dashboard confirmed)"
    - "The old prod gateway/embed/rerank/redis stacks on n8n-ia-vm are stopped and the dev stack 34 on vps-ifix-vm is deleted"
    - "docker compose down uses a volume-retention policy (networks removed, volumes + compose dirs + env files kept for a retention window)"
    - "ops-claude systemd user timers are UNMASKED and repointed at the consolidated worker-vm gateway / bd_ai_gateway (they were masked in 19-04)"
    - "The public hostname still returns 200 (served by worker-vm) after teardown"
    - "bd_ai_gateway_prod is retained as a billing archive (NOT dropped)"
  artifacts:
    - "ops-claude:~/gw-decomm-archive-19/ root-600: compose yml + env-var-names + image digests + inspect json + recent logs, per old stack"
    - "n8n-ia-vm: ai-gateway prod/embed/rerank/redis containers stopped (docker ps shows none); compose dirs + volumes retained"
    - "Portainer: stack 34 removed from endpoint 3"
    - "updated ops-claude timers (unmasked+repointed) + CLAUDE.md topology reflecting worker-vm as the single gateway"
  key_links:
    - from: "ops-claude daily report + primary schedule timers"
      to: "worker-vm gateway / bd_ai_gateway"
      via: "unmask + repoint host/DSN so they act on the consolidated stack (were masked in 19-04)"
    - from: "public hostname"
      to: "worker-vm (only remaining gateway)"
      via: "edge Traefik (already flipped in 19-05)"
---

<objective>
After a MEASURABLE soak on the worker-vm gateway, decommission the now-redundant gateways: stop the n8n-ia-vm prod compose stacks (gateway/embed/rerank/redis-gateway-prod) and delete the vps-ifix-vm dev stack 34. First pre-archive all old runtime config (root-owned) so rollback-by-rebuild stays possible, then unmask + repoint the ops-claude systemd user timers at the consolidated stack. Gated by a BLOCKING soak checkpoint — this removes the rollback target, so it only runs once cutover is proven stable against concrete metrics.

Purpose: LOCKED — worker-vm becomes the ONLY gateway. Embed already moved to worker-vm (19-02) so teardown does not kill embeds (research Pitfall 1). billing_events history in bd_ai_gateway_prod is kept as archive.
Output: Single consolidated gateway on worker-vm; old stacks torn down (dirs+volumes retained for a retention window); a root-owned rebuild archive; ops timers unmasked+repointed; topology docs updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-gateway-consolidation-worker-vm/19-RESEARCH.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-05-SUMMARY.md

Teardown targets (research Runtime State Inventory):
- n8n-ia-vm: manual compose at /opt/ai-gateway-prod, /opt/ai-gateway-embed, /opt/ai-gateway-rerank + redis-gateway-prod container.
- vps-ifix-vm: Portainer stack 34 (ai-gateway-dev, endpoint 3, type compose).
- ops-claude systemd USER timers: primary schedule + daily 9:30 report (memory prod-primary-schedule-and-email) — MASKED in 19-04, must be unmasked + repointed here. The masked unit names are recorded in 19-04-SUMMARY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-archive old runtime config (root-owned) BEFORE any teardown</name>
  <files>ops-claude:~/gw-decomm-archive-19/</files>
  <action>
    Capture everything needed to rebuild the old stacks, into a root-owned dir `install -d -m 700 ~/gw-decomm-archive-19` (chmod 600 files). Do this while the old stacks are still UP (forensics + rebuild source). Per old stack (ai-gateway-prod, ai-gateway-embed, ai-gateway-rerank, redis-gateway-prod on n8n-ia-vm; stack 34 on vps-ifix-vm):
    - Compose files: `ssh n8n-ia-vm 'cat /opt/ai-gateway-prod/docker-compose.yml'` (+ embed/rerank) → save each. For stack 34, GET the Portainer stack file via API.
    - Env VAR NAMES ONLY (never values — keep secrets out of the archive and out of .planning): `ssh n8n-ia-vm 'cut -d= -f1 /opt/ai-gateway-prod/.env'` (+ embed/rerank).
    - Image DIGESTS (pin for exact repro): `ssh n8n-ia-vm 'docker inspect --format "{{.Image}} {{index .RepoDigests 0}}" ifix-ai-gateway ai-gateway-embed ai-gateway-rerank redis-gateway-prod'`.
    - `docker inspect` for each container; for stack 34 record its service defs via Portainer API.
    - Recent logs: `ssh n8n-ia-vm 'docker logs ifix-ai-gateway --tail 500'` (+ others) for forensic reference.
    Then write the EMERGENCY REBUILD PROCEDURE into 19-06-SUMMARY: exact source locations (this archive + the retained /opt/ai-gateway-* dirs + retained volumes), the image digests to pull, the env var names to repopulate from the worker-vm .env root-600, and the steps to `docker compose up` the old prod gateway pointing back at bd_ai_gateway_prod + flip the edge line back (19-05 rollback command). This makes rollback-by-rebuild concrete.
  </action>
  <verify>
    <automated>ssh ops-claude 'ls ~/gw-decomm-archive-19/ | grep -cE "compose|digest|inspect|env-names"' | grep -qvE '^0$'; test $(ssh ops-claude 'stat -c %a ~/gw-decomm-archive-19') = 700 # archive populated, root-owned 700</automated>
  </verify>
  <done>Root-owned archive holds old compose files, env var names (no values), image digests, inspect output, and recent logs; emergency rebuild procedure documented in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: measurable soak gate before decommission</name>
  <action>PAUSE — blocking human-verify gate. Do not proceed to the next task until the resume-signal is given. Present the measurable soak evidence below and wait for approval.</action>
  <what-built>Cutover (19-05) is live and the old-config rebuild archive is captured. This checkpoint authorizes tearing down the rollback target (n8n-ia-vm stacks + dev stack 34) — only if the soak passes concrete metrics.</what-built>
  <how-to-verify>
    Confirm the MEASURABLE soak before removing rollback:
    1. DURATION: cutover has been live for at least 24 hours / one full business cycle (real client traffic across a business day observed).
    2. PASS METRICS (all must hold over the window):
       - No sustained edge 5xx spike (same monitoring as 19-05 Task 2; brief blips OK, no sustained elevation).
       - billing_events rows accumulating in bd_ai_gateway across the window: `psql "$DST" -tc "SELECT count(*) FROM ai_gateway.billing_events WHERE ts > now() - interval '24 hours'"` > 0 and growing across representative tenants.
       - Embeddings served from worker-vm embed (not n8n-ia-vm) — an embed call 200 + gateway log shows the worker-vm embed upstream.
       - Admin + dashboard confirmed: prod admin `/admin/metrics` 200; dashboard login 200/302.
       - Gateway 401/403 at/below the pre-cutover baseline (no auth regression).
    3. You accept that after this, rollback = REBUILD (n8n-ia-vm stacks gone) — the Task-1 archive + rebuild procedure is the path. bd_ai_gateway_prod DB is kept as billing archive. Old /opt/ai-gateway-* dirs + volumes are retained for a retention window (not deleted on compose down).
    Approve teardown, or extend the soak.
  </how-to-verify>
  <resume-signal>Type "approved" to decommission, or "soak longer".</resume-signal>
</task>

<task type="auto">
  <name>Task 1b: Unmask + repoint ops-claude systemd user timers at the consolidated gateway</name>
  <files>ops-claude:systemd user units (primary schedule + daily report)</files>
  <action>
    Unmask the timers masked in 19-04 (`systemctl --user unmask <units>` — names from 19-04-SUMMARY). Inspect each for the primary-pod schedule + daily 9:30 report (memory prod-primary-schedule-and-email). If any unit targets the n8n-ia-vm prod host or `bd_ai_gateway_prod`, repoint it at the worker-vm gateway (`10.10.10.50` / `ai-gateway.converse-ai.app`) and `bd_ai_gateway`. `systemctl --user daemon-reload` + start/enable the timers. Verify the report runs against the new gateway (dry-run or next-trigger inspection). If the timers only call the public hostname / admin API (already flipped), no repoint needed — record that. Do NOT leave them masked (they were only masked for the migration+cutover window).
  </action>
  <verify>
    <automated>systemctl --user list-timers 2>/dev/null | grep -E "report|primary"; systemctl --user list-unit-files 2>/dev/null | grep -E "report|primary" | grep -vqi masked # units active (not masked), reference worker-vm/bd_ai_gateway or the public hostname</automated>
  </verify>
  <done>ops timers unmasked + repointed; daily report + primary schedule act on worker-vm / bd_ai_gateway (or confirmed public-hostname-based); no unit left masked.</done>
</task>

<task type="auto">
  <name>Task 2: Tear down old stacks (retain dirs+volumes) + retain billing archive + update topology docs</name>
  <files>n8n-ia-vm compose stacks, portainer stack 34, CLAUDE.md</files>
  <action>
    With approval given:
    - n8n-ia-vm: `ssh n8n-ia-vm 'cd /opt/ai-gateway-prod && docker compose down'` and same for `/opt/ai-gateway-embed`, `/opt/ai-gateway-rerank`; stop/remove `redis-gateway-prod`. VOLUME-RETENTION POLICY: plain `docker compose down` (removes containers + networks, KEEPS named volumes) — do NOT pass `-v`/`--volumes`. Leave the `/opt/ai-gateway-*` compose dirs + `.env` files on disk for the retention window (rebuild source). Confirm no ai-gateway containers remain (`docker ps`).
    - vps-ifix-vm: delete Portainer stack 34 (ai-gateway-dev) on endpoint 3 via the API (`DELETE $BASE/stacks/34?endpointId=3`) or UI — only after confirming no internal tooling still points at the dev stack (the ops timers were repointed in Task 1b).
    - Retain `bd_ai_gateway_prod` as billing archive — do NOT drop it.
    - Immediately re-confirm the public hostname is still served (by worker-vm): `curl https://ai-gateway.converse-ai.app/health` → 200 + a real-key chat 200 + billing to bd_ai_gateway.
    - Update CLAUDE.md topology notes (n8n-ia-vm no longer runs the gateway; worker-vm is the single consolidated ai-gateway; embed/rerank/dashboard on worker-vm; bd_ai_gateway is the live DB, bd_ai_gateway_prod archived; note the retained rebuild archive + retention window). CLAUDE.md is local-only — do NOT commit secrets (var names/topology only).
  </action>
  <verify>
    <automated>ssh n8n-ia-vm 'docker ps --format "{{.Names}}" | grep -c -E "ifix-ai-gateway|ai-gateway-embed|ai-gateway-rerank|redis-gateway-prod"' | grep -q '^0$'; curl -s -o /dev/null -w "%{http_code}" https://ai-gateway.converse-ai.app/health | grep -q 200</automated>
  </verify>
  <done>Old prod stacks stopped on n8n-ia-vm (dirs+volumes retained, no -v); dev stack 34 deleted; public hostname still 200 via worker-vm; bd_ai_gateway_prod archived; CLAUDE.md topology updated. worker-vm is the single gateway.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude → n8n-ia-vm / vps-ifix-vm / Portainer | teardown control plane |
| ~/gw-decomm-archive-19/ | root-owned rebuild archive (env var NAMES only, no secret values) |
| bd_ai_gateway_prod | retained archive; must not be dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Denial of service | tearing down embed/gateway that still serves traffic | mitigate | measurable soak gate confirms worker-vm embed live before n8n-ia-vm down; immediate post-teardown public 200 re-check |
| T-19-06-02 | Repudiation | dropping billing history | mitigate | bd_ai_gateway_prod explicitly retained as archive (not dropped) |
| T-19-06-03 | Tampering | removing rollback target with no rebuild path | mitigate | pre-teardown root-owned archive (compose/env-names/digests/inspect/logs) + documented emergency rebuild procedure; dirs+volumes retained (no `-v`) |
| T-19-06-04 | Information disclosure | archive/CLAUDE.md leaking secrets | mitigate | env var NAMES only (no values); CLAUDE.md local-only; archive chmod 600 root-owned |
| T-19-06-05 | Elevation of privilege | timers left masked / pointing at wrong DB | mitigate | Task 1b unmasks + repoints before teardown; verify not masked |
| T-19-06-SC | Tampering | package installs | n/a | Teardown only, no installs |
</threat_model>

<verification>
- Pre-teardown root-owned archive populated (compose/env-names/digests/inspect/logs); emergency rebuild procedure documented.
- Measurable soak passed (≥24h/business cycle; no sustained 5xx; billing accumulating in bd_ai_gateway; embed+admin+dashboard confirmed).
- n8n-ia-vm: zero ai-gateway/embed/rerank/redis-gateway-prod containers; compose dirs + volumes retained (no `-v`).
- Portainer stack 34 removed from endpoint 3 (after tooling-repoint confirmed).
- ops timers unmasked + repointed (or confirmed public-hostname-based); no unit masked.
- Public `ai-gateway.converse-ai.app/health` 200 + real-key chat 200 + billing to bd_ai_gateway AFTER teardown.
- bd_ai_gateway_prod retained; CLAUDE.md topology updated (no secrets).
</verification>

<success_criteria>
worker-vm is the single, Portainer-managed, consolidated ai-gateway (gateway + embed + rerank + dashboard + infra-redis-1), serving all prod tenants over the unchanged public hostname; the old prod + dev gateways are decommissioned after a measurable soak, with a root-owned rebuild archive + retained dirs/volumes/DB archive making rollback-by-rebuild concrete; ops timers unmasked+repointed; billing history archived.
</success_criteria>

<output>
Create `.planning/phases/19-gateway-consolidation-worker-vm/19-06-SUMMARY.md` when done. Record the archive contents + emergency rebuild procedure, the soak metrics observed, teardown confirmations, timer unmask/repoint status, volume-retention note, post-teardown public smoke, and the final consolidated topology.
</output>
</content>
