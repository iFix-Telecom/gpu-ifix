---
phase: 19-gateway-consolidation-worker-vm
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - "portainer:stack/ai-gateway-prod (endpoint 6, swarm compose-string)"
  - "portainer:stack/ai-gateway-embed (endpoint 6, swarm compose-string)"
  - "worker-vm:.env root-600 (gateway secrets — values user-provided, NOT committed)"
autonomous: true
requirements: [CONS-01, CONS-02, EMB-01, UI-01, NET-01]
user_setup:
  - service: worker-vm .env (root-600)
    why: "Real DSN / OpenAI+OpenRouter+Gemini bearers / R2 secret live here; Portainer env array references structure, values are operator-provided"
    env_vars:
      - name: AI_GATEWAY_PG_DSN
        source: "DO managed PG → bd_ai_gateway (sslmode=require)"
      - name: MINIO_* (R2 endpoint/bucket/creds)
        source: "Cloudflare R2 bucket ai-gateway-weights"

must_haves:
  truths:
    - "The consolidated ai-gateway swarm service is running on worker-vm, reading bd_ai_gateway + infra-redis-1 + R2, editable in the Portainer UI (endpoint 6)"
    - "A bge-m3 embed service runs on worker-vm and the gateway's UPSTREAM_EMBED_URL points at it (not n8n-ia-vm) so embed survives n8n-ia-vm decommission"
    - "UPSTREAM_EMBED_URL=http://embed:7997 resolves from the GATEWAY task's own network namespace (not just a debug container)"
    - "Gateway + embed images are pinned by DIGEST for the migration (not :main / :latest-dev)"
    - "172.18.0.1:18000 is explicitly documented as intentionally tier-1-only (dormant pod, GPU-less VM) — not a latent-broken path"
    - "curl http://10.10.10.50:80/health via internal Traefik returns 200"
    - "A DEV tenant key returns 200 on POST /v1/chat/completions AND a billing_events row appears in bd_ai_gateway"
    - "X-Admin-Key (dev admin) → /admin/metrics returns 200"
  artifacts:
    - "Portainer swarm stack 'ai-gateway-prod' on endpoint 6 (gateway + FRESH redis aliased infra-redis-1), UI-editable, env in env array, image pinned by digest"
    - "Portainer swarm stack 'ai-gateway-embed' on endpoint 6 (Infinity bge-m3, CPU, image pinned by digest)"
  key_links:
    - from: "gateway service"
      to: "bd_ai_gateway (DO PG :25060)"
      via: "AI_GATEWAY_PG_DSN, MIGRATE_ON_BOOT=false"
    - from: "gateway service"
      to: "embed service (worker-vm)"
      via: "UPSTREAM_EMBED_URL → overlay DNS embed:7997 (NOT 10.10.10.20:7997), verified from gateway netns"
    - from: "edge/internal Traefik"
      to: "gateway:8080"
      via: "router labels Host(ai-gateway.converse-ai.app) from 19-01 recon"
---

<objective>
Stand the consolidated gateway + embed up on worker-vm as UI-editable Portainer swarm stacks, wired to the dev-style target env (bd_ai_gateway, R2 weights, infra-redis-1, local-embed, tier-1 LLM/STT fallback). This runs in PARALLEL with the live prod gateway — both read the same DO cluster but DIFFERENT dbs (`_prod` still serves clients; new stack reads `bd_ai_gateway`), so there is no write contention and no client impact yet.

Purpose: Deliverables #1 (stacks on worker-vm) + #2 (dev-style env) + #3 (Portainer-managed). Embed must move to worker-vm now (research Pitfall 1) so it does not die when n8n-ia-vm is decommissioned.
Output: Two swarm stacks live on endpoint 6 (both digest-pinned), validated directly (pre-cutover) against a DEV tenant. Prod-key validation happens after the DB migration (19-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-CONTEXT.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-RESEARCH.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-01-SUMMARY.md

Baseline stacks to convert (pull live, do NOT reference secrets):
- prod gateway compose: `ssh n8n-ia-vm 'cat /opt/ai-gateway-prod/docker-compose.yml'` (+ its .env for VAR NAMES only)
- prod embed compose: `ssh n8n-ia-vm 'cat /opt/ai-gateway-embed/docker-compose.yml'`
- dev gateway stack 34 (env shape reference): Portainer endpoint 3 stack id 34
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + deploy the ai-gateway-prod swarm stack (compose-string, endpoint 6)</name>
  <files>portainer:stack/ai-gateway-prod, worker-vm:.env</files>
  <action>
    Convert the standalone `/opt/ai-gateway-prod` compose to a swarm compose-string per research Pattern 2: drop `container_name`, replace `restart:` with `deploy.restart_policy.condition: any`, attach services to the external `ai-gateway` overlay (from 19-01), remove host-port publish in favor of the internal-Traefik router labels captured in 19-01 recon (Host(ai-gateway.converse-ai.app) → gateway service port 8080). Include a `redis: image redis:7.4.2` service with `networks: {ai-gateway: {aliases: [infra-redis-1]}}` so the hardcoded `infra-redis-1:6379` resolves with zero env change.
    REDIS DECISION (record explicitly, Codex MEDIUM): this is a FRESH Redis dedicated to the consolidated gateway (NOT the pre-existing `redis_redis` on worker-vm). Accepted behavior change: rate-limit counters / quota mirror / FSM state start empty and the app rebuilds all durable state from Postgres (bd_ai_gateway) — Redis is treated as ephemeral cache, so a cold Redis at cutover is safe. Name the service clearly (alias `infra-redis-1`) and document it in SUMMARY as the live gateway Redis.
    Image: PIN BY DIGEST for the migration (Codex MEDIUM — `:main` drifts). Resolve the current `ghcr.io/ifixtelecom/converseai-gateway:main` digest and deploy `ghcr.io/ifixtelecom/converseai-gateway@sha256:<digest>` (promoted prod image, matches goose v31); record the digest in SUMMARY for rollback/repro. Confirm the deploy by behavior/billing rows, not by rev label (STATE: VM builds bake version="dev").
    Env goes in the Portainer `env` array (UI Environment-variables panel), values sourced from the worker-vm `.env` root-600 — NEVER baked into stackFileContent. Required env (dev-style, per 19-CONTEXT env-alvo): `AI_GATEWAY_PG_DSN`→bd_ai_gateway (sslmode=require), `AI_GATEWAY_REDIS_ADDR=infra-redis-1:6379`, `AI_GATEWAY_MIGRATE_ON_BOOT=false` (do NOT flip true — v31 already applied), UPSTREAM_LLM/STT/HEALTH=`http://172.18.0.1:18000` (dormant pod; tier-1 fallback active), `UPSTREAM_EMBED_URL` set in Task 2, `MINIO_*`→R2 (bucket ai-gateway-weights), `WEIGHTS_QWEN_KEY` (known-gap: references qwen3.5 absent in R2 — non-blocking, no pod runs), tier-1 bearers (OpenRouter/Gemini/OpenAI), `PRIMARY_POD_SCHEDULE_DISABLED=true`, `LOG_LEVEL=debug`, emergency/primary tunables per env-alvo.
    Deploy via the confirmed Portainer swarm-string route (19-01 Task 1) with the captured swarmID. Since the Portainer webhook does NOT pull images (research Pitfall 4), if the service does not pull the intended digest, force it: `docker service update --image ghcr.io/ifixtelecom/converseai-gateway@sha256:<digest> --force <gateway-svc>`.
    172.18.0.1:18000 RESOLUTION (Codex MEDIUM — do NOT leave latent-broken/ambiguous): DECIDE IT tier-1-only. No pod runs on worker-vm (GPU-less) and `PRIMARY_POD_SCHEDULE_DISABLED=true`, so `:18000` is intentionally dormant/unreachable and the gateway operates on tier-1 fallback (OpenRouter/Gemini) exactly as dev does today. The gateway's upstream probe for `:18000` WILL show unhealthy — that is EXPECTED, not a defect. Confirm in SUMMARY that no upstream health-probe failure on `:18000` blocks serving (breaker-open on the tier-0 local upstream falls through to tier-1). Keep the env value `http://172.18.0.1:18000` as the documented future-pod target; when a pod is ever added, switch to the host LAN IP `10.10.10.50:18000` or attach the gateway to the host bridge (recorded as the future path).
  </action>
  <verify>
    <automated>ssh worker-vm 'docker service ls --filter name=ai-gateway-prod --format "{{.Name}} {{.Replicas}}"' | grep -E "1/1"; ssh worker-vm 'docker service inspect ai-gateway-prod_gateway --format "{{.Spec.TaskTemplate.ContainerSpec.Image}}"' | grep -q '@sha256:'; curl -s -o /dev/null -w "%{http_code}" http://10.10.10.50:80/health # 1/1, digest-pinned, → 200 via internal Traefik Host route</automated>
  </verify>
  <done>ai-gateway-prod swarm stack running on endpoint 6, UI-editable, image digest-pinned, /health 200 via internal Traefik, reading bd_ai_gateway + fresh infra-redis-1 + R2; 172.18.0.1:18000 documented tier-1-only.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy the ai-gateway-embed swarm stack + point the gateway at it (verify from gateway netns)</name>
  <files>portainer:stack/ai-gateway-embed, portainer:stack/ai-gateway-prod (env update)</files>
  <action>
    Convert `/opt/ai-gateway-embed` (Infinity bge-m3, CPU) to a swarm compose-string stack on endpoint 6, attached to the `ai-gateway` overlay, service name `embed` exposing :7997 on the overlay (worker-vm has ~9G free; bge-m3 CPU ~2-4G — research A6, measure under load). No host port; reachable by overlay DNS `embed:7997`. PIN the embed image by DIGEST (Codex MEDIUM) and record it in SUMMARY.
    Update the ai-gateway-prod env `UPSTREAM_EMBED_URL=http://embed:7997` (worker-vm-local) — do NOT leave it at `http://10.10.10.20:7997` (n8n-ia-vm), which dies at decommission (Pitfall 1). Force the gateway service to pick up the env change: `docker service update --force <gateway-svc>` (or redeploy the stack).
    VERIFY FROM THE GATEWAY TASK NETNS (Codex HIGH — cross-Portainer-stack overlay DNS must resolve from the gateway itself, not just a throwaway curl container): exec inside the running gateway task and hit embed: `ssh worker-vm 'GTID=$(docker ps -q -f name=ai-gateway-prod_gateway | head -1); docker exec $GTID sh -c "wget -qO- http://embed:7997/health || curl -s http://embed:7997/health"'` → healthy. Also record an embed LATENCY sample under an expected payload size (a real /v1/embeddings call in Task 3) so CPU-embed slowness surfaces now, not under prod load.
  </action>
  <verify>
    <automated>ssh worker-vm 'docker service ls --filter name=ai-gateway-embed --format "{{.Replicas}}"' | grep 1/1; ssh worker-vm 'GTID=$(docker ps -q -f name=ai-gateway-prod_gateway | head -1); docker exec $GTID sh -c "wget -qO- http://embed:7997/health 2>/dev/null || curl -s http://embed:7997/health"' | grep -qiE "ok|healthy|200|model" # embed reachable FROM the gateway netns</automated>
  </verify>
  <done>Embed running on worker-vm (digest-pinned); gateway UPSTREAM_EMBED_URL points at worker-vm embed (overlay DNS) and resolves FROM the gateway task netns; latency sampled; embed no longer depends on n8n-ia-vm.</done>
</task>

<task type="auto">
  <name>Task 3: Validate the consolidated gateway directly (pre-cutover, DEV tenant)</name>
  <files>validation only (no resource changes)</files>
  <action>
    Prove the worker-vm gateway is fully functional against `bd_ai_gateway` BEFORE any prod-key migration or cutover, using the existing DEV tenant/admin already in bd_ai_gateway (4 dev tenants / dev admin). Do NOT touch the edge Traefik. Test via the internal Traefik `http://10.10.10.50:80`:
    - `GET /health` → 200.
    - Create (if needed) or use a dev tenant key: `POST /v1/chat/completions` with a small prompt → 200; then confirm a fresh `billing_events` row lands in `bd_ai_gateway` (SELECT ... ORDER BY ts DESC LIMIT 1, route=chat).
    - Embed: `POST /v1/embeddings` (or the gateway's embed route) → 200 with vectors; confirm billing row route=embed AND record the round-trip latency (CPU bge-m3 sanity, research A6).
    - Admin: `X-Admin-Key: <dev admin>` → `GET /admin/metrics` → 200.
    Record results in SUMMARY (these become the pre-cutover green baseline, including the deployed image digests + embed latency).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer <dev_key>" -X POST http://10.10.10.50:80/v1/chat/completions -d '{"model":"...","messages":[{"role":"user","content":"ping"}]}' # → 200; psql bd_ai_gateway confirms a new billing_events row</automated>
  </verify>
  <done>Gateway proven green on bd_ai_gateway (chat 200 + billing write, embed 200 + latency sampled, admin 200) using DEV tenant — parallel to live prod, zero client impact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → gateway | Bearer tenant keys authenticate; key_lookup_hash checked in bd_ai_gateway |
| gateway → DO PG / R2 / tier-1 providers | outbound secrets (DSN, R2 creds, bearers) |
| ops-claude → Portainer/SSH | deploy control plane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Information disclosure | DSN/R2/bearer secrets | mitigate | Secrets only in worker-vm .env root-600 + Portainer env array; never in stackFileContent or committed .planning files |
| T-19-02-02 | Tampering | wrong DB (bd_ai_gateway_prod) | mitigate | Pin DSN to bd_ai_gateway; keep MIGRATE_ON_BOOT=false (Pitfall 5) — running against _prod would double-serve |
| T-19-02-03 | Denial of service | embed CPU OOM / slowness on worker-vm | accept | ~9G free fits bge-m3 CPU; latency sampled in Task 2/3; tier-1 OpenAI embed fallback exists |
| T-19-02-04 | Elevation of privilege | dev admin key on consolidated stack | accept | Pre-cutover only; prod admin_keys reconciled in 19-04 |
| T-19-02-05 | Tampering | image tag drift (:main) during migration | mitigate | gateway + embed pinned by @sha256 digest; digests recorded in SUMMARY |
| T-19-02-SC | Tampering | package installs | n/a | Image redeploy only, no package installs |
</threat_model>

<verification>
- ai-gateway-prod + ai-gateway-embed swarm stacks running on endpoint 6, both editable in Portainer UI, both digest-pinned.
- Internal Traefik `http://10.10.10.50:80/health` = 200.
- embed:7997 resolves + healthy FROM the gateway task netns (not just a debug container); embed latency sampled.
- DEV tenant chat 200 + billing_events row in bd_ai_gateway; embed 200; admin 200.
- Gateway UPSTREAM_EMBED_URL points at worker-vm embed (overlay DNS), not n8n-ia-vm.
- 172.18.0.1:18000 documented tier-1-only (dormant pod), not blocking serving; fresh Redis documented as ephemeral gateway cache.
- Live prod on n8n-ia-vm still serving (`curl http://10.10.10.20:80/health` 200) — no contention.
</verification>

<success_criteria>
Consolidated gateway + embed live on worker-vm, dev-style env, Portainer-managed, digest-pinned, proven green against bd_ai_gateway with a DEV tenant (embed reachable from the gateway netns) — ready to receive prod tenants (19-04) and public traffic (19-05).
</success_criteria>

<output>
Create `.planning/phases/19-gateway-consolidation-worker-vm/19-02-SUMMARY.md` when done. Record stack IDs, the deployed image DIGESTS, the 172.18.0.1:18000 tier-1-only decision, the fresh-Redis note, the gateway-netns embed verification + latency, and the pre-cutover green baseline results.
</output>
</content>
