---
phase: 19-gateway-consolidation-worker-vm
plan: 04
type: execute
wave: 3
depends_on: ["19-02", "19-03"]
files_modified:
  - "bd_ai_gateway:tenants (DELETE dev-colliding + COPY 18 prod)"
  - "bd_ai_gateway:api_keys (COPY 19 prod, hash-verbatim)"
  - "bd_ai_gateway:admin_keys (COPY prod)"
  - "bd_ai_gateway:usage_counters (COPY prod current-period)"
autonomous: false
requirements: [DB-01, DB-02]

must_haves:
  truths:
    - "bd_ai_gateway holds the 18 prod tenants + 19 prod api_keys with key_lookup_hash / key_hash byte-for-byte preserved (no key regeneration)"
    - "The prod admin key (X-Admin-Key) authenticates against the worker-vm gateway"
    - "Every real prod tenant key tested returns 200 + writes a billing_events row to bd_ai_gateway"
    - "billing_events history stays archived in bd_ai_gateway_prod (not copied)"
    - "A pre-migration backup of both DBs exists on ops-claude for rollback"
  artifacts:
    - "pg_dump backups of bd_ai_gateway (pre) + bd_ai_gateway_prod (source) on ops-claude"
    - "COPY-format data-only dump of prod core tables (tenants, api_keys, admin_keys, usage_counters)"
    - "bd_ai_gateway with tenants=18(+non-colliding dev survivors), api_keys=19, admin_keys matching source"
  key_links:
    - from: "api_keys.tenant_id"
      to: "tenants.id (prod UUIDs preserved)"
      via: "DELETE-dev-first so prod UUIDs win the UNIQUE(slug) + FK resolves"
    - from: "prod tenant Bearer key"
      to: "worker-vm gateway auth"
      via: "key_lookup_hash lookup against bd_ai_gateway"
---

<objective>
Migrate the production tenant/key set from `bd_ai_gateway_prod` into `bd_ai_gateway` so the ~18 real client apps keep authenticating against the consolidated worker-vm gateway WITHOUT key regeneration. This is the irreversible DB step (it deletes throwaway dev tenants and loads prod rows) — it is its own atomic plan, gated by a BLOCKING human checkpoint, with a full backup for rollback.

Purpose: LOCKED decision — migrate tenants + api_keys (preserve HASH) + admin_keys + current-period usage_counters; leave the 33k billing_events in `_prod` as archive (research recommendation A5). Slug collisions with the 4 dev tenants are resolved DELETE-dev-first so prod UUIDs win and api_keys FK resolves.
Output: bd_ai_gateway carries the prod tenant/key/admin/quota state; every prod key proven to authenticate + bill against the worker-vm gateway (still pre-cutover — no public traffic yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-gateway-consolidation-worker-vm/19-CONTEXT.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-RESEARCH.md
@.planning/phases/19-gateway-consolidation-worker-vm/19-02-SUMMARY.md

FK order + strategy (verified from gateway/db/migrations/): tenants → api_keys (CASCADE, UNIQUE key_lookup_hash BYTEA) → usage_counters (CASCADE) → billing_events (PARTITIONED, NOT copied); admin_keys independent (no tenant FK).
DB access: from ops-claude (DO whitelists 162.55.92.154). SRC=bd_ai_gateway_prod, DST=bd_ai_gateway, both on db-grupoifix-...:25060, sslmode=require. Real DSN creds NOT in this file — operator-provided.
Representative prod keys for the auth proof (from CLAUDE.md): chat-ifix (ifix_sk_npday2…), converseai, telefonia (sensitive — expect 503 upstream_unavailable if breaker open, that is correct behavior, not an auth fail). Prod admin ifix_admin_49e4….
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — enumerate collisions, back up, dump prod core tables</name>
  <files>ops-claude:/tmp backups + gw-core.sql</files>
  <action>
    From ops-claude:
    1. Full safety backups: `pg_dump "$DST"` (bd_ai_gateway pre-state) and `pg_dump "$SRC"` (bd_ai_gateway_prod) to timestamped files on ops-claude (rollback source).
    2. Enumerate ACTUAL dev slugs (research A3 — don't assume): `psql "$DST" -c "SELECT slug FROM ai_gateway.tenants ORDER BY slug"` and prod slugs `psql "$SRC" -c "SELECT slug FROM ai_gateway.tenants ORDER BY slug"`. Compute the colliding set + record the full dev slug list to DELETE (dev data is throwaway — safest is delete ALL dev tenants).
    3. Data-only COPY-format dump of the prod core tables preserving UUID/BYTEA exactly:
       `pg_dump "$SRC" --data-only --no-owner --schema=ai_gateway --table=ai_gateway.tenants --table=ai_gateway.api_keys --table=ai_gateway.admin_keys --table=ai_gateway.usage_counters -f /tmp/gw-core.sql`
       For usage_counters, restrict to current-period rows if the table is large (research Open Q2) — filter by date >= current month.
    4. Capture source counts: tenants (=18), api_keys (=19), admin_keys.
    Do NOT modify any DB in this task.
  </action>
  <verify>
    <automated>test -s /tmp/gw-core.sql && psql "$SRC" -tc "SELECT count(*) FROM ai_gateway.tenants" | grep -qE '18' && ls -la /tmp/*bd_ai_gateway*pre*.dump # backups + dump present, source counts captured</automated>
  </verify>
  <done>Backups of both DBs exist; dev slug collision list enumerated; COPY-format prod core dump created; source counts recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: approve DELETE-dev-first + COPY load</name>
  <action>PAUSE — blocking human-verify gate. Do not proceed to the next task until the resume-signal is given. Present the details below and wait for approval.</action>
  <what-built>Pre-flight complete: backups of bd_ai_gateway + bd_ai_gateway_prod taken; the exact dev-tenant slug DELETE list enumerated; a COPY-format data-only dump of prod tenants/api_keys/admin_keys/usage_counters ready to load.</what-built>
  <how-to-verify>
    Review before the irreversible load:
    1. The DELETE list — confirm these slugs are all throwaway dev tenants (expected: converseai, chat-ifix, uat10-test + any other dev-only). The corresponding prod slugs will replace them with prod UUIDs.
    2. Source counts: tenants=18, api_keys=19, admin_keys=<n>. billing_events (33k) is intentionally NOT migrated (stays archived in _prod).
    3. The transaction plan: BEGIN → DELETE colliding dev tenants (CASCADE removes their api_keys+usage_counters) → load prod rows with `psql -1` (single transaction; hard-errors + rolls back on any PK/UNIQUE dup, e.g. an admin_keys key_lookup_hash collision — delete the dev admin row first if flagged).
    4. Rollback: bd_ai_gateway is disposable dev data; if anything goes wrong, restore from the pre-dump. No public traffic hits this DB yet (pre-cutover).
    Approve to execute the load, or describe changes to the DELETE list.
  </how-to-verify>
  <resume-signal>Type "approved" to run the DELETE-dev-first + COPY load, or describe adjustments.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute migration + prove prod keys authenticate against worker-vm gateway</name>
  <files>bd_ai_gateway:tenants, api_keys, admin_keys, usage_counters</files>
  <action>
    On DST, one transaction: `BEGIN; SET search_path=ai_gateway,public; DELETE FROM ai_gateway.tenants WHERE slug IN (<enumerated dev slugs>);` (CASCADE drops dev api_keys+usage_counters). If Task-1/checkpoint flagged an admin_keys key_lookup_hash collision, `DELETE FROM ai_gateway.admin_keys WHERE ...` the dev admin row too. Then load: `psql "$DST" -1 -f /tmp/gw-core.sql` (single transaction, aborts whole load on any error — safe against the astronomically-unlikely UUID PK dup).
    Verify counts BEFORE trusting: tenants=18 (+ non-colliding dev survivors), api_keys=19, admin_keys matches source. Spot-check a known key row: `SELECT tenant_id,key_prefix,status,data_class FROM api_keys WHERE key_prefix LIKE 'ifix_sk_%npday%'` (chat-ifix) — status/data_class intact, hash columns present.
    Keep `MIGRATE_ON_BOOT=false`; no schema migration needed (target is v31, matches code).
    THEN prove end-to-end against the worker-vm gateway (internal Traefik, still pre-cutover): for 2-3 real prod keys (chat-ifix, converseai — a `normal` tenant) `POST http://10.10.10.50:80/v1/chat/completions` → 200 + confirm a billing_events row in bd_ai_gateway. For a `sensitive` tenant (telefonia/cobrancas) a 503 `upstream_unavailable_for_sensitive_tenant` with the primary breaker open is CORRECT (RES-08), not an auth failure — verify it authenticated (not 401). Prod admin: `X-Admin-Key: ifix_admin_49e4…` → `/admin/metrics` → 200.
  </action>
  <verify>
    <automated>psql "$DST" -tc "SELECT count(*) FROM ai_gateway.api_keys" | grep -qE '19'; for k in chat-ifix converseai; do curl -s -o /dev/null -w "%{http_code}\n" -H "Authorization: Bearer <real_key>" -X POST http://10.10.10.50:80/v1/chat/completions -d '{...}'; done | grep -q 200 # + new billing_events rows in bd_ai_gateway; admin /admin/metrics 200</automated>
  </verify>
  <done>18 tenants / 19 api_keys / admin_keys / current-period usage_counters migrated hash-verbatim; every tested prod key authenticates (200 + billing write, or correct sensitive-503) and the prod admin key works — against the worker-vm gateway, still pre-cutover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-claude → DO PG (both dbs) | pg_dump/psql over sslmode=require; carries key hashes |
| dump file on ops-claude | contains key_hash/key_lookup_hash BYTEA (opaque, not raw keys) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Information disclosure | /tmp/gw-core.sql + backups (key hashes) | mitigate | Dumps live on ops-claude root-owned; delete gw-core.sql after load; hashes are opaque (raw keys not stored) — no plaintext key exposure |
| T-19-04-02 | Tampering | wrong DELETE (prod slug in list) | mitigate | Enumerate actual slugs + BLOCKING checkpoint review before DELETE; full backup enables restore |
| T-19-04-03 | Denial of service | partial load / FK break | mitigate | `psql -1` single transaction rolls back entirely on any error; count-verify before trusting |
| T-19-04-04 | Repudiation | quota under-count mid-cycle | accept | current-period usage_counters copied to minimize; QuotaFailOpen=false grace, not a break |
| T-19-04-SC | Tampering | package installs | n/a | No installs — psql/pg_dump only |
</threat_model>

<verification>
- bd_ai_gateway: tenants=18(+survivors), api_keys=19, admin_keys matches source; spot-checked key row intact (status/data_class/hash).
- 2-3 real prod keys → 200 + billing_events row in bd_ai_gateway (or correct sensitive-503, authenticated not 401).
- Prod admin key → /admin/metrics 200.
- billing_events NOT copied (archive stays in _prod); gw-core.sql deleted post-load.
- Backups of both DBs retained on ops-claude.
</verification>

<success_criteria>
The consolidated gateway now authenticates the full production tenant/key set against bd_ai_gateway with hashes preserved and admin ops intact — proven live pre-cutover. Cutover (19-05) is now a pure ingress flip.
</success_criteria>

<output>
Create `.planning/phases/19-gateway-consolidation-worker-vm/19-04-SUMMARY.md` when done. Record final counts, the dev slug DELETE list used, backup paths, and the per-key auth proof results.
</output>
