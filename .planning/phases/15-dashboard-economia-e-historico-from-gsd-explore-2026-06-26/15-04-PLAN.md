---
phase: 15-dashboard-economia-e-historico
plan: 04
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - dashboard/src/lib/gateway.ts
  - dashboard/src/lib/gateway.test.ts
  - dashboard/src/app/(dashboard)/incidents/page.tsx
  - dashboard/src/components/audit-table.tsx
autonomous: true
requirements: [OBS-10]

must_haves:
  truths:
    - "/incidents has a date-range picker and a search box above the table"
    - "The pager uses a real total (offset+limit < total), not a row-count heuristic"
    - "fetchAudit forwards from/to/search params to the endpoint"
  artifacts:
    - path: "dashboard/src/lib/gateway.ts"
      provides: "AuditResponse.total + fetchAudit(limit, offset, from, to, search)"
      contains: "total"
    - path: "dashboard/src/app/(dashboard)/incidents/page.tsx"
      provides: "/incidents date-range + search controls"
      contains: "Calendar"
    - path: "dashboard/src/components/audit-table.tsx"
      provides: "pager driven by total"
      contains: "total"
  key_links:
    - from: "dashboard/src/app/(dashboard)/incidents/page.tsx"
      to: "fetchAudit"
      via: "useQuery queryFn with from/to/search"
      pattern: "fetchAudit"
    - from: "dashboard/src/components/audit-table.tsx"
      to: "total"
      via: "canNext = offset + limit < total"
      pattern: "offset \\+ limit < total"
---

<objective>
Build the OBS-10 frontend: extend the audit client wrapper for the new total + filter params, add a
date-range picker and search box to `/incidents`, and convert the pager from a row-count heuristic to
honest pagination driven by the real `total`.

Purpose: Make the incident history navigable — filter by period, search by route/reason/code, and
page with accurate bounds.
Output: gateway.ts AuditResponse.total + fetchAudit(from,to,search) + test, incidents/page.tsx
controls, audit-table.tsx total-driven pager.

Depends on 15-02 for the endpoint (from/to/search/total) and on 15-03 because both edit the shared
dashboard/src/lib/gateway.ts and gateway.test.ts (serialize to avoid a merge conflict).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-RESEARCH.md
@.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-PATTERNS.md
@.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs — already extracted. -->

AuditResponse currently (gateway.ts:144-148) has NO total — add `total: number` (and update the
"no total" comment at l.141-143). fetchAudit (gateway.ts:333-338) currently takes (limit, offset)
only — extend to (limit=50, offset=0, from?, to?, search?) forwarding optional params to
proxyGet<AuditResponse>("audit", {...}). Browser never reads GATEWAY_ADMIN_KEY (T-07-24).

Date-range helpers to COPY from consumo/page.tsx: isoDate() (l.65-67), currentMonthRange()
(l.69-76), Popover+Calendar block (l.140-163). Search box: Input from @/components/ui/input
(verified to exist).

incidents/page.tsx existing useQuery at l.34-37; loading/error branches l.55-70 (keep).

audit-table.tsx pager: AuditTableProps l.35-43; inferred canNext = rows.length >= limit at l.73
(plus comment l.71-72 and docstring l.14-17); pager UI l.125-148; row key={row.request_id} l.100.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuditResponse.total + fetchAudit(from/to/search) in gateway.ts + vitest</name>
  <files>dashboard/src/lib/gateway.ts, dashboard/src/lib/gateway.test.ts</files>
  <read_first>
    - dashboard/src/lib/gateway.ts (AuditResponse l.141-148; fetchAudit l.333-338; proxyGet l.298-322)
    - dashboard/src/lib/gateway.test.ts (existing fetchAudit test if present; wrapper test pattern)
  </read_first>
  <behavior>
    - Test: fetchAudit(50, 0, "2026-06-01", "2026-06-30", "503") calls proxy path "audit" with params {limit:"50", offset:"0", from, to, search}
    - Test: fetchAudit(50, 0) omits from/to/search keys (optional params not forwarded when undefined)
    - Test: an AuditResponse fixture including `total` typechecks and round-trips
  </behavior>
  <action>
    In gateway.ts add `total: number` to the AuditResponse interface and update the l.141-143 comment
    that claims no total. Extend fetchAudit to signature `(limit = 50, offset = 0, from?, to?,
    search?)`, forwarding `limit`/`offset` always and conditionally spreading `from`/`to`/`search`
    only when truthy into `proxyGet<AuditResponse>("audit", {...})`. Add/extend vitest cases in
    gateway.test.ts covering the <behavior>. Coordinate with 15-03's edits to the same files (this
    plan runs after 15-03 — re-read the current gateway.ts before editing).
  </action>
  <verify>
    <automated>cd dashboard && bun run test src/lib/gateway.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'total' dashboard/src/lib/gateway.ts` within the AuditResponse interface (total: number)
    - fetchAudit signature includes optional from/to/search and forwards them only when truthy
    - `cd dashboard && bun run test src/lib/gateway.test.ts` exits 0
    - `bunx tsc --noEmit` reports no new type errors
  </acceptance_criteria>
  <done>AuditResponse carries total; fetchAudit forwards filter params; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: /incidents date-range + search controls + total-driven pager</name>
  <files>dashboard/src/app/(dashboard)/incidents/page.tsx, dashboard/src/components/audit-table.tsx</files>
  <read_first>
    - dashboard/src/app/(dashboard)/incidents/page.tsx (useQuery l.34-37; loading/error l.55-70)
    - dashboard/src/app/(dashboard)/consumo/page.tsx (isoDate l.65-67, currentMonthRange l.69-76, Popover+Calendar l.140-163)
    - dashboard/src/components/audit-table.tsx (props l.35-43; canNext l.71-73; pager UI l.125-148; docstring l.14-17)
    - dashboard/src/components/ui/input.tsx (search box component)
    - dashboard/src/lib/gateway.ts (fetchAudit + AuditResponse.total from Task 1)
  </read_first>
  <action>
    In incidents/page.tsx add `range`/`applied`/`search` state plus the copied isoDate()/
    currentMonthRange() helpers (LOCAL components, NOT toISOString — WR-08; default current month per
    Pitfall 6). Render the Popover+Calendar date-range block (copy consumo/page.tsx:140-163) and an
    Input search box above the table. Thread the new args into the existing useQuery:
    `queryKey: ["audit", offset, applied, search]`,
    `queryFn: () => fetchAudit(PAGE_SIZE, offset, applied.from, applied.to, search)`. Pass
    `total={data?.total}` to AuditTable. Keep the existing loading/error branches. In audit-table.tsx
    add `total?: number` to AuditTableProps and replace the inferred canNext with
    `canNext = total !== undefined ? offset + limit < total : rows.length >= limit` (update the
    l.71-72 comment + l.14-17 docstring); optionally show `de {total}` in the pager footer. Keep
    `key={row.request_id}` and the existing column set (metadata only).
  </action>
  <verify>
    <automated>cd dashboard && bunx tsc --noEmit && bun run lint && grep -q 'offset + limit < total' src/components/audit-table.tsx && grep -q 'Calendar' "src/app/(dashboard)/incidents/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - incidents/page.tsx renders a Popover+Calendar date-range and an Input search box; useQuery passes from/to/search into fetchAudit
    - incidents/page.tsx uses isoDate/currentMonthRange (local components), not toISOString
    - audit-table.tsx canNext is `offset + limit < total` when total is defined (grep confirms)
    - audit-table.tsx keeps metadata-only columns (no prompt/response content column added)
    - `bunx tsc --noEmit` clean; `bun run lint` passes
  </acceptance_criteria>
  <done>/incidents filters by period + search; pager driven by real total.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next server proxy route | only route.ts holds GATEWAY_ADMIN_KEY |
| /incidents → /api/gateway/audit (GET) | read-only; search/date params travel as query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-13 | Tampering | search box input | mitigate | Search travels as a query param to the parameterized ILIKE on the backend (15-02 T-15-05); no client-side SQL; React escapes rendered values |
| T-15-14 | Info Disclosure | admin key in browser | mitigate | fetchAudit goes through proxyGet → server route.ts; browser never reads GATEWAY_ADMIN_KEY (T-07-24) |
| T-15-15 | Info Disclosure | audit content render | mitigate | Table renders only metadata columns returned by the backend (audit_log_content never selected — T-07-09) |
| T-15-16 | Tampering | mutating call | mitigate | /incidents is GET-only via proxyGet; proxy rejects non-GET |
</threat_model>

<verification>
- `cd dashboard && bun run test src/lib/gateway.test.ts && bunx tsc --noEmit && bun run lint`
- Manual (recorded, not gating): open /incidents, apply a period + search, confirm pager bounds use total
</verification>

<success_criteria>
- /incidents has date-range + search controls; pager uses real total
- fetchAudit forwards from/to/search; AuditResponse.total typed
- metadata-only render preserved; typecheck + lint + tests green
</success_criteria>

<output>
Create `.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-04-SUMMARY.md` when done
</output>
