---
phase: 15-dashboard-economia-e-historico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/db/queries/billing.sql
  - gateway/db/queries/primary_lifecycles.sql
  - gateway/internal/db/gen/billing.sql.go
  - gateway/internal/db/gen/primary_lifecycles.sql.go
  - gateway/internal/db/gen/models.go
  - gateway/internal/admin/economy.go
  - gateway/internal/admin/economy_test.go
  - gateway/cmd/gateway/main.go
autonomous: true
requirements: [OBS-09]

must_haves:
  truths:
    - "GET /admin/economy?from=&to= returns 200 JSON with keys {range, summary, series}"
    - "summary carries all 5 locked metrics: economia_liquida_brl, roi_multiplier, custo_openrouter_brl, pct_servido_local, horas_pod_up"
    - "summary.roi_multiplier is null (not Inf/NaN) when vast_brl == 0; pct_servido_local null when zero requests"
    - "phantom + external cost summed gateway-wide (all tenants) with NO tenant filter"
    - "Vast cost + pod-up hours buckets by started_at BRT date reusing the operations.go accrual"
  artifacts:
    - path: "gateway/db/queries/billing.sql"
      provides: "SumPhantomAllTenantsByDate (series) + SumBillingAllTenantsRange (phantom/external/local-requests/total-requests, no tenant filter)"
      contains: "SumBillingAllTenantsRange"
    - path: "gateway/db/queries/primary_lifecycles.sql"
      provides: "ListPrimaryLifecyclesInRange (range overlap)"
      contains: "ListPrimaryLifecyclesInRange"
    - path: "gateway/internal/admin/economy.go"
      provides: "EconomyHandler GET /admin/economy (5-metric summary + daily series)"
      min_lines: 140
    - path: "gateway/internal/admin/economy_test.go"
      provides: "unit coverage: gateway-wide sum, ROI nil-when-vast-0, pct-local, pod-up hours, 400 bad date"
  key_links:
    - from: "gateway/cmd/gateway/main.go"
      to: "admin.NewEconomyHandler"
      via: "adminRouter.Method GET /economy"
      pattern: "adminEconomyHandler"
    - from: "gateway/internal/admin/economy.go"
      to: "gen.SumBillingAllTenantsRange"
      via: "economyQueries interface"
      pattern: "SumBillingAllTenants"
---

<objective>
Build the OBS-09 backend: the gateway-wide phantom SUM (the documented blocker) + a new
`GET /admin/economy?from=&to=` handler that computes — server-side — the 5 locked CONTEXT metrics
(economia líquida = phantom − Vast, ROI multiplier, custo OpenRouter fallback, % servido local,
horas pod UP) plus a daily time series.

Purpose: Unblock the deferred Economia panel by providing the no-tenant-filter sums that left
`phantom_month_brl` nil in operations.go, and expose a clean read-only endpoint the dashboard
consumes without per-tenant fan-out.
Output: 3 new sqlc queries (regenerated gen layer), `economy.go` + `economy_test.go`, and the
3-edit main.go wiring mirroring `adminOperationsHandler`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-RESEARCH.md
@.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-PATTERNS.md
@.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs — already extracted, no codebase exploration required. -->

usage.go 5-part handler contract (clone EXACTLY):
1. private `economyQueries` interface listing only sqlc methods used (test injection)
2. dual constructor `NewEconomyHandler(q *gen.Queries, cfg config.Config, log *slog.Logger)` +
   `newEconomyHandlerWithQueries(q economyQueries, cfg config.Config, log *slog.Logger)`
3. `httpx.WriteOpenAIError(w, status, type, code, msg)` on every failure branch
4. `obs.GatewayAdminRequests.WithLabelValues("/admin/economy", "2xx"|"4xx"|"5xx").Inc()` per branch
5. tz block VERBATIM from usage.go:132-150 (LoadLocation America/Sao_Paulo, ParseInLocation
   "2006-01-02", toT = toT.Add(24h) exclusive end)

billing_events columns (confirmed): cost_local_brl, cost_local_phantom_brl, cost_external_brl,
requests_count. INVARIANT (CONTEXT): cost_local_phantom_brl is written ONLY when a request was
served local/GPU → a request is "served local" iff cost_local_phantom_brl > 0.

The 5 locked summary metrics and their sources:
- economia_liquida_brl = phantom_brl − vast_brl
- roi_multiplier (*float64) = phantom_brl / vast_brl, nil when vast_brl == 0 (never Inf/NaN)
- custo_openrouter_brl = SUM(cost_external_brl) gateway-wide (real external spend when pod was DOWN)
- pct_servido_local (*float64) = local_requests / total_requests, nil when total_requests == 0
- horas_pod_up = SUM over lifecycles of (ended−started) for closed, (now−started) for open, in hours

Accrual to copy from operations.go:214-245 (numericPtr is in operations.go:363-373, SAME package):
- closed row (EndedAt.Valid): cost = *numericPtr(row.TotalCostBrl); hours = ended−started
- open row: cost = *numericPtr(row.AcceptedDph) * hours-since-StartedAt * h.cfg.USDToBRLRate (clamp>=0)
- bucket each lifecycle + its hours into row.StartedAt.In(loc) date (one lifecycle = one BRT day, A1)

main.go wiring anchors (mirror adminOperationsHandler):
- struct field at line 110: `adminEconomyHandler http.Handler`
- construct at line 1244: `adminEconomyHandler := admin.NewEconomyHandler(gen.New(pool), cfg, log)`
- assign to px at line 1263: `adminEconomyHandler: adminEconomyHandler,`
- mount at line 1485: `adminRouter.Method(http.MethodGet, "/economy", px.adminEconomyHandler)`

pgtype.Numeric SUM rows decode via `.Float64Value()` (usage.go:183-185).
Nullable summary fields are `*float64` → JSON null — mirror CostBRL nullable-ptr (operations.go:92-93).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add no-tenant billing sums (phantom series + wide range) + lifecycle range query + regenerate sqlc</name>
  <files>gateway/db/queries/billing.sql, gateway/db/queries/primary_lifecycles.sql, gateway/internal/db/gen/billing.sql.go, gateway/internal/db/gen/primary_lifecycles.sql.go, gateway/internal/db/gen/models.go</files>
  <read_first>
    - gateway/db/queries/billing.sql (analogs: SumBillingEventsByDate/Range l.43-77 with full column set incl cost_external_brl + requests_count; tz idiom flagged at l.7-10)
    - gateway/db/queries/primary_lifecycles.sql (analog: ListPrimaryLifecycles l.65-77)
    - gateway/sqlc.yaml (codegen config; pgx/v5, uuid override)
    - .github/workflows/build-gateway.yml (codegen gate l.61-67: git diff --exit-code internal/db/gen/)
  </read_first>
  <action>
    In billing.sql add (DROPPING the `WHERE tenant_id = $1` predicate — this omission IS the OBS-09
    blocker fix — and using ts range $1/$2):
    (a) `SumPhantomAllTenantsByDate :many` — SELECT `(ts AT TIME ZONE 'America/Sao_Paulo')::date AS
    date`, `COALESCE(SUM(cost_local_phantom_brl), 0)::numeric(20,6) AS phantom_brl` GROUP BY the BRT
    date ORDER BY date (this drives the chart series).
    (b) `SumBillingAllTenantsRange :one` — over the same ts range, SELECT
    `COALESCE(SUM(cost_local_phantom_brl),0)::numeric(20,6) AS phantom_brl`,
    `COALESCE(SUM(cost_external_brl),0)::numeric(20,6) AS external_brl`,
    `COUNT(*) FILTER (WHERE cost_local_phantom_brl > 0)::bigint AS local_requests`,
    `COUNT(*)::bigint AS total_requests` (feeds custo OpenRouter + % servido local).
    Keep the `(ts AT TIME ZONE 'America/Sao_Paulo')::date` idiom verbatim — do NOT use CURRENT_DATE+tz.
    In primary_lifecycles.sql add `ListPrimaryLifecyclesInRange :many` cloning ListPrimaryLifecycles's
    full column list (id, started_at, drain_started_at, ended_at, trigger_reason, vast_offer_id,
    vast_instance_id, accepted_dph, total_cost_brl, shutdown_reason, leader_replica) with
    `WHERE started_at >= $1 AND started_at < $2 ORDER BY started_at DESC` (no LIMIT). Then run
    `cd gateway && sqlc generate` and stage internal/db/gen/. Do NOT pre-add any `(ts)` index
    (RESEARCH Pitfall 7 — seq-scan over the low-volume month partition is acceptable).
  </action>
  <verify>
    <automated>cd gateway && sqlc generate && git diff --exit-code internal/db/gen/ && grep -q SumPhantomAllTenantsByDate internal/db/gen/billing.sql.go && grep -q SumBillingAllTenantsRange internal/db/gen/billing.sql.go && grep -q ListPrimaryLifecyclesInRange internal/db/gen/primary_lifecycles.sql.go</automated>
  </verify>
  <acceptance_criteria>
    - `gateway/db/queries/billing.sql` contains `SumPhantomAllTenantsByDate` and `SumBillingAllTenantsRange`, neither containing `tenant_id`
    - `SumBillingAllTenantsRange` row exposes phantom_brl, external_brl, local_requests, total_requests
    - `gateway/db/queries/primary_lifecycles.sql` contains `ListPrimaryLifecyclesInRange` with both `started_at >= $1` and `started_at < $2`
    - `cd gateway && sqlc generate` produces NO diff after staging (CI gate `git diff --exit-code internal/db/gen/` passes)
  </acceptance_criteria>
  <done>Three new sqlc queries compile, gen layer regenerated and in sync, no index added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EconomyHandler (clone usage.go) — 5-metric summary + Vast accrual + tests + main.go wiring</name>
  <files>gateway/internal/admin/economy.go, gateway/internal/admin/economy_test.go, gateway/cmd/gateway/main.go</files>
  <read_first>
    - gateway/internal/admin/usage.go (5-part contract; tz block l.132-150; Float64Value l.183-185; phantom-excluded-from-total l.206-209)
    - gateway/internal/admin/operations.go (accrual l.214-245; numericPtr l.363-373; nullable CostBRL l.92-93)
    - gateway/internal/admin/operations_test.go (fakeOperationsQueries l.24-38; opNumeric/opTimestamptz/opInt8 l.43-57; opTestCfg l.81-90 supplies USDToBRLRate:5.0; opResponse decode l.94-120)
    - gateway/internal/admin/audit_test.go (fake-queries shape l.17-48)
    - gateway/cmd/gateway/main.go (adminOperationsHandler wiring: l.110, l.1244, l.1263, l.1485)
    - gateway/internal/config/config.go (USDToBRLRate default 5.0 at l.162)
  </read_first>
  <behavior>
    - Test: gateway-wide sum — fake returns phantom across tenants/days → summary.phantom_brl == total; series has one entry per BRT day
    - Test: economia + ROI — phantom>0, vast>0 → economia_liquida_brl == phantom−vast, roi_multiplier == phantom/vast
    - Test: ROI nil when vast=0 — phantom>0, zero lifecycles → roi_multiplier is nil (JSON null), economia == phantom, no Inf/NaN
    - Test: custo OpenRouter — fake SumBillingAllTenantsRange external_brl=X → summary.custo_openrouter_brl == X
    - Test: % servido local — local_requests=8, total_requests=10 → pct_servido_local == 0.8; total_requests=0 → nil
    - Test: horas pod UP — one closed lifecycle (ended−started=3h) + one open (now−started=2h) → horas_pod_up == 5 (±tolerance)
    - Test: open-lifecycle accrual — open lifecycle vast_brl == dph * hours * USDToBRLRate (opTestCfg 5.0)
    - Test: 400 on bad/missing date param via WriteOpenAIError invalid_date
  </behavior>
  <action>
    Create economy.go with EconomyResponse {Range, Summary, Series}; EconomySummary
    {PhantomBRL, VastBRL, EconomiaLiquidaBRL, CustoOpenRouterBRL, HorasPodUp float64;
    ROIMultiplier *float64 `json:"roi_multiplier"`; PctServidoLocal *float64
    `json:"pct_servido_local"`}; EconomyDayRow {Date string; PhantomBrl, VastBrl, EconomiaBrl float64}.
    Define `economyQueries` with exactly SumPhantomAllTenantsByDate, SumBillingAllTenantsRange,
    ListPrimaryLifecyclesInRange. Dual constructor takes cfg config.Config (for cfg.USDToBRLRate).
    In ServeHTTP: copy tz + param-parse from usage.go:132-150 (default current-month when from/to
    absent). Call SumBillingAllTenantsRange → phantom_brl, external_brl(→custo_openrouter_brl),
    local_requests/total_requests (→pct_servido_local as *float64, nil when total==0). Call
    SumPhantomAllTenantsByDate for the per-day phantom series (decode via .Float64Value()). Call
    ListPrimaryLifecyclesInRange and reduce, per the operations.go:214-245 accrual VERBATIM (reuse
    numericPtr directly): vast_brl total AND horas_pod_up total (closed = ended−started hours, open =
    now−started hours clamped>=0), bucketing each lifecycle's vast cost into row.StartedAt.In(loc)
    date for the series. Compute economia_liquida_brl = phantom − vast; ROIMultiplier = *float64 of
    phantom/vast ONLY when vast_brl != 0 else nil (never Inf/NaN). Merge per-day phantom + per-day
    vast maps into Series sorted by date (economia_brl = phantom_brl − vast_brl per day). Emit
    WriteOpenAIError + GatewayAdminRequests "/admin/economy" labels on every branch incl 2xx.
    Create economy_test.go cloning operations_test.go's fake-queries + opNumeric/opTimestamptz/
    opTestCfg helpers, covering ALL <behavior> cases (decode into a pointer-ROI/pointer-pct struct).
    Wire into main.go with the 3 edits mirroring adminOperationsHandler (field l.110, construct l.1244
    passing cfg, px assign l.1263, mount GET /economy at l.1485).
    Do NOT downgrade any of the 5 metrics to a placeholder/"v1" — all are computed from the queried
    data above.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/admin/ -run Economy && go vet ./internal/admin/ ./cmd/gateway/ && gofmt -l internal/admin/economy.go internal/admin/economy_test.go cmd/gateway/main.go</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go test ./internal/admin/ -run Economy` exits 0
    - EconomySummary has all 5 metrics: economia_liquida_brl, roi_multiplier (*float64), custo_openrouter_brl, pct_servido_local (*float64), horas_pod_up; tests assert each
    - roi_multiplier nil when vast_brl==0 and a value when vast_brl>0; pct_servido_local nil when total_requests==0
    - economy.go reuses `numericPtr` (no new numeric→float helper) and `h.cfg.USDToBRLRate` in the open-lifecycle branch
    - economy.go does NOT sum phantom into any "total real cost" field (economia = phantom − vast)
    - `cd gateway && go build ./...` succeeds; `grep -c adminEconomyHandler cmd/gateway/main.go` >= 3
    - `gofmt -l` prints nothing for the three Go files
  </acceptance_criteria>
  <done>EconomyHandler returns {range, summary(5 metrics), series}, ROI/pct null-safe, wired at GET /admin/economy, tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next proxy → /admin/economy | admin-key injected server-side in route.ts; client never sees it |
| handler → Postgres | from/to params cross into SQL via sqlc parameterized queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | from/to date params on /admin/economy | mitigate | Parse via time.ParseInLocation; reject bad dates with 400 invalid_date; values bound sqlc params, never string-concatenated |
| T-15-02 | Info Disclosure | admin key | mitigate | Endpoint rides existing X-Admin-Key bcrypt middleware on the /admin sub-router; no key handling added; key stays in route.ts (T-07-24) |
| T-15-03 | Info Disclosure | phantom/Vast aggregate read | accept | Aggregate cost numbers only; no per-tenant PII, no prompt/response content selected |
| T-15-04 | DoS | unbounded series scan | accept | Low volume; default range = current month; seq-scan over one month partition acceptable (Pitfall 7) |
</threat_model>

<verification>
- `cd gateway && go test ./internal/admin/... && sqlc generate && git diff --exit-code internal/db/gen/`
- `cd gateway && go build ./... && go vet ./internal/admin/ ./cmd/gateway/`
</verification>

<success_criteria>
- GET /admin/economy?from=&to= returns 200 {range, summary, series} with all 5 locked metrics
- roi_multiplier / pct_servido_local null-safe (no Inf/NaN)
- phantom + external summed gateway-wide (no tenant filter); Vast cost + pod-up hours via reused accrual
- sqlc gen committed and in sync; handler wired and unit-tested
</success_criteria>

<output>
Create `.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-01-SUMMARY.md` when done
</output>
