---
phase: 15-dashboard-economia-e-historico
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - gateway/db/queries/audit.sql
  - gateway/internal/db/gen/audit.sql.go
  - gateway/internal/admin/audit.go
  - gateway/internal/admin/audit_test.go
autonomous: true
requirements: [OBS-10]

must_haves:
  truths:
    - "GET /admin/audit accepts from/to/search params and returns total count"
    - "audit queries select ONLY audit_log metadata, never audit_log_content"
    - "search is a parameterized ILIKE arg, never string-concatenated"
    - "AuditResponse includes a total field for honest pagination"
  artifacts:
    - path: "gateway/db/queries/audit.sql"
      provides: "ListAuditStateChanges (+from/to/search) + CountAuditStateChanges"
      contains: "CountAuditStateChanges"
    - path: "gateway/internal/admin/audit.go"
      provides: "audit handler parsing from/to/search + total in response"
      contains: "Total"
  key_links:
    - from: "gateway/internal/admin/audit.go"
      to: "gen.CountAuditStateChanges"
      via: "auditQueries interface"
      pattern: "CountAuditStateChanges"
    - from: "gateway/internal/admin/audit.go"
      to: "ListAuditStateChangesParams"
      via: "from/to/search + limit/offset params"
      pattern: "ILIKE|searchPattern"
---

<objective>
Extend the OBS-10 backend: add date-range (`from`/`to`), `search`, and a real `COUNT(*)` to the
audit log endpoint that today only supports limit/offset pagination.

Purpose: Make the `/incidents` history navigable — operators can filter by period, search, and get
honest pager bounds (offset+limit < total) instead of a heuristic canNext.
Output: modified `ListAuditStateChanges` + new `CountAuditStateChanges` (regenerated gen), extended
`audit.go` handler with `Total` in `AuditResponse`, extended `audit_test.go`.

Depends on 15-01 only because `sqlc generate` rewrites the shared `internal/db/gen/` tree — run
after 15-01's gen output is committed to avoid a regen conflict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-RESEARCH.md
@.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-PATTERNS.md
@.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs — already extracted. -->

Existing ListAuditStateChanges (audit.sql:18-23) selects ONLY metadata columns:
ts, request_id, tenant_id, route, method, upstream, status_code, latency_ms, error_code,
event_kind, reason — NEVER audit_log_content (T-07-09 invariant; PRESERVE).

Param renumber (RESEARCH Pitfall 8): adding from/to/search at $1/$2/$3 pushes limit/offset to
$4/$5. sqlc auto-names range params `Ts`/`Ts_2`. MUST re-read the regenerated
`ListAuditStateChangesParams` struct in internal/db/gen/audit.sql.go BEFORE editing the handler.

Search idiom (single reused $3): `($3 = '%' OR route ILIKE $3 OR reason ILIKE $3 OR error_code
ILIKE $3 OR event_kind ILIKE $3)`. Handler builds:
`searchPattern := "%"; if q != "" { searchPattern = "%"+q+"%" }`.

tz block VERBATIM from usage.go:132-150 (LoadLocation America/Sao_Paulo, ParseInLocation, exclusive
end). Default to current-month when from/to absent (Pitfall 6 — partitions only cover recent months).

Existing handler keeps: pgTextPtr nullable rendering (audit.go:171-179), maxAuditLimit=200 cap
(audit.go:40), per-branch obs.GatewayAdminRequests "/admin/audit" labels.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend audit.sql with from/to/search + CountAuditStateChanges, regenerate sqlc</name>
  <files>gateway/db/queries/audit.sql, gateway/internal/db/gen/audit.sql.go</files>
  <read_first>
    - gateway/db/queries/audit.sql (analog: ListAuditStateChanges l.9-23; preserve metadata-only column list)
    - gateway/db/queries/billing.sql (tz idiom reference l.7-10)
    - .github/workflows/build-gateway.yml (codegen gate l.61-67)
  </read_first>
  <action>
    Modify `ListAuditStateChanges :many` keeping its EXACT metadata column list (never add
    audit_log_content) and append to the WHERE clause: `AND ts >= $1 AND ts < $2 AND ($3 = '%' OR
    route ILIKE $3 OR reason ILIKE $3 OR error_code ILIKE $3 OR event_kind ILIKE $3)`, with LIMIT/
    OFFSET becoming `$4`/`$5`. Add `CountAuditStateChanges :one` selecting
    `COUNT(*)::bigint AS total` with the identical WHERE clause ($1/$2/$3). Run `cd gateway && sqlc
    generate` and stage internal/db/gen/.
  </action>
  <verify>
    <automated>cd gateway && sqlc generate && git diff --exit-code internal/db/gen/ && grep -q CountAuditStateChanges internal/db/gen/audit.sql.go && grep -q ILIKE db/queries/audit.sql && ! grep -q audit_log_content db/queries/audit.sql</automated>
  </verify>
  <acceptance_criteria>
    - `gateway/db/queries/audit.sql` ListAuditStateChanges WHERE clause contains `ts >= $1`, `ts < $2`, and the `$3 ... ILIKE` group; LIMIT `$4` OFFSET `$5`
    - `gateway/db/queries/audit.sql` contains `CountAuditStateChanges` returning `total`
    - `db/queries/audit.sql` does NOT contain `audit_log_content` (T-07-09 preserved)
    - `cd gateway && sqlc generate` produces NO diff after staging; `gen.CountAuditStateChangesRow` and the renumbered `gen.ListAuditStateChangesParams` exist
  </acceptance_criteria>
  <done>audit.sql filters by range+search and exposes a COUNT; gen layer regenerated and in sync, metadata-only preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend audit.go handler (from/to/search + total) + extend audit_test.go</name>
  <files>gateway/internal/admin/audit.go, gateway/internal/admin/audit_test.go</files>
  <read_first>
    - gateway/internal/admin/audit.go (handler l.98-169; AuditResponse l.44-48; maxAuditLimit l.40; pgTextPtr l.171-179)
    - gateway/internal/admin/usage.go (tz block to copy l.132-150)
    - gateway/internal/admin/audit_test.go (fake-queries shape l.17-48; extend)
    - gateway/internal/db/gen/audit.sql.go (RE-READ regenerated ListAuditStateChangesParams + CountAuditStateChangesParams AFTER Task 1 — param renumber, Pitfall 8)
  </read_first>
  <behavior>
    - Test: default range — no from/to → handler defaults to current-month bounds; ListAuditStateChanges receives those bounds and searchPattern "%"
    - Test: explicit from/to/search — params threaded into both List and Count; searchPattern == "%term%"
    - Test: total in response — fake Count returns N → AuditResponse.Total == N; canNext semantics derivable (offset+limit<total)
    - Test: 400 on bad date param via WriteOpenAIError invalid_date
    - Test: limit cap — request limit>200 clamped to maxAuditLimit (existing behavior preserved)
  </behavior>
  <action>
    In audit.go add the usage.go:132-150 tz + param-parse block (default current-month when
    from/to absent). Build `searchPattern := "%"; if q := r.URL.Query().Get("search"); q != "" {
    searchPattern = "%" + q + "%" }`. Thread the new from/to/searchPattern into
    ListAuditStateChangesParams alongside the existing Limit/Offset — use the regenerated field names
    (re-read gen first; sqlc auto-named the range fields Ts/Ts_2). Call the new
    CountAuditStateChanges with the same from/to/search args and add `Total int64 \`json:"total"\``
    to AuditResponse, populating it. Keep maxAuditLimit=200, pgTextPtr nullable rendering, and the
    per-branch GatewayAdminRequests "/admin/audit" 2xx/4xx/5xx increments. Extend audit_test.go's
    fake to implement CountAuditStateChanges and cover the <behavior> cases. The route is already
    mounted in main.go (GET /audit) — NO main.go change.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/admin/ -run Audit && go vet ./internal/admin/ && gofmt -l internal/admin/audit.go internal/admin/audit_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go test ./internal/admin/ -run Audit` exits 0
    - `grep -q 'Total' gateway/internal/admin/audit.go` (AuditResponse has Total int64 json:"total")
    - handler passes a parameterized searchPattern into the sqlc params (no `+` string concatenation building SQL; only the "%"+q+"%" value)
    - audit.go still references no `audit_log_content`; existing maxAuditLimit cap retained
    - `gofmt -l` prints nothing for the two Go files
  </acceptance_criteria>
  <done>Audit handler filters by range+search, returns total; tests green; metadata-only and limit cap preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next proxy → /admin/audit | admin-key injected server-side; client never sees it |
| handler → Postgres | from/to/search params cross into SQL via sqlc parameterized queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering | audit search box → ILIKE | mitigate | search bound as a single parameterized `$3` ILIKE arg ("%"+q+"%"); never string-interpolated into SQL (sqlc) |
| T-15-06 | Tampering | from/to date params | mitigate | time.ParseInLocation; bad dates → 400 invalid_date; values bind sqlc params |
| T-15-07 | Info Disclosure | audit content leak | mitigate | Queries select ONLY audit_log metadata columns; audit_log_content (prompts/responses) NEVER selected (T-07-09 invariant preserved) |
| T-15-08 | DoS | unbounded search result set | mitigate | maxAuditLimit=200 cap retained; COUNT bounded by date range |
| T-15-09 | Info Disclosure | admin key | mitigate | Rides existing X-Admin-Key middleware; no key handling added |
</threat_model>

<verification>
- `cd gateway && go test ./internal/admin/... && sqlc generate && git diff --exit-code internal/db/gen/`
- `cd gateway && go build ./... && go vet ./internal/admin/`
- `! grep -q audit_log_content gateway/db/queries/audit.sql` (T-07-09)
</verification>

<success_criteria>
- GET /admin/audit accepts from/to/search and returns total
- search is parameterized ILIKE; metadata-only select preserved
- gen committed and in sync; handler + tests green
</success_criteria>

<output>
Create `.planning/phases/15-dashboard-economia-e-historico-from-gsd-explore-2026-06-26/15-02-SUMMARY.md` when done
</output>
