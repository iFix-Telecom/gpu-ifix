---
phase: quick-260614-ny5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/middleware.ts
  - dashboard/src/middleware.test.ts
autonomous: true
requirements: [QUICK-NY5]

must_haves:
  truths:
    - "First visit without a session redirects to a clean /login (no query param)"
    - "The 'Sessão encerrada por inatividade' banner does NOT appear on a fresh no-session visit"
    - "The login page still contains the banner rendering code (untouched)"
  artifacts:
    - path: "dashboard/src/middleware.ts"
      provides: "No-session redirect to clean /login"
      contains: 'NextResponse.redirect(new URL("/login", req.url))'
    - path: "dashboard/src/middleware.test.ts"
      provides: "Updated case (a) asserting clean /login redirect"
  key_links:
    - from: "dashboard/src/middleware.ts"
      to: "/login"
      via: "Stage 1 no-session redirect (no session_expired param)"
      pattern: 'redirect\(new URL\("/login"'
---

<objective>
Stop the dashboard Edge middleware from appending `?session_expired=1` when redirecting an unauthenticated visitor. The Edge runtime cannot distinguish "never logged in" from "session genuinely expired" (both are simply "no session cookie"), so showing "Sessão encerrada por inatividade" on every first visit is incorrect. Middleware now redirects to a clean `/login`.

Purpose: Correct the false "session expired" banner on first visit.
Output: Updated `middleware.ts` (Stage 1 redirect + header comment) and updated `middleware.test.ts` case (a).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@dashboard/src/middleware.ts
@dashboard/src/middleware.test.ts

<interfaces>
<!-- Current Stage 1 block in middleware.ts (lines 93-99) sets the param: -->
<!--   if (!claims.hasSession) {                                          -->
<!--     const url = new URL("/login", req.url);                          -->
<!--     url.searchParams.set("session_expired", "1");                    -->
<!--     return NextResponse.redirect(url);                               -->
<!--   }                                                                  -->

<!-- login/page.tsx STILL reads `session_expired` (line 69) and renders the  -->
<!-- banner (lines 147-157). That code MUST NOT be touched — a future         -->
<!-- client-side flow could still set the param. Only middleware stops it.    -->

<!-- middleware.test.ts case (a) (lines 47-54) currently asserts:             -->
<!--   expect(loc).toContain("/login");                                       -->
<!--   expect(loc).toContain("session_expired=1");   <- this must flip        -->

<!-- dashboard/package.json scripts: test = "vitest run"                      -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redirect no-session to clean /login + sync comments + update test</name>
  <files>dashboard/src/middleware.ts, dashboard/src/middleware.test.ts</files>
  <action>
In `dashboard/src/middleware.ts`:
1. Replace the Stage 1 no-session block (the four lines that build a URL and call `url.searchParams.set("session_expired", "1")` before redirecting) with a single clean redirect: `return NextResponse.redirect(new URL("/login", req.url));`. Keep the surrounding `if (!claims.hasSession) { ... }` guard.
2. Update the inline comment immediately above the Stage 1 block so it no longer says "/login?session_expired=1" — describe it as redirecting to a clean /login.
3. Update the file-header decision-tree comment line "1. No session cookie → redirect to /login?session_expired=1" to "1. No session cookie → redirect to /login" (clean, no param).

In `dashboard/src/middleware.test.ts`:
4. In case (a) ("cookie absent"): rename the `it()` title to reflect the clean target (e.g. "(a) cookie absent → /login (no session_expired param)"). Keep `expect(loc).toContain("/login")`. Change `expect(loc).toContain("session_expired=1")` to `expect(loc).not.toContain("session_expired")`.
5. Update the header comment list item "a. cookie absent → /login?session_expired=1" to "a. cookie absent → /login (clean, no param)".

Do NOT modify `dashboard/src/app/login/page.tsx` — its `session_expired` read (line 69) and banner render (lines 147-157) stay verbatim per the locked decision.
  </action>
  <verify>
    <automated>cd dashboard && bunx tsc --noEmit && bun run test src/middleware.test.ts</automated>
  </verify>
  <done>
`bunx tsc --noEmit` passes. `vitest run` passes all middleware cases, with case (a) now asserting the redirect location contains "/login" and does NOT contain "session_expired". `grep -n session_expired dashboard/src/middleware.ts` returns no matches; `dashboard/src/app/login/page.tsx` still contains the banner code unchanged.
  </done>
</task>

</tasks>

<verification>
- `cd dashboard && bunx tsc --noEmit` — no type errors.
- `cd dashboard && bun run test` — full vitest suite green (or at minimum `src/middleware.test.ts`).
- `grep -rn session_expired dashboard/src/middleware.ts` returns nothing.
- `grep -n "session_expired" dashboard/src/app/login/page.tsx` still returns the read + banner lines (proves login page untouched).
</verification>

<success_criteria>
- Middleware no-session branch redirects to clean `/login` with no query string.
- Both header comment and inline Stage 1 comment reflect the clean target.
- `middleware.test.ts` case (a) asserts absence of `session_expired`; suite passes.
- `login/page.tsx` banner code is unchanged.
</success_criteria>

<output>
Create `.planning/quick/260614-ny5-middleware-redirect-login-limpo-sem-sess/260614-ny5-SUMMARY.md` when done.
</output>
