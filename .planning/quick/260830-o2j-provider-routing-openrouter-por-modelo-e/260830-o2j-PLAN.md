---
phase: quick-260830-o2j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/db/migrations/0037_model_aliases_provider_prefs.sql
  - gateway/db/queries/model_aliases.sql
  - gateway/internal/db/gen/model_aliases.sql.go
  - gateway/internal/db/gen/models.go
  - gateway/internal/db/gen/querier.go
  - gateway/internal/models/provider_prefs.go
  - gateway/internal/models/provider_prefs_test.go
  - gateway/internal/models/resolver.go
  - gateway/internal/proxy/openrouter_director.go
  - gateway/internal/proxy/openrouter_director_test.go
  - gateway/internal/admin/model_aliases_admin_http.go
  - gateway/internal/admin/model_aliases_admin_http_test.go
  - gateway/internal/admin/upstreams_admin_http.go
  - gateway/internal/admin/upstreams_admin_http_test.go
  - gateway/internal/admin/primary_control_http.go
  - gateway/internal/admin/primary_control_http_test.go
  - gateway/cmd/gateway/main.go
  - gateway/cmd/gatewayctl/model_alias.go
  - dashboard/src/lib/gateway.ts
  - dashboard/src/lib/gateway-admin.ts
  - dashboard/src/lib/gateway-server.ts
  - dashboard/src/lib/admin-actions-core.ts
  - dashboard/src/lib/admin-actions.ts
  - dashboard/src/lib/admin-actions.test.ts
  - dashboard/src/app/(dashboard)/modelos/page.tsx
  - dashboard/src/app/(dashboard)/modelos/modelos-controls.tsx
  - dashboard/src/app/(dashboard)/operacao/page.tsx
  - dashboard/src/app/(dashboard)/operacao/operacao-client.tsx
  - dashboard/src/components/operacao-pod-controls.tsx
  - dashboard/src/components/app-sidebar.tsx
autonomous: true
requirements: [GW-PROVIDER-PREFS-01, DASH-CONTROL-01]
must_haves:
  truths:
    - "A model_aliases row for upstream openrouter-chat can carry a provider_prefs JSON (only/order/ignore/allow_fallbacks/require_parameters/quantizations/max_price/preferred_min_throughput/preferred_max_latency/data_collection/zdr/sort) and the openrouter director injects it VERBATIM as the request `provider` object; rows without prefs keep the legacy global env order/allow_fallbacks."
    - "Invalid provider_prefs (unknown field, bad enum, negative price) are rejected on write by gatewayctl AND by the admin API — never stored."
    - "The dashboard has a 'Modelos' page listing every alias row (alias/upstream/role/target/prefs) and every upstream (role/tier/enabled/probe); owner can create/edit/delete alias rows (incl. provider prefs form) and enable/disable upstreams; operator is read-only."
    - "The Operação page has owner-only 'Ligar pod' / 'Desligar pod' controls that publish force_up_request / force_down_request to gw:primary:events (same contract as gatewayctl primary force-up/force-down)."
    - "Every owner mutation writes exactly one admin_audit_log row and re-checks requireOwner server-side."
    - "Admin API writes to model_aliases refresh the resolver immediately (no 60s wait)."
  artifacts:
    - path: "gateway/db/migrations/0037_model_aliases_provider_prefs.sql"
      provides: "provider_prefs JSONB nullable column"
    - path: "gateway/internal/models/provider_prefs.go"
      provides: "ValidateProviderPrefs + Resolver.ProviderPrefs"
    - path: "gateway/internal/admin/model_aliases_admin_http.go"
      provides: "GET/PUT /admin/model-aliases, DELETE /admin/model-aliases/{alias}/{upstream}"
    - path: "gateway/internal/admin/upstreams_admin_http.go"
      provides: "GET /admin/upstreams, POST /admin/upstreams/{name}/enabled"
    - path: "gateway/internal/admin/primary_control_http.go"
      provides: "POST /admin/primary/force-up, POST /admin/primary/force-down"
    - path: "dashboard/src/app/(dashboard)/modelos/page.tsx"
      provides: "Modelos & upstreams management page"
    - path: "dashboard/src/components/operacao-pod-controls.tsx"
      provides: "owner-only force-up/force-down card"
  key_links:
    - from: "gateway/internal/proxy/openrouter_director.go"
      to: "models.Resolver.ProviderPrefs"
      via: "PASS 2 provider injection"
    - from: "dashboard/src/lib/admin-actions-core.ts"
      to: "gateway-admin.ts gatewayAdminPut/Delete/Post"
      via: "owner-gated server actions"
---

<decisions>
## Decisões do Pedro (2026-08-30, via AskUserQuestion) — LOCKED

1. **Precedência do provider prefs: TENANT > linha do alias > global env.** `tenants.provider_prefs` (1 objeto por tenant, vale p/ todos os modelos dele) + `model_aliases.provider_prefs` (por alias, openrouter-chat). Sem nenhum → comportamento atual (env `UPSTREAM_LLM_OPENROUTER_PROVIDER_ORDER`). Editável em /tenants/gerenciar (por tenant) e /modelos (por alias).
2. **Gateway sempre vence** o `provider` enviado pelo cliente (política centralizada, igual hoje).
3. **Pod controls em prod: validar só render + dialog de confirmação** — NÃO disparar force-up/force-down real na validação.
4. **E2E owner: promover `claude.ops@ifixtelecom.com.br` a owner** no `bd_ai_dashboard_prod` (reversível) p/ validar mutações.
5. Fora de escopo: start/stop do pod 3060 (timers `vast3060.py`), edição estrutural do pod.
</decisions>

<objective>
(1) Controle de provedor OpenRouter POR MODELO: hoje o openrouter director injeta um `provider.order` GLOBAL (env `UPSTREAM_LLM_OPENROUTER_PROVIDER_ORDER`, default novita) em todo request e sobrescreve o que o cliente mandou. Passa a existir `model_aliases.provider_prefs` (jsonb) por linha (alias, upstream_name) seguindo o schema oficial do OpenRouter (print do Pedro 2026-08-30: only/order/allow_fallbacks/quantizations/max_price/preferred_min_throughput/preferred_max_latency/data_collection/zdr). Row com prefs → injeta verbatim; sem prefs → comportamento atual.

(2) Controle total de pod + LLM pelo ai-dashboard: página nova "Modelos" (CRUD de alias rows + provider prefs + enable/disable de upstreams) e card "Controle do pod" na Operação (force-up/force-down do primário). Tudo owner-only com audit, espelhando o padrão Phase 17/18 (requireOwner → validação → gateway admin → 1 audit row → revalidate).

Validação: go test (unit + integration), vitest, deploy gateway (stack 38, com `migrate up` 0037) + dashboard (stack 40), E2E no prod via agent-browser.
</objective>

<tasks>

<task id="1" name="Gateway: provider_prefs + admin endpoints + gatewayctl">
  <files>gateway/db/migrations/0037_*.sql, gateway/db/queries/model_aliases.sql, gateway/internal/db/gen/*, gateway/internal/models/{provider_prefs.go,resolver.go}, gateway/internal/proxy/openrouter_director.go, gateway/internal/admin/{model_aliases,upstreams,primary_control}_admin_http.go, gateway/cmd/gateway/main.go, gateway/cmd/gatewayctl/model_alias.go</files>
  <action>
  - Migration 0037: ADD COLUMN provider_prefs JSONB NULL + COMMENT. Down: DROP COLUMN.
  - Queries: List/Get retornam provider_prefs; Upsert recebe $5 provider_prefs e atualiza no conflict. `sqlc generate` (v1.30.0).
  - models/provider_prefs.go: struct tipada com DisallowUnknownFields; validações: data_collection∈{allow,deny}; quantizations⊂{int4,int8,fp4,mxfp4,nvfp4,fp6,fp8,mxfp8,fp16,bf16,fp32,unknown}; sort string∈{price,throughput,latency} ou {by,partition}; max_price campos ≥0; preferred_* número ≥0 ou objeto com chaves p50/p75/p90/p99; listas de slugs não vazias/sem whitespace. `Normalize` re-serializa canônico.
  - Resolver: cache `prefs map[aliasKey]json.RawMessage`; `ProviderPrefs(alias, upstream) json.RawMessage` (nil se ausente).
  - openrouter_director: lê alias do body ANTES do rewrite; PASS 2 usa prefs da row quando presente (injeta objeto inteiro), senão legado (order/allow_fallbacks do env). Interface `providerPrefsLookup` p/ teste.
  - Admin: GET /admin/model-aliases (lista); PUT /admin/model-aliases {alias,upstream_name,target,provider_prefs?} (valida campos R10 + upstream existe via GetUpstreamByName, role = row.Role; prefs só aceitas se upstream_name=openrouter-chat, senão 400) → 200 row + resolver.Refresh; DELETE /admin/model-aliases/{alias}/{upstream_name} → 204 + Refresh. GET /admin/upstreams (ListAllUpstreams tipado); POST /admin/upstreams/{name}/enabled {enabled} → 409 se desabilitar o ÚNICO enabled do role; 204. POST /admin/primary/force-up|force-down {reason?} → PublishPrimaryEvent (ReplicaID=hostname, reason prefixado "dashboard:") → 202; 503 se rdb nil.
  - gatewayctl model-alias set: flags `--provider-prefs '<json>'` e `--clear-provider-prefs`; list ganha coluna PREFS; get imprime prefs.
  - main.go: construir + montar rotas no adminRouter.
  </action>
  <verify>cd gateway && gofmt -l . (vazio) && go build ./... && go test ./internal/models/... ./internal/proxy/... ./internal/admin/... && go test -tags integration ./internal/... (se DB de teste disponível)</verify>
  <done>Testes verdes; director injeta prefs da row; endpoints respondem conforme contrato.</done>
</task>

<task id="2" name="Dashboard: página Modelos + controle do pod">
  <files>dashboard/src/lib/{gateway,gateway-admin,gateway-server,admin-actions-core,admin-actions}.ts, dashboard/src/app/(dashboard)/modelos/*, dashboard/src/app/(dashboard)/operacao/*, dashboard/src/components/{operacao-pod-controls,app-sidebar}.tsx, testes</files>
  <action>
  - gateway.ts: tipos ModelAliasRow/UpstreamRow/ProviderPrefs + fetchModelAliases/fetchUpstreams. gateway-admin.ts: gatewayAdminPut/gatewayAdminDelete (mutate generalizado). gateway-server.ts: fetchModelAliasesServer/fetchUpstreamsServer.
  - admin-actions-core.ts: upsertModelAliasCore, deleteModelAliasCore, setUpstreamEnabledCore, primaryForceUpCore, primaryForceDownCore — requireOwner PRIMEIRO, validação server-side (alias/upstream/target regex sem whitespace; prefs validadas por schema TS espelho), gateway call, 1 audit row (model_alias.upsert / model_alias.delete / upstream.set_enabled / primary.force_up / primary.force_down), revalidate. Wrappers "use server" em admin-actions.ts.
  - /modelos (RSC owner-aware) + modelos-controls.tsx (client): tabela de aliases agrupada por alias; editor (dialog) com target + form de provider prefs (only/order/ignore como listas, allow_fallbacks/require_parameters/zdr switches, data_collection select, quantizations chips, sort select, max_price prompt/completion, preferred_min_throughput p90, preferred_max_latency p90) + preview JSON; criar linha; excluir com confirm. Seção Upstreams com Switch enabled (owner) + probe status.
  - Operação: page.tsx vira RSC (lê role) → operacao-client.tsx (conteúdo atual) + OperacaoPodControls (Ligar/Desligar com AlertDialog + motivo).
  - Sidebar: item "Modelos" (/modelos).
  - vitest: casos owner→1 call+1 audit / operator→0 call+0 audit / prefs inválidas→0 call, para as 5 actions.
  </action>
  <verify>cd dashboard && npx vitest run && npx tsc --noEmit</verify>
  <done>Página renderiza, ações owner-gated testadas, leak-guard intacto.</done>
</task>

<task id="3" name="Deploy prod + validação E2E agent-browser">
  <action>
  - Commit + push develop → CI build-gateway/build-dashboard. Pré-pull imagens no worker-vm. `gatewayctl migrate status/up` (0037) ANTES do PUT do stack 38 (Env completo, PullImage). PUT stack 40 (dashboard). Health + logs.
  - agent-browser: login ai-dashboard (conta claude.ops; promover a owner se necessário p/ validar mutações), navegar /modelos: criar alias de teste em openrouter-chat com provider prefs, verificar via gatewayctl/DB, disparar chat via gateway com esse alias e confirmar no OpenRouter (provider resolvido) — depois remover alias de teste. Validar toggle de upstream (desligar/religar local-tts — já desabilitado — ou outro seguro). Validar botões do pod (apenas render + confirm; NÃO disparar force-down em horário de pico sem necessidade; force-up fora de janela é aceitável se custo baixo — decidir na hora e registrar).
  </action>
  <done>Screenshots/evidências registradas no SUMMARY; prod com feature ativa; sem regressão em /health e chat.</done>
</task>

</tasks>
