---
phase: quick-260824-s2t-kv-q8-0-ctx-32k-por-slot-no-pod-primario
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/internal/primary/onstart.go
  - gateway/internal/emerg/lifecycle.go
  - gateway/internal/proxy/tokencount.go
  - pod/primary/supervisord.conf
  - pod/docker-compose.yml
  - gateway/docs/RUNBOOK-PRIMARY-POD.md
  - gateway/docs/RUNBOOK-EMERGENCY-POD.md
  - pod/README.md
autonomous: true
requirements: [KV-Q8-32K]

must_haves:
  truths:
    - "Os 4 pontos que definem os args do llama-server (supervisord.conf, docker-compose.yml, primaryLlamaArgsDefault, emergencyLlamaArgsDefault) carregam o MESMO conjunto: --ctx-size 65536 --cache-type-k q8_0 --cache-type-v q8_0 -fa on"
    - "Nenhum dos 4 pontos tem cache-type quantizado sem -fa on (sem flash-attn o llama-server RECUSA carregar e o pod morre no boot)"
    - "ChatContextCap vale 32768 (novo teto por slot: 65536 total / -np 2)"
    - "gateway compila, gofmt limpo e os testes unitarios de proxy/primary/emerg passam"
  artifacts:
    - path: "pod/primary/supervisord.conf"
      provides: "runtime REAL do llama-server no pod primario com KV q8_0 + 65536 total"
      contains: "--ctx-size 65536 --cache-type-k q8_0 --cache-type-v q8_0 -fa on"
    - path: "pod/docker-compose.yml"
      provides: "pod legacy/dev com o mesmo conjunto de flags"
      contains: "--ctx-size 65536"
    - path: "gateway/internal/primary/onstart.go"
      provides: "primaryLlamaArgsDefault espelhando supervisord.conf"
      contains: '"--ctx-size", "65536"'
    - path: "gateway/internal/emerg/lifecycle.go"
      provides: "emergencyLlamaArgsDefault com o mesmo conjunto"
      contains: '"--ctx-size", "65536"'
    - path: "gateway/internal/proxy/tokencount.go"
      provides: "guard RES-07 alinhado ao novo teto por slot"
      contains: "ChatContextCap = 32768"
  key_links:
    - from: "gateway/internal/primary/onstart.go"
      to: "pod/primary/supervisord.conf"
      via: "args espelhados (doc comment de onstart.go declara sync 1:1 com supervisord.conf)"
      pattern: "ctx-size.*65536"
    - from: "gateway/internal/proxy/tokencount.go"
      to: "pod/primary/supervisord.conf"
      via: "ChatContextCap == contexto por slot (65536 / -np 2 = 32768)"
      pattern: "32768"
---

<objective>
Dobrar o teto de contexto por slot do pod primario de 16384 para 32768 tokens sem trocar de GPU, quantizando o KV cache para q8_0 e subindo `--ctx-size` de 32768 para 65536 (mantendo `-np 2`), e alinhar o guard `ChatContextCap` do gateway ao novo teto.

**Medicoes ja feitas (2026-08-24, pod Vast de teste, RTX 3090 24.576 MiB, mesma imagem `ghcr.io/ggml-org/llama.cpp:server-cuda`, mesmo GGUF Q4_K_M) — NAO re-medir:**

| config | VRAM | n_ctx por slot | geracao |
|---|---|---|---|
| `--ctx-size 32768` KV f16 (ATUAL) | 21.021 MiB | 16384 | 202 tok/s |
| `--ctx-size 65536 --cache-type-k q8_0 --cache-type-v q8_0 -fa on` | 21.299 MiB | **32768** | 190 tok/s |

Um request de 20.934 tokens — exatamente o tamanho que hoje devolve 400 `exceed_context_size_error` em producao — foi servido com sucesso na config nova (prefill 3.285 tok/s, geracao 65 tok/s). Custo: +278 MiB de VRAM (1,3%) e -6% de geracao em contexto curto.

**`-fa on` e OBRIGATORIO em TODOS os pontos.** Sem ele o llama-server recusa carregar: `llama_init_from_model: quantized V cache requires flash_attn to be enabled` → `failed to create context`. Isso e fatal no boot do pod — um ponto que receba `--cache-type-*` sem `-fa on` produz um pod que morre no boot.

Purpose: analises de chamadas longas (classe ~21k tokens de input) passam a caber no slot em vez de 400ar.
Output: 4 pontos de args identicos entre si + `ChatContextCap = 32768` + docs/runbooks sem valor stale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Arquivos alvo (ja localizados por grep — NAO precisa explorar o codebase):

Os 4 pontos de args do llama-server (precisam ficar IDENTICOS entre si):
- `pod/primary/supervisord.conf:44` — `command=/app/llama-server ...` — **o RUNTIME REAL do pod primario**
- `pod/docker-compose.yml:49` — bloco `command: >` do service `llama` (+ comentario de linha 36 que documenta as flags travadas)
- `gateway/internal/primary/onstart.go:23-30` — `primaryLlamaArgsDefault`
- `gateway/internal/emerg/lifecycle.go:642-651` — `emergencyLlamaArgsDefault`

Guard do gateway:
- `gateway/internal/proxy/tokencount.go:56` — `ChatContextCap = 16384` (+ doc comment nas linhas 48-55)

Docs com o valor hardcoded (varredura ja feita, sao exatamente estes 3):
- `gateway/docs/RUNBOOK-PRIMARY-POD.md:108` — diagrama, linha `--ctx-size 32768 --jinja`
- `gateway/docs/RUNBOOK-EMERGENCY-POD.md:402` — `exec /app/llama-server ... -np 2 --ctx-size 32768 --jinja --chat-template-file <path>`
- `pod/README.md:138` — linha de troubleshooting citando `--ctx-size 32768`

**Achados de grep que economizam trabalho ao executor:**
- NENHUM teste hardcoda o valor numerico de `ChatContextCap`. Todos os consumidores usam o simbolo: `tokencount_test.go` (7 usos, incl. `ChatContextCap+1`), `dispatcher_test.go:300,317`, `cmd/gateway/main.go:1232`. Subir a constante NAO exige editar teste algum.
- `gateway/internal/proxy/audio_duration_test.go:63-64` tem o literal `16384`, mas e contagem de BYTES de um fixture mp3 — NAO tocar.
- Nenhum teste assere o valor numerico de `--ctx-size` nos arg slices. `TestPrimaryLlamaArgsDefault_NoChatTemplateFile` (lifecycle_test.go:662) so checa presenca de `--jinja` e ausencia de `--chat-template-file`; `emerg/lifecycle_test.go:147-149` so checa `exec /app/llama-server`, `--host 0.0.0.0` e `--jinja` como substrings.
- **Gotcha do gate de grep:** o token nu `-fa` da FALSO POSITIVO em `pod/primary/supervisord.conf` (o path do cache HF contem `models--Systran--faster-whisper-...`, que casa a substring `-fa`). O gate por isso casa a string completa: `-fa on` nos artefatos do pod e `"-fa", "on"` no Go. Verificado: ambas as formas tem ZERO ocorrencias pre-mudanca nos 4 arquivos, entao o gate discrimina de verdade. Nao trocar por `grep -- -fa`.
- Existem DOIS budgets de tamanho do onstart que crescem com estas flags: `emerg/lifecycle_test.go:302` exige `len(req.Args[1]) < 2500` e `primary/lifecycle_test.go:214` exige `< 14000`. As flags novas somam ~48 chars — folga larga, mas por isso a suite de `primary` e `emerg` entra no gate, nao so `proxy`.

**Contexto de risco (memoria `llama-ctx-per-slot-32k`):** `--ctx-size` e dividido pelos slots de `-np`. Na ultima mexida nisso, um dos 4 pontos foi esquecido e o fix nao pegou no pod. Por isso a Task 2 termina com um gate de grep que confere os 4 pontos de uma vez, por token.

**FORA DE ESCOPO (nao fazer):** o bug conhecido em `.planning/debug/HANDOFF-tokenizer-fail-open-pod-ctx-16k.md` — o guard `ChatContextCap` hoje e inerte porque tokeniza contra uma URL morta e falha-aberto. NAO corrigir aqui. Nesta task so muda a constante. Tambem NAO fazer deploy (build de imagem / stack Portainer 38) — o orquestrador cuida disso depois.
</context>

<tasks>

<task type="auto">
  <name>Task 1: KV q8_0 + ctx 65536 nos dois arg slices Go e ChatContextCap 32768</name>
  <files>gateway/internal/primary/onstart.go, gateway/internal/emerg/lifecycle.go, gateway/internal/proxy/tokencount.go</files>
  <action>
    1. `gateway/internal/primary/onstart.go` — em `primaryLlamaArgsDefault`, trocar `"--ctx-size", "32768",` por `"--ctx-size", "65536",` e acrescentar, imediatamente depois, os tres pares `"--cache-type-k", "q8_0",` / `"--cache-type-v", "q8_0",` / `"-fa", "on",`. Manter `-np 2`, `-ngl 99` e `--jinja` intocados e manter a ausencia de `--chat-template-file` (invariante B1 embedded LOCKED). No doc comment do var, acrescentar uma nota curta: KV cache quantizado q8_0 dobra o contexto por slot para 32768 (65536 total / -np 2) ao custo de +278 MiB de VRAM e ~6% de geracao (medido 2026-08-24 em 3090 24 GB), e `-fa on` e OBRIGATORIO porque o llama-server recusa carregar V cache quantizado sem flash-attn (`quantized V cache requires flash_attn to be enabled` → `failed to create context`).
    2. `gateway/internal/emerg/lifecycle.go` — mesma alteracao em `emergencyLlamaArgsDefault`: `--ctx-size` 32768 → 65536 e os mesmos tres pares novos, inseridos depois de `--ctx-size` e antes de `--jinja`. O `--chat-template-file /app/templates/qwen3.5-27b-tool-calling.jinja` do final permanece. O doc comment na linha ~636 diz "canonical 13-flag llama-server CLI invocation" — a contagem fica errada com as flags novas; reescrever essa frase para nao citar um numero de flags (ex.: "canonical llama-server CLI invocation"), sem alterar o resto do comentario.
    3. `gateway/internal/proxy/tokencount.go` — mudar `ChatContextCap = 16384` para `ChatContextCap = 32768` e atualizar o doc comment (linhas ~48-55): o cap continua sendo o contexto PER-SLOT do llama-server, mas agora vale 65536 total / -np 2 = 32768 tokens por slot, viabilizado pelo KV cache q8_0 + `-fa on`. Preservar a referencia a RES-07 / CONTEXT.md e a nota historica do OPERACOES-26306 (ela explica por que o cap tem que seguir o per-slot e nao o total). Ajustar tambem o header do package no topo do arquivo, que diz "the chat (16k) and embed (8k BGE-M3) caps" → o chat agora e 32k.
    4. Se `gateway/internal/proxy/errors.go:29` ainda citar o literal `16384` no comentario de `ErrContextLengthExceeded`, atualizar para 32768. NAO mudar comportamento nem assinatura.
    NAO editar consumidores de `ChatContextCap` (dispatcher.go, cmd/gateway/main.go, testes) — eles usam o simbolo. NAO tocar em `EmbedContextCap` (BGE-M3 continua 8192). NAO tocar em `audio_duration_test.go`.
  </action>
  <verify>
    <automated>cd gateway && gofmt -l . | tee /dev/stderr | wc -l | grep -qx 0 && go build ./... && go test ./internal/proxy/... ./internal/primary/... ./internal/emerg/... ./internal/config/... && grep -c 'ChatContextCap = 32768' internal/proxy/tokencount.go | grep -qx 1 && for t in '"--ctx-size", "65536"' '"--cache-type-k", "q8_0"' '"--cache-type-v", "q8_0"' '"-fa", "on"'; do c=$(grep -v '^[[:space:]]*//' internal/primary/onstart.go internal/emerg/lifecycle.go | grep -cF -- "$t"); [ "$c" = 2 ] || { echo "FAIL token $t count=$c want 2"; exit 1; }; done && ! grep -rn -- '"--ctx-size", "32768"' internal/</automated>
  </verify>
  <done>Os dois arg slices Go carregam `--ctx-size 65536` + `--cache-type-k q8_0` + `--cache-type-v q8_0` + `-fa on` (2 ocorrencias de cada token em codigo nao-comentado, zero `--ctx-size 32768` restante em internal/); `ChatContextCap = 32768`; gateway compila, gofmt limpo, suites proxy/primary/emerg/config verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Mesmas flags nos artefatos do pod, docs sem valor stale e gate de paridade dos 4 pontos</name>
  <files>pod/primary/supervisord.conf, pod/docker-compose.yml, gateway/docs/RUNBOOK-PRIMARY-POD.md, gateway/docs/RUNBOOK-EMERGENCY-POD.md, pod/README.md</files>
  <action>
    1. `pod/primary/supervisord.conf:44` — este e o RUNTIME REAL do pod primario; se so ele ficar de fora, a mudanca nao existe no pod. Na linha `command=/app/llama-server ...`, trocar `--ctx-size 32768` por `--ctx-size 65536 --cache-type-k q8_0 --cache-type-v q8_0 -fa on`, preservando na mesma ordem tudo o que ja esta la: `-ngl 99 -np 2` antes e `--split-mode none --main-gpu 0 --jinja` depois. Acrescentar acima do bloco `[program:llama]`, junto do comentario existente do SEED-019, uma nota de 2-3 linhas: KV q8_0 leva o contexto por slot de 16384 para 32768 (65536 total / -np 2), custo medido +278 MiB VRAM e -6% de geracao em 3090 24 GB (2026-08-24), e `-fa on` e obrigatorio senao o llama-server aborta no boot com `quantized V cache requires flash_attn to be enabled`.
    2. `pod/docker-compose.yml:49` — no bloco `command: >` do service `llama`, trocar a linha `--ctx-size 32768` por `--ctx-size 65536` e acrescentar tres linhas logo abaixo, no mesmo recuo do bloco folded: `--cache-type-k q8_0`, `--cache-type-v q8_0`, `-fa on`. Manter `-np 2`, `--jinja`, `--chat-template-file` e `--log-format json`.
    3. `pod/docker-compose.yml:36` — o comentario "Flags locked: -np 2, --ctx-size 32768 (32768 total / -np 2 = 16384/slot — OPERACOES-26306), --jinja, --chat-template-file (D-08, POD-06)" esta stale. Reescrever para o conjunto novo, mantendo o formato: citar `-np 2`, `--ctx-size 65536`, `--cache-type-k/v q8_0`, `-fa on` e o calculo `65536 total / -np 2 = 32768/slot`.
    4. `gateway/docs/RUNBOOK-PRIMARY-POD.md:108` — no diagrama, a linha `--ctx-size 32768 --jinja` vira `--ctx-size 65536 --cache-type-k q8_0 --cache-type-v q8_0 -fa on --jinja` (quebrar em duas linhas do diagrama se necessario para manter o alinhamento ASCII do bloco).
    5. `gateway/docs/RUNBOOK-EMERGENCY-POD.md:402` — atualizar o comando citado para `-ngl 99 -np 2 --ctx-size 65536 --cache-type-k q8_0 --cache-type-v q8_0 -fa on --jinja --chat-template-file <path>`.
    6. `pod/README.md:138` — a linha de troubleshooting cita `--ctx-size 32768` como valor corrente; atualizar para `--ctx-size 65536`. A orientacao da linha (reduzir ctx sob pressao de VRAM, fallback D-09) permanece; so o valor citado muda. A linha 140 nao cita valor — deixar como esta.
    7. Varredura final: confirmar que nao sobrou nenhum `ctx-size 32768` fora de `.planning/` (historico).
    8. Commitar tudo (Task 1 + Task 2) em UM commit convencional na branch atual (`develop`):
       `fix(pod): KV cache q8_0 + --ctx-size 65536 → 32768 tokens por slot`
       Corpo mencionando: -np 2 mantido; `-fa on` obrigatorio (quantized V cache exige flash-attn); ChatContextCap 16384 → 32768 acompanhando o novo teto por slot; medido em 3090 24 GB (+278 MiB VRAM, -6% geracao, request de 20.934 tokens servido); repo-only — rebuild da imagem e redeploy sao passo separado.
    NAO fazer build de imagem, push de imagem, nem mexer em stack Portainer.
  </action>
  <verify>
    <automated>for f in pod/primary/supervisord.conf pod/docker-compose.yml; do for t in '--ctx-size 65536' '--cache-type-k q8_0' '--cache-type-v q8_0' '-fa on'; do grep -v '^[[:space:]]*[#;]' "$f" | grep -qF -- "$t" || { echo "FAIL: $f sem [$t]"; exit 1; }; done; done && for f in gateway/internal/primary/onstart.go gateway/internal/emerg/lifecycle.go; do for t in '"--ctx-size", "65536"' '"--cache-type-k", "q8_0"' '"--cache-type-v", "q8_0"' '"-fa", "on"'; do grep -v '^[[:space:]]*//' "$f" | grep -qF -- "$t" || { echo "FAIL: $f sem [$t]"; exit 1; }; done; done && ! grep -rn 'ctx-size 32768' pod/ gateway/ && ! grep -rn -- '"--ctx-size", "32768"' gateway/ && git log -1 --pretty=%s | grep -q 65536</automated>
  </verify>
  <done>Os 4 pontos de args (supervisord.conf, docker-compose.yml, primaryLlamaArgsDefault, emergencyLlamaArgsDefault) carregam cada um os 4 tokens novos — nenhum ficou com cache-type quantizado sem `-fa on`; zero `ctx-size 32768` fora de `.planning/`; docs e runbooks atualizados; um unico commit convencional na develop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway → pod primario (tier-0 llm) | request de tenant atravessa o guard `ChatContextCap` antes de chegar ao slot do llama-server |
| repo → imagem do pod | `supervisord.conf` e bakeado na imagem em build time; divergencia repo-vs-imagem so aparece no proximo provision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q8-01 | Denial of Service | `pod/primary/supervisord.conf` [program:llama] | mitigate | `--cache-type-*` sem `-fa on` faz o llama-server abortar no boot (`failed to create context`) → pod primario nunca fica Ready → gateway cai para tier-1 pago. Gate da Task 2 exige os 4 tokens presentes em cada um dos 4 pontos, e o gate da Task 1 exige contagem exata 2/2 no lado Go. |
| T-q8-02 | Denial of Service | `gateway/internal/proxy/tokencount.go` | mitigate | Se `ChatContextCap` ficasse em 16384 com o pod servindo 32768/slot, o guard rejeitaria requests que agora cabem. Gate da Task 1 assere `ChatContextCap = 32768` literalmente. |
| T-q8-03 | Tampering | divergencia entre os 4 pontos | mitigate | Precedente registrado (memoria `llama-ctx-per-slot-32k`): a ultima mudanca de ctx esqueceu o `supervisord.conf` e nao pegou no pod. Gate final varre os 4 arquivos por token, ignorando linhas de comentario. |
| T-q8-04 | Denial of Service | VRAM da 3090 24 GB | accept | +278 MiB medidos (21.021 → 21.299 MiB de 24.576 disponiveis, 1,3%). Margem preservada; nao ha instalacao de dependencia nova. |
| T-q8-SC | Tampering | npm/pip/cargo installs | n/a | Nenhum package manager e invocado nesta task — mudanca e puramente de flags e de uma constante. |
</threat_model>

<verification>
- `cd gateway && go build ./... && go test ./internal/proxy/... ./internal/primary/... ./internal/emerg/... ./internal/config/...` verde
- `cd gateway && gofmt -l .` sem saida
- Paridade dos 4 pontos: cada um de `pod/primary/supervisord.conf`, `pod/docker-compose.yml`, `gateway/internal/primary/onstart.go`, `gateway/internal/emerg/lifecycle.go` contem `65536`, `cache-type-k`, `cache-type-v` e `-fa` em linha nao-comentada
- `grep -rn 'ctx-size 32768' pod/ gateway/` → zero hits
- `grep -n 'ChatContextCap = 32768' gateway/internal/proxy/tokencount.go` → 1 hit
- Um unico commit convencional na `develop` cobrindo as duas tasks
</verification>

<success_criteria>
- Os 5 pontos da mudanca estao aplicados: 4 pontos de args identicos entre si + `ChatContextCap = 32768`
- Nenhum ponto tem `--cache-type-*` sem `-fa on`
- Build, gofmt e testes unitarios de proxy/primary/emerg/config verdes
- Docs (2 runbooks + pod/README.md) e o comentario de flags travadas do compose sem valor stale
- Nada de deploy: sem build de imagem, sem push, sem alteracao de stack
</success_criteria>

<output>
Create `.planning/quick/260824-s2t-kv-q8-0-ctx-32k-por-slot-no-pod-primario/260824-s2t-SUMMARY.md` when done
</output>
