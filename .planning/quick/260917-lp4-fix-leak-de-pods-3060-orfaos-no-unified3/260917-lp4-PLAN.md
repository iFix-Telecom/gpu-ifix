---
phase: quick-260917-lp4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ops/vast-3060/unified3060.py
autonomous: true
requirements: [LP4-01, LP4-02, LP4-03]

must_haves:
  truths:
    - "Falha de provision em QUALQUER step destroi a instancia recem-criada (nenhuma GPU paga sobrevive a um provision falho)"
    - "Sweep por label exato destroi instancias orfas do pod unificado que nao sejam a corrente"
    - "Sweep roda no fim do caminho de sucesso do start e no stop"
    - "Sweep nunca destroi a instancia corrente do state nem instancias de outro label (primary 3090)"
    - "Falha do sweep (HTTP != 200 / excecao) nao derruba provision nem stop"
    - "Orfa encontrada gera notify com quantidade, ids e $/h liberado"
  artifacts:
    - path: "ops/vast-3060/unified3060.py"
      provides: "fail() sempre destroi + LABEL + vast_list + select_orphans + sweep_orphans wired em cmd_start/cmd_stop"
      contains: "def select_orphans"
  key_links:
    - from: "ops/vast-3060/unified3060.py:sweep_orphans"
      to: "ops/vast-3060/unified3060.py:vast_destroy"
      via: "chamada direta por id da orfa"
      pattern: "vast_destroy\\(env"
    - from: "ops/vast-3060/unified3060.py:cmd_stop"
      to: "sweep_orphans"
      via: "chamada apos destroy do state"
      pattern: "sweep_orphans\\(env"
---

<objective>
Fechar o leak de pods 3060 orfaos no scheduler `ops/vast-3060/unified3060.py`.

Root cause ja confirmado (NAO re-investigar):
1. `fail(step, inst=None, destroy_new=False)` so destroi a instancia nova quando
   `destroy_new=True`. Os caminhos `speaches health timeout 20min`,
   `validacao STT/TTS: STT direto HTTP 0`, `install`, `infinity/xtts health`,
   `embed/rerank`, `flip stack` chamam `fail()` SEM `destroy_new` -> logam
   "{new_id} preservada p/ diagnostico" e `sys.exit(1)`. Ninguem volta pra
   destruir. GPU paga vive indefinidamente.
2. `cmd_stop` destroi SOMENTE `state.json["instance_id"]`. A orfa nunca entra no
   state (sobrescrito pela instancia boa do retry seguinte), entao sobrevive a
   todo stop das 20:00.

Evidencia medida 2026-09-17: 5 instancias label `stt-tts-rerank-unified` vivas
simultaneas; 4 orfas destruidas a mao (50479985 7,4 dias, 51002316, 51201042,
51203451), $13,01 queimados; burn $0,3814/h -> $0,2036/h pos-limpeza.

Purpose: provision falho e stop deixam custo ZERO de GPU orfa, com alerta quando
o sweep encontra vazamento.
Output: `ops/vast-3060/unified3060.py` patchado (3 mudancas), commitado no repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@ops/vast-3060/unified3060.py
@ops/vast-3060/vast3060.py

<interfaces>
<!-- Contratos JA existentes. Usar estes helpers — NAO criar cliente HTTP novo. -->

De ops/vast-3060/vast3060.py (importado como `v` em unified3060.py):
  http(method, url, headers=None, body=None, timeout=30) -> (int_status, bytes)
      # status 0 = falha de dial/timeout (nunca levanta excecao)
  http_json(method, url, headers=None, payload=None, timeout=30) -> (int_status, dict)
      # dict {"_raw": "..."} quando o corpo nao e JSON
  notify(env, text) -> None            # best-effort WhatsApp, nunca levanta
  load_env() -> dict                   # VAST_API_KEY, PORTAINER_API_KEY, ...
  VAST = "https://console.vast.ai/api/v0"

De ops/vast-3060/unified3060.py (ja existentes, reutilizar):
  VAST = "https://console.vast.ai/api/v0"
  log(m) -> None
  load_state() / save_state(st)        # /var/lib/vast-3060/state.json
  vast_get(env, iid) -> dict | None    # GET /instances/{id}/ -> ["instances"]
  vast_destroy(env, iid) -> int        # DELETE /instances/{id}/ -> HTTP code
  cmd_start(env, resume_id=None), cmd_stop(env)

Shape relevante de GET /instances/ (lista, mesma forma usada por
vast3060.get_instance): {"instances": [ {"id": int, "label": str|None,
"dph_total": float, "actual_status": str, ...}, ... ]}

Label do pod unificado (hoje literal na linha 272 do PUT /asks/): "stt-tts-rerank-unified"
Label do pod PRIMARY do gateway (NAO TOCAR): "ifix-primary-lifecycle-*" (3090,
ciclo de vida gerido pelo gateway em Go) -> por isso o filtro casa label por
IGUALDADE, nunca por substring/prefixo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fail() destroi a nova SEMPRE</name>
  <files>ops/vast-3060/unified3060.py</files>
  <action>
Na closure `fail` dentro de `cmd_start` (~linha 285): remover o parametro
`destroy_new` da assinatura, deixando `fail(step, inst=None)`.

Ordem de operacoes OBRIGATORIA dentro de fail (o diagnostico precisa ser
coletado ANTES do destroy, senao a evidencia morre com a instancia):
  1. `log(f"FALHA em '{step}'")`
  2. `if inst: diag(env, inst)`  — ja existe, dump de logs via ssh pro journal
  3. `c = vast_destroy(env, new_id)` + `log(f"nova {new_id} destruida -> HTTP {c}")`
     — SEM condicional, em todos os caminhos
  4. machine -> `st["machine_avoid"]` + `save_state(st)` (bloco atual, inalterado)
  5. `v.notify(...)` com texto novo: dizer que a nova foi DESTRUIDA e que o
     diagnostico esta no journal/log — remover por completo o trecho
     `f'{new_id} preservada p/ diagnostico'` e o ternario que o produz.
  6. `sys.exit(1)` (inalterado — o loop de 3 tentativas em `__main__` depende
     de exit code 1 pra re-tentar)

Ajustar as 2 chamadas que hoje passam o kwarg: `fail("boot timeout 15min", inst,
destroy_new=True)` -> `fail("boot timeout 15min", inst)`. As outras chamadas
(`speaches health timeout 20min`, `install {m}`, `infinity health timeout 30min`,
`xtts health timeout 40min`, `xtts speech HTTP`, `validacao STT/TTS`, `embed`,
`rerank`, `flip stack`) ficam com a mesma forma que ja tem — passam a destruir
por default.

Atualizar a docstring de `fail` para refletir o comportamento novo (ex: "Falha de
provision: diagnostica via journal e SEMPRE destroi a nova (leak de GPU paga era
o bug 2026-09-17: 4 orfas / $13,01). Machine -> avoid.").

NAO tocar no caminho de `edge_ok == False` (~linha 401): ali o flip ja foi feito,
a nova E a instancia corrente, `sys.exit(2)` e decisao humana de rollback — esse
caminho persiste `instance_id=new_id` no state e NAO e um leak.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && python3 -m py_compile ops/vast-3060/unified3060.py && echo OK-COMPILE && test "$(grep -v '^\s*#' ops/vast-3060/unified3060.py | grep -c 'destroy_new')" = "0" && echo OK-NO-DESTROY-NEW && test "$(grep -v '^\s*#' ops/vast-3060/unified3060.py | grep -c 'preservada p/ diagnostico')" = "0" && echo OK-NO-PRESERVA && test "$(grep -c 'return fail(' ops/vast-3060/unified3060.py)" -ge "9" && echo OK-FAIL-CALLS</automated>
  </verify>
  <done>`python3 -m py_compile` limpo; zero ocorrencias de `destroy_new` e de "preservada p/ diagnostico" fora de comentario; >=9 chamadas `return fail(` presentes; `vast_destroy(env, new_id)` incondicional depois do `diag()` dentro de `fail`; caminho `exit(2)` do edge intacto.</done>
</task>

<task type="auto">
  <name>Task 2: sweep/reconciliacao por label exato + alerta</name>
  <files>ops/vast-3060/unified3060.py</files>
  <action>
Adicionar constante e 3 funcoes de modulo (stdlib apenas, reusando `v.http_json`
e `vast_destroy`), e substituir o literal do label no PUT /asks/ pela constante.

1. `LABEL = "stt-tts-rerank-unified"` junto das outras constantes de topo
   (STACK / VAST / DISK_GB). Trocar `"label": "stt-tts-rerank-unified"` no payload
   do PUT `/asks/` por `"label": LABEL`.

2. `vast_list(env)` -> `list | None`: `GET {VAST}/instances/` via
   `v.http_json` com `{"Authorization": f"Bearer {env['VAST_API_KEY']}"}`,
   timeout=30. Retorna `None` se status != 200 (sinal de "nao sei, nao mexe"),
   senao `data.get("instances") or []`.

3. `select_orphans(instances, label=LABEL, keep_id=None)` -> `list` — funcao PURA
   (sem I/O, testavel com fixtures):
   - ignora item cujo `(i.get("label") or "") != label` (IGUALDADE EXATA —
     e isso que protege o primary `ifix-primary-lifecycle-*`; NUNCA usar
     `in`, `startswith` ou substring);
   - ignora item sem `id`;
   - ignora `int(i["id"]) == int(keep_id)` quando `keep_id` nao e None
     (protege a instancia corrente do state);
   - retorna a lista dos dicts restantes.

4. `sweep_orphans(env, keep_id=None, context="")` -> `list` de ids destruidos.
   BEST-EFFORT integral: TODO o corpo dentro de `try/except Exception` que apenas
   loga (`log(f"sweep({context}): excecao {e} — best-effort")`) e retorna `[]`.
   Fluxo: `vast_list` -> se `None`, log "listagem HTTP != 200, sweep pulado" e
   retorna `[]` (NAO notifica, pra nao virar spam num blip de API) -> `select_orphans`
   -> se vazio, `log(f"sweep({context}): nenhuma orfa")` e retorna `[]` -> para cada
   orfa `vast_destroy(env, id)` + log do id/machine/dph/HTTP -> somar
   `dph_total` das destruidas -> `v.notify(env, f"pod 3060 sweep ({context}): "
   f"{n} orfa(s) destruida(s) ids={ids} — liberado ${total:.4f}/h")`.
   O notify SO dispara quando `select_orphans` retornou >= 1 (orfa encontrada).

5. Wiring em DOIS pontos:
   - `cmd_start`, no FIM do caminho de sucesso: depois do `if old_id:
     vast_destroy(...)`, do `st.update(instance_id=new_id, ...)` + `save_state(st)`
     e do `v.notify` de "pod 3060 UP" — ou seja, quando o id bom ja esta
     persistido. Chamar `sweep_orphans(env, keep_id=new_id, context="start")`.
     NUNCA antes do flip/validacao (id bom ainda indefinido).
   - `cmd_stop`: manter o destroy do `state["instance_id"]` e o
     `st.update(instance_id=None)` + `save_state(st)` como estao; depois chamar
     `sweep_orphans(env, keep_id=None, context="stop")` — no stop o alvo e
     destruir TUDO do label, sem excecao. O sweep TAMBEM deve rodar quando o
     state esta vazio (hoje `cmd_stop` faz `return` cedo em `if not iid` — trocar
     esse early-return por um log + seguir pro sweep, que e exatamente o caso em
     que a orfa sobrevivia).

Docstring do modulo: acrescentar uma linha no bloco `Uso:` registrando que start
e stop reconciliam por label (sweep de orfas) desde 2026-09-17.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && python3 -m py_compile ops/vast-3060/unified3060.py && python3 -c "
import sys; sys.path.insert(0,'ops/vast-3060')
import unified3060 as u
insts=[{'id':1,'label':'stt-tts-rerank-unified','dph_total':0.05},{'id':2,'label':'stt-tts-rerank-unified','dph_total':0.06},{'id':3,'label':'ifix-primary-lifecycle-42','dph_total':0.9},{'id':4,'label':'stt-tts-3060-auto','dph_total':0.04},{'id':5,'label':None,'dph_total':0.01},{'id':6,'label':'stt-tts-rerank-unified-old','dph_total':0.05},{'label':'stt-tts-rerank-unified','dph_total':0.05}]
a=[o['id'] for o in u.select_orphans(insts, keep_id=1)]
b=sorted(o['id'] for o in u.select_orphans(insts, keep_id=None))
assert a==[2], a
assert b==[1,2], b
assert u.select_orphans([], keep_id=None)==[]
assert u.LABEL=='stt-tts-rerank-unified'
print('OK-FILTER', a, b)
" && test "$(grep -c 'sweep_orphans(env' ops/vast-3060/unified3060.py)" = "2" && echo OK-WIRED-2X && grep -q '\"label\": LABEL' ops/vast-3060/unified3060.py && echo OK-LABEL-CONST</automated>
  </verify>
  <done>py_compile limpo; dry-check passa: com `keep_id=1` so o id 2 e orfa (corrente 1 preservado, primary `ifix-primary-lifecycle-42` preservado, label legado `stt-tts-3060-auto` preservado, label None preservado, `stt-tts-rerank-unified-old` preservado por igualdade exata, item sem id ignorado) e com `keep_id=None` saem 1 e 2; `sweep_orphans(env` chamado exatamente 2x (start + stop); payload do create usa `LABEL`; NENHUMA chamada a API Vast real e nenhum destroy executado na verificacao.</done>
</task>

<task type="auto">
  <name>Task 3: dry-check de resiliencia do sweep + registro no SUMMARY</name>
  <files>ops/vast-3060/unified3060.py, .planning/quick/260917-lp4-fix-leak-de-pods-3060-orfaos-no-unified3/260917-lp4-SUMMARY.md</files>
  <action>
Provar por dry-check (com stubs em memoria, SEM rede) que `sweep_orphans` e
best-effort e que o wiring nao derruba nem provision nem stop, ajustando o codigo
se algum caso quebrar:
  - `vast_list` devolvendo `None` (HTTP != 200) -> `sweep_orphans` retorna `[]`,
    zero chamadas de destroy, zero notify, sem excecao propagada;
  - `vast_list` levantando excecao -> mesmo resultado (capturado, log, `[]`);
  - `vast_destroy` levantando excecao no meio da lista -> nao propaga.
Stub via monkeypatch dos atributos do modulo (`u.vast_list = ...`,
`u.vast_destroy = ...`, `u.v.notify = ...`) num snippet `python3 -c`, contando
chamadas em lista local. NAO tocar na API Vast e NAO destruir nada.

Escrever o SUMMARY do quick registrando:
  - as 3 mudancas e o porque (com a evidencia: 4 orfas / $13,01 / burn
    $0,3814 -> $0,2036/h);
  - por que o filtro e por IGUALDADE de label (protege o primary 3090 do gateway);
  - por que o sweep do start roda depois do state salvo (id bom definido);
  - **PASSO MANUAL DE DEPLOY PENDENTE** (nao executado nesta task): copiar
    `ops/vast-3060/unified3060.py` do repo para `/opt/vast-3060/unified3060.py`
    no host que roda os timers, sem mexer em systemd; primeira validacao real =
    proximo `start` (07:00) e proximo `stop` (20:00), conferindo no log a linha
    `sweep(start)`/`sweep(stop)`.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && python3 -c "
import sys; sys.path.insert(0,'ops/vast-3060')
import unified3060 as u
calls=[]; notes=[]
u.v.notify=lambda env,t: notes.append(t)
u.vast_destroy=lambda env,i: calls.append(i) or 200
u.vast_list=lambda env: None
assert u.sweep_orphans({'VAST_API_KEY':'x'}, keep_id=1, context='t')==[]
def boom(env): raise RuntimeError('api down')
u.vast_list=boom
assert u.sweep_orphans({'VAST_API_KEY':'x'}, keep_id=None, context='t')==[]
assert calls==[] and notes==[], (calls,notes)
u.vast_list=lambda env:[{'id':9,'label':u.LABEL,'dph_total':0.05}]
def bad(env,i): raise RuntimeError('delete exploded')
u.vast_destroy=bad
u.sweep_orphans({'VAST_API_KEY':'x'}, keep_id=None, context='t')
print('OK-BEST-EFFORT')
" && test -f .planning/quick/260917-lp4-fix-leak-de-pods-3060-orfaos-no-unified3/260917-lp4-SUMMARY.md && grep -qi 'opt/vast-3060' .planning/quick/260917-lp4-fix-leak-de-pods-3060-orfaos-no-unified3/260917-lp4-SUMMARY.md && echo OK-SUMMARY</automated>
  </verify>
  <done>Dry-check imprime OK-BEST-EFFORT: listagem None, excecao na listagem e excecao no destroy todas resultam em `[]`/retorno sem propagar, com zero destroy e zero notify nos dois primeiros casos. SUMMARY existe e documenta o deploy manual pendente para `/opt/vast-3060/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unified3060.py -> API Vast (DELETE /instances/{id}/) | operacao destrutiva e irreversivel sobre recurso pago |
| resposta da API Vast -> logica de selecao | `label`/`id` vem de fonte externa e decidem o que sera destruido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-LP4-01 | Denial of Service | sweep_orphans -> vast_destroy | mitigate | filtro por IGUALDADE de label (`!= LABEL` descarta) + `keep_id` exclui a corrente; dry-check da Task 2 prova que `ifix-primary-lifecycle-42`, `stt-tts-3060-auto` e label None sobrevivem |
| T-LP4-02 | Denial of Service | cmd_start / cmd_stop | mitigate | sweep 100% best-effort (try/except + retorno `[]`); falha de listagem/destroy nunca propaga — provado na Task 3 |
| T-LP4-03 | Tampering | resposta JSON da API Vast | mitigate | so `label` (igualdade) e `id` (int) sao usados como criterio; nenhum campo externo vira comando/caminho; `dph_total` so entra em texto de notify |
| T-LP4-04 | Information disclosure | v.notify (WhatsApp) | accept | notify carrega apenas ids de instancia e $/h; nenhum segredo (VAST_API_KEY nunca entra em log/notify) |
| T-LP4-SC | Tampering | npm/pip/cargo installs | n/a | zero dependencia nova — stdlib Python 3 apenas |
</threat_model>

<verification>
1. `python3 -m py_compile ops/vast-3060/unified3060.py` limpo.
2. Dry-check de filtro (Task 2) verde com fixtures fixas — provando protecao da
   corrente, do primary `ifix-primary-lifecycle-*` e do label legado.
3. Dry-check de best-effort (Task 3) verde — sweep nao derruba start/stop.
4. `grep -v '^\s*#' ops/vast-3060/unified3060.py | grep -c destroy_new` = 0.
5. `grep -c 'sweep_orphans(env' ops/vast-3060/unified3060.py` = 2.
6. `git diff` revisado: mudancas restritas a `ops/vast-3060/unified3060.py`;
   nenhuma chamada real a API Vast durante a execucao; nenhuma instancia
   destruida durante a execucao; nada de systemd/deploy tocado.
</verification>

<success_criteria>
- Nenhum caminho de falha de provision termina com instancia nova viva.
- Sweep por label exato roda no fim do start (preservando `new_id`) e no stop
  (destruindo tudo do label), inclusive quando o state esta vazio.
- Orfa encontrada gera notify com quantidade, ids e $/h liberado.
- Falha do sweep e inofensiva (best-effort provado).
- Zero dependencia nova; stdlib Python 3.
- Deploy para `/opt/vast-3060/` documentado como pendente manual no SUMMARY.
</success_criteria>

<output>
Create `.planning/quick/260917-lp4-fix-leak-de-pods-3060-orfaos-no-unified3/260917-lp4-SUMMARY.md` when done
</output>
