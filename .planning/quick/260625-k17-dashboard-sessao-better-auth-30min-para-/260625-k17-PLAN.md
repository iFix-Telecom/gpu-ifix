---
phase: quick-260625-k17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/lib/auth.ts
  - dashboard/src/app/2fa/challenge/page.tsx
autonomous: true
requirements: [QUICK-260625-k17]
must_haves:
  truths:
    - "Sessão better-auth dura 7 dias (não mais 30min)"
    - "cookieCache.maxAge casa com expiresIn (7 dias) — middleware Edge não bounce mid-session"
    - "verifyTotp marca trustDevice → 2FA pulado por ~30 dias no mesmo navegador"
    - "dashboard build Next standalone passa"
  artifacts:
    - path: "dashboard/src/lib/auth.ts"
      provides: "session.expiresIn=7d, cookieCache.maxAge=7d, comentários WR-01/D-15 atualizados"
      contains: "7 * 24 * 60 * 60"
    - path: "dashboard/src/app/2fa/challenge/page.tsx"
      provides: "verifyTotp com trustDevice:true"
      contains: "trustDevice: true"
  key_links:
    - from: "dashboard/src/lib/auth.ts session.cookieCache.maxAge"
      to: "session.expiresIn"
      via: "ambos = 7 * 24 * 60 * 60"
      pattern: "maxAge: 7 \\* 24 \\* 60 \\* 60"
---

<objective>
Aumentar TTL de sessão do dashboard `ifix-ai-dashboard` de 30min para 7 dias e adicionar trust-device de ~30 dias no fluxo 2FA. Para o bounce indesejado para `/2fa/challenge` e o relogin+2FA repetido.

Purpose: O dashboard só bate em `/api/gateway` (nunca `/api/auth`), então o uso normal não reheata cookieCache nem renova a sessão. Com expiresIn=30min a sessão expira mid-use, o middleware Edge faz fallback pessimista e bounce para `/2fa/challenge`. Decisão do usuário: sessão 7 dias + trust-device 30 dias.

Output: 2 arquivos editados + build local verde. Deploy fica fora deste plano (orquestrador faz pós-merge).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@dashboard/src/lib/auth.ts
@dashboard/src/app/2fa/challenge/page.tsx

<interfaces>
<!-- Estado atual confirmado por leitura direta (não re-explorar). -->

dashboard/src/lib/auth.ts — bloco session (linhas ~71-102):
- expiresIn: 30 * 60
- updateAge: 5 * 60            (MANTER — sliding refresh ok)
- cookieCache: { enabled: true, maxAge: 1800 }
- additionalFields.twoFactorVerified (MANTER intacto)
Comentários D-15 (linhas 65-70) e WR-01 (linhas 74-92) descrevem o contrato cookie-claim e o tradeoff de revogação — precisam refletir 7 dias.

dashboard/src/app/2fa/challenge/page.tsx — handleVerify (linha 52):
- const res = await authClient.twoFactor.verifyTotp({ code });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sessão 7 dias + trustDevice no 2FA</name>
  <files>dashboard/src/lib/auth.ts, dashboard/src/app/2fa/challenge/page.tsx</files>
  <action>
Em `dashboard/src/lib/auth.ts`, bloco `session:` (~linha 71-93):
- Trocar `expiresIn: 30 * 60` por `expiresIn: 7 * 24 * 60 * 60` (7 dias).
- Trocar `cookieCache: { enabled: true, maxAge: 1800 }` por `cookieCache: { enabled: true, maxAge: 7 * 24 * 60 * 60 }` — DEVE casar com expiresIn. O middleware Edge lê `twoFactorVerified` do cookieCache; se maxAge < expiresIn ele bounce para `/2fa/challenge` no meio da sessão (exatamente o bug do WR-01).
- MANTER `updateAge: 5 * 60` (sliding refresh) e o bloco `additionalFields.twoFactorVerified` inalterados.
- Atualizar os comentários inline D-15 (linhas ~65-70) e WR-01 (linhas ~74-92) para refletir 7 dias em vez de 30 min: trocar as menções a "30-min"/"30 minutes"/"1800"/"60s" pelo novo valor de 7 dias, e reescrever o tradeoff de revogação — a revogação de sessão/2FA (DELETE FROM session / reset TOTP) NÃO propaga às decisões do middleware Edge por até 7 dias (era até 30min). Acrescentar que para revogação imediata o operador deve deletar a row em `public.session` no banco. Manter as referências aos runbooks (RUNBOOK-INCIDENTS.md class 4 + RUNBOOK-2FA-RECOVERY.md), ajustando a janela de espera para 7 dias.

Em `dashboard/src/app/2fa/challenge/page.tsx` (~linha 52):
- Trocar `authClient.twoFactor.verifyTotp({ code })` por `authClient.twoFactor.verifyTotp({ code, trustDevice: true })`. O `trustDevice` do plugin twoFactor do better-auth marca o dispositivo confiável (default ~30 dias), pulando o TOTP em logins futuros nesse navegador — casa com a decisão de 30 dias.

NÃO tocar em `dashboard/src/middleware.ts` (lógica de bounce já correta; o problema era só o TTL curto). NÃO mexer no enroll/backup.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/dashboard && grep -n "7 \* 24 \* 60 \* 60" src/lib/auth.ts | grep -c . | grep -qx 2 && grep -q "trustDevice: true" src/app/2fa/challenge/page.tsx && echo OK</automated>
  </verify>
  <done>auth.ts tem expiresIn e cookieCache.maxAge ambos = 7*24*60*60; comentários WR-01/D-15 mencionam 7 dias e revogação via DELETE session; challenge/page.tsx passa trustDevice:true.</done>
</task>

<task type="auto">
  <name>Task 2: Build local do dashboard</name>
  <files>dashboard/</files>
  <action>
Rodar o build Next standalone do dashboard para garantir que os edits compilam e tipam. Se houver script de lint/typecheck no package.json, rodar também. NÃO rodar deploy (SSH/docker build na VM) — fora do escopo; o orquestrador deploia pós-merge.
  </action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/dashboard && npm run build</automated>
  </verify>
  <done>`npm run build` conclui sem erro de compilação/tipo.</done>
</task>

</tasks>

<verification>
- `grep -n "7 \* 24 \* 60 \* 60" dashboard/src/lib/auth.ts` retorna 2 ocorrências (expiresIn + cookieCache.maxAge).
- `grep -q "trustDevice: true" dashboard/src/app/2fa/challenge/page.tsx`.
- `cd dashboard && npm run build` passa.
- `dashboard/src/middleware.ts` inalterado (git diff vazio para esse arquivo).
</verification>

<success_criteria>
- Sessão configurada para 7 dias com cookieCache casado (sem bounce mid-session).
- 2FA confia no dispositivo por ~30 dias via trustDevice.
- Comentários WR-01/D-15 refletem 7 dias e o novo procedimento de revogação imediata.
- Build local verde. Deploy deliberadamente fora deste plano.
</success_criteria>

<output>
Create `.planning/quick/260625-k17-dashboard-sessao-better-auth-30min-para-/260625-k17-SUMMARY.md` when done
</output>
